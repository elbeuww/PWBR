---
phase: 15-design-system-v3-dark-neon-unique
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/components/LanguageSwitcher.tsx
  - apps/web/src/app/[locale]/affiliation/dashboard/page.tsx
  - apps/web/src/app/(admin)/signaux/page.tsx
  - apps/web/src/app/(admin)/page.tsx
  - apps/web/src/app/(admin)/sante/page.tsx
  - apps/web/src/app/(admin)/affiliation/page.tsx
  - apps/web/src/app/(admin)/affiliation/payouts/page.tsx
  - apps/web/src/app/(admin)/file/page.tsx
  - apps/web/src/components/track-record/TrackRecordView.tsx
autonomous: true
requirements: [THEME-02]
user_setup: []

must_haves:
  truths:
    - "Shared/interactive components render colors via semantic tokens, never raw hex or raw Tailwind palette utilities"
    - "The hardcoded ring-[#2563EB] focus ring is replaced by the --ring token everywhere it remained"
    - "Admin / affiliation / track-record surfaces route amber/emerald/red palette utilities through semantic tokens (signal/risk/destructive)"
    - "Glow (box-shadow) stays distinct from focus-visible:ring-* (no Tailwind utility collision)"
  artifacts:
    - path: "apps/web/src/components/LanguageSwitcher.tsx"
      provides: "Token-based focus ring (ring-ring) + bg-popover dropdown"
      contains: "ring-ring"
    - path: "apps/web/src/app/(admin)/sante/page.tsx"
      provides: "Tokenized status badges (signal/destructive)"
      contains: "bg-destructive"
  key_links:
    - from: "apps/web/src/components/LanguageSwitcher.tsx"
      to: "--ring token (--primary green)"
      via: "focus-visible:ring-ring utility"
      pattern: "ring-ring"
    - from: "admin / affiliation / track-record surfaces"
      to: "--signal-bullish / --risk-moderate / --destructive tokens"
      via: "arbitrary-value Tailwind utilities text-[--signal-bullish] / bg-destructive"
      pattern: "signal-bullish|risk-moderate|destructive"
---

<objective>
Eliminate the residual bespoke palette and the hardcoded institutional-blue focus ring so success criterion #2 (THEME-02) is provable: every touched foundation surface renders via semantic tokens with zero raw palette utility and zero Tailwind `ring` collision.

Purpose: this is the cleanup half of THEME-02. It runs in parallel with Plan 02 (disjoint files — Plan 02 owns globals.css/layout/messages, this plan owns LanguageSwitcher + admin/affiliation/track-record). Together they turn the Plan 01 scan test GREEN.

Output: a token-pure foundation surface set, ready for the Phase 16 reskin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md

<interfaces>
<!-- Exact raw→token replacement map (15-PATTERNS.md §"Bespoke palette utilities" + §"LanguageSwitcher"). -->
Semantic tokens available (from globals.css, exposed to Tailwind v4 arbitrary values):
  --ring (= primary green) → `ring-ring`
  --signal-bullish → `text-[--signal-bullish]`, `bg-[--signal-bullish]/10`, `border-[--signal-bullish]/30`
  --signal-bearish / --destructive → `text-destructive`, `bg-destructive`, `bg-destructive/10`, `border-destructive/30`
  --risk-moderate → `text-[--risk-moderate]`, `bg-[--risk-moderate]/10`, `border-[--risk-moderate]/30`
  --popover / --border → `bg-popover`, `border-border`

Replacement map (raw → token):
  text-emerald-700 dark:text-emerald-400  → text-[--signal-bullish]
  text-red-700 dark:text-red-400          → text-destructive
  text-amber-700 dark:text-amber-400      → text-[--risk-moderate]
  bg-emerald-500/10 border-emerald-600/30 → bg-[--signal-bullish]/10 border-[--signal-bullish]/30
  bg-red-500/10 border-red-600/30         → bg-destructive/10 border-destructive/30
  bg-amber-500/10 border-amber-600/30     → bg-[--risk-moderate]/10 border-[--risk-moderate]/30
  bg-red-500 (dot, no opacity)            → bg-destructive
  ring-[#2563EB]                          → ring-ring
  bg-white (LanguageSwitcher dropdown)    → bg-popover ; border-black/10 → border-border

Exact locations (15-PATTERNS.md):
  LanguageSwitcher.tsx: line 168 (trigger ring), line 200 (list item ring), line 183 (dropdown bg-white + border-black/10)
  affiliation/dashboard/page.tsx: line 203 (amber)
  (admin)/signaux/page.tsx: line 197 (emerald badge)
  (admin)/page.tsx: line 56 (red dot)
  (admin)/sante/page.tsx: lines 67 (red dot), 217 (emerald badge), 221 (red badge)
  (admin)/affiliation/page.tsx: line 101 (amber badge)
  (admin)/affiliation/payouts/page.tsx: line 130 (amber)
  (admin)/file/page.tsx: line 121 (amber)
  track-record/TrackRecordView.tsx: line 218 (emerald)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize focus ring + dropdown in LanguageSwitcher</name>
  <files>apps/web/src/components/LanguageSwitcher.tsx</files>
  <read_first>
    - apps/web/src/components/LanguageSwitcher.tsx (read lines 160–210 — trigger button, dropdown listbox, list items)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"LanguageSwitcher.tsx" (before/after for lines 168, 183, 200)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md §"Color guardrails" (ring collision note) and §"Accent reserved for" (focus rings = --ring)
  </read_first>
  <action>Three replacements, preserving all other classes and the `min-h-11 min-w-11` touch targets and logical-property classes (RTL): (1) line 168 trigger button — replace `focus-visible:ring-[#2563EB]` with `focus-visible:ring-ring`. (2) line 200 list item — replace `focus-visible:ring-[#2563EB]` with `focus-visible:ring-ring` (keep `focus-visible:ring-inset` and `data-[active=true]:font-semibold`). (3) line 183 dropdown listbox — replace `bg-white` with `bg-popover` and `border-black/10` with `border-border`. Do NOT introduce any `ring-`+glow combination on the same element (glow is box-shadow only, never a ring utility — UI-SPEC collision guard). No other change to this file.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/theme-scan.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: LanguageSwitcher.tsx contains `focus-visible:ring-ring` (x2) and NO `ring-[#2563EB]`.
    - Source assertion: the dropdown uses `bg-popover` and `border-border`; no `bg-white` / `border-black/10` remain in this file.
    - Source assertion: `min-h-11 min-w-11` touch target and logical-property classes (`end-0`, `text-start`) preserved.
    - Behavior assertion: theme-scan.test.ts Test 1 (ring) no longer fails on LanguageSwitcher (count contribution = 0).
  </acceptance_criteria>
  <done>LanguageSwitcher focus ring + dropdown route through --ring/--popover/--border tokens; no hardcoded hex or white.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize admin palette utilities (5 files)</name>
  <files>apps/web/src/app/(admin)/signaux/page.tsx, apps/web/src/app/(admin)/page.tsx, apps/web/src/app/(admin)/sante/page.tsx, apps/web/src/app/(admin)/affiliation/page.tsx, apps/web/src/app/(admin)/file/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/signaux/page.tsx (line ~197), (admin)/page.tsx (line ~56), (admin)/sante/page.tsx (lines ~67, 217, 221), (admin)/affiliation/page.tsx (line ~101), (admin)/file/page.tsx (line ~121)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"File-by-file locations" (exact before/after per file)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md §Color (token roles: signal vs risk vs destructive)
  </read_first>
  <action>Apply the raw→token replacement map from the interfaces block at each listed location, preserving every surrounding class and the component structure (these are status badges and indicator dots). signaux line 197: emerald badge → `border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]`. (admin)/page.tsx line 56: `red: 'bg-red-500'` → `red: 'bg-destructive'` (status-dot color map value). sante line 67: `red: 'bg-red-500'` → `red: 'bg-destructive'`; line 217 emerald badge → `border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]`; line 221 red badge → `border-destructive/30 bg-destructive/10 text-destructive`. affiliation line 101: amber badge → `border-[--risk-moderate]/30 bg-[--risk-moderate]/10 text-[--risk-moderate]`. file line 121: same amber → `--risk-moderate` triple. Map every `text-emerald-* dark:text-emerald-*` to a single `text-[--signal-bullish]` (the token is theme-frozen now, the `dark:` variant is redundant), every `text-red-* dark:text-red-*` to `text-destructive`, every `text-amber-* dark:text-amber-*` to `text-[--risk-moderate]`. Do not alter copy, data, gating, or RLS calls in these pages.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/theme-scan.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: none of the 5 admin files contains `text-amber-700`, `text-emerald-700`, `text-red-700`, `text-amber-400`, `text-emerald-400`, `text-red-400`, `bg-emerald-500/10`, `bg-red-500/10`, `bg-red-500`, `bg-amber-500/10`, `border-emerald-600/30`, `border-red-600/30`, or `border-amber-600/30`.
    - Source assertion: replacements use `--signal-bullish`, `--risk-moderate`, and `destructive` tokens per the role mapping (bull→signal, risk→risk-moderate, error/loss→destructive).
    - Behavior assertion: theme-scan.test.ts Test 2 reports 0 forbidden-palette matches in these 5 files.
    - No change to data-fetch, gating, or RLS logic (grep for `createClient`/`requireSuperadmin`/`has_active_subscription` unchanged in diff).
  </acceptance_criteria>
  <done>All 5 admin surfaces route status colors through semantic tokens; zero raw palette utility remains.</done>
</task>

<task type="auto">
  <name>Task 3: Tokenize affiliation dashboard + track-record</name>
  <files>apps/web/src/app/[locale]/affiliation/dashboard/page.tsx, apps/web/src/app/(admin)/affiliation/payouts/page.tsx, apps/web/src/components/track-record/TrackRecordView.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/affiliation/dashboard/page.tsx (line ~203), (admin)/affiliation/payouts/page.tsx (line ~130), track-record/TrackRecordView.tsx (line ~218)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"File-by-file locations" (affiliation/dashboard line 203, payouts line 130, TrackRecordView line 218)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md §Color (signal vs risk role)
  </read_first>
  <action>affiliation/dashboard/page.tsx line 203: `text-amber-700 dark:text-amber-400` → `text-[--risk-moderate]` (preserve `text-2xl font-semibold`). (admin)/affiliation/payouts/page.tsx line 130: amber badge → `border-[--risk-moderate]/30 bg-[--risk-moderate]/10 text-[--risk-moderate]`. TrackRecordView.tsx line 218: the conditional `'text-emerald-700 dark:text-emerald-400'` branch → `'text-[--signal-bullish]'` (keep the conditional structure and the opposite branch unchanged unless it is also a raw red palette utility, in which case map it to `text-destructive`). Do NOT alter the measured-% logic (VITR-03) or any provenance/threshold rendering in TrackRecordView — only the color class.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/theme-scan.test.ts apps/web/test/no-perf-claims.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the 3 files contain no `text-amber-700`, `text-emerald-700`, `text-amber-400`, `text-emerald-400`, `bg-amber-500/10`, or `border-amber-600/30`; they use `--risk-moderate` / `--signal-bullish` tokens.
    - Behavior assertion: theme-scan.test.ts Test 2 reports 0 forbidden-palette matches across all 8 foundation files (combined with Tasks 1+2 this turns the whole scan GREEN once Plan 02's ThemeToggle deletion also lands).
    - Behavior assertion: no-perf-claims.test.ts stays green (TrackRecordView measured-% logic untouched).
    - Source assertion: TrackRecordView provenance/threshold logic unchanged in the diff (only the color class string changed).
  </acceptance_criteria>
  <done>affiliation dashboard, payouts, and track-record route their status colors through semantic tokens; measured-% guardrail intact; full THEME-02 scan satisfied alongside Plans 01+02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only class swaps on admin/member surfaces. Gating (`is_superadmin()`/RLS) and data fetch are NOT touched by this plan; the trust boundaries they enforce are preserved verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | accidental edit to admin gating/RLS while changing classes | mitigate | Actions restrict edits to color class strings; acceptance criteria assert createClient/gating/measured-% logic unchanged in diff. |
| T-15-05 | Information Disclosure | re-introducing a non-measured % while editing TrackRecordView | mitigate | no-perf-claims.test.ts re-run as a gate (Task 3); only the color class changes. |
| T-15-SC | Tampering | npm installs | accept | No installs in this plan. No legitimacy gate triggered. |

Surface is presentation-only; gating/RLS untouched. Block-on severity: none expected.
</threat_model>

<verification>
- theme-scan.test.ts: ring + palette matches drop to 0 across the 8 foundation files (full GREEN once Plan 02 deletes ThemeToggle too).
- no-perf-claims.test.ts green (TrackRecordView measured-% logic intact).
- Admin gating/RLS/data-fetch unchanged in diff.
</verification>

<success_criteria>
- Zero `ring-[#2563EB]` and zero raw amber/emerald/red palette utility across LanguageSwitcher + the 8 admin/affiliation/track-record surfaces (THEME-02).
- Status colors route through `--signal-bullish` / `--risk-moderate` / `--destructive` per role; glow stays box-shadow (no ring collision).
- No change to gating, RLS, data fetch, or the measured-% guardrail.
</success_criteria>

<output>
Create `.planning/phases/15-design-system-v3-dark-n-on-unique/15-03-SUMMARY.md` when done.
</output>
