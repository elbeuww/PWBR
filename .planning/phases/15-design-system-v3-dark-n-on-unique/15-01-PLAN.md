---
phase: 15-design-system-v3-dark-neon-unique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/__tests__/contrast-aa.test.ts
  - apps/web/src/styles/__tests__/theme-scan.test.ts
autonomous: true
requirements: [THEME-02, THEME-05]
decisions_covered: [D-02, D-08, D-09, D-10]
user_setup: []

must_haves:
  truths:
    - "D-09: A contrast test proves WCAG AA on opaque dark pairs (primary/bg 11.39:1, text/bg 18.93:1, muted/bg 6.76:1) anchored on the D-02 frozen GREEN values"
    - "D-10: The contrast test also proves AA on composited translucent surfaces (rgba over #070b08), measured on the flattened color, not the token alpha alone"
    - "D-08: A scan proves zero bespoke CSS per page and zero Tailwind utility collision — no hardcoded ring-[#2563EB] and no raw palette utility (text-amber-700, text-emerald-700, bg-red-500, etc.) across the touched foundation surfaces"
    - "D-02: The contrast and scan gates are anchored on the frozen GREEN :root values (background #070b08, primary oklch(0.84 0.18 150)) so they measure the real promoted palette"
  artifacts:
    - path: "apps/web/src/styles/__tests__/contrast-aa.test.ts"
      provides: "WCAG AA contrast assertions for opaque + composited GREEN dark pairs (THEME-05)"
      contains: "11.39"
    - path: "apps/web/src/styles/__tests__/theme-scan.test.ts"
      provides: "Bespoke-CSS / Tailwind-utility-collision scan (THEME-02)"
      contains: "ring-[#2563EB]"
  key_links:
    - from: "apps/web/src/styles/__tests__/contrast-aa.test.ts"
      to: "WCAG AA thresholds D-09/D-10"
      via: "computed contrast ratio assertions on composited backgrounds"
      pattern: "070b08"
    - from: "apps/web/src/styles/__tests__/theme-scan.test.ts"
      to: "apps/web/src (admin / affiliation / LanguageSwitcher / track-record)"
      via: "filesystem glob + regex over .tsx sources"
      pattern: "ring-\\[#2563EB\\]"
---

<objective>
Author the Wave-0 verification guards for Phase 15 BEFORE the token migration runs, so success criteria #2 (THEME-02 scan, D-08) and #5 (THEME-05 contrast, D-09/D-10) are proven by real failing-then-passing tests, not false positives on an unmigrated tree.

Purpose: lock the WCAG AA thresholds (D-09) on opaque AND composited translucent surfaces (D-10), anchored on the D-02 frozen GREEN values, and lock the THEME-02 anti-bespoke / anti-collision scan (D-08), so the migration (Plan 02) and the tokenization cleanup (Plan 03) have an objective GREEN target. Both tests are authored to FAIL now (RED) and pass once Plans 02 + 03 land.

Output: two Vitest files under `apps/web/src/styles/__tests__/` (already inside the root `vitest.config.ts` include glob — no config change needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md

<interfaces>
<!-- Established Wave-0 test pattern (read these to MATCH style — node:fs read + regex, no deps). -->
From apps/web/src/styles/__tests__/design-tokens.test.ts (Phase 10 analog):
  - reads globals.css via readFileSync(path.resolve(__dirname, '../globals.css'))
  - uses `import { describe, it, expect } from 'vitest'` (globals:false in vitest.config.ts)
  - extractThemeInline(source) helper pattern for block extraction

From vitest.config.ts:
  - include already covers 'apps/**/__tests__/**/*.test.ts' → these two files run with no config edit
  - environment: 'node', globals: false (explicit imports required)

Frozen GREEN values to assert against (CONTEXT.md D-02 / D-09, UI-SPEC §Color):
  - background #070b08 ; surface-solid #0f1611 ; foreground (text) #eafff1
  - primary oklch(0.84 0.18 150) ; muted-foreground rgba(212,244,224,0.64)
  - translucent surface rgba(255,255,255,0.035) ; line-soft rgba(255,255,255,0.06)
  - Verified ratios (D-09): primary/bg 11.39:1 ; text/bg 18.93:1 ; muted/bg 6.76:1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author WCAG AA contrast test (opaque + composited) — RED</name>
  <files>apps/web/src/styles/__tests__/contrast-aa.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/design-tokens.test.ts (style/imports analog to match exactly)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md §Color (the full token table + "Translucent-surface AA" paragraph)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md D-02, D-09, D-10
    - vitest.config.ts (confirm include glob + globals:false)
  </read_first>
  <behavior>
    - Test 1 (opaque): contrast(primary oklch(0.84 0.18 150), background #070b08) >= 11.0 and rounds to 11.39:1 (tolerance ±0.15).
    - Test 2 (opaque): contrast(text #eafff1, background #070b08) >= 18.0 (expect ~18.93:1).
    - Test 3 (opaque): contrast(muted-foreground rgba(212,244,224,0.64) composited over #070b08, background #070b08) >= 4.5 (AA normal text) and matches ~6.76:1.
    - Test 4 (composited translucent, D-10): composite rgba(255,255,255,0.035) over #070b08 → flattened card surface; assert contrast(text #eafff1, flattened-card) >= 4.5 (AA). MUST composite, not use the rgba alpha value alone.
    - Test 5 (composited translucent): composite line-soft rgba(255,255,255,0.06) over #070b08 and assert the card border/text pairing still clears AA for its role.
    - Sanity: a deliberately failing pair (e.g. muted-foreground over a light surface) returns < 4.5 so the harness is proven non-trivial.
  </behavior>
  <action>Create `apps/web/src/styles/__tests__/contrast-aa.test.ts`. Implement a self-contained relative-luminance + WCAG contrast-ratio computation in the test file (no new runtime dep): parse hex `#rrggbb` to sRGB 0–255, parse `oklch(L C H)` and `oklch(L C H / a)` to linear→sRGB (use a compact OKLCH→sRGB conversion), parse `rgba(r,g,b,a)`. Add an `alphaComposite(fg, bgOpaque)` helper that flattens a translucent color over an opaque base: `out = fg.rgb * a + bg.rgb * (1 - a)`. Add `relativeLuminance(rgb)` (sRGB gamma-expand then 0.2126/0.7152/0.0722) and `contrastRatio(c1, c2)` = `(Lmax + 0.05) / (Lmin + 0.05)`. Encode the frozen pairs from D-02/D-09 as a table of `{ label, fg, bg, composedOver?, minRatio, expectedRatio }`. For translucent entries (`--surface-translucent` rgba 0.035, `--line-soft` rgba 0.06) set `composedOver: '#070b08'` so the assertion runs on the composited color, never the alpha token alone (D-10). Assert each pair `>= minRatio` and `toBeCloseTo(expectedRatio, 1)` for the three D-09 anchor ratios (11.39 / 18.93 / 6.76). Include one sanity assertion proving a known-bad pair fails AA. Use `import { describe, it, expect } from 'vitest'`. RED now because the values are asserted independent of globals.css — the test is the spec; it stays GREEN regardless of file state for the math, but the file-binding assertions in Task 2 link it to the migration.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/contrast-aa.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run apps/web/src/styles/__tests__/contrast-aa.test.ts` exits 0 with all assertions green (the math proves the frozen GREEN palette is AA+).
    - Source assertion: file contains the literals `11.39`, `18.93`, `6.76`, `#070b08`, and `oklch(0.84 0.18 150)`.
    - Source assertion: file contains an `alphaComposite` (or equivalently named) helper AND at least two table rows with a `composedOver`/`#070b08` translucent case (D-10 proven, not the rgba alpha alone).
    - Behavior assertion: a deliberately-bad sanity pair asserts `< 4.5` (harness non-trivial).
    - No new dependency added to any package.json (grep `package.json` shows no diff).
  </acceptance_criteria>
  <done>contrast-aa.test.ts exists, runs green, proves AA on the 3 D-09 anchors plus ≥2 composited translucent surfaces, and includes a non-trivial sanity check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author THEME-02 bespoke/collision scan test — RED</name>
  <files>apps/web/src/styles/__tests__/theme-scan.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/design-tokens.test.ts (filesystem-scan style analog)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"Bespoke palette utilities — 7 files" (exact file list + raw→token mapping) and §"Shared Patterns / Focus ring"
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md §"Color guardrails (THEME-02 scan targets)"
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md D-08
  </read_first>
  <behavior>
    - Test 1: no occurrence of `ring-[#2563EB]` anywhere under apps/web/src (LanguageSwitcher fixed, ThemeToggle deleted) → expected count 0.
    - Test 2: no raw palette text/bg/border utility from the forbidden set in the 8 named foundation files: `text-amber-700`, `text-emerald-700`, `text-red-700`, `bg-emerald-500/10`, `bg-red-500/10`, `bg-red-500` (standalone), `border-emerald-600/30`, `border-red-600/30`, `border-amber-600/30`, `bg-amber-500/10`, `text-amber-400`, `text-emerald-400`, `text-red-400` → expected count 0 across those files.
    - Test 3 (anti-collision): glow must never be expressed as a Tailwind `ring-*` utility on the brand surfaces — assert `--glow`/box-shadow glow is separate from `focus-visible:ring-*` (no token file pairs `ring-` with the glow custom prop on the same class string in the scanned set).
    - Sanity: the scan regexes match a known planted bad string in a fixture array (prove the detector is non-trivial), then assert real files contain none.
  </behavior>
  <action>Create `apps/web/src/styles/__tests__/theme-scan.test.ts`. This is the D-08 proof gate (zero bespoke CSS per page + zero Tailwind utility collision). Use `node:fs` `readFileSync` + `node:path`. Define `FOUNDATION_FILES` as the exact relative paths from 15-PATTERNS.md: `src/components/LanguageSwitcher.tsx`, `src/app/[locale]/affiliation/dashboard/page.tsx`, `src/app/(admin)/signaux/page.tsx`, `src/app/(admin)/page.tsx`, `src/app/(admin)/sante/page.tsx`, `src/app/(admin)/affiliation/page.tsx`, `src/app/(admin)/affiliation/payouts/page.tsx`, `src/app/(admin)/file/page.tsx`, `src/components/track-record/TrackRecordView.tsx` (resolve each via `path.resolve(__dirname, '../../', rel)`; skip-with-explicit-fail if a path is missing so a moved file surfaces). Define `FORBIDDEN_RING = /ring-\[#2563EB\]/` and a `FORBIDDEN_PALETTE` regex array covering the amber/emerald/red raw utilities listed in the behavior block (escape brackets/slashes). Test 1 scans ALL `.tsx` under `src` (recursive readdir) for `FORBIDDEN_RING`, asserting total count 0. Test 2 scans only `FOUNDATION_FILES` for each `FORBIDDEN_PALETTE` entry, asserting 0 matches per file, with an error message naming the offending file+token. Test 3 asserts no class string in the scanned files contains both `ring-` and `--glow` (collision guard, D-08). Add a sanity block that runs the same regexes against an in-test fixture string containing the bad tokens and asserts they DO match (detector proven). Use `import { describe, it, expect } from 'vitest'`. This is RED now: the unmigrated `ring-[#2563EB]` (LanguageSwitcher lines 168/200) and the raw palette utilities still exist; it goes GREEN after Plans 02 (deletes ThemeToggle) and 03 (tokenizes the rest).</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/theme-scan.test.ts || echo "EXPECTED RED until Plans 02+03 land"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file contains the literal `ring-[#2563EB]` (as the forbidden pattern) and the 8 foundation file paths from 15-PATTERNS.md.
    - Behavior assertion (RED-now): running the test against the current tree FAILS on `ring-[#2563EB]` and at least one palette token (proves it measures the real migration, not a no-op).
    - Behavior assertion (sanity): the in-test fixture sanity block PASSES (regexes match planted bad strings) regardless of file state.
    - The test uses only `node:fs`/`node:path` — no new dependency.
    - A missing foundation file causes an explicit failure (not a silent skip).
  </acceptance_criteria>
  <done>theme-scan.test.ts exists; its sanity block proves the detector works; against the current (unmigrated) tree it is RED on the residual hardcodes — the objective GREEN target for Plans 02+03 (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Presentation/test-only plan. No network, auth, payment, DB, or user input. Tests read local source files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | test fixtures asserting wrong ratios | mitigate | Anchor ratios are the externally-verified D-09 values; sanity assertions prove the harness is non-trivial (no false-green). |
| T-15-SC | Tampering | npm installs | mitigate | This plan adds ZERO dependencies (constraint: no new runtime/dev dep this phase). No install task → no legitimacy gate triggered. |

Surface is presentation/verification only. No high-severity threats.
</threat_model>

<verification>
- `npx vitest run apps/web/src/styles/__tests__/contrast-aa.test.ts` → green (palette proven AA+ on opaque + composited).
- `npx vitest run apps/web/src/styles/__tests__/theme-scan.test.ts` → RED on residual hardcodes now; the gate Plans 02+03 must turn green.
- No package.json changed.
</verification>

<success_criteria>
- Two new Vitest files exist under `apps/web/src/styles/__tests__/`.
- Contrast test green and binds the D-09 anchors + ≥2 composited translucent surfaces (D-10).
- Scan test sanity-green and file-RED on the unmigrated tree (real target, not false positive).
- THEME-02 and THEME-05 now have objective, executable gates.
</success_criteria>

<output>
Create `.planning/phases/15-design-system-v3-dark-n-on-unique/15-01-SUMMARY.md` when done.
</output>
