---
phase: 15-design-system-v3-dark-neon-unique
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/styles/globals.css
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/components/ThemeToggle.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
  - apps/web/src/messages/__tests__/theme-parity.test.ts
autonomous: true
requirements: [THEME-01, THEME-03, THEME-04]
user_setup: []

must_haves:
  truths:
    - "On loading any page (fr/en/ar) the platform renders the single GREEN dark theme with no light flash (no-FOUC)"
    - "No code path can switch to a light theme (forcedTheme=dark, ThemeToggle deleted, no setTheme('light'))"
    - "The frozen GREEN values live in :root AND .dark resolves to the same values, so forced .dark cannot flip to navy"
    - "The theme i18n namespace is purged from all three locales in strict key parity"
    - "Arabic RTL (single <html lang dir>, logical properties, :lang(ar) font) is untouched"
  artifacts:
    - path: "apps/web/src/styles/globals.css"
      provides: "Frozen GREEN semantic layer in :root, .dark reconciled to identical values"
      contains: "oklch(0.84 0.18 150)"
    - path: "apps/web/src/app/[locale]/layout.tsx"
      provides: "ThemeProvider forcedTheme=dark, ThemeToggle removed"
      contains: "forcedTheme"
    - path: "apps/web/src/messages/fr.json"
      provides: "theme namespace removed (parity-safe)"
  key_links:
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "next-themes ThemeProvider"
      via: "forcedTheme prop"
      pattern: "forcedTheme=\"dark\""
    - from: "apps/web/src/styles/globals.css :root"
      to: "apps/web/src/styles/globals.css .dark"
      via: "identical frozen GREEN values so forced .dark never flips"
      pattern: "#070b08"
    - from: "apps/web/src/components/CandleChart.tsx + ui/sonner.tsx"
      to: ".dark selector (kept, D-06)"
      via: "MutationObserver / useTheme reading stable .dark"
      pattern: "\\.dark"
---

<objective>
Freeze the platform to a single GREEN dark theme: promote the `.nxl[data-theme="green"]` semantic values verbatim into `:root` (D-02/D-03), reconcile `.dark` to the same frozen values so the forced `.dark` class cannot flip to the legacy navy, set `forcedTheme="dark"`, delete `ThemeToggle`, and purge the dead `theme` i18n namespace in strict trilingual parity.

Purpose: deliver THEME-01 (single dark theme, no-FOUC, no light path), THEME-03 (GREEN frozen in `:root`), and preserve THEME-04 (RTL untouched). This is semantic-layer PROMOTION, not mechanical `.nxl` de-scoping (Pitfall #1).

Output: a frozen DS the downstream reskin (Phase 16) builds on without retrofitting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-UI-SPEC.md
@.planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md

<interfaces>
<!-- Exact current state of the files (read these line ranges before editing). -->
globals.css 3-layer system (verified):
  - Layer 1 @theme (lines 30–64): primitives --nexa-* — DO NOT EDIT.
  - Layer 3 @theme inline (lines 71–98): shadcn name → var() mapping — DO NOT EDIT (Pitfall 3).
  - Layer 2 :root (lines 111–139): CURRENT light values (var(--nexa-white) etc.) — REPLACE with frozen GREEN.
  - Layer 2 .dark (lines 145–168): CURRENT navy override (--nexa-ink / --nexa-neutral-800) — RECONCILE to frozen GREEN (CRITICAL: PATTERNS.md wrongly assumed .dark was already green; it is navy. Under forcedTheme="dark" .dark always wins, so it MUST match :root or the platform renders navy not green).
  - :lang(ar) (lines 175–178): font-family var(--font-arabic) + line-height 1.6 — DO NOT TOUCH (THEME-04).

layout.tsx (verified line numbers):
  - line 22: `import { ThemeToggle } from '../../components/ThemeToggle'` — DELETE.
  - line 63: `<ThemeProvider attribute="class" defaultTheme="light" enableSystem>` — CHANGE to `<ThemeProvider attribute="class" forcedTheme="dark">`.
  - line 82: `<ThemeToggle />` inside the `ms-auto` header div — DELETE (keep `<LanguageSwitcher />`).
  - line 57–61: <html lang dir suppressHydrationWarning> — KEEP verbatim (THEME-04 / no-FOUC).

messages/fr.json lines 13–17 (en.json / ar.json same structure): top-level `"theme"` namespace { toggleLabel, light, dark } — DELETE in all three.
  - WARNING (PATTERNS.md): a DIFFERENT `"theme"` key exists inside an (admin) namespace (fr.json ~line 597, en/ar ~line 402) — do NOT delete that one.

Frozen GREEN target values (CONTEXT.md D-02, PATTERNS.md target :root block):
  --background #070b08 ; --foreground #eafff1 ; --card #0f1611 ; --card-foreground #eafff1
  --popover #0f1611 ; --popover-foreground #eafff1 ; --primary oklch(0.84 0.18 150)
  --primary-foreground #051009 ; --secondary #0f1611 ; --secondary-foreground #eafff1
  --muted #0f1611 ; --muted-foreground rgba(212,244,224,0.64) ; --accent #0f1611 ; --accent-foreground #eafff1
  --destructive oklch(0.68 0.2 24) ; --border rgba(255,255,255,0.10) ; --input rgba(255,255,255,0.10)
  --ring oklch(0.84 0.18 150) ; --radius 0.625rem
  Extended (no shadcn name, custom props): --background-alt #0b110d ; --surface-translucent rgba(255,255,255,0.035)
  --line-soft rgba(255,255,255,0.06) ; --muted-tertiary rgba(212,244,224,0.40) ; --glow oklch(0.84 0.18 150 / 0.55)
  Kept distinct (D-05): --signal-bullish var(--nexa-signal-bull) ; --signal-bearish var(--nexa-signal-bear)
  --accent-brand var(--nexa-purple-400) ; --risk-moderate var(--nexa-amber-500)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote GREEN to :root and reconcile .dark in globals.css</name>
  <files>apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/styles/globals.css (read lines 100–178 — the full :root, .dark, :lang(ar) blocks)
    - apps/web/src/components/landing/nexa-landing.css lines 19–37 (the .nxl[data-theme="green"] source VALUES)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"globals.css" (the exact "Target :root after promotion" block) and §"Token-via-var() rule / Exception for Phase 15"
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md D-02, D-03, D-06
  </read_first>
  <action>Edit ONLY the Layer 2 blocks. (1) Replace the `:root` block (current lines ~111–139) so each shadcn-named semantic token carries the frozen GREEN literal value from the §Color table: `--background:#070b08; --foreground:#eafff1; --card:#0f1611; --card-foreground:#eafff1; --popover:#0f1611; --popover-foreground:#eafff1; --primary:oklch(0.84 0.18 150); --primary-foreground:#051009; --secondary:#0f1611; --secondary-foreground:#eafff1; --muted:#0f1611; --muted-foreground:rgba(212,244,224,0.64); --accent:#0f1611; --accent-foreground:#eafff1; --destructive:oklch(0.68 0.2 24); --border:rgba(255,255,255,0.10); --input:rgba(255,255,255,0.10); --ring:oklch(0.84 0.18 150);` keep `--radius:0.625rem;`. Add the extended custom props with no shadcn name: `--background-alt:#0b110d; --surface-translucent:rgba(255,255,255,0.035); --line-soft:rgba(255,255,255,0.06); --muted-tertiary:rgba(212,244,224,0.40); --glow:oklch(0.84 0.18 150 / 0.55);`. Keep `--signal-bullish:var(--nexa-signal-bull); --signal-bearish:var(--nexa-signal-bear); --accent-brand:var(--nexa-purple-400); --risk-moderate:var(--nexa-amber-500);` (signals stay DISTINCT from --primary per D-05). This is the documented Phase-15-only exception to the "var() only in Layer 2" rule (PATTERNS.md §Token-via-var). (2) RECONCILE the `.dark` block (current lines ~145–168): set every token it currently overrides to navy (--background, --foreground, --card*, --popover*, --secondary*, --muted*, --accent*, --border, --input, --accent-brand) to the SAME frozen GREEN values as `:root` so the forced `.dark` class resolves identically — the platform must paint GREEN, never the legacy --nexa-ink/--nexa-neutral-800 navy. Keep the `.dark` selector PRESENT (D-06 — CandleChart + sonner observe it). (3) DO NOT touch Layer 1 `@theme` (lines 30–64), Layer 3 `@theme inline` (lines 71–98), or `:lang(ar)` (lines 175–178). Add a comment marking the frozen-GREEN promotion with D-02/D-03 reference.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/contrast-aa.test.ts apps/web/src/styles/__tests__/design-tokens.test.ts apps/web/src/styles/__tests__/rtl-logical-props.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `:root` block contains `--background: #070b08`, `--primary: oklch(0.84 0.18 150)`, `--primary-foreground: #051009`, `--destructive: oklch(0.68 0.2 24)`, `--glow: oklch(0.84 0.18 150 / 0.55)`.
    - Source assertion: the `.dark` selector still EXISTS (grep `.dark {` count >= 1) AND no longer references `--nexa-ink` or `--nexa-neutral-800` for --background/--card (it now resolves to the same frozen GREEN as :root).
    - Source assertion: `@theme` Layer 1 and `@theme inline` Layer 3 are byte-unchanged (no edit inside lines 30–98); `:lang(ar)` block unchanged.
    - Source assertion: `--signal-bullish`/`--signal-bearish` still resolve via `var(--nexa-signal-*)` (D-05 distinction preserved).
    - Test command: design-tokens.test.ts stays green (oklch present, --nexa-green-500 still defined, @theme inline still var()-only); rtl-logical-props.test.ts green; contrast-aa.test.ts green.
  </acceptance_criteria>
  <done>:root carries frozen GREEN, .dark resolves identically (no navy flip), primitives + component layer + :lang(ar) untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Force dark theme + delete ThemeToggle in layout</name>
  <files>apps/web/src/app/[locale]/layout.tsx, apps/web/src/components/ThemeToggle.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/layout.tsx (lines 1–95 — full top + header + provider)
    - apps/web/src/components/ThemeToggle.tsx (confirm it is the only consumer pattern before deletion)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"layout.tsx" (3 surgical edits) and §"ThemeToggle.tsx"
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md D-04, D-05, D-06
  </read_first>
  <action>In `layout.tsx`: (1) delete line 22 `import { ThemeToggle } from '../../components/ThemeToggle'`. (2) change the provider open tag (line 63) from `<ThemeProvider attribute="class" defaultTheme="light" enableSystem>` to `<ThemeProvider attribute="class" forcedTheme="dark">` (D-04 — removes defaultTheme + enableSystem, adds forcedTheme). (3) delete the `<ThemeToggle />` line inside the `ms-auto` header div (line 82), leaving `<LanguageSwitcher />` as the sole control. KEEP `<html lang dir suppressHydrationWarning>` (lines 57–61) verbatim and the `dir={locale === 'ar' ? 'rtl' : 'ltr'}` logic untouched (THEME-04). Then DELETE the entire file `apps/web/src/components/ThemeToggle.tsx` (no replacement; it was the only `useTranslations('theme')` consumer and the only `ring-[#2563EB]` source besides LanguageSwitcher). Confirm no other import of ThemeToggle remains in apps/web/src.</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx tsc -b --force 2>&1 | grep -v "forbidden-service-import" | grep -iE "ThemeToggle|error TS" | grep -c . | grep -qx 0 && echo "no new tsc errors / no ThemeToggle refs"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `layout.tsx` contains `forcedTheme="dark"` and does NOT contain `defaultTheme`, `enableSystem`, or `ThemeToggle`.
    - Source assertion: `apps/web/src/components/ThemeToggle.tsx` no longer exists.
    - Behavior assertion: grep across `apps/web/src` for `ThemeToggle` returns 0 matches; grep for `setTheme('light')` / `setTheme("light")` returns 0.
    - Source assertion: `<LanguageSwitcher />` still rendered in the header; `suppressHydrationWarning` and the `dir=` ternary preserved.
    - Test command: `tsc -b --force` introduces no NEW errors beyond the pre-existing `forbidden-service-import.ts` fixture baseline (D-01-03-BASELINE).
  </acceptance_criteria>
  <done>forcedTheme="dark" set, ThemeToggle deleted with no dangling import, RTL/no-FOUC html attributes intact.</done>
</task>

<task type="auto">
  <name>Task 3: Purge theme i18n namespace + update parity test (trilingual)</name>
  <files>apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json, apps/web/src/messages/__tests__/theme-parity.test.ts</files>
  <read_first>
    - apps/web/src/messages/fr.json lines 1–20, en.json lines 1–20, ar.json lines 1–20 (the top-level `theme` namespace)
    - apps/web/src/messages/__tests__/theme-parity.test.ts (the test that currently REQUIRES the theme namespace — it WILL break unless updated)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-PATTERNS.md §"messages/{fr,en,ar}.json" (the WARNING about the second (admin)-scoped `theme` key NOT to delete)
    - .planning/phases/15-design-system-v3-dark-n-on-unique/15-CONTEXT.md D-05
  </read_first>
  <action>Delete the top-level `"theme": { "toggleLabel", "light", "dark" }` namespace (fr.json lines 13–17 and the structurally identical block in en.json + ar.json) from all THREE files IN THE SAME CHANGE (strict key parity, D-05 — never one locale only). Do NOT delete the unrelated `"theme"` key nested inside the `(admin)` namespace (fr ~line 597, en/ar ~line 402) — verify by confirming the deleted block is the top-level one adjacent to the `language` namespace. Then update `theme-parity.test.ts`: it currently asserts the `theme` namespace EXISTS with keys toggleLabel/light/dark — invert it to assert the top-level `theme` namespace is ABSENT in all three locales (e.g. `expect(messages).not.toHaveProperty('theme')`), proving the purge happened in parity. Keep the file as a real guard (rename describe to reflect "namespace removed").</action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/messages/__tests__/theme-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: none of fr.json/en.json/ar.json contains a top-level `"theme"` namespace with `toggleLabel`/`light`/`dark` (grep for `"toggleLabel"` returns 0 across the three message files).
    - Source assertion: the `(admin)`-nested `theme` key is still present (not collaterally deleted).
    - Behavior assertion: `theme-parity.test.ts` now asserts ABSENCE of the top-level theme namespace and passes for all three locales.
    - Test command: `npx vitest run apps/web/src/messages/__tests__/theme-parity.test.ts` exits 0.
    - All three JSON files remain valid JSON (no trailing-comma break) — `node -e "require('./apps/web/src/messages/fr.json')"` style parse succeeds for each.
  </acceptance_criteria>
  <done>theme namespace removed from all three locales in parity, the admin-nested theme key preserved, parity test inverted and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/i18n loader → next-intl | Message JSON consumed at build; a partial/asymmetric purge would orphan keys and break the loader/parity test (build failure, not a security threat). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | i18n purge leaves orphan/asymmetric keys | mitigate | Delete the `theme` namespace in all 3 locales in the same change; theme-parity.test.ts (inverted) blocks an asymmetric purge. |
| T-15-03 | Tampering | .dark left as navy under forcedTheme | mitigate | Task 1 reconciles .dark to frozen GREEN; contrast-aa + visual rendering depend on identical :root/.dark values. |
| T-15-SC | Tampering | npm installs | accept | No installs in this plan (no new dep this phase). No legitimacy gate triggered. |

Surface is presentation/config only — no auth, payment, DB, or user input. Block-on severity: none expected.
</threat_model>

<verification>
- contrast-aa + design-tokens + rtl-logical-props tests green (Task 1).
- `tsc -b --force` = baseline only, zero ThemeToggle references (Task 2).
- theme-parity test green with inverted assertion (Task 3).
- Manual no-FOUC is the expected RESULT of frozen :root, not a failure path (UI-SPEC §Error state).
</verification>

<success_criteria>
- `:root` and `.dark` both resolve to frozen GREEN; no light path; no-FOUC by construction (THEME-01/THEME-03).
- `forcedTheme="dark"` set; ThemeToggle deleted; no `setTheme('light')` anywhere.
- `theme` namespace purged in strict trilingual parity; RTL/:lang(ar) untouched (THEME-04).
- Primitive + component token layers byte-unchanged (no mechanical .nxl de-scoping — Pitfall #1 respected).
</success_criteria>

<output>
Create `.planning/phases/15-design-system-v3-dark-n-on-unique/15-02-SUMMARY.md` when done.
</output>
