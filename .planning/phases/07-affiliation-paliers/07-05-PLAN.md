---
phase: 07-affiliation-paliers
plan: 05
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - apps/web/src/app/(admin)/affiliation/page.tsx
  - apps/web/src/app/(admin)/affiliation/actions.ts
  - apps/web/src/app/(admin)/affiliation/payouts/page.tsx
  - apps/web/src/app/(admin)/affiliation/payouts/actions.ts
  - apps/web/src/messages/fr.json
autonomous: true
requirements: [AFF-01, AFF-04]

must_haves:
  truths:
    - "Le superadmin voit la file des candidatures pending et peut Approuver (promotion affiliate + code vanity) / Rejeter (motif requis)"
    - "D-06: code vanity choisi par l'affilié (A-Z0-9 borné, unique) posé à l'approbation ; D-07: posé par le superadmin, aucun self-serve d'activation"
    - "Le superadmin voit les commissions dues et marque payé (tx_hash + montant + date) via alert-dialog"
    - "D-15: marquer payé enregistre tx_hash + date + montant (traçabilité on-chain), commission due→paid"
    - "Chaque server action re-valide requireRole('superadmin') en tête (endpoint POST direct)"
    - "Marquer payé insère payouts + transitionne commission due→paid via service_role/RPC"
  artifacts:
    - path: "apps/web/src/app/(admin)/affiliation/page.tsx"
      provides: "file de revue des candidatures (RSC, mono-FR)"
      contains: "loadQueue"
    - path: "apps/web/src/app/(admin)/affiliation/payouts/actions.ts"
      provides: "payCommission (requireRole superadmin + markCommissionPaid)"
      contains: "requireRole"
  key_links:
    - from: "apps/web/src/app/(admin)/affiliation/payouts/actions.ts"
      to: "markCommissionPaid (@app/supabase)"
      via: "service_role local"
      pattern: "markCommissionPaid\\("
    - from: "apps/web/src/app/(admin)/affiliation/actions.ts"
      to: "promoteAffiliate / createCode"
      via: "service_role local après requireRole"
      pattern: "createAdminServiceClient"
---

<objective>
Back-office affilié (surfaces 2 et 4, mono-FR, hors `[locale]`, 404 non-superadmin) : file de revue des candidatures (approuver → rôle + code vanity / rejeter → motif) et vue payout (marquer payé → tx_hash + montant + date). Miroir exact de `(admin)/file`.

Purpose: AFF-01 (le superadmin pose le code vanity, D-07 : pas de self-serve) et AFF-04 (payout manuel tracé tx_hash, D-15). Producteur-unique : écritures service_role re-validées.
Output: 2 pages + 2 actions admin + sous-clés i18n FR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-affiliation-paliers/07-UI-SPEC.md
@.planning/phases/07-affiliation-paliers/07-PATTERNS.md

<interfaces>
apps/web/src/app/(admin)/file/actions.ts (analog) :
- `'use server'` + `'server-only'` ; helper `guard()` = `await requireRole('superadmin')` puis `createAdminServiceClient()` ; type QueueActionResult ; `fail()` ; parse strict FormData ; revalidatePath.
apps/web/src/app/(admin)/file/page.tsx (analog page RSC) :
- loadQueue via createAdminServiceClient ; getTranslations('admin') ; conteneur `mx-auto max-w-6xl px-4 py-8` ; `<h1 className="text-2xl font-semibold">` ; Table ; badge ambre pending `border-amber-600/30 bg-amber-500/10 text-amber-700 dark:text-amber-400` ; empty state ; lien externe `target="_blank" rel="noopener noreferrer"`.
@app/supabase (07-03) : promoteAffiliate, createCode, listPendingApplications, transitionApplication, markCommissionPaid + CODE_TAKEN.
apps/web/src/lib/auth/gate.ts : requireRole('superadmin') → notFound() si pas superadmin.
UI-SPEC §Copywriting + §Color : badges ambre/neutre (PAS vert/rouge D-04) ; CTA payout « Marquer comme payé » ; rejet en destructive gris.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: File de revue des candidatures (admin)/affiliation</name>
  <read_first>
    - apps/web/src/app/(admin)/file/page.tsx (RSC table, loadQueue, getTranslations('admin'), badge ambre, empty state, conteneur)
    - apps/web/src/app/(admin)/file/actions.ts (guard() requireRole superadmin, QueueActionResult, fail, parse strict, revalidatePath, rejectPayment motif requis)
    - .planning/phases/07-affiliation-paliers/07-PATTERNS.md section file de revue (L.172-178)
    - .planning/phases/07-affiliation-paliers/07-UI-SPEC.md §Copywriting + §Color (badges, errors.codeTaken)
  </read_first>
  <action>
    Créer `apps/web/src/app/(admin)/affiliation/page.tsx` (RSC mono-FR) : `loadQueue()` via createAdminServiceClient → `listPendingApplications` ; `getTranslations('admin')` ; conteneur `mx-auto max-w-6xl px-4 py-8` ; `<h1 className="text-2xl font-semibold">` ; Table colonnes (email, réseaux/Telegram/Facebook, nb abonnés, interactions, statut, actions) ; badge ambre pending ; empty state UI-SPEC (« Aucune candidature en attente »). Lecture status='pending'.
    Créer `apps/web/src/app/(admin)/affiliation/actions.ts` : `'use server'` + `'server-only'` ; helper `guard()` (requireRole('superadmin') puis createAdminServiceClient) ; `approveApplication(formData)` → parse application_id + user_id (l'email de candidature doit être résolu en user_id existant — si pas de compte, traiter via promoteAffiliate après création/lookup) + code vanity (input borné A-Z0-9) → `promoteAffiliate` + `createCode` (capture CODE_TAKEN → erreur i18n `admin.affiliateQueue.errors.codeTaken`) + `transitionApplication(approved)` ; `rejectApplication(formData)` → motif requis (miroir rejectPayment) → `transitionApplication(rejected, {reject_reason})`. `revalidatePath('/affiliation')` après écriture.
    Ajouter les sous-clés mono-FR dans `apps/web/src/messages/fr.json` sous `admin` : `affiliateQueue.*` (titre, colonnes, actions, codeInput, errors.codeTaken, empty, rejectDialog). NE PAS toucher en.json/ar.json (back-office mono-FR, invariant D-04-03-B).
  </action>
  <verify>
    <automated>grep -c "requireRole('superadmin')" "apps/web/src/app/(admin)/affiliation/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - actions.ts : `guard()` appelle `requireRole('superadmin')` AVANT toute écriture ; approve + reject présents.
    - approveApplication appelle `promoteAffiliate` ET `createCode` (capture CODE_TAKEN).
    - rejectApplication exige un motif (`if (!reject_reason) throw`).
    - page.tsx : badge ambre pending (`amber-500/10`), aucune classe vert/rouge (`grep -c "green-\|red-" page.tsx` == 0).
    - fr.json contient `admin.affiliateQueue` ; en.json/ar.json non modifiés.
    - `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0.
  </acceptance_criteria>
  <done>File de revue : superadmin approuve (rôle + code vanity) ou rejette (motif) via service_role re-validé.</done>
</task>

<task type="auto">
  <name>Task 2: Vue payout (admin)/affiliation/payouts</name>
  <read_first>
    - apps/web/src/app/(admin)/file/page.tsx (table + lien externe TronScan target=_blank rel=noopener)
    - apps/web/src/app/(admin)/file/actions.ts (guard, parse FormData, revalidatePath)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Pattern 7 (L.392-414) squelette payCommission
    - .planning/phases/07-affiliation-paliers/07-UI-SPEC.md §Copywriting (CTA « Marquer comme payé », alert-dialog tx_hash+montant+date)
  </read_first>
  <action>
    Créer `apps/web/src/app/(admin)/affiliation/payouts/page.tsx` (RSC mono-FR) : `getTranslations('admin')` ; lire les commissions status='due' via createAdminServiceClient ; Table colonnes (affilié, période, base, montant dû en `<bdi>{formatAtomic(BigInt(x))}`, statut badge ambre due / neutre paid, action) ; lien tx_hash → TronScan `target="_blank" rel="noopener noreferrer"` ; empty state (« Aucune commission à payer pour le moment. »). Vert/rouge interdits.
    Créer `apps/web/src/app/(admin)/affiliation/payouts/actions.ts` (RESEARCH §Pattern 7) : `'use server'` + `'server-only'` ; `payCommission(formData)` → `requireRole('superadmin')` en tête ; lire commission_id + tx_hash (trim) + amount_atomic (string bigint) + paid_at ; valider non vides ; `markCommissionPaid(createAdminServiceClient(), { commission_id, tx_hash, amount_atomic })` (RPC insert payouts + commission due→paid, atomique) ; `revalidatePath('/affiliation/payouts')`. Confirmation via alert-dialog (saisie obligatoire tx_hash+montant+date, D-15) côté page (composant client).
    Ajouter sous-clés mono-FR `admin.payouts.*` dans fr.json (titre, colonnes, CTA « Marquer comme payé », dialog tx_hash/montant/date, empty). en/ar non modifiés.
  </action>
  <verify>
    <automated>grep -c "markCommissionPaid" "apps/web/src/app/(admin)/affiliation/payouts/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - payouts/actions.ts : `requireRole('superadmin')` en tête + `markCommissionPaid` + `revalidatePath`.
    - tx_hash + amount_atomic + date obligatoires (`if (!commission_id || !tx_hash) throw`).
    - page.tsx : lien tx_hash `target="_blank" rel="noopener noreferrer"` ; montants en `<bdi>` + `formatAtomic(BigInt(...))` ; `grep -c "green-\|red-" page.tsx` == 0.
    - fr.json contient `admin.payouts` ; en/ar non modifiés.
    - `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Vue payout : superadmin marque payé (tx_hash+montant+date) → payouts + commission due→paid atomique, lien TronScan anti-tabnabbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superadmin → server action | endpoint POST appelable directement, re-validation requireRole obligatoire |
| action → DB | écritures (promotion, code, payout) via service_role bypass RLS |
| lien tx_hash → TronScan | navigation externe (reverse-tabnabbing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-ADMIN-WRITE | Elevation | actions admin | mitigate | requireRole('superadmin') re-validé en tête de CHAQUE action (T-04-ADMIN-WRITE) |
| T-07-DOUBLEPAY | Tampering | payCommission | mitigate | markCommissionPaid via RPC atomique (mark_commission_paid raise si déjà paid) |
| T-07-CODETAKEN | Tampering | createCode | mitigate | UNIQUE DB + capture 23505 → message i18n, jamais d'écrasement |
| T-07-EXTLINK | — | lien TronScan | mitigate | `target="_blank" rel="noopener noreferrer"` (T-04-EXTLINK) |
| T-07-DESTRUCT | Tampering | rejectApplication | mitigate | motif requis (textarea), confirmation alert-dialog |
| T-07-SC | Tampering | npm installs | accept | aucun paquet npm ajouté (composants ui/ déjà installés) |
</threat_model>

<verification>
- requireRole re-validé dans chaque action ; payout atomique.
- Badges ambre/neutre seuls (zéro vert/rouge) ; lien externe sécurisé.
- `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0 ; suite complète non régressée.
</verification>

<success_criteria>
- Back-office : pose du code vanity (AFF-01 D-07) + payout tracé (AFF-04 D-15).
</success_criteria>

<output>
Create `.planning/phases/07-affiliation-paliers/07-05-SUMMARY.md` when done
</output>
</output>
