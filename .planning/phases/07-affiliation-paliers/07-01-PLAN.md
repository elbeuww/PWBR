---
phase: 07-affiliation-paliers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0016_affiliation.sql
  - packages/supabase/src/database.types.ts
autonomous: false
requirements: [AFF-01, AFF-02, AFF-03, AFF-04, AFF-05]

must_haves:
  truths:
    - "Les 5 tables d'affiliation existent en base LIVE (affiliate_applications, affiliate_codes, referrals, commissions, payouts) avec RLS active"
    - "Un affilié ne peut SELECT que ses propres lignes ; le superadmin voit tout ; aucune policy insert/update/delete (write = service_role bypass)"
    - "Le RPC compute_affiliate_commissions calcule taux × Σ amount_atomic des filleuls ACTIFS, exclut l'auto-parrainage, upsert idempotent UNIQUE(affiliate_id, referral_id, period)"
    - "D-02: taux dérivé du nombre d'inscrits via le code ; D-03: base = Σ amount_atomic des filleuls actifs ; D-04: découverte ET standard commissionnent ; D-05: période = mois calendaire UNIQUE(affiliate_id,referral_id,period) ; D-12: exclusion auto-parrainage au calcul (r.user_id <> a.user_id)"
    - "Le RPC mark_commission_paid insère payouts + transitionne commission due→paid en une transaction"
    - "La vue affiliate_dashboard (security_invoker=true) ne renvoie que des agrégats scopés auth.uid(), zéro ligne par filleul, et expose le revenu brut décomposé cumul vs mois courant (D-14)"
    - "database.types.ts expose les nouveaux Row/Insert avec base_atomic/amount_atomic typés string"
  artifacts:
    - path: "supabase/migrations/0016_affiliation.sql"
      provides: "5 tables + RLS + affiliate_rate_bps + compute_affiliate_commissions + mark_commission_paid + vue affiliate_dashboard (revenu cumul + mois courant)"
      contains: "create table public.commissions"
    - path: "packages/supabase/src/database.types.ts"
      provides: "Types des nouvelles tables + alias maison + override string *_atomic"
      contains: "AffiliateRow"
  key_links:
    - from: "supabase/migrations/0016_affiliation.sql"
      to: "public.payments / public.subscriptions"
      via: "join filleuls actifs dans compute_affiliate_commissions"
      pattern: "subscriptions.*status.*active"
    - from: "packages/supabase/src/database.types.ts"
      to: "commissions.amount_atomic"
      via: "override manuel bigint→string"
      pattern: "amount_atomic.*string"
    - from: "07-03-PLAN.md (repos + tests)"
      to: "compute_affiliate_commissions / mark_commission_paid (RPC de ce plan)"
      via: "wrappers RPC computeCommissions/markCommissionPaid + tests d'intégration valident fonctionnellement la logique du RPC (re-run idempotent, self-ref→0, expiré→0, double-payout bloqué)"
      pattern: "compute_affiliate_commissions"
    - from: "07-06-PLAN.md (dashboard RSC)"
      to: "vue affiliate_dashboard (revenue_total_atomic + revenue_current_month_atomic)"
      via: "lecture auth-client RSC des colonnes revenu D-14"
      pattern: "affiliate_dashboard"
---

<objective>
Poser le socle DB de l'affiliation : migration 0016 (5 tables, RLS isolation, grille de taux SQL, RPC de calcul idempotent + RPC payout atomique, vue dashboard no-PII avec revenu cumul + mois courant), l'appliquer LIVE via MCP, puis ré-aligner database.types.ts à la main.

Purpose: Tout le déterminisme financier, l'idempotence et l'isolation vivent en DB (miroir 0012). Les couches JS (repos, job, UI des plans suivants) ne font qu'appeler ces objets. La logique du RPC `compute_affiliate_commissions` posée ici est validée FONCTIONNELLEMENT par les tests d'intégration de 07-03 (re-run idempotent, self-ref→0, expiré→0). Sans cette couche LIVE, build + tsc passent à tort (faux positif) car les types viennent du fichier édité main.
Output: schéma affiliation appliqué en base + types TS alignés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-affiliation-paliers/07-CONTEXT.md
@.planning/phases/07-affiliation-paliers/07-RESEARCH.md
@.planning/phases/07-affiliation-paliers/07-PATTERNS.md

<interfaces>
<!-- Analogs de référence — l'exécuteur copie ces structures, ne les explore pas -->

supabase/migrations/0012_payments.sql (analog principal) :
- en-tête « apply_migration PAS db push » + STRIDE
- create table public.payments + alter table ... enable row level security
- create unique index payments_tx_hash_global_idx
- 3 policies : "...: insérer la sienne", "...: lire les siennes" (user_id = auth.uid()), "...: superadmin voit tout" (public.is_superadmin())
- commentaire « AUCUNE policy insert/update/delete → service_role bypass (D-08) »
- create function public.activate_subscription_for_payment(...) returns void language plpgsql security definer set search_path = public ... get diagnostics v_updated = row_count ... raise exception ... ; revoke execute on function ... from public, anon, authenticated;

supabase/migrations/0009_subscriptions_gating.sql :
- has_active_subscription() : `status='active' AND current_period_end > now()` = définition canonique d'« actif »

supabase/migrations/0008_profiles_role.sql :
- profiles.role check (member/affiliate/superadmin) ; is_superadmin() security definer

RESEARCH §Pattern 4 (RPC) : SQL complet affiliate_rate_bps + compute_affiliate_commissions (à reproduire).
RESEARCH §Code Examples (vue) : vue affiliate_dashboard security_invoker=true AVEC revenue_total_atomic + revenue_current_month_atomic (D-14).
PATTERNS §database.types.ts : bloc override + alias à ré-appliquer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration 0016_affiliation.sql (5 tables + RLS + grille + RPC + vue revenu cumul/mois)</name>
  <files>supabase/migrations/0016_affiliation.sql</files>
  <read_first>
    - supabase/migrations/0012_payments.sql (analog complet : en-tête, table, RLS triplet, RPC, revoke)
    - supabase/migrations/0009_subscriptions_gating.sql (définition « actif » has_active_subscription)
    - supabase/migrations/0008_profiles_role.sql (is_superadmin, profiles.role)
    - supabase/migrations/0015_telegram_posts.sql (RLS sans policy write — modèle le plus récent)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Pattern 4 + §Code Examples vue (revenue_total_atomic / revenue_current_month_atomic)
    - .planning/phases/07-affiliation-paliers/07-PATTERNS.md section migration 0016
  </read_first>
  <action>
    Créer `supabase/migrations/0016_affiliation.sql`. En-tête : recopier la convention 0012 (apply_migration PAS db push, édition main de database.types.ts, numéro 0016, ⚠️ 0013 absente) + bloc STRIDE listant T-07-SELFREF, T-07-DOUBLEPAY, T-07-PII, T-07-RLS-ISO, T-07-REFINJ.

    Tables (toutes `enable row level security`) :
    - `affiliates` (id uuid pk default gen_random_uuid, user_id uuid not null references profiles(id) on delete cascade unique, created_at timestamptz default now()).
    - `affiliate_codes` (code text primary key check (code ~ '^[A-Z0-9]{3,20}$'), affiliate_id uuid not null references affiliates(id) on delete cascade, created_at). D-06 : charset/longueur bornés ; UNIQUE filet inviolable.
    - `affiliate_applications` (id, applicant_email text not null, social_links text, telegram text, facebook text, subscriber_count int, interactions text, status text not null default 'pending' check (status in ('pending','approved','rejected')), reject_reason text, created_at). D-08.
    - `referrals` (id, affiliate_id uuid not null references affiliates(id) on delete cascade, user_id uuid not null references profiles(id) on delete cascade, attributed_at timestamptz default now(), UNIQUE(user_id) — un filleul attribué une seule fois, last-touch figé au cookie D-11).
    - `commissions` (id, affiliate_id, referral_id uuid references profiles(id), period text not null, rate_bps int not null, base_atomic bigint not null default 0, amount_atomic bigint not null default 0, status text not null default 'due' check (status in ('due','paid')), created_at). A7/Q4 RESOLVED : valeurs SQL `'due'`/`'paid'` SANS accent.
    - `payouts` (id, commission_id uuid not null references commissions(id) on delete cascade, tx_hash text not null, amount_atomic bigint not null, paid_at timestamptz not null default now()). D-15.

    Index idempotence : `create unique index commissions_aff_ref_period_idx on public.commissions (affiliate_id, referral_id, period);` (D-05, miroir payments_tx_hash_global_idx).

    RLS (miroir EXACT du triplet payments) — SELECT scopé + superadmin, AUCUNE policy write :
    - `affiliates` : SELECT where user_id = auth.uid() ; SELECT where public.is_superadmin().
    - `affiliate_codes` : SELECT via affiliate_id propriété de auth.uid() (`affiliate_id in (select id from affiliates where user_id = auth.uid())`) + superadmin.
    - `referrals`, `commissions`, `payouts` : SELECT scopé sur l'affilié propriétaire + superadmin.
    - `affiliate_applications` : §Open Q1 RESOLVED → PAS de policy insert authenticated (l'insert candidature passe par service_role, Plan 07-06). SELECT superadmin uniquement.
    - Commentaire explicite « AUCUNE policy insert/update/delete → write = service_role bypass » sous chaque table.

    Fonction grille `public.affiliate_rate_bps(p_signups int) returns int language sql immutable` : case D-01 en basis points (>=50000→2000, >=25001→1800, >=10001→1700, >=5001→1600, >=1001→1500, >=501→1400, >=100→1200, >=1→800, else 0).

    RPC `public.compute_affiliate_commissions(p_period text) returns jsonb language plpgsql security definer set search_path = public` (miroir activate_subscription_for_payment) : INSERT ... SELECT joignant affiliates × referrals (EXCLUSION `r.user_id <> a.user_id` D-12) × lateral count(referrals) → signups → affiliate_rate_bps (D-02 audience) × payments (status='verified', filleul, paiement dans le mois p_period via bornes timestamptz UTC `[date_trunc('month', (p_period||'-01')::date) , + interval '1 month')` — PAS to_char, Pitfall 4) × subscriptions (status='active' AND current_period_end > now() — AFF-05). amount_atomic = (sum(amount_atomic) * rate_bps) / 10000 (entier, troncature Postgres `floor`, A5/Q3 RESOLVED). `on conflict (affiliate_id, referral_id, period) do update set ... where public.commissions.status = 'due'` (jamais écraser une payée). `get diagnostics` + return jsonb. `revoke execute ... from public, anon, authenticated;`

    RPC `public.mark_commission_paid(p_commission_id uuid, p_tx_hash text, p_amount_atomic bigint) returns void language plpgsql security definer set search_path = public` : insert payouts + update commissions set status='paid' where id=p_commission_id and status='due' + get diagnostics raise si row_count=0 (anti double-payout). `revoke execute ... from public, anon, authenticated;`

    Vue `public.affiliate_dashboard with (security_invoker = true)` (RESEARCH §Code Examples) : agrégats seuls par affilié, `where a.user_id = auth.uid()`, `::text` sur les bigint sommés (CR-02). Colonnes : total_signups / active_referrals / commissions_due_atomic / commissions_paid_atomic ET — WARNING 3 / D-14 « revenus générés (cumul + mois courant) » — `revenue_total_atomic` = `coalesce(sum(p.amount_atomic),0)::text` (revenu brut cumulé des filleuls, join `payments` status='verified') + `revenue_current_month_atomic` = même somme `filter (where to_char(p.verified_at,'YYYY-MM') = to_char(now() at time zone 'utc','YYYY-MM'))::text` (mois courant). ZÉRO user_id de filleul exposé (D-13).
  </action>
  <verify>
    <automated>test "$(grep -vc '^--' supabase/migrations/0016_affiliation.sql)" -gt 0 && echo "--- invariants ---" && echo "tables=$(grep -c 'create table public' supabase/migrations/0016_affiliation.sql) (attendu 6)" && echo "rls=$(grep -c 'enable row level security' supabase/migrations/0016_affiliation.sql) (attendu 6)" && echo "writepolicy=$(grep -v '^--' supabase/migrations/0016_affiliation.sql | grep -ic 'for insert\|for update\|for delete') (attendu 0)" && echo "idx=$(grep -c 'commissions_aff_ref_period_idx' supabase/migrations/0016_affiliation.sql) (attendu >=1)" && echo "invoker=$(grep -c 'security_invoker = true' supabase/migrations/0016_affiliation.sql) (attendu >=1)" && echo "revoke=$(grep -c 'revoke execute on function' supabase/migrations/0016_affiliation.sql) (attendu 2)" && echo "selfref=$(grep -c 'r.user_id <> a.user_id' supabase/migrations/0016_affiliation.sql) (attendu >=1)" && echo "accent=$(grep -c \"'payée'\" supabase/migrations/0016_affiliation.sql) (attendu 0)" && echo "revenuecols=$(grep -c 'revenue_total_atomic\|revenue_current_month_atomic' supabase/migrations/0016_affiliation.sql) (attendu >=2)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create table public" supabase/migrations/0016_affiliation.sql` == 6 (affiliates, affiliate_codes, affiliate_applications, referrals, commissions, payouts)
    - `grep -c "enable row level security" supabase/migrations/0016_affiliation.sql` == 6
    - `grep -v '^--' supabase/migrations/0016_affiliation.sql | grep -ic "for insert\|for update\|for delete"` == 0 (aucune policy write — service_role bypass)
    - Fichier contient `r.user_id <> a.user_id` (exclusion auto-parrainage D-12)
    - Fichier contient `commissions_aff_ref_period_idx` et `on conflict (affiliate_id, referral_id, period)`
    - Fichier contient `security_invoker = true` (vue dashboard)
    - Fichier contient deux lignes `revoke execute on function` (compute + mark_paid)
    - Aucune valeur SQL accentuée : `grep -c "'payée'" supabase/migrations/0016_affiliation.sql` == 0
    - Vue contient `revenue_total_atomic` ET `revenue_current_month_atomic` (D-14 revenu cumul + mois courant)
  </acceptance_criteria>
  <done>Migration 0016 écrite : 6 tables, RLS isolation sans write front, grille bps, 2 RPC security definer + revoke, vue security_invoker no-PII avec revenu cumul + mois courant (D-14).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Appliquer 0016 LIVE via MCP apply_migration + régénérer + ré-aligner database.types.ts</name>
  <files>packages/supabase/src/database.types.ts</files>
  <read_first>
    - supabase/migrations/0016_affiliation.sql (le SQL à appliquer — produit en Task 1)
    - packages/supabase/src/database.types.ts (bloc override *_atomic + bloc alias à ré-appliquer)
    - .planning/STATE.md (D-05-02-F : generate_typescript_types écrase tout le fichier ; D-04-02-B : apply via MCP, jamais db push)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Pitfall 5 + §Project Constraints
  </read_first>
  <action>
    [BLOCKING — mécanisme PROJET : MCP Supabase `apply_migration`, JAMAIS `supabase db push`, JAMAIS `gen types --linked` (projet non lié, canal 0008–0015).] Étapes orchestrateur après confirmation humaine :
    1. `apply_migration` name `0016_affiliation` avec le contenu de supabase/migrations/0016_affiliation.sql.
    2. `list_tables` → confirmer affiliates, affiliate_codes, affiliate_applications, referrals, commissions, payouts + index commissions_aff_ref_period_idx + RLS enabled.
    3. `get_advisors` security → confirmer qu'AUCUNE table d'affiliation n'est exposée à anon ; la vue affiliate_dashboard (security_invoker=true) ne doit PAS générer d'advisor security_definer_view ; advisors préexistants (has_active_subscription/is_superadmin) hors scope.
    4. `generate_typescript_types` → écrase packages/supabase/src/database.types.ts.
    5. RÉ-APPLIQUER MANUELLEMENT (Pitfall 5 / D-05-02-F) dans packages/supabase/src/database.types.ts : override `base_atomic: string` / `amount_atomic: string` sur commissions ET payouts (Row+Insert+Update, commentaire CR-02) ; ajouter en fin de fichier les alias AffiliateRow/Insert, AffiliateCodeRow/Insert, ReferralRow/Insert, CommissionRow/Insert, PayoutRow/Insert, AffiliateApplicationRow/Insert (miroir bloc existant).
    6. re-run `pnpm typecheck` → 0 erreur.
    Marqué autonomous:false — requiert l'opérateur pour le MCP apply_migration. Resume-signal : `applied` + sortie list_tables/get_advisors collée, OU description du blocage.
  </action>
  <verify>
    <automated>grep -c "AffiliateRow\|CommissionRow\|PayoutRow" "packages/supabase/src/database.types.ts"</automated>
  </verify>
  <acceptance_criteria>
    - list_tables confirme les 6 tables + index commissions_aff_ref_period_idx + RLS enabled.
    - get_advisors security : aucune table d'affiliation anon-readable ; pas de nouvel advisor security_definer_view sur affiliate_dashboard.
    - database.types.ts contient `base_atomic: string` et `amount_atomic: string` (commissions + payouts) ET les alias AffiliateRow/CommissionRow/PayoutRow/ReferralRow/AffiliateApplicationRow.
    - `pnpm typecheck` 0 erreur après ré-alignement.
  </acceptance_criteria>
  <done>0016 appliquée LIVE (list_tables + get_advisors confirmés) ; database.types.ts ré-aligné (string *_atomic + alias) ; typecheck vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job/service_role → DB | le job de commission écrit via RPC privilégié (bypass RLS) |
| affilié authentifié → DB | lecture dashboard via vue security_invoker, isolation par auth.uid() |
| superadmin → DB | écritures payout via RPC, re-validées côté action (Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SELFREF | Tampering/Fraud | compute_affiliate_commissions | mitigate | exclusion `r.user_id <> a.user_id` au calcul (D-12, inviolable) |
| T-07-DOUBLEPAY | Tampering | commissions upsert / mark_commission_paid | mitigate | UNIQUE(affiliate_id,referral_id,period) + `on conflict do update where status='due'` ; mark_paid raise si row_count=0 |
| T-07-PII | Information Disclosure | vue affiliate_dashboard | mitigate | security_invoker=true, agrégats seuls (dont revenu cumul/mois), zéro user_id filleul (D-13) ; get_advisors au gate |
| T-07-RLS-ISO | Information Disclosure/Elevation | RLS toutes tables | mitigate | SELECT scopé auth.uid() + is_superadmin() ; aucune policy write (service_role bypass) |
| T-07-EXPIRED | Tampering/Fraud | compute (join subscriptions) | mitigate | join `status='active' AND current_period_end > now()` (AFF-05) |
| T-07-SC | Tampering | npm/pip installs | accept | aucun paquet npm dans ce plan (migration SQL pure) — N/A |
</threat_model>

<verification>
- 0016 appliquée LIVE (list_tables confirme 6 tables + RLS).
- get_advisors security : isolation affilié confirmée (aucune table anon-readable).
- Vue expose revenu cumul + mois courant (D-14).
- Logique du RPC validée fonctionnellement par les tests d'intégration de 07-03 (key_link explicite).
- `pnpm typecheck` 0 erreur après ré-alignement des types.
</verification>

<success_criteria>
- 5 tables d'affiliation + payouts LIVE avec RLS isolation.
- RPC compute/mark_paid + grille bps + vue no-PII (revenu cumul + mois courant) en base.
- database.types.ts aligné (string *_atomic + alias).
</success_criteria>

<output>
Create `.planning/phases/07-affiliation-paliers/07-01-SUMMARY.md` when done
</output>
</output>
