---
phase: 07-affiliation-paliers
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/affiliate/tiers.ts
  - packages/core/src/affiliate/__tests__/tiers.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [AFF-03]

must_haves:
  truths:
    - "La grille de paliers est une source unique TS, golden-testée sur les 8 bornes de D-01"
    - "D-02: le palier (et donc le taux bps) est dérivé du NOMBRE D'INSCRITS via le code (affiliateRateBps(signups)), pas des abonnés actifs"
    - "600 inscrits → palier 3 → 1400 bps (14 %) ; 0 inscrit → 0 bps ; 50000 → 2000 bps (20 %)"
    - "Le calcul de commission BigInt = (base_atomic × rate_bps) / 10000 par troncature entière, zéro float"
  artifacts:
    - path: "packages/core/src/affiliate/tiers.ts"
      provides: "TIERS (grille D-01), affiliateRateBps(signups), computeCommissionAtomic(base, bps)"
      exports: ["affiliateRateBps", "computeCommissionAtomic", "TIERS"]
    - path: "packages/core/src/affiliate/__tests__/tiers.test.ts"
      provides: "tests golden des 8 bornes + arrondi floor + zéro float"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "affiliate/tiers"
      via: "barrel export"
      pattern: "affiliate/tiers"
---

<objective>
Porter la grille de paliers D-01 et le calcul de commission BigInt en logique pure golden-testée dans `@app/core` (zéro I/O), miroir exact de la fonction SQL `affiliate_rate_bps`. Source unique TS, réutilisable par le dashboard (affichage palier/progression) et alignée bit-à-bit sur le RPC.

Purpose: La grille existe en double (SQL pour le calcul, TS pour l'affichage du dashboard). Les figer toutes deux sur la MÊME table de bornes golden-testée empêche toute dérive (un palier mal affiché vs payé serait un bug financier). Le calcul pur permet de tester l'arrondi floor (Q3) sans réseau.
Output: `tiers.ts` pur + tests golden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-affiliation-paliers/07-CONTEXT.md
@.planning/phases/07-affiliation-paliers/07-RESEARCH.md

<interfaces>
RESEARCH §Pattern 4 (L.315-328) : grille SQL affiliate_rate_bps (basis points) — la table de bornes à reproduire EXACTEMENT en TS.
Précédent de portage pur en core : threshold.ts (D-06-01-A) — même pattern « source unique partagée vitrine ↔ job ».
packages/core/src/index.ts : barrel à étendre (miroir exports replayOutcome / threshold).
</interfaces>
</context>

<feature>
  <name>Grille de paliers + commission BigInt (logique pure)</name>
  <files>packages/core/src/affiliate/tiers.ts, packages/core/src/affiliate/__tests__/tiers.test.ts</files>
  <behavior>
    - `TIERS` : tableau readonly des 8 paliers D-01 `{ tier, minSignups, maxSignups, rateBps }` (8% = 800 bps … 20% = 2000 bps).
    - `affiliateRateBps(signups: number): number` :
      - signups 0 → 0 ; 1..99 → 800 ; 100..500 → 1200 ; 501..1000 → 1400 ; 1001..5000 → 1500 ; 5001..10000 → 1600 ; 10001..25000 → 1700 ; 25001..50000 → 1800 ; ≥50000 → 2000.
      - Tester CHAQUE borne haute et basse (1, 99, 100, 500, 501, 1000, 1001, 5000, 5001, 10000, 10001, 25000, 25001, 50000, 50001).
      - 600 → 1400 (palier 3, exemple Pitfall 2).
    - `computeCommissionAtomic(baseAtomic: bigint, rateBps: number): bigint` = `(baseAtomic * BigInt(rateBps)) / 10000n` (division entière BigInt = troncature/floor, Q3). Tester : base 9_000_000n (9 USDT) × 1400 bps → 1_260_000n ; arrondi floor sur un cas non divisible (ex. base 1n × 800 → 0n).
    - Aucune utilisation de `Number()` sur des montants, aucun float.
  </behavior>
  <implementation>
    Créer le dossier `packages/core/src/affiliate/`. Écrire `tiers.ts` (RED→GREEN→REFACTOR). La table TIERS est la source ; `affiliateRateBps` la parcourt. Étendre `packages/core/src/index.ts` pour exporter `affiliateRateBps`, `computeCommissionAtomic`, `TIERS` et leurs types (miroir export threshold).
  </implementation>
  <verify>
    <automated>npx vitest run packages/core/src/affiliate/__tests__/tiers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RED prouvé : le test échoue avant `tiers.ts` (« No test files found » impossible — le fichier de test existe ; assertion échoue car module absent).
    - GREEN : `npx vitest run packages/core/src/affiliate/__tests__/tiers.test.ts` passe, ≥15 assertions de bornes.
    - `affiliateRateBps(600) === 1400` et `affiliateRateBps(50001) === 2000` et `affiliateRateBps(0) === 0`.
    - `computeCommissionAtomic(9_000_000n, 1400) === 1_260_000n`.
    - `grep -c "Number(" packages/core/src/affiliate/tiers.ts` == 0.
    - `pnpm typecheck` 0 erreur ; barrel `@app/core` exporte affiliateRateBps.
  </acceptance_criteria>
  <done>Grille pure + commission BigInt golden-testées sur les 8 bornes, exportées de @app/core, zéro float.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune) | logique pure zéro I/O, pas de frontière de confiance traversée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-FLOAT | Tampering | computeCommissionAtomic | mitigate | BigInt exclusif, division entière déterministe ; test golden floor (Q3) ; grep Number==0 |
| T-07-DRIFT | Tampering | TIERS vs affiliate_rate_bps SQL | mitigate | table de bornes golden-testée identique aux bps SQL (07-01) ; toute divergence casse un test |
| T-07-SC | Tampering | npm installs | accept | aucun paquet npm ajouté (logique pure dans package existant) |
</threat_model>

<verification>
- Tests golden verts sur les 8 paliers + arrondi floor.
- `pnpm typecheck` 0 erreur, suite core non régressée.
</verification>

<success_criteria>
- Grille D-01 et commission BigInt en source unique pure, golden-testées, exportées de @app/core.
</success_criteria>

<output>
Create `.planning/phases/07-affiliation-paliers/07-02-SUMMARY.md` when done
</output>
</output>
