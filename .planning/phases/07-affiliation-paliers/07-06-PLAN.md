---
phase: 07-affiliation-paliers
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-05"]
files_modified:
  - apps/web/src/app/[locale]/affiliation/page.tsx
  - apps/web/src/app/[locale]/affiliation/actions.ts
  - apps/web/src/app/[locale]/(affiliate)/dashboard/page.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
  - apps/web/src/messages/__tests__/messages-parity-affiliate.test.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-05]

must_haves:
  truths:
    - "Un visiteur/membre soumet une candidature (réseaux sociaux, Telegram, Facebook, nb abonnés, interactions) → insert affiliate_applications via service_role"
    - "D-08: onboarding = candidature → file de revue superadmin avec vérification manuelle (statut pending), pas d'auto-évaluation"
    - "L'affilié voit son dashboard : palier/taux, inscrits, abonnés actifs ramenés, revenus générés (cumul + mois courant), commissions dues/payées — AUCUNE ligne par filleul (no-PII strict D-13)"
    - "Le dashboard lit la vue affiliate_dashboard en anon/auth-client (RLS isolation, jamais service_role)"
    - "Le namespace i18n affiliate est à parité stricte fr/en/ar"
  artifacts:
    - path: "apps/web/src/app/[locale]/(affiliate)/dashboard/page.tsx"
      provides: "dashboard no-PII agrégats seuls (requireRole affiliate), revenu cumul + mois courant (D-14)"
      contains: "requireRole"
    - path: "apps/web/src/app/[locale]/affiliation/page.tsx"
      provides: "formulaire de candidature (form + zod)"
      contains: "affiliate"
    - path: "apps/web/src/messages/__tests__/messages-parity-affiliate.test.ts"
      provides: "garde de parité du namespace affiliate"
  key_links:
    - from: "apps/web/src/app/[locale]/(affiliate)/dashboard/page.tsx"
      to: "vue affiliate_dashboard (revenue_total_atomic + revenue_current_month_atomic)"
      via: "anon/auth-client (RLS), jamais service_role — lecture des colonnes revenu cumul/mois D-14"
      pattern: "affiliate_dashboard"
    - from: "apps/web/src/app/[locale]/affiliation/actions.ts"
      to: "affiliate_applications"
      via: "server action service_role (Open Q1)"
      pattern: "createAdminServiceClient"
---

<objective>
Surfaces membre `[locale]` (trilingue + RTL) : page de candidature (formulaire D-08, insert via service_role) et dashboard affilié no-PII (agrégats seuls via la vue RLS, grille 8 paliers + progression, revenus cumul + mois courant). Namespace i18n `affiliate` trilingue + test de parité.

Purpose: AFF-01 (candidature, point d'entrée du programme), AFF-02 (dashboard sans PII des filleuls — la vue ne renvoie que des compteurs, D-13), AFF-05 visible (abonnés actifs ramenés distincts des inscrits, découplage audience/argent D-02). D-14 : revenus générés affichés en cumul ET mois courant (la vue 07-01 expose revenue_total_atomic + revenue_current_month_atomic).
Output: 2 surfaces membre + namespace affiliate ×3 + parité.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-affiliation-paliers/07-UI-SPEC.md
@.planning/phases/07-affiliation-paliers/07-PATTERNS.md

<interfaces>
apps/web/src/messages/__tests__/messages-parity-payment.test.ts (analog) : garde parité récursive d'un namespace fr/en/ar + sentinelles + no-perf VITR-03.
apps/web/src/app/(admin)/file/page.tsx : `<bdi>{formatAtomic(BigInt(x))}` pour les montants ; pattern table RSC.
apps/web/src/lib/auth/gate.ts : `requireRole('affiliate')` → redirect '/' si pas affilié.
apps/web/src/lib/supabase/admin-service.ts : createAdminServiceClient (server-only) — pour l'insert candidature (Open Q1, pas de policy insert front).
@app/supabase : createServerSupabaseClient (anon/auth-client RSC) pour lire la vue affiliate_dashboard ; transitionApplication (07-03).
@app/core (07-02) : affiliateRateBps, TIERS — pour afficher palier/progression (jamais recalculer le montant, juste l'afficher).
Vue affiliate_dashboard (07-01) : total_signups, active_referrals, revenue_total_atomic, revenue_current_month_atomic, commissions_due_atomic, commissions_paid_atomic — agrégats string. WARNING 3 / D-14 : revenue_total_atomic = revenu brut cumulé des filleuls, revenue_current_month_atomic = mois courant.
UI-SPEC §Tier Grid & Progression + §Copywriting (namespace affiliate, empty states, disclaimer P2).
Composant `<Disclaimer>` RSC P2 (disclaimer.footer) — aucune promesse de revenu.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page de candidature [locale]/affiliation (form + zod + server action service_role)</name>
  <read_first>
    - apps/web/src/app/[locale]/(auth)/actions.ts (pattern server action form)
    - apps/web/src/app/(admin)/file/actions.ts (parse strict FormData, service_role)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Open Questions (RESOLVED) Q1 + Assumption A6
    - .planning/phases/07-affiliation-paliers/07-UI-SPEC.md §Surface 1 + §Copywriting (application.*)
    - apps/web/src/messages/__tests__/messages-parity-payment.test.ts (modèle test parité)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/affiliation/page.tsx` (RSC + form client react-hook-form + zod, pattern signup P2/P4) : champs D-08 (réseaux sociaux, canal Telegram, page Facebook, nb abonnés entier ≥0, niveau d'interactions). Validation zod client + serveur. CTA « Envoyer ma candidature » (primary). Toast `sonner` succès. Anti double-submit (disabled pendant pending). Réutiliser `<Disclaimer>` P2 (aucune promesse de revenu d'affiliation, LEGAL-01).
    Créer `apps/web/src/app/[locale]/affiliation/actions.ts` : `'use server'` + `'server-only'` ; `submitApplication(formData)` → validation zod serveur (≥1 réseau social, nb abonnés entier ≥0) → insert `affiliate_applications(status='pending')` via `createAdminServiceClient()` (Open Q1 RESOLVED : pas de policy insert front) ; capturer l'email (saisi ou de la session si connecté). Retour `{ok}` / erreur i18n. PAS de redirect (toast côté client).
    Ajouter le namespace `affiliate.application.*` dans fr.json/en.json/ar.json (parité STRICTE) : labels champs, submit, success, errors (submitFailed UI-SPEC).
  </action>
  <verify>
    <automated>grep -c "affiliate_applications" "apps/web/src/app/[locale]/affiliation/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - actions.ts insère `affiliate_applications` via `createAdminServiceClient()` avec `status` pending (Open Q1).
    - Validation zod serveur présente (≥1 réseau social, nb abonnés entier).
    - page.tsx : CTA primary « Envoyer ma candidature » (clé i18n), Disclaimer P2 réutilisé, aucune classe vert/rouge.
    - fr/en/ar contiennent `affiliate.application` à parité.
    - `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Candidature : formulaire validé zod, insert affiliate_applications via service_role, disclaimer sans promesse de revenu.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard affilié no-PII [locale]/(affiliate)/dashboard (revenu cumul/mois) + parité affiliate</name>
  <read_first>
    - apps/web/src/app/(admin)/file/page.tsx (`<bdi>{formatAtomic(BigInt(x))}`, table RSC)
    - apps/web/src/lib/auth/gate.ts (requireRole('affiliate') L.99-118)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Code Examples vue (revenue_total_atomic / revenue_current_month_atomic) + §Pitfall 6 (no-PII)
    - .planning/phases/07-affiliation-paliers/07-UI-SPEC.md §Tier Grid & Progression + §No-PII strict
    - packages/core (07-02) : TIERS, affiliateRateBps (affichage palier/progression)
    - apps/web/src/messages/__tests__/messages-parity-payment.test.ts (modèle test parité à dupliquer)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/(affiliate)/dashboard/page.tsx` : `requireRole('affiliate')` en tête ; lire la vue `affiliate_dashboard` via `createServerSupabaseClient` (anon/auth-client RSC — JAMAIS service_role, RLS isolation Pitfall 6) → un seul objet d'agrégats (l'affilié courant). Rendre les 6 tuiles `card` (D-14) : palier & taux actuel (badge accent, dérivé de total_signups via affiliateRateBps de @app/core), inscrits via le code, abonnés actifs ramenés (distinct, tooltip découplage D-02), revenus générés (`tabs` cumul/mois — WARNING 3 / D-14 : lire `revenue_total_atomic` pour le cumul et `revenue_current_month_atomic` pour le mois courant, tous deux `<bdi>` + `formatAtomic(BigInt(...))`), commissions dues (ambre), commissions payées (neutre). Grille 8 paliers en `table` lecture seule (TIERS), palier actuel surligné `bg-primary/10`. `progress` vers le palier suivant (ou « Palier maximum atteint » au palier 8). `@tanstack/react-query` pour le fetch + `skeleton` loading. Empty state UI-SPEC (« Pas encore de filleuls »). ZÉRO ligne/identifiant par filleul (D-13). Vert/rouge interdits.
    Ajouter `affiliate.dashboard.*` (dont labels `revenueTotal` / `revenueCurrentMonth`) + `affiliate.tiers.*` dans fr/en/ar (parité stricte). Créer `apps/web/src/messages/__tests__/messages-parity-affiliate.test.ts` (miroir messages-parity-payment : parité récursive fr/en/ar du namespace affiliate + sentinelles sous-clés + garde no-perf VITR-03).
  </action>
  <verify>
    <automated>npx vitest run apps/web/src/messages/__tests__/messages-parity-affiliate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx : `requireRole('affiliate')` + lecture `affiliate_dashboard` via `createServerSupabaseClient` (PAS createAdminServiceClient — `grep -c "createAdminServiceClient" dashboard/page.tsx` == 0).
    - dashboard/page.tsx lit `revenue_total_atomic` ET `revenue_current_month_atomic` (D-14 revenu cumul + mois courant) : `grep -c "revenue_total_atomic\|revenue_current_month_atomic" dashboard/page.tsx` >= 2.
    - Aucune boucle/table sur des referrals individuels : aucun `user_id` de filleul rendu (`grep -ci "referral.*map\|filleul.*id" dashboard/page.tsx` == 0).
    - Montants en `<bdi>` + `formatAtomic(BigInt(...))` ; grille TIERS de @app/core ; `grep -c "green-\|red-" dashboard/page.tsx` == 0.
    - messages-parity-affiliate.test.ts vert (parité fr/en/ar stricte).
    - `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Dashboard no-PII : agrégats seuls via vue RLS (auth-client), palier/progression, revenus générés cumul + mois courant (D-14), parité affiliate trilingue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidat → server action | input formulaire non fiable, validé zod serveur |
| affilié → vue dashboard | lecture RLS-scopée (auth.uid()), agrégats seuls |
| candidature → DB | insert via service_role (pas de policy front, Open Q1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-PII | Information Disclosure | dashboard | mitigate | vue security_invoker=true agrégats seuls (D-13, dont revenu cumul/mois) ; aucune boucle par filleul ; lecture auth-client (jamais service_role) |
| T-07-RLS-ISO | Information Disclosure | dashboard | mitigate | RLS `auth.uid()` — un affilié ne lit jamais un autre (test isolation 07-03 affiliate-rls / get_advisors) |
| T-07-APP-INPUT | Tampering/Injection | submitApplication | mitigate | validation zod serveur ; valeurs paramétrées (PostgREST), jamais concaténées |
| T-07-NOPERF | — | copy affiliate | mitigate | aucune promesse de revenu (Disclaimer P2) ; garde no-perf VITR-03 dans le test parité |
| T-07-SC | Tampering | npm installs | accept | aucun paquet npm ajouté (react-query/zod/composants déjà installés) |
</threat_model>

<verification>
- Dashboard lit la vue en auth-client (RLS), zéro PII filleul, revenu cumul + mois courant affichés (D-14).
- Parité affiliate fr/en/ar verte ; no-perf garde verte.
- `pnpm typecheck` 0 erreur ; `lint:i18n` exit 0 ; suite complète non régressée.
- Gate de phase : `get_advisors security` confirme l'isolation (aucune fuite affilié).
</verification>

<success_criteria>
- Candidature (AFF-01) + dashboard no-PII agrégats seuls (AFF-02, D-13) avec revenus cumul + mois courant (D-14) + découplage audience/argent visible (D-02/AFF-05).
</success_criteria>

<output>
Create `.planning/phases/07-affiliation-paliers/07-06-SUMMARY.md` when done
</output>
</output>
