---
phase: 07-affiliation-paliers
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - packages/supabase/src/repositories/affiliates.ts
  - packages/supabase/src/repositories/affiliateApplications.ts
  - packages/supabase/src/repositories/commissions.ts
  - packages/supabase/src/repositories/referrals.ts
  - packages/supabase/src/repositories/__tests__/affiliates.test.ts
  - packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts
  - packages/supabase/src/index.ts
  - apps/jobs/src/jobs/affiliate-commission.ts
  - apps/jobs/src/dispatch.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-03, AFF-04, AFF-05]

must_haves:
  truths:
    - "attributeReferral résout code→affilié, no-op si inconnu, skip self-ref, idempotent sur 23505, ne lève jamais (best-effort)"
    - "Le repo commissions enveloppe les RPC compute_affiliate_commissions et mark_commission_paid (jamais de calcul en JS)"
    - "Le job affiliate-commission appelle le RPC pour le mois calendaire UTC ; re-run = même total (idempotent)"
    - "affiliate-commission est enregistré dans dispatch.ts (trace job_runs via runJob)"
    - "L'isolation RLS cross-user est prouvée : un affilié A ne lit jamais les données (referrals/commissions/vue) d'un affilié B (AFF-02)"
  artifacts:
    - path: "packages/supabase/src/repositories/affiliates.ts"
      provides: "attributeReferral, promoteAffiliate, createCode (service_role)"
      exports: ["attributeReferral"]
    - path: "packages/supabase/src/repositories/commissions.ts"
      provides: "computeCommissions(client, period), markCommissionPaid(client, input) via RPC"
      exports: ["computeCommissions", "markCommissionPaid"]
    - path: "packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts"
      provides: "Test d'isolation RLS cross-user (anon/auth-client) : A ne lit pas les données de B — AFF-02 isolation prouvée"
    - path: "apps/jobs/src/jobs/affiliate-commission.ts"
      provides: "job mensuel idempotent (Promise<Json>)"
      exports: ["affiliateCommission"]
  key_links:
    - from: "apps/jobs/src/jobs/affiliate-commission.ts"
      to: "compute_affiliate_commissions (RPC)"
      via: "client.rpc"
      pattern: "rpc.*compute_affiliate_commissions"
    - from: "apps/jobs/src/dispatch.ts"
      to: "affiliateCommission"
      via: "JOB_REGISTRY"
      pattern: "affiliate-commission.*affiliateCommission"
    - from: "packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts"
      to: "RLS des tables affiliation + vue affiliate_dashboard (07-01)"
      via: "anon/auth-client cross-user — A authentifié lit 0 ligne de B"
      pattern: "affiliate_dashboard|referrals|commissions"
---

<objective>
Écrire la couche service_role : repos d'attribution (`attributeReferral`), repos de commission (wrappers RPC), repos candidatures/codes, le job mensuel idempotent `affiliate-commission` (miroir outcome-tracker), ET le test d'isolation RLS cross-user prouvant AFF-02. Tout le calcul reste en DB (RPC 07-01) ; les repos sont des wrappers minces.

Purpose: Frontière producteur-unique : aucune écriture front. L'attribution (AFF-01), le calcul (AFF-03/05) et le payout (AFF-04) passent par service_role/RPC. Le job rend le calcul exécutable mensuellement et traçable (job_runs). Le test d'isolation cross-user prouve la barrière RLS exigée par AFF-02 (« isolation RLS prouvée », Pitfall 6).
Output: 4 repos + barrel + job + registre dispatch + test RLS isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-affiliation-paliers/07-RESEARCH.md
@.planning/phases/07-affiliation-paliers/07-PATTERNS.md

<interfaces>
packages/supabase/src/repositories/payments.ts (analog) :
- `type ServiceClient = SupabaseClient<Database>` ; en-tête « JAMAIS importé depuis apps/web »
- `const PG_UNIQUE_VIOLATION = '23505'` + `isUniqueViolation(err)`
- `.toString()` à l'écriture bigint, jamais Number()

packages/supabase/src/repositories/subscriptions.ts (analog RPC wrapper) :
- `activateForPayment(client, input)` enveloppe `client.rpc('activate_subscription_for_payment', { p_payment_id, p_user_id, p_plan, p_period })` ; interface ActivateForPaymentInput explicite

apps/jobs/src/jobs/outcome-tracker.ts (analog job) :
- `import 'dotenv/config'` ; `getServiceClient()` lazy (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, throw explicite) ; `createClient<Database>(...)` ; `export async function outcomeTracker(): Promise<Json>` ; retour stats `as Json`

apps/jobs/src/dispatch.ts (L.30, L.57-58) : import + entrée JOB_REGISTRY commentée par cadence ; runJob trace job_runs.

Test d'isolation RLS (analog) : `apps/web/test/**` ou `packages/**/__tests__/gating-rls.test.ts` (P1) + isolation `subscriptions` cross-user — créer 2 affiliés via service_role, puis lire avec un auth-client scopé sur A et asserter 0 ligne de B.

RESEARCH §Code Examples (attributeReferral) : squelette complet.
RESEARCH §Pattern 3 (job) : squelette complet affiliateCommission (luxon `DateTime.utc().toFormat('yyyy-MM')`).
RESEARCH §Pitfall 6 + §Validation Architecture : test cross-user obligatoire pour AFF-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repos service_role (affiliates, referrals, commissions, applications) + barrel + tests + isolation RLS cross-user</name>
  <read_first>
    - packages/supabase/src/repositories/payments.ts (ServiceClient, isUniqueViolation, PG_UNIQUE_VIOLATION '23505', toString bigint, en-tête « JAMAIS apps/web »)
    - packages/supabase/src/repositories/subscriptions.ts (activateForPayment : wrapper RPC + input interface)
    - packages/supabase/src/index.ts (barrel — bloc payments/subscriptions à imiter)
    - packages/supabase/src/repositories/__tests__/ (analog test RLS cross-user : gating-rls / subscriptions isolation)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Code Examples attributeReferral + §Pitfall 6 (isolation)
    - .planning/phases/07-affiliation-paliers/07-PATTERNS.md sections affiliates/commissions/referrals
  </read_first>
  <behavior>
    - attributeReferral(client, {affiliate_code, referral_user_id}) : lookup affiliate_codes(code) → maybeSingle ; code inconnu → {attributed:false} ; self-ref (affiliate.user_id === referral_user_id) → {attributed:false} ; insert referrals ; 23505 → {attributed:true} (idempotent) ; autre erreur → throw. NE lève jamais sur code inconnu/self-ref (best-effort).
    - promoteAffiliate(client, {user_id}) : upsert affiliates(user_id) + set profiles.role='affiliate' (idempotent sur unique user_id).
    - createCode(client, {affiliate_id, code}) : normalise toUpperCase().trim(), insert affiliate_codes ; 23505 → throw mappable CODE_TAKEN.
    - computeCommissions(client, period) : `client.rpc('compute_affiliate_commissions', { p_period: period })`.
    - markCommissionPaid(client, {commission_id, tx_hash, amount_atomic}) : `client.rpc('mark_commission_paid', { p_commission_id, p_tx_hash, p_amount_atomic })` ; amount_atomic en string bigint.
    - listPendingApplications(client) / transitionApplication(client, id, status, {reject_reason}) (miroir transitionPayment).
    - Isolation RLS (affiliate-rls.test.ts) : un auth-client scopé sur affilié A (auth.uid()=A) ne renvoie AUCUNE ligne de B sur `referrals`, `commissions`, et la vue `affiliate_dashboard` ; le superadmin voit tout. (AFF-02 « isolation RLS prouvée », Pitfall 6.)
  </behavior>
  <action>
    Créer `affiliates.ts`, `referrals.ts`, `commissions.ts`, `affiliateApplications.ts` dans packages/supabase/src/repositories/. Copier les conventions payments.ts (ServiceClient, isUniqueViolation, en-tête « JAMAIS apps/web », CODE_TAKEN const). commissions.ts = wrappers minces RPC (miroir subscriptions.activateForPayment) — ZÉRO calcul JS. Étendre `packages/supabase/src/index.ts` (bloc « Phase 7 — affiliation ») : exporter attributeReferral, promoteAffiliate, createCode, computeCommissions, markCommissionPaid, listPendingApplications, transitionApplication, countReferrals + types d'input. NE PAS exporter service-client (garde ESLint).

    Tests dans `__tests__/affiliates.test.ts` : attributeReferral avec client mocké (code inconnu→no-op, self-ref→skip, 23505→idempotent) ; markCommissionPaid : forme d'appel RPC exacte.

    Tests d'isolation dans `__tests__/affiliate-rls.test.ts` (WARNING 5 — AFF-02 « isolation RLS prouvée », miroir gating-rls / isolation subscriptions cross-user) : via service_role, semer 2 affiliés A et B + leurs referrals/commissions. Construire un auth-client (anon key + JWT/session de A, PAS service_role). Asserter que A ne lit AUCUNE ligne appartenant à B sur `referrals`, `commissions`, et la vue `affiliate_dashboard` (résultat scopé = uniquement A, jamais B) ; asserter qu'un client superadmin voit A et B. Cleanup en teardown. Si l'environnement n'a pas de session réelle disponible en CI, marquer ce fichier `describe.skipIf(!process.env.SUPABASE_SERVICE_ROLE_KEY)` (miroir des tests réseau Supabase existants) — mais la structure d'assertion cross-user DOIT être présente et exécutable localement.
  </action>
  <verify>
    <automated>npx vitest run packages/supabase/src/repositories/__tests__/affiliates.test.ts packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - attributeReferral : test « code inconnu → {attributed:false} sans throw », « self-ref → skip », « 23505 → {attributed:true} » verts.
    - `grep -c "Number(" packages/supabase/src/repositories/commissions.ts` == 0.
    - commissions.ts contient `rpc('compute_affiliate_commissions'` ET `rpc('mark_commission_paid'` (aucun INSERT/SELECT de calcul).
    - barrel `@app/supabase` exporte attributeReferral + computeCommissions + markCommissionPaid : `grep -c "attributeReferral" packages/supabase/src/index.ts` >= 1.
    - affiliate-rls.test.ts existe et asserte l'isolation cross-user : `grep -c "affiliate_dashboard\|referrals\|commissions" packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts` >= 1 ET une assertion « A ne lit pas B » (ex. `toHaveLength(0)` / `.not.toContain(B)` sur les lignes de B). Lecture NON service_role (auth-client) : `grep -c "service_role\|SERVICE_ROLE" ...` sur le bloc de lecture A == 0 (le seeding peut utiliser service_role, la LECTURE testée non).
    - `pnpm typecheck` 0 erreur.
  </acceptance_criteria>
  <done>4 repos service_role écrits (attribution best-effort, RPC wrappers sans calcul JS), exportés au barrel, tests verts + isolation RLS cross-user prouvée (AFF-02).</done>
</task>

<task type="auto">
  <name>Task 2: Job mensuel affiliate-commission + enregistrement dispatch</name>
  <read_first>
    - apps/jobs/src/jobs/outcome-tracker.ts (squelette job : dotenv, getServiceClient lazy, Promise<Json>, retour stats)
    - apps/jobs/src/dispatch.ts (L.30 import outcomeTracker, L.57-58 entrée registre commentée, runJob)
    - .planning/phases/07-affiliation-paliers/07-RESEARCH.md §Pattern 3 + §Pitfall 4 (bornes UTC)
    - packages/supabase/src/index.ts (export computeCommissions de Task 1)
  </read_first>
  <action>
    Créer `apps/jobs/src/jobs/affiliate-commission.ts` (miroir outcome-tracker) : `import 'dotenv/config'`, `getServiceClient()` lazy (réutilise le pattern SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY avec throw explicite), `export async function affiliateCommission(): Promise<Json>`. Calculer `period = DateTime.utc().toFormat('yyyy-MM')` (luxon — JAMAIS new Date(), Pitfall 4) ou lire `process.argv[3]` si fourni (re-calcul d'un mois passé). Appeler `computeCommissions(client, period)` (Task 1) ; throw sur error ; retour `{ period, ...stats } as Json`. AUCUN traçage manuel (runJob écrit job_runs). Dans `dispatch.ts` : ajouter l'import en tête (miroir L.30) et l'entrée registre `'affiliate-commission': affiliateCommission` avec commentaire de cadence « AFF-03 — calcul mensuel idempotent. run-job.cmd affiliate-commission » (miroir L.57-58).
  </action>
  <verify>
    <automated>cd apps/jobs && npx tsx -e "import('./src/jobs/affiliate-commission.ts').then(m=>console.log(typeof m.affiliateCommission))" 2>&1 | grep -q function && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - affiliate-commission.ts contient `DateTime.utc().toFormat('yyyy-MM')` et `rpc`/`computeCommissions` (via Task 1) — `grep -c "new Date(" apps/jobs/src/jobs/affiliate-commission.ts` == 0.
    - dispatch.ts contient `'affiliate-commission': affiliateCommission` et l'import.
    - `pnpm typecheck` 0 erreur.
    - Documenter (dans le SUMMARY, hors-code) : ajouter une tâche Windows Task Scheduler mensuelle `run-job.cmd affiliate-commission` (ops, Runtime State Inventory).
  </acceptance_criteria>
  <done>Job mensuel idempotent qui appelle le RPC pour le mois UTC courant, enregistré au dispatch, tracé par job_runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job → DB | service_role bypass RLS via RPC (calcul de commission) |
| signUp server action → DB | attributeReferral via service_role (consommé en 07-04) |
| affilié auth-client → DB | lecture scopée RLS (test isolation cross-user) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-DOUBLEPAY | Tampering | job re-run | mitigate | toute idempotence dans le RPC (UNIQUE + on conflict) ; le job ne fait qu'appeler — re-run = même total |
| T-07-SELFREF | Tampering/Fraud | attributeReferral | mitigate | skip self-ref + exclusion finale au calcul (D-12) |
| T-07-ATTR-CRASH | Denial of Service | attributeReferral | mitigate | best-effort : code inconnu/self-ref → no-op, ne casse jamais le signup |
| T-07-FLOAT | Tampering | commissions.ts | mitigate | wrappers RPC sans calcul JS ; grep Number==0 ; amount_atomic string |
| T-07-TZ | Tampering | affiliate-commission period | mitigate | luxon UTC `toFormat('yyyy-MM')`, jamais new Date() (Pitfall 4) |
| T-07-RLS-ISO | Information Disclosure/Elevation | RLS tables affiliation | mitigate | test cross-user affiliate-rls.test.ts prouve A ne lit pas B (AFF-02, Pitfall 6) |
| T-07-SC | Tampering | npm installs | accept | aucun paquet npm ajouté (luxon/tsx déjà présents) |
</threat_model>

<verification>
- Tests repos verts (dont isolation RLS cross-user) ; `pnpm typecheck` 0 erreur.
- Job importable, enregistré au dispatch.
- Suite complète (`npx vitest run`) non régressée P1–P6.
</verification>

<success_criteria>
- Attribution best-effort + RPC wrappers + job idempotent mensuel câblés + isolation RLS cross-user prouvée (AFF-02).
</success_criteria>

<output>
Create `.planning/phases/07-affiliation-paliers/07-03-SUMMARY.md` when done
</output>
