---
phase: 06-canal-telegram-public
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - apps/jobs/package.json
  - apps/jobs/src/telegram/bot.ts
  - apps/jobs/src/jobs/telegram-publish.ts
  - apps/jobs/src/jobs/__tests__/telegram-publish.test.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/.env.example
  - .env.example
autonomous: false
requirements: [TG-01, TG-03]
user_setup:
  - service: telegram
    why: "Publication automatique sur le canal Telegram public (TG-01)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather → /newbot → token (apps/jobs/.env uniquement, jamais en code/DB)"
      - name: TELEGRAM_CHANNEL_ID
        source: "ID numérique -100… du canal (préféré au @username)"
    dashboard_config:
      - task: "Créer le bot via @BotFather, créer le canal public, ajouter le bot comme ADMINISTRATEUR avec droit « Post Messages »"
        location: "Telegram (@BotFather + paramètres du canal)"
must_haves:
  truths:
    - "Le job telegram-publish, exécuté chaque heure après outcome-tracker, sélectionne les trades fraîchement résolus (resolved_at) et décide du format à poster"
    - "À chaque run il poste les notables (realized_r ≥ 2.0) fraîchement résolus ; au run ~21h UTC le récap quotidien ; le vendredi le post win-rate-seul"
    - "Un jour sans trade clos poste quand même « aucun trade » + win rate (D-10) ; jamais de skip silencieux"
    - "Un 2e run consécutif n'envoie aucun message en double (getPostedKeys + UNIQUE dedupe_key)"
    - "Le token/channel_id ne sont lus que depuis apps/jobs/.env, jamais loggés ni mis dans job_runs.stats"
    - "La traçabilité job_runs est assurée par runJob — rien de codé à la main"
  artifacts:
    - path: "apps/jobs/src/jobs/telegram-publish.ts"
      provides: "Job publication-only idempotent service_role (clone outcome-tracker)"
      exports: ["telegramPublish"]
      min_lines: 60
    - path: "apps/jobs/src/telegram/bot.ts"
      provides: "Client grammY publication-only (getBot env-throw + sendPost p-retry)"
      exports: ["getBot", "getChannelId", "sendPost"]
    - path: "apps/jobs/src/jobs/__tests__/telegram-publish.test.ts"
      provides: "Test idempotence + format + jour vide + notable gate (mock client + mock grammy)"
  key_links:
    - from: "apps/jobs/src/dispatch.ts"
      to: "telegramPublish"
      via: "JOB_REGISTRY entry"
      pattern: "'telegram-publish': telegramPublish"
    - from: "apps/jobs/src/jobs/telegram-publish.ts"
      to: "formatMessage"
      via: "import @app/core"
      pattern: "formatMessage"
    - from: "apps/jobs/src/jobs/telegram-publish.ts"
      to: "telegram_posts"
      via: "insertPost / getPostedKeys @app/supabase"
      pattern: "getPostedKeys|insertPost"
---

<objective>
Assembler le job `telegram-publish` (TG-01) : installer grammY 1.43.0 (publication-only), écrire le client bot (env-throw + p-retry), le job idempotent service_role (clone `outcome-tracker.ts`) qui sélectionne les trades clos sur `resolved_at` (Open Q1 tranchée), décide du format (D-01), formate via `formatMessage` (06-01), envoie via grammY puis insère `telegram_posts` (06-02), l'enregistrer dans `dispatch.ts`, le tester sans réseau (mocks), et franchir le checkpoint d'envoi réel sur le canal.

Purpose: Livrer la publication automatique idempotente et tracée (TG-01/TG-03) — le « fil de l'eau » MENA FR+AR.
Output: grammY installé, client bot, job + test mockés verts, dispatch câblé, .env.example documenté, checkpoint envoi réel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-canal-telegram-public/06-CONTEXT.md
@.planning/phases/06-canal-telegram-public/06-RESEARCH.md
@.planning/phases/06-canal-telegram-public/06-PATTERNS.md

<interfaces>
<!-- Contrats exacts — produits par 06-01 / 06-02 et l'existant. -->

From @app/core (06-01 Task 1+3):
```typescript
export const MIN_SAMPLE: number
export function applyThreshold(row: StatRow): ThresholdResult
export function formatMessage(input): string        // bilingue FR+AR, HTML, < 4096
export function escapeHtml(s: string): string
export type Outcome = { outcome: 'hit_tp'|'hit_sl'|'flat'; realized_r: number }
export const UTC_ZONE = 'UTC'
```

From @app/supabase (06-02 + 06-01 Task 2 + existant):
```typescript
export function getPatternStats(client): Promise<{ rows: PatternStatRow[]; error: string|null }>
export function insertPost(client: ServiceClient, row: TelegramPostInsert): Promise<void>     // onConflict dedupe_key
export function getPostedKeys(client: ServiceClient): Promise<Set<string>>
export type { Json, Database, TelegramPostInsert }
// pattern_stats global row = dimension==='overall' && bucket==='all' && period==='all_time'
```

From apps/jobs/src/jobs/outcome-tracker.ts (analog STRUCTURAL à cloner — l.20-44 getServiceClient, l.68-118 shape):
```typescript
function getServiceClient() { /* env-throw SUPABASE_URL/SERVICE_ROLE_KEY, createClient<Database> persistSession:false */ }
export async function telegramPublish(): Promise<Json> { /* return { posted, skipped } as Json */ }
```

From apps/jobs/src/dispatch.ts (registry — import block l.16-30, JOB_REGISTRY l.36-53):
```typescript
import { outcomeTracker } from './jobs/outcome-tracker'
const JOB_REGISTRY: Record<string, () => Promise<Json | undefined>> = { /* ... */ 'outcome-tracker': outcomeTracker }
```

From packages/data-sources/src/finnhub/client.ts (p-retry analog l.39-68):
```typescript
import pRetry from 'p-retry'
await pRetry(() => fn(), { retries: 3 })
```

grammY publication-only (RESEARCH Code Examples l.410-435) — `new Bot(token)`, JAMAIS bot.start() ; `bot.api.sendMessage(chatId, html, { parse_mode: 'HTML', disable_web_page_preview: true })` → retourne `{ message_id }`.

Sélection trades clos (RESEARCH Pattern 6, Open Q1 = resolved_at):
```sql
select o.setup_id, o.outcome, o.realized_r, o.resolved_at, ts.direction, i.symbol
from prediction_outcomes o
join trade_setups ts on ts.id = o.setup_id
join instruments  i  on i.id = ts.instrument_id
where o.resolved_at >= :window_start
order by o.resolved_at desc
-- JAMAIS entry_price/stop_loss/take_profits (D-03)
```

Cadence (RESEARCH Pattern 7):
```typescript
import { DateTime } from 'luxon'
const now = DateTime.now().setZone('UTC')
const isFriday = now.weekday === 5          // luxon 1=lundi..7=dimanche
const isRecapHour = now.hour === RECAP_HOUR_UTC   // = 21 (D-08)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [vetting] Confirmer grammY 1.43.0 puis pnpm --filter jobs add grammy@1.43.0</name>
  <files>apps/jobs/package.json</files>
  <action>Checkpoint vetting léger (grammy verrouillé D-V2-02 + audité RESEARCH §Package Legitimacy) : confirmer grammy@1.43.0 sur npm (version dispo, absence de postinstall réseau via `npm view grammy@1.43.0 scripts`, repo officiel grammyjs), puis `pnpm --filter jobs add grammy@1.43.0` (version épinglée exacte, pas ^/~). Étapes dans how-to-verify.</action>
  <read_first>
    - 06-RESEARCH.md §Package Legitimacy Audit (grammy approuvé : 3.56M dl/sem, repo officiel grammyjs, pas de postinstall réseau, transitive deps minimales)
    - 06-RESEARCH.md §Standard Stack (grammy 1.43.0 pin, CLAUDE.md D-V2-02 verrouillé)
    - apps/jobs/package.json (cible de l'install)
  </read_first>
  <what-built>
    grammY est verrouillé au niveau projet (CLAUDE.md D-V2-02) et vetté dans le RESEARCH (Package Legitimacy Audit : downloads massifs, repo officiel knorpelsenf, deps transitives ultra-courantes @grammyjs/types/abort-controller/debug/node-fetch, AUCUN postinstall réseau — scripts prepare/backport = build git source, pas tarball). Contrairement à la lib QR P4, ce paquet n'entre PAS dans le bundle client ni ne touche les paiements (job backend uniquement). Le vetting est donc une confirmation légère, pas un blocage « argent ».
  </what-built>
  <how-to-verify>
    1. `npm view grammy@1.43.0 version` → confirme 1.43.0 disponible.
    2. `npm view grammy@1.43.0 scripts` → confirmer absence de postinstall réseau (prepare/backport = build source attendus).
    3. npmjs.com/package/grammy → âge/downloads/repo officiel grammyjs cohérents avec le RESEARCH.
    4. Après confirmation : `pnpm --filter jobs add grammy@1.43.0` (version épinglée exacte).
    5. `grep grammy apps/jobs/package.json` → "grammy": "1.43.0".
  </how-to-verify>
  <resume-signal>`approved: grammy@1.43.0` + confirmation absence postinstall réseau, OU motif de refus.</resume-signal>
  <acceptance_criteria>
    - apps/jobs/package.json contient "grammy" épinglé "1.43.0" (pas ^/~)
    - `pnpm install` réussit, lockfile mis à jour
  </acceptance_criteria>
  <done>grammy 1.43.0 vetté et installé dans apps/jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Client bot grammY (env-throw + p-retry) + job telegram-publish + dispatch + .env.example</name>
  <read_first>
    - apps/jobs/src/jobs/outcome-tracker.ts (l.20-44 getServiceClient ; l.68-118 shape job idempotent 2-niveaux)
    - apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts (modèle de test mock client — store mémoire)
    - apps/jobs/src/dispatch.ts (l.16-30 imports ; l.36-53 JOB_REGISTRY ; l.51-52 commentaire d'entrée)
    - packages/data-sources/src/finnhub/client.ts (l.13, 39-68 pattern pRetry)
    - packages/core/src/telegram/format.ts (formatMessage — 06-01)
    - packages/supabase/src/repositories/telegramPosts.ts (insertPost/getPostedKeys — 06-02)
    - packages/supabase/src/repositories/patternStats.ts (getPatternStats — 06-01)
    - apps/jobs/.env.example (modèle de doc env, sans valeurs)
    - 06-RESEARCH.md §Pattern 1/6/7 + §Code Examples + Pitfall 1 (resolved_at) + Pitfall 2 (send THEN insert, log échec insert)
  </read_first>
  <action>
    Créer apps/jobs/src/telegram/bot.ts (client grammY publication-only). `getBot(): Bot` — lire `process.env['TELEGRAM_BOT_TOKEN']`, throw `telegram-publish: TELEGRAM_BOT_TOKEN must be set in apps/jobs/.env` si absent, retourner `new Bot(token)` (JAMAIS bot.start()). `getChannelId(): string` — `process.env['TELEGRAM_CHANNEL_ID']`, throw similaire, forme numérique -100… recommandée. `sendPost(bot, chatId, html): Promise<number>` — `pRetry(() => bot.api.sendMessage(chatId, html, { parse_mode: 'HTML', disable_web_page_preview: true }), { retries: 3 })`, retourner `msg.message_id`. NE JAMAIS logger le token (threat T-06-TOKEN).

    Créer apps/jobs/src/jobs/telegram-publish.ts en clonant outcome-tracker.ts :
    - `getServiceClient()` identique (préfixe erreur `telegram-publish:`).
    - Constante `RECAP_HOUR_UTC = 21` (D-08) en tête de fichier, commentée « aligner sur DAILY_ANCHOR/constants.ts ».
    - `telegramPublish(): Promise<Json>` :
      1. Win rate : `getPatternStats(client)` → trouver `overall && all && all_time` → `applyThreshold(...)` (06-01). C'est le bloc win rate de chaque message.
      2. Trades clos : SELECT `prediction_outcomes` JOIN `trade_setups` (direction) JOIN `instruments` (symbol) borné sur `o.resolved_at >= window_start` (Open Q1 = resolved_at, Pitfall 1) ; mapper en `FormatTrade` (symbol+direction+outcome+realized_r) — JAMAIS entry/sl/tp (D-03).
      3. Clés déjà postées : `getPostedKeys(client)` (idempotence niveau 1).
      4. Cadence (luxon, UTC) : `now.weekday===5` (vendredi, D-09), `now.hour===RECAP_HOUR_UTC` (récap, D-08). Calculer les dedupe_key candidats : notables `notable:<setup_id>` pour chaque trade `realized_r >= 2.0` fraîchement résolu (D-02) ; récap `recap:<YYYY-MM-DD UTC>` au run récap (D-08, jour vide → poster quand même D-10) ; winrate `winrate:<YYYY-MM-DD UTC>` le vendredi (D-09).
      5. Pour chaque dedupe_key candidat ABSENT de getPostedKeys : `formatMessage(...)` → `sendPost(bot, channelId, html)` PUIS `insertPost(client, { dedupe_key, post_type, tg_message_id, run_id })` (send THEN insert, Pitfall 2 ; si insert échoue, logger pino error bruyamment, ne pas masquer).
      6. Retourner `{ posted, skipped } as Json` — NE JAMAIS inclure le token ni le contenu brut dans les stats.
    - Throw avec préfixe sur toute erreur `client.from(...)` (jamais swallow).

    Modifier apps/jobs/src/dispatch.ts : ajouter `import { telegramPublish } from './jobs/telegram-publish'` au bloc d'imports (après l.29) ; ajouter dans JOB_REGISTRY (après l.52) un commentaire « TG-01/02/03 — publication Telegram horaire (APRÈS outcome-tracker). run-job.cmd telegram-publish. » + `'telegram-publish': telegramPublish,`.

    Modifier apps/jobs/.env.example ET .env.example racine : documenter `TELEGRAM_BOT_TOKEN=` et `TELEGRAM_CHANNEL_ID=` (sans valeurs, commentaire « apps/jobs/.env uniquement, jamais committé »).
  </action>
  <verify>
    <automated>npx tsc -b --force 2>&1 | grep -v forbidden-service-import || echo "typecheck ok"</automated>
  </verify>
  <acceptance_criteria>
    - apps/jobs/src/telegram/bot.ts exporte getBot/getChannelId/sendPost ; getBot throw si TELEGRAM_BOT_TOKEN absent ; sendPost utilise pRetry + parse_mode:'HTML'
    - apps/jobs/src/jobs/telegram-publish.ts contient 'resolved_at' (borne, jamais valid_until pour la fenêtre), 'RECAP_HOUR_UTC = 21', "weekday === 5", "realized_r" et NE contient PAS 'entry_price'/'stop_loss'/'take_profits' dans le SELECT
    - apps/jobs/src/dispatch.ts contient "'telegram-publish': telegramPublish"
    - apps/jobs/.env.example ET .env.example contiennent TELEGRAM_BOT_TOKEN et TELEGRAM_CHANNEL_ID (sans valeur)
    - grep -rn "TELEGRAM_BOT_TOKEN" apps/jobs/src/jobs/telegram-publish.ts montre 0 occurrence dans un appel logger/pino/job_runs (token jamais loggé)
    - `pnpm typecheck` sans nouvelle erreur (baseline tolérée)
  </acceptance_criteria>
  <done>Client bot + job + dispatch + env.example livrés, typecheck vert, token jamais loggé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test telegram-publish (mock client + vi.mock grammy) — idempotence, format, jour vide, notable</name>
  <read_first>
    - apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts (analog EXACT — store mémoire mock client, mock upsert onConflict)
    - apps/jobs/src/jobs/telegram-publish.ts (job écrit Task 2)
    - apps/jobs/src/telegram/bot.ts (à mocker via vi.mock('grammy'))
    - 06-RESEARCH.md §Validation Architecture → Test Map + §Wave 0 Gaps
  </read_first>
  <behavior>
    - TG-03 idempotence: 1er run avec 2 notables frais → posted=2 ; 2e run consécutif (mêmes dedupe_key en store) → posted=0, sendMessage NON rappelé
    - TG-03 clés distinctes: recap:<jour> ≠ notable:<setup_id> ≠ winrate:<jour> (3 formes coexistent sans collision)
    - TG-01 jour vide (D-10): au run récap sans trade résolu → 1 post recap envoyé contenant « Aucun trade » + win rate, pas de skip
    - TG-01 notable gate (D-02): trade realized_r=2.5 → post notable ; trade realized_r=1.2 → AUCUN post notable
    - TG-02 seuil (D-11): pattern_stats overall N=12 → message contient « échantillon insuffisant » ; N=142 → « % »
    - secret: le token n'apparaît jamais dans la valeur de retour Json (stats)
  </behavior>
  <action>
    Créer apps/jobs/src/jobs/__tests__/telegram-publish.test.ts (Vitest). Mocker le client service_role en store mémoire (miroir outcome-tracker.test.ts) : tables `prediction_outcomes` (avec resolved_at/outcome/realized_r), `trade_setups` (direction), `instruments` (symbol), `pattern_stats` (overall/all/all_time), `telegram_posts` (dedupe_key store). `vi.mock('grammy')` pour stub `bot.api.sendMessage` → retourne `{ message_id: <fake> }` et compter les appels. Couvrir les cas du bloc `<behavior>`. Assertion clé idempotence : 2e run = 0 sendMessage. Figer `now` via injection ou mock luxon/Date pour tester run récap (~21h UTC) et vendredi de façon déterministe.
  </action>
  <verify>
    <automated>npx vitest run apps/jobs/src/jobs/__tests__/telegram-publish.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/jobs/src/jobs/__tests__/telegram-publish.test.ts couvre: idempotence 2e run=0, clés distinctes par type, jour vide D-10, notable gate ≥2.0R, seuil N<30 vs N≥30
    - vi.mock('grammy') présent ; assertion « sendMessage non rappelé au 2e run »
    - `npx vitest run apps/jobs/src/jobs/__tests__/telegram-publish.test.ts` exit 0
    - `npx vitest run` (suite complète) verte ; `pnpm typecheck` sans nouvelle erreur
  </acceptance_criteria>
  <done>Job testé sans réseau : idempotence, format, jour vide, notable gate, seuil — tous verts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [checkpoint ops] Envoi réel sur le canal Telegram</name>
  <files>apps/jobs/.env</files>
  <action>Checkpoint ops (non autonome, secrets requis hors-code) : créer le bot via @BotFather, créer le canal public, ajouter le bot ADMIN « Post Messages » (Pitfall 4), renseigner TELEGRAM_BOT_TOKEN + TELEGRAM_CHANNEL_ID dans apps/jobs/.env (jamais committé), exécuter `tsx src/dispatch.ts telegram-publish`, vérifier le message bilingue FR+AR posté (disclaimer, win rate, sans niveaux) + idempotence réelle (re-run = 0 doublon) + job_runs sans token. Étapes dans how-to-verify.</action>
  <read_first>
    - 06-RESEARCH.md §Environment Availability (token/canal/bot admin à provisionner ops) + Pitfall 4 (bot non-admin → 403)
    - apps/jobs/src/jobs/telegram-publish.ts (job à exécuter)
    - apps/jobs/.env (à provisionner — jamais committé)
  </read_first>
  <what-built>
    Le job + le client bot + les tests (mock) sont livrés et verts SANS secrets réels (miroir P5/P1 : logique livrée, envoi réel = human-verify). L'envoi réel exige des secrets et un canal que Claude ne peut pas créer.
  </what-built>
  <how-to-verify>
    Étapes ops (hors-code) :
    1. Créer le bot via @BotFather (`/newbot`) → récupérer `TELEGRAM_BOT_TOKEN`.
    2. Créer le canal public Telegram, y ajouter le bot comme ADMINISTRATEUR avec droit « Post Messages » (sinon 403, Pitfall 4).
    3. Récupérer `TELEGRAM_CHANNEL_ID` numérique `-100…`.
    4. Renseigner les 2 vars dans `apps/jobs/.env` (jamais committé).
    5. Exécuter `pnpm --filter jobs exec tsx src/dispatch.ts telegram-publish` (forcer un run récap ou seed un trade notable pour produire un message).
    6. Vérifier : un message bilingue FR (haut) + AR (RTL, bas) apparaît sur le canal, avec disclaimer FR+AR, win rate (ou « échantillon insuffisant »), AUCUN niveau entrée/SL/TP, R lisible LTR dans le bloc arabe.
    7. Vérifier idempotence réelle : re-exécuter immédiatement → aucun doublon (job_runs.stats posted=0).
    8. Vérifier `job_runs` : une ligne success, et le token N'APPARAÎT PAS dans stats.
  </how-to-verify>
  <resume-signal>`approved` + capture/description du message posté (FR+AR + disclaimer + pas de niveaux) + confirmation re-run = 0 doublon.</resume-signal>
  <acceptance_criteria>
    - Un message bilingue FR+AR posté sur le canal avec disclaimer FR+AR et win rate, sans niveaux
    - Re-run immédiat → 0 doublon (idempotence réelle TG-03)
    - job_runs montre success sans token dans stats
  </acceptance_criteria>
  <done>Publication réelle confirmée sur le canal, idempotence vérifiée live, traçabilité job_runs propre, aucun secret fuité.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apps/jobs/.env → job runtime | Secrets token/channel_id lus uniquement ici, jamais en code/DB/logs |
| job (service_role) → Telegram API (egress) | Envoi sortant ; aucun input user (publication-only, pas de bot.start) |
| job → canal Telegram PUBLIC | D-03 minimisation : actif+direction+résultat+R seulement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-TOKEN | Information Disclosure | TELEGRAM_BOT_TOKEN | mitigate | lu depuis apps/jobs/.env uniquement ; jamais loggé (pino), jamais dans job_runs.stats, .env.example sans valeur ; test « token absent du Json retourné » |
| T-06-LEAK | Information Disclosure | SELECT trades clos | mitigate | SELECT limité à symbol+direction+outcome+realized_r ; JAMAIS entry/sl/tp (D-03) ; grep absence dans le job |
| T-06-DUP | Integrity (TG-03) | re-run / race | mitigate | getPostedKeys (niveau 1) + insertPost onConflict dedupe_key (niveau 2) ; test idempotence 2e run=0 + vérif live |
| T-06-INJ | Tampering | sendMessage HTML | mitigate | formatMessage échappe toute donnée dynamique (06-01) ; parse_mode HTML (3 chars) |
| T-06-403 | Denial of Service | bot non-admin | accept (ops) | checkpoint ops : bot admin du canal avant 1er run (Pitfall 4) ; le job remonte l'erreur dans job_runs, ne la masque pas |
| T-06-LISTENER | Elevation of Privilege | grammY | mitigate | publication-only : `new Bot(token)` sans bot.start()/long-polling/webhook (aucun listener entrant) |
| T-06-SC | Tampering | pnpm add grammy | mitigate | checkpoint vetting (Task 1) ; grammy verrouillé + audité (RESEARCH §Package Legitimacy) ; version épinglée 1.43.0 |
</threat_model>

<verification>
- `npx vitest run apps/jobs/src/jobs/__tests__/telegram-publish.test.ts` vert (idempotence 2e run=0).
- `npx vitest run` suite complète verte ; `pnpm typecheck` sans nouvelle erreur.
- `grep` : token jamais dans logger/job_runs ; SELECT job sans entry/sl/tp.
- Checkpoint ops : message réel FR+AR posté, re-run 0 doublon, job_runs success sans token.
</verification>

<success_criteria>
- Job telegram-publish enregistré, idempotent, tracé (TG-01/TG-03) ; clone fidèle outcome-tracker.
- grammY 1.43.0 vetté + épinglé, publication-only (jamais bot.start).
- Tests mockés couvrent idempotence/format/jour-vide/notable/seuil ; envoi réel confirmé live.
- Aucun secret en code/DB/logs ; aucun niveau premium publié.
</success_criteria>

<output>
Create `.planning/phases/06-canal-telegram-public/06-03-SUMMARY.md` when done
</output>
