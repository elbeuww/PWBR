---
phase: 06-canal-telegram-public
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - supabase/migrations/0015_telegram_posts.sql
  - packages/supabase/src/repositories/telegramPosts.ts
  - packages/supabase/src/database.types.ts
  - packages/supabase/src/index.ts
autonomous: false
requirements: [TG-03]
user_setup: []
must_haves:
  truths:
    - "Une table telegram_posts existe en base live avec une contrainte UNIQUE GLOBALE sur dedupe_key (filet anti double-post inviolable)"
    - "Un même événement publiable (recap:<jour>, notable:<setup_id>, winrate:<jour>) ne peut être inséré qu'une fois quel que soit le nombre de re-runs"
    - "Seul le service_role écrit telegram_posts (RLS active, aucune policy write) ; le repo n'est jamais importé depuis apps/web"
    - "Les types TelegramPost{Row,Insert,Update} sont régénérés et les alias maison ré-appliqués après gen types (Pitfall 6)"
  artifacts:
    - path: "supabase/migrations/0015_telegram_posts.sql"
      provides: "DDL table telegram_posts + UNIQUE(dedupe_key) + RLS producteur-unique"
      contains: "dedupe_key text"
    - path: "packages/supabase/src/repositories/telegramPosts.ts"
      provides: "insertPost (onConflict dedupe_key) + getPostedKeys (Set<string>)"
      exports: ["insertPost", "getPostedKeys"]
  key_links:
    - from: "packages/supabase/src/repositories/telegramPosts.ts"
      to: "telegram_posts"
      via: "upsert onConflict dedupe_key ignoreDuplicates"
      pattern: "onConflict: 'dedupe_key'"
    - from: "packages/supabase/src/index.ts"
      to: "telegramPosts repo"
      via: "barrel export"
      pattern: "from './repositories/telegramPosts'"
---

<objective>
Créer la table d'idempotence `telegram_posts` (migration 0015) avec contrainte UNIQUE globale sur `dedupe_key`, l'appliquer LIVE via MCP, régénérer les types, et livrer le repo `telegramPosts.ts` (insert idempotent + lecture des clés déjà postées). Tranche Open Q (schéma + clé d'idempotence) du CONTEXT/RESEARCH Pattern 4.

Purpose: Fournir le filet DB inviolable (TG-03) qui garantit qu'aucun post n'est dupliqué, miroir exact de `UNIQUE(tx_hash)` P4 + `onConflict ignoreDuplicates` P5.
Output: migration 0015 appliquée live, types régénérés + alias ré-appliqués, repo `telegramPosts.ts` exporté.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-canal-telegram-public/06-CONTEXT.md
@.planning/phases/06-canal-telegram-public/06-RESEARCH.md
@.planning/phases/06-canal-telegram-public/06-PATTERNS.md

<interfaces>
<!-- Modèles exacts à porter — pas d'exploration nécessaire. -->

From packages/supabase/src/repositories/predictionOutcomes.ts (analog EXACT à porter):
```typescript
type ServiceClient = SupabaseClient<Database>
export async function insertOutcomes(client: ServiceClient, rows: PredictionOutcomeInsert[]): Promise<void> {
  if (rows.length === 0) return
  const { error } = await client.from('prediction_outcomes')
    .upsert(rows, { onConflict: 'setup_id', ignoreDuplicates: true })
  if (error) throw new Error(`insertOutcomes failed: ${error.message}`)
}
export async function getResolvedSetupIds(client: ServiceClient): Promise<Set<string>> {
  const { data, error } = await client.from('prediction_outcomes').select('setup_id')
  if (error) throw new Error(`getResolvedSetupIds failed: ${error.message}`)
  return new Set((data ?? []).map((r) => r.setup_id))
}
```

Migration recommandée 0015 (RESEARCH Pattern 4, miroir 0012 UNIQUE + 0014 RLS):
```sql
create table public.telegram_posts (
  id            uuid        primary key default gen_random_uuid(),
  dedupe_key    text        not null unique,
  post_type     text        not null check (post_type in ('recap','notable','winrate')),
  posted_at     timestamptz not null default now(),
  tg_message_id bigint,
  run_id        uuid        references public.job_runs(id) on delete set null
);
alter table public.telegram_posts enable row level security;
-- frontière producteur-unique : AUCUNE policy write (service_role bypass)
```

dedupe_key forms (Pattern 4):
- recap quotidien   → `recap:<YYYY-MM-DD UTC>`
- notable intraday  → `notable:<setup_id>`
- winrate vendredi  → `winrate:<YYYY-MM-DD UTC>`

Convention MCP (D-01-01-D, D-05-02-D): apply via `apply_migration`, JAMAIS `supabase db push`. Projet non link → database.types.ts édité à la main après gen types (Pitfall 6 / D-05-02-F).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration 0015_telegram_posts.sql + repo telegramPosts.ts</name>
  <read_first>
    - supabase/migrations/0012_payments.sql (l.63-64 UNIQUE global ; l.85-95 RLS producteur-unique sans policy write)
    - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql (modèle table + RLS Phase 5)
    - packages/supabase/src/repositories/predictionOutcomes.ts (l.1-46 — analog EXACT à porter)
    - packages/supabase/src/index.ts (l.79-80 — modèle d'export repo)
    - 06-RESEARCH.md §Pattern 4 (DDL recommandé) + §Runtime State Inventory (numéro 0015 confirmé)
  </read_first>
  <action>
    Écrire supabase/migrations/0015_telegram_posts.sql (PAS 0013, réservé P4 ; 0014 = dernière sur disque). Table `public.telegram_posts` avec: `id uuid pk default gen_random_uuid()`, `dedupe_key text not null unique` (filet idempotence GLOBAL, TG-03), `post_type text not null check (post_type in ('recap','notable','winrate'))` (text+check, jamais enum natif — convention 0001/0012), `posted_at timestamptz not null default now()`, `tg_message_id bigint` (id sendMessage, traçabilité), `run_id uuid references public.job_runs(id) on delete set null`. `alter table ... enable row level security`. AUCUNE policy write (frontière producteur-unique, service_role bypass) — commentaire SQL explicite « write = service_role bypass, no policy by design (D-05/producer-boundary) ». En-tête de migration: commentaire « apply via MCP apply_migration, NOT supabase db push ».
    Créer packages/supabase/src/repositories/telegramPosts.ts en portant predictionOutcomes.ts: `insertPost(client: ServiceClient, row: TelegramPostInsert): Promise<void>` → `client.from('telegram_posts').upsert([row], { onConflict: 'dedupe_key', ignoreDuplicates: true })`, throw `insertPost failed: ${error.message}` sur erreur. `getPostedKeys(client: ServiceClient): Promise<Set<string>>` → `select('dedupe_key')`, throw `getPostedKeys failed`, retourne `new Set((data ?? []).map(r => r.dedupe_key))`. Header doc « frontière producteur-unique : seul le job (service_role) écrit ; JAMAIS importé depuis apps/web ». Exporter dans packages/supabase/src/index.ts: `export { insertPost, getPostedKeys } from './repositories/telegramPosts'` + (après gen types Task 2) `export type { TelegramPostRow, TelegramPostInsert, TelegramPostUpdate } from './database.types'`.
    NOTE: à ce stade `TelegramPostInsert` n'existe pas encore dans database.types.ts → caster localement `as never`/import différé OU déclarer un type local provisoire commenté « remplacé après gen types Task 2 » (pas de stub de type inventé permanent). Le typecheck plein passe après Task 2.
  </action>
  <verify>
    <automated>grep -q "dedupe_key text" supabase/migrations/0015_telegram_posts.sql && grep -q "onConflict: 'dedupe_key'" packages/supabase/src/repositories/telegramPosts.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0015_telegram_posts.sql contient 'dedupe_key text' ET 'unique' ET "check (post_type in ('recap','notable','winrate'))" ET 'enable row level security'
    - Le fichier 0015 NE contient AUCUNE 'create policy ... insert/update/delete' (frontière producteur-unique)
    - packages/supabase/src/repositories/telegramPosts.ts contient "onConflict: 'dedupe_key'" et "ignoreDuplicates: true" et exporte insertPost + getPostedKeys
    - packages/supabase/src/index.ts contient "from './repositories/telegramPosts'"
    - Aucun secret (token/channel_id) dans la migration ou le repo
  </acceptance_criteria>
  <done>Migration 0015 + repo écrits, idempotence par UNIQUE(dedupe_key) + onConflict, RLS producteur-unique sans policy write.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Appliquer 0015 LIVE via MCP + régénérer types + ré-appliquer alias</name>
  <files>packages/supabase/src/database.types.ts, packages/supabase/src/repositories/telegramPosts.ts</files>
  <action>Checkpoint orchestrateur (non autonome) : appliquer la migration 0015 à la base LIVE via MCP `apply_migration` (JAMAIS `supabase db push`, projet non link), confirmer via `list_tables` + `get_advisors` security, régénérer les types via `generate_typescript_types` vers packages/supabase/src/database.types.ts, RÉ-APPLIQUER en fin de fichier le bloc dalias maison + ajouter TelegramPostRow/Insert/Update (Pitfall 6 / D-05-02-F), retirer les casts provisoires posés en Task 1, puis re-run `pnpm typecheck`. Étapes détaillées dans how-to-verify.</action>
  <read_first>
    - supabase/migrations/0015_telegram_posts.sql (écrit Task 1)
    - packages/supabase/src/database.types.ts (fin de fichier — bloc d'alias maison à préserver, cf. D-05-02-F)
    - STATE.md D-05-02-F (Pitfall 6 : gen types écrase les alias maison → ré-appliquer)
  </read_first>
  <what-built>
    Migration 0015 écrite sur disque (Task 1) mais PAS appliquée à la base live. Le typecheck/build passe SANS l'application (les types viennent du fichier généré) → faux positif de vérification si la migration n'est pas réellement appliquée. Cette tâche est non négociable.
  </what-built>
  <how-to-verify>
    Étapes orchestrateur via MCP Supabase (JAMAIS `supabase db push`, projet non link — D-01-01-D) :
    1. `apply_migration` name `0015_telegram_posts` avec le contenu de supabase/migrations/0015_telegram_posts.sql.
    2. `list_tables` → confirmer `telegram_posts` présente avec colonne `dedupe_key` UNIQUE + check `post_type` + RLS enabled.
    3. `get_advisors` type=security → confirmer que `telegram_posts` n'est PAS exposé à anon (RLS, aucune policy write). Tout nouvel advisor doit être attendu (table interne au job).
    4. `generate_typescript_types` → écraser packages/supabase/src/database.types.ts.
    5. RÉ-APPLIQUER en fin de database.types.ts le bloc d'alias maison (CandleInsert/TradeSetupInsert/ProfileRow/Timeframe/PredictionOutcome{Row,Insert,Update}/… — source = version précédente du fichier) ET AJOUTER `TelegramPostRow = Tables<'telegram_posts'>`, `TelegramPostInsert`, `TelegramPostUpdate` (Pitfall 6 / D-05-02-F).
    6. Retirer tout cast/type provisoire posé en Task 1 dans telegramPosts.ts (le type réel existe désormais).
    7. Re-run `pnpm typecheck` → 0 nouvelle erreur.
  </how-to-verify>
  <resume-signal>`applied` + sortie list_tables (telegram_posts confirmée) + get_advisors security ; typecheck vert.</resume-signal>
  <acceptance_criteria>
    - `list_tables` montre telegram_posts avec dedupe_key UNIQUE + RLS enabled
    - `get_advisors` security: telegram_posts inaccessible anon, aucun advisor inattendu
    - packages/supabase/src/database.types.ts contient 'telegram_posts' (généré) ET le bloc d'alias maison ré-appliqué ET TelegramPostRow/Insert/Update
    - `pnpm typecheck` exit 0 (sans nouvelle erreur), casts provisoires Task 1 retirés
  </acceptance_criteria>
  <done>Table live, types régénérés + alias maison + TelegramPost* ré-ajoutés, typecheck vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job (service_role) → telegram_posts | Seul le service_role écrit ; aucune écriture via clients anon/authenticated |
| migration LIVE → base de production | apply via MCP uniquement (jamais db push), checkpoint humain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-DUP | Integrity (TG-03) | telegram_posts | mitigate | UNIQUE(dedupe_key) global + onConflict ignoreDuplicates (filet inviolable, miroir UNIQUE tx_hash P4) |
| T-06-RLS | Elevation of Privilege | telegram_posts RLS | mitigate | RLS active, AUCUNE policy write → seul service_role écrit ; get_advisors confirme inaccessible anon |
| T-06-SC | Tampering | apply_migration LIVE | mitigate | checkpoint [BLOCKING] humain ; apply via MCP, pas de db push ; get_advisors post-apply |
| T-06-LEAK-DB | Information Disclosure | telegram_posts contenu | accept | la table ne stocke que dedupe_key/type/tg_message_id (pas de niveaux, pas de secret) ; faible valeur |
</threat_model>

<verification>
- `grep -q "dedupe_key text" supabase/migrations/0015_telegram_posts.sql` OK.
- Post-apply: `list_tables` confirme telegram_posts ; `get_advisors` security propre.
- `pnpm typecheck` exit 0 après gen types + ré-application des alias.
- `npx vitest run` non régressé.
</verification>

<success_criteria>
- telegram_posts existe live avec UNIQUE(dedupe_key) + RLS producteur-unique.
- Types régénérés + alias maison + TelegramPost{Row,Insert,Update} présents.
- Repo telegramPosts.ts exporté, prêt pour le job 06-03.
</success_criteria>

<output>
Create `.planning/phases/06-canal-telegram-public/06-02-SUMMARY.md` when done
</output>
