---
phase: 06-canal-telegram-public
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/track-record/threshold.ts
  - packages/core/src/track-record/threshold.test.ts
  - packages/core/src/index.ts
  - packages/supabase/src/repositories/patternStats.ts
  - packages/supabase/src/index.ts
  - apps/web/src/lib/track-record/threshold.ts
  - apps/web/src/lib/track-record/patternStats.ts
  - packages/core/src/telegram/format.ts
  - packages/core/src/telegram/format.test.ts
autonomous: true
requirements: [TG-02, LEGAL-01]
must_haves:
  truths:
    - "Le win rate permanent est lisible par le job ET par la vitrine depuis une source unique partagée (pas d'import cross-app jobs→web)"
    - "Sous N<30 le message dit « échantillon insuffisant, N=… » ; sous N≥30 il affiche « XX% (N=…) » — strictement aligné sur la vitrine P5"
    - "Chaque message formaté contient le disclaimer FR ET AR (LEGAL-01)"
    - "Chaque trade affiché montre actif+direction+résultat+R, JAMAIS les niveaux entrée/SL/TP (D-03)"
    - "Le bloc arabe est RTL avec segments LTR (ticker/R/%) isolés par bidi ; toute donnée dynamique est échappée HTML"
  artifacts:
    - path: "packages/core/src/track-record/threshold.ts"
      provides: "applyThreshold + MIN_SAMPLE déplacés en core (pur, partagé web+jobs)"
      contains: "MIN_SAMPLE = 30"
    - path: "packages/supabase/src/repositories/patternStats.ts"
      provides: "getPatternStats partagé (repo lecture agrégats) importable par web et jobs"
      exports: ["getPatternStats", "PatternStatRow"]
    - path: "packages/core/src/telegram/format.ts"
      provides: "formatMessage pur bilingue FR+AR + escapeHtml + isolats bidi + disclaimer + seuil"
      exports: ["formatMessage", "escapeHtml"]
  key_links:
    - from: "apps/web/src/lib/track-record/threshold.ts"
      to: "packages/core/src/track-record/threshold.ts"
      via: "re-export"
      pattern: "export .* from '@app/core'"
    - from: "packages/core/src/telegram/format.ts"
      to: "applyThreshold"
      via: "import @app/core threshold"
      pattern: "applyThreshold"
---

<objective>
Extraire le socle partagé que le job Telegram réutilisera SANS dupliquer la logique P5 ni franchir la frontière app (jobs→web/lib interdit, D-49), puis construire le formateur de message pur bilingue FR+AR (le SEUL code vraiment neuf de la phase). Tranche Open Q2 (extraction) du RESEARCH.

Purpose: Garantir la cohérence stricte vitrine ↔ Telegram (D-11) avec une source de vérité unique pour le seuil N≥30 et la lecture du win rate, et produire un formateur golden-testable couvrant TG-02, LEGAL-01, D-03, D-05.
Output: `threshold.ts` déplacé en `@app/core`, `getPatternStats` extrait en repo `@app/supabase`, web re-câblé sur ces sources, `formatMessage` pur livré et golden-testé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-canal-telegram-public/06-CONTEXT.md
@.planning/phases/06-canal-telegram-public/06-RESEARCH.md
@.planning/phases/06-canal-telegram-public/06-PATTERNS.md

<interfaces>
<!-- Contrats existants à réutiliser tels quels — pas d'exploration codebase nécessaire. -->

From apps/web/src/lib/track-record/threshold.ts (à DÉPLACER vers packages/core/src/track-record/threshold.ts, contenu identique):
```typescript
export const MIN_SAMPLE = 30
export type StatRow = { n: number; win_rate: number | null; expectancy: number | null; avg_r?: number | null }
export type SufficientStat = { sufficient: true; n: number; winRatePct: number; expectancy: number | null; avgR: number | null }
export type InsufficientStat = { sufficient: false; n: number }
export type ThresholdResult = SufficientStat | InsufficientStat
export function applyThreshold(row: StatRow): ThresholdResult
```

From apps/web/src/lib/track-record/patternStats.ts (forme de requête à PORTER vers packages/supabase/src/repositories/patternStats.ts):
```typescript
export interface PatternStatRow {
  dimension: string | null; bucket: string | null; period: string | null
  n: number | null; win_rate: number | null; avg_r: number | null; expectancy: number | null
}
export async function getPatternStats(supabase: SupabaseClient<Database>):
  Promise<{ rows: PatternStatRow[]; error: string | null }>
// SELECT 'dimension, bucket, period, n, win_rate, avg_r, expectancy' FROM pattern_stats
// La ligne « win rate permanent global » = dimension='overall' AND bucket='all' AND period='all_time' (migration 0014 l.85-92)
```

From packages/core/src/replay/outcome.ts (enum résultat, mapping D-03):
```typescript
export type Outcome = { outcome: 'hit_tp' | 'hit_sl' | 'flat'; realized_r: number }
// hit_tp → « ✅ TP1 atteint » ; hit_sl → « ❌ SL touché » ; flat → « ➖ clôture neutre (flat) »
```

RESEARCH escapeHtml + bidi (spec, Pattern 3 / Code Examples l.439-448):
```
LRI = '⁦' (U+2066), PDI = '⁩' (U+2069), RLM = '‏' (U+200F)
escapeHtml: & → &amp; , < → &lt; , > → &gt;
ltr(s) = LRI + escapeHtml(s) + PDI   // pour ticker/R/% noyés en arabe
ligne AR préfixée RLM
```

Disclaimer (D-06) — réutiliser la copy P2 si disponible (apps/web messages legal/disclaimer), sinon:
- FR : « ⚠️ Contenu éducatif — pas un conseil en investissement, aucune promesse de gain. »
- AR : « ⚠️ محتوى تعليمي — ليس نصيحة استثمارية، ولا وعد بأي ربح. »
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Déplacer threshold.ts (pur) vers @app/core et re-câbler web</name>
  <files>packages/core/src/track-record/threshold.ts, packages/core/src/track-record/threshold.test.ts, packages/core/src/index.ts, apps/web/src/lib/track-record/threshold.ts</files>
  <read_first>
    - apps/web/src/lib/track-record/threshold.ts (source à déplacer, contenu identique)
    - packages/core/src/index.ts (barrel core, l.1-45 — voir bloc replay l.23-25 comme modèle d'export)
    - apps/web/src/lib/track-record/threshold.ts consommateurs: apps/web/src/components/track-record/* + apps/web/src/lib/track-record/patternStats.ts (vérifier qui importe applyThreshold/MIN_SAMPLE avant déplacement)
    - 06-PATTERNS.md §win-rate read (PLANNER DECISION: extraire, threshold.ts pur → packages/core)
  </read_first>
  <behavior>
    - applyThreshold({n:30,win_rate:0.58,...}) → { sufficient:true, n:30, winRatePct:58, ... }
    - applyThreshold({n:12,win_rate:null,...}) → { sufficient:false, n:12 }
    - applyThreshold({n:0,win_rate:null,expectancy:null}) → { sufficient:false, n:0 } (jamais NaN)
    - MIN_SAMPLE === 30
  </behavior>
  <action>
    Créer packages/core/src/track-record/threshold.ts avec le contenu EXACT de apps/web/src/lib/track-record/threshold.ts (export `MIN_SAMPLE=30`, types `StatRow`/`SufficientStat`/`InsufficientStat`/`ThresholdResult`, fonction `applyThreshold`). Aucun changement de logique (cohérence vitrine ↔ Telegram D-11). Ajouter le bloc d'export dans packages/core/src/index.ts (suffixe `.js` comme les autres exports core): `export { MIN_SAMPLE, applyThreshold } from './track-record/threshold.js'` + `export type { StatRow, SufficientStat, InsufficientStat, ThresholdResult } from './track-record/threshold.js'`. Remplacer apps/web/src/lib/track-record/threshold.ts par un re-export mince: `export { MIN_SAMPLE, applyThreshold } from '@app/core'` + `export type { ... } from '@app/core'` (préserve tous les imports web existants, surface inchangée). Copier le test golden vers packages/core/src/track-record/threshold.test.ts (mêmes cas que l'existant web).
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/track-record/threshold.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/track-record/threshold.ts contient 'MIN_SAMPLE = 30' et 'export function applyThreshold'
    - packages/core/src/index.ts contient "from './track-record/threshold.js'"
    - apps/web/src/lib/track-record/threshold.ts contient "from '@app/core'" et NE contient PLUS 'MIN_SAMPLE = 30' (re-export pur)
    - `npx vitest run packages/core/src/track-record/threshold.test.ts` exit 0
    - `pnpm typecheck` ne fait apparaître aucune NOUVELLE erreur (baseline forbidden-service-import.ts tolérée)
  </acceptance_criteria>
  <done>threshold.ts vit en @app/core, web re-exporte, tests golden verts, typecheck non régressé.</done>
</task>

<task type="auto">
  <name>Task 2: Extraire getPatternStats en repo @app/supabase partagé et re-câbler web</name>
  <files>packages/supabase/src/repositories/patternStats.ts, packages/supabase/src/index.ts, apps/web/src/lib/track-record/patternStats.ts</files>
  <read_first>
    - apps/web/src/lib/track-record/patternStats.ts (source de la forme de requête à porter)
    - packages/supabase/src/repositories/predictionOutcomes.ts (l.1-46 — modèle de repo + header frontière producteur-unique)
    - packages/supabase/src/index.ts (l.79-80 — modèle d'export repo Phase 5)
    - 06-RESEARCH.md §Pattern 5 + §Open Questions Q2 (extraction recommandée pour graphe propre D-49)
  </read_first>
  <action>
    Créer packages/supabase/src/repositories/patternStats.ts: porter `getPatternStats(client: SupabaseClient<Database>)` → `{ rows: PatternStatRow[]; error: string | null }` avec le SELECT EXACT `'dimension, bucket, period, n, win_rate, avg_r, expectancy'` sur la table `pattern_stats`, et l'interface `PatternStatRow` (champs dimension/bucket/period/n/win_rate/avg_r/expectancy tous nullable). Note: typer le client `SupabaseClient<Database>` (générique anon OU service — `pattern_stats` grant SELECT anon+authenticated, service_role bypass aussi). En cas d'erreur, renvoyer `{ rows: [], error: error.message }` (jamais throw — l'appelant rend l'état). Exporter dans packages/supabase/src/index.ts: `export { getPatternStats } from './repositories/patternStats'` + `export type { PatternStatRow } from './repositories/patternStats'`. Remplacer apps/web/src/lib/track-record/patternStats.ts par un re-export mince: `export { getPatternStats } from '@app/supabase'` + `export type { PatternStatRow } from '@app/supabase'` (surface web inchangée). NE PAS exposer service-client (le barrel reste sous garde ESLint D-07).
  </action>
  <verify>
    <automated>npx vitest run && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/supabase/src/repositories/patternStats.ts contient ".from('pattern_stats')" et "'dimension, bucket, period, n, win_rate, avg_r, expectancy'"
    - packages/supabase/src/index.ts contient "from './repositories/patternStats'"
    - apps/web/src/lib/track-record/patternStats.ts contient "from '@app/supabase'" et NE contient PLUS ".from('pattern_stats')"
    - Les tests track-record P5 existants (05-03) restent verts: `npx vitest run` exit 0
    - `pnpm typecheck` sans nouvelle erreur
  </acceptance_criteria>
  <done>getPatternStats partagé en @app/supabase, web re-câblé, suite P5 non régressée, graphe de packages propre (jobs n'importera jamais apps/web).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: formatMessage pur bilingue FR+AR (escape HTML + bidi + disclaimer + seuil)</name>
  <files>packages/core/src/telegram/format.ts, packages/core/src/telegram/format.test.ts</files>
  <read_first>
    - packages/core/src/replay/outcome.ts (l.40-44 enum Outcome — mapping résultat D-03)
    - packages/core/src/track-record/threshold.ts (créé Task 1 — applyThreshold pour le bloc win rate)
    - 06-RESEARCH.md §Pattern 2 (l.220-239) + §Pattern 3 (l.241-252) + §Code Examples (l.439-448) + Pitfall 3 (cap 3500) + Pitfall 5 (escape)
    - 06-PATTERNS.md §packages/core/src/telegram/format.ts (NO ANALOG — la spec EST le RESEARCH)
  </read_first>
  <behavior>
    - Test escapeHtml: escapeHtml('a<b>&c') === 'a&lt;b&gt;&amp;c'
    - Test seuil N<30 (D-11): un récap avec winRate {n:12,...} contient « échantillon insuffisant » ET « N=12 » ET PAS de « % »
    - Test seuil N≥30 (TG-02): {n:142,win_rate:0.58} contient « 58% » ET « N=142 »
    - Test disclaimer (LEGAL-01): toute sortie contient le disclaimer FR ET le disclaimer AR
    - Test D-03/D-05: un trade { symbol:'EUR/USD', direction:'long', outcome:'hit_tp', realized_r:2.3 } rend « EUR/USD », « +2.3R », « TP1 atteint » et NE contient AUCUNE des chaînes 'entry'/'stop_loss'/'take_profit'/'SL ='/'TP =' (pas de niveaux)
    - Test jour vide (D-10): récap sans trade contient « Aucun trade » (FR) + l'équivalent AR + le win rate
    - Test bidi: les segments ticker/R sont entourés de U+2066/U+2069 ; les lignes AR sont préfixées U+200F
    - Test cap (Pitfall 3): un récap de 50 trades produit une sortie < 4096 chars et contient « autres » (résumé top ~10 par |R|)
  </behavior>
  <action>
    Créer packages/core/src/telegram/format.ts (pur, zéro I/O). Définir constantes `LRI='⁦'`, `PDI='⁩'`, `RLM='‏'`. `escapeHtml(s)`: `&`→`&amp;` PUIS `<`→`&lt;` PUIS `>`→`&gt;` (ordre important). `ltr(s)` = `LRI + escapeHtml(s) + PDI`. Type d'entrée `FormatTrade = { symbol: string; direction: 'long'|'short'; outcome: Outcome['outcome']; realized_r: number }` ; `WinRateInput = StatRow` (passé à applyThreshold). Type de discrimination du format `PostKind = 'recap' | 'notable' | 'winrate'`. Exporter `formatMessage(input)` retournant un string HTML unique:
    - Bloc FR (LTR) en haut: titre selon kind, lignes trades (mapping D-03: hit_tp→« ✅ TP1 atteint », hit_sl→« ❌ SL touché », flat→« ➖ clôture neutre (flat) »), R formaté avec signe (`+2.3R`/`-1.0R`), ligne win rate via applyThreshold (sufficient→`${winRatePct}% (N=${n})`, sinon→`échantillon insuffisant, N=${n}`), disclaimer FR.
    - Séparateur ligne « ──────────── ».
    - Bloc AR (RTL) en bas: mêmes infos, chaque ligne préfixée RLM, ticker/R/% via ltr(), disclaimer AR.
    - Récap (D-10) jour vide: « Aucun trade clôturé aujourd'hui » + win rate, jamais de skip.
    - Cap (Pitfall 3): si > ~10 trades, ne détailler que le top 10 par |realized_r| décroissant + ligne « +X autres trades » (FR) / équivalent AR ; viser < 3500 chars.
    - escapeHtml appliqué à TOUTE donnée dynamique (symbol surtout) avant insertion (Pitfall 5, threat T-06-INJ). NE JAMAIS inclure entry_price/stop_loss/take_profits (D-03, threat T-06-LEAK).
    Réutiliser la copy disclaimer EXACTE de P2 si présente dans les messages legal/disclaimer (sinon les chaînes fournies dans `<interfaces>`).
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/telegram/format.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/telegram/format.ts exporte `formatMessage` et `escapeHtml`
    - format.test.ts couvre: escape, seuil N<30, seuil N≥30, disclaimer FR+AR, no-niveaux (grep absence 'stop_loss'/'take_profit' dans la sortie), jour vide D-10, bidi (présence U+2066/U+200F), cap 50-trades (< 4096 + « autres »)
    - grep -v '^//' packages/core/src/telegram/format.ts | grep -c 'entry_price\|stop_loss\|take_profits' == 0 (aucun niveau référencé dans le code de sortie)
    - `npx vitest run packages/core/src/telegram/format.test.ts` exit 0
  </acceptance_criteria>
  <done>formatMessage golden-testé couvre TG-02/LEGAL-01/D-03/D-05/D-10/D-11 ; aucun niveau premium fuité ; cap 4096 respecté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job (service_role) → canal Telegram PUBLIC | Tout ce qui sort de formatMessage est publiquement lisible — minimisation de données obligatoire (D-03) |
| données internes (trades, R) → template HTML | parse_mode HTML interprète les balises — toute donnée doit être échappée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LEAK | Information Disclosure | formatMessage (canal public) | mitigate | formatMessage ne prend JAMAIS entry_price/stop_loss/take_profits en entrée (type FormatTrade limité à symbol+direction+outcome+realized_r, D-03) ; test grep absence niveaux |
| T-06-INJ | Tampering | escapeHtml / template HTML | mitigate | escapeHtml systématique sur toute donnée dynamique avant insertion (Pitfall 5) ; test escape golden |
| T-06-BIDI | Tampering | isolats bidi | mitigate | n'injecter QUE des isolats contrôlés U+2066/U+2069/U+200F (pas d'embeddings, pas de contrôle bidi venant de données externes) |
| T-06-LEGAL | Compliance (LEGAL-01) | disclaimer | mitigate | test golden bloquant « disclaimer FR+AR présent dans chaque sortie » |
| T-06-INVENT | Integrity (D-11) | bloc win rate | mitigate | applyThreshold partagé (jamais de % sous N<30 ; « échantillon insuffisant, N » sinon) — source unique avec la vitrine |
</threat_model>

<verification>
- `npx vitest run` (suite complète) verte, dont les nouveaux tests core (threshold + format).
- `pnpm typecheck` sans nouvelle erreur (baseline forbidden-service-import.ts tolérée).
- `pnpm lint:i18n` exit 0 (pas de texte UI nouveau côté web ; re-exports seulement).
- Graphe de packages: aucune nouvelle import apps/web depuis jobs (vérifié au plan 06-03).
</verification>

<success_criteria>
- threshold.ts + getPatternStats sont des sources uniques partagées (web re-exporte, jobs pourra importer sans toucher apps/web).
- formatMessage couvre tous les cas D-03/D-05/D-06/D-10/D-11 en golden tests, < 4096 chars, zéro niveau premium.
- Suite P5 non régressée.
</success_criteria>

<output>
Create `.planning/phases/06-canal-telegram-public/06-01-SUMMARY.md` when done
</output>
