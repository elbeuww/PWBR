---
phase: 10-fondation-design-system-nexa
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/web/src/fonts/Archivo-Regular.woff2
  - apps/web/src/fonts/Archivo-SemiBold.woff2
  - apps/web/src/fonts/SpaceGrotesk-Regular.woff2
  - apps/web/src/fonts/SpaceGrotesk-SemiBold.woff2
  - apps/web/src/fonts/JetBrainsMono-Regular.woff2
  - apps/web/src/fonts/JetBrainsMono-SemiBold.woff2
  - apps/web/src/fonts/ChakraPetch-Regular.woff2
  - apps/web/src/fonts/ChakraPetch-SemiBold.woff2
  - apps/web/src/fonts/NotoSansArabic-Regular.woff2
  - apps/web/src/fonts/NotoSansArabic-SemiBold.woff2
  - apps/web/src/lib/fonts.ts
  - apps/web/src/app/[locale]/layout.tsx
autonomous: false
requirements: [DESIGN-02]

must_haves:
  truths:
    - "Les 5 familles NEXA (Archivo, Space Grotesk, JetBrains Mono, Chakra Petch, Noto Sans Arabic) sont self-hostées en .woff2 versionnés et chargées via next/font/local"
    - "lib/fonts.ts exporte 5 familles avec leurs variables --font-* ; aucune trace de Inter (next/font/google) ni IBM Plex"
    - "Au runtime, aucune requête vers fonts.googleapis.com / fonts.gstatic.com (self-host pur)"
    - "Le <body> de [locale]/layout.tsx injecte les 5 .variable"
  artifacts:
    - path: "apps/web/src/lib/fonts.ts"
      provides: "5 exports localFont avec --font-archivo/--font-space-grotesk/--font-jetbrains-mono/--font-chakra-petch/--font-noto-arabic"
      contains: "localFont"
    - path: "apps/web/src/app/[locale]/layout.tsx"
      provides: "Injection des 5 .variable sur <body>"
      contains: "archivo"
  key_links:
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/lib/fonts.ts"
      via: "import { archivo, spaceGrotesk, jetbrainsMono, chakraPetch, notoArabic }"
      pattern: "from '\\.\\./\\.\\./lib/fonts'"
    - from: "apps/web/src/lib/fonts.ts"
      to: "apps/web/src/fonts/*.woff2"
      via: "localFont src path"
      pattern: "\\.\\./fonts/.*\\.woff2"
---

<objective>
Migrer le setup de polices de 2 familles (Inter via next/font/google + IBM Plex Arabic) vers les 5 familles NEXA self-hostées via next/font/local (D-01/D-02/D-03), en sourçant les .woff2 depuis les paquets @fontsource (dev-time), et en injectant leurs variables CSS sur le <body>.

Purpose : DESIGN-02 — exposer 5 --font-* en CSS vars, zéro appel CDN au runtime. C'est le plumbing typographique que globals.css (plan 03) repointera et que Phase 11 consommera.

Output : 10 .woff2 versionnés, lib/fonts.ts réécrit (5 exports), layout.tsx repointé ; suppression des artefacts IBM Plex orphelins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-fondation-design-system-nexa/10-CONTEXT.md
@.planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md
@.planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md

<interfaces>
<!-- D-02 family → CSS var → role mapping (CONTEXT.md / UI-SPEC §Typography) -->
<!--   Archivo            → --font-archivo            (display/titres/wordmark) -->
<!--   Space Grotesk      → --font-space-grotesk      (corps/UI ; body repointe ici en plan 03) -->
<!--   JetBrains Mono     → --font-jetbrains-mono     (chiffres/tabulaire) -->
<!--   Chakra Petch       → --font-chakra-petch       (accents techniques) -->
<!--   Noto Sans Arabic   → --font-noto-arabic        (arabe via :lang(ar) ; REMPLACE IBM Plex, D-01) -->

<!-- Weights: 400 + 600 SEULEMENT par famille (UI-SPEC type scale = regular + semibold). 5×2 = 10 woff2. -->
<!-- Subset: latin pour les 4 latines ; arabic pour Noto (Pitfall 4 — JAMAIS le subset latin pour Noto). -->

<!-- Current fonts.ts exports (TO REMOVE): inter (next/font/google), ibmPlexArabic (next/font/local). -->
<!-- Current layout.tsx injection (TO REPLACE, line 52): className={`${inter.variable} ${ibmPlexArabic.variable}`} -->

<!-- Existing localFont shape to MIRROR ×5 (fonts.ts lines 24-31):
       export const ibmPlexArabic = localFont({
         src: [{ path: '../fonts/IBMPlexSansArabic-Regular.woff2', weight: '400', style: 'normal' },
               { path: '../fonts/IBMPlexSansArabic-SemiBold.woff2', weight: '600', style: 'normal' }],
         variable: '--font-ibm-plex-arabic', display: 'swap' }) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Vetting provenance des 4 paquets @fontsource (gate supply-chain)</name>
  <what-built>
    Provenance des 4 nouveaux paquets @fontsource sourçant les .woff2 NEXA. RESEARCH §Package Legitimacy Audit classe les 5 @fontsource « Approved — official Fontsource org » mais slopcheck était INDISPONIBLE en session et l'absence de `postinstall` reste [ASSUMED A1]. Avant tout `pnpm add -D`, vérification humaine de la légitimité (gate non auto-approuvable).
  </what-built>
  <action>
    NE PAS exécuter de `pnpm add` avant l'approbation. Présenter à l'humain la vérification de provenance ci-dessous (npmjs.com + absence de postinstall) et attendre le signal "approved". Aucune écriture de fichier dans cette tâche — c'est une porte de revue supply-chain qui débloque la Task 1.
  </action>
  <how-to-verify>
    1. Confirmer chaque paquet sur npmjs.com : `@fontsource/archivo` (5.2.8), `@fontsource/space-grotesk` (5.2.10), `@fontsource/jetbrains-mono` (5.2.8), `@fontsource/chakra-petch` (5.2.7) — org `fontsource/fontsource`, downloads élevés.
    2. Vérifier l'absence de script postinstall : `npm view @fontsource/archivo scripts.postinstall` (attendu : vide). Idem pour les 3 autres.
    3. `@fontsource/noto-sans-arabic` est DÉJÀ installé (précédent IBM Plex) — pas de nouvelle install pour Noto.
  </how-to-verify>
  <verify>
    <automated>MISSING — gate humain bloquant : vérification npmjs.com + `npm view @fontsource/<name> scripts.postinstall` (attendu vide) avant tout install.</automated>
  </verify>
  <done>Les 4 @fontsource sont confirmés légitimes (org officielle, pas de postinstall) ; l'humain a tapé "approved" ; la Task 1 est débloquée.</done>
  <resume-signal>Tape "approved" pour autoriser le `pnpm add -D` des 4 paquets, ou décris le problème de provenance.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Sourcer les 10 .woff2 NEXA, supprimer les 2 IBM Plex orphelins</name>
  <files>apps/web/src/fonts/Archivo-Regular.woff2, apps/web/src/fonts/Archivo-SemiBold.woff2, apps/web/src/fonts/SpaceGrotesk-Regular.woff2, apps/web/src/fonts/SpaceGrotesk-SemiBold.woff2, apps/web/src/fonts/JetBrainsMono-Regular.woff2, apps/web/src/fonts/JetBrainsMono-SemiBold.woff2, apps/web/src/fonts/ChakraPetch-Regular.woff2, apps/web/src/fonts/ChakraPetch-SemiBold.woff2, apps/web/src/fonts/NotoSansArabic-Regular.woff2, apps/web/src/fonts/NotoSansArabic-SemiBold.woff2</files>
  <read_first>
    - apps/web/src/fonts/ (état actuel : contient IBMPlexSansArabic-Regular.woff2 + -SemiBold.woff2 ; ce sont les analogs de sourcing)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §"Sourcing precedent" (lignes 45, 54-61) + §"Pitfall 4" (subset Noto)
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §"apps/web/src/fonts/*.woff2" (lignes 134-145 : noms de fichiers exacts, chemin de copie)
  </read_first>
  <action>
    Après le checkpoint approuvé : `pnpm --filter web add -D @fontsource/archivo @fontsource/space-grotesk @fontsource/jetbrains-mono @fontsource/chakra-petch` (noto-sans-arabic déjà présent). Copier depuis `node_modules/@fontsource/<name>/files/<name>-<subset>-<weight>-normal.woff2` vers `apps/web/src/fonts/` avec les noms cibles EXACTS (qui doivent matcher les `path` de fonts.ts en Task 2) :
      - Archivo : subset `latin`, weights 400+600 → `Archivo-Regular.woff2`, `Archivo-SemiBold.woff2`
      - Space Grotesk : `latin` 400+600 → `SpaceGrotesk-Regular.woff2`, `SpaceGrotesk-SemiBold.woff2`
      - JetBrains Mono : `latin` 400+600 → `JetBrainsMono-Regular.woff2`, `JetBrainsMono-SemiBold.woff2`
      - Chakra Petch : `latin` 400+600 → `ChakraPetch-Regular.woff2`, `ChakraPetch-SemiBold.woff2`
      - Noto Sans Arabic : subset `arabic` (PAS latin — Pitfall 4) 400+600 → `NotoSansArabic-Regular.woff2`, `NotoSansArabic-SemiBold.woff2`
    Si un poids 600 n'existe pas sous le nom exact dans `files/`, utiliser le weight `600` réel du paquet (les @fontsource exposent 100-900 ; choisir 400 et 600). Supprimer `apps/web/src/fonts/IBMPlexSansArabic-Regular.woff2` et `IBMPlexSansArabic-SemiBold.woff2` (orphelins après D-01).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && ls apps/web/src/fonts/*.woff2 | wc -l && ls apps/web/src/fonts/IBMPlexSansArabic-* 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/src/fonts/*.woff2 | wc -l` retourne 10.
    - Les 10 noms exacts attendus sont présents (Archivo/SpaceGrotesk/JetBrainsMono/ChakraPetch/NotoSansArabic × Regular+SemiBold).
    - `ls apps/web/src/fonts/IBMPlexSansArabic-*` retourne 0 (orphelins supprimés).
    - Le fichier NotoSansArabic-Regular.woff2 provient du subset `arabic` (taille typiquement ≥ 30 KB ; pas le fichier latin de ~15 KB).
    - Les 4 @fontsource sont en devDependencies (pas en dependencies runtime).
  </acceptance_criteria>
  <done>10 .woff2 NEXA versionnés sous src/fonts/ aux noms exacts ; 2 IBM Plex supprimés ; 4 @fontsource ajoutés en devDep ; Noto en subset arabic.</done>
</task>

<task type="auto">
  <name>Task 2: Réécrire lib/fonts.ts (5 familles) et repointer l'injection layout.tsx</name>
  <files>apps/web/src/lib/fonts.ts, apps/web/src/app/[locale]/layout.tsx</files>
  <read_first>
    - apps/web/src/lib/fonts.ts (état v2.0 COMPLET : import next/font/google + Inter export ligne 16-22, ibmPlexArabic localFont ligne 24-31 = la forme à mirrorer ×5)
    - apps/web/src/app/[locale]/layout.tsx (état v2.0 : import ligne 23 `{ inter, ibmPlexArabic }`, injection body ligne 52 ; TOUT le reste — hasLocale, setRequestLocale, <html lang dir suppressHydrationWarning>, ThemeProvider, header, Footer — HORS SCOPE, garder verbatim)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §"Pattern 2" (lignes 182-203 : forme localFont ×5 + join body)
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §"lib/fonts.ts" (lignes 97-130) + §"layout.tsx" (lignes 148-173 : import + injection cibles, KEEP verbatim list)
    - apps/web/src/styles/__tests__/fonts.test.ts (la garde RED du plan 01 — ce code doit la faire passer GREEN)
  </read_first>
  <action>
    fonts.ts : SUPPRIMER `import { Inter } from 'next/font/google'` et l'export `inter` (D-03), SUPPRIMER l'export `ibmPlexArabic` (D-01). Conserver `import localFont from 'next/font/local'`. Ajouter 5 exports `localFont` en mirrorant la forme array existante (src = liste de {path,weight,style}, `display: 'swap'` sur chaque) :
      - `archivo` → paths `../fonts/Archivo-Regular.woff2` (400) + `../fonts/Archivo-SemiBold.woff2` (600), `variable: '--font-archivo'`
      - `spaceGrotesk` → SpaceGrotesk-Regular/SemiBold, `variable: '--font-space-grotesk'`
      - `jetbrainsMono` → JetBrainsMono-Regular/SemiBold, `variable: '--font-jetbrains-mono'`
      - `chakraPetch` → ChakraPetch-Regular/SemiBold, `variable: '--font-chakra-petch'`
      - `notoArabic` → NotoSansArabic-Regular/SemiBold, `variable: '--font-noto-arabic'`
    Mettre à jour le header JSDoc FR pour décrire les 5 familles NEXA et citer D-01/D-02/D-03 + 10-RESEARCH §Pattern 2 (la convention de header est obligatoire — PATTERNS §Shared Patterns).

    layout.tsx : remplacer l'import ligne 23 par `import { archivo, spaceGrotesk, jetbrainsMono, chakraPetch, notoArabic } from '../../lib/fonts'`. Remplacer l'injection body (ligne 52) par `<body className={[archivo, spaceGrotesk, jetbrainsMono, chakraPetch, notoArabic].map((f) => f.variable).join(' ')}>`. NE RIEN d'autre toucher (no-flash + RTL contract verbatim).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/fonts.test.ts 2>&1 | tail -15 && pnpm --filter web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `fonts.test.ts` passe GREEN (5 --font-* présents, Inter/IBM absents, 10 woff2, pas d'IBMPlexSansArabic-*).
    - `tsc --noEmit` ne fait apparaître AUCUNE nouvelle erreur au-delà de la baseline P1 connue (`__lint_fixtures__/forbidden-service-import.ts`).
    - fonts.ts n'importe plus `next/font/google` ; layout.tsx importe les 5 nouvelles familles ; le `<body>` joint 5 `.variable`.
    - Le reste de layout.tsx (html lang dir, suppressHydrationWarning, ThemeProvider, header, Footer) est inchangé.
  </acceptance_criteria>
  <done>lib/fonts.ts réécrit avec 5 familles NEXA ; layout.tsx injecte les 5 variables ; fonts.test.ts GREEN ; tsc sans nouvelle erreur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @fontsource npm (build-time) → repo assets | Les .woff2 entrent dans le repo via copie depuis node_modules ; revue humaine de provenance avant install. |
| navigateur → origine app (runtime) | Les polices doivent être servies depuis l'origine app, jamais un CDN tiers (privacy/availability). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | `pnpm add -D @fontsource/*` (supply chain) | mitigate | Checkpoint blocking-human (non auto-approuvable) vérifiant npmjs.com + absence de postinstall AVANT install ([ASSUMED A1] levé). @fontsource = org officielle, downloads élevés, déjà de confiance (IBM Plex). |
| T-10-ID | Information disclosure | Fuite CDN police au runtime (Google Fonts) | mitigate | next/font/local + .woff2 versionnés = zéro requête CDN ; vérifié par no-cdn-fonts.spec.ts (plan 01) au merge de wave. D-03 supprime entièrement la voie next/font/google. |
| T-10-04 | Tampering | Mauvais subset Noto (boîtes □□□ ou fichier géant) | mitigate | Acceptance Task 1 impose le subset `arabic` pour Noto (Pitfall 4). |
</threat_model>

<verification>
- `ls apps/web/src/fonts/*.woff2 | wc -l` = 10 ; aucun IBMPlexSansArabic-*.
- `fonts.test.ts` GREEN.
- `tsc --noEmit` : aucune nouvelle erreur hors baseline.
- Les 4 @fontsource en devDependencies uniquement.
</verification>

<success_criteria>
Les 5 familles NEXA sont self-hostées et exposées en --font-* ; fonts.ts ne référence plus aucune police v2.0 ; layout.tsx injecte les 5 variables ; DESIGN-02 (volet build/exposition) couvert, le volet no-CDN runtime étant vérifié au merge de wave par la spec Playwright.
</success_criteria>

<output>
Create `.planning/phases/10-fondation-design-system-nexa/10-02-SUMMARY.md` when done
</output>
