---
phase: 10-fondation-design-system-nexa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - apps/web/src/styles/__tests__/design-tokens.test.ts
  - apps/web/src/styles/__tests__/fonts.test.ts
  - apps/web/src/styles/__tests__/rtl-logical-props.test.ts
  - apps/web/tests/no-cdn-fonts.spec.ts
  - apps/web/tests/no-flash.spec.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04]

must_haves:
  truths:
    - "Les 5 fichiers de test Wave-0 existent et sont collectés par leur runner (Vitest pour les 3 .test.ts, Playwright pour les 2 .spec.ts)"
    - "Les 3 tests Vitest sont RED contre l'état v2.0 actuel (bleu HEX + 2 polices) — ils échouent pour la bonne raison, pas par erreur de collecte"
    - "Le glob include de vitest.config.ts couvre l'emplacement choisi des 3 .test.ts"
  artifacts:
    - path: "apps/web/src/styles/__tests__/design-tokens.test.ts"
      provides: "Garde DESIGN-01 (OKLCH présent, --nexa-green-500 défini, pas de HEX marque obsolète, @theme inline = var() refs only)"
      min_lines: 25
    - path: "apps/web/src/styles/__tests__/fonts.test.ts"
      provides: "Garde DESIGN-02 (5 exports + 5 --font-*; 10 .woff2; pas de IBMPlexSansArabic-*)"
      min_lines: 25
    - path: "apps/web/src/styles/__tests__/rtl-logical-props.test.ts"
      provides: "Garde DESIGN-04 (scan props physiques sur fichiers fondation)"
      min_lines: 20
    - path: "apps/web/tests/no-cdn-fonts.spec.ts"
      provides: "Garde DESIGN-02 runtime (zéro requête Google Fonts)"
      min_lines: 20
    - path: "apps/web/tests/no-flash.spec.ts"
      provides: "Garde DESIGN-03 (thème stocké rendu sans flash, fr/en/ar)"
      min_lines: 25
  key_links:
    - from: "vitest.config.ts"
      to: "apps/web/src/styles/__tests__/*.test.ts"
      via: "glob include 'apps/**/__tests__/**/*.test.ts'"
      pattern: "apps/\\*\\*/__tests__/\\*\\*/\\*.test.ts"
---

<objective>
Poser la couche de validation Nyquist de la Phase 10 AVANT toute implémentation : créer les 5 fichiers de test Wave-0 (3 Vitest unit + 2 Playwright E2E) qui encodent les 4 critères DESIGN-01..04, et résoudre le CONFIG GAP du glob Vitest pour qu'ils soient collectés.

Purpose : ces tests sont les portes automatiques qui gatent les plans 02 (fonts) et 03 (tokens). Sans eux, l'exécution de la fondation n'a aucun signal de régression — exactement le retrofit que la phase interdit.

Output : 5 fichiers de test (3 RED contre l'état v2.0, 2 specs Playwright nécessitant le dev server) + un include Vitest élargi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fondation-design-system-nexa/10-VALIDATION.md
@.planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md

<interfaces>
<!-- CONFIG GAP (PATTERNS.md §Wave-0 Test Files) — load-bearing -->
<!-- Current vitest.config.ts include array (lines 37-44) covers:
       'packages/**/*.test.ts'
       'apps/**/__tests__/**/*.test.ts'        ← chosen path uses THIS
       'apps/**/__tests__/**/*.test.tsx'
       'apps/web/test/**/*.test.ts'
       'apps/web/src/lib/**/*.test.ts'
       'apps/jobs/src/**/*.test.ts'
     It does NOT cover top-level 'apps/web/tests/**/*.test.ts'.
     DECISION FOR THIS PLAN (Option A from PATTERNS.md): place the 3 Vitest unit
     files under apps/web/src/styles/__tests__/ — already matched by
     'apps/**/__tests__/**/*.test.ts', NO glob change strictly required for them.
     We STILL widen the include with 'apps/web/tests/**/*.test.ts' for safety/future
     parity AND because Playwright .spec.ts under apps/web/tests/ are unaffected. -->

<!-- playwright.config.ts: testDir 'apps/web', match 'tests/**/*.spec.ts', baseURL http://localhost:3000.
     So apps/web/tests/no-cdn-fonts.spec.ts and no-flash.spec.ts are collected as-named, zero config change. -->

<!-- Vitest globals: false → import { describe, it, expect } from 'vitest' is MANDATORY (no ambient globals). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Élargir le glob Vitest et créer les 3 tests unit RED (design-tokens, fonts, rtl-logical-props)</name>
  <files>vitest.config.ts, apps/web/src/styles/__tests__/design-tokens.test.ts, apps/web/src/styles/__tests__/fonts.test.ts, apps/web/src/styles/__tests__/rtl-logical-props.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__ — N'EXISTE PAS encore ; les fichiers s'y placent (couvert par le glob existant `apps/**/__tests__/**/*.test.ts`)
    - vitest.config.ts (état actuel : include array lignes 37-44, `globals: false` ligne 45)
    - apps/web/src/messages/__tests__/theme-parity.test.ts (analog STRUCTURE : header JSDoc FR, `import { describe, it, expect } from 'vitest'`, `fs.readFileSync` du fichier cible, nesting describe/it)
    - apps/web/test/no-perf-claims.test.ts (analog text-scan guard pour rtl-logical-props)
    - apps/web/src/styles/globals.css (état v2.0 CIBLE du scan — c'est CE fichier que design-tokens lit ; il contient aujourd'hui #1E5FBF, pas d'oklch( → le test DOIT être RED)
    - apps/web/src/lib/fonts.ts (état v2.0 : exporte `inter` + `ibmPlexArabic`, 2 vars → fonts.test DOIT être RED)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §"Phase Requirements → Test Map" (lignes 326-331) + §"Wave 0 Gaps"
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §"Wave-0 Test Files" (assertions exactes par fichier, lignes 190-203)
  </read_first>
  <action>
    Ajouter la chaîne `'apps/web/tests/**/*.test.ts'` à la fin de l'array `include` de vitest.config.ts (préserver les 6 entrées existantes et `globals: false`). Les 3 fichiers unit vont sous `apps/web/src/styles/__tests__/` (déjà couvert par `apps/**/__tests__/**/*.test.ts` — c'est la voie A de PATTERNS.md, choisie ici explicitement).

    Chaque test : header JSDoc FR citant la décision/req + `import { describe, it, expect } from 'vitest'` + `import { readFileSync, readdirSync } from 'node:fs'` + chemins résolus relativement au cwd racine (les autres tests lisent via chemins relatifs racine ; utiliser `path.resolve(__dirname, ...)` ou chemin relatif racine cohérent avec l'analog theme-parity).

    design-tokens.test.ts — lit `apps/web/src/styles/globals.css` en string et assert : (1) `oklch(` présent au moins une fois ; (2) déclaration `--nexa-green-500` présente ; (3) AUCUN littéral HEX de marque obsolète dans la couche sémantique — interdire `#1E5FBF` ET `#03d87f` ET `#63279b` apparaissant comme valeur (le brand doit vivre en oklch, pas en HEX) ; (4) le bloc `@theme inline` ne contient QUE des références `var(` pour ses valeurs `--color-*`/`--font-*` (anti-Pitfall 3 : aucune valeur oklch/hex littérale dans `@theme inline`). Contre globals.css v2.0 actuel : assertions (1)(2) RED (pas d'oklch, pas de --nexa-green-500), (3) RED (#1E5FBF présent).

    fonts.test.ts — lit `apps/web/src/lib/fonts.ts` en string et assert : présence des 5 noms de variables CSS `--font-archivo`, `--font-space-grotesk`, `--font-jetbrains-mono`, `--font-chakra-petch`, `--font-noto-arabic` ; ABSENCE de `--font-inter` et `--font-ibm-plex-arabic` ; ABSENCE de l'import `next/font/google`. Puis `readdirSync('apps/web/src/fonts')` doit lister les 10 `.woff2` attendus (Archivo/SpaceGrotesk/JetBrainsMono/ChakraPetch/NotoSansArabic × Regular+SemiBold) ET ne PAS lister `IBMPlexSansArabic-Regular.woff2` / `-SemiBold.woff2`. Contre l'état v2.0 : RED (vars Inter/IBM présentes, woff2 NEXA absents).

    rtl-logical-props.test.ts — scanne le texte des fichiers fondation touchés (`apps/web/src/styles/globals.css`, `apps/web/src/app/[locale]/layout.tsx`) et FAIL si une classe utilitaire à propriété PHYSIQUE apparaît : regex `\b(ml-|mr-|pl-|pr-|left-|right-)\b` (échapper correctement ; ne PAS matcher `ms-`/`me-`/`ps-`/`pe-`/`start-`/`end-` qui sont logiques). Mirror le style text-scan de no-perf-claims.test.ts. Note : `font-family`/`line-height` sont direction-neutres, ne pas les flaguer. Contre l'état actuel : layout.tsx utilise déjà `ms-6`/`ms-auto` (logiques) → ce test doit être GREEN dès maintenant (garde de non-régression), c'est attendu et non bloquant.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers sont COLLECTÉS (pas de "No test files found") — preuve que le glob les couvre.
    - design-tokens.test.ts : RED (au moins l'assertion oklch/--nexa-green-500 échoue contre globals.css v2.0).
    - fonts.test.ts : RED (vars NEXA absentes / woff2 NEXA absents).
    - rtl-logical-props.test.ts : GREEN (aucune prop physique dans globals.css + layout.tsx actuels) — garde de non-régression active.
    - vitest.config.ts contient la nouvelle entrée `apps/web/tests/**/*.test.ts` ET conserve les 6 entrées existantes + `globals: false`.
    - Aucune nouvelle dépendance npm.
  </acceptance_criteria>
  <done>3 fichiers de test unit créés et collectés ; design-tokens + fonts RED contre v2.0 ; rtl-logical-props GREEN ; glob Vitest élargi sans régression des includes existants.</done>
</task>

<task type="auto">
  <name>Task 2: Créer les 2 specs Playwright (no-cdn-fonts, no-flash) trilingues</name>
  <files>apps/web/tests/no-cdn-fonts.spec.ts, apps/web/tests/no-flash.spec.ts</files>
  <read_first>
    - apps/web/e2e/i18n.spec.ts (analog STRUCTURE Playwright : `import { test, expect } from '@playwright/test'`, `test.describe`, boucle fr/en/ar, header JSDoc « requiert next dev :3000 », idiome `page.locator('html')` + `toHaveAttribute`)
    - playwright.config.ts (testDir 'apps/web', match 'tests/**/*.spec.ts', baseURL http://localhost:3000) — confirme que ces 2 fichiers sont collectés as-named
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §"no-cdn-fonts.spec.ts + no-flash.spec.ts" (lignes 205-223 : assertions exactes)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §Test Map (lignes 329-330)
  </read_first>
  <action>
    no-cdn-fonts.spec.ts (DESIGN-02 runtime) : header JSDoc FR « requiert `pnpm --filter web dev` sur :3000 ». Installer `page.on('request', ...)` AVANT `page.goto`, collecter les URLs ; visiter `/fr/login` (route publique stable) ; après chargement réseau idle, asserter que ZÉRO requête vers `fonts.googleapis.com` ni `fonts.gstatic.com` n'a été émise. Mirror le grouping `test.describe` d'i18n.spec.ts.

    no-flash.spec.ts (DESIGN-03) : header JSDoc FR « requiert dev :3000 ». Pour chaque locale de la boucle fr/en/ar : `page.addInitScript` (ou `localStorage.setItem('theme','dark')` via `context.addInitScript` AVANT navigation) pour poser le thème stocké `dark` ; naviguer vers `/{locale}/login` ; asserter que `<html>` porte la classe `dark` au premier rendu (`expect(page.locator('html')).toHaveAttribute('class', /dark/)`) — preuve que le script pré-paint next-themes applique le thème stocké sans frame clair. Mirror la boucle locale d'i18n.spec.ts lignes 57-78.

    Ne PAS exécuter ces specs en GREEN dans ce plan (elles requièrent le dev server live + l'implémentation des plans 02/03). Leur création + parse/`--list` OK suffit pour ce plan ; le GREEN est porté au merge de wave 2/3.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx playwright test --list apps/web/tests/no-cdn-fonts.spec.ts apps/web/tests/no-flash.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Les 2 specs PARSENT et apparaissent dans `playwright test --list` (collectées par le testDir/match).
    - no-flash.spec.ts itère explicitement sur fr, en, ar (3 cas listés ou une boucle paramétrée visible).
    - no-cdn-fonts.spec.ts contient une interception `page.on('request'` et l'assertion d'absence `fonts.googleapis.com`/`fonts.gstatic.com`.
    - Aucune nouvelle dépendance npm.
  </acceptance_criteria>
  <done>2 specs Playwright créées, parseables et listées ; assertions no-CDN et no-flash trilingue encodées ; exécution GREEN déférée au merge de wave aval (dev server requis).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time test files → CI runner | Les tests lisent des fichiers source du repo ; aucune entrée utilisateur, aucune frontière réseau de prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Glob Vitest élargi pourrait masquer/dé-collecter des tests existants | mitigate | N'AJOUTER qu'une entrée à l'array `include` ; vérifier que les 6 entrées existantes restent ; `globals: false` préservé. |
| T-10-02 | Repudiation | Test RED faussement vert (collecte échoue silencieusement) | mitigate | Acceptance exige la preuve de COLLECTE (pas de "No test files found") avant d'accepter le RED. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Plan 01 n'installe AUCUN paquet (RESEARCH §Package Legitimacy : phase sans nouvelle dép runtime ; les `@fontsource/*` dev-time sont au plan 02). Aucun checkpoint requis ici. |
</threat_model>

<verification>
- `npx vitest run apps/web/src/styles/__tests__` collecte 3 fichiers ; design-tokens + fonts RED, rtl-logical-props GREEN.
- `npx playwright test --list` inclut les 2 specs.
- `vitest.config.ts` include conserve les 6 entrées + ajoute `apps/web/tests/**/*.test.ts`.
- Zéro paquet npm ajouté.
</verification>

<success_criteria>
Les 5 portes de validation Wave-0 existent et sont collectées par leur runner respectif ; les gardes Vitest échouent contre l'état v2.0 (preuve qu'elles mesurent la migration) ; le CONFIG GAP du glob est résolu.
</success_criteria>

<output>
Create `.planning/phases/10-fondation-design-system-nexa/10-01-SUMMARY.md` when done
</output>
