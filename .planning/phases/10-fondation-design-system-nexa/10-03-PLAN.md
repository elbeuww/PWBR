---
phase: 10-fondation-design-system-nexa
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - apps/web/src/styles/globals.css
autonomous: true
requirements: [DESIGN-01, DESIGN-03, DESIGN-04]

must_haves:
  truths:
    - "globals.css porte une couche primitive OKLCH NEXA (brand green/purple, ink, neutres, signaux) dans @theme, theme-indépendante"
    - "La couche sémantique (:root/.dark) repointe --primary/--ring/--destructive… sur les primitives NEXA (sortie du bleu #1E5FBF)"
    - "@theme inline ne contient que des références var() (aucune valeur OKLCH/HEX littérale) — la bascule .dark reste fonctionnelle"
    - "--font-latin/--font-sans repointent sur Space Grotesk ; --font-arabic sur Noto ; le body et :lang(ar) résolvent une var existante"
    - "Brand green (hue 155) et signal-bullish (hue 149) sont des tokens distincts, séparation documentée en commentaire CSS (D-05)"
    - "Aucune propriété physique introduite dans la couche base/:lang(ar) (RTL préservé)"
  artifacts:
    - path: "apps/web/src/styles/globals.css"
      provides: "Système de tokens OKLCH en 3 couches (primitive→semantic→component) NEXA"
      contains: "--nexa-green-500"
  key_links:
    - from: "globals.css @theme inline --color-primary"
      to: ":root/.dark --primary"
      via: "var(--primary)"
      pattern: "--color-primary:\\s*var\\(--primary\\)"
    - from: "globals.css :root --primary"
      to: "@theme --nexa-green-500"
      via: "var(--nexa-green-500)"
      pattern: "--primary:\\s*var\\(--nexa-green-500\\)"
    - from: "globals.css @theme --font-arabic"
      to: "next/font Noto variable"
      via: "var(--font-noto-arabic)"
      pattern: "--font-arabic:\\s*var\\(--font-noto-arabic\\)"
---

<objective>
Migrer globals.css de tokens plats HEX bleus (v2.0) vers un système OKLCH NEXA en 3 couches : primitives (brand green/purple, ink, neutres, signaux trading) dans @theme, sémantiques (:root/.dark) repointées sur ces primitives, component (@theme inline) inchangé en noms. Repointer les --font-* sur les 5 familles du plan 02. Garder next-themes et les propriétés logiques intacts.

Purpose : DESIGN-01 (tokens OKLCH en couches, palette NEXA) + DESIGN-03 (bascule clair/sombre sans flash, primitives en :root parsées synchroniquement) + DESIGN-04 (base layer en props logiques). C'est la bascule couleur DÈS la Phase 10 (D-04) — Phase 11 ne refera jamais ce changement.

Output : globals.css réécrit en 3 couches OKLCH, faisant passer design-tokens.test.ts au GREEN, sans casser la bascule .dark ni le RTL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-fondation-design-system-nexa/10-CONTEXT.md
@.planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md
@.planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md
@.planning/phases/10-fondation-design-system-nexa/10-UI-SPEC.md

<interfaces>
<!-- OKLCH values LOCKED (RESEARCH §Code Examples lignes 266-274 ; UI-SPEC §Color ; re-vérifiées par calcul node) -->
<!--   brand green #03d87f   → oklch(0.7754 0.1896 155.45)   = --nexa-green-500 -->
<!--   green darker step                                      = --nexa-green-600  oklch(0.7100 0.1800 155.45) -->
<!--   royal purple #63279b  → oklch(0.4233 0.1772 302.98)   = --nexa-purple-500 -->
<!--   purple lighter (dark eyebrow)                          = --nexa-purple-400 oklch(0.5400 0.1900 302.98) -->
<!--   ink #0a0e1a           → oklch(0.1663 0.0262 269.37)   = --nexa-ink   (dark bg) -->
<!--   white                  → oklch(1.0000 0 0)             = --nexa-white -->
<!--   signal bullish #16a34a → oklch(0.6271 0.1699 149.21)  = --nexa-signal-bull  (hue 149, SOBRE) -->
<!--   signal bearish/destructive #dc2626 → oklch(0.5771 0.2152 27.33) = --nexa-signal-bear -->

<!-- D-05 HARD RULE: --nexa-green-500 (hue 155, vivid, brand) ≠ --nexa-signal-bull (hue 149, sober).
     --primary ne doit JAMAIS encoder une direction de trade. Documenter en commentaire CSS. -->

<!-- 3-couche partitioning (RESEARCH §Recommended globals.css layer partitioning lignes 103-166):
       @theme        → primitives (5 --font-* + ramps OKLCH brutes). Theme-INDÉPENDANT, ne flippe jamais.
       @theme inline → component (--color-* + --radius-*). NOMS INCHANGÉS depuis v2.0. var() refs ONLY.
       :root / .dark → sémantique (--primary/--background/--ring/--destructive…). SEULE couche qui flippe. -->

<!-- @theme inline existant (KEEP names, change SEULEMENT --font-sans → var(--font-space-grotesk)) :
       18 mappings --color-* + 4 --radius-* (globals.css lignes 28-52). -->

<!-- Sémantique cible (RESEARCH lignes 142-165) :
       :root  --background: var(--nexa-white); --foreground: var(--nexa-ink);
              --primary: var(--nexa-green-500); --primary-foreground: var(--nexa-ink);  (green clair → texte sombre)
              --ring: var(--nexa-green-500); --destructive: var(--nexa-signal-bear);
              --signal-bullish: var(--nexa-signal-bull); --signal-bearish: var(--nexa-signal-bear);
       .dark  --background: var(--nexa-ink); --foreground: oklch(0.95 0.01 260);
              --primary: var(--nexa-green-500); --primary-foreground: var(--nexa-ink); --ring: var(--nexa-green-500); -->

<!-- Préambule à GARDER verbatim (lignes 1-9): @import tailwindcss; @import tw-animate-css; @custom-variant dark (&:where(.dark, .dark *)); -->
<!-- :lang(ar) garde line-height:1.6, repointe font-family sur var(--font-arabic) → désormais Noto. -->
<!-- @layer base body: garder @apply bg-background text-foreground; repointer font-family sur la var Space Grotesk qui résout. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Réécrire les couches primitive + sémantique OKLCH NEXA (brand, ink, neutres, signaux)</name>
  <files>apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/styles/globals.css (état v2.0 COMPLET : @theme lignes 17-20 [2 fonts], @theme inline lignes 28-52, :root lignes 59-79 [#1E5FBF…], .dark lignes 82-101, :lang(ar) 108-111, @layer base 113-121)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §"Recommended globals.css layer partitioning" (lignes 103-171, valeurs exactes) + §"Pattern 1" (stops ciblés, lignes 173-180) + §"Trading-signal vs brand separation" (lignes 277-286)
    - .planning/phases/10-fondation-design-system-nexa/10-UI-SPEC.md §Color (lignes 98-137 : rôles sémantiques 60/30/10, --destructive réel rouge, namespace signaux)
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §globals.css (lignes 28-93 : ce qui change vs ce qui reste verbatim)
    - apps/web/src/styles/__tests__/design-tokens.test.ts (la garde RED du plan 01 — ce code la rend GREEN)
  </read_first>
  <action>
    Garder le préambule (lignes 1-9) verbatim. Dans `@theme` : remplacer les 2 vars font par les 5 (`--font-display: var(--font-archivo)`, `--font-sans: var(--font-space-grotesk)`, `--font-mono: var(--font-jetbrains-mono)`, `--font-accent: var(--font-chakra-petch)`, `--font-arabic: var(--font-noto-arabic)`) ET ajouter la couche primitive OKLCH brute (theme-indépendante) avec les valeurs verrouillées :
      `--nexa-green-500: oklch(0.7754 0.1896 155.45)` (brand), `--nexa-green-600: oklch(0.7100 0.1800 155.45)`, `--nexa-purple-500: oklch(0.4233 0.1772 302.98)`, `--nexa-purple-400: oklch(0.5400 0.1900 302.98)`, `--nexa-white: oklch(1.0000 0 0)`, `--nexa-ink: oklch(0.1663 0.0262 269.37)`, et le namespace signaux SÉPARÉ `--nexa-signal-bull: oklch(0.6271 0.1699 149.21)` (hue 149 sobre) + `--nexa-signal-bear: oklch(0.5771 0.2152 27.33)`. Dériver une rampe neutre 5 pas OKLCH à chroma ~0.02 hue ~265 (navy-teinté) pour card/secondary/muted/border/input (light + dark anchors). Inscrire un commentaire CSS portant la RÈGLE D-05 : « --primary NE DOIT JAMAIS encoder une direction de trade ; brand green (hue 155) ≠ bullish (hue 149) ».

    Réécrire `:root` (light) : `--radius: 0.625rem` (garder), `--background: var(--nexa-white)`, `--foreground: var(--nexa-ink)`, `--primary: var(--nexa-green-500)`, `--primary-foreground: var(--nexa-ink)` (green clair → texte sombre), `--ring: var(--nexa-green-500)`, `--destructive: var(--nexa-signal-bear)` (rouge réel, Unknown #6), + card/popover/secondary/muted/accent/border/input depuis la rampe neutre, + exposer `--signal-bullish: var(--nexa-signal-bull)` et `--signal-bearish: var(--nexa-signal-bear)` (consommés en Phase 11).

    Réécrire `.dark` : n'overrider que les valeurs qui flippent — `--background: var(--nexa-ink)`, `--foreground: oklch(0.95 0.01 260)`, `--primary: var(--nexa-green-500)` (garder ou légèrement éclaircir L≈0.80 si le contraste l'exige — A2, taste), `--primary-foreground: var(--nexa-ink)`, `--ring: var(--nexa-green-500)`, neutres dark-tunés. Les signaux GARDENT la même teinte entre thèmes (une perte est rouge dans les deux).

    INTERDIT : aucune valeur OKLCH/HEX littérale dans `@theme inline` (Pitfall 3 — casse silencieusement .dark). Aucun HEX de marque (#1E5FBF/#03d87f/#63279b) ne doit subsister comme valeur dans la couche sémantique.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/design-tokens.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `design-tokens.test.ts` GREEN : `oklch(` présent, `--nexa-green-500` défini, aucun #1E5FBF/#03d87f/#63279b résiduel en valeur sémantique, `@theme inline` = var() only.
    - `:root --primary` résout `var(--nexa-green-500)` ; `--destructive` résout `var(--nexa-signal-bear)`.
    - Les tokens `--signal-bullish`/`--signal-bearish` existent et sont DISTINCTS de `--primary` (D-05).
    - Un commentaire CSS porte explicitement la règle de séparation D-05.
    - Préambule (lignes 1-9) et noms `--color-*`/`--radius-*` de `@theme inline` inchangés.
  </acceptance_criteria>
  <done>globals.css porte la couche primitive OKLCH NEXA + sémantique repointée light/dark ; design-tokens.test.ts GREEN ; bascule .dark préservée (@theme inline var-only).</done>
</task>

<task type="auto">
  <name>Task 2: Repointer les vars de police (body / :lang(ar) / @theme inline --font-sans) et garder le base layer logique</name>
  <files>apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/styles/globals.css (après Task 1 : @theme inline --font-sans ligne ~29, :lang(ar) font-family, @layer base body font-family ligne ~119)
    - apps/web/src/lib/fonts.ts (plan 02 : variables réelles produites — --font-space-grotesk, --font-noto-arabic, etc. — ces noms DOIVENT correspondre à ce que repointe globals.css)
    - .planning/phases/10-fondation-design-system-nexa/10-RESEARCH.md §"Runtime State Inventory" (ligne 236 : tout ce qui référence l'ancien state ; --font-latin/--font-inter/--font-ibm-plex-arabic à repointer sinon fallback system-ui silencieux)
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md §"Arabic + base layer" (lignes 79-93) + §"Logical-properties-only" (lignes 237-239)
    - apps/web/src/styles/__tests__/rtl-logical-props.test.ts (garde — aucune prop physique ne doit apparaître)
  </read_first>
  <action>
    Dans `@theme inline`, repointer `--font-sans: var(--font-space-grotesk)` (était `var(--font-inter)`). Dans le sélecteur `:lang(ar)`, garder `line-height: 1.6` (direction-neutre, RTL-safe) et `font-family: var(--font-arabic)` (qui résout désormais vers Noto via le repointage @theme de Task 1). Dans `@layer base`, garder `* { @apply border-border outline-ring/50 }` et `body { @apply bg-background text-foreground }`, et repointer la `font-family` du body sur une var qui RÉSOUT — soit `var(--font-sans)` (Space Grotesk via @theme inline) soit `var(--font-display)`/une var primitive existante ; NE PAS laisser `var(--font-latin)` si cette var n'est plus définie (sinon fallback system-ui silencieux). Choisir Space Grotesk pour le corps (D-02 : Space Grotesk = body/UI) avec fallback `system-ui, sans-serif`.

    Vérifier qu'aucune classe utilitaire à propriété PHYSIQUE (`ml-`/`mr-`/`pl-`/`pr-`/`left-`/`right-`) n'est introduite dans le base layer ou `:lang(ar)`. `font-family`/`line-height` sont direction-neutres → autorisés.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && npx vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts apps/web/src/styles/__tests__/design-tokens.test.ts 2>&1 | tail -15 && pnpm --filter web exec tsc --noEmit 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `rtl-logical-props.test.ts` GREEN (aucune prop physique dans globals.css après migration).
    - `design-tokens.test.ts` reste GREEN.
    - `:lang(ar)` résout `var(--font-arabic)` (→ Noto) ; le body résout une var de police définie (Space Grotesk), jamais un fallback system-ui silencieux dû à une var orpheline.
    - Aucune occurrence résiduelle de `--font-latin`/`--font-inter`/`--font-ibm-plex-arabic` non définie référencée dans globals.css.
    - `tsc --noEmit` : aucune nouvelle erreur hors baseline P1.
  </acceptance_criteria>
  <done>Vars de police repointées (body→Space Grotesk, :lang(ar)→Noto) ; base layer reste en propriétés logiques ; rtl-logical-props + design-tokens GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feuille de style (build) → navigateur (runtime) | Tokens statiques compilés par Tailwind v4 ; aucune entrée utilisateur, aucun secret. Surface de sécurité minimale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Valeur littérale dans `@theme inline` casse la bascule .dark | mitigate | Acceptance Task 1 impose `@theme inline` = var() only ; design-tokens.test.ts l'assert ; no-flash.spec.ts (plan 01) vérifie la bascule au merge de wave. |
| T-10-06 | Information disclosure | FOUC si une var de token résout tardivement (primitive hors :root) | mitigate | Primitives en `@theme`/`:root` (parsées synchroniquement avec la feuille), jamais injectées par JS (Pitfall 1) ; next-themes pré-paint conservé (D-06). |
| T-10-07 | Tampering | Fuite RTL via propriété physique dans le base layer | mitigate | rtl-logical-props.test.ts scanne globals.css ; acceptance interdit ml-/mr-/pl-/pr-/left-/right-. |
| T-10-SC | Tampering | npm installs | accept | Plan 03 n'installe aucun paquet (CSS pur). |
</threat_model>

<verification>
- `design-tokens.test.ts` GREEN (OKLCH, --nexa-green-500, pas de HEX marque, @theme inline var-only).
- `rtl-logical-props.test.ts` GREEN.
- `tsc --noEmit` sans nouvelle erreur.
- Bascule .dark fonctionnelle + no-flash vérifiés au merge de wave par no-flash.spec.ts (plan 01, dev server requis).
</verification>

<success_criteria>
globals.css est un système OKLCH NEXA en 3 couches (primitive→semantic→component) ; le brand a quitté le bleu institutionnel pour le cyber green/royal purple ; signaux trading séparés et documentés (D-05) ; vars de police repointées sur les 5 familles ; bascule clair/sombre et RTL préservés. DESIGN-01/03/04 (volet tokens/CSS) couverts.
</success_criteria>

<output>
Create `.planning/phases/10-fondation-design-system-nexa/10-03-SUMMARY.md` when done
</output>
