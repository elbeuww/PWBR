---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - supabase/migrations/0021_admin_cockpit.sql
  - packages/supabase/database.types.ts
autonomous: false
requirements: [ADASH-01, ADASH-02, ADASH-03, ADASH-04, ADASH-05, ADASH-07]

must_haves:
  truths:
    - "Un superadmin lit profiles/telegram_posts/candles/trade_setups/analyses ; un non-superadmin lit 0 ligne"
    - "Les RPC d'écriture admin sont SECURITY DEFINER gated is_superadmin(), search_path figé, atomiques avec l'audit"
    - "Chaque écriture admin sensible insère une ligne admin_audit_log dans la même transaction"
    - "Un compte suspendu lit 0 ligne via has_active_subscription() étendu (and not suspended)"
    - "Les KPI funnel/churn/mix sont lisibles via wrappers gated renvoyant 0 ligne pour un non-superadmin (jamais throw)"
    - "get_advisors(security) ne montre aucune nouvelle fuite ; get_advisors(performance) reste sans auth_rls_initplan"
  artifacts:
    - path: "supabase/migrations/0021_admin_cockpit.sql"
      provides: "policies SELECT superadmin + admin_audit_log + profiles.suspended + RPC écriture + wrappers KPI"
      contains: "admin_audit_log"
    - path: "packages/supabase/database.types.ts"
      provides: "types régénérés (admin_audit_log, profiles.suspended, nouveaux RPC), alias maison + *_atomic string"
      contains: "admin_audit_log"
  key_links:
    - from: "RPC suspend_account/grant_subscription_time/admin_mark_commission_paid"
      to: "admin_audit_log"
      via: "insert dans la même transaction"
      pattern: "insert into .*admin_audit_log"
    - from: "has_active_subscription()"
      to: "profiles.suspended"
      via: "and not exists suspended"
      pattern: "suspended"
---

<objective>
Écrire et appliquer LIVE la migration `0021_admin_cockpit.sql` — la fondation données/sécurité de tout le cockpit. Elle pose : les policies SELECT « superadmin voit tout » manquantes, la table `admin_audit_log`, la colonne `profiles.suspended` + extension de `has_active_subscription()` (suspension = vraie barrière RLS), les 4 RPC d'écriture `SECURITY DEFINER` gated + audit atomique, et les wrappers KPI gated (funnel/churn/mix). Puis appliquer via MCP `apply_migration`, régénérer les types, et passer les gates advisors + EXPLAIN.

Purpose: Sans cette migration, la bascule anon-client (D-01) renvoie des pages vides (Pitfall 1) et aucune écriture admin n'est tracée. C'est l'arête dure de la phase : RLS = source de vérité.
Output: `0021_admin_cockpit.sql` LIVE + `database.types.ts` régénéré ; `admin-rls.test.ts` (plan 20-01) passe au vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-CONTEXT.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md

<interfaces>
<!-- Calques SQL exacts. Lire chaque source avant d'écrire le bloc correspondant. -->
- `supabase/migrations/0008_profiles_role.sql` (L.29-47) : `is_superadmin()` security definer search_path=public ; contrat grant (revoke public/anon, grant authenticated). Garde de chaque RPC : `if not (select public.is_superadmin()) then raise exception 'forbidden'`.
- `supabase/migrations/0009_subscriptions_gating.sql` (L.46-50 forme policy ; L.63-77 `has_active_subscription()` ; L.30-35 définition canonique « actif ») : table subscriptions, helper à drop/recreate.
- `supabase/migrations/0016_affiliation.sql` (`mark_commission_paid` L.331-362) : RPC atomique update+insert audit, anti double-effet `get diagnostics row_count` + raise.
- `supabase/migrations/0017_scalable_foundation.sql` (`get_mrr()` L.262-278, `mv_mrr` `revoke all` L.278 ; wrap InitPlan `(select ...)` sur toutes policies) : calque wrapper KPI gated + convention wrap dès l'écriture.
- `supabase/migrations/0018_seed_source_column.sql` : colonne `source` sur profiles (filtre, pas policy).
- `apps/web/test/admin-rls.test.ts` (plan 20-01) : noms exacts attendus (RPC/tables) que cette migration doit honorer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration 0021_admin_cockpit.sql (5 couches)</name>
  <files>supabase/migrations/0021_admin_cockpit.sql</files>
  <read_first>
    - supabase/migrations/0008_profiles_role.sql (is_superadmin + contrat grant)
    - supabase/migrations/0009_subscriptions_gating.sql (forme policy L.46-50 + has_active_subscription L.63-77 + def actif L.30-35)
    - supabase/migrations/0016_affiliation.sql (mark_commission_paid L.331-362, atomicité + anti double-payout)
    - supabase/migrations/0017_scalable_foundation.sql (get_mrr L.262-278, revoke all mv_mrr L.278, wrap InitPlan)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Migration 0021 Mapping", §"RPC SECURITY DEFINER + admin_audit_log", §"KPIs : à-la-volée vs matview"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"supabase/migrations/0021_admin_cockpit.sql" (Layers A-E)
  </read_first>
  <action>Écrire `supabase/migrations/0021_admin_cockpit.sql` avec en-tête commenté, en 5 couches :
  **Layer A — policies SELECT superadmin** wrappées InitPlan dès l'écriture `using ((select public.is_superadmin()))` `for select to authenticated`, sur exactement : `profiles` (nom « profiles: superadmin voit tout »), `telegram_posts`, `candles`, ET `trade_setups` + `analyses` (assumption A1 : superadmin non garanti abonné — ajouter pour cohérence cockpit ; documenter en commentaire que si le superadmin de prod est toujours abonné ces 2 dernières sont superflues mais inoffensives). Les autres tables sont déjà couvertes par 0017 (ne rien ajouter).
  **Layer B — table `admin_audit_log`** : colonnes `id uuid pk default gen_random_uuid()`, `actor_id uuid not null references profiles(id)`, `action text not null check (action in ('grant_subscription_time','suspend_account','unsuspend_account','mark_commission_paid'))`, `target_type text not null check (target_type in ('user','commission'))`, `target_id uuid not null`, `payload jsonb not null default '{}'::jsonb`, `created_at timestamptz not null default now()`. `enable row level security`. UNE policy SELECT « admin_audit_log: superadmin voit tout » `using ((select public.is_superadmin()))`. AUCUNE policy insert/update/delete (écriture uniquement via RPC SECURITY DEFINER, miroir 0016).
  **Layer C — `profiles.suspended`** : `add column suspended boolean not null default false`, `add column suspended_reason text`, `add column suspended_at timestamptz`. Puis DROP/RECREATE `has_active_subscription()` (calque 0009 L.63-77, re-wrap InitPlan préservé) en ajoutant `and not exists (select 1 from public.profiles p where p.id = (select auth.uid()) and p.suspended)` → un suspendu lit 0 ligne partout où ce helper garde (trade_setups/analyses/candles). Source unique de vérité de la suspension (D-17).
  **Layer D — 4 RPC d'écriture** `language plpgsql security definer set search_path = public`, garde `if not (select public.is_superadmin()) then raise exception 'forbidden'` en tête, action + `insert into admin_audit_log (...) values ((select auth.uid()), ...)` dans la même transaction, puis `revoke execute ... from public, anon` + `grant execute ... to authenticated` : `grant_subscription_time(p_user_id uuid, p_interval text)` (p_interval whitelist `{'7 days','1 month','3 months'}` + borne custom ; upsert subscriptions : si actif `current_period_end = greatest(current_period_end, now()) + p_interval::interval`, sinon crée `status='active'` réplique def 0009) ; `suspend_account(p_user_id uuid, p_reason text)` ; `unsuspend_account(p_user_id uuid)` (réversible : suspended=false, reason=null) ; `admin_mark_commission_paid(p_commission_id uuid, p_tx_hash text, p_amount_atomic bigint)` (réplique `mark_commission_paid` 0016 : update commission `due→paid` avec `where status='due'` + `get diagnostics row_count` raise si 0 = anti double-payout, insert payouts, insert audit `action='mark_commission_paid' target_type='commission'`).
  **Layer E — wrappers KPI gated** `language sql stable security definer set search_path = public`, avec `where (select public.is_superadmin())` à la FIN (0 ligne pour non-superadmin, jamais throw, calque get_mrr) + revoke/grant : `get_acquisition_funnel(p_from date, p_to date) returns table(stage text, n bigint, source text)` (3 COUNT(DISTINCT user_id) : inscription profiles → 1er paiement verified → renouvellement, segment profiles.source, D-11) ; `get_churn(p_month date) returns table(churn_count bigint, active_start bigint)` (D-12 sur subscriptions.current_period_end) ; `get_plan_mix() returns table(plan text, n bigint)` (COUNT GROUP BY plan WHERE status='active' and current_period_end>now()). NE PAS créer de matview (D-10 : à-la-volée par défaut ; bascule décidée par EXPLAIN en Task 2). NE PAS toucher mv_mrr/get_mrr (réutilisés tels quels).</action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0021_admin_cockpit.sql | grep -q "admin_audit_log" && grep -c "security definer set search_path = public" supabase/migrations/0021_admin_cockpit.sql</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier définit `grant_subscription_time`, `suspend_account`, `unsuspend_account`, `admin_mark_commission_paid`, `get_acquisition_funnel`, `get_churn`, `get_plan_mix` toutes en `security definer set search_path = public`
    - Chaque RPC d'écriture contient `if not (select public.is_superadmin()) then raise` ET un `insert into ... admin_audit_log` dans le même bloc
    - `has_active_subscription()` recréé contient `suspended`
    - Policies SELECT superadmin créées pour `profiles`, `telegram_posts`, `candles`, `trade_setups`, `analyses` (wrap `(select public.is_superadmin())`)
    - Chaque fonction porte `revoke execute ... from public, anon` + `grant execute ... to authenticated`
    - Aucune policy insert/update/delete sur `admin_audit_log`
  </acceptance_criteria>
  <done>Migration 0021 écrite, 5 couches présentes, calques 0008/0009/0016/0017 respectés. PAS encore appliquée.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Appliquer 0021 LIVE (MCP) + régénérer types + gates advisors/EXPLAIN</name>
  <files>supabase/migrations/0021_admin_cockpit.sql, packages/supabase/database.types.ts</files>
  <read_first>
    - supabase/migrations/0021_admin_cockpit.sql (à appliquer)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Gate post-apply (D-02)", §"KPIs" (EXPLAIN ANALYZE A3), §"Common Pitfalls" Pitfall 5 (types désalignés)
    - .planning/STATE.md (convention 0017/0018 : apply_migration MCP, gen types, get_advisors)
  </read_first>
  <what-built>Migration 0021 prête à appliquer LIVE sur le projet Supabase via MCP. Cette tâche est BLOQUANTE : la phase ne peut pas passer la vérification sans elle (le build/typecheck passe sans, car les types viennent du fichier committé, pas de la DB live → faux positif). Aucune commande `db push`.</what-built>
  <action>[BLOCKING — autonomous:false] Appliquer `0021_admin_cockpit.sql` LIVE via le MCP Supabase `apply_migration` (JAMAIS `db push`, CONTEXT D-02), après confirmation interactive (écriture LIVE). Puis `generate_typescript_types` (MCP) et éditer à la main `packages/supabase/database.types.ts` (projet non `link`é) : ré-appliquer les alias maison + override `*_atomic` string, ajouter `admin_audit_log`, `profiles.suspended/suspended_reason/suspended_at` et les signatures des nouveaux RPC. Lancer `get_advisors(security)` (0 nouvelle fuite — les 2 WARN security-definer historiques sur is_superadmin/has_active_subscription sont EXPECTED BY DESIGN) et `get_advisors(performance)` (0 `auth_rls_initplan`) ; résoudre toute nouvelle alerte. Lancer `EXPLAIN ANALYZE` via `execute_sql` sur `get_acquisition_funnel`/`get_churn`/`get_plan_mix` sur le seed ~10k (D-10/A3) : confirmer Index Scan ; si coût rédhibitoire → basculer ce KPI en matview+wrapper gated et ré-appliquer ; sinon garder à-la-volée. Confirmer `EXPLAIN` keyset `profiles order by created_at desc, id desc limit 20` = Index Scan (si seq scan+sort → `execute_sql CREATE INDEX CONCURRENTLY` hors tx + gérer INVALID). Voir `<how-to-verify>` pour la séquence numérotée.</action>
  <how-to-verify>
    1. `apply_migration` via Supabase MCP avec le contenu de `0021_admin_cockpit.sql` (jamais `db push`, CONTEXT D-02). Aucun index CONCURRENTLY requis a priori (keyset profiles/payments déjà LIVE en 0017) — si l'EXPLAIN du point 4 révèle un seq scan+sort, créer l'index manquant hors transaction via `execute_sql` (`CREATE INDEX CONCURRENTLY`) et gérer l'état INVALID.
    2. `generate_typescript_types` via MCP → mettre à jour `packages/supabase/database.types.ts` à la main (le projet n'est pas `link`é) : ré-appliquer les alias maison + override `*_atomic` en `string` ; ajouter `admin_audit_log`, `profiles.suspended/suspended_reason/suspended_at`, et les signatures des nouveaux RPC.
    3. `get_advisors(security)` → 0 NOUVELLE fuite ; `get_advisors(performance)` → 0 `auth_rls_initplan` (wrap dès l'écriture). Résoudre toute nouvelle alerte avant de continuer.
    4. `EXPLAIN ANALYZE` via `execute_sql` sur `get_acquisition_funnel`, `get_churn`, `get_plan_mix` sur le seed ~10k (D-10/A3) : confirmer Index Scan, pas de seq scan + sort. Si un KPI montre un coût rédhibitoire → basculer ce KPI en matview + wrapper gated (D-10 l'autorise) et ré-appliquer ; sinon garder à-la-volée. Confirmer aussi `EXPLAIN` keyset `profiles order by created_at desc, id desc limit 20` = Index Scan (index 0017 LIVE).
    5. Lancer `pnpm --filter web test -- --run apps/web/test/admin-rls.test.ts` et `pnpm --filter web typecheck` : le test RLS deux-rôles (20-01) doit PASSER au vert, typecheck vert.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/test/admin-rls.test.ts && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apply_migration` exécuté (pas `db push`) ; objets 0021 présents en DB (vérifiable via `list_tables`/`execute_sql` : `admin_audit_log` existe, `profiles.suspended` existe)
    - `database.types.ts` contient `admin_audit_log` + `suspended` + types des nouveaux RPC ; `pnpm --filter web typecheck` vert
    - `get_advisors(security)` 0 nouvelle fuite ; `get_advisors(performance)` 0 `auth_rls_initplan`
    - `EXPLAIN ANALYZE` des 3 KPI = Index Scan (ou bascule matview documentée) ; keyset profiles = Index Scan
    - `apps/web/test/admin-rls.test.ts` vert (member 0 ligne, RPC KPI 0 ligne, RPC écriture forbidden, suspendu 0 ligne)
  </acceptance_criteria>
  <done>0021 LIVE via apply_migration, types régénérés et alignés (typecheck vert), advisors verts, EXPLAIN Index Scan, admin-rls.test vert.</done>
  <resume-signal>Tapez « appliqué » une fois 0021 LIVE, types régénérés, advisors verts, EXPLAIN Index Scan et admin-rls vert ; sinon décrivez l'alerte advisor / le plan EXPLAIN problématique.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anon-client (rôle quelconque) → DB | Toute lecture/écriture passe par RLS/RPC — seule barrière non contournable |
| RPC SECURITY DEFINER → tables | Le definer bypass RLS ; la garde interne `is_superadmin()` est le seul contrôle |
| matview/agrégat → PostgREST | Une matview sans wrapper est exposée à tout authentifié |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | profiles/telegram_posts/candles/trade_setups/analyses lus cross-tenant | mitigate | policies SELECT `using ((select public.is_superadmin()))` ; non-superadmin = 0 ligne (admin-rls.test) |
| T-20-06 | Elevation of Privilege | RPC d'écriture appelé par un non-superadmin | mitigate | garde `if not (select is_superadmin()) then raise 'forbidden'` en tête de chaque RPC |
| T-20-07 | Repudiation | action admin sensible non tracée | mitigate | `insert into admin_audit_log` atomique DANS chaque RPC (acteur+cible+action+payload+ts) |
| T-20-08 | Tampering | search_path mutable / fonction détournée | mitigate | `set search_path = public` figé sur chaque fonction (Pitfall function_search_path_mutable) |
| T-20-09 | Tampering | double-payout commission | mitigate | `where status='due'` + `get diagnostics row_count` raise si 0 (calque 0016) |
| T-20-10 | Tampering | injection valeur RPC `p_interval` | mitigate | whitelist `{'7 days','1 month','3 months'}` + borne custom avant cast interval |
| T-20-11 | Elevation of Privilege | compte suspendu continue de lire | mitigate | `has_active_subscription()` étendu `and not suspended` → 0 ligne RLS (barrière unique) |
| T-20-12 | Information Disclosure | matview KPI lue en direct | mitigate | wrappers gated `where (select is_superadmin())` ; `revoke all` matview ; jamais `.from()` direct |
| T-20-SC | Tampering | installs npm/pip | accept | Aucun install (migration SQL pure ; D-14) |
</threat_model>

<verification>
- `grep` prouve la présence des 7 fonctions en `security definer set search_path = public` et de `admin_audit_log`
- 0021 appliquée LIVE via `apply_migration` (jamais `db push`) ; objets présents en DB
- `get_advisors(security|performance)` sans nouvelle alerte ; EXPLAIN KPI/keyset = Index Scan
- `apps/web/test/admin-rls.test.ts` vert ; `pnpm --filter web typecheck` vert
</verification>

<success_criteria>
- Migration 0021 LIVE : 5 policies SELECT superadmin, admin_audit_log, profiles.suspended + helper étendu, 4 RPC écriture gated+audit, 3 wrappers KPI gated
- `database.types.ts` régénéré et aligné (typecheck vert)
- Gates advisors + EXPLAIN passés ; suspension = barrière RLS réelle prouvée
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-02-SUMMARY.md` à la fin (consigner : KPI gardés à-la-volée vs basculés matview, résultat EXPLAIN, alias types ajoutés).
</output>
