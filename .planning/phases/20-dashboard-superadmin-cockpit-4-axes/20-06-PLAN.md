---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 06
type: execute
wave: 4
depends_on: ["20-03", "20-04"]
files_modified:
  - apps/web/src/app/(admin)/membres/page.tsx
  - apps/web/src/app/(admin)/file/page.tsx
  - apps/web/src/app/(admin)/sante/page.tsx
  - apps/web/src/app/(admin)/signaux/page.tsx
  - apps/web/src/app/(admin)/signaux/[id]/page.tsx
  - apps/web/src/app/(admin)/affiliation/page.tsx
  - apps/web/src/app/(admin)/affiliation/affilies/page.tsx
  - apps/web/src/app/(admin)/affiliation/payouts/page.tsx
autonomous: true
requirements: [ADASH-01, ADASH-03, ADASH-04, ADASH-05, ADASH-07]

must_haves:
  truths:
    - "Toutes les pages détail (admin) lisent via anon-client createClient(), aucune n'importe createAdminServiceClient"
    - "La table utilisateurs (membres) pagine par curseur keyset avec filtres serveur (état abo / source / recherche), DOM borné ~50 lignes"
    - "La file de paiements pagine par keyset ; santé et signaux lisent via RLS (candles/telegram_posts débloqués par 0021)"
    - "Signaux admin restent en lecture seule (aucune édition/création)"
    - "Le scan rls-unchanged étendu à (admin) passe au vert (zéro service_role pages + actions)"
  artifacts:
    - path: "apps/web/src/app/(admin)/membres/page.tsx"
      provides: "table users keyset + filtres serveur + pagination « Charger la page suivante »"
      contains: "fetchAdminUsers"
    - path: "apps/web/src/app/(admin)/file/page.tsx"
      provides: "file paiements anon-client + keyset"
    - path: "apps/web/src/app/(admin)/sante/page.tsx"
      provides: "santé Ops anon-client (candles via 0021)"
  key_links:
    - from: "apps/web/src/app/(admin)/membres/page.tsx"
      to: "apps/web/src/lib/admin/queries.ts"
      via: "fetchAdminUsers + AdminUsersParamsSchema"
      pattern: "fetchAdminUsers"
    - from: "pages (admin)"
      to: "apps/web/src/lib/supabase/server.ts"
      via: "createClient anon"
      pattern: "createClient"
---

<objective>
Basculer les 8 pages détail `(admin)` du service_role vers l'anon-client + RLS (D-01), débloquées par les policies 0021. Câbler la table utilisateurs sur `fetchAdminUsers` (keyset + filtres serveur, DOM borné), la file de paiements sur le keyset, et reskiner DS v3 Tier App sobre. Santé/signaux/affiliation = simple swap anon (lecture seule pour signaux). C'est le dernier maillon qui éteint `rls-unchanged` étendu (avec les actions du plan 20-04).

Purpose: Retirer le dernier service_role des pages, tenir 10k lignes par keyset, prouver zéro fuite cross-tenant via RLS.
Output: 8 pages sur anon-client ; table users filtrable/paginée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md

<interfaces>
- `apps/web/src/lib/admin/queries.ts` (plan 20-03) : `fetchAdminUsers(params)` → `{ rows, nextCursor }`, keyset + filtres serveur, anon-client.
- `apps/web/src/lib/admin/searchParams.ts` (plan 20-01) : `parseAdminUsersParams(searchParams)`, `AdminUsersParamsSchema`.
- `apps/web/src/components/admin/MemberRowActions.tsx` (plan 20-04) : row-actions offrir/suspendre/réactiver (contrat props figé).
- `apps/web/src/app/(admin)/membres/page.tsx` : table render L.130-225 (markup à conserver), filtre JS en mémoire L.101-107 (à SUPPRIMER), `formatAtomic` L.202, `<bdi>`, Badge/Table shadcn. Utilise `createAdminServiceClient` L.26/L.57 → anon.
- `apps/web/src/lib/keyset/cursor.ts` : `encodeCursor`/`decodeCursor` (pour le lien page suivante).
- 0021 (plan 20-02) policies : `profiles`/`telegram_posts`/`candles`/`trade_setups`/`analyses` superadmin → débloque `profiles!inner(email)`, statut Telegram, fraîcheur candles.
- 20-UI-SPEC : tables denses (cell py 8px), pagination « Charger la page suivante » (keyset, pas de numéros de page), empty/error states FR verrouillés.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table utilisateurs (membres) — anon-client + keyset + filtres serveur</name>
  <files>apps/web/src/app/(admin)/membres/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/membres/page.tsx (markup table L.130-225, filtre JS L.101-107 à supprimer, formatAtomic L.202)
    - apps/web/src/lib/admin/queries.ts (fetchAdminUsers, plan 20-03)
    - apps/web/src/lib/admin/searchParams.ts (parseAdminUsersParams, plan 20-01)
    - apps/web/src/components/admin/MemberRowActions.tsx (props, plan 20-04)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"membres/page.tsx + file/page.tsx"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md §"Component Inventory", §"Interaction States"
  </read_first>
  <action>Remplacer `createAdminServiceClient` (L.26/L.57) par `await createClient()` anon de `@/lib/supabase/server` (le `profiles!inner(email)` fonctionne désormais sous RLS grâce à la policy 0021 `profiles`). SUPPRIMER le filtre en mémoire JS (L.101-107). Lire `parseAdminUsersParams(searchParams)` puis appeler `fetchAdminUsers(params)` (keyset + filtres serveur). Rendre la barre de filtres serveur (état d'abonnement active/expired/none, `source` demo/backtest/live, recherche email/id) qui réécrit l'URL (calque searchParams). Conserver le markup Table/Badge shadcn, `formatAtomic(BigInt(...))` pour les montants, `<bdi>`. Rendre `<MemberRowActions>` (offrir/suspendre/réactiver) par ligne en passant l'état `suspended`. Pagination keyset : bouton « Charger la page suivante » utilisant `nextCursor` (encodeCursor), désactivé quand `hasNext` faux ; pas de numéros de page. États vides/erreur avec la copy FR verrouillée (« Aucun résultat »…, « Impossible de charger ces données… »). Densité table : cell py 8px.</action>
  <verify>
    <automated>grep -L "createAdminServiceClient" "apps/web/src/app/(admin)/membres/page.tsx" | grep -q membres && grep -q "fetchAdminUsers" "apps/web/src/app/(admin)/membres/page.tsx" && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `membres/page.tsx` n'importe pas `createAdminServiceClient` (grep -L le renvoie) ; importe `fetchAdminUsers`
    - Le filtre JS en mémoire (ancien L.101-107) est supprimé ; filtres poussés côté requête
    - Pagination « Charger la page suivante » keyset présente (pas de numéros de page)
    - Rend `<MemberRowActions>` par ligne ; états vides/erreur FR verrouillés ; typecheck vert
  </acceptance_criteria>
  <done>Table users anon-client keyset + filtres serveur, DOM borné ~50, row-actions câblées.</done>
</task>

<task type="auto">
  <name>Task 2: File paiements (keyset) + bascule anon des pages restantes (sante/signaux/affiliation)</name>
  <files>apps/web/src/app/(admin)/file/page.tsx, apps/web/src/app/(admin)/sante/page.tsx, apps/web/src/app/(admin)/signaux/page.tsx, apps/web/src/app/(admin)/signaux/[id]/page.tsx, apps/web/src/app/(admin)/affiliation/page.tsx, apps/web/src/app/(admin)/affiliation/affilies/page.tsx, apps/web/src/app/(admin)/affiliation/payouts/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/file/page.tsx, sante/page.tsx, signaux/page.tsx, signaux/[id]/page.tsx
    - apps/web/src/app/(admin)/affiliation/page.tsx, affilies/page.tsx, payouts/page.tsx
    - apps/web/src/lib/keyset/cursor.ts (keyset file)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"{sante,signaux,affiliation}/page.tsx"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Bascule anon+RLS" (Pitfall 1/2)
  </read_first>
  <action>Pour les 7 pages : remplacer chaque `createAdminServiceClient()` par `await createClient()` anon de `@/lib/supabase/server` (retirer l'import `admin-service`).
  `file/page.tsx` : ajouter la pagination keyset (calque `membres` Task 1 : `.order(created_at desc, id desc).limit(PAGE_SIZE+1)` + curseur sanitizé + « Charger la page suivante ») sur la file de paiements ; `profiles!inner(email)` débloqué par 0021. Conserver `formatAtomic` pour les montants.
  `sante/page.tsx` : swap anon ; `v_data_freshness`/candles débloqués par la policy 0021 `candles` (sinon santé faussement rouge — Pitfall 2) ; feux via `freshness.ts`/`jobs.ts`.
  `signaux/page.tsx` + `[id]/page.tsx` : swap anon ; `telegram_posts` débloqué par 0021 (statut Telegram) ; trade_setups via policy 0021 ; RESTER EN LECTURE SEULE (aucune édition/création de signaux — D-09, frontière `persist.ts`).
  `affiliation/{page,affilies,payouts}` : simple swap anon (tables déjà gated superadmin en 0016/0017) ; la page payouts consomme l'action `admin_mark_commission_paid` (plan 20-04). Reskin DS v3 Tier App sobre partout (feux tokenisés, pas de néon sur tables denses).</action>
  <verify>
    <automated>for f in file/page.tsx sante/page.tsx signaux/page.tsx signaux/'[id]'/page.tsx affiliation/page.tsx affiliation/affilies/page.tsx affiliation/payouts/page.tsx; do grep -l "createAdminServiceClient" "apps/web/src/app/(admin)/$f" && echo "LEAK $f"; done | grep -q LEAK && exit 1; pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Aucune des 7 pages n'importe `createAdminServiceClient` (grep ne trouve aucun LEAK)
    - `file/page.tsx` pagine par keyset (« Charger la page suivante »)
    - `signaux` reste lecture seule (aucune Server Action d'édition/création de signal)
    - `sante` lit candles via RLS (policy 0021) ; typecheck vert
  </acceptance_criteria>
  <done>7 pages restantes sur anon-client, file keyset, signaux read-only, candles/telegram débloqués.</done>
</task>

<task type="auto">
  <name>Task 3: Confirmer l'extinction de rls-unchanged étendu à (admin)</name>
  <files>apps/web/src/styles/__tests__/rls-unchanged.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/rls-unchanged.test.ts (scan (admin) ajouté en 20-01)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Validation Architecture" (Wave 0 Gaps)
  </read_first>
  <action>Lancer le scan `rls-unchanged` (étendu à `(admin)` en plan 20-01) : il doit désormais PASSER au VERT, toutes les pages (plans 20-05/20-06) ET actions (plan 20-04) ayant retiré service_role/`admin-service`/`createAdminServiceClient`/client navigateur. Si rouge → identifier le fichier `(admin)` fautif encore porteur de service_role et le basculer en anon `createClient()` (ne jamais ajouter d'exception au scan). Lancer aussi la suite complète pour confirmer l'absence de régression.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/src/styles/__tests__/rls-unchanged.test.ts && pnpm --filter web test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `rls-unchanged.test.ts` (scan `(admin)`) VERT — zéro service_role pages + actions
    - Suite complète `pnpm --filter web test -- --run` verte ; `pnpm --filter web typecheck` vert
    - Aucun fichier `(admin)` n'importe `createAdminServiceClient`
  </acceptance_criteria>
  <done>Extinction de rls-unchanged confirmée ; bascule anon complète prouvée par scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anon-client (superadmin) → RLS | Toute lecture des tables détail repose sur les policies 0021/0017 |
| curseur/filtres URL → requête | Curseur opaque + filtres validés |
| service_role → bundle page | Doit être absent de toute page (admin) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | tables détail lues cross-tenant | mitigate | policies RLS superadmin 0021/0017 ; `admin-rls.test` prouve 0 ligne non-superadmin |
| T-20-03 | Elevation of Privilege | service_role résiduel sur une page | mitigate | bascule anon `createClient()` ; `rls-unchanged` étendu vert (Task 3) |
| T-20-13 | Tampering | injection `.or()` keyset (membres/file) | mitigate | `sanitizeCursor` dans `fetchAdminUsers` (plan 20-03) ; curseur opaque |
| T-20-18 | Tampering | édition/création de signal côté admin | mitigate | signaux read-only (D-09) ; aucune Server Action d'écriture sur signaux (préserve `persist.ts`) |
| T-20-02 | Tampering | filtres URL injectés | mitigate | `parseAdminUsersParams` safeParse champ-par-champ |
| T-20-SC | Tampering | installs npm | accept | Aucun install (D-14) |
</threat_model>

<verification>
- Aucune page `(admin)` n'importe `createAdminServiceClient` (grep)
- `membres` keyset+filtres serveur ; `file` keyset ; `signaux` read-only ; `sante` candles via RLS
- `rls-unchanged.test.ts` VERT + suite complète verte + `pnpm --filter web typecheck` vert
</verification>

<success_criteria>
- 8 pages détail sur anon-client + RLS (zéro service_role)
- Table users filtrable/paginée keyset à 10k, file paiements keyset
- Signaux lecture seule ; scan rls-unchanged au vert (bascule complète prouvée)
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-06-SUMMARY.md` à la fin.
</output>
