---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - apps/web/src/lib/admin/kpis.ts
  - apps/web/src/lib/admin/queries.ts
  - apps/web/src/lib/auth/gate.ts
  - apps/web/src/lib/admin/__tests__/queries.sanitize.test.ts
  - apps/web/src/lib/admin/__tests__/kpis.test.ts
autonomous: true
requirements: [ADASH-01, ADASH-02, ADASH-04, ADASH-07]

must_haves:
  truths:
    - "Les KPI (MRR/funnel/churn/mix) sont lus via wrappers .rpc() gated sur un anon-client, jamais via mv_mrr en direct"
    - "La table utilisateurs se pagine par curseur keyset avec curseur anti-injection (sanitizeCursor) et filtres serveur"
    - "Un compte suspendu est déconnecté au gate (requireUser → signOut) en plus de la barrière RLS"
  artifacts:
    - path: "apps/web/src/lib/admin/kpis.ts"
      provides: "wrappers typés .rpc(get_mrr/get_acquisition_funnel/get_churn/get_plan_mix), MRR via formatAtomic"
      contains: "get_acquisition_funnel"
    - path: "apps/web/src/lib/admin/queries.ts"
      provides: "fetchAdminUsers keyset + filtres (status/source/q), sanitizeCursor"
      contains: "sanitizeCursor"
    - path: "apps/web/src/lib/auth/gate.ts"
      provides: "branche suspension (suspended → signOut + redirect)"
      contains: "suspended"
  key_links:
    - from: "apps/web/src/lib/admin/queries.ts"
      to: "apps/web/src/lib/keyset/cursor.ts"
      via: "encodeCursor/decodeCursor"
      pattern: "decodeCursor"
    - from: "apps/web/src/lib/admin/kpis.ts"
      to: "supabase.rpc"
      via: "wrappers gated"
      pattern: "rpc\\('get_"
---

<objective>
Construire la couche d'accès données du cockpit sur anon-client : `kpis.ts` (wrappers typés des RPC KPI gated), `queries.ts` (requête keyset + filtres serveur de la table utilisateurs, curseur anti-injection), et la branche suspension du gate (`gate.ts`). Aucune lecture ne passe par service_role ; les agrégats passent par les wrappers gated, jamais par la matview en direct.

Purpose: Donner aux pages (plans 20-05/20-06) des fonctions typées prêtes à consommer — pas de scavenger hunt. Interface-first : ces contrats existent avant les pages qui les utilisent.
Output: 3 modules lib ; le contrat de suspension côté UX complète la barrière RLS de 0021.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md

<interfaces>
- `apps/web/src/lib/watchlist/queries.ts` : `sanitizeCursor` (L.87-101, regex ISO+UUID), `.or()` tuple-compare keyset (L.114-153), sentinelle PAGE_SIZE+1, `encodeCursor`.
- `apps/web/src/lib/keyset/cursor.ts` : `encodeCursor({createdAt,id})` / `decodeCursor(raw)` base64url opaque, tolérant (null = 1re page).
- `apps/web/src/lib/admin/searchParams.ts` (plan 20-01) : `AdminUsersParamsSchema`, `parseAdminUsersParams`.
- `apps/web/src/lib/supabase/server.ts` : `createClient()` anon @supabase/ssr (cookies) — remplace `admin-service`.
- `apps/web/src/lib/auth/gate.ts` : `authedClient()` (L.58-76), `requireUser`, `requireActiveSub` (L.83-97), `requireRole` (lit profiles L.102-106).
- `@app/core` : `formatAtomic(BigInt)` → string. `database.types.ts` (régénéré 20-02) : signatures `get_mrr/get_acquisition_funnel/get_churn/get_plan_mix`, `profiles.suspended`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/admin/queries.ts — keyset utilisateurs + filtres serveur + curseur anti-injection</name>
  <files>apps/web/src/lib/admin/queries.ts</files>
  <read_first>
    - apps/web/src/lib/watchlist/queries.ts (sanitizeCursor L.87-101, keyset .or() L.114-153)
    - apps/web/src/lib/keyset/cursor.ts (encode/decode)
    - apps/web/src/lib/admin/searchParams.ts (AdminUsersParamsSchema)
    - apps/web/src/lib/supabase/server.ts (createClient anon)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Keyset pagination & filtres serveur admin", Code Examples L.421-432
  </read_first>
  <behavior>
    - sanitizeCursor copié verbatim : curseur dont createdAt non-ISO ou id non-UUID → null (pas d'interpolation)
    - fetchAdminUsers retourne au plus PAGE_SIZE lignes + un `nextCursor` quand la sentinelle PAGE_SIZE+1 est présente
    - filtre source → `.eq('source', …)` ; filtre q → `.ilike('email', '%…%')` paramétré ; filtre status traduit sur la jointure subscriptions
    - curseur posé via `.or('created_at.lt.<c>,and(created_at.eq.<c>,id.lt.<id>)')` SEULEMENT après sanitizeCursor
  </behavior>
  <action>Créer `lib/admin/queries.ts` exportant `PAGE_SIZE` (~50) et `fetchAdminUsers(params: AdminUsersParams)`. Copier `sanitizeCursor` (regex `ISO_TIMESTAMP`/`UUID`) verbatim depuis `watchlist/queries.ts`. Client = `createClient()` anon de `../supabase/server` (JAMAIS `admin-service`). Requête : `.from('profiles').select('id, email, created_at, source, role, suspended, subscriptions(status, plan, current_period_end)').order('created_at',{ascending:false}).order('id',{ascending:false}).limit(PAGE_SIZE + 1)`. Appliquer `.eq('source', params.source)` si présent, `.ilike('email', '%'+params.q+'%')` si présent (valeur paramétrée), et le filtre `status` (active/expired/none) sur la jointure subscriptions (colonne indexée `subscriptions_active_idx`). Si `cursor = sanitizeCursor(decodeCursor(params.cursor))` non-null : `.or(\`created_at.lt.${cursor.createdAt},and(created_at.eq.${cursor.createdAt},id.lt.${cursor.id})\`)`. Détacher la sentinelle PAGE_SIZE+1 pour calculer `nextCursor = encodeCursor(...)`. Retourner `{ rows, nextCursor }`. NE PAS filtrer en mémoire JS. Co-localiser un petit test de `sanitizeCursor` (cas ISO/UUID valides/invalides) dans `lib/admin/__tests__/queries.sanitize.test.ts`.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/src/lib/admin/__tests__/queries.sanitize.test.ts && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `queries.ts` importe `createClient` de `../supabase/server` (assertion source : pas d'import `admin-service`)
    - `sanitizeCursor` présent ; le `.or()` n'est posé qu'après sanitize (behavior assertion)
    - `fetchAdminUsers` retourne `{ rows, nextCursor }` ; aucune filtration en mémoire
    - typecheck vert avec les types régénérés 20-02
  </acceptance_criteria>
  <done>Requête keyset+filtres serveur livrée, curseur anti-injection testé, anon-client only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/admin/kpis.ts — wrappers typés des RPC KPI gated</name>
  <files>apps/web/src/lib/admin/kpis.ts</files>
  <read_first>
    - apps/web/src/lib/auth/gate.ts (L.89 forme `.rpc('has_active_subscription')`)
    - supabase/migrations/0021_admin_cockpit.sql (signatures get_acquisition_funnel/get_churn/get_plan_mix, plan 20-02)
    - supabase/migrations/0017_scalable_foundation.sql (get_mrr contrat, revenue_atomic)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"KPIs", §"Don't Hand-Roll" (MRR)
  </read_first>
  <behavior>
    - getMrr() retourne le dernier mois { month, payments_count, revenue_atomic } ; montant via formatAtomic(BigInt(revenue_atomic)), libellé « cash encaissé / mois »
    - getAcquisitionFunnel(from,to)/getChurn(month)/getPlanMix() délèguent à supabase.rpc(...) et renvoient des lignes typées
    - chaque wrapper renvoie [] / null gracieusement si l'appelant n'est pas superadmin (le RPC renvoie 0 ligne, jamais throw)
    - aucun accès `.from('mv_mrr')` direct
  </behavior>
  <action>Créer `lib/admin/kpis.ts` exportant `getMrr()`, `getAcquisitionFunnel(pFrom, pTo)`, `getChurn(pMonth)`, `getPlanMix()`. Chaque fonction obtient `const supabase = await createClient()` (anon @supabase/ssr) et appelle `supabase.rpc('get_mrr')` / `.rpc('get_acquisition_funnel', { p_from, p_to })` / `.rpc('get_churn', { p_month })` / `.rpc('get_plan_mix')`. Typer les retours à partir de `database.types.ts`. Exposer un helper de présentation MRR qui formate `revenue_atomic` via `formatAtomic(BigInt(revenue_atomic))` et porte le libellé verrouillé « cash encaissé / mois » (JAMAIS « MRR récurrent », D-13). NE JAMAIS lire `mv_mrr` via `.from()` (Pitfall 3 — `revoke all` 0017). Les valeurs `%` (churn) restent des nombres mesurés rendus via `applyThreshold` côté composant, jamais des chaînes littérales. Co-localiser un test `lib/admin/__tests__/kpis.test.ts` qui mocke le client et vérifie que chaque wrapper appelle le bon nom de RPC avec les bons paramètres, et qu'aucun `.from('mv_mrr')` n'est utilisé.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/src/lib/admin/__tests__/kpis.test.ts && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `kpis.ts` appelle `rpc('get_mrr')`, `rpc('get_acquisition_funnel')`, `rpc('get_churn')`, `rpc('get_plan_mix')` (assertion source)
    - Aucun `.from('mv_mrr')` (assertion source) ; MRR formaté via `formatAtomic`
    - Test : chaque wrapper passe les paramètres attendus ; typecheck vert
  </acceptance_criteria>
  <done>Wrappers KPI typés gated livrés, MRR honnête, zéro lecture matview directe.</done>
</task>

<task type="auto">
  <name>Task 3: gate.ts — branche suspension (couche UX complémentaire à la RLS)</name>
  <files>apps/web/src/lib/auth/gate.ts</files>
  <read_first>
    - apps/web/src/lib/auth/gate.ts (authedClient L.58-76, requireUser, requireRole lecture profiles L.102-106)
    - supabase/migrations/0021_admin_cockpit.sql (colonne suspended + helper étendu, plan 20-02)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"profiles.suspended (D-17) — colonne + barrière réelle"
  </read_first>
  <action>Dans `gate.ts`, après `getUser()` dans `requireUser` (ou `authedClient()`), lire `profiles.suspended` (calque la lecture profile déjà faite par `requireRole` L.102-106). Si `suspended === true` → `supabase.auth.signOut()` puis `redirect('/login?suspended=1')`. C'est la couche UX : la VRAIE barrière est la RLS (`has_active_subscription()` étendu en 0021). Ne pas dupliquer la logique dans chaque page. Conserver les signatures existantes de `requireUser`/`requireActiveSub`/`requireRole` (ajout interne uniquement). Préserver le comportement superadmin (`requireRole('superadmin')` du layout admin inchangé).</action>
  <verify>
    <automated>grep -q "suspended" apps/web/src/lib/auth/gate.ts && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `gate.ts` lit `profiles.suspended` après `getUser()` ; `suspended` → `signOut()` + `redirect('/login?suspended=1')`
    - Signatures publiques de `requireUser`/`requireActiveSub`/`requireRole` inchangées
    - typecheck vert ; aucune régression des tests existants du gate
  </acceptance_criteria>
  <done>Branche suspension UX ajoutée, complémentaire à la barrière RLS 0021.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL (curseur/filtres) → requête PostgREST | Curseur opaque interpolé dans `.or()` (non paramétré) |
| anon-client → RPC KPI | Lecture agrégée ; la garde wrapper est la seule frontière |
| session → gate | Un compte suspendu tente d'accéder |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Tampering | injection via `.or()` keyset | mitigate | `sanitizeCursor` (ISO+UUID) avant interpolation ; curseur opaque |
| T-20-14 | Information Disclosure | lecture `mv_mrr` directe | mitigate | wrappers `.rpc(get_*)` uniquement ; assertion source « pas de `.from('mv_mrr')` » |
| T-20-12 | Information Disclosure | KPI lu hors gate | mitigate | RPC gated (0 ligne non-superadmin) ; le wrapper ne contourne pas la garde DB |
| T-20-11 | Elevation of Privilege | suspendu continue d'accéder | mitigate | gate `signOut` + RLS `has_active_subscription` étendu (double barrière) |
| T-20-SC | Tampering | installs npm | accept | Aucun install (réutilise lock-file, D-14) |
</threat_model>

<verification>
- `pnpm --filter web test -- --run apps/web/src/lib/admin/__tests__/` vert (sanitize + kpis)
- Assertions source : `queries.ts`/`kpis.ts` n'importent pas `admin-service` et ne lisent pas `mv_mrr` en direct
- `grep suspended gate.ts` ; `pnpm --filter web typecheck` vert
</verification>

<success_criteria>
- `kpis.ts` + `queries.ts` + branche suspension `gate.ts` livrés sur anon-client
- Curseur anti-injection et filtres serveur prêts pour 10k lignes
- MRR honnête (« cash encaissé / mois », formatAtomic), zéro lecture matview directe
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-03-SUMMARY.md` à la fin.
</output>
