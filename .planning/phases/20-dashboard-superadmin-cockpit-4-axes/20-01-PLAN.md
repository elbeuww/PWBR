---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/admin/searchParams.ts
  - apps/web/src/lib/admin/__tests__/searchParams.test.ts
  - apps/web/test/admin-rls.test.ts
  - apps/web/src/styles/__tests__/rls-unchanged.test.ts
  - apps/web/test/no-perf-claims.test.ts
  - apps/web/test/no-perf-seed-claims.test.ts
autonomous: true
requirements: [ADASH-02, ADASH-04, ADASH-07]

must_haves:
  truths:
    - "Un test prouve qu'un non-superadmin (anon-client) lit 0 ligne sur chaque table admin et que les RPC gated refusent l'accès"
    - "Un scan statique échoue si une page/action de (admin) importe createAdminServiceClient ou service_role"
    - "Un scan échoue si la copy admin contient un % littéral ou un chiffre de perf fabriqué (equity/ROI/PnL)"
    - "Le helper de filtres users parse état d'abonnement / source / recherche par champ et rejette les valeurs hors-enum sans throw"
  artifacts:
    - path: "apps/web/src/lib/admin/searchParams.ts"
      provides: "AdminUsersParamsSchema + parse/serialize (status/source/q/cursor)"
      contains: "AdminUsersParamsSchema"
    - path: "apps/web/test/admin-rls.test.ts"
      provides: "RLS deux-rôles cross-tenant + RPC forbidden + suspension 0-ligne"
      min_lines: 40
    - path: "apps/web/src/lib/admin/__tests__/searchParams.test.ts"
      provides: "round-trip parse/serialize, hors-enum → undefined"
  key_links:
    - from: "apps/web/src/styles/__tests__/rls-unchanged.test.ts"
      to: "apps/web/src/app/(admin)"
      via: "SCANNED_GROUPS includes admin"
      pattern: "admin"
    - from: "apps/web/test/no-perf-claims.test.ts"
      to: "namespace admin"
      via: "SCANNED_NAMESPACES"
      pattern: "admin"
---

<objective>
Poser tous les garde-fous Wave 0 de la phase 20 AVANT toute implémentation : le helper de filtres serveur de la table utilisateurs (avec son test unitaire, vert immédiatement), le test d'intégration RLS deux-rôles (rouge jusqu'à la migration 0021), et l'extension des scans statiques de sécurité/honnêteté au groupe `(admin)` (rouge jusqu'au retrait de service_role et au reskin du cockpit).

Purpose: Donner à chaque tâche d'implémentation aval une cible exécutable (Nyquist). Les tests RED définissent le contrat ; ils passent au vert au fur et à mesure (migration → cockpit → pages → actions).
Output: 1 helper + 1 test unitaire (GREEN) ; 1 test RLS d'intégration + 3 extensions de scans (RED par conception, mappés à leur tâche d'extinction).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-CONTEXT.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-VALIDATION.md

<interfaces>
<!-- Contrats à répliquer. Lire les analogs avant d'écrire. -->
- `apps/web/src/lib/signals/searchParams.ts` exporte `WatchlistParamsSchema` (z.object) + parse field-by-field `.safeParse` + serialize qui omet les défauts. Calque exact pour `AdminUsersParamsSchema`.
- `apps/web/src/lib/signals/__tests__/searchParams.test.ts` : structure vitest (round-trip parse/serialize, valeur hors-enum → undefined).
- `apps/web/src/styles/__tests__/rls-unchanged.test.ts` : `SCANNED_GROUPS` (liste de route groups scannés), détecteurs `FORBIDDEN` (admin-service / createAdminServiceClient / service_role / browser client), `stripComments` (ignore la prose d'en-tête).
- `apps/web/test/no-perf-claims.test.ts` : `SCANNED_NAMESPACES` (namespaces i18n scannés), détecteur `FORBIDDEN = /%|\d+\s*%|garanti|.../i`.
- `apps/web/test/no-perf-seed-claims.test.ts` : volet C overview scanne des fichiers source ; ajouter un tableau `ADMIN_UI_FILES` + détecteur `FORBIDDEN_PERF_UI = /\bequity\b|P&L|PnL|\bROI\b|[+-]\s*\d+%/i`.
- `vitest.config.ts` (racine) charge `.env.test` (clé anon) → les tests RLS d'intégration utilisent un anon-client deux-rôles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper de filtres serveur AdminUsersParamsSchema + test unitaire</name>
  <files>apps/web/src/lib/admin/searchParams.ts, apps/web/src/lib/admin/__tests__/searchParams.test.ts</files>
  <read_first>
    - apps/web/src/lib/signals/searchParams.ts (calque exact `WatchlistParamsSchema`, parse field-by-field, serialize)
    - apps/web/src/lib/signals/__tests__/searchParams.test.ts (structure du test round-trip)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Keyset pagination & filtres serveur admin" (D-15)
  </read_first>
  <behavior>
    - parse({status:'active'}) → { status:'active' } ; parse({status:'bogus'}) → status absent (jamais throw)
    - parse({source:'demo'}) accepté ; source hors {demo,backtest,live} → undefined
    - parse({q:'a@b.co'}) → { q:'a@b.co' } ; q vide → undefined
    - parse({cursor:'<opaque>'}) conservé tel quel (validé plus tard par sanitizeCursor côté requête)
    - serialize(parse(x)) round-trip stable, omet les champs absents/défaut
  </behavior>
  <action>Créer `lib/admin/searchParams.ts` exportant `AdminUsersParamsSchema` (z.object) avec `status: z.enum(['active','expired','none']).optional()`, `source: z.enum(['demo','backtest','live']).optional()`, `q: z.string().min(1).optional()` (recherche email/id), `cursor: z.string().min(1).optional()`. Exporter `parseAdminUsersParams(searchParams)` qui fait du `.safeParse` CHAMP PAR CHAMP (calque `signals/searchParams.ts` : une valeur hors-enum devient `undefined`, jamais d'exception, ne parvient jamais à la requête — anti-injection T-03-05) et `serializeAdminUsersParams(params)` qui omet défauts/absents. Créer le dossier `lib/admin/__tests__/` et le test calqué sur `signals/__tests__/searchParams.test.ts`.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/src/lib/admin/__tests__/searchParams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/admin/searchParams.ts` exporte `AdminUsersParamsSchema`, `parseAdminUsersParams`, `serializeAdminUsersParams`
    - Test : `parseAdminUsersParams({ status: 'bogus' }).status === undefined` (pas de throw)
    - Test : round-trip `serialize(parse(x))` stable ; suite verte sur ce fichier
  </acceptance_criteria>
  <done>Helper + test verts ; aucune dépendance nouvelle (réutilise zod déjà au lock-file).</done>
</task>

<task type="auto">
  <name>Task 2: Test d'intégration RLS deux-rôles admin-rls.test.ts (RED jusqu'à 0021)</name>
  <files>apps/web/test/admin-rls.test.ts</files>
  <read_first>
    - apps/web/src/lib/signals/__tests__/searchParams.test.ts (structure vitest)
    - vitest.config.ts (chargement `.env.test`, clé anon)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Validation Architecture" → "Wave 0 Gaps" et tableau "Phase Requirements → Test Map"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>Créer `apps/web/test/admin-rls.test.ts` : test d'intégration anon-client deux rôles (un compte superadmin seedé, un compte member seedé) qui assert : (1) le member lit 0 ligne sur `profiles` (autres que la sienne), `telegram_posts`, `candles`, `trade_setups`, `payments`, `payouts`, `subscriptions` (cross-tenant) ; (2) `supabase.rpc('get_mrr')`, `get_acquisition_funnel`, `get_churn`, `get_plan_mix` renvoient 0 ligne (jamais throw) en rôle member ; (3) les RPC d'écriture `grant_subscription_time`, `suspend_account`, `unsuspend_account`, `admin_mark_commission_paid` lèvent `forbidden` en rôle member ; (4) après `suspend_account`, le compte suspendu lit 0 `trade_setups`/`analyses` via RLS. Marquer le bloc RPC/policy avec un commentaire `// RED until 0021 (plan 20-02)`. Le test DOIT référencer les noms exacts de RPC/tables ci-dessus pour servir de contrat. Le fichier est attendu ROUGE tant que 0021 n'est pas appliquée (plan 20-02 l'éteint).</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/test/admin-rls.test.ts 2>&1 | grep -Eq "admin-rls|get_mrr|forbidden|profiles" && echo "test present"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/test/admin-rls.test.ts` existe, ≥ 40 lignes, référence les identifiants exacts : `get_mrr`, `get_acquisition_funnel`, `get_churn`, `get_plan_mix`, `grant_subscription_time`, `suspend_account`, `unsuspend_account`, `admin_mark_commission_paid`, `admin_audit_log`
    - Behavior assertion : en rôle member, RPC KPI → 0 ligne ; RPC écriture → exception `forbidden`
    - RED documenté : commentaire `RED until 0021` présent ; sera vert après 20-02
  </acceptance_criteria>
  <done>Fichier de test présent, contrat RLS deux-rôles encodé, RED par conception (éteint en 20-02).</done>
</task>

<task type="auto">
  <name>Task 3: Étendre les scans statiques (rls-unchanged + no-perf-claims + no-perf-seed-claims) au groupe (admin)</name>
  <files>apps/web/src/styles/__tests__/rls-unchanged.test.ts, apps/web/test/no-perf-claims.test.ts, apps/web/test/no-perf-seed-claims.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/rls-unchanged.test.ts (`SCANNED_GROUPS`, `FORBIDDEN`, `stripComments`)
    - apps/web/test/no-perf-claims.test.ts (`SCANNED_NAMESPACES`, `FORBIDDEN`)
    - apps/web/test/no-perf-seed-claims.test.ts (volet C overview, pattern de scan source)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Test no-perf-claims étendu admin" (D-13)
  </read_first>
  <action>(1) Dans `rls-unchanged.test.ts`, ajouter `(admin)` à `SCANNED_GROUPS` afin que les détecteurs existants (`createAdminServiceClient` / import `admin-service` / `SUPABASE_SERVICE_ROLE_KEY` / client navigateur) couvrent toutes les pages ET actions sous `(admin)`. Conserver `stripComments` (la prose d'en-tête « jamais service_role » ne doit pas déclencher). (2) Dans `no-perf-claims.test.ts`, ajouter `'admin'` à `SCANNED_NAMESPACES` (le scan tolérant gère un namespace partiel/absent). (3) Dans `no-perf-seed-claims.test.ts`, ajouter un tableau `ADMIN_UI_FILES` listant `apps/web/src/app/(admin)/page.tsx` (+ `_components/AxisSummary*.tsx` une fois créés) avec un détecteur `FORBIDDEN_PERF_UI = /\bequity\b|P&L|PnL|\bROI\b|[+-]\s*\d+%/i`. Ces 3 extensions sont attendues ROUGES : rls-unchanged passe au vert quand service_role est retiré (plans 20-04 actions + 20-06 pages) ; no-perf-claims/seed passent au vert quand le cockpit honnête est livré (plan 20-05). Documenter chaque extinction en commentaire.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/src/styles/__tests__/rls-unchanged.test.ts apps/web/test/no-perf-claims.test.ts apps/web/test/no-perf-seed-claims.test.ts 2>&1 | grep -Eq "admin|ADMIN_UI_FILES|SCANNED" && echo "scans extended"</automated>
  </verify>
  <acceptance_criteria>
    - `rls-unchanged.test.ts` : `(admin)` présent dans `SCANNED_GROUPS` ; `stripComments` conservé
    - `no-perf-claims.test.ts` : `'admin'` présent dans `SCANNED_NAMESPACES`
    - `no-perf-seed-claims.test.ts` : `ADMIN_UI_FILES` + `FORBIDDEN_PERF_UI` présents, scannant `(admin)/page.tsx`
    - Chaque extension porte un commentaire d'extinction (quel plan la passe au vert)
  </acceptance_criteria>
  <done>3 scans étendus à `(admin)`, RED par conception, extinctions documentées.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL query → serveur | Filtres/curseur entrants non fiables (status/source/q/cursor) |
| anon-client (rôle member) → DB | Le test simule un attaquant authentifié non-superadmin |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | tables admin lues cross-tenant | mitigate | `admin-rls.test.ts` prouve 0 ligne en rôle member (contrat Wave 0 ; barrière réelle posée en 20-02) |
| T-20-02 | Tampering | filtres URL injectés dans la requête | mitigate | `AdminUsersParamsSchema` safeParse champ-par-champ ; hors-enum → undefined, jamais concaténé brut |
| T-20-03 | Elevation of Privilege | service_role bundlé côté page | mitigate | `rls-unchanged` étendu à `(admin)` détecte tout import service_role |
| T-20-04 | Repudiation | chiffre de perf fabriqué affiché | mitigate | `no-perf-claims`/`no-perf-seed-claims` étendus bloquent `%` littéral et equity/ROI |
| T-20-SC | Tampering | installs npm/pip | accept | Aucun install cette phase (D-14 zéro nouvelle dépendance) — pas de surface supply-chain |
</threat_model>

<verification>
- `pnpm --filter web test -- --run apps/web/src/lib/admin/__tests__/searchParams.test.ts` vert
- Les fichiers `admin-rls.test.ts` et les 3 extensions de scan existent et référencent les identifiants/groupes attendus (RED par conception, extinctions tracées)
- `pnpm --filter web typecheck` vert (helper typé)
</verification>

<success_criteria>
- Helper de filtres serveur livré + test unitaire vert
- Contrat RLS deux-rôles encodé dans `admin-rls.test.ts` (8 RPC/tables nommés)
- 3 scans statiques étendus à `(admin)` avec extinction documentée par plan
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-01-SUMMARY.md` à la fin.
</output>
