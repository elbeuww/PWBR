---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 05
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - apps/web/src/app/(admin)/page.tsx
  - apps/web/src/app/(admin)/_components/AdminSidebar.tsx
  - apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx
  - apps/web/src/app/(admin)/_components/AxisSummaryOps.tsx
  - apps/web/src/app/(admin)/_components/AxisSummaryAcquisition.tsx
  - apps/web/src/app/(admin)/_components/AxisSummaryConformite.tsx
autonomous: true
requirements: [ADASH-01, ADASH-02, ADASH-03, ADASH-06]

must_haves:
  truths:
    - "La home (admin) est un cockpit 4 sections dans l'ordre Revenus → Ops → Acquisition → Conformité (D-07)"
    - "Chaque carte d'axe affiche des KPI mesurés via les wrappers gated, une ligne de provenance, et un lien Voir le détail"
    - "Le MRR est libellé « cash encaissé / mois » via formatAtomic ; aucun % littéral ni chiffre de perf fabriqué"
    - "La sidebar regroupe les liens sous les 4 axes, URLs détail inchangées (aucune redirection)"
    - "Le panneau Conformité affiche le feu LEGAL_REVIEW_DONE + version artefact + date de revue"
    - "La home lit via anon-client createClient(), jamais createAdminServiceClient"
  artifacts:
    - path: "apps/web/src/app/(admin)/page.tsx"
      provides: "cockpit 4 sections ordonnées, anon-client, KPI via lib/admin/kpis"
      contains: "AxisSummary"
    - path: "apps/web/src/app/(admin)/_components/AxisSummaryConformite.tsx"
      provides: "feu LEGAL_REVIEW_DONE + version + date (D-18)"
      contains: "isLegalReviewDone"
    - path: "apps/web/src/app/(admin)/_components/AdminSidebar.tsx"
      provides: "nav regroupée 4 axes, URLs inchangées"
      contains: "Acquisition"
  key_links:
    - from: "apps/web/src/app/(admin)/page.tsx"
      to: "apps/web/src/lib/admin/kpis.ts"
      via: "getMrr/getAcquisitionFunnel/getChurn/getPlanMix"
      pattern: "getMrr|getChurn|getPlanMix"
    - from: "AxisSummary cards"
      to: "pages détail existantes"
      via: "Link Voir le détail"
      pattern: "Voir le détail"
---

<objective>
Transformer la home `(admin)/page.tsx` (germe head-counts service_role) en cockpit 4 sections dans l'ordre Revenus → Ops → Acquisition → Conformité (D-05/D-07), sur anon-client, KPI lus via `lib/admin/kpis.ts`. Créer les 4 cartes d'axe (KPI mesuré + ligne de provenance obligatoire + lien « Voir le détail »), regrouper la sidebar par les 4 axes (URLs inchangées), et livrer le panneau Conformité (feu LEGAL_REVIEW_DONE + version + date).

Purpose: Donner la salle de contrôle sobre — chiffres mesurés, jamais fabriqués. Éteint les scans no-perf-claims/no-perf-seed-claims (plan 20-01) une fois la copy honnête en place.
Output: home cockpit + 4 AxisSummary + sidebar 4 axes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md

<interfaces>
- `apps/web/src/lib/admin/kpis.ts` (plan 20-03) : `getMrr()`, `getAcquisitionFunnel(pFrom,pTo)`, `getChurn(pMonth)`, `getPlanMix()` (anon-client, gated, MRR « cash encaissé / mois » via formatAtomic).
- `apps/web/src/app/(admin)/page.tsx` (germe) : KPI head-counts L.67-149, pattern Card-in-Link L.166-205, feux tokenisés L.55-59 (green→bg-[var(--signal-bullish)], amber→bg-[var(--risk-moderate)], red→bg-destructive). Utilise `createAdminServiceClient` L.19/L.68 → à remplacer par `createClient()` anon.
- `apps/web/src/app/(admin)/_components/AdminSidebar.tsx` : `NAV_ITEMS` L.36-44, `isActive` prefix-match L.46-49, accent `--primary` actif L.68, hrefs littéraux `/admin/...` (PAS de Link i18n).
- `apps/web/src/lib/legal-gate.ts` : `isLegalReviewDone()` (env `LEGAL_REVIEW_DONE`).
- `apps/web/src/lib/admin/{freshness,jobs,signals}.ts` : helpers feux (Ops).
- 20-UI-SPEC : ordre Revenus→Ops→Acquisition→Conformité ; KPI value mono/tabular ; ligne provenance « Mesuré · N = {n} · {période} · source : {source} » (rendue, jamais string i18n) ; MRR « Cash encaissé / mois » ; CTA « Voir le détail » ; accent `--primary` réservé.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cartes d'axe AxisSummary (Revenus/Ops/Acquisition/Conformité)</name>
  <files>apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx, apps/web/src/app/(admin)/_components/AxisSummaryOps.tsx, apps/web/src/app/(admin)/_components/AxisSummaryAcquisition.tsx, apps/web/src/app/(admin)/_components/AxisSummaryConformite.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/page.tsx (Card-in-Link L.166-205, feux L.55-59)
    - apps/web/src/lib/admin/kpis.ts (wrappers KPI, plan 20-03)
    - apps/web/src/lib/legal-gate.ts (isLegalReviewDone)
    - apps/web/src/lib/admin/freshness.ts, apps/web/src/lib/admin/jobs.ts, apps/web/src/lib/admin/signals.ts (feux Ops)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"AxisSummary*.tsx"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md §"Typography", §"Color", §"Copywriting Contract"
  </read_first>
  <action>Créer 4 composants RSC `AxisSummary*.tsx` réutilisant `Card`/`CardHeader`/`CardContent` shadcn (calque page.tsx L.166-205). Chacun reçoit ses données KPI en props (calculées dans page.tsx, Task 2) et rend : un titre d'axe (28px semibold), la valeur KPI headline en `--font-mono` tabulaire (20px), une ligne de provenance OBLIGATOIRE « Mesuré · N = {n} · {période} · source : {source} » avec les nombres RENDUS (jamais des chaînes i18n littérales), et un Link « Voir le détail » vers la page détail existante.
  `AxisSummaryRevenus` : MRR libellé « Cash encaissé / mois » (formatAtomic), churn (valeur mesurée via `applyThreshold`, jamais `%` littéral), mix de plans (counts par plan) → lien `/admin/membres`.
  `AxisSummaryOps` : fraîcheur données (feux via `freshness.ts`), jobs récents (`jobs.ts`), file de paiements (count due) → lien `/admin/sante` (et `/admin/file`).
  `AxisSummaryAcquisition` : étapes funnel (inscription → activation → rétention, counts) + perf affiliés → lien `/admin/affiliation`.
  `AxisSummaryConformite` : feu `isLegalReviewDone()` (green `--signal-bullish` si done, red `--destructive` sinon) + version de l'artefact légal en vigueur + date de revue (D-18), panneau read-only.
  Feux : green→`--signal-bullish`, amber→`--risk-moderate`, red→`--destructive` (D-08), jamais d'hex en dur ; accent `--primary` uniquement sur le CTA/lien. Aucun `%` littéral, aucun equity/ROI/PnL.</action>
  <verify>
    <automated>grep -q "Cash encaissé" "apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx" && grep -q "Mesuré" "apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx" && grep -q "Voir le détail" "apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx" && grep -q "isLegalReviewDone" "apps/web/src/app/(admin)/_components/AxisSummaryConformite.tsx" && ! grep -q "%" "apps/web/src/app/(admin)/_components/AxisSummaryRevenus.tsx" && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers `AxisSummary*.tsx` existent ; `AxisSummaryRevenus` contient « Cash encaissé »
    - `AxisSummaryConformite` appelle `isLegalReviewDone` et affiche version + date
    - Chaque carte rend une ligne de provenance « Mesuré · N = … » avec nombres rendus + un Link « Voir le détail »
    - Aucun `%` littéral / equity / ROI dans la copy ; feux via tokens (`--signal-bullish`/`--risk-moderate`/`--destructive`)
    - typecheck vert
  </acceptance_criteria>
  <done>4 cartes d'axe honnêtes (KPI mesuré + provenance + drill-down), conformité D-18 incluse.</done>
</task>

<task type="auto">
  <name>Task 2: Home cockpit page.tsx (anon-client, 4 sections ordonnées) + sidebar 4 axes</name>
  <files>apps/web/src/app/(admin)/page.tsx, apps/web/src/app/(admin)/_components/AdminSidebar.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/page.tsx (head-counts L.67-149, Card-in-Link L.166-205, feux L.55-59)
    - apps/web/src/app/(admin)/_components/AdminSidebar.tsx (NAV_ITEMS L.36-44, isActive L.46-49)
    - apps/web/src/lib/admin/kpis.ts (plan 20-03)
    - apps/web/src/lib/supabase/server.ts (createClient anon)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"(admin)/page.tsx" et §"AdminSidebar.tsx"
  </read_first>
  <action>`page.tsx` : remplacer `createAdminServiceClient()` (L.19/L.68) par `await createClient()` anon de `@/lib/supabase/server` (retirer l'import `admin-service`). Charger les KPI via `lib/admin/kpis.ts` (`getMrr`, `getChurn`, `getPlanMix`, `getAcquisitionFunnel`) + les agrégats Ops via les helpers existants, puis rendre les 4 `AxisSummary*` dans l'ordre Revenus → Ops → Acquisition → Conformité (D-07), passant les données en props. Conserver les feux tokenisés L.55-59. Espacement dense « salle de contrôle » (20-UI-SPEC : xl entre blocs d'axe, 2xl header→1er bloc).
  `AdminSidebar.tsx` : regrouper les `NAV_ITEMS` existants sous 4 en-têtes d'axe (Acquisition / Revenus / Ops / Conformité, D-06). URLs détail INCHANGÉES (`/admin/membres`, `/admin/file`, `/admin/sante`, `/admin/signaux`, `/admin/affiliation/*`) → aucune redirection (A5). Conserver `isActive` prefix-match, l'accent `--primary` sur l'item actif, et les hrefs littéraux `/admin/...` (PAS de Link i18n).</action>
  <verify>
    <automated>grep -q "AxisSummaryRevenus" "apps/web/src/app/(admin)/page.tsx" && grep -q "AxisSummaryConformite" "apps/web/src/app/(admin)/page.tsx" && ! grep -q "admin-service" "apps/web/src/app/(admin)/page.tsx" && grep -q "Acquisition" "apps/web/src/app/(admin)/_components/AdminSidebar.tsx" && grep -q "Conformité" "apps/web/src/app/(admin)/_components/AdminSidebar.tsx" && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` n'importe plus `createAdminServiceClient`/`admin-service` (assertion source) ; utilise `createClient` anon
    - `page.tsx` rend les 4 `AxisSummary*` dans l'ordre Revenus → Ops → Acquisition → Conformité
    - `AdminSidebar.tsx` regroupe les liens sous 4 en-têtes d'axe, hrefs `/admin/...` inchangés
    - typecheck vert
  </acceptance_criteria>
  <done>Home cockpit anon-client à 4 sections ordonnées + sidebar regroupée 4 axes, URLs préservées.</done>
</task>

<task type="auto">
  <name>Task 3: Étendre no-perf-seed-claims ADMIN_UI_FILES aux AxisSummary + vérifier l'extinction des scans</name>
  <files>apps/web/test/no-perf-seed-claims.test.ts</files>
  <read_first>
    - apps/web/test/no-perf-seed-claims.test.ts (ADMIN_UI_FILES + FORBIDDEN_PERF_UI ajoutés en 20-01)
    - apps/web/test/no-perf-claims.test.ts (namespace admin)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Test no-perf-claims étendu admin"
  </read_first>
  <action>Ajouter les 4 fichiers `_components/AxisSummary*.tsx` (et `(admin)/page.tsx` s'il n'y est pas déjà) au tableau `ADMIN_UI_FILES` de `no-perf-seed-claims.test.ts` afin que `FORBIDDEN_PERF_UI` scanne le cockpit livré. Lancer ensuite les scans `no-perf-claims` (namespace admin) et `no-perf-seed-claims` : ils doivent désormais PASSER au VERT (la copy admin honnête est en place : MRR « cash encaissé », churn via `applyThreshold`, aucun `%` littéral ni equity/ROI). Si rouge → corriger la copy fautive (remplacer tout `%` littéral par un rendu `applyThreshold`), jamais désactiver le scan.</action>
  <verify>
    <automated>pnpm --filter web test -- --run apps/web/test/no-perf-claims.test.ts apps/web/test/no-perf-seed-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ADMIN_UI_FILES` inclut les 4 `AxisSummary*.tsx` + `(admin)/page.tsx`
    - `no-perf-claims.test.ts` et `no-perf-seed-claims.test.ts` VERTS (extinction des scans 20-01 confirmée)
    - Aucun scan désactivé/skip
  </acceptance_criteria>
  <done>Scans no-perf étendus au cockpit livré et au vert ; honnêteté des chiffres prouvée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anon-client (superadmin) → RPC/RLS | La home lit agrégats + tables via gated/RLS |
| copy admin → utilisateur | Risque d'afficher un chiffre fabriqué / promesse |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Elevation of Privilege | service_role résiduel sur la home | mitigate | bascule `createClient()` anon ; `rls-unchanged` étendu (20-01) couvre page.tsx |
| T-20-04 | Repudiation | chiffre de perf fabriqué affiché | mitigate | KPI via wrappers mesurés + `applyThreshold` ; `no-perf-claims`/`no-perf-seed-claims` verts (Task 3) |
| T-20-12 | Information Disclosure | KPI lu hors gate | mitigate | lecture via wrappers gated (`lib/admin/kpis.ts`), 0 ligne non-superadmin |
| T-20-17 | Information Disclosure | conformité affichée à tort « OK » | mitigate | feu piloté par `isLegalReviewDone()` (env) ; red si état illisible (20-UI-SPEC) |
| T-20-SC | Tampering | installs npm | accept | Aucun install (shadcn vendored, D-14) |
</threat_model>

<verification>
- `pnpm --filter web typecheck` vert ; home rend 4 sections ordonnées sur anon-client
- `no-perf-claims.test.ts` + `no-perf-seed-claims.test.ts` VERTS (extinction)
- Assertion source : `page.tsx` n'importe pas `admin-service`
</verification>

<success_criteria>
- Cockpit home 4 sections (Revenus→Ops→Acquisition→Conformité) sur anon-client
- 4 cartes d'axe avec KPI mesuré + ligne de provenance + drill-down ; conformité D-18
- Sidebar regroupée 4 axes (URLs préservées) ; scans no-perf verts
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-05-SUMMARY.md` à la fin.
</output>
