---
phase: 20-dashboard-superadmin-cockpit-4-axes
plan: 04
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - apps/web/src/app/(admin)/membres/actions.ts
  - apps/web/src/app/(admin)/file/actions.ts
  - apps/web/src/app/(admin)/affiliation/actions.ts
  - apps/web/src/app/(admin)/affiliation/payouts/actions.ts
  - apps/web/src/components/admin/MemberRowActions.tsx
autonomous: true
requirements: [ADASH-04, ADASH-05, ADASH-07]

must_haves:
  truths:
    - "Chaque Server Action admin re-garde requireRole('superadmin') puis appelle un RPC gated sur anon-client (jamais service_role)"
    - "Offrir du temps gratuit et Suspendre/Réactiver passent par grant_subscription_time / suspend_account / unsuspend_account"
    - "Marquer une commission payée passe par admin_mark_commission_paid (anti double-payout DB), plus de service_role"
    - "Les erreurs sont opaques (jamais err.message brut) et chaque succès affiche un toast sonner"
  artifacts:
    - path: "apps/web/src/app/(admin)/membres/actions.ts"
      provides: "grantSubscriptionTime + suspendAccount + unsuspendAccount via RPC gated"
      contains: "grant_subscription_time"
    - path: "apps/web/src/components/admin/MemberRowActions.tsx"
      provides: "Dialog presets 7j/1mois/3mois + AlertDialog suspension (raison requise)"
      contains: "Confirmer la prolongation"
    - path: "apps/web/src/app/(admin)/affiliation/payouts/actions.ts"
      provides: "admin_mark_commission_paid (anon RPC, plus de markCommissionPaid service_role)"
      contains: "admin_mark_commission_paid"
  key_links:
    - from: "apps/web/src/components/admin/MemberRowActions.tsx"
      to: "membres/actions.ts"
      via: "grantSubscriptionTime / suspendAccount"
      pattern: "suspend|grant"
    - from: "actions admin"
      to: "RPC gated"
      via: "supabase.rpc"
      pattern: "rpc\\('(grant_subscription_time|suspend_account|unsuspend_account|admin_mark_commission_paid)'"
---

<objective>
Basculer toutes les écritures admin du pattern germe (helpers service_role : `activateForPayment`/`changePlan`/`markCommissionPaid`) vers les RPC `SECURITY DEFINER` gated de 0021, appelés sur anon-client après re-gate `requireRole('superadmin')`. Étendre `MemberRowActions` avec le dialog « Offrir du temps gratuit » (presets) et l'AlertDialog destructif « Suspendre » (raison requise) + « Réactiver ».

Purpose: Faire de chaque action sensible un geste tracé (audit atomique DB) sans aucun service_role côté page. Contribue à l'extinction de `rls-unchanged` (avec les pages, plan 20-06).
Output: 4 fichiers d'actions gated + le composant row-actions enrichi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md

<interfaces>
- `apps/web/src/app/(admin)/affiliation/payouts/actions.ts` : template d'action gated (re-gate `requireRole('superadmin')` L.28, `createClient` anon, `TX_HASH_PATTERN` L.26, `ATOMIC_PATTERN` L.23, `fail()` erreur opaque L.55-61). Appelle aujourd'hui `markCommissionPaid` service_role → à remplacer.
- `apps/web/src/app/(admin)/membres/actions.ts` : aujourd'hui `createAdminServiceClient` + `activateForPayment`/`changePlan` (L.16-18, L.66, L.86) + whitelist `PERIODS` (L.31-42).
- `apps/web/src/components/admin/MemberRowActions.tsx` : DropdownMenu + Dialog + AlertDialog + `useTransition` + sonner ; helpers `run()` (L.71-81), `buildForm` (L.83-88) ; pattern revoke AlertDialog L.120-139 ; pattern Dialog+Select L.142-193 / PERIODS L.61.
- RPC 0021 (plan 20-02) : `grant_subscription_time(p_user_id, p_interval)`, `suspend_account(p_user_id, p_reason)`, `unsuspend_account(p_user_id)`, `admin_mark_commission_paid(p_commission_id, p_tx_hash, p_amount_atomic)`.
- Copy verrouillée (20-UI-SPEC) : « Offrir du temps gratuit » → CTA « Confirmer la prolongation » ; « Suspendre ce compte ? » raison requise → CTA « Suspendre » (destructive) ; « Réactiver » ; « Marquer comme payé » → « Confirmer le paiement ».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions gated (membres/file/affiliation/payouts) → RPC 0021, plus de service_role</name>
  <files>apps/web/src/app/(admin)/membres/actions.ts, apps/web/src/app/(admin)/file/actions.ts, apps/web/src/app/(admin)/affiliation/actions.ts, apps/web/src/app/(admin)/affiliation/payouts/actions.ts</files>
  <read_first>
    - apps/web/src/app/(admin)/affiliation/payouts/actions.ts (template gated complet : re-gate, fail(), patterns validation)
    - apps/web/src/app/(admin)/membres/actions.ts (PERIODS L.31-42, helpers service_role à retirer)
    - apps/web/src/app/(admin)/file/actions.ts, apps/web/src/app/(admin)/affiliation/actions.ts
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-PATTERNS.md §"membres/actions.ts" et §"payouts/actions.ts"
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-RESEARCH.md §"Bascule anon+RLS" (liste service_role), §"Security Domain"
  </read_first>
  <action>Dans chaque fichier d'actions : conserver `'use server'` + `import 'server-only'` ; en TÊTE de chaque action `await requireRole('superadmin')` (re-gate POST) ; remplacer `createAdminServiceClient()` par `createClient()` anon de `@/lib/supabase/server` (retirer l'import `admin-service`).
  `membres/actions.ts` : exporter `grantSubscriptionTime(formData)` validant `p_interval` contre la whitelist `PERIODS` (`{'7 days','1 month','3 months'}` + custom borné) puis `supabase.rpc('grant_subscription_time', { p_user_id, p_interval })` ; `suspendAccount(formData)` (raison requise non vide) → `.rpc('suspend_account', { p_user_id, p_reason })` ; `unsuspendAccount(formData)` → `.rpc('unsuspend_account', { p_user_id })`. Supprimer les appels `activateForPayment`/`changePlan` (helpers repo service_role).
  `file/actions.ts` + `affiliation/actions.ts` : remplacer toute mutation service_role par l'appel `.rpc()` gated équivalent (ou retirer l'action si elle ne fait plus partie du modèle) ; conserver le re-gate.
  `affiliation/payouts/actions.ts` : remplacer `markCommissionPaid(client, …)` (service_role) par `supabase.rpc('admin_mark_commission_paid', { p_commission_id, p_tx_hash, p_amount_atomic })` sur anon-client ; conserver `TX_HASH_PATTERN`/`ATOMIC_PATTERN` et `fail()` (erreur opaque) inchangés ; `revalidatePath`.
  Partout : mapper les erreurs vers des clés opaques via `fail()`, jamais `throw err.message` brut.</action>
  <verify>
    <automated>grep -RL "createAdminServiceClient" "apps/web/src/app/(admin)/membres/actions.ts" "apps/web/src/app/(admin)/file/actions.ts" "apps/web/src/app/(admin)/affiliation/actions.ts" "apps/web/src/app/(admin)/affiliation/payouts/actions.ts" | wc -l | grep -q 4 && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Aucun des 4 fichiers d'actions n'importe `createAdminServiceClient`/`admin-service` (assertion source : grep -L renvoie les 4)
    - `membres/actions.ts` appelle `rpc('grant_subscription_time')`, `rpc('suspend_account')`, `rpc('unsuspend_account')`
    - `payouts/actions.ts` appelle `rpc('admin_mark_commission_paid')` ; `TX_HASH_PATTERN`/`ATOMIC_PATTERN`/`fail()` conservés
    - Chaque action débute par `requireRole('superadmin')` ; typecheck vert
  </acceptance_criteria>
  <done>4 fichiers d'actions sur RPC gated + anon-client, re-gate + erreurs opaques préservés, zéro service_role.</done>
</task>

<task type="auto">
  <name>Task 2: MemberRowActions — dialog « Offrir du temps gratuit » + AlertDialog « Suspendre/Réactiver »</name>
  <files>apps/web/src/components/admin/MemberRowActions.tsx</files>
  <read_first>
    - apps/web/src/components/admin/MemberRowActions.tsx (DropdownMenu + Dialog + AlertDialog + useTransition + sonner ; run() L.71-81, buildForm L.83-88, AlertDialog revoke L.120-139, Dialog+Select L.142-193, PERIODS L.61)
    - apps/web/src/app/(admin)/membres/actions.ts (signatures grantSubscriptionTime/suspendAccount/unsuspendAccount — Task 1)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UI-SPEC.md §"Copywriting Contract" + §"Destructive / sensitive actions"
  </read_first>
  <action>Étendre `MemberRowActions.tsx` (réutiliser `run()` transition+toast et `buildForm`). Ajouter une entrée « Offrir du temps gratuit » ouvrant un `Dialog` avec un `Select` de presets `7 j / 1 mois / 3 mois / custom` (intervalles whitelistés, calque PERIODS), affichant la nouvelle date d'expiration résultante avant confirmation, CTA accent « Confirmer la prolongation » → appelle l'action `grantSubscriptionTime`. Ajouter une entrée destructive « Suspendre » ouvrant un `AlertDialog` variant destructive (titre « Suspendre ce compte ? », body sur le blocage RLS réel réversible, champ `reason` REQUIS, CTA rouge « Suspendre ») → `suspendAccount` ; et « Réactiver » (confirm léger, CTA « Réactiver ») → `unsuspendAccount`, affiché selon l'état `suspended` de la ligne. Toast sonner au succès (Toaster déjà monté au layout), bouton re-désactivé pendant `pending`. Couleurs : accent `--primary` réservé au CTA de prolongation, `--destructive` pour suspendre (D-08, 20-UI-SPEC) ; aucun `%` ni chiffre fabriqué dans la copy.</action>
  <verify>
    <automated>grep -q "Confirmer la prolongation" "apps/web/src/components/admin/MemberRowActions.tsx" && grep -q "Suspendre ce compte" "apps/web/src/components/admin/MemberRowActions.tsx" && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `MemberRowActions.tsx` rend un Dialog presets (« Confirmer la prolongation ») câblé à `grantSubscriptionTime`
    - Rend un AlertDialog destructif « Suspendre ce compte ? » avec champ raison requis câblé à `suspendAccount`, et « Réactiver » câblé à `unsuspendAccount`
    - Toast sonner au succès ; bouton désactivé pendant `pending` ; aucun `%` littéral dans la copy
    - typecheck vert
  </acceptance_criteria>
  <done>Row-actions enrichies (offrir/suspendre/réactiver) câblées aux RPC gated, copy FR verrouillée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| POST Server Action → DB | Endpoint d'écriture ; re-gate obligatoire (le layout ne protège pas les POST) |
| formData (interval/reason/tx_hash) → RPC | Entrées utilisateur non fiables |
| message d'erreur DB → UI | Risque de fuite d'interne |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Elevation of Privilege | Server Action POST sans re-gate | mitigate | `requireRole('superadmin')` en tête CHAQUE action + garde `is_superadmin()` DANS le RPC (double) |
| T-20-03 | Elevation of Privilege | service_role résiduel dans une action | mitigate | bascule anon `createClient()` ; `rls-unchanged` étendu scanne les actions |
| T-20-10 | Tampering | `p_interval` injecté | mitigate | whitelist `PERIODS` avant l'appel RPC (V5) |
| T-20-09 | Tampering | double-payout | mitigate | `admin_mark_commission_paid` DB-side `where status='due'` + raise (calque 0016) |
| T-20-16 | Information Disclosure | fuite `err.message` brut | mitigate | `fail()` mappe vers clés opaques ; jamais de message DB exposé |
| T-20-SC | Tampering | installs npm | accept | Aucun install (composants shadcn déjà vendored, D-14) |
</threat_model>

<verification>
- `grep -L createAdminServiceClient` renvoie les 4 fichiers d'actions (aucun service_role)
- `MemberRowActions` rend les dialogs « Confirmer la prolongation » + « Suspendre ce compte ? »
- `pnpm --filter web typecheck` vert ; suite existante non régressée
</verification>

<success_criteria>
- 4 fichiers d'actions sur RPC gated + anon-client (offrir/suspendre/réactiver/payout)
- Row-actions enrichies avec dialogs FR verrouillés et toasts
- Toutes les écritures admin tracées (audit DB) sans service_role
</success_criteria>

<output>
Créer `.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-04-SUMMARY.md` à la fin.
</output>
