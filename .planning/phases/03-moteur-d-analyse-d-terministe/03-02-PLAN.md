---
phase: 03-moteur-d-analyse-d-terministe
plan: 02
type: tdd
wave: 2
depends_on: []
files_modified:
  - packages/indicators/package.json
  - packages/indicators/tsconfig.json
  - packages/indicators/src/index.ts
  - packages/indicators/src/ohlcv.ts
  - packages/indicators/src/wrappers/rsi.ts
  - packages/indicators/src/wrappers/macd.ts
  - packages/indicators/src/wrappers/ema.ts
  - packages/indicators/src/wrappers/atr.ts
  - packages/indicators/src/wrappers/bollinger.ts
  - packages/indicators/src/wrappers/wrappers.test.ts
  - packages/indicators/src/structure/swings.ts
  - packages/indicators/src/structure/structure.ts
  - packages/indicators/src/structure/levels.ts
  - packages/indicators/src/structure/volume.ts
  - packages/indicators/src/structure/structure.test.ts
  - packages/indicators/src/snapshots/schema.ts
  - packages/indicators/src/snapshots/hash.ts
  - packages/indicators/src/snapshots/snapshots.test.ts
  - packages/indicators/src/__fixtures__/btcusdt-h4.json
  - vitest.config.ts
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03]
user_setup: []

must_haves:
  truths:
    - "RSI/MACD/EMA/ATR/Bollinger produisent des valeurs déterministes pinées par golden tests (valeur ET longueur de sortie)"
    - "Les swings (pivot fractal + filtre ATR, D-32) sont détectés sur cas connus vérifiés à la main"
    - "BOS/CHoCH sont confirmés UNIQUEMENT sur clôture du corps (D-33), pas sur mèche"
    - "Les niveaux S/R sont des clusters de swings avec force multi-facteurs ; chaque POC porte un flag volume_source real|proxy (D-35)"
    - "Le hash de contenu est déterministe (JSON canonique + précision fixe) — mêmes bougies → même hash"
  artifacts:
    - path: "packages/indicators/src/wrappers/rsi.ts"
      provides: "Wrapper RSI alignant la valeur sur la dernière bougie clôturée"
      min_lines: 10
    - path: "packages/indicators/src/structure/swings.ts"
      provides: "Pivot fractal + filtre ATR, N et k = constantes nommées"
      min_lines: 20
    - path: "packages/indicators/src/structure/volume.ts"
      provides: "POC proportional-overlap + flag source real|proxy"
      contains: "volume_source"
    - path: "packages/indicators/src/snapshots/schema.ts"
      provides: "Zod §3 TechnicalSnapshotSchema/FundamentalContextSchema/NewsContextSchema"
      exports: ["TechnicalSnapshotSchema", "FundamentalContextSchema", "NewsContextSchema"]
    - path: "packages/indicators/src/snapshots/hash.ts"
      provides: "snapshotContentHash sha256 sur JSON canonique"
      exports: ["snapshotContentHash"]
  key_links:
    - from: "wrappers/rsi.ts"
      to: "technicalindicators"
      via: "RSI.calculate({period,values})"
      pattern: "RSI\\.calculate"
    - from: "structure/structure.ts"
      to: "@app/core lastClosedCandleStart"
      via: "convention bougie clôturée (jamais redéfinie)"
      pattern: "@app/core"
    - from: "snapshots/schema.ts"
      to: "ARCHITECTURE §3 shape"
      via: "Zod enums sur les unions littérales LOCKED"
      pattern: "z\\.enum"
---

<objective>
Construire le cœur de calcul déterministe `packages/indicators` : wrappers d'indicateurs classiques (TECH-01), module de structure de marché MAISON (swings, BOS/CHoCH, S/R, POC volume — TECH-02/03, absent des libs, différenciateur "vétéran"), schéma Zod des formes §3 LOCKED, et hash de contenu déterministe (D-41). Tout est golden-testé offline : un même jeu de bougies produit toujours les mêmes valeurs ET le même hash.

Purpose: C'est la fondation calculatoire consommée par les trois moteurs (plans 03/04). Le code calcule TOUS les chiffres ; Claude (Phase 4) ne fait que raisonner — aucun nombre laissé à l'IA.
Output: package @app/indicators golden-testé, schéma §3 Zod, hash déterministe.

TDD obligatoire : pour chaque fichier de calcul, écrire le golden test (RED) avant l'implémentation. Le piège warmup-offset (sortie technicalindicators PLUS COURTE que l'entrée) doit être épinglé par un test sur la longueur de sortie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-RESEARCH.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md

<interfaces>
<!-- technicalindicators API (RESEARCH Pattern 1). Outputs SHORTER than inputs (warmup offset). -->
RSI.calculate({ period:14, values: closes }) → number[]   (length = closes.length - period)
MACD.calculate({ values, fastPeriod:12, slowPeriod:26, signalPeriod:9, SimpleMAOscillator:false, SimpleMASignal:false }) → { MACD, signal, histogram }[]
EMA.calculate({ period, values }) → number[]
ATR.calculate({ period:14, high, low, close }) → number[]
BollingerBands.calculate({ period:20, stdDev:2, values: closes }) → { middle, upper, lower, pb }[]

<!-- LOCKED §3 shapes — encode field-by-field. From ARCHITECTURE.md §3 + 03-PATTERNS.md lines 164-189 -->
technical_snapshot: trend_htf/trend_ltf enum(bullish|bearish|range); momentum{rsi,macd_hist,slope};
  volatility{atr,atr_percentile}; key_levels[{price,type:support|resistance|poc,strength,volume_source?:real|proxy}];
  structure{last_swing_high,last_swing_low,bos_choch:enum(bos|choch)|null}; volume_state enum(expanding|contracting)
fundamental_context: macro_bias risk_on|risk_off|neutral; rate_environment hawkish|dovish|neutral; dxy_trend; real_yields; asset_specific_drivers[]
news_context: net_sentiment -1..+1; recent_catalysts[]; upcoming_events[] (+ news_risk flag, D-40)

<!-- @app/core (consume, never redefine — D-10) -->
lastClosedCandleStart, TIMEFRAMES, UTC_ZONE, DAILY_ANCHOR from @app/core
</interfaces>
</context>

<feature>
  <name>packages/indicators — wrappers + structure maison + schéma §3 + hash déterministe</name>
  <files>voir files_modified</files>
  <behavior>
    - Wrappers : pour une fixture OHLCV fixe, RSI14/MACD/EMA20-50-200/ATR14/BB20-2 retournent une valeur épinglée (toBeCloseTo) ET une longueur de sortie épinglée (toHaveLength = input.length - period). La valeur exposée est alignée sur la DERNIÈRE bougie clôturée (result.at(-1)), jamais index-alignée sur le tableau de bougies.
    - Swings : sur un cas connu, detectSwings retourne les indices de swing high/low attendus ; un pivot non confirmé par k×ATR est rejeté (D-32). N et k = constantes nommées (candidats RESEARCH : N∈{2,3,5}, k∈{0.5,1,1.5}, valeur finale épinglée par le test).
    - BOS/CHoCH : une mèche qui perce le swing mais un corps qui clôture en-deçà → AUCUN signal ; une clôture de corps au-delà → BOS (même sens) ou CHoCH (sens opposé). (D-33)
    - S/R : clustering des swings dans une tolérance c×ATR → zones ; strength = w(touches, âge, récence du dernier test). (D-34)
    - POC : profil volume proportional-overlap sur OHLCV → bin de plus fort volume ; chaque POC porte volume_source 'real' (crypto Binance) ou 'proxy' (tick OANDA). (D-35)
    - Hash : snapshotContentHash(payload) déterministe — clés triées + précision décimale fixe ; même payload → même hash sur deux appels. (D-41)
    - Zod : TechnicalSnapshotSchema.parse(payload valide) passe ; un payload avec un enum hors §3 est rejeté.
  </behavior>
  <implementation>
    Étape 1 — Scaffold package (RED prep) : créer packages/indicators/package.json (analog packages/core/package.json : name @app/indicators, type:module, exports {".":"./src/index.ts","./*":"./src/*.ts"}, deps technicalindicators 3.1.0 + luxon 3.7.2 + zod 4.4.3, workspace deps @app/core et @app/supabase si cross-import CandleRow) ; tsconfig.json (analog packages/data-sources/tsconfig.json : module Preserve, moduleResolution Bundler, noEmit true) ; ajouter l'alias `'@app/indicators': path.resolve(__dirname, 'packages/indicators/src/index.ts')` dans vitest.config.ts (bloc alias lignes 23-25). Installer via `pnpm --filter @app/indicators add technicalindicators@3.1.0`. Créer src/__fixtures__/btcusdt-h4.json (petit slice OHLCV dérivé des vraies bougies BTC en base — assez long pour EMA200, soit ≥210 bougies).
    Étape 2 — Wrappers (TDD RED→GREEN par indicateur) : src/ohlcv.ts mappe CandleRow[] (ts ascendant) → arrays {closes,highs,lows,vols} (vols = r.volume ?? 0). Chaque wrapper (rsi/macd/ema/atr/bollinger.ts) = fonction pure thin, type de retour explicite, ne jamais exposer le type de la lib, retourne la valeur at(-1) alignée. Header doc-comment avec TECH-01 + citation npmjs. Écrire wrappers.test.ts AVANT (golden : valeur + longueur).
    Étape 3 — Structure maison (TDD RED→GREEN, RESEARCH Patterns 2-5) : swings.ts (pivot fractal fenêtre 2N+1 + filtre k×ATR, N/k constantes nommées) ; structure.ts (classification HH/HL + BOS/CHoCH sur CLÔTURE DU CORPS, consomme @app/core) ; levels.ts (clustering c×ATR + strength multi-facteurs) ; volume.ts (POC proportional-overlap + flag volume_source). structure.test.ts AVANT avec fixtures vérifiées à la main (indices de swing exacts, flags de break exacts).
    Étape 4 — Schéma + hash (TDD) : src/snapshots/schema.ts encode les 3 formes §3 LOCKED champ par champ en Zod v4 (z.enum sur les unions littérales, volume_source z.enum(['real','proxy']).optional() sur key_levels poc), types via z.infer. src/snapshots/hash.ts : snapshotContentHash via node:crypto createHash('sha256') sur canonicalJson (clés triées + arrondi décimal fixe — jamais de float brut, RESEARCH Pitfall 3). snapshots.test.ts golden : parse OK/KO + hash stable sur deux appels.
    Étape 5 — Barrel src/index.ts : ré-exporter wrappers + structure (detectSwings, detectBosChoch, clusterLevels, computePoc) + snapshotContentHash + les 3 schémas, avec extension .js sur chaque re-export relatif (ESM + moduleResolution Bundler), groupés par module avec commentaire d'en-tête.
  </implementation>
</feature>

<verification>
- `pnpm vitest run packages/indicators` vert (wrappers + structure + snapshots, offline, < 30s).
- Tests épinglent VALEUR et LONGUEUR pour chaque indicateur (anti warmup-offset).
- Test BOS/CHoCH prouve qu'une mèche ne confirme pas (D-33).
- Test POC vérifie le flag volume_source real ET proxy (D-35).
- Test hash prouve la stabilité sur deux appels (D-41).
- `pnpm tsc` clean.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB candles (trusted) → indicators (pure fns) | Données déjà validées en Phase 2 ; revalidation de forme à la frontière snapshot (Zod §3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | hash non déterministe (float brut / clés non triées) | mitigate | canonicalJson : clés triées + précision décimale fixe avant sha256 ; golden-test sur fixture fixe (RESEARCH Pitfall 3) |
| T-03-06 | Integrity | POC tick-volume FX présenté comme vrai volume | mitigate | flag volume_source 'proxy' obligatoire sur chaque POC OANDA (D-35), testé |
| T-03-07 | Cryptography | hand-roll de hash | mitigate | node:crypto sha256 builtin, jamais de hash maison (RESEARCH Don't Hand-Roll) |
| T-03-08 | Input Validation | dérive de forme du snapshot avant persist | mitigate | Zod §3 schema parse avant tout upsert (V5 ASVS) — schéma fourni ici, appliqué plans 03/04 |
| T-03-SC | Tampering | install technicalindicators@3.1.0 | mitigate | LOCKED par CLAUDE.md, audité dans 03-RESEARCH.md Package Legitimacy (Approved) — pas de checkpoint requis |
</threat_model>

<success_criteria>
- Wrappers TECH-01 golden-testés (valeur + longueur), alignés sur bougie clôturée.
- Structure maison TECH-02/03 golden-testée sur cas connus (swings, BOS/CHoCH corps, S/R, POC + flag source).
- Schéma Zod §3 + hash déterministe exportés et testés.
- `pnpm vitest run packages/indicators` vert.
</success_criteria>

<output>
Create `.planning/phases/03-moteur-d-analyse-d-terministe/03-02-SUMMARY.md` when done
</output>
