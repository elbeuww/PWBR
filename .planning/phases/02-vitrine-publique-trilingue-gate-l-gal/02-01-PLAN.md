---
phase: 02-vitrine-publique-trilingue-gate-l-gal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/components.json
  - apps/web/src/styles/globals.css
  - apps/web/src/fonts/
  - apps/web/src/lib/fonts.ts
  - apps/web/src/components/ThemeProvider.tsx
  - apps/web/src/components/ThemeToggle.tsx
  - apps/web/src/components/ui/
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: false
requirements: [VITR-01]
user_setup: []

must_haves:
  truths:
    - "L'utilisateur bascule dark/light via un toggle dans le header, sans flash au chargement."
    - "L'arabe s'affiche en IBM Plex Sans Arabic self-hostée (zéro requête CDN au runtime)."
    - "Les composants shadcn/ui (button, card, badge…) sont disponibles et stylés aux tokens de marque bleus."
    - "Le layout locale conserve un seul <html lang dir> et n'a pas régressé hasLocale/notFound."
  artifacts:
    - path: "apps/web/src/lib/fonts.ts"
      provides: "next/font/local IBM Plex Sans Arabic + Inter, exposés en variables CSS"
      contains: "ibm-plex-arabic"
    - path: "apps/web/src/components/ThemeProvider.tsx"
      provides: "wrapper next-themes class strategy"
      contains: "next-themes"
    - path: "apps/web/src/components/ThemeToggle.tsx"
      provides: "bouton bascule de thème accessible RTL"
      contains: "useTheme"
    - path: "apps/web/src/styles/globals.css"
      provides: "tokens de marque 2 thèmes + @custom-variant dark + :lang(ar)"
      contains: "@custom-variant dark"
    - path: "apps/web/src/components/ui/button.tsx"
      provides: "primitive shadcn button"
      contains: "export"
  key_links:
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/components/ThemeProvider.tsx"
      via: "wrapper autour de NextIntlClientProvider"
      pattern: "ThemeProvider"
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/lib/fonts.ts"
      via: "className body variables de police"
      pattern: "\\.variable"
    - from: "apps/web/src/styles/globals.css"
      to: "apps/web/src/lib/fonts.ts"
      via: "--font-arabic = var exposée par next/font"
      pattern: "--font-arabic"
---

<objective>
Initialiser le design system de marque (reporté de P1) AVANT toute page marketing : installer les dépendances de theming/icônes/polices, initialiser shadcn/ui contre Tailwind v4 CSS-first, poser les tokens de marque (bleu, 2 thèmes, D-01/D-02/D-04), self-host les polices (D-03), et greffer le shell (ThemeProvider + ThemeToggle + slot footer) dans `[locale]/layout.tsx`.

Purpose: Toutes les pages des plans suivants (home, tarifs, légales) consomment ces tokens, ces composants `ui/`, ces polices et ce shell. Sans cette fondation, chaque page réinventerait le style et casserait le RTL/no-flash.
Output: package.json mis à jour, components.json, globals.css (tokens), lib/fonts.ts, ThemeProvider/ThemeToggle, components/ui/*, layout shell, namespace `theme`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-CONTEXT.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-UI-SPEC.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-PATTERNS.md

<interfaces>
<!-- Contrats que les plans aval (02, 03) consommeront. Extraits du socle P1. -->

État actuel `apps/web/src/styles/globals.css` (à transformer) :
- `@import "tailwindcss";`
- `@theme { --font-arabic: "Noto Sans Arabic", system-ui, sans-serif; }`  ← à remplacer par `var(--font-ibm-plex-arabic)`
- `:lang(ar) { font-family: var(--font-arabic); }`  ← à CONSERVER

Tokens de marque cibles (UI-SPEC §Color — light / dark) :
- `--color-background`: #FFFFFF / #0B1220
- `--color-surface`: #F4F6F9 / #141C2B
- `--color-accent`: #1E5FBF / #3B82F6
- `--color-foreground`: #0B1220 / #E6EBF2
- `--color-muted`: #5B6675 / #9AA7B8
- `--color-border`: #E2E7EE / #26303F
- `--font-latin`: var(--font-inter) ; `--font-arabic`: var(--font-ibm-plex-arabic)

Header existant `[locale]/layout.tsx` lignes 41-46 : `<header className="flex h-14 items-center justify-between bg-secondary px-4 md:px-6">` avec `<span>Vétéran Trading</span> {/* i18n-ignore: marque */}` et `<div className="ms-auto"><LanguageSwitcher /></div>`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vérifier la légitimité des paquets avant install (AR-01-SC)</name>
  <what-built>Aucun code encore. Cette tâche est le gate supply-chain obligatoire (slopcheck indisponible, politique projet AR-01-SC) AVANT tout `pnpm add`/`npx shadcn`.</what-built>
  <read_first>
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md (§Package Legitimacy Audit — table des 4 paquets)
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-SECURITY.md (AR-01-SC)
  </read_first>
  <how-to-verify>
    Vérifier sur les registres officiels que chaque paquet et version existe et provient du repo officiel attendu :
    1. https://www.npmjs.com/package/next-themes → version 0.4.6, repo github.com/pacocoursey/next-themes, aucun postinstall.
    2. https://www.npmjs.com/package/lucide-react → version 1.18.0, repo github.com/lucide-icons/lucide. ⚠️ v1 major (training connaît 0.x).
    3. https://www.npmjs.com/package/@fontsource/ibm-plex-sans-arabic → version 5.2.9, famille fontsource officielle (github.com/fontsource/font-files).
    4. https://www.npmjs.com/package/@fontsource-variable/inter → famille fontsource (si retenu plutôt que next/font/google).
    5. shadcn CLI : `npx shadcn@latest` provient du registre officiel shadcn — composants copiés depuis le registre officiel uniquement (UI-SPEC §Registry Safety).
  </how-to-verify>
  <acceptance_criteria>
    - Les 4 paquets confirmés existants aux versions exactes sur npmjs.com, repos officiels, sans script postinstall.
    - L'utilisateur tape "approved" pour autoriser les installs (Task 2). Tout doute → STOP.
  </acceptance_criteria>
  <resume-signal>Tapez "approved" pour autoriser les installs, ou décrivez les anomalies.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Installer les deps + init shadcn (commit séparé) + self-host polices</name>
  <files>apps/web/package.json, apps/web/components.json, apps/web/src/components/ui/, apps/web/src/fonts/, apps/web/src/lib/fonts.ts</files>
  <read_first>
    - apps/web/package.json (versions épinglées exactes existantes — ne pas introduire de range)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md (§Installation, §Pattern 2, Pitfall D)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-PATTERNS.md (§lib/fonts.ts)
    - apps/web/src/styles/globals.css (état actuel — vérifier survie de :lang(ar) après shadcn init)
  </read_first>
  <action>
    Installer avec versions EXACTES épinglées (pas de `^`) via pnpm filter web : `next-themes@0.4.6`, `lucide-react@1.18.0`, `@fontsource/ibm-plex-sans-arabic@5.2.9`. Pour Inter latin : utiliser `next/font/google` (self-host au build, conforme à l'esprit D-03 — décision A5 du RESEARCH) ; ne PAS ajouter `@fontsource-variable/inter` sauf si le build Google échoue.
    Lancer `npx shadcn@latest init` dans apps/web contre Tailwind v4 CSS-first (génère `components.json`, installe @radix-ui/*, class-variance-authority, clsx, tailwind-merge, lucide-react). PITFALL D : commit l'init SÉPARÉMENT, puis vérifier que `:lang(ar)` et le bloc `--font-arabic` de globals.css survivent (Task 3 les surchargera). Si shadcn écrase globals.css, restaurer `:lang(ar)`.
    Ajouter les blocks : `npx shadcn@latest add button card badge dialog form input label dropdown-menu separator` → crée `components/ui/*`.
    Créer `lib/fonts.ts` : `Inter` via `next/font/google` (subsets latin, variable `--font-inter`, display swap) + `ibmPlexArabic` via `next/font/local` pointant sur les `.woff2` copiés dans `apps/web/src/fonts/` depuis `node_modules/@fontsource/ibm-plex-sans-arabic/files/` (weights 400 + 600), variable `--font-ibm-plex-arabic`, display swap. Exporter `inter` et `ibmPlexArabic`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc -b --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"next-themes": "0.4.6"' apps/web/package.json` retourne 1 (version exacte, pas de `^`).
    - `apps/web/components.json` existe.
    - Au moins 4 fichiers dans `apps/web/src/components/ui/` (button.tsx, card.tsx, badge.tsx, …).
    - `apps/web/src/fonts/` contient ≥ 2 fichiers `.woff2` (IBM Plex Sans Arabic 400 + 600).
    - `lib/fonts.ts` exporte `inter` et `ibmPlexArabic` ; `grep -c "next/font/local" apps/web/src/lib/fonts.ts` ≥ 1.
    - `grep -c ":lang(ar)" apps/web/src/styles/globals.css` retourne 1 (P1 non régressé après shadcn init).
    - `tsc -b --noEmit` : aucune NOUVELLE erreur au-delà des baselines P1 documentées (fixture ESLint, types Supabase déférés).
  </acceptance_criteria>
  <done>Deps installées version exacte, shadcn initialisé (commit séparé), ui/* présents, polices self-hostées dans le repo, fonts.ts exporte les 2 polices, :lang(ar) intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tokens de marque 2 thèmes (globals.css) + shell layout (ThemeProvider/ThemeToggle/header/footer-slot)</name>
  <files>apps/web/src/styles/globals.css, apps/web/src/components/ThemeProvider.tsx, apps/web/src/components/ThemeToggle.tsx, apps/web/src/app/[locale]/layout.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/app/[locale]/layout.tsx (shell à étendre chirurgicalement — lignes 38-50)
    - apps/web/src/styles/globals.css (transformer : ajouter @custom-variant + tokens + .dark, remplacer --font-arabic)
    - apps/web/src/components/LanguageSwitcher.tsx (analog a11y/RTL pour ThemeToggle : min-h-11 min-w-11, aria-label, focus-visible:ring-2)
    - apps/web/src/messages/fr.json (structure namespace plat — ajouter `theme`)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md (§Pattern 1, Pitfall C)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-UI-SPEC.md (§Color, §Theme mechanics)
  </read_first>
  <behavior>
    - Le toggle bascule l'attribut `class` `.dark` sur `<html>` et persiste (next-themes).
    - Aucun warning d'hydratation au reload en mode dark (suppressHydrationWarning présent).
    - Le namespace `theme` est à parité de clés stricte fr/en/ar.
  </behavior>
  <action>
    globals.css : ajouter `@custom-variant dark (&:where(.dark, .dark *));` ; étendre `@theme` avec les 6 tokens de marque LIGHT (UI-SPEC §Color) + `--font-latin: var(--font-inter)` + `--font-arabic: var(--font-ibm-plex-arabic)` (REMPLACE l'ancien `"Noto Sans Arabic"`) ; ajouter un bloc `.dark { … }` avec les 6 valeurs dark ; CONSERVER `:lang(ar) { font-family: var(--font-arabic); }`. NE PAS introduire de tailwind.config (CSS-first).
    ThemeProvider.tsx : `'use client'`, ré-exporte `next-themes` ThemeProvider (cf. PATTERNS §ThemeProvider).
    ThemeToggle.tsx : `'use client'`, `useTheme()` de next-themes, bouton `min-h-11 min-w-11` + `aria-label={t(...)}` (namespace `theme`) + `focus-visible:ring-2`, icônes `Sun`/`Moon` named imports de lucide-react (vérifier noms v1.x), onClick bascule dark/light. RTL-safe (pas de classes physiques).
    layout.tsx (greffe chirurgicale, ne pas régresser hasLocale/notFound/un-seul-html) : ajouter `suppressHydrationWarning` sur `<html>` ; `<body className={`${inter.variable} ${ibmPlexArabic.variable}`}>` (import depuis lib/fonts) ; envelopper avec `<ThemeProvider attribute="class" defaultTheme="light" enableSystem>` à l'intérieur de body, autour de NextIntlClientProvider ; ajouter `<ThemeToggle />` dans le header à côté de `<LanguageSwitcher />` ; après `{children}`, laisser un emplacement pour `<Footer />` (le Footer réel arrive au Plan 02 — pour CE plan, ne PAS importer Footer, juste documenter le slot par un commentaire pour éviter un import cassé). Garder `{/* i18n-ignore: marque */}` sur le nom de marque.
    messages : ajouter namespace `theme` (`{ toggleLabel, light, dark }`) dans fr/en/ar à parité.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm exec tsc -b --noEmit 2>&1 | tail -3 && pnpm lint:i18n 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@custom-variant dark" apps/web/src/styles/globals.css` retourne 1.
    - `grep -c "Noto Sans Arabic" apps/web/src/styles/globals.css` retourne 0 (placeholder remplacé).
    - `grep -c "var(--font-ibm-plex-arabic)" apps/web/src/styles/globals.css` ≥ 1.
    - `grep -c ":lang(ar)" apps/web/src/styles/globals.css` retourne 1.
    - `grep -c "suppressHydrationWarning" apps/web/src/app/[locale]/layout.tsx` retourne 1.
    - `grep -c "ThemeProvider" apps/web/src/app/[locale]/layout.tsx` ≥ 1 ; `grep -c "ThemeToggle" apps/web/src/app/[locale]/layout.tsx` ≥ 1.
    - Aucune classe physique dans les nouveaux composants : `grep -nE "\b(ml-|mr-|pl-|pr-|left-|right-|text-left|text-right)" apps/web/src/components/ThemeToggle.tsx` ne retourne RIEN.
    - `theme` présent et à parité dans fr.json/en.json/ar.json (mêmes clés).
    - `tsc -b --noEmit` vert (hors baselines P1) ; `pnpm lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Tokens 2 thèmes posés en CSS-first, police arabe self-hostée câblée, toggle dark/light fonctionnel et accessible RTL, layout shell étendu sans régression P1, namespace theme à parité.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Installation next-themes/lucide-react/fontsource + deps shadcn (Radix/cva/clsx/tailwind-merge). |
| Préférence thème (client) → rendu | next-themes pose `.dark` avant paint ; risque de FOUC + hydration mismatch si mal câblé. |
| URL segment `[locale]` → résolution serveur | Layout modifié — ne pas régresser hasLocale/notFound (T-01-05 P1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/shadcn installs | mitigate | Checkpoint human-verify (Task 1) AVANT install (AR-01-SC, slopcheck absent) ; versions exactes épinglées ; repos officiels vérifiés. |
| T-02-01 | Tampering | shadcn init écrase globals.css | mitigate | Commit init séparé ; vérifier survie `:lang(ar)` (acceptance Task 2) ; tokens marque posés PAR-DESSUS (Task 3). |
| T-02-02 | Information Disclosure | hydration mismatch / FOUC thème | mitigate | `suppressHydrationWarning` sur `<html>` + ThemeProvider class strategy (script pré-paint next-themes). |
| T-02-03 | Input Validation | régression résolution locale | accept | Greffe chirurgicale au layout ; `hasLocale`/`notFound` inchangés (vérifié par tsc + revue) ; couvert par E2E i18n P1. |
</threat_model>

<verification>
- `tsc -b --noEmit` vert (hors baselines P1 documentées).
- `pnpm lint:i18n` exit 0 (aucune chaîne en dur, marque exclue).
- `:lang(ar)` survit dans globals.css ; `--font-arabic` pointe sur la police self-hostée.
- Aucune classe physique RTL-cassante dans les nouveaux composants.
</verification>

<success_criteria>
- Design system de marque initialisé (tokens 2 thèmes, CSS-first, zéro tailwind.config).
- Toggle dark/light opérationnel, sans flash, accessible et RTL-safe.
- Polices Inter + IBM Plex Sans Arabic self-hostées (zéro CDN runtime, D-03).
- Composants shadcn/ui disponibles pour les plans aval.
- Shell layout prêt à accueillir le Footer (Plan 02) et les pages (Plan 03).
</success_criteria>

<output>
Create `.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-01-SUMMARY.md` when done
</output>
