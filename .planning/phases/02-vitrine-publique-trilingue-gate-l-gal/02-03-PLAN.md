---
phase: 02-vitrine-publique-trilingue-gate-l-gal
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - apps/web/src/app/[locale]/(marketing)/page.tsx
  - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx
  - apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
  - apps/web/test/no-perf-claims.test.ts
autonomous: true
requirements: [VITR-01, VITR-02, VITR-03]
user_setup: []

must_haves:
  truths:
    - "Un visiteur voit une page d'accueil dans sa langue : hero bénéfice-first + CTA vers les tarifs + section « comment ça marche » + aperçu tarifs, SANS aucun chiffre de performance (D-08)."
    - "Le visiteur voit la page tarifs : 9 $/mois ET 3 $/7 jours (utilisable une seule fois), mention « payable en USDT (TRC-20) », CTA « S'abonner »."
    - "Le parcours d'abonnement s'arrête proprement : clic « S'abonner » → signup (auth au clic) → écran « Paiement disponible très bientôt » SANS adresse USDT ni flux de paiement (D-09)."
    - "Aucune promesse de gain ni allégation de performance n'apparaît dans le contenu (VITR-03) — prouvé par un test scannant les messages home/pricing."
  artifacts:
    - path: "apps/web/src/app/[locale]/(marketing)/page.tsx"
      provides: "page d'accueil RSC trilingue, sections D-05, proof slot masqué (D-08)"
      contains: "setRequestLocale"
    - path: "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"
      provides: "2 cartes tarifs (9$/mois + 3$/7j), mention USDT TRC-20, CTA signup (D-10/D-11/D-12)"
      contains: "getTranslations"
    - path: "apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx"
      provides: "écran post-signup honnête « paiement bientôt » (D-09), aucune adresse USDT"
      contains: "setRequestLocale"
    - path: "apps/web/test/no-perf-claims.test.ts"
      provides: "garde anti-allégation de perf — scanne messages home/pricing pour %/garanti/profit"
      contains: "no-perf-claims"
  key_links:
    - from: "apps/web/src/app/[locale]/(marketing)/page.tsx"
      to: "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"
      via: "Link i18n/navigation (CTA hero → /tarifs)"
      pattern: "tarifs"
    - from: "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"
      to: "apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx"
      via: "CTA → signup (P1) → /paiement-bientot"
      pattern: "signup|paiement-bientot"
---

<objective>
Construire les pages marketing trilingues de la vitrine : la page d'accueil bénéfice-first (VITR-01, sections D-05/D-06, CTA D-07, proof slot masqué D-08), la page tarifs étoffée (VITR-02 : 9 $/mois + 3 $/7 j une seule fois, mention USDT TRC-20 D-10, tarifs publics D-12), et l'écran de fin de funnel honnête « paiement bientôt » (D-09). Tout en respectant VITR-03 : zéro promesse de gain, zéro chiffre de performance, vert/rouge bannis.

Purpose: C'est le cœur conversion de la vitrine — ce que le visiteur voit et le parcours qui le mène à créer un compte. Le funnel s'arrête proprement à « paiement bientôt » (le paiement réel = P4). La home accueille le slot % mais ne l'affiche pas (P5).
Output: page.tsx (home), tarifs/page.tsx (étoffé), paiement-bientot/page.tsx, namespaces `home`/`paiement` + enrichissement `pricing` à parité fr/en/ar, test no-perf-claims.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-CONTEXT.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-UI-SPEC.md
@.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-PATTERNS.md

<interfaces>
<!-- Contrats consommés des Plans 01 (02-01) et 02 (02-02). NE PAS recréer. -->
Du Plan 01 (02-01) :
- Primitives shadcn dans `apps/web/src/components/ui/` : `Button` (`@/components/ui/button`), `Card` (`@/components/ui/card`), `Badge` (`@/components/ui/badge`), `Separator`. Utiliser le CTA accent via `<Button>`.
- Tokens de marque : `bg-background`, `bg-surface`, `text-foreground`, `text-muted`, `text-accent`, `border-border`, focus `focus-visible:ring-2` couleur accent. Vert/rouge ABSENTS (D-04).
- Polices câblées (Inter latin / IBM Plex Sans Arabic) via le layout — rien à faire ici, juste utiliser les classes de texte.

Du Plan 02 (02-02) :
- `<Footer />` + `<Disclaimer />` rendus globalement par le layout → NE PAS ré-ajouter de disclaimer dans ces pages (déjà présent en footer sur toutes les pages).
- Namespaces `disclaimer` et `legal` déjà posés.

Patterns P1 à honorer (cf. 02-PATTERNS.md) :
- Page RSC : `import { setRequestLocale, getTranslations } from 'next-intl/server'`, `const { locale } = await params`, `setRequestLocale(locale)` en tête (garde SSG), `const t = await getTranslations('namespace')`.
- Navigation localisée : `import { Link } from '@/i18n/navigation'` (jamais `next/link`) ; href sans préfixe locale (`/tarifs`, `/signup`, `/paiement-bientot`).
- Route signup existante (P1) : `app/[locale]/(auth)/signup` — le CTA pricing y mène ; succès signup → redirection vers `/paiement-bientot` (D-09). NE PAS recâbler l'auth (invariant P1 `getUser()` jamais `getSession()`), juste poser/ajuster la redirection de succès si nécessaire et au minimum câbler le lien.
- RTL : classes logiques (`ms/me/ps/pe/start/end`, `text-start`). INTERDIT `ml/mr/pl/pr/left/right/text-left`. Prix dans `<bdi>` + `Intl.NumberFormat` (anti-inversion RTL des montants).
- Container home : `mx-auto max-w-screen-xl px-4 md:px-6 lg:px-8` ; container prose : `max-w-prose`.

Allowlist conversion : home → `/tarifs` → `/signup` → `/paiement-bientot`. AUCUNE adresse USDT, AUCUN QR, AUCUN flux paiement (D-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page tarifs étoffée (VITR-02) + écran « paiement bientôt » (D-09) + namespaces pricing/paiement</name>
  <files>apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx, apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx (placeholder existant à étoffer — conserver setRequestLocale + getTranslations('pricing'))
    - apps/web/src/app/[locale]/(auth)/signup/page.tsx (route signup P1 — cible du CTA ; vérifier la redirection de succès pour la diriger vers /paiement-bientot, D-09)
    - apps/web/src/messages/fr.json (namespace `pricing` actuel = {title, body} lignes 25-28 — à enrichir ; ajouter `paiement`)
    - apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/components/ui/button.tsx (primitives Plan 01)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-UI-SPEC.md (§Copywriting — pricing plan1/plan2, post-signup ; §Color — badge « découverte » accent, pas de vert/rouge ; §Interaction — états cartes)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-PATTERNS.md (§tarifs/page.tsx, §paiement-bientot/page.tsx — squelette + namespace pricing étendu)
  </read_first>
  <action>
    Étoffer `apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx` (RSC, conserver `setRequestLocale` + `getTranslations('pricing')`). Container `<main className="mx-auto max-w-screen-xl px-4 py-16 text-start md:px-6 lg:px-8">`. Rendre 2 cartes (`Card` shadcn) côte à côte ≥ md (stack < md) :
      - Carte 1 « Standard » : prix « 9 $ / mois » via `<bdi>` (D-11), sous-texte « payable en USDT (TRC-20) » (D-10), CTA `<Button asChild><Link href="/signup">{t('cta')}</Link></Button>` (D-12 : auth au clic).
      - Carte 2 « Découverte » : prix « 3 $ / 7 jours » via `<bdi>`, note « offre découverte, utilisable une seule fois » (D-11, PAS 3$/15j), même sous-texte USDT, même CTA signup. Porte UN `<Badge>` accent « découverte » (UI-SPEC §Color point 4 — seul badge accent autorisé).
    ⛔ Aucun vert/rouge (D-04) ; aucun `%`/« garanti »/« profit » (VITR-03). Prix : `Intl.NumberFormat` pour le nombre, wrappé `<bdi>` pour ne pas s'inverser en arabe. États cartes : hover bordure/élévation subtile (pas de néon), CTA `focus-visible:ring-2`.
    Créer `apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx` (RSC, même squelette : `setRequestLocale` + `getTranslations('paiement')`). Container `max-w-prose`. Rendre heading « Paiement disponible très bientôt » + body « Votre compte est créé. Le paiement en USDT (TRC-20) arrive prochainement — vous serez notifié dès son ouverture. » (copy UI-SPEC §Copywriting, D-09). ⛔ AUCUNE adresse USDT, AUCUN QR, AUCUN champ de hash, AUCUN flux. Page purement informative statique.
    Câbler la redirection de succès du signup vers `/paiement-bientot` (D-09) — OBLIGATOIRE, le funnel doit être complet bout en bout (home → tarifs → signup → paiement-bientot). Lire la logique de succès existante de signup (P1) ; modifier UNIQUEMENT la cible de redirection de succès vers `/paiement-bientot` via `redirect` de `@/i18n/navigation`, SANS recâbler l'auth (invariant P1 `getUser()` jamais `getSession()` inchangé). Ne PAS laisser cette étape optionnelle : le must_haves.key_links exige la connexion `signup → /paiement-bientot`.
    Étendre les 3 messages (parité stricte) : enrichir `pricing` (`title`, `plan1Title`, `plan1Price`, `plan1Usdt`, `plan2Title`, `plan2Price`, `plan2Note`, `plan2Badge`, `cta`) — supprimer/remplacer l'ancienne clé `body` placeholder ; ajouter namespace `paiement` (`title`, `body`). FR source, en/ar traductions pro. ⛔ Aucun `%`/perf.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm --filter web exec tsc -b --noEmit 2>&1 | tail -3 && pnpm lint:i18n 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Tarifs : `grep -c "bdi" "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"` ≥ 1 (prix protégés RTL) ; CTA mène à signup : `grep -c "signup" "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"` ≥ 1.
    - Les deux offres présentes dans `pricing` : `grep -c "plan1Price" apps/web/src/messages/fr.json` retourne 1 ET `grep -c "plan2Price" apps/web/src/messages/fr.json` retourne 1 ; `grep -c "7 jours" apps/web/src/messages/fr.json` ≥ 1 ; `grep -c "15 j" apps/web/src/messages/fr.json` retourne 0 (D-11, pas l'ancien tarif).
    - Mention USDT : `grep -c "USDT (TRC-20)" apps/web/src/messages/fr.json` ≥ 1.
    - `apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx` existe ; `setRequestLocale` présent ; AUCUNE adresse/flux : `grep -niE "(adresse|wallet|0x|TR[A-Za-z0-9]{20}|qr|hash)" "apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx"` ne retourne RIEN (hors la mention « USDT (TRC-20) » textuelle de la copy).
    - Aucune classe physique : `grep -nE "\b(ml-|mr-|pl-|pr-|left-|right-|text-left|text-right)" "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx" "apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx"` ne retourne RIEN.
    - Aucun vert/rouge décoratif : `grep -nE "\b(text-green|bg-green|text-red|bg-red|border-green|border-red)" "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"` ne retourne RIEN (D-04).
    - Namespaces `pricing` enrichi + `paiement` à parité stricte fr/en/ar.
    - Redirect signup→paiement-bientot câblé (D-09, OBLIGATOIRE) : la cible de succès du signup contient `paiement-bientot` (`grep -rniE "paiement-bientot" "apps/web/src/app/[locale]/(auth)/signup/"` retourne ≥ 1) ; aucune modification de `getUser`/`getSession` dans le diff signup.
    - `tsc -b --noEmit` vert (hors baselines P1) ; `pnpm lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Page tarifs avec 2 offres (9$/mois + 3$/7j), mention USDT TRC-20, CTA signup ; écran « paiement bientôt » honnête sans flux ; funnel câblé tarifs→signup→paiement-bientot.</done>
</task>

<task type="auto">
  <name>Task 2: Page d'accueil bénéfice-first (VITR-01) + proof slot masqué (D-08) + namespace home</name>
  <files>apps/web/src/app/[locale]/(marketing)/page.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx (analog squelette page RSC — copier signature setRequestLocale + getTranslations, changer namespace en 'home')
    - apps/web/src/components/ui/button.tsx (CTA accent)
    - apps/web/src/messages/fr.json (ajouter namespace `home` à parité)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-UI-SPEC.md (§Copywriting — Hero H1 « Décidez avec discipline. », lede, méthode ; §Typography — Display 40/56px hero ; §Color — accent réservé CTA + 1 mot hero)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-PATTERNS.md (§page.tsx HOME — proof slot masqué pattern, sections D-05)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md (§Code Examples home + §Pattern 4 proof slot ; Pitfall A promesse de gain)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/(marketing)/page.tsx` (RSC). Signature identique à tarifs : `const { locale } = await params` ; `setRequestLocale(locale)` ; `const t = await getTranslations('home')`. Container `<main className="mx-auto max-w-screen-xl px-4 text-start md:px-6 lg:px-8">`.
    Sections (D-05, ordre : hero → comment-ça-marche → [proof masqué] → aperçu tarifs) :
      1. HERO : `<h1>` Display (`text-[40px] md:text-[56px] font-semibold leading-tight`) = `t('heroTitle')` (« Décidez avec discipline. », bénéfice-first D-06) ; lede `text-base text-muted` = `t('heroLede')` (« Chaque opportunité notée sur 100, son niveau de risque, et un plan clair : entrée, stop-loss, take-profits, ratio R:R. » — AUCUN % D-08) ; CTA `<Button asChild><Link href="/tarifs">{t('heroCta')}</Link></Button>` (« S'abonner / Voir les tarifs », D-07).
      2. COMMENT ÇA MARCHE (D-06 méthode) : titre `t('methodTitle')` (« Comment ça marche ») + body `t('methodBody')` (« Une IA façon trader vétéran qui combine analyse technique, fondamentale et news pour chaque opportunité. »). Optionnellement 3 étapes/cartes neutres (pas de chiffre de perf).
      3. PROOF SLOT MASQUÉ (D-08) : déclarer `const SHOW_PROOF = false` (commentaire « P5 activera le track record mesuré ») ; rendre `{SHOW_PROOF && null}` — STRUCTURE de layout vide, AUCUN nombre, AUCUN `%` ni en code ni en message (Pitfall 8). NE PAS rendre de `<div hidden>` contenant un faux chiffre.
      4. APERÇU TARIFS : court bloc renvoyant vers `/tarifs` via `<Link>` (réutilise la copy, pas de duplication des prix obligatoire — peut afficher « À partir de 3 $ » via `<bdi>` sans aucune perf).
    ⛔ Zéro `%`/« garanti »/« profit »/« 90% »/« rentable » (VITR-03/D-08, Pitfall A). Vert/rouge bannis (D-04). Si la home paraît creuse, ajouter au plus des métriques FACTUELLES non-perf (ex. « marchés couverts : crypto, forex, métaux, énergie ») via messages — JAMAIS un taux de réussite.
    Ajouter namespace `home` aux 3 messages à parité stricte (heroTitle, heroLede, heroCta, methodTitle, methodBody, + clés optionnelles factuelles). FR source ; en/ar traductions pro.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm --filter web exec tsc -b --noEmit 2>&1 | tail -3 && pnpm lint:i18n 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/[locale]/(marketing)/page.tsx` existe ; `setRequestLocale` présent ; `grep -c "getTranslations('home')" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ≥ 1.
    - CTA hero → tarifs : `grep -c "tarifs" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ≥ 1 ; navigation localisée : `grep -c "i18n/navigation" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ≥ 1.
    - Proof slot masqué : `grep -c "SHOW_PROOF" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ≥ 1 ET sa valeur est `false`.
    - AUCUN chiffre de perf : `grep -nE "%|90 *%|garanti|profit|rentable" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ne retourne RIEN dans le code source.
    - Aucune classe physique : `grep -nE "\b(ml-|mr-|pl-|pr-|left-|right-|text-left|text-right)" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ne retourne RIEN.
    - Aucun vert/rouge : `grep -nE "\b(text-green|bg-green|text-red|bg-red|border-green|border-red)" "apps/web/src/app/[locale]/(marketing)/page.tsx"` ne retourne RIEN.
    - Namespace `home` à parité stricte fr/en/ar ; aucun `%`/« garanti »/« profit » dans `home`.
    - `tsc -b --noEmit` vert (hors baselines P1) ; `pnpm lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Home trilingue bénéfice-first : hero + CTA tarifs + méthode + aperçu tarifs, proof slot construit mais masqué (zéro chiffre), zéro promesse de gain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Garde anti-allégation de perf (no-perf-claims) sur les messages (VITR-03/D-08)</name>
  <files>apps/web/test/no-perf-claims.test.ts</files>
  <read_first>
    - apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json (cibles du scan — namespaces home/pricing/paiement créés/enrichis dans ce plan)
    - .planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-RESEARCH.md (§Validation Architecture — no-perf-claims.test.ts ; Pitfall A signes : %/garanti/profit)
    - apps/web/test/messages-parity-legal.test.ts (créé Plan 02 — pattern de chargement des messages JSON pour Vitest, à réutiliser)
  </read_first>
  <behavior>
    - Test : aucune valeur de chaîne sous les namespaces `home`, `pricing`, `paiement` (3 langues) ne contient `%` ni « garanti »/« guaranteed » ni « profit » ni un motif de pourcentage type « 90% ». FAIL avec message clair listant la clé fautive si trouvé.
    - Test (sanity) : un objet de contrôle contenant « 90% » est bien détecté par la fonction de scan (preuve que le détecteur fonctionne, pas un test toujours-vert).
    - Note : la mention factuelle « USDT (TRC-20) » et les prix « 9 $ »/« 3 $ » sont AUTORISÉS (ce ne sont pas des allégations de perf) — le détecteur cible `%`/perf-words, pas le symbole `$`.
  </behavior>
  <action>
    Créer `apps/web/test/no-perf-claims.test.ts` (Vitest). Charger les 3 fichiers messages. Implémenter une fonction de scan récursive qui collecte toutes les valeurs string sous une liste de namespaces ciblés (`home`, `pricing`, `paiement`) et teste chaque valeur contre une regex de termes interdits : le caractère `%`, et les mots (insensibles à la casse, par langue) « garanti », « guaranteed », « profit », « rentable », un motif chiffre+`%`. Le test FAIL si une valeur matche, en rapportant le chemin de clé + la langue + la valeur. Ajouter le test de sanity (le détecteur attrape « 90% » dans un objet de contrôle). NE PAS scanner `legal`/`disclaimer` (le disclaimer mentionne légitimement « risque de perte » — cibler uniquement les namespaces marketing). Documenter en commentaire que `$` et « USDT (TRC-20) » sont explicitement non interdits.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm --filter web exec vitest run test/no-perf-claims.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/test/no-perf-claims.test.ts` existe et passe (messages réels home/pricing/paiement propres).
    - Le test de sanity prouve la détection de « 90% » (le détecteur n'est pas un no-op).
    - `grep -c "profit" apps/web/test/no-perf-claims.test.ts` ≥ 1 (terme interdit listé) ; le scan cible `home`/`pricing`/`paiement` et exclut `legal`/`disclaimer`.
    - Le test ne fait pas échouer sur « USDT (TRC-20) » ni sur les prix `$` (mentions factuelles autorisées).
  </acceptance_criteria>
  <done>Garde automatisée prouvant l'absence de promesse de gain / chiffre de perf dans le contenu marketing (VITR-03/D-08), avec détecteur vérifié non trivial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL segment `[locale]` → résolution serveur | Nouvelles pages publiques RSC ; hasLocale/notFound déjà géré au layout (P1) — ne pas régresser. |
| Visiteur → funnel signup | Le CTA mène à la route auth P1 ; aucune écriture métier en P2 ; l'invariant `getUser()` (jamais `getSession()`) reste intact. |
| Contenu marketing i18n → markup | Texte = clés de messages ; aucune injection HTML brute ; pas de chiffre de perf stocké en code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure / Conformité | allégation de performance ou promesse de gain sur la vitrine (risque légal/réputation, D-08/VITR-03) | mitigate | Proof slot vide (SHOW_PROOF=false, aucun nombre en code) ; test `no-perf-claims` scanne home/pricing/paiement ; copy validée juriste via gate LEGAL-02. |
| T-02-10 | Tampering | fausse adresse USDT / faux flux paiement sur l'écran post-signup (D-09) | mitigate | Page « paiement bientôt » purement informative ; acceptance grep interdit adresse/wallet/qr/hash ; paiement réel = P4. |
| T-02-11 | Input Validation | régression résolution locale sur les nouvelles pages | accept | Pages RSC standard sous `[locale]` ; hasLocale/notFound au layout (P1) inchangés ; couvert par E2E i18n P1 + home/pricing.spec à la phase gate. |
| T-02-12 | Spoofing / Auth | recâblage involontaire de l'auth via le funnel signup | mitigate | Réutilise la route signup P1 telle quelle ; redirection succès → /paiement-bientot via `@/i18n/navigation` sans toucher `getUser()`. |
| T-02-SC | Tampering | npm installs | n/a | Ce plan n'installe AUCUN paquet (pas de checkpoint AR-01-SC requis). Toutes les deps viennent de P1/02-01. |
</threat_model>

<verification>
- `tsc -b --noEmit` vert (hors baselines P1 documentées).
- `pnpm lint:i18n` exit 0 (aucune chaîne en dur ; marque exclue).
- Test `no-perf-claims.test.ts` vert (messages marketing propres + détecteur prouvé).
- Aucune classe physique RTL-cassante ; aucun vert/rouge décoratif (D-04) ; prix en `<bdi>`.
- Funnel : home → /tarifs → /signup → /paiement-bientot ; aucune adresse USDT/flux paiement.
- Proof slot masqué (SHOW_PROOF=false), aucun chiffre de perf en code ni en messages.
</verification>

<success_criteria>
- Home trilingue bénéfice-first avec CTA conversion (VITR-01), proof slot masqué jusqu'à P5 (D-08).
- Page tarifs publique : 9 $/mois + 3 $/7 j (une seule fois) + USDT TRC-20 + CTA signup (VITR-02 / D-10/D-11/D-12).
- Funnel honnête s'arrêtant à « paiement bientôt » sans flux ni adresse (D-09).
- Aucune promesse de gain / chiffre de performance, prouvé par test (VITR-03 / D-08).
- Aucune régression du socle P1 (i18n/RTL/locale/auth).
</success_criteria>

<output>
Create `.planning/phases/02-vitrine-publique-trilingue-gate-l-gal/02-03-SUMMARY.md` when done
</output>
