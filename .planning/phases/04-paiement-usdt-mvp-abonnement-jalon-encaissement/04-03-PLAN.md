---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
  - apps/web/src/components/ui/table.tsx
  - apps/web/src/components/ui/form.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/sonner.tsx
  - apps/web/src/components/ui/tabs.tsx
  - apps/web/src/components/ui/alert.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
  - apps/web/src/components/ui/progress.tsx
  - apps/web/package.json
autonomous: false
requirements: [PAY-01, PAY-04, PAY-05, PAY-06, ADMIN-01, ADMIN-02]
must_haves:
  truths:
    - "Le namespace payment existe en fr/en/ar a parite STRICTE de cles (CI check-i18n-hardcoded passe)"
    - "Les 8 blocs shadcn (table, form, textarea, sonner, tabs, alert, alert-dialog, progress) sont presents"
    - "La lib QR rend 100% offline, n'encode que l'adresse publique, zero fetch/CDN/telemetry (vettee avant install)"
  artifacts:
    - path: "apps/web/src/messages/fr.json"
      provides: "namespace payment + admin (copy canonique UI-SPEC)"
      contains: "payment"
    - path: "apps/web/src/components/ui/form.tsx"
      provides: "form react-hook-form+zod (HashForm)"
      contains: "FormField"
    - path: "apps/web/package.json"
      provides: "QR lib vettee + sonner dep"
  key_links:
    - from: "apps/web/src/messages/en.json"
      to: "apps/web/src/messages/fr.json"
      via: "parite stricte de cles"
      pattern: "payment"
---

<objective>
Preparer toute la couche presentation partagee du paiement : namespace i18n `payment` (member-facing, parite stricte fr/en/ar) + `admin` (FR), les 8 nouveaux blocs shadcn requis par les surfaces paiement/admin, et la lib QR vettee (offline, no telemetry) derriere un checkpoint de legitimite de paquet. Ces artefacts sont consommes par les Plans 05 (paiement) et 06 (admin/expiry).

Purpose: Decoupler l'installation/vetting (un seul nouveau paquet npm = QR lib, ASSUMED A9) et le scaffolding UI des plans qui assemblent les ecrans. La parite i18n stricte est gardee par CI ; le vetting QR est non-auto-approuvable (argent + bundle client).
Output: messages fr/en/ar etendus, 8 blocs ui/*, QR lib installee+vettee, package.json a jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md
@apps/web/components.json
@apps/web/src/messages/fr.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [GATE] Vetter + installer la lib QR (offline, no telemetry)</name>
  <read_first>
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Package Legitimacy Audit, A9, Stack QR lib)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (Registry Safety, QR rendering)
    - apps/web/package.json
  </read_first>
  <what-built>
    Un seul nouveau paquet npm est introduit dans cette phase : la lib QR. Elle est ASSUMED (slopcheck non execute, reseau bloque en recherche) et entre dans le BUNDLE CLIENT d'une phase tout-l'argent -> legitimite non auto-approuvable. Exigences dures (UI-SPEC Registry) : rendu 100% offline (zero fetch/CDN), encode UNIQUEMENT l'adresse publique, aucun service QR distant, aucune telemetrie.
  </what-built>
  <how-to-verify>
    1. Candidat principal : qrcode (mode toString/svg) OU une micro-lib svg pure. Verifier sur npmjs.com/package : age, downloads, repo source, derniere publication.
    2. npm view <qr-lib> version + npm view <qr-lib> scripts.postinstall (aucun postinstall suspect attendu).
    3. Inspecter que la lib ne fait AUCUN fetch/reseau au rendu (lecture du code ou doc) - elle encode une string en SVG/canvas localement.
    4. Installer : pnpm --filter web add <qr-lib-verifiee>.
    5. Confirmer dans apps/web/package.json la version epinglee.
  </how-to-verify>
  <acceptance_criteria>
    - QR lib choisie, legitimite verifiee (npmjs : age/downloads/repo/postinstall propre).
    - Installee dans apps/web, version epinglee dans package.json.
    - Confirme : rendu offline, encode l'adresse seule, zero reseau/telemetry.
  </acceptance_criteria>
  <resume-signal>Tape "approved: <nom-lib>@<version>" avec le verdict de legitimite, OU signale un paquet douteux (repli : SVG QR maison).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ajouter les 8 blocs shadcn (table, form, textarea, sonner, tabs, alert, alert-dialog, progress)</name>
  <files>apps/web/src/components/ui/table.tsx, apps/web/src/components/ui/form.tsx, apps/web/src/components/ui/textarea.tsx, apps/web/src/components/ui/sonner.tsx, apps/web/src/components/ui/tabs.tsx, apps/web/src/components/ui/alert.tsx, apps/web/src/components/ui/alert-dialog.tsx, apps/web/src/components/ui/progress.tsx, apps/web/package.json</files>
  <read_first>
    - apps/web/components.json (style radix-nova, registries officiel)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (Design System "New shadcn components", Registry Safety l.249)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md (components.json — existants a NE PAS re-ajouter)
  </read_first>
  <action>Ajouter via la CLI shadcn (registry officiel radix-nova, vetting non requis — UI-SPEC Registry Safety) les 8 blocs NEW : table, form (react-hook-form + zod resolver — note P2 D-02-01-D l'avait reporte ; confirmer presence sinon ajouter maintenant), textarea, sonner, tabs, alert, alert-dialog, progress. NE PAS re-ajouter les existants (badge, button, card, collapsible, dialog, dropdown-menu, input, label, select, separator, skeleton, tooltip). Respecter l'invariant Pitfall 7 P1 : ne pas reecrire le root app/layout.tsx (les ajouts shadcn ne doivent pas reinjecter une police au root). Externaliser tout label en dur genere par shadcn vers une prop traduite (precedent D-02-01-F sur ui/dialog closeLabel) pour que lint:i18n reste vert. Verifier que les deps react-hook-form + @hookform/resolvers + sonner sont dans package.json apres ajout.</action>
  <verify>
    <automated>ls apps/web/src/components/ui/table.tsx apps/web/src/components/ui/form.tsx apps/web/src/components/ui/textarea.tsx apps/web/src/components/ui/sonner.tsx apps/web/src/components/ui/tabs.tsx apps/web/src/components/ui/alert.tsx apps/web/src/components/ui/alert-dialog.tsx apps/web/src/components/ui/progress.tsx</automated>
  </verify>
  <done>Les 8 fichiers ui/* existent ; existants non re-ajoutes ; lint:i18n exit 0 ; root layout.tsx inchange (pass-through P1 preserve).</done>
</task>

<task type="auto">
  <name>Task 3: Namespaces i18n payment (fr/en/ar parite stricte) + admin (FR)</name>
  <files>apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/messages/fr.json (namespaces existants : common, language, theme, auth, access, pricing, paiement, home, disclaimer, legal, signals, signalDetail, glossary, dashboard)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (Copywriting Contract — table complete des cles, FR canonique)
    - apps/web/src/messages/__tests__ (test de parite existant)
  </read_first>
  <action>Ajouter le namespace `payment` (member-facing) dans fr.json (copy canonique = UI-SPEC Copywriting Contract : CTA plan/submit/renew, copy address/amount + toasts, network label D-01, amount instruction D-05, hash field, screenshot field D-03, polling heading/sub-copy/steps/timeout D-02, success state PAY-03, failures wrong_amount/wrong_token/not_confirmed/replay + re-submit D-04, ambiguous manual-review D-06, expiry banner ICU plural D-09, status chips, expired gated state D-10, discovery consumed + early upgrade D-12). Repliquer EXACTEMENT les memes cles dans en.json (traduction EN) et ar.json (traduction AR) — parite STRICTE imperative (CI check-i18n-hardcoded.mjs). Ajouter le namespace `admin` (FR uniquement acceptable pour le back-office mais route via next-intl, jamais hardcode) : colonnes membres/file, actions activate/reject/revoke/adjust + dialogs, empty states (UI-SPEC Copywriting). Montants/dates restent rendus via Intl + <bdi> cote composant (pas dans les messages). EN disclaimer : eviter le mot "profit" (garde no-perf-claims VITR-03, precedent D-02-02-D). Etendre `pricing` pour D-12 (offre decouverte consommee). Cles ICU plural pour l'expiry banner.</action>
  <verify>
    <automated>node -e "const f=require('./apps/web/src/messages/fr.json'),e=require('./apps/web/src/messages/en.json'),a=require('./apps/web/src/messages/ar.json');const keys=o=>Object.keys(o.payment||{}).sort().join(',');if(!(keys(f)&&keys(f)===keys(e)&&keys(f)===keys(a)))throw new Error('payment key parity mismatch fr/en/ar');console.log('parity OK')"</automated>
  </verify>
  <done>Namespace payment present aux 3 langues a parite STRICTE de cles (script parity OK) ; namespace admin FR present ; pricing etendu D-12 ; lint:i18n exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> bundle client | QR lib ASSUMED entrant dans le bundle d'une phase argent |
| message files -> UI | Aucun secret/cle privee ne doit apparaitre dans les messages i18n |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install QR lib | mitigate | checkpoint:human-verify bloquant (legitimite + offline + postinstall) ; non auto-approuvable (slopcheck non execute) |
| T-04-QR-NET | Info Disclosure | QR rendering | mitigate | QR 100% offline, encode l'adresse publique seule, zero service distant (UI-SPEC) |
| T-04-I18N-LEAK | Info Disclosure | message files | mitigate | aucune cle privee/secret dans les messages ; montants via Intl cote composant |
</threat_model>

<verification>
- Les 8 blocs ui/* presents (ls vert).
- Parite stricte payment fr/en/ar (script node vert).
- lint:i18n exit 0 ; pnpm typecheck sans nouvelle erreur.
</verification>

<success_criteria>
- QR lib vettee + installee (offline, no telemetry).
- 8 blocs shadcn ajoutes, existants intacts.
- Namespaces payment (3 langues parite stricte) + admin (FR) poses.
</success_criteria>

<output>
Creer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-03-SUMMARY.md` a la fin.
</output>
