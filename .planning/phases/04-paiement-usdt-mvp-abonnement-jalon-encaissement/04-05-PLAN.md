---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - apps/web/src/app/[locale]/(member)/abonnement/page.tsx
  - apps/web/src/app/[locale]/(member)/abonnement/actions.ts
  - apps/web/src/app/[locale]/(member)/abonnement/PlanCard.tsx
  - apps/web/src/app/[locale]/(member)/abonnement/PaymentPanel.tsx
  - apps/web/src/app/[locale]/(member)/abonnement/HashForm.tsx
  - apps/web/src/app/[locale]/(member)/abonnement/VerificationPolling.tsx
  - apps/web/src/app/[locale]/(member)/abonnement/__tests__/discovery.test.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-06]
must_haves:
  truths:
    - "L'utilisateur voit l'adresse USDT TRC-20 (reseau en grand + QR offline + copie 1-tap) et le montant atomique exact (nominal+offset reserve serveur)"
    - "A la soumission d'un hash, la server action attache le tx_hash AVANT toute lecture reseau (anti-TOCTOU), verifie les 5 invariants cote service_role et active l'abonnement via la RPC atomique si exact"
    - "Le polling react-query montre les etapes et ouvre l'acces a la confirmation ; timeout = repli doux, jamais erreur dure"
    - "L'offre decouverte 3$/7j est utilisable une seule fois (enforcement DB plan='discovery'), puis seul le standard reste visible"
    - "Une erreur de verification renvoie une raison localisee (typed error code) ; un hash deja utilise est rejete (replay) SANS appel reseau"
    - "Decisions couvertes: D-01 (adresse USDT TRC-20 unique partagee affichee, jamais de cle privee), D-02 (polling live), D-03 (screenshot optionnel, jamais dans la verif auto), D-04 (echec verif -> raison claire + re-soumission tracee), D-05 (montant unique reserve serveur), D-11 (renouvellement = meme parcours paiement, pas d'auto-renew), D-12 (offre decouverte 3$/7j one-shot + upgrade anticipe sans remboursement)"
  artifacts:
    - path: "apps/web/src/app/[locale]/(member)/abonnement/actions.ts"
      provides: "verifyPayment (service_role local, ordre anti-TOCTOU, 5 invariants, RPC, gate legal mainnet) + reservePayment (consomme reserveOffset)"
      contains: "isLegalReviewDone"
    - path: "apps/web/src/app/[locale]/(member)/abonnement/page.tsx"
      provides: "PlanCard (PAY-06) + PaymentPanel (PAY-01)"
      contains: "PaymentPanel"
    - path: "apps/web/src/app/[locale]/(member)/abonnement/VerificationPolling.tsx"
      provides: "polling react-query (D-02)"
      contains: "refetchInterval"
  key_links:
    - from: "apps/web/src/app/[locale]/(member)/abonnement/PlanCard.tsx"
      to: "apps/web/src/app/[locale]/(member)/abonnement/actions.ts"
      via: "onClick/form submit -> reservePayment (declenche la reservation d'offset + montant unique)"
      pattern: "reservePayment"
    - from: "apps/web/src/app/[locale]/(member)/abonnement/actions.ts"
      to: "packages/supabase reserveOffset"
      via: "reservePayment consomme reserveOffset (service_role, D-05)"
      pattern: "reserveOffset"
    - from: "apps/web/src/app/[locale]/(member)/abonnement/actions.ts"
      to: "packages/data-sources trongrid verify"
      via: "verifyTransfer"
      pattern: "verifyTransfer"
    - from: "apps/web/src/app/[locale]/(member)/abonnement/actions.ts"
      to: "activate_subscription_for_payment"
      via: "activateForPayment (RPC service_role)"
      pattern: "activateForPayment"
---

<objective>
Assembler le parcours de paiement member-facing : choix d'offre (PlanCard, decouverte one-shot D-12), ecran adresse/QR/montant (PaymentPanel), soumission de hash (HashForm), verification live (VerificationPolling), et la server action `verifyPayment` qui orchestre les 5 invariants cote service_role + l'activation atomique. Remplace le placeholder P2 "paiement bientot".

Purpose: C'est le chemin par lequel l'argent entre. Toute la logique financiere est 100% serveur (service_role cree LOCALEMENT, jamais le barrel) ; le client n'ecrit qu'une ligne payments(pending). Le montant attendu est reserve serveur (reserveOffset, jamais choisi par le client). L'ordre d'operations dans verifyPayment est inviolable : le tx_hash arme le UNIQUE(tx_hash) AVANT toute lecture reseau (anti-TOCTOU). Le gate legal (LEGAL-02) bloque l'activation reelle en mainnet.
Output: page abonnement + 4 composants + actions.ts (reservePayment + verifyPayment) + test discovery one-shot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md
@apps/web/src/app/[locale]/(auth)/actions.ts
@apps/web/src/lib/legal-gate.ts
@apps/web/src/lib/supabase/server.ts

<interfaces>
<!-- Consommes (deja livres par 02/03/04) : -->
From packages/supabase (barrel, service_role only):
  reserveOffset(client, { user_id, plan, base_amount_atomic, reservation_expires_at }) -> { payment_id, expected_amount_atomic }  // D-05, montant unique serveur
  OFFSET_RESERVATION_MINUTES  // = 60, valeur canonique partagee (reservation_expires_at = now()+OFFSET_RESERVATION_MINUTES)
  insertPendingPayment(client, row) ; getByHash(client, hash) ; transitionPayment(client, id, status, opts)
  activateForPayment(client, { payment_id, user_id, plan, period })  // RPC atomique
From packages/data-sources/src/trongrid:
  fetchTrc20TransfersForReceiver() ; verifyTransfer(transfer, ctx) -> VerificationResult
From packages/core:
  toAtomic(decimal) ; formatAtomic(atomic) ; USDT_DECIMALS
From apps/web/src/lib/legal-gate.ts:
  isLegalReviewDone(): boolean

<!-- Pattern service_role LOCAL (jamais le barrel) — RESEARCH Code Examples l.464-480 : -->
  'use server' ; import 'server-only'
  import { createClient as createServiceClient } from '@supabase/supabase-js'
  function serviceClientLocal() { /* process.env SUPABASE_URL + SERVICE_ROLE_KEY */ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: actions.ts — reservePayment (consomme reserveOffset) + verifyPayment (ordre anti-TOCTOU inviolable, 5 invariants, RPC, gate legal)</name>
  <files>apps/web/src/app/[locale]/(member)/abonnement/actions.ts</files>
  <read_first>
    - apps/web/src/app/[locale]/(auth)/actions.ts ('use server' + getUser SSR + toSafeErrorKey l.1-44)
    - apps/jobs/src/runJob.ts (getServiceClient l.30-41 — a inliner LOCALEMENT)
    - apps/web/src/lib/legal-gate.ts (isLegalReviewDone)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Code Examples, Verification Decision Tree, Pitfalls 2/6/7, Open Q3/Q4)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md (abonnement/actions.ts)
  </read_first>
  <action>Creer actions.ts ('use server' + import 'server-only'). Helper `serviceClientLocal()` inline (createClient @supabase/supabase-js depuis process.env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY) — JAMAIS `import { serviceClient }` du barrel (Pitfall 6, ESLint no-restricted-imports).

Server action `reservePayment(plan)` (Open Q3) : (1) `getUser()` via lib/supabase/server (anon SSR), (2) enforcement discovery one-shot (D-12) : si plan='discovery' et l'user a deja un payment/subscription plan='discovery' -> refuser (renvoyer code i18n), (3) calculer `reservation_expires_at = now()+OFFSET_RESERVATION_MINUTES` (constante importee du barrel supabase, valeur canonique D-05), (4) appeler `reserveOffset(serviceClientLocal(), { user_id, plan, base_amount_atomic, reservation_expires_at })` -> `{ payment_id, expected_amount_atomic }`. C'est `reserveOffset` (Plan 02) qui pose le montant attendu unique cote service_role ; le client ne choisit JAMAIS `expected_amount_atomic`. Retourner `{ payment_id, expected_amount_atomic }` au composant pour affichage.

Server action `verifyPayment(payment_id, tx_hash)` — ORDRE D'OPERATIONS INVIOLABLE (anti-TOCTOU, Pitfall 2) :
  (1) `getUser()` (anon SSR) ;
  (2) instancier `serviceClientLocal()` ;
  (3) GATE LEGAL : si `process.env.TRON_NETWORK === 'mainnet'` et `!isLegalReviewDone()` -> bloquer l'activation reelle (code 'legal_gate', Pitfall 7, Open Q4) ; testnet = libre ;
  (4a) ARMER LE UNIQUE D'ABORD — INSERT/UPDATE du `tx_hash` sur le payment pending via service_role. Cette ecriture declenche la contrainte `UNIQUE(tx_hash)` (23505) si le hash a deja servi. SI 23505 -> retourner IMMEDIATEMENT `{ status: 'rejected', reason: 'replay' }` SANS AUCUN appel TronGrid (le UNIQUE DB est le filet, jamais de lecture reseau sur un replay) ;
  (4b) SEULEMENT si pas de 23505 (le hash est inedit, desormais reserve par ce payment) -> proceder a la lecture reseau ;
  (5) `fetchTrc20TransfersForReceiver()` + trouver le transfer correspondant au hash ;
  (6) `verifyTransfer(transfer, { contract: env, receiver: env, expected_amount_atomic })` : exact -> `activateForPayment` (RPC atomique) period '7 days'(discovery)/'1 month'(standard) via luxon-derived interval -> verified+active ; over/under -> transitionPayment 'ambiguous' (file) ; rejected (wrong token/dest/non confirme) -> transitionPayment 'rejected' + reject_reason.
La regle d'or : l'ecriture du tx_hash qui arme le UNIQUE (4a) DOIT preceder toute lecture reseau (5). Jamais l'inverse — sinon fenetre TOCTOU de double-credit.

Server action `getPaymentStatus(payment_id)` pour le polling (lit le statut). Tous les retours d'erreur = typed error codes -> messages i18n (toSafeErrorKey-like, jamais error.message brut, RESEARCH Decision Tree). Chaque tentative tracee (D-04).</action>
  <verify>
    <automated>grep -v '^//' "apps/web/src/app/[locale]/(member)/abonnement/actions.ts" | grep -c "isLegalReviewDone"</automated>
  </verify>
  <done>actions.ts : reservePayment consomme `reserveOffset` (jamais expected_amount_atomic cote client) + `OFFSET_RESERVATION_MINUTES` ; verifyPayment respecte l'ordre inviolable (4a arme UNIQUE/23505->replay SANS appel reseau, PUIS 4b/5/6 lecture+verif) ; service_role LOCAL (pas le barrel) ; gate legal sur mainnet ; over/under->ambiguous ; typed error codes ; grep isLegalReviewDone >= 1 ; pnpm typecheck vert.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx + PlanCard (declenche reservePayment) + PaymentPanel + HashForm</name>
  <files>apps/web/src/app/[locale]/(member)/abonnement/page.tsx, apps/web/src/app/[locale]/(member)/abonnement/PlanCard.tsx, apps/web/src/app/[locale]/(member)/abonnement/PaymentPanel.tsx, apps/web/src/app/[locale]/(member)/abonnement/HashForm.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (RSC gated member) + tarifs page (P2)
    - apps/web/src/app/[locale]/(auth)/login/page.tsx (form -> action)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (Component Anatomy PlanCard/PaymentPanel/HashForm, Copywriting, Money-display law, Color)
    - apps/web/src/messages/fr.json (namespace payment de Plan 03)
  </read_first>
  <action>Creer page.tsx (RSC sous [locale]/(member)/abonnement, remplace le placeholder paiement-bientot P2). PlanCard (PAY-06/D-12) : standard 9$/mois + decouverte 3$/7j, prix en Display `<bdi>`, badge "une seule fois", carte decouverte masquee/desactivee si consommee (lit l'historique), "Passer au standard" si periode decouverte active. CABLAGE CLE : la selection d'un plan dans PlanCard (onClick/form submit) appelle la server action `reservePayment(plan)` ; la valeur retournee `{ payment_id, expected_amount_atomic }` est propagee a PaymentPanel qui affiche le montant unique exact. Le client n'invente JAMAIS le montant — il rend ce que reservePayment a reserve serveur. PaymentPanel (PAY-01/D-01/D-05) : bloc reseau "Reseau : TRC-20 (TRON)" en grand + adresse boxee tabular `<bdi>` + bouton copie 1-tap (Copy->Check + sonner toast), QR offline (lib Plan 03, encode l'adresse SEULE, alt label) ; bloc montant exact `expected_amount_atomic` (Display brand-blue `<bdi>`, ex 9.02 USDT via formatAtomic+Intl) + copie + note offset D-05 + compte-a-rebours de reservation (OFFSET_RESERVATION_MINUTES) ; HashForm (form+zod resolver, champ hash + validation inline, submit "J'ai paye — verifier" -> verifyPayment(payment_id, hash), chaque soumission tracee D-04) ; screenshot optionnel (D-03, jamais bloquant, jamais dans la verif). Tout texte via next-intl namespace payment ; montants/dates `<bdi>`+Intl ; touch targets >=44px ; RTL via proprietes logiques. Mobile-first. Le client n'importe JAMAIS le service-client (frontiere producteur-unique).</action>
  <verify>
    <automated>grep -rl "useTranslations\|getTranslations" "apps/web/src/app/[locale]/(member)/abonnement/PaymentPanel.tsx" "apps/web/src/app/[locale]/(member)/abonnement/PlanCard.tsx"</automated>
  </verify>
  <done>page+PlanCard+PaymentPanel+HashForm rendus ; PlanCard appelle reservePayment et rend le `expected_amount_atomic` retourne (jamais calcule client) ; tout texte via next-intl, montant exact via formatAtomic+Intl+<bdi>, QR offline, copie 1-tap, decouverte one-shot ; lint:i18n exit 0 ; pas d'import service-client.</done>
</task>

<task type="auto">
  <name>Task 3: VerificationPolling (react-query) + test discovery one-shot</name>
  <files>apps/web/src/app/[locale]/(member)/abonnement/VerificationPolling.tsx, apps/web/src/app/[locale]/(member)/abonnement/__tests__/discovery.test.ts</files>
  <read_first>
    - apps/web/src/components/providers/QueryProvider.tsx (QueryClient deja monte)
    - apps/web/src/lib/signals/queries.ts (lecture anon-only)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (VerificationPolling anatomy, Copywriting polling/success/failure/timeout, D-02)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Open Q2 polling)
  </read_first>
  <action>Creer VerificationPolling.tsx (client) : `useQuery` avec `refetchInterval: 4000`, `staleTime: 0`, key sur payment_id, queryFn = getPaymentStatus action (Open Q2). Checklist steppee (Transaction trouvee -> Montant et destinataire verifies -> Confirmations on-chain -> Abonnement active), Loader2/Check par etape. Stop sur verified/rejected/ambiguous OU ~120s -> message timeout doux "Continuer en arriere-plan" (jamais erreur dure, D-02). verified -> success state + CTA "Acceder aux signaux". rejected -> VerificationFailure (alert rouge, raison localisee depuis typed error code, bouton "Soumettre un autre hash"). ambiguous -> message calme "revue manuelle" (D-06). prefers-reduced-motion honore, focus deplace vers le nouveau heading. Test discovery.test.ts (PAY-06) : la logique d'enforcement one-shot (plan='discovery' deja consomme -> refus) testee en unite sur la fonction de decision (extraite de actions.ts si besoin pour testabilite) ; 2e discovery refuse, standard seul visible apres consommation.</action>
  <verify>
    <automated>npx vitest run "apps/web/src/app/[locale]/(member)/abonnement/__tests__/discovery.test.ts"</automated>
  </verify>
  <done>VerificationPolling : refetchInterval 4000, stop sur etat final/timeout, repli doux ; discovery.test.ts vert (one-shot enforce) ; failure/ambiguous/success states presents ; tout texte via next-intl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action | Le client ne doit ecrire que pending ; montant attendu reserve serveur (reservePayment/reserveOffset) |
| server action -> TronGrid/DB | Logique financiere + cle TronGrid + service_role 100% serveur |
| ordre tx_hash vs lecture reseau | Le tx_hash doit armer le UNIQUE AVANT toute lecture TronGrid (fenetre TOCTOU) |
| chemin mainnet -> activation | Encaissement reel interdit avant sign-off juriste (LEGAL-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SVCCLIENT | Info Disclosure | actions.ts | mitigate | service_role cree LOCALEMENT dans 'use server'/'server-only' ; jamais le barrel (Pitfall 6, ESLint) |
| T-04-AMOUNTSET | Tampering | reservePayment | mitigate | montant attendu pose par reserveOffset (service_role), jamais choisi par le client (Open Q3) |
| T-04-LEGAL | Compliance | verifyPayment | mitigate | isLegalReviewDone() consomme sur chemin mainnet AVANT activation reelle (Pitfall 7, Open Q4) |
| T-04-REPLAY-A | Tampering | verifyPayment | mitigate | tx_hash arme le UNIQUE(tx_hash) DB (4a) AVANT toute lecture reseau ; 23505 -> replay immediat sans appel TronGrid (ordre inviolable, anti-TOCTOU) |
| T-04-ERRLEAK | Info Disclosure | actions.ts | mitigate | typed error codes -> i18n opaque, jamais error.message brut au client (toSafeErrorKey pattern) |
| T-04-AUTODEC-A | Tampering | verifyPayment | mitigate | over/under -> ambiguous (file), jamais activation/rejet auto (D-06/D-07) |
</threat_model>

<verification>
- grep isLegalReviewDone dans actions.ts >= 1.
- npx vitest run discovery.test.ts vert.
- Ordre anti-TOCTOU : tx_hash ecrit (4a) PUIS lecture reseau (4b/5) ; 23505 -> replay sans appel TronGrid.
- lint:i18n exit 0 ; pnpm typecheck vert ; aucun import service-client dans apps/web.
- Manual (human-verify a la verification de phase) : parcours testnet Nile bout en bout (adresse->hash->polling->actif).
</verification>

<success_criteria>
- Parcours paiement complet rendu (offre->reservePayment->adresse/QR/montant unique->hash->polling->resultat).
- verifyPayment : ordre anti-TOCTOU inviolable, 5 invariants service_role, RPC atomique, gate legal mainnet, replay rejete sans appel reseau.
- Montant attendu reserve serveur (reserveOffset), jamais cote client.
- Decouverte one-shot enforce (test vert).
</success_criteria>

<output>
Creer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-05-SUMMARY.md` a la fin.
</output>
