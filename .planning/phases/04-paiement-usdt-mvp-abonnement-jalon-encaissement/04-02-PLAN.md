---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0012_payments.sql
  - packages/supabase/src/database.types.ts
  - packages/supabase/src/index.ts
  - packages/supabase/src/repositories/payments.ts
  - packages/supabase/src/repositories/subscriptions.ts
  - .env.example
autonomous: false
requirements: [PAY-03, PAY-04, PAY-06, ADMIN-01, ADMIN-02]
user_setup:
  - service: trongrid
    why: "Lecture on-chain TRC-20 (clé tier gratuit) + adresse de réception cold wallet"
    env_vars:
      - name: TRONGRID_API_KEY
        source: "TronGrid dashboard (compte gratuit)"
      - name: USDT_RECEIVE_ADDRESS
        source: "Cold wallet TRON (adresse publique de réception, jamais la clé privée)"
      - name: USDT_CONTRACT_ADDRESS
        source: "Contrat USDT — mainnet TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t / Nile = autre adresse"
      - name: TRON_NETWORK
        source: "nile (dev/test) | mainnet (prod) — définit le gate LEGAL-02"
must_haves:
  truths:
    - "L'utilisateur ne peut INSÉRER qu'une ligne payments(pending) pour lui-même ; aucune transition verified/active possible par lui"
    - "Le même tx_hash ne peut jamais exister deux fois (UNIQUE GLOBAL), même en course concurrente"
    - "Une RPC atomique service_role transitionne payment->verified ET subscription->active en une transaction"
    - "Le superadmin lit tous les paiements ; un membre ne lit que les siens"
  artifacts:
    - path: "supabase/migrations/0012_payments.sql"
      provides: "table payments + RLS + UNIQUE(tx_hash) + index offset + RPC activate_subscription_for_payment"
      contains: "activate_subscription_for_payment"
    - path: "packages/supabase/src/repositories/payments.ts"
      provides: "insertPendingPayment, getByHash, transitionPayment (service_role)"
      contains: "transitionPayment"
    - path: "packages/supabase/src/repositories/subscriptions.ts"
      provides: "activateForPayment (RPC), expireDue, changePlan (service_role)"
      contains: "activateForPayment"
  key_links:
    - from: "packages/supabase/src/repositories/subscriptions.ts"
      to: "activate_subscription_for_payment"
      via: "client.rpc"
      pattern: "rpc\\(.activate_subscription_for_payment"
    - from: "packages/supabase/src/index.ts"
      to: "packages/supabase/src/database.types.ts"
      via: "barrel re-export du type payments"
      pattern: "payments"
---

<objective>
Poser la couche données du paiement : migration 0012 (table `payments`, RLS producteur-unique, `UNIQUE(tx_hash)` GLOBAL anti-replay, réservation d'offset, RPC atomique d'activation), application LIVE via MCP `apply_migration`, régénération des types, et repositories service_role typés (payments + subscriptions).

Purpose: Le `UNIQUE(tx_hash)` DB est le SEUL garde-fou inviolable contre le double-crédit (le check applicatif a une TOCTOU). La RPC atomique garantit qu'un paiement vérifié et un abonnement activé ne divergent jamais. Tout ce socle est consommé par la server action (Plan 05) et le job (Plan 06).
Output: 0012 appliquée live, types/barrel à jour, repos payments+subscriptions, .env.example étendu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md
@supabase/migrations/0006_analyses_trade_setups.sql
@supabase/migrations/0008_profiles_role.sql
@supabase/migrations/0009_subscriptions_gating.sql

<interfaces>
<!-- Contrats consommés par Plan 05 (server action) et Plan 06 (admin + job). -->

À créer dans packages/supabase/src/repositories/payments.ts :
  type ServiceClient = SupabaseClient<Database>
  insertPendingPayment(client, { user_id, tx_hash, plan, expected_amount_atomic, screenshot_url? }): Promise<{ id: string }>
  getByHash(client, tx_hash): Promise<PaymentRow | null>
  transitionPayment(client, id, status: 'rejected'|'ambiguous', { reject_reason?, amount_atomic? }): Promise<void>
  // capter code 23505 (unique violation) = replay -> erreur typée 'replay'

À créer dans packages/supabase/src/repositories/subscriptions.ts :
  activateForPayment(client, { payment_id, user_id, plan, period }): Promise<void> // appelle RPC
  expireDue(client): Promise<{ expired: number }>   // pour le job (Plan 06)
  changePlan(client, { user_id, plan, period? }): Promise<void> // pour admin (Plan 06)

Analog appel RPC (gate.ts l.89) : const { data } = await supabase.rpc('has_active_subscription')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire migration 0012_payments.sql (table + RLS + UNIQUE + offset + RPC)</name>
  <files>supabase/migrations/0012_payments.sql</files>
  <read_first>
    - supabase/migrations/0006_analyses_trade_setups.sql (table + RLS lecture authenticated, AUCUNE policy write, index unique partiel l.93-95)
    - supabase/migrations/0009_subscriptions_gating.sql (lecture scopée user_id=auth.uid() + superadmin l.41-50, schéma subscriptions status/plan/current_period_end)
    - supabase/migrations/0008_profiles_role.sql (RPC security definer + revoke/grant l.29-47)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (§Pattern 4 RPC, §RLS payments l.484-523, §Open Question 1 réservation offset)
  </read_first>
  <action>Créer `0012_payments.sql`. Table `public.payments` (per RESEARCH l.486-499 + CONTEXT Claude's Discretion) : `id uuid pk default gen_random_uuid()`, `user_id uuid not null references profiles(id) on delete cascade`, `tx_hash text not null`, `plan text not null check (plan in ('discovery','standard'))`, `expected_amount_atomic bigint not null`, `amount_atomic bigint` (null tant que pending), `status text not null default 'pending' check (status in ('pending','verified','rejected','ambiguous'))`, `reject_reason text`, `screenshot_url text`, `reservation_expires_at timestamptz` (D-05), `created_at timestamptz not null default now()`, `verified_at timestamptz`. Enable RLS. Index : (a) `create unique index payments_tx_hash_global_idx on payments(tx_hash)` GLOBAL anti-replay (PAY-04) ; (b) réservation offset D-05 : `create unique index payments_expected_amount_active_idx on payments(expected_amount_atomic) where status='pending' and reservation_expires_at > now()` — un seul montant attendu actif à la fois ; saturation -> repli millièmes/file documenté en commentaire SQL (Open Question 1, réservation ~60 min). Policies : INSERT authenticated `with check (user_id = auth.uid() and status = 'pending')` ; SELECT authenticated `using (user_id = auth.uid())` ; SELECT authenticated `using (public.is_superadmin())` (ADMIN-01/02). AUCUNE policy update/delete (transitions service_role, D-08). RPC `activate_subscription_for_payment(p_payment_id uuid, p_user_id uuid, p_plan text, p_period interval)` en `plpgsql security definer set search_path = public` : UPDATE payment status='verified', verified_at=now() WHERE id=p_payment_id AND status='pending' (raise si not found) ; puis UPSERT subscriptions (status='active', plan=p_plan, current_period_end = greatest(coalesce(current_period_end, now()), now()) + p_period) — prolonge en cas de renouvellement (D-11). `revoke execute on function ... from public, anon, authenticated` (service_role bypass, A8 à confirmer). En-tête de migration = convention appliquée via MCP apply_migration (PAS db push, cf. 0006).</action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0012_payments.sql | grep -c "payments_tx_hash_global_idx"</automated>
  </verify>
  <done>0012 contient : table payments, RLS (1 insert + 2 select, 0 update/delete), UNIQUE(tx_hash) global, index offset partiel, RPC security definer revoke. Grep tx_hash_global_idx >= 1 (hors commentaires).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Appliquer 0012 via MCP apply_migration + régénérer types</name>
  <read_first>
    - supabase/migrations/0012_payments.sql (écrit en Task 1)
    - packages/supabase/src/database.types.ts (à étendre — précédent D-03-01-C : édition manuelle si non `link`é)
    - packages/supabase/src/index.ts (barrel à étendre l.47-73, précédent Phase 4)
  </read_first>
  <what-built>
    La migration 0012 existe en SQL mais n'est PAS dans la base live. Convention repo (CRITIQUE) : les migrations s'appliquent via le MCP Supabase `apply_migration`, JAMAIS `supabase db push`. Le build/typecheck passe SANS cette application (les types viennent du fichier généré) — d'où un faux-positif si on saute cette étape. Le projet n'est pas `link`é localement (D-01-01-D / D-03-01-C) -> `supabase gen types --linked` échoue par design ; éditer `database.types.ts` à la main pour la table `payments` + ré-exporter dans le barrel.
  </what-built>
  <how-to-verify>
    1. Appliquer la migration via MCP : `apply_migration` avec le contenu de `supabase/migrations/0012_payments.sql` (name: `0012_payments`).
    2. Vérifier via MCP `list_tables` que `payments` existe avec ses colonnes + via `get_advisors` (security) qu'aucun nouvel avertissement bloquant n'apparaît (les WARN security-definer sur la RPC sont EXPECTED BY DESIGN, cf. D-01-01-D).
    3. Régénérer/éditer `packages/supabase/src/database.types.ts` : ajouter le type Row/Insert/Update de `payments` + la signature de la fonction `activate_subscription_for_payment` dans `Database['public']['Functions']`.
    4. Ré-exporter `PaymentRow`/`PaymentInsert` dans le barrel `packages/supabase/src/index.ts` (suivre le précédent l.47-73).
    5. `pnpm typecheck` doit rester vert (baseline P1 inchangée).
  </how-to-verify>
  <acceptance_criteria>
    - MCP `list_tables` montre `payments` avec UNIQUE(tx_hash), RLS activée, RPC `activate_subscription_for_payment` présente.
    - `database.types.ts` exporte le type de la table payments + la fonction RPC ; barrel ré-exporte payments.
    - `pnpm typecheck` vert (aucune nouvelle erreur).
  </acceptance_criteria>
  <resume-signal>Tape "applied" avec la sortie MCP list_tables/get_advisors, OU décris l'échec d'application.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Repositories payments.ts + subscriptions.ts (service_role typés)</name>
  <files>packages/supabase/src/repositories/payments.ts, packages/supabase/src/repositories/subscriptions.ts, packages/supabase/src/index.ts, .env.example</files>
  <read_first>
    - packages/supabase/src/repositories/tradeSetups.ts (analog exact : type ServiceClient, write service_role, transition status-only, doc-bloc "JAMAIS importé depuis apps/web" l.15)
    - apps/web/src/lib/auth/gate.ts (l.89 : pattern .rpc())
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md (§payments.ts, §subscriptions.ts)
    - .env.example (à étendre, sans valeurs)
  </read_first>
  <action>Créer `payments.ts` : `type ServiceClient = SupabaseClient<Database>`, doc-bloc "JAMAIS importé depuis apps/web (D-07)" (copier de tradeSetups.ts l.15). Fonctions : `insertPendingPayment(client, row)` -> retourne `{ id }`, capter le code Postgres `23505` (unique violation tx_hash) et lever une erreur typée reconnaissable `'replay'` (Pitfall 2) ; `getByHash(client, tx_hash)` -> PaymentRow|null ; `transitionPayment(client, id, status, { reject_reason?, amount_atomic? })` (update status-only, jamais delete). Créer `subscriptions.ts` : `activateForPayment(client, { payment_id, user_id, plan, period })` -> `client.rpc('activate_subscription_for_payment', { p_payment_id, p_user_id, p_plan, p_period })` (period en string interval, ex '7 days'/'1 month') ; `expireDue(client)` -> `UPDATE subscriptions SET status='expired' WHERE status='active' AND current_period_end <= now()` retourne `{ expired: count }` (pour le job Plan 06) ; `changePlan(client, { user_id, plan, period? })` (pour admin Plan 06). Toutes les erreurs Supabase -> `throw new Error('...failed: ' + error.message)` (pattern tradeSetups). Ré-exporter les deux repos dans le barrel `index.ts`. Étendre `.env.example` (SANS valeurs) avec `TRONGRID_API_KEY`, `USDT_RECEIVE_ADDRESS`, `USDT_CONTRACT_ADDRESS`, `TRON_NETWORK`.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>payments.ts + subscriptions.ts compilent, ré-exportés du barrel ; insertPendingPayment mappe 23505->'replay' ; activateForPayment appelle la RPC ; .env.example contient les 4 nouvelles vars sans valeurs ; aucune nouvelle erreur tsc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client authenticated -> table payments | L'user ne doit écrire QUE pending pour lui-même ; jamais verified/active |
| insert concurrent même tx_hash | TOCTOU : deux soumissions simultanées du même hash |
| repository -> apps/web | Le service_role ne doit jamais fuiter côté client (barrel ne l'exporte pas) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-REPLAY | Tampering/Elevation | payments table | mitigate | `UNIQUE(tx_hash)` GLOBAL DB + catch 23505->'replay' (filet inviolable, le check applicatif a une TOCTOU) |
| T-04-RLS-ELEV | Elevation | payments RLS | mitigate | INSERT policy `status='pending' and user_id=auth.uid()` ; ZÉRO policy update/delete -> transitions service_role only |
| T-04-INCONSIST | Tampering | activation | mitigate | RPC atomique security definer : payment->verified + sub->active en une transaction |
| T-04-OFFSET | Denial of Service | offset réservation | accept | index unique partiel sur pending non expiré (réservation ~60 min) ; saturation -> repli documenté (Open Q1) |
| T-04-SVCKEY | Info Disclosure | barrel supabase | mitigate | repos doc-bloc + barrel n'exporte pas service-client ; consommation côté serveur uniquement |
</threat_model>

<verification>
- `grep -v '^--' supabase/migrations/0012_payments.sql | grep -c payments_tx_hash_global_idx` >= 1.
- MCP `list_tables` montre payments + RPC après apply_migration.
- `pnpm typecheck` vert.
</verification>

<success_criteria>
- 0012 appliquée live via MCP, types + barrel à jour.
- Repos payments + subscriptions service_role typés, anti-replay (23505) câblé.
- .env.example documente les nouvelles vars (jamais de clé privée).
</success_criteria>

<output>
Créer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-02-SUMMARY.md` à la fin.
</output>
