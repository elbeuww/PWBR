---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 04
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/data-sources/src/trongrid/schema.ts
  - packages/data-sources/src/trongrid/schema.test.ts
  - packages/data-sources/src/trongrid/client.ts
  - packages/data-sources/src/trongrid/verify.ts
  - packages/data-sources/src/trongrid/verify.test.ts
  - packages/data-sources/src/trongrid/index.ts
  - packages/data-sources/src/index.ts
autonomous: true
requirements: [PAY-02, PAY-04]
must_haves:
  truths:
    - "Le parseur Zod valide la fixture TronGrid Nile reelle (figee Plan 01) sans erreur"
    - "Les 5 invariants conjoints rejettent : mauvais token, mauvais destinataire, montant faux, non confirme ; et acceptent l'exact"
    - "Sur-paiement et sous-paiement renvoient 'ambiguous' (file), jamais une activation/rejet auto"
    - "Le montant on-chain (value string atomique) est compare en BigInt exact, zero float"
  artifacts:
    - path: "packages/data-sources/src/trongrid/verify.ts"
      provides: "verifyTransfer -> VerificationResult (exact/over/under/wrong_token/wrong_recipient/not_confirmed)"
      contains: "verifyTransfer"
    - path: "packages/data-sources/src/trongrid/client.ts"
      provides: "fetchTrc20TransfersForReceiver (fetch+Zod+p-retry+Retry-After)"
      contains: "TRON-PRO-API-KEY"
    - path: "packages/data-sources/src/trongrid/schema.ts"
      provides: "Zod schemas figes sur fixture reelle"
      contains: "passthrough"
  key_links:
    - from: "packages/data-sources/src/trongrid/verify.ts"
      to: "packages/data-sources/src/trongrid/address.ts"
      via: "sameAddress (normalisation hex<->base58)"
      pattern: "sameAddress"
    - from: "packages/data-sources/src/trongrid/verify.ts"
      to: "packages/core money atomic"
      via: "comparaison BigInt exacte"
      pattern: "=== "
---

<objective>
Construire la logique de verification on-chain : client TronGrid (fetch+Zod+p-retry, miroir fred/marketaux), schema Zod fige sur la fixture REELLE Nile (Plan 01), et la decision des 5 invariants conjoints. C'est le coeur de correction financiere : un seul paiement crédite a tort = perte directe. Tout est golden-teste sur fixtures, sans dependance reseau a l'execution des tests.

Purpose: La vérification doit valider le ET logique des 5 invariants (contrat USDT officiel .env, destinataire normalise, montant atomique exact, confirme, hash inedit). Sur/sous-paiement -> file superadmin (jamais d'auto-decision risquee).
Output: trongrid/{schema,client,verify}.ts + tests sur fixtures, barrels exportes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md
@packages/data-sources/src/fred/client.ts
@packages/data-sources/src/marketaux/client.ts
@packages/data-sources/src/trongrid/__fixtures__/nile-trc20-transfer.json

<interfaces>
<!-- Briques deja livrees par Plan 01 (a consommer, ne pas recreer). -->
From packages/core (barrel):
  export const USDT_DECIMALS: bigint
  export function toAtomic(decimal: string): bigint
  export function formatAtomic(atomic: bigint): string

From packages/data-sources/src/trongrid/address.ts:
  export function sameAddress(a: string, b: string): boolean

<!-- A creer dans verify.ts : -->
  type VerificationResult =
    | { kind: 'exact'; amount_atomic: bigint }
    | { kind: 'over'; amount_atomic: bigint }       // -> ambiguous (D-07)
    | { kind: 'under'; amount_atomic: bigint }      // -> ambiguous (D-06)
    | { kind: 'rejected'; reason: 'wrong_token'|'wrong_recipient'|'not_confirmed' }
  export function verifyTransfer(transfer, ctx: { contract, receiver, expected_amount_atomic }): VerificationResult
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: schema.ts — Zod fige sur fixture Nile reelle</name>
  <files>packages/data-sources/src/trongrid/schema.ts, packages/data-sources/src/trongrid/schema.test.ts</files>
  <read_first>
    - packages/data-sources/src/trongrid/__fixtures__/nile-trc20-transfer.json (forme REELLE figee Plan 01 — source de verite, PAS l'esquisse ASSUMED de RESEARCH)
    - packages/data-sources/src/marketaux/client.ts (Zod tolerant .passthrough() + .catch() l.21-47)
    - packages/data-sources/src/fred/schema.test.ts (style test)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Pattern 1, Pitfall 1)
  </read_first>
  <behavior>
    - Test: parse(fixture nile reelle) reussit, expose data[] avec from/to/value/token_info.address/token_info.decimals
    - Test: value reste une string (atomique) ; BigInt(value) donne le montant attendu de la fixture
    - Test: token_info.decimals === 6 sur la fixture USDT-test
    - Test: un objet avec champs en trop passe (.passthrough)
    - Test: un objet sans token_info.address echoue (champ requis pour l'invariant contrat)
  </behavior>
  <action>Creer schema.ts CALQUE sur la forme REELLE de la fixture (Plan 01), PAS sur l'esquisse ASSUMED de RESEARCH (les noms/casing peuvent diverger — c'est tout le point du checkpoint Plan 01). `.passthrough()` obligatoire. `value` reste string -> consommateur fait `BigInt(value)` (jamais Number, RESEARCH Pattern 3/Pitfall 4). token_info.address requis (sert l'invariant contrat). Exporter le type infere. RED (schema.test.ts contre la fixture) puis GREEN.</action>
  <verify>
    <automated>npx vitest run packages/data-sources/src/trongrid/schema.test.ts</automated>
  </verify>
  <done>schema.test.ts parse la fixture reelle ; value en string ; decimals===6 ; passthrough OK ; champ contrat requis.</done>
</task>

<task type="auto">
  <name>Task 2: client.ts — fetch+Zod+p-retry (miroir fred)</name>
  <files>packages/data-sources/src/trongrid/client.ts, packages/data-sources/src/trongrid/index.ts, packages/data-sources/src/index.ts</files>
  <read_first>
    - packages/data-sources/src/fred/client.ts (miroir complet : pLimit + pRetry + Retry-After l.57-95, cle env throw l.48-52)
    - packages/data-sources/src/trongrid/schema.ts (parser de Task 1)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Pattern 1 l.269-292, A2/A3)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md (client.ts)
  </read_first>
  <action>Creer client.ts en copiant la mecanique fred verbatim (pLimit(2), pRetry retries:3, onFailedAttempt Retry-After 429). DIFFERENCES TronGrid : cle `TRONGRID_API_KEY` (throw si absente), passee en HEADER `TRON-PRO-API-KEY` (PAS query param). Base URL conditionnee : `process.env.TRON_NETWORK === 'mainnet' ? https://api.trongrid.io : https://nile.trongrid.io`. Endpoint A (recommande) : `GET {base}/v1/accounts/{USDT_RECEIVE_ADDRESS}/transactions/trc20?only_confirmed=true&contract_address={USDT_CONTRACT_ADDRESS}&limit=200`. Parser via schema.ts. Exporter `fetchTrc20TransfersForReceiver()`. Ajouter `getByHash` optionnel si la fixture montre que l'endpoint A ne filtre pas par hash (filtrer cote code sur transaction_id). Creer index.ts barrel trongrid (re-export schema/client/verify/address/types) + ajouter au barrel data-sources/src/index.ts. Cette tache n'est pas testable par un appel reseau (interdit en CI) -> la logique pure (URL construction, header) est verifiee par le typecheck + revue ; les tests de comportement vivent dans schema/verify sur fixtures.</action>
  <verify>
    <automated>grep -v '^//' packages/data-sources/src/trongrid/client.ts | grep -c "TRON-PRO-API-KEY"</automated>
  </verify>
  <done>client.ts construit l'URL only_confirmed+contract_address, cle en header TRON-PRO-API-KEY, base URL par TRON_NETWORK, parse via schema.ts ; barrels exportent trongrid ; pnpm typecheck vert.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: verify.ts — les 5 invariants conjoints (decision tree)</name>
  <files>packages/data-sources/src/trongrid/verify.ts, packages/data-sources/src/trongrid/verify.test.ts</files>
  <read_first>
    - packages/data-sources/src/trongrid/address.ts (sameAddress — Plan 01)
    - packages/data-sources/src/trongrid/schema.ts (type transfer — Task 1)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Verification Decision Tree l.527-538, Pitfalls 1/3/4)
  </read_first>
  <behavior>
    - Test exact: contrat==USDT(.env) AND to==receiver AND value===expected AND confirme -> { kind:'exact', amount_atomic }
    - Test wrong_token: contrat != USDT officiel (faux token homonyme, meme decimals/symbol) -> { kind:'rejected', reason:'wrong_token' }
    - Test wrong_recipient: to != receiver (apres normalisation hex<->base58) -> rejected wrong_recipient
    - Test not_confirmed: transfer marque non confirme / sous seuil -> rejected not_confirmed
    - Test over: value > expected (exact contrat+dest+confirme) -> { kind:'over' } (D-07, ambiguous)
    - Test under: value < expected -> { kind:'under' } (D-06, ambiguous)
    - Test: jamais d'identification par symbol/decimals seuls (un faux token nomme USDT avec decimals 6 est rejete car contrat != .env)
    - Test: comparaison montant en BigInt strict (===), jamais epsilon
  </behavior>
  <action>Creer verify.ts : `verifyTransfer(transfer, ctx)` evalue le ET des invariants dans l'ordre du decision tree (RESEARCH l.527-538). (1) contrat : `sameAddress(transfer.token_info.address, ctx.contract)` sinon rejected:'wrong_token' (JAMAIS symbol/decimals, Pitfall 1). (2) destinataire : `sameAddress(transfer.to, ctx.receiver)` sinon rejected:'wrong_recipient'. (3) confirmation : champ de la fixture reelle (`only_confirmed` filtre deja cote client ; verifier ici le marqueur de confirmation/seuil A7) sinon rejected:'not_confirmed'. (4) montant : `received = BigInt(transfer.value)` ; received===ctx.expected -> exact ; received>expected -> over ; received<expected -> under (Pitfall 4, BigInt exact). Le replay (5e invariant) n'est PAS ici — c'est le UNIQUE(tx_hash) DB (Plan 02) ; verify.ts ne traite QUE les 4 invariants on-chain + le sur/sous-montant. RED (verify.test.ts cas exact/wrong_token/wrong_recipient/not_confirmed/over/under) puis GREEN.</action>
  <verify>
    <automated>npx vitest run packages/data-sources/src/trongrid/verify.test.ts</automated>
  </verify>
  <done>verify.test.ts couvre exact/over/under/wrong_token/wrong_recipient/not_confirmed ; faux token homonyme rejete par contrat ; montant compare en BigInt strict ; aucune decision par symbol/decimals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TronGrid response -> verify | Donnee on-chain non fiable : token usurpe, montant faux, non confirme |
| value string -> montant | Conversion atomique : float = sous/sur-paiement faux |
| token_info.symbol/decimals -> identite token | Spoofing : faux token nomme USDT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-FAKETOKEN | Spoofing | verify.ts | mitigate | identite token = `sameAddress(token_info.address, USDT_CONTRACT .env)` UNIQUEMENT ; jamais symbol/decimals (Pitfall 1) ; test du faux homonyme |
| T-04-REORG | Tampering | client.ts/verify.ts | mitigate | only_confirmed=true cote client + verification marqueur confirmation/seuil cote verify (Pitfall 3, A7) |
| T-04-PREC-V | Tampering | verify.ts | mitigate | `BigInt(transfer.value)` direct, comparaison === stricte, zero float (Pitfall 4) |
| T-04-KEYLEAK | Info Disclosure | client.ts | mitigate | cle TronGrid en header serveur, lue de process.env ; client jamais importe cote bundle navigateur |
| T-04-AUTODECIDE | Tampering | verify.ts | mitigate | over/under -> 'ambiguous' (file superadmin), jamais activation/rejet auto (D-06/D-07) |
</threat_model>

<verification>
- npx vitest run packages/data-sources/src/trongrid/schema.test.ts vert (fixture reelle).
- npx vitest run packages/data-sources/src/trongrid/verify.test.ts vert (6 cas).
- grep TRON-PRO-API-KEY dans client.ts >= 1 (hors commentaires).
- pnpm typecheck vert.
</verification>

<success_criteria>
- Parseur Zod fige sur la forme reelle Nile.
- 5 invariants (4 on-chain + montant) golden-testes ; faux token rejete par contrat.
- over/under -> ambiguous ; montant en BigInt exact.
</success_criteria>

<output>
Creer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-04-SUMMARY.md` a la fin.
</output>
