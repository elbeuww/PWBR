---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/money/atomic.ts
  - packages/core/src/money/atomic.test.ts
  - packages/core/src/index.ts
  - packages/data-sources/src/trongrid/address.ts
  - packages/data-sources/src/trongrid/address.test.ts
  - packages/data-sources/src/trongrid/__fixtures__/nile-trc20-transfer.json
  - packages/data-sources/src/trongrid/__fixtures__/GOLDEN.md
autonomous: false
requirements: [PAY-01, PAY-02]
must_haves:
  truths:
    - "Le montant 9.02 USDT se convertit exactement en 9020000n (zéro float)"
    - "L'adresse TRON TR7NH... se normalise vers son hex 41... et un checksum corrompu lève une exception"
    - "Une vraie réponse TronGrid Nile (TX TRC-20 réelle) est figée en fixture + golden values avant tout parseur"
  artifacts:
    - path: "packages/core/src/money/atomic.ts"
      provides: "toAtomic/formatAtomic/USDT_DECIMALS BigInt zéro-float"
      contains: "toAtomic"
    - path: "packages/data-sources/src/trongrid/address.ts"
      provides: "base58check hex<->base58 + sameAddress"
      contains: "sameAddress"
    - path: "packages/data-sources/src/trongrid/__fixtures__/nile-trc20-transfer.json"
      provides: "forme réelle de réponse TronGrid figée (débloque Wave 2)"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/money/atomic.ts"
      via: "barrel export"
      pattern: "money/atomic"
---

<objective>
Poser les fondations déterministes pures du paiement on-chain : conversion monétaire atomique BigInt (zéro float), normalisation d'adresse TRON base58check (sans tronweb), et — CRITIQUE — figer la forme réelle de la réponse TronGrid Nile sur une vraie transaction via un checkpoint réseau. Toutes les hypothèses on-chain HAUT-risque (RESEARCH A1-A7) sont vérifiées ici AVANT que tout parseur ou code de vérification ne soit écrit (Plan 04).

Purpose: La correction financière dépend de deux briques déterministes golden-testées (montant exact, adresse exacte) et de fixtures réelles. Réseau bloqué pendant la recherche → ces valeurs sont ASSUMED tant que ce checkpoint n'a pas frappé TronGrid en direct.
Output: atomic.ts + address.ts (golden-tested), fixtures Nile réelles + golden values figées, barrel core étendu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md

<interfaces>
<!-- Contrats que les plans aval (04) consommeront. Extraits de RESEARCH §Pattern 2/3. -->

À créer dans packages/core/src/money/atomic.ts :
  export const USDT_DECIMALS = 6n
  export function toAtomic(decimal: string): bigint   // "9.02" -> 9020000n, parse regex strict, throw sinon
  export function formatAtomic(atomic: bigint): string // 9020000n -> "9.020000"

À créer dans packages/data-sources/src/trongrid/address.ts :
  export function base58ToHex(addr: string): string   // 'T...' -> '41...' lower, throw si checksum invalide
  export function hexToBase58(hex: string): string
  export function sameAddress(a: string, b: string): boolean  // normalise hex<->base58 puis compare

Analog crypto natif (marketaux/client.ts l.15, 52-54) :
  import { createHash } from 'crypto'
  createHash('sha256').update(b).digest()   // JAMAIS réimplémenter sha256
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Checkpoint réseau — figer fixtures TronGrid Nile + golden values base58</name>
  <read_first>
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (§Assumptions Log A1-A7, §Pattern 1/2, §Wave 0 Gaps)
    - packages/data-sources/src/fred/client.ts (forme client fetch+Zod)
  </read_first>
  <what-built>
    Cette tâche ne produit pas de code applicatif : elle fige la VÉRITÉ on-chain que tout le reste consomme. Le réseau était bloqué pendant la recherche → A1-A7 sont ASSUMED. Cette tâche les vérifie en direct sur le testnet Nile et écrit deux fichiers : (1) `__fixtures__/nile-trc20-transfer.json` = réponse JSON RÉELLE de TronGrid pour une vraie TX USDT-test Nile, (2) `__fixtures__/GOLDEN.md` = les golden values base58<->hex confirmées.
  </what-built>
  <how-to-verify>
    1. Provisionner (ops, hors-code) une clé TronGrid (tier gratuit) + l'endpoint Nile testnet.
    2. Frapper en direct : `GET https://nile.trongrid.io/v1/accounts/{RECEIVE_ADDR_NILE}/transactions/trc20?only_confirmed=true&contract_address={USDT_NILE}&limit=10` avec header `TRON-PRO-API-KEY: <clé>`, sur un compte ayant reçu une vraie TX USDT-test. Coller la réponse JSON BRUTE dans `nile-trc20-transfer.json`.
    3. Confirmer A1 (noms de champs exacts : `from`/`to`/`value`/`token_info.address`/`token_info.decimals`, base58 vs hex), A2 (`only_confirmed` + `contract_address` réellement supportés), A3 (header `TRON-PRO-API-KEY` correct), A5 (`decimals === 6`), A6 (adresse contrat USDT Nile, DIFFÉRENTE de la mainnet `TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t`), A7 (seuil de confirmations / finalité TRON).
    4. Golden values base58 (vérifier sur un convertisseur TRON ou un node de test) dans `GOLDEN.md` : `TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t` -> sa valeur hex `41...` EXACTE ; l'adresse de réception cold wallet -> son hex ; un cas négatif (checksum corrompu attendu = throw).
    5. Noter toute divergence vs le schéma ASSUMED de RESEARCH §Pattern 1 (casing, base58 vs hex retourné, champs imbriqués) — c'est ce qui pilote le Zod du Plan 04.
  </how-to-verify>
  <acceptance_criteria>
    - `__fixtures__/nile-trc20-transfer.json` existe et contient une réponse TronGrid RÉELLE (pas l'esquisse ASSUMED).
    - `__fixtures__/GOLDEN.md` liste : hex de TR7NH... (mainnet), hex de l'adresse de réception, format retourné par l'API (base58 ou hex), `decimals` confirmé, seuil de confirmations retenu, contrat USDT Nile.
    - Les divergences vs RESEARCH §Pattern 1 sont documentées (pilotent le Zod du Plan 04).
  </acceptance_criteria>
  <resume-signal>Tape "approved" avec les golden values + fixture collées, OU décris le blocage (clé/accès réseau/wallet manquant).</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: atomic.ts — montant atomique BigInt zéro-float (golden)</name>
  <files>packages/core/src/money/atomic.ts, packages/core/src/money/atomic.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (§Pattern 3 l.328-353, §Pitfall 4)
    - packages/core/src/index.ts (barrel à étendre — style export `.js`)
    - packages/data-sources/src/fred/schema.test.ts (style de test golden)
  </read_first>
  <behavior>
    - Test: toAtomic("9.02") === 9020000n (le cas qui n'est PAS représentable en float)
    - Test: toAtomic("9") === 9000000n ; toAtomic("3.01") === 3010000n
    - Test: toAtomic("0.000001") === 1n (précision max 6 décimales)
    - Test: toAtomic("9.0000001") throw (plus de 6 décimales rejeté)
    - Test: toAtomic("abc") / toAtomic("") / toAtomic("9.") throw (regex strict)
    - Test: formatAtomic(9020000n) === "9.020000" ; round-trip toAtomic(formatAtomic(x)) === x
    - Test: USDT_DECIMALS === 6n
  </behavior>
  <action>Créer `packages/core/src/money/atomic.ts` net-new (aucun analog — RESEARCH §No Analog). Exporter `USDT_DECIMALS = 6n`, `SCALE = 10n ** USDT_DECIMALS`, `toAtomic(decimal: string): bigint` (regex strict `/^(\d+)(?:\.(\d{1,6}))?$/`, jamais `Number()`/`parseFloat`/`* 1e6` — Pitfall 4), `formatAtomic(atomic: bigint): string` (division/modulo BigInt, padStart 6). Comparaison toujours `===` exacte, jamais d'epsilon. RED d'abord (atomic.test.ts), puis GREEN. Étendre le barrel `packages/core/src/index.ts` avec `export { USDT_DECIMALS, toAtomic, formatAtomic } from './money/atomic.js'` (extension `.js` comme les exports existants).</action>
  <verify>
    <automated>npx vitest run packages/core/src/money/atomic.test.ts</automated>
  </verify>
  <done>atomic.test.ts passe (incl. 9.02->9020000n), barrel core ré-exporte les 3 symboles, zéro `Number`/`parseFloat`/`* 1e6` dans atomic.ts.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: address.ts — base58check hex<->base58 + sameAddress (golden)</name>
  <files>packages/data-sources/src/trongrid/address.ts, packages/data-sources/src/trongrid/address.test.ts</files>
  <read_first>
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (§Pattern 2 l.297-326, §Stack "NE PAS installer tronweb")
    - packages/data-sources/src/marketaux/client.ts (l.15, 52-54 : createHash natif — SEUL analog)
    - packages/data-sources/src/trongrid/__fixtures__/GOLDEN.md (golden values figées Task 1)
  </read_first>
  <behavior>
    - Test: base58ToHex("TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t") === <hex golden de GOLDEN.md> (commence par "41", 42 chars hex)
    - Test: hexToBase58(<hex golden>) === "TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t" (round-trip)
    - Test: sameAddress("TR7NH...", "41<hex>...") === true (compare hex<->base58 normalisé)
    - Test: sameAddress(adresse_réception_base58, adresse_réception_hex) === true (depuis GOLDEN.md)
    - Test: sameAddress("TR7NH...", autre_adresse) === false
    - Test: base58ToHex(<adresse à checksum corrompu>) throw (jamais comparer une adresse non vérifiée)
  </behavior>
  <action>Créer `address.ts` ~40 lignes déterministes. `import { createHash } from 'crypto'` (JAMAIS réimplémenter sha256 — Don't Hand-Roll). Alphabet base58 `123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz`. `base58ToHex`: décode base58 -> bigint -> bytes, sépare payload(21 octets, préfixe 0x41) + checksum(4 octets), recalcule `sha256(sha256(payload))[0..4]`, throw si mismatch, retourne hex lowercase. `hexToBase58`: inverse + ajout checksum. `sameAddress(a,b)`: normalise chacun via `s.startsWith('T') ? base58ToHex(s) : s.replace(/^0x/,'')` puis `.toLowerCase()` et compare. Utiliser EXACTEMENT les golden values de GOLDEN.md (figées Task 1) dans le test — ne jamais inventer un hex. RED puis GREEN.</action>
  <verify>
    <automated>npx vitest run packages/data-sources/src/trongrid/address.test.ts</automated>
  </verify>
  <done>address.test.ts passe contre les golden values RÉELLES de GOLDEN.md ; checksum corrompu -> throw ; aucune dépendance tronweb ajoutée ; sha256 via crypto natif uniquement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TronGrid API -> backend | Forme de réponse on-chain non vérifiée (réseau bloqué en recherche) — figée ici sur fixture réelle |
| string montant -> logique financière | Conversion décimale->atomique : précision flottante = perte/crédit faux |
| string adresse -> comparaison destinataire | Normalisation hex<->base58 : faux négatif (rejette vrai paiement) / faux positif |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-PREC | Tampering | atomic.ts | mitigate | BigInt de bout en bout, regex strict, zéro `Number`/`parseFloat`/`* 1e6` ; golden test 9.02->9020000n |
| T-04-ADDR | Spoofing | address.ts | mitigate | base58check avec vérification de checksum (throw si corrompu) ; golden values figées sur valeurs réelles |
| T-04-CRYPTO | Tampering | address.ts | mitigate | sha256 via `crypto` natif Node — jamais réimplémenté (V6 ASVS) |
| T-04-ASSUMED | Tampering | fixtures TronGrid | mitigate | checkpoint réseau bloquant : aucun parseur écrit avant fixture réelle Nile (A1-A7) |
</threat_model>

<verification>
- `npx vitest run packages/core/src/money/atomic.test.ts` vert (incl. 9.02->9020000n).
- `npx vitest run packages/data-sources/src/trongrid/address.test.ts` vert contre golden values réelles.
- `__fixtures__/nile-trc20-transfer.json` + `__fixtures__/GOLDEN.md` présents et issus du réseau réel.
- `pnpm typecheck` : aucune nouvelle erreur tsc au-delà de la baseline P1 connue.
</verification>

<success_criteria>
- Fixtures TronGrid Nile RÉELLES figées (débloquent Plan 04).
- atomic.ts golden-testé zéro-float, exporté du barrel core.
- address.ts golden-testé contre valeurs réelles, checksum vérifié, sans tronweb.
</success_criteria>

<output>
Créer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-01-SUMMARY.md` à la fin.
</output>
