---
phase: 04-paiement-usdt-mvp-abonnement-jalon-encaissement
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/jobs/src/jobs/subscription-expiry.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/src/jobs/__tests__/subscription-expiry.test.ts
  - apps/web/src/app/(admin)/membres/page.tsx
  - apps/web/src/app/(admin)/membres/actions.ts
  - apps/web/src/app/(admin)/file/page.tsx
  - apps/web/src/app/(admin)/file/actions.ts
  - apps/web/src/components/member/ExpiryBanner.tsx
autonomous: true
requirements: [PAY-05, ADMIN-01, ADMIN-02]
must_haves:
  truths:
    - "Le job subscription-expiry passe active->expired quand current_period_end<=now() et est idempotent (re-run = 0 ligne)"
    - "Un membre voit un bandeau in-app a J-3/J-1 avant expiration (pas d'email) ; coupe nette a current_period_end"
    - "Le superadmin voit les membres (statut/email/plan/expiration/dernier paiement) et agit (activer/prolonger/revoquer/changer de plan) via service_role"
    - "Le superadmin traite la file des paiements ambigus (activer / rejeter avec motif / ajuster) ; non-superadmin -> 404"
    - "Decisions couvertes: D-06 (sous-paiement -> file validation superadmin), D-07 (sur-paiement -> activer periode normale, surplus ignore), D-08 (file superadmin: activer/rejeter+motif/ajuster ; service_role transitionne), D-09 (rappel pre-expiration = bandeau in-app J-3/J-1, aucune infra email), D-10 (coupe nette a current_period_end via RLS existante, pas de grace), D-11 (renouvellement = meme parcours paiement, pas d'auto-renew), D-13 (vue membres superadmin = lecture + actions manuelles service_role), D-14 (liste membres: filtre statut + recherche email + colonnes plan/expiry/dernier paiement), D-15 (emplacement = route group (admin) hors [locale], 404 non-superadmin)"
  artifacts:
    - path: "apps/jobs/src/jobs/subscription-expiry.ts"
      provides: "job idempotent active->expired + job_runs"
      contains: "expireDue"
    - path: "apps/web/src/app/(admin)/membres/page.tsx"
      provides: "table membres + filtres (ADMIN-01)"
      contains: "table"
    - path: "apps/web/src/app/(admin)/file/page.tsx"
      provides: "file de validation paiements ambigus (ADMIN-02)"
      contains: "ambiguous"
  key_links:
    - from: "apps/jobs/src/dispatch.ts"
      to: "subscription-expiry"
      via: "JOB_REGISTRY"
      pattern: "subscription-expiry"
    - from: "apps/web/src/app/(admin)/file/actions.ts"
      to: "activate_subscription_for_payment"
      via: "activateForPayment / transitionPayment (service_role)"
      pattern: "activateForPayment|transitionPayment"
---

<objective>
Faire vivre l'abonnement et donner au superadmin le controle : job idempotent `subscription-expiry` (coupe nette a current_period_end + job_runs), bandeau d'expiry in-app J-3/J-1 (pas d'email), vue membres admin (lecture + actions service_role) et file de validation des paiements ambigus (activer/rejeter/ajuster).

Purpose: PAY-05 (cycle de vie) + ADMIN-01/02 (back-office indispensable des le 1er encaissement). Toute ecriture passe par service_role ; l'admin vit dans (admin) HORS [locale], 404 pour non-superadmin (P1 D-09). La file rattrape les cas ambigus que la verif auto refuse de trancher.
Output: job+dispatch+test, ExpiryBanner, pages+actions admin membres & file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md
@.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md
@apps/jobs/src/jobs/calendar-ingest.ts
@apps/jobs/src/dispatch.ts
@apps/web/src/app/(admin)/layout.tsx
@apps/web/src/lib/auth/gate.ts

<interfaces>
<!-- Consommes (deja livres par Plan 02, service_role only) : -->
From packages/supabase (barrel):
  expireDue(client): Promise<{ expired: number }>     // active->expired WHERE current_period_end<=now()
  activateForPayment(client, { payment_id, user_id, plan, period })  // RPC atomique
  transitionPayment(client, id, status: 'rejected', { reject_reason })
  changePlan(client, { user_id, plan, period? })

Analog garde admin (admin/layout.tsx l.7-12) : await requireRole('superadmin') protege deja TOUT le groupe -> 404 non-superadmin.
Analog job (calendar-ingest.ts) : getServiceClient lazy + stats Json + runJob wrappe job_runs ; dispatch JOB_REGISTRY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job subscription-expiry (idempotent) + dispatch + test</name>
  <files>apps/jobs/src/jobs/subscription-expiry.ts, apps/jobs/src/dispatch.ts, apps/jobs/src/jobs/__tests__/subscription-expiry.test.ts</files>
  <read_first>
    - apps/jobs/src/jobs/calendar-ingest.ts (analog exact : getServiceClient lazy l.13-32, stats Json l.40-68)
    - apps/jobs/src/dispatch.ts (JOB_REGISTRY l.23, 34-46)
    - packages/supabase/src/repositories/subscriptions.ts (expireDue de Plan 02)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-RESEARCH.md (Pattern 5, PAY-05 test map)
  </read_first>
  <action>Creer subscription-expiry.ts calque sur calendar-ingest : `getServiceClient()` lazy local (process.env SUPABASE_URL + SERVICE_ROLE_KEY, throw si absent), `subscriptionExpiry(): Promise<Json>` qui appelle `expireDue(client)` (UPDATE active->expired WHERE current_period_end<=now() — naturellement idempotent, re-run = 0 ligne, Pattern 5) et retourne `{ expired: n } as Json`. Enregistrer dans dispatch.ts : import + entree `'subscription-expiry': subscriptionExpiry` dans JOB_REGISTRY (Windows Task Scheduler appellera run-job.cmd subscription-expiry — generique deja en place). Test subscription-expiry.test.ts : verifier l'idempotence (mock du client service_role : 1er run expire N lignes, 2e run = 0 ligne car deja expirees). Coupe nette a current_period_end (D-10) = la RLS has_active_subscription() (P1, inchangee) fait deja current_period_end>now() -> rien a coder cote gating.</action>
  <verify>
    <automated>npx vitest run apps/jobs/src/jobs/__tests__/subscription-expiry.test.ts</automated>
  </verify>
  <done>subscriptionExpiry idempotent (test 2e run = 0 ligne), enregistre dans JOB_REGISTRY, stats Json {expired} ; pnpm typecheck vert.</done>
</task>

<task type="auto">
  <name>Task 2: Admin membres (table + filtres + actions service_role)</name>
  <files>apps/web/src/app/(admin)/membres/page.tsx, apps/web/src/app/(admin)/membres/actions.ts</files>
  <read_first>
    - apps/web/src/app/(admin)/layout.tsx (requireRole('superadmin') protege deja le groupe l.7-12)
    - apps/web/src/lib/auth/gate.ts (requireRole -> notFound 404 l.99-118)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (AdminMembers anatomy, Copywriting admin columns/actions, color destructive red)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-PATTERNS.md (admin pages)
  </read_first>
  <action>Creer membres/page.tsx (RSC, (admin) HORS [locale], mono-FR ; D-15 : emplacement route group (admin) hors [locale] ; le layout applique deja requireRole superadmin -> 404 non-superadmin, D-15). Lecture via anon-client + RLS is_superadmin() (lecture). Table (bloc shadcn table de Plan 03) : colonnes email, statut (badge Actif/Inactif/Expire), plan (discovery/standard), date d'expiration (Intl `<bdi>`), dernier paiement (D-14). Filtres : select statut + recherche email (D-14, local ou URL au choix). Row actions (dropdown-menu) : "Activer / Prolonger", "Changer de plan", destructive "Revoquer / Suspendre" (rouge, alert-dialog de confirmation D-13). Creer membres/actions.ts ('use server' + 'server-only') : service_role LOCAL (jamais le barrel), `activateForPayment`/`changePlan` (Plan 02), revoke = update subscriptions status='canceled'/'expired' via service_role. Aucune ecriture front sur subscriptions (D-13). Textes via next-intl namespace admin (FR). Destructive = alert-dialog obligatoire avant execution. Tables = wrapper scroll horizontal sur mobile.</action>
  <verify>
    <automated>grep -rn "requireRole\|is_superadmin\|isSuperadmin" "apps/web/src/app/(admin)/membres/page.tsx" "apps/web/src/app/(admin)/layout.tsx"</automated>
  </verify>
  <done>membres : table colonnes D-14 + filtres + actions service_role (activer/prolonger/changer/revoquer avec alert-dialog) ; lecture is_superadmin, 404 non-superadmin (via layout) ; pas d'ecriture front subscriptions ; lint:i18n exit 0 ; typecheck vert.</done>
</task>

<task type="auto">
  <name>Task 3: File de validation paiements ambigus + ExpiryBanner</name>
  <files>apps/web/src/app/(admin)/file/page.tsx, apps/web/src/app/(admin)/file/actions.ts, apps/web/src/components/member/ExpiryBanner.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/membres/page.tsx (Task 2 — meme pattern admin)
    - .planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-UI-SPEC.md (AdminQueue anatomy, ExpiryBanner anatomy, Copywriting queue/expiry, amber state)
    - packages/supabase/src/repositories (activateForPayment, transitionPayment, changePlan)
  </read_first>
  <action>Creer file/page.tsx (RSC (admin) hors [locale], D-15 ; tab "File de validation") : table des payments status='ambiguous' (les sous-paiements D-06 et sur-paiements D-07 atterrissent ici ; lecture is_superadmin) : colonnes Montant attendu / Montant recu (les deux `<bdi>` via formatAtomic+Intl), Hash (lien TronScan target=_blank rel="noopener noreferrer"), Membre, Capture (lien optionnel), badge "A valider" amber. Actions par ligne (D-08) : "Activer le paiement" (primary, -> activateForPayment service_role ; D-07 = activer la periode normale, le surplus est ignore), "Rejeter" (destructive rouge -> alert-dialog avec textarea motif REQUIS -> transitionPayment 'rejected' + reject_reason, D-08), "Ajuster duree/plan" (dialog -> changePlan, D-08). Empty state rassurant (la file est normalement vide). Creer file/actions.ts ('use server'+'server-only', service_role LOCAL ; user ecrit payments(pending), service_role transitionne, D-08). Creer ExpiryBanner.tsx (composant member, alert amber) : affiche a J-3/J-1 (calcul luxon depuis current_period_end), ICU plural "expire dans {n} jours", CTA "Renouveler" -> /[locale]/(member)/abonnement (meme parcours paiement, pas d'auto-renew, D-11). In-app uniquement, AUCUN email (D-09). prefers-reduced-motion + RTL. Coupe nette a current_period_end (D-10, pas de grace). Tous textes via next-intl (admin FR pour la file ; payment pour le banner).</action>
  <verify>
    <automated>grep -rn "noopener noreferrer" "apps/web/src/app/(admin)/file/page.tsx"</automated>
  </verify>
  <done>file : table ambigus (attendu/recu <bdi>, TronScan rel=noopener, badge amber), actions activer/rejeter+motif/ajuster service_role avec alert-dialog ; ExpiryBanner J-3/J-1 in-app, ICU plural, CTA renouveler, pas d'email ; lint:i18n exit 0 ; typecheck vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-superadmin -> (admin) | Doit etre 404 (discretion), jamais 403/200 silencieux |
| admin action -> subscriptions/payments | Toute mutation via service_role, jamais ecriture front |
| job -> subscriptions | service_role hors requete user, idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ADMIN-ELEV | Elevation | (admin) | mitigate | requireRole('superadmin') au layout -> notFound 404 ; lecture RLS is_superadmin() (P1) |
| T-04-ADMIN-WRITE | Elevation | admin actions | mitigate | mutations via service_role LOCAL ('use server'/'server-only') ; aucune ecriture front subscriptions/payments (D-13) |
| T-04-DESTRUCT | Tampering | revoke/reject | mitigate | alert-dialog de confirmation obligatoire ; motif requis pour rejeter (D-08) |
| T-04-EXTLINK | Tampering | TronScan link | mitigate | rel="noopener noreferrer" target=_blank (anti reverse-tabnabbing) |
| T-04-EXPIRE | Tampering | job | mitigate | UPDATE idempotent (WHERE active AND period_end<=now()) + job_runs ; coupe nette via RLS existante |
| T-04-NOEMAIL | Info Disclosure | ExpiryBanner | accept | rappel in-app uniquement (D-09) ; aucune infra email au MVP (pas de fuite par canal email) |
</threat_model>

<verification>
- npx vitest run subscription-expiry.test.ts vert (idempotence).
- grep requireRole/is_superadmin dans membres + layout.
- grep noopener noreferrer dans file/page.tsx.
- lint:i18n exit 0 ; pnpm typecheck vert.
- Manual (human-verify phase) : superadmin voit membres+file ; non-superadmin -> 404 ; expiry banner a J-3 testnet.
</verification>

<success_criteria>
- Job subscription-expiry idempotent enregistre + teste.
- Admin membres (table/filtres/actions service_role) + file ambigus (activer/rejeter/ajuster).
- ExpiryBanner in-app J-3/J-1, coupe nette, sans email.
</success_criteria>

<output>
Creer `.planning/phases/04-paiement-usdt-mvp-abonnement-jalon-encaissement/04-06-SUMMARY.md` a la fin.
</output>
