---
phase: 03-espace-membre-signaux-gated-rls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_realtime_trade_setups.sql
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/src/components/ui/tooltip.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/components/ui/select.tsx
  - apps/web/src/components/ui/collapsible.tsx
  - apps/web/src/components/providers/QueryProvider.tsx
  - apps/web/src/lib/signals/searchParams.ts
  - apps/web/src/lib/signals/__tests__/searchParams.test.ts
  - apps/web/src/lib/signals/format.ts
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: false
requirements: [MEMB-01, MEMB-02, MEMB-05]
must_haves:
  truths:
    - "La table trade_setups est membre de la publication supabase_realtime et a replica identity full (les events postgres_changes INSERT/UPDATE sont émis et filtrables par RLS)."
    - "lightweight-charts 5.2.0 et @tanstack/react-query 5.101.0 sont au lockfile web ; tooltip/skeleton/select/collapsible existent sous components/ui."
    - "searchParams.ts parse/sérialise filtres+tri en round-trip, défaut sort=score, et rejette les valeurs hors enum (anti-injection .eq/.in)."
    - "Les 3 fichiers de messages (fr/en/ar) ont la parité de clés stricte pour les namespaces signals/signalDetail/glossary."
  artifacts:
    - path: "supabase/migrations/0011_realtime_trade_setups.sql"
      provides: "Publication realtime + replica identity full sur trade_setups + décision RLS candles"
      contains: "alter publication supabase_realtime add table"
    - path: "apps/web/src/lib/signals/searchParams.ts"
      provides: "Zod schema parse/serialize des filtres+tri persistés en URL"
      exports: ["SignalsParamsSchema", "parseSignalsParams", "serializeSignalsParams"]
    - path: "apps/web/src/lib/signals/format.ts"
      provides: "Formatage prix par actif (precision) + âge relatif via Intl, encadré <bdi>"
      exports: ["formatPrice", "formatRelativeAge"]
    - path: "apps/web/src/components/providers/QueryProvider.tsx"
      provides: "QueryClientProvider react-query monté au-dessus de la surface signaux"
      exports: ["QueryProvider"]
  key_links:
    - from: "apps/web/src/lib/signals/searchParams.ts"
      to: "trade_setups columns (style, risk_level, instrument_id)"
      via: "enums miroirs 0006/0001"
      pattern: "z\\.enum"
    - from: "supabase/migrations/0011_realtime_trade_setups.sql"
      to: "publication supabase_realtime"
      via: "alter publication add table"
      pattern: "supabase_realtime"
---

<objective>
Poser le socle DB + plumbing de la phase signaux : (1) la migration 0011 qui active le temps réel sur `trade_setups` (publication + replica identity) et tranche la cohérence RLS de `candles`, (2) l'installation des libs verrouillées (lightweight-charts, react-query) et des composants shadcn manquants, (3) le provider react-query, et (4) les utilitaires partagés purs (parsing searchParams Zod, formatage prix/âge) avec leurs clés i18n. Aucun rendu de surface ici : ce plan débloque les plans 02 (liste) et 03 (détail) qui tournent en parallèle ensuite.

Purpose: Sans la migration 0011, MEMB-05 (temps réel) ne peut PAS passer la vérification (les events ne sont jamais émis — Pitfall 2). Sans les libs/composants, les plans de surface n'ont rien à composer. Les utilitaires purs (searchParams, format) sont testables en isolation (Nyquist) et consommés à la fois par la liste et le détail.
Output: migration 0011 poussée live, lockfile web à jour, 4 primitives shadcn, QueryProvider, searchParams + format + test unit, namespaces i18n signals/signalDetail/glossary à parité.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-CONTEXT.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-VALIDATION.md

<interfaces>
<!-- Colonnes RÉELLES (corrigées par PATTERNS vs RESEARCH). L'exécuteur les utilise telles quelles. -->

trade_setups (migration 0006 — colonnes filtrables/triables) :
  id, instrument_id, direction ('long'|'short'), opportunity_score (int 0-100),
  risk_level ('low'|'medium'|'high'|'extreme'), risk_reward (numeric), style ('day'|'swing'),
  session, status ('active'|'expired'|'invalidated'), valid_until, created_at,
  entry_price, stop_loss, take_profits, confidence, payload (Output §3 JSON)

instruments (migration 0001 — colonnes RÉELLES, PAS canonical_symbol/price_decimals) :
  symbol, broker, asset_class ('crypto'|'forex'|'metal'|'energy'), display_name,
  pip_size, min_size, precision (int — décimales prix), active

Join correct : instruments!inner(symbol, asset_class, precision, display_name)

payload (= packages/core/src/schemas/output.ts Output §3, verbatim) :
  veteran_note, technical_reasons[], fundamental_reasons[], news_catalysts[],
  upcoming_risk_events[], invalidation, entry, stop_loss, take_profits[],
  timeframe_analysis ('H1'|'H4'|'D'...), direction, session, style
  ⛔ opportunity_score / risk_reward / risk_level NE SONT PAS dans payload (colonnes trade_setups)
</interfaces>

<existing_message_shape>
fr.json a déjà : signals.title, signals.body, dashboard.* (à conserver, ne pas casser).
Étendre signals + ajouter signalDetail + glossary. Copy FR canonique = 03-UI-SPEC §Copywriting Contract.
</existing_message_shape>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vérifier légitimité packages + installer libs et composants shadcn</name>
  <read_first>
    - apps/web/package.json (deps actuelles : pas de lightweight-charts/react-query)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md §Package Legitimacy Audit + §Installation
    - apps/web/src/components/ui/dialog.tsx (convention import `radix-ui` unifié, data-slot, cn depuis @/lib/utils)
    - apps/web/src/components/ui/dropdown-menu.tsx (analog du select)
  </read_first>
  <what-built>
    Aucune install encore exécutée. Les deux libs npm sont verrouillées dans CLAUDE.md et confirmées via npm registry dans l'audit RESEARCH (disposition Approuvé), mais l'audit a été produit sans slopcheck → checkpoint de prudence avant install (threat T-03-SC).
  </what-built>
  <how-to-verify>
    1. Confirmer sur npmjs.com que `lightweight-charts@5.2.0` (publisher TradingView) et `@tanstack/react-query@5.101.0` (publisher TanStack) sont légitimes (downloads élevés, repo officiel).
    2. Confirmer que recharts N'est PAS à installer (repli D-11 = liste « Facteurs contributifs », pas de barres par dimension — décision RESEARCH verrouillée).
    3. Approuver pour lancer : `pnpm --filter @app/web add lightweight-charts@5.2.0 @tanstack/react-query@5.101.0` puis `pnpm --filter @app/web dlx shadcn@latest add tooltip skeleton select collapsible`.
    4. Après install : vérifier que tooltip/skeleton/select/collapsible.tsx utilisent l'import `radix-ui` unifié (PAS `@radix-ui/react-*`) et `cn` depuis `@/lib/utils` (corriger sinon, miroir dialog.tsx).
  </how-to-verify>
  <action>
    Lancer après approbation : `pnpm --filter @app/web add lightweight-charts@5.2.0 @tanstack/react-query@5.101.0`. Puis ajouter les 4 composants shadcn officiels via `shadcn add tooltip skeleton select collapsible` depuis apps/web. NE PAS installer recharts. Aligner les imports des 4 nouveaux fichiers ui/ sur la convention du repo (`import { X as XPrimitive } from "radix-ui"`, `data-slot`, `cn` depuis `@/lib/utils`) en miroir de `ui/dialog.tsx`/`ui/dropdown-menu.tsx`. Externaliser tout label en dur généré par shadcn en prop (règle D-02-01-F, sinon `lint:i18n` casse).
  </action>
  <acceptance_criteria>
    - `grep -c "lightweight-charts" apps/web/package.json` >= 1 ET `grep -c "@tanstack/react-query" apps/web/package.json` >= 1
    - `grep -c "recharts" apps/web/package.json` == 0
    - Les fichiers `apps/web/src/components/ui/{tooltip,skeleton,select,collapsible}.tsx` existent
    - Dans ces 4 fichiers : `grep -L "@radix-ui/react-" <files>` ne signale aucun import séparé `@radix-ui/react-*` (la convention est l'import `radix-ui` unifié) pour tooltip/select/collapsible (skeleton n'a pas de primitive radix → exempt)
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/web" && node -e "const p=require('./package.json');const d={...p.dependencies};if(!d['lightweight-charts'])throw new Error('lightweight-charts manquant');if(!d['@tanstack/react-query'])throw new Error('react-query manquant');if(d['recharts'])throw new Error('recharts NE doit PAS etre installe (repli D-11)');console.log('deps OK')"</automated>
  </verify>
  <resume-signal>Tapez "approved" pour lancer les installs, ou décrivez un blocage.</resume-signal>
  <done>Les 2 libs sont au lockfile, recharts absent, les 4 primitives shadcn existent et suivent la convention radix-ui unifiée.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 [BLOCKING]: Écrire la migration 0011 (realtime + replica identity + RLS candles) et la pousser live</name>
  <read_first>
    - supabase/migrations/0009_subscriptions_gating.sql (style entête commenté + drop/recreate par NOM EXACT — analog)
    - supabase/migrations/0006_analyses_trade_setups.sql (schéma trade_setups + RLS lecture)
    - supabase/migrations/0003_data_ingestion_tables.sql l.89-93 (RLS candles actuelle `using(true)` authenticated)
    - supabase/migrations/0010_has_active_subscription_null_expiry.sql (helper has_active_subscription)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md §migration 0011 (contenu + idempotence A4)
  </read_first>
  <action>
    Créer `supabase/migrations/0011_realtime_trade_setups.sql` avec un entête commenté (rationale MEMB-05, références A1/A4, invariant producteur-unique) en miroir de 0009. Contenu :
    (a) `alter table public.trade_setups replica identity full;` — porte l'ancien enregistrement sur les UPDATE (D-14, transition active→expired, A1).
    (b) Ajout idempotent de `public.trade_setups` à la publication `supabase_realtime` : envelopper dans un bloc `do $$ begin ... exception when duplicate_object then null; end $$;` OU vérifier `pg_publication_tables` avant l'`alter publication ... add table public.trade_setups;` (A4 : la table peut déjà y être → ne pas faire échouer la migration).
    (c) DÉCISION RLS candles (Open Question 1, à trancher AVEC le développeur dans ce checkpoint) : par cohérence de la barrière payante (recommandation RESEARCH), `drop policy` la policy candles authenticated existante par son NOM EXACT (lire 0003 pour le nom réel) et `create policy` candles `for select to authenticated using (public.has_active_subscription())`. Si le développeur préfère garder l'OHLCV ouvert, NE PAS toucher candles et documenter le choix dans l'entête commenté de la migration + dans le SUMMARY.
    Pousser ensuite la migration LIVE via le canal projet établi : MCP Supabase `apply_migration` (convention 0006/0009 — PAS `supabase db push`, pas de `supabase link` local). Vérifier d'abord l'état via SQL MCP `select * from pg_publication_tables where pubname='supabase_realtime';` pour rendre l'ajout idempotent.
  </action>
  <acceptance_criteria>
    - Le fichier `supabase/migrations/0011_realtime_trade_setups.sql` existe et contient `replica identity full` ET `supabase_realtime` ET `public.trade_setups`.
    - Post-push, la requête MCP `select tablename from pg_publication_tables where pubname='supabase_realtime' and tablename='trade_setups';` retourne exactement 1 ligne.
    - Post-push, `select relreplident from pg_class where relname='trade_setups';` retourne `f` (replica identity full).
    - Si la décision candles = align : `select polname, pg_get_expr(polqual,polrelid) from pg_policy where polrelid='public.candles'::regclass and polcmd='r';` montre `has_active_subscription()`. Si décision = garder ouvert : l'entête de 0011 documente explicitement le choix (`grep -ci "candles" supabase/migrations/0011_realtime_trade_setups.sql` >= 1).
  </acceptance_criteria>
  <verify>
    <automated>cd "." && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/0011_realtime_trade_setups.sql','utf8');for(const t of ['replica identity full','supabase_realtime','public.trade_setups']){if(!s.includes(t))throw new Error('manque: '+t)}console.log('migration 0011 OK (push verifie via MCP pg_publication_tables manuellement)')"</automated>
  </verify>
  <resume-signal>Confirmez la décision RLS candles (align / garder ouvert) ET que `apply_migration` a réussi (pg_publication_tables montre trade_setups). Tapez "pushed" + décision.</resume-signal>
  <done>0011 écrite, décision candles tranchée et documentée, migration appliquée live, pg_publication_tables confirme trade_setups + replica identity full.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: searchParams Zod + format prix/âge + QueryProvider + i18n (3 langues)</name>
  <read_first>
    - packages/core/src/schemas/output.ts (style Zod v4 du repo : z.enum, z.infer — analog searchParams)
    - apps/web/src/components/ThemeProvider.tsx (analog provider client 'use client')
    - apps/web/src/lib/__tests__/legal-gate.test.ts (structure test vitest du repo — analog)
    - apps/web/src/messages/fr.json (shape namespaces existants signals/dashboard — ne pas casser)
    - apps/web/src/messages/en.json + ar.json (parité de clés stricte exigée)
    - apps/web/src/components/Disclaimer.tsx (analog composant i18n simple, classes logiques ps/pe)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md §Copywriting Contract (copy FR canonique)
  </read_first>
  <behavior>
    - searchParams round-trip : serializeSignalsParams(parseSignalsParams({style:'swing',risk:'low',sort:'rr'})) reproduit les mêmes valeurs.
    - défaut : parseSignalsParams({}) → sort === 'score' (D-08), aucun filtre.
    - rejet hors-enum : parseSignalsParams({style:'scalp'}) → style ignoré/undefined (pas d'injection dans .eq). parseSignalsParams({risk:'DROP TABLE'}) → risk undefined.
    - class hors enum (crypto|forex|metal|energy) ignorée.
    - formatPrice(123.456, 2) === '123.46' (Intl, decimals = instruments.precision).
    - formatRelativeAge(now-2h) produit une chaîne non vide (Intl.RelativeTimeFormat).
  </behavior>
  <action>
    Créer `apps/web/src/lib/signals/searchParams.ts` : `SignalsParamsSchema` Zod v4 avec `style: z.enum(['day','swing']).optional()`, `risk: z.enum(['low','medium','high','extreme']).optional()`, `class: z.enum(['crypto','forex','metal','energy']).optional()`, `asset: z.string().optional()`, `sort: z.enum(['score','recent','rr']).default('score')`. Exporter `parseSignalsParams(raw: Record<string,string|string[]|undefined>)` (utilise `.safeParse`, ignore les champs invalides sans throw) et `serializeSignalsParams(params): URLSearchParams`. Exporter `type SignalsParams = z.infer<...>`.
    Créer `apps/web/src/lib/signals/format.ts` : `formatPrice(value: number, precision: number, locale?: string): string` via `Intl.NumberFormat` (minimum/maximumFractionDigits = precision) et `formatRelativeAge(date: Date|string, locale?: string): string` via `Intl.RelativeTimeFormat`. Fonctions pures (l'encadrement `<bdi>` est fait par les composants, pas ici).
    Créer `apps/web/src/components/providers/QueryProvider.tsx` ('use client') : exporte `QueryProvider` qui mémorise un `QueryClient` (useState) et wrappe `{children}` dans `QueryClientProvider`. Miroir structurel de ThemeProvider.
    Créer `apps/web/src/lib/signals/__tests__/searchParams.test.ts` (vitest) couvrant les comportements ci-dessus (round-trip, défaut score, rejet hors-enum).
    Étendre `apps/web/src/messages/fr.json` namespace `signals` et ajouter `signalDetail` + `glossary` avec la copy FR canonique de l'UI-SPEC : sortLabels (scoreDesc/recent/rr), filterLabels (asset/class/style/risk + chips day/swing/low/medium/high + reset), scoreBands (forte/modérée/prudente), emptyHeading/emptyBody, error (heading/body/retry), realtimeBadge (avec pluriel : `{count} nouveaux signaux — afficher` / singulier), realtimeLost, notFound + backLink, disclaimerBanner, detail section titles (explicationSimple/analyseApprofondie/scenarioInvalidation/evenementsRisque/decompositionScore→fallback facteursContributifs), cardAction (« Voir le détail »). Ajouter `glossary` avec au moins RSI/MACD/BOS/ATR (définitions vulgarisées FR). Répliquer la MÊME arborescence de clés dans en.json (EN) et ar.json (AR) — parité stricte (CI check-i18n bloque les chaînes en dur ; le parser de parité casse si une clé manque).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/src/lib/signals/__tests__/searchParams.test.ts` passe (round-trip + défaut score + rejet hors-enum).
    - `parseSignalsParams({})` retourne `sort:'score'` et aucun filtre défini.
    - `node -e` chargeant les 3 JSON prouve la parité : l'ensemble des chemins de clés de `signals`+`signalDetail`+`glossary` est identique entre fr/en/ar.
    - `apps/web/src/components/providers/QueryProvider.tsx` exporte `QueryProvider` et contient `QueryClientProvider`.
    - searchParams.ts ne contient aucune concaténation de string brute injectée dans une requête (seulement des enums Zod) — `grep -c "z.enum" apps/web/src/lib/signals/searchParams.ts` >= 3.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && pnpm vitest run apps/web/src/lib/signals/__tests__/searchParams.test.ts && node -e "const f=require('./apps/web/src/messages/fr.json'),e=require('./apps/web/src/messages/en.json'),a=require('./apps/web/src/messages/ar.json');const keys=o=>{const out=[];const w=(x,p)=>{for(const k in x){const np=p?p+'.'+k:k;typeof x[k]==='object'&&x[k]?w(x[k],np):out.push(np)}};for(const ns of ['signals','signalDetail','glossary'])o[ns]&&w(o[ns],ns);return out.sort().join('|')};if(keys(f)!==keys(e))throw new Error('parite fr/en cassee');if(keys(f)!==keys(a))throw new Error('parite fr/ar cassee');console.log('i18n parite OK')"</automated>
  </verify>
  <done>searchParams + format testés verts, QueryProvider exporté, namespaces signals/signalDetail/glossary à parité stricte fr/en/ar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → DB (Realtime WS) | events postgres_changes ; la RLS doit filtrer pour les non-abonnés |
| URL searchParams → requête Supabase | entrée utilisateur traduite en `.eq/.in` (injection possible) |
| npm registry → lockfile | packages tiers introduits dans le build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | publication realtime sur trade_setups | mitigate | RLS `has_active_subscription()` (0009/0010) héritée par les events postgres_changes ; replica identity full ne contourne pas la RLS (les events restent filtrés par policy). |
| T-03-02 | Information Disclosure | RLS candles `using(true)` | mitigate/accept | Décision tranchée en Task 2 : aligner sur `has_active_subscription()` (recommandé) OU documenter que l'OHLCV brut n'est pas le produit payant. |
| T-03-05 | Tampering | searchParams → `.eq/.in` | mitigate | Zod `z.enum` whitelist sur style/risk/class ; `asset` validé/normalisé avant injection (Task 3). Aucune string brute concaténée. |
| T-03-SC | Tampering | npm installs (lightweight-charts, react-query) | mitigate | Checkpoint blocking-human (Task 1) ; vérif npmjs.com avant install ; recharts explicitement exclu. |
</threat_model>

<verification>
- Migration 0011 appliquée live ; `pg_publication_tables` contient `trade_setups` ; `relreplident='f'`.
- Décision RLS candles tranchée et tracée (align ou documenté).
- lightweight-charts 5.2.0 + react-query 5.101.0 au lockfile ; recharts absent ; 4 primitives shadcn présentes.
- `pnpm vitest run apps/web/src/lib/signals/__tests__/searchParams.test.ts` vert.
- Parité i18n stricte fr/en/ar sur signals/signalDetail/glossary.
- `pnpm --filter @app/web exec tsc -b --noEmit` ne révèle aucune NOUVELLE erreur (baseline P1 = fixture `forbidden-service-import.ts` seule).
</verification>

<success_criteria>
Le socle DB + plumbing permet aux plans 02 (liste) et 03 (détail) de composer sans réinventer : temps réel armé côté DB, libs/composants présents, utilitaires purs testés, i18n prête.
</success_criteria>

<output>
Créer `.planning/phases/03-espace-membre-signaux-gated-rls/03-01-SUMMARY.md` à la fin.
</output>
