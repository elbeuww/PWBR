---
phase: 03-espace-membre-signaux-gated-rls
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx
  - apps/web/src/components/signals/SignalDetail.tsx
  - apps/web/src/components/signals/CandleChart.tsx
  - apps/web/src/components/signals/ContributingFactors.tsx
  - apps/web/src/components/signals/GlossaryTooltip.tsx
  - apps/web/src/components/signals/__tests__/SignalDetail.test.tsx
autonomous: true
requirements: [MEMB-03, MEMB-04]
must_haves:
  truths:
    - "Un abonne ouvre le detail a /[locale]/(member)/signaux/[id] : graphique chandeliers (lightweight-charts v5) avec entree, SL et TP traces et legendes."
    - "Le detail montre d'abord une explication simple (veteran_note + plan resume) puis une analyse approfondie depliable (raisons + facteurs contributifs + invalidation + evenements a risque)."
    - "Le contenu IA (veteran_note, *_reasons, invalidation, news_catalysts) est rendu VERBATIM ; le glossaire est additif (tooltip)."
    - "Un signal expire entre liste et detail to notFound (anti-IDOR, aucune fuite)."
  artifacts:
    - path: "apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx"
      provides: "RSC detail : trade_setup by id status=active + candles, chart no-SSR"
      min_lines: 40
    - path: "apps/web/src/components/signals/CandleChart.tsx"
      provides: "Chart lightweight-charts v5 lecture seule + price lines entree/SL/TP"
    - path: "apps/web/src/components/signals/SignalDetail.tsx"
      provides: "Niveau 1 simple + niveau 2 approfondi depliable, contenu VERBATIM"
    - path: "apps/web/src/components/signals/ContributingFactors.tsx"
      provides: "Repli D-11 facteurs contributifs (PAS de recharts)"
  key_links:
    - from: "apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx"
      to: "trade_setups + candles (anon-client RLS)"
      via: "maybeSingle by id status=active + candles"
      pattern: "maybeSingle"
    - from: "apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx"
      to: "CandleChart"
      via: "next/dynamic ssr:false"
      pattern: "dynamic\\("
    - from: "apps/web/src/components/signals/CandleChart.tsx"
      to: "lightweight-charts v5"
      via: "addSeries(CandlestickSeries) + createPriceLine"
      pattern: "addSeries"
---

<objective>
Surface DETAIL d'un trade : route RSC [id] qui lit le trade_setup actif + ses candles (anon-client + RLS), monte le CandleChart (lightweight-charts v5, lecture seule, lignes entree/SL/TP) en client no-SSR, et rend SignalDetail (niveau 1 explication simple veteran_note+plan, niveau 2 analyse approfondie depliable) avec contenu IA VERBATIM, repli facteurs contributifs (D-11) et glossaire additif.

Purpose: MEMB-03 (chart entree/SL/TP) et MEMB-04 (explication simple puis approfondie verbatim). Le score n'etant PAS decomposable depuis la DB (preuve persist.ts), la decomposition tombe en repli facteurs contributifs (NON recharts).
Output: route detail, CandleChart, SignalDetail, ContributingFactors, GlossaryTooltip, test verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-CONTEXT.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-01-SUMMARY.md
@apps/web/src/app/[locale]/dashboard/page.tsx
@apps/web/src/lib/supabase/server.ts
@apps/web/src/lib/auth/gate.ts
@packages/core/src/schemas/output.ts

<interfaces>
From 03-01 : formatPrice/formatRelativeAge (format.ts) ; shadcn tooltip+collapsible ; lightweight-charts 5.2.0.
payload (Output §3) VERBATIM : veteran_note, technical_reasons[], fundamental_reasons[], news_catalysts[], upcoming_risk_events[], invalidation, entry, stop_loss, take_profits[] (.price), timeframe_analysis, direction.
Colonnes trade_setups (HORS payload) : opportunity_score, risk_reward, risk_level, status, valid_until.
candles (RLS authenticated 0003) : ts, open, high, low, close ; eq instrument_id + eq timeframe ; limit 150 (D-12).
instruments!inner(symbol, asset_class, precision, display_name) — precision = decimales prix.
</interfaces>

<laws>
D-10/MEMB-04 : contenu IA AS-IS, jamais reformule. Glossaire = tooltip additif.
D-11 : score NON persiste (persist.ts) to repli Facteurs contributifs. PAS de recharts, PAS de barres par dimension.
D-12 : chart lecture seule (handleScroll/handleScale false, crosshair mode 0). up=vert/down=rouge ; entree=brand-blue dashed ; SL=rouge ; TP=vert ; legendees. Canvas LTR.
D-03 : score NEUTRE (--primary).
Pitfall 5 : chart via next/dynamic ssr:false.
</laws>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route RSC detail [id] + CandleChart (no-SSR, lignes entree/SL/TP)</name>
  <read_first>
    - apps/web/src/app/[locale]/dashboard/page.tsx (analog lecture serveur)
    - apps/web/src/lib/auth/gate.ts (pattern notFound)
    - apps/web/src/lib/supabase/server.ts (createClient anon+cookies)
    - packages/core/src/schemas/output.ts (champs payload §3)
    - packages/supabase/src/repositories/candles.ts (pattern maybeSingle ; INTERDIT a l'import en front)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md (section signaux/[id]/page.tsx + CandleChart NO ANALOG)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md (Pattern 4 lightweight-charts v5)
  </read_first>
  <action>
    Creer apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx (RSC) : params Promise locale+id (Next 15 async). setRequestLocale, getTranslations('signalDetail'). createClient() serveur. SELECT trade_setups '*, instruments!inner(symbol, asset_class, precision, display_name)' eq id, eq status active, maybeSingle(). Si pas de setup to notFound() (copy signalDetail.notFound + lien retour). mapTimeframe(setup.payload.timeframe_analysis) vers H1/H4/D. SELECT candles 'ts, open, high, low, close' eq instrument_id, eq timeframe, order ts asc, limit 150. Monter via const CandleChart = dynamic(import CandleChart, { ssr:false }) et passer candles + entry/stop_loss/take_profits + direction + precision. Rendre SignalDetail(setup) sous le chart. PAS de guard inline. AUCUN repo service_role.
    Creer apps/web/src/components/signals/CandleChart.tsx ('use client') : props candles[], entry, stopLoss, takeProfits[], direction, precision. createChart(el, { handleScroll:false, handleScale:false, crosshair:{mode:0} }) (lecture seule D-12). series = chart.addSeries(CandlestickSeries, upColor vert/downColor rouge) (API v5, PAS addCandlestickSeries). series.setData(candles map time/open/high/low/close). createPriceLine entree (brand-blue, LineStyle.Dashed, title signalDetail.lineEntry), SL (rouge, title lineSL), chaque TP (vert, title TP{i+1}). ResizeObserver + cleanup chart.remove() au unmount. Hauteur responsive 360/420. Erreur de montage to ne pas casser la page (plan resume reste lisible).
  </action>
  <acceptance_criteria>
    - [id]/page.tsx : contient maybeSingle, eq status active (anti-IDOR/expire), notFound, dynamic ssr:false, instruments!inner(symbol, asset_class, precision, display_name) ; grep repositories/ == 0 ; grep requireActiveSub == 0.
    - CandleChart.tsx : contient addSeries(CandlestickSeries (v5) ; ne contient PAS addCandlestickSeries (v4) ; contient createPriceLine ; contient handleScroll (lecture seule) ; contient chart.remove (cleanup).
    - tsc -b --noEmit : aucune nouvelle erreur vs baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && grep -q "maybeSingle" "apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx" && grep -q "addSeries(CandlestickSeries" apps/web/src/components/signals/CandleChart.tsx && ! grep -q "addCandlestickSeries" apps/web/src/components/signals/CandleChart.tsx && grep -q "createPriceLine" apps/web/src/components/signals/CandleChart.tsx && echo OK</automated>
  </verify>
  <done>Route detail lit le setup actif + candles via anon-client (anti-IDOR), chart v5 lecture seule monte no-SSR avec lignes entree/SL/TP legendees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SignalDetail (niveaux 1/2 verbatim) + ContributingFactors + GlossaryTooltip</name>
  <read_first>
    - apps/web/src/components/ui/collapsible.tsx (03-01, niveau 2 depliable)
    - apps/web/src/components/ui/tooltip.tsx (03-01, glossaire)
    - packages/core/src/schemas/output.ts (champs payload a rendre VERBATIM)
    - apps/web/src/lib/__tests__/legal-gate.test.ts (structure test vitest)
    - apps/web/src/lib/signals/format.ts (03-01)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md (SignalDetail anatomy + Copywriting + glossary)
  </read_first>
  <behavior>
    - SignalDetail rend setup.payload.veteran_note EXACTEMENT (string identique, aucune transformation).
    - Chaque entree de technical_reasons/fundamental_reasons/news_catalysts/invalidation apparait verbatim dans le DOM.
    - Le score affiche = setup.opportunity_score (colonne), couleur neutre, jamais derive du payload.
    - ContributingFactors derive sa liste des *_reasons (PAS de valeur numerique par dimension, PAS de recharts).
    - Niveau 2 collapsed par defaut.
  </behavior>
  <action>
    Creer apps/web/src/components/signals/SignalDetail.tsx : titre = symbol + direction (couleur semantique) + score NEUTRE (colonne opportunity_score). Niveau 1 Explication simple (signalDetail.explicationSimple) = veteran_note AS-IS (texte brut echappe par React, JAMAIS dangerouslySetInnerHTML) + plan resume (direction, entry/stop_loss/take_profits via formatPrice+precision en bdi, R:R, risk_level i18n). Niveau 2 Analyse approfondie (collapsible, collapsed par defaut) = technical_reasons / fundamental_reasons / news_catalysts (listes verbatim) + ContributingFactors + invalidation (signalDetail.scenarioInvalidation, verbatim) + upcoming_risk_events (signalDetail.evenementsRisque). Titres via i18n ; contenu IA PAS une cle i18n. Classes logiques. respect prefers-reduced-motion sur le fold.
    Creer apps/web/src/components/signals/ContributingFactors.tsx : recoit les *_reasons, rend une liste Facteurs contributifs (signalDetail.facteursContributifs) derivee de ces raisons. AUCUNE barre par dimension, AUCUN import recharts. Optionnel : une seule barre de progression NEUTRE du score global via div Tailwind.
    Creer apps/web/src/components/signals/GlossaryTooltip.tsx ('use client') : tooltip shadcn sur un affixe ? a cote d'un terme ; definition depuis le namespace glossary (RSI/MACD/BOS/ATR). Additif uniquement. Trigger >=44px, focusable, dismiss Esc/blur, tap-to-open mobile.
    Creer apps/web/src/components/signals/__tests__/SignalDetail.test.tsx : monte SignalDetail avec un payload fixture, asserte que veteran_note + une technical_reason + invalidation apparaissent verbatim, et que le score affiche == opportunity_score colonne (MEMB-04).
  </action>
  <acceptance_criteria>
    - SignalDetail : grep dangerouslySetInnerHTML == 0 (rendu echappe).
    - ContributingFactors : grep recharts == 0 (repli D-11).
    - pnpm vitest run apps/web/src/components/signals/__tests__/SignalDetail.test.tsx passe (verbatim + score == colonne).
    - pnpm --filter @app/web run lint:i18n exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && ! grep -q "dangerouslySetInnerHTML" apps/web/src/components/signals/SignalDetail.tsx && ! grep -q "recharts" apps/web/src/components/signals/ContributingFactors.tsx && pnpm vitest run apps/web/src/components/signals/__tests__/SignalDetail.test.tsx</automated>
  </verify>
  <done>SignalDetail rend niveaux 1/2 avec contenu IA verbatim (zero dangerouslySetInnerHTML), ContributingFactors en repli (zero recharts), glossaire additif, test verbatim vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur to DB (RSC) | lecture d'un signal par id ; RLS + status=active = barriere |
| payload IA to DOM | contenu genere affiche dans la page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-IDOR | Information Disclosure | /signaux/[id] | mitigate | maybeSingle + eq status=active + RLS has_active_subscription() ; un id non-actif/non-autorise to notFound (aucune fuite). |
| T-03-XSS | Tampering | rendu veteran_note/*_reasons | mitigate | rendu echappe par React (texte), JAMAIS dangerouslySetInnerHTML (D-10). |
| T-03-RLS | Information Disclosure | lecture trade_setups/candles | mitigate | anon-client + RLS ; aucun import repo service_role (lint fixture). candles : selon decision Plan 03-01 Task 2. |
| T-03-LEG | Compliance | plan de trade affiche | mitigate | bandeau disclaimer (D-20) herite de la surface signaux + footer P2. |
</threat_model>

<verification>
- Detail accessible a un abonne : chart v5 lecture seule avec entree/SL/TP legendees ; niveaux 1/2 ; analyse approfondie depliable.
- Contenu IA verbatim (test SignalDetail vert) ; zero dangerouslySetInnerHTML ; zero recharts (repli D-11).
- Signal non-actif/inexistant to notFound (anti-IDOR).
- lint:i18n exit 0 ; tsc -b sans nouvelle erreur ; aucun import repo service_role.
</verification>

<success_criteria>
La page detail est fonctionnelle et gated : chart entree/SL/TP, explication simple puis approfondie, contenu IA non altere. MEMB-03/04 observables.
</success_criteria>

<output>
Creer .planning/phases/03-espace-membre-signaux-gated-rls/03-03-SUMMARY.md a la fin.
</output>
