---
phase: 03-espace-membre-signaux-gated-rls
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/app/[locale]/(member)/signaux/page.tsx
  - apps/web/src/components/signals/SignalCard.tsx
  - apps/web/src/components/signals/FilterBar.tsx
  - apps/web/src/components/signals/SignalList.tsx
  - apps/web/src/components/signals/RealtimeBadge.tsx
  - apps/web/src/components/signals/SignalsDisclaimerBanner.tsx
  - apps/web/src/lib/signals/queries.ts
  - apps/web/tests/signals-rls.spec.ts
autonomous: true
requirements: [MEMB-01, MEMB-02, MEMB-05]
must_haves:
  truths:
    - "Un abonne voit la grille de cartes des signaux status=active tries par score decroissant ; chaque carte montre actif, direction (long=vert/short=rouge), score (neutre), risque, R:R, fraicheur."
    - "Un abonne filtre par actif, classe, style, risque ; filtres cumulables (ET), persistes dans l'URL (RSC-readable)."
    - "Nouveau signal actif to badge N nouveaux ; clic insere sans reflow ; signal non-actif disparait live ; Realtime tombe to repli refetch silencieux."
    - "Un test anon-client prouve qu'un non-abonne lit 0 trade_setup (RLS barriere)."
    - "Page signaux : AUCUN guard inline (layout gere requireActiveSub), AUCUN repo service_role."
  artifacts:
    - path: "apps/web/src/lib/signals/queries.ts"
      provides: "SELECT lecture seule anon-client filtre/trie status=active limit 100"
      exports: ["fetchActiveSignals"]
    - path: "apps/web/src/components/signals/SignalList.tsx"
      provides: "react-query + Realtime overlay (badge / retrait live / repli)"
  key_links:
    - from: "apps/web/src/components/signals/SignalList.tsx"
      to: "supabase realtime"
      via: "createBrowserClient + postgres_changes status=eq.active"
      pattern: "postgres_changes"
    - from: "apps/web/src/components/signals/FilterBar.tsx"
      to: "URL query params"
      via: "useRouter().replace @/i18n/navigation"
      pattern: "useRouter"
---

<objective>
Surface LISTE membre : page RSC qui lit trade_setups actifs (tri/filtre URL via anon-client + RLS), grille SignalCard, FilterBar URL-sync, overlay client SignalList (react-query + Realtime) avec badge N nouveaux, retrait live et repli silencieux. Bandeau disclaimer dedie + test E2E RLS abonne/non-abonne.

Purpose: MEMB-01 (liste triee score), MEMB-02 (filtres URL), MEMB-05 (temps reel). Barriere RLS prouvee par test anon-client.
Output: page liste, 5 composants signals, queries, test RLS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-CONTEXT.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md
@.planning/phases/03-espace-membre-signaux-gated-rls/03-01-SUMMARY.md
@apps/web/src/app/[locale]/dashboard/page.tsx
@apps/web/src/app/[locale]/(member)/layout.tsx
@apps/web/src/components/LanguageSwitcher.tsx
@apps/web/src/lib/supabase/server.ts
@apps/web/src/lib/supabase/client.ts

<interfaces>
From 03-01 : parseSignalsParams/serializeSignalsParams/SignalsParams (searchParams.ts) ; formatPrice/formatRelativeAge (format.ts) ; QueryProvider ; shadcn select+skeleton.
Colonnes REELLES : trade_setups(id, instrument_id, direction, opportunity_score, risk_level, risk_reward, style, valid_until, created_at) + instruments!inner(symbol, asset_class, precision, display_name). status=active D-02 · limit 100 D-19 · order opportunity_score desc D-08.
</interfaces>

<color_law>D-03 : vert=long, rouge=short (direction + bornes prix UNIQUEMENT). Score NEUTRE (--primary). Jamais vert/rouge sur score ni decoratif.</color_law>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: queries.ts + page RSC liste</name>
  <read_first>
    - apps/web/src/app/[locale]/dashboard/page.tsx (analog lecture serveur gated)
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (placeholder a remplacer)
    - apps/web/src/app/[locale]/(member)/layout.tsx (gate deja pose)
    - apps/web/src/lib/supabase/server.ts (createClient anon+cookies)
    - apps/web/src/lib/signals/searchParams.ts (parseSignalsParams, 03-01)
    - packages/supabase/src/repositories/tradeSetups.ts (interdit en front)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-PATTERNS.md
  </read_first>
  <action>
    Creer apps/web/src/lib/signals/queries.ts : fetchActiveSignals(supabase, params) lecture seule. from('trade_setups').select('id, instrument_id, direction, opportunity_score, risk_level, risk_reward, style, valid_until, created_at, instruments!inner(symbol, asset_class, precision, display_name)').eq('status','active').limit(100). Filtres conditionnels eq style/risk_level/instruments.asset_class/instruments.symbol. Tri recent=created_at desc, rr=risk_reward desc, sinon opportunity_score desc (D-08). Retourne data/error. AUCUN repo service_role.
    Remplacer signaux/page.tsx (RSC) : params+searchParams Promise (Next 15), setRequestLocale, getTranslations('signals'), createClient serveur, parseSignalsParams, fetchActiveSignals. Etats D-18 : error to bloc reessayer (signals.error.*), length 0 to vide rassurant (signals.emptyHeading/emptyBody), sinon FilterBar + QueryProvider>SignalList(initialData,locale) + SignalsDisclaimerBanner. PAS de guard inline. text-start.
  </action>
  <acceptance_criteria>
    - queries.ts : contient le select exact instruments!inner(symbol, asset_class, precision, display_name), eq status active, limit(100), opportunity_score ; grep repositories/ == 0.
    - signaux/page.tsx : grep requireActiveSub == 0 ; grep parseSignalsParams >= 1.
    - tsc -b --noEmit : aucune nouvelle erreur vs baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && grep -q "instruments!inner(symbol, asset_class, precision, display_name)" apps/web/src/lib/signals/queries.ts && grep -q "limit(100)" apps/web/src/lib/signals/queries.ts && ! grep -q "repositories/" apps/web/src/lib/signals/queries.ts && echo OK</automated>
  </verify>
  <done>queries.ts lecture anon RLS, page RSC remplace placeholder, etats soignes, gate au layout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SignalCard + FilterBar + DisclaimerBanner</name>
  <read_first>
    - apps/web/src/components/ui/card.tsx, badge.tsx, select.tsx
    - apps/web/src/components/LanguageSwitcher.tsx (analog useRouter @/i18n/navigation)
    - apps/web/src/components/Disclaimer.tsx
    - apps/web/src/i18n/navigation.ts
    - apps/web/src/lib/signals/format.ts (03-01)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md (Anatomy + Copywriting)
  </read_first>
  <action>
    SignalCard.tsx (server) : symbol + badge direction (long vert/short rouge, i18n Long/Short) ; score NEUTRE --primary tabular + libelle bands (signals.scoreBands.*) + barre fine neutre ; risque chip neutre, R:R en bdi, style chip ; fraicheur formatRelativeAge(created_at,locale)+valid_until en bdi ; prix formatPrice(v,precision). Carte = Link @/i18n/navigation vers /signaux/{id}. Classes logiques. Score jamais vert/rouge.
    FilterBar.tsx ('use client') : useRouter/usePathname @/i18n/navigation + useSearchParams next/navigation. Chips style+risque, select actif+classe, tri (score/recent/rr). router.replace(... { scroll: false }). Bouton Reinitialiser. >=44px. Labels next-intl. RTL.
    SignalsDisclaimerBanner.tsx (RSC) : getTranslations('signals') rend signals.disclaimerBanner, non-dismissible (D-20).
  </action>
  <acceptance_criteria>
    - FilterBar : grep @/i18n/navigation >=1, grep router.replace >=1, contient scroll: false.
    - SignalCard : contient bdi ; aucune classe directionnelle physique (text-left/right, ml-/mr-/pl-/pr-) ; grep @/i18n/navigation >=1.
    - pnpm --filter @app/web run lint:i18n exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && grep -q "@/i18n/navigation" apps/web/src/components/signals/FilterBar.tsx && grep -q "router.replace" apps/web/src/components/signals/FilterBar.tsx && grep -q "bdi" apps/web/src/components/signals/SignalCard.tsx && pnpm --filter @app/web run lint:i18n</automated>
  </verify>
  <done>SignalCard rend toutes les meta (D-03 color law respectee), FilterBar sync URL localisee, bandeau disclaimer present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SignalList + RealtimeBadge (react-query + canal Realtime, badge/retrait/repli)</name>
  <read_first>
    - apps/web/src/lib/supabase/client.ts (createClient = createBrowserSupabaseClient, porte la session)
    - packages/supabase/src/anon-client.ts (createBrowserClient via cookies — token Realtime)
    - apps/web/src/components/ThemeProvider.tsx (forme 'use client' provider)
    - apps/web/src/components/signals/SignalCard.tsx (carte a rendre dans la liste, Task 2)
    - apps/web/src/components/providers/QueryProvider.tsx (03-01)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md (Pattern 3 + Pitfalls 2/3/4)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-UI-SPEC.md (SignalList states + Realtime badge)
  </read_first>
  <action>
    Creer apps/web/src/components/signals/SignalList.tsx ('use client') : props initialData (rows RSC) + locale. useQuery react-query avec initialData (queryKey base sur les filtres courants) + refetchInterval leger (~60s) en repli D-16. createClient() navigateur (porte la session). channel('signals-active') : on postgres_changes INSERT table=trade_setups filter=status=eq.active to incremente un compteur "nouveaux" (NE PAS inserer directement, D-13) ; on postgres_changes UPDATE table=trade_setups SANS filtre statut (Pitfall 4) to si p.new.status !== 'active' retirer la carte (D-14). subscribe(status) to si status !== 'SUBSCRIBED' activer le repli refetch (D-16) + note discrete signals.realtimeLost. Cleanup removeChannel au unmount. Rendre la grille de SignalCard (1/2/3 col) + skeleton au chargement + RealtimeBadge en tete. Valeurs numeriques en bdi.
    Creer apps/web/src/components/signals/RealtimeBadge.tsx ('use client') : affiche signals.realtimeBadge pluralise ({count} nouveaux signaux — afficher / singulier) quand count>0 ; au clic, applique les nouveaux (merge dans la query) et remet count a 0. Non-intrusif (brand-blue subtil), >=44px, pas de reflow pendant la lecture.
  </action>
  <acceptance_criteria>
    - SignalList : grep postgres_changes >= 2 (INSERT + UPDATE) ; contient status=eq.active (filtre INSERT) ; contient removeChannel (cleanup) ; contient refetchInterval ou refetch (repli D-16).
    - SignalList : grep "@/lib/supabase/client" >= 1 (browser client porte la session) ; grep service_role == 0.
    - RealtimeBadge : grep realtimeBadge >= 1 (cle i18n pluralisee).
    - pnpm --filter @app/web run lint:i18n exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "." && test $(grep -c "postgres_changes" apps/web/src/components/signals/SignalList.tsx) -ge 2 && grep -q "status=eq.active" apps/web/src/components/signals/SignalList.tsx && grep -q "removeChannel" apps/web/src/components/signals/SignalList.tsx && ! grep -q "service_role" apps/web/src/components/signals/SignalList.tsx && echo OK</automated>
  </verify>
  <done>SignalList ecoute INSERT (badge) et UPDATE (retrait live), repli refetch si canal tombe, lecture via browser client (session), zero service_role.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Test E2E RLS (abonne lit / non-abonne 0 ligne)</name>
  <read_first>
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-VALIDATION.md (Wave 0 fixtures RLS Playwright)
    - .planning/phases/03-espace-membre-signaux-gated-rls/03-RESEARCH.md (Validation Architecture, mirroir test RLS journal P1)
    - apps/web/playwright.config.ts (confirmer la config ; sinon l'ajouter — Wave 0)
  </read_first>
  <action>
    Confirmer/installer la config Playwright dans apps/web (Wave 0 — si absente, l'ajouter en miroir d'un projet Next standard, baseURL local). Creer apps/web/tests/signals-rls.spec.ts : deux fixtures user — un abonne (has_active_subscription true) et un non-abonne. Le non-abonne authentifie qui appelle directement le client anon sur trade_setups (lecture) recoit 0 ligne (RLS barriere ACCESS-02/MEMB-01). L'abonne recoit >=0 lignes sans erreur RLS. Le non-abonne sur /signaux est redirige (gate UX) — assert l'URL finale n'est pas /signaux. Reutiliser le pattern du test RLS journal P1 (a localiser). Aucun service_role cote test front (injection eventuelle de donnees via util service_role hors apps/web seulement).
  </action>
  <acceptance_criteria>
    - apps/web/tests/signals-rls.spec.ts existe et reference trade_setups + un assert "0 ligne" pour le non-abonne.
    - apps/web/playwright.config.ts existe (confirme ou ajoute en Wave 0).
    - pnpm --filter @app/web exec playwright test signals-rls : le test est decouvert (peut etre human-verify pour GREEN si un dev server + .env sont requis, comme D-01-04-C en P1).
  </acceptance_criteria>
  <verify>
    <automated>cd "." && test -f apps/web/tests/signals-rls.spec.ts && grep -q "trade_setups" apps/web/tests/signals-rls.spec.ts && test -f apps/web/playwright.config.ts && echo OK</automated>
  </verify>
  <done>Test RLS signaux author, config Playwright presente, isolement abonne/non-abonne assertible (MEMB-01 barriere).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur to DB (RSC + Realtime) | lecture des signaux payants ; RLS = barriere |
| URL searchParams to requete | entree utilisateur dans .eq/.in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-RLS | Information Disclosure | lecture trade_setups (page + SignalList) | mitigate | anon-client + RLS has_active_subscription() (0009/0010) ; test signals-rls prouve non-abonne = 0 ligne ; aucun import repo service_role (lint fixture forbidden-service-import). |
| T-03-RT | Information Disclosure | canal Realtime postgres_changes | mitigate | createBrowserClient porte la session to RLS filtre les events (Pitfall 3) ; depend de la migration 0011 (Plan 03-01). |
| T-03-05 | Tampering | searchParams to .eq/.in | mitigate | parseSignalsParams (Zod whitelist, 03-01) avant toute requete ; asset normalise. |
| T-03-LEG | Compliance | surface plans de trade | mitigate | bandeau disclaimer dedie (D-20, LEGAL-01) + footer Disclaimer transverse P2. |
</threat_model>

<verification>
- Abonne voit la grille triee score desc ; filtres cumulables persistes URL (rechargeable).
- Realtime : INSERT to badge ; UPDATE non-active to retrait ; repli refetch si canal tombe.
- Test signals-rls : non-abonne lit 0 trade_setup ; abonne lit la liste.
- lint:i18n exit 0 ; tsc -b sans nouvelle erreur ; aucun import repo service_role dans apps/web.
</verification>

<success_criteria>
La liste membre est fonctionnelle, gated (RLS prouvee), filtrable/triable via URL, et temps reel avec repli silencieux. MEMB-01/02/05 observables.
</success_criteria>

<output>
Creer .planning/phases/03-espace-membre-signaux-gated-rls/03-02-SUMMARY.md a la fin.
</output>
</content>
</invoke>
