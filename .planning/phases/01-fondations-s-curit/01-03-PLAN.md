---
phase: 01-fondations-s-curit
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/jobs/package.json
  - apps/jobs/tsconfig.json
  - apps/jobs/.env
  - apps/jobs/src/runJob.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/src/jobs/heartbeat.ts
  - apps/jobs/windows/run-job.cmd
  - apps/jobs/__tests__/runJob.test.ts
  - docs/routines-claude.md
autonomous: false
requirements: [JOB-03, JOB-04]
user_setup: []

must_haves:
  truths:
    - "Un job déterministe est exécutable hors agent Claude via un entrypoint tsx unique (appelable par Windows Task Scheduler, croner, ou Routine Claude)."
    - "Chaque exécution de job écrit une entrée job_runs (statut running→success/error, timing, erreur, stats) via service_role."
    - "Le modèle d'exécution réel des Routines Claude (cloud, quota ~15 runs/j Max, secrets via Environments, MCP cloud-hosted, jobs via SDK supabase-js) est documenté."
  artifacts:
    - path: "apps/jobs/src/dispatch.ts"
      provides: "Entrypoint tsx ESM agnostique du scheduler"
      contains: "process.argv"
    - path: "apps/jobs/src/runJob.ts"
      provides: "Wrapper qui écrit job_runs (JOB-04)"
      contains: "job_runs"
    - path: "apps/jobs/windows/run-job.cmd"
      provides: "Wrapper Windows Task Scheduler (JOB-03)"
      contains: "tsx"
    - path: "apps/jobs/__tests__/runJob.test.ts"
      provides: "Test intégration runJob écrit job_runs (success + error)"
    - path: "docs/routines-claude.md"
      provides: "Doc du modèle d'exécution Routines Claude"
      contains: "Environments"
  key_links:
    - from: "apps/jobs/src/runJob.ts"
      to: "job_runs"
      via: "serviceClient insert/update"
      pattern: "from\\(.job_runs.\\)"
    - from: "apps/jobs/src/dispatch.ts"
      to: "apps/jobs/src/runJob.ts"
      via: "import + invoke par nom (argv)"
      pattern: "runJob"
    - from: "apps/jobs/src/runJob.ts"
      to: "@app/supabase/service-client"
      via: "import serviceClient"
      pattern: "service-client"
---

<objective>
Livrer la tranche verticale "runner de jobs + monitoring" du walking skeleton : un job déterministe (`heartbeat`) exécutable hors agent Claude via un dispatcher `tsx` unique, dont chaque exécution écrit une entrée `job_runs` via `service_role`. Plus la documentation vérifiée du modèle d'exécution des Routines Claude. Couvre JOB-03 et JOB-04.

Purpose: Boucler le dernier maillon du socle (`un job déterministe écrit job_runs`) et rendre native la robustesse "PC potentiellement éteint" (runner agnostique D-08 : Task Scheduler / croner / Routine Claude appellent le MÊME binaire). Le job appelle Supabase via le SDK supabase-js (PAS le MCP — Pitfall 5 / RESEARCH).
Output: `apps/jobs` (dispatcher + runJob + job heartbeat + wrapper .cmd Windows), test d'intégration JOB-04, et `docs/routines-claude.md`.
</objective>

## Décisions couvertes

- D-08: runner **agnostique dès le départ** — un entrypoint `tsx` unique dans `apps/jobs` (dispatcher), appelable indifféremment par une Routine Claude, Windows Task Scheduler (via `.cmd`) ou croner. Chaque run écrit une entrée `job_runs` (statut/timing/erreur, JOB-04) via `service_role` (SDK supabase-js, pas le MCP). Rend native la robustesse « PC potentiellement éteint ».

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/01-fondations-s-curit/01-CONTEXT.md
@.planning/phases/01-fondations-s-curit/01-RESEARCH.md
@.planning/phases/01-fondations-s-curit/01-SKELETON.md
@ARCHITECTURE.md
@.planning/phases/01-fondations-s-curit/01-02-SUMMARY.md

<interfaces>
<!-- Contrats consommés depuis packages/supabase (créé au plan 02). Source: 01-RESEARCH.md §Code Examples (Wrapper runJob + dispatcher). -->

depuis @app/supabase/service-client :
  serviceClient  // createClient<Database>(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) — import 'server-only'

depuis @app/supabase (repositories) :
  startRun(jobName): Promise<{ id }>        // INSERT job_runs status='running'
  finishRun(id, status, { error?, stats? })  // UPDATE job_runs finished_at + status

table job_runs (RLS: select authenticated, write service_role bypass) :
  id, job_name, status(running|success|error), started_at, finished_at, error, stats jsonb
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apps/jobs — wrapper runJob (écrit job_runs) + dispatcher tsx + job heartbeat + test intégration (JOB-04)</name>
  <read_first>
    - .planning/phases/01-fondations-s-curit/01-RESEARCH.md §Code Examples (Wrapper runJob + dispatcher) + §Pattern 6 + §Pitfall 5 (jobs via SDK pas MCP)
    - .planning/phases/01-fondations-s-curit/01-CONTEXT.md (D-08 runner agnostique, D-12 secrets .env)
    - packages/supabase/src/service-client.ts + repositories/jobRuns.ts (plan 02 — contrats à consommer)
    - ARCHITECTURE.md §5 (anatomie d'un job, monitoring job_runs)
  </read_first>
  <files>apps/jobs/package.json, apps/jobs/tsconfig.json, apps/jobs/.env, apps/jobs/src/runJob.ts, apps/jobs/src/dispatch.ts, apps/jobs/src/jobs/heartbeat.ts, apps/jobs/__tests__/runJob.test.ts</files>
  <behavior>
    - `runJob('heartbeat', fn)` insère une ligne `job_runs` avec `status='running'` puis, si `fn` réussit, met à jour `status='success'`, `finished_at` non nul, `stats` = retour de `fn`.
    - Si `fn` lève, `runJob` met à jour `status='error'`, `finished_at` non nul, `error` = message, puis re-throw (exit non-zéro).
    - Le dispatcher `dispatch.ts heartbeat` exécute le job `heartbeat` et sort avec code 0 en succès, 1 si job inconnu ou échec.
  </behavior>
  <action>
    Créer `apps/jobs` (`package.json` `"type":"module"`, deps `@app/supabase` workspace, `luxon@3.7.2`, `pino@10.3.1`, `dotenv` ; pas de `@supabase/supabase-js` direct — passe par `@app/supabase/service-client`). `apps/jobs/.env` (NON commité) avec `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (D-12 ; rempli au checkpoint du plan 02). `runJob.ts` : `runJob(jobName, fn)` utilisant `startRun`/`finishRun` de `@app/supabase` (ou directement `serviceClient.from('job_runs')`) — insère `running`, exécute `fn`, update `success`+`stats` ou `error`+message, re-throw en cas d'échec (comportement du bloc `<behavior>`). pino pour les logs structurés. `jobs/heartbeat.ts` : job pilote retournant `{ ok: true, ts: ISO }`. `dispatch.ts` (entrypoint tsx) : `import 'dotenv/config'`, registre `Record<string, fn>` (heartbeat), lit `process.argv[2]`, exécute via `runJob`, `process.exit(0|1)`. Écrire `apps/jobs/__tests__/runJob.test.ts` (Vitest intégration) qui appelle `runJob` avec un fn succès et un fn qui throw, puis vérifie via le client (service_role ou anon authentifié) qu'une ligne `job_runs` existe avec le bon `status` dans chaque cas. NE PAS dépendre du MCP Supabase (Pitfall 5) — uniquement le SDK supabase-js.
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs && pnpm --filter jobs exec tsx src/dispatch.ts heartbeat; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/jobs` passe : une ligne `job_runs` avec `status='success'` est écrite pour le cas succès, `status='error'` pour le cas échec (JOB-04).
    - `pnpm --filter jobs exec tsx src/dispatch.ts heartbeat` sort avec code 0 et a écrit une ligne `job_runs` success.
    - `dispatch.ts` lit `process.argv` et sort avec code 1 pour un job inconnu.
    - `apps/jobs/.env` n'est PAS commité (présent dans `.gitignore` du plan 01) ; aucun secret dans le code source.
    - `runJob.ts` importe le client via `@app/supabase` (pas de clé en dur, pas de MCP).
  </acceptance_criteria>
  <done>Le runner agnostique écrit job_runs (success + error) via service_role ; le dispatcher tsx est exécutable hors agent Claude. JOB-04 prouvé.</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper Windows Task Scheduler (.cmd) + doc du modèle d'exécution Routines Claude (JOB-03)</name>
  <read_first>
    - .planning/phases/01-fondations-s-curit/01-RESEARCH.md §Summary + §Code Examples (Wrapper Windows) + §Common Pitfalls (5,6) + §Open Questions + §Assumptions Log (A1,A2)
    - .planning/phases/01-fondations-s-curit/01-CONTEXT.md (D-08)
    - ARCHITECTURE.md §5 (horaires sessions UTC, robustesse PC éteint)
    - apps/jobs/src/dispatch.ts (Task 1 — entrypoint à invoquer)
  </read_first>
  <files>apps/jobs/windows/run-job.cmd, docs/routines-claude.md</files>
  <action>
    Créer `apps/jobs/windows/run-job.cmd` : `@echo off`, `cd /d` vers la racine du repo (relatif `%~dp0\..\..\..`), puis `call pnpm --filter jobs exec tsx src/dispatch.ts %1` (passe le nom de job en argument). C'est le déclencheur Windows Task Scheduler (JOB-03) ; le même `dispatch.ts` est ainsi invocable hors agent Claude. Créer `docs/routines-claude.md` documentant le modèle d'exécution VÉRIFIÉ (RESEARCH §Summary) : (1) Routines Remote tournent dans le cloud Anthropic (PC éteint OK) ; (2) quota ~15 runs/jour sur Max, PARTAGÉ avec les sessions interactives (Pitfall 6) ; (3) secrets injectés via Environments (variables chiffrées + setup script + network access réglable) ; (4) les connectors sont des MCP cloud-hosted — un MCP local stdio n'est PAS accessible (Pitfall 5) → les jobs appellent Supabase via le SDK supabase-js avec network access autorisé vers `*.supabase.co` ; (5) fallback = Windows Task Scheduler exécute le dispatcher déterministe hors quota Claude (le `.cmd` ci-dessus) ; (6) en Phase 1, AUCUNE Routine n'est planifiée (l'analyse IA = Phase 4) — ce document est la base que Phase 4 utilisera pour configurer l'Environment + network access. Inclure la table des horaires UTC d'ARCHITECTURE.md §5 et les Open Questions (network access par défaut vers Supabase — à confirmer Phase 4, A1). Documenter la commande locale de lancement (`pnpm --filter jobs exec tsx src/dispatch.ts <job>`) et le wrapper `.cmd`.
  </action>
  <verify>
    <automated>test -f apps/jobs/windows/run-job.cmd && grep -c "tsx" apps/jobs/windows/run-job.cmd && grep -c "Environments" docs/routines-claude.md</automated>
  </verify>
  <acceptance_criteria>
    - `apps/jobs/windows/run-job.cmd` invoque `tsx src/dispatch.ts %1` (JOB-03 fallback déterministe).
    - `docs/routines-claude.md` documente : cloud/PC-éteint, quota ~15/j Max partagé, secrets via Environments, MCP cloud-hosted (jobs via SDK pas MCP), fallback Task Scheduler.
    - `docs/routines-claude.md` note explicitement qu'aucune Routine n'est planifiée en Phase 1 (analyse = Phase 4).
  </acceptance_criteria>
  <done>Le fallback Windows Task Scheduler pointe sur le dispatcher (JOB-03) et le modèle d'exécution des Routines Claude est documenté et vérifié.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Runner de jobs complet : dispatcher tsx, runJob écrivant job_runs, wrapper .cmd Windows, doc Routines. Le job heartbeat a écrit des lignes job_runs réelles dans le projet Supabase cloud.</what-built>
  <how-to-verify>
    1. Confirmer via MCP Supabase (`execute_sql` SELECT sur `job_runs` ORDER BY started_at DESC) que les exécutions de heartbeat ont écrit des lignes (status success ET error des tests).
    2. (Optionnel, manuel) Créer une tâche Windows Task Scheduler pointant sur `apps/jobs/windows/run-job.cmd heartbeat` et vérifier qu'elle écrit une ligne `job_runs` même sans session Claude — prouve JOB-03 hors agent. (Peut être différé ; la commande locale `tsx` suffit pour la vérification automatisée.)
    3. Vérifier que `docs/routines-claude.md` reflète fidèlement RESEARCH §Summary (pas d'affirmation non vérifiée sur le MCP en Routine).
  </how-to-verify>
  <resume-signal>Taper "approved" quand des lignes job_runs sont visibles côté cloud et la doc est exacte, sinon décrire l'écart.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler (Task Scheduler / croner / Routine) → apps/jobs | Déclenchement externe du dispatcher |
| apps/jobs → Supabase (service_role) | Écriture privilégiée (bypass RLS) — doit rester hors du bundle web |
| Routine Claude cloud → Supabase | Network access cloud (Phase 4) — documenté, pas activé en P1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07 | Information Disclosure | `service_role` dans apps/jobs (`.env`) | mitigate | `.env` jobs non commité (`.gitignore` plan 01) ; clé consommée via `@app/supabase/service-client` (server-only) ; jamais préfixée `NEXT_PUBLIC_`. D-12/AUTH-03. |
| T-08 | Tampering | Job suppose le MCP Supabase disponible en Routine cloud (échec silencieux) | mitigate | Jobs appellent Supabase via SDK supabase-js uniquement (Pitfall 5) ; doc `routines-claude.md` explicite que le MCP stdio local n'est pas dans une Routine Remote. |
| T-09 | Denial of Service | Quota 15 runs/j Max épuisé par jobs planifiés | accept | Documenté (Pitfall 6) ; en P1 aucune Routine planifiée ; ingestion déterministe sur Task Scheduler hors quota Claude. Risque accepté car non bloquant en P1. |
| T-10 | Repudiation | Échec de job non tracé | mitigate | `runJob` écrit systématiquement job_runs (running→success/error + error + timing), prouvé par test intégration. JOB-04. |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs` → job_runs écrit (success + error) GREEN (JOB-04).
- `pnpm --filter jobs exec tsx src/dispatch.ts heartbeat` → exit 0 + ligne job_runs.
- `apps/jobs/windows/run-job.cmd` invoque le dispatcher (JOB-03).
- `docs/routines-claude.md` documente le modèle d'exécution vérifié.
- MCP SELECT sur `job_runs` montre des lignes réelles côté cloud.
</verification>

<success_criteria>
- JOB-03 : étapes déterministes exécutables hors agent Claude (dispatcher tsx + wrapper .cmd Task Scheduler).
- JOB-04 : chaque exécution écrit une entrée job_runs (statut/timing/erreur), prouvé par test.
- Modèle d'exécution des Routines Claude vérifié et documenté.
</success_criteria>

<output>
Create `.planning/phases/01-fondations-s-curit/01-03-SUMMARY.md` when done
</output>
