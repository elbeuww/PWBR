---
phase: 01-fondations-s-curit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - eslint.config.mjs
  - .prettierrc
  - .gitignore
  - .env.example
  - vitest.config.ts
  - playwright.config.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/core/src/time/constants.ts
  - packages/core/src/time/sessions.ts
  - packages/core/src/time/candle.ts
  - packages/core/src/time/candle.test.ts
  - packages/core/src/time/sessions.test.ts
autonomous: false
requirements: [DATA-05]
user_setup: []

must_haves:
  truths:
    - "Le monorepo pnpm s'installe et `pnpm typecheck` + `pnpm lint` passent sur un workspace vide cohérent."
    - "Les constantes temps (UTC, bougie clôturée, daily natif par source OANDA 17:00 NY / Binance 00:00 UTC) sont calculées de façon déterministe dans packages/core."
    - "Vitest et Playwright sont configurés et exécutables à la racine."
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Déclaration workspaces apps/* packages/*"
      contains: "packages:"
    - path: "packages/core/src/time/candle.ts"
      provides: "lastClosedCandleStart (anti look-ahead) + helpers UTC"
      exports: ["lastClosedCandleStart"]
    - path: "packages/core/src/time/sessions.ts"
      provides: "DAILY_ANCHOR par source + bornes de session luxon"
      exports: ["DAILY_ANCHOR"]
    - path: "packages/core/src/time/candle.test.ts"
      provides: "Golden values DATA-05 (bougie clôturée)"
      contains: "lastClosedCandleStart"
    - path: "vitest.config.ts"
      provides: "Config Vitest racine"
    - path: "playwright.config.ts"
      provides: "Config Playwright racine"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/time/*"
      via: "re-export barrel"
      pattern: "export.*time"
---

<objective>
Poser le socle du monorepo pnpm (scaffold strict skeleton D-11) + l'infrastructure de test (Wave 0 : Vitest + Playwright) + la première capacité déterministe réelle : les constantes temporelles partagées de `packages/core` (UTC, bougie clôturée anti look-ahead, daily natif par source) couvrant DATA-05.

Purpose: Établir le terrain commun sur lequel les plans 02 (auth/RLS) et 03 (jobs) se grefferont, et livrer immédiatement une valeur testable (les constantes temps consommées par tous les jobs et phases futures). Pas de coquilles vides : seuls `apps/web`, `apps/jobs`, `packages/core`, `packages/supabase` sont déclarés ; `data-sources`/`indicators` viendront à leur phase.
Output: Workspace pnpm installable, ESLint flat config (avec la règle anti-import service_role préparée), `.env.example` contrat, configs de test, et `packages/core` testé par golden values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-fondations-s-curit/01-CONTEXT.md
@.planning/phases/01-fondations-s-curit/01-RESEARCH.md
@.planning/phases/01-fondations-s-curit/01-SKELETON.md

<interfaces>
<!-- Constantes temps à exporter depuis packages/core (forme indicative, structure exacte = discrétion D-Claude). Source: 01-RESEARCH.md §Code Examples. -->

DAILY_ANCHOR (sessions.ts) — objet const par source:
  oanda:   { zone: 'America/New_York', hour: 17 }  // convention FX réelle (D-09)
  binance: { zone: 'UTC',              hour: 0  }  // crypto 24/7 (D-09)

lastClosedCandleStart(now: DateTime, tfMinutes: number): DateTime
  // borne haute EXCLUSIVE — n'inclut que les bougies clôturées (anti look-ahead, D-10)
  // dernière clôturée = floor(epoch / tf) - 1, en UTC

TIMEFRAMES (constants.ts) — minutes par timeframe MVP: H1=60, H4=240, D=1440
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold monorepo pnpm + ESLint flat config + .env.example + .gitignore</name>
  <read_first>
    - CLAUDE.md (versions verrouillées : Next 15, @supabase/ssr 0.12.0, @supabase/supabase-js 2.108.0, zod 4.4.3, luxon 3.7.2, tsx 4.22.4, vitest 4.1.8, @playwright/test 1.60.0, pnpm 9)
    - .planning/phases/01-fondations-s-curit/01-RESEARCH.md §Recommended Project Structure + §ESLint no-restricted-imports
    - .planning/phases/01-fondations-s-curit/01-CONTEXT.md (D-07, D-11, D-12)
  </read_first>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, eslint.config.mjs, .prettierrc, .gitignore, .env.example</files>
  <action>
    Créer `pnpm-workspace.yaml` listant `apps/*` et `packages/*`. Créer le `package.json` racine (`"type": "module"`, `"private": true`, packageManager pnpm@9) avec scripts : `dev` (→ `pnpm --filter web dev`), `typecheck` (→ `tsc -b` ou `pnpm -r exec tsc --noEmit`), `lint` (→ `eslint .`), `test` (→ `vitest run`), `test:e2e` (→ `playwright test`). Installer en devDependencies racine (`-w`) : `typescript@5.7`+, `tsx@4.22.4`, `vitest@4.1.8`, `@playwright/test@1.60.0`, `eslint`, `prettier`, `@eslint/js`, `typescript-eslint`. Créer `tsconfig.base.json` avec `strict: true`, `module`/`moduleResolution` ESM (`NodeNext` ou `Bundler`), et `paths` pour `@app/core` et `@app/supabase`. Créer `eslint.config.mjs` (flat config) incluant un bloc `no-restricted-imports` ciblant `files: ['apps/web/**/*.{ts,tsx}']` qui interdit le groupe `['**/supabase/**/service-*', '@app/supabase/service-client']` avec message explicite (prépare AUTH-03 ; le module cible sera créé au plan 02 — la règle est posée maintenant pour éviter une régression). Créer `.prettierrc` minimal. Créer `.gitignore` excluant `node_modules`, `.next`, `dist`, `.env`, `.env.local`, `.env*.local`, `test-results`, `playwright-report`. Créer `.env.example` (commité = contrat D-12) avec les clés vides documentées : `NEXT_PUBLIC_SUPABASE_URL=`, `NEXT_PUBLIC_SUPABASE_ANON_KEY=`, `SUPABASE_URL=`, `SUPABASE_SERVICE_ROLE_KEY=` + commentaire indiquant quelle clé va dans `apps/web/.env.local` (anon) vs `apps/jobs/.env` (service_role). NE PAS commiter de valeur réelle. Choix exact des patterns ESLint = discrétion D-Claude.
  </action>
  <verify>
    <automated>pnpm install && pnpm exec eslint --print-config eslint.config.mjs > /dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` réussit sans erreur (workspace valide).
    - `pnpm-workspace.yaml` contient `apps/*` et `packages/*`.
    - `tsconfig.base.json` contient `"strict": true`.
    - `eslint.config.mjs` contient une règle `no-restricted-imports` mentionnant `service-` et restreinte à `apps/web/**`.
    - `.gitignore` contient `.env` et `.env.local` ; `.env.example` est présent et NE contient AUCUNE valeur de secret (clés à droite du `=` vides).
    - `.env.example` documente les 4 variables (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).
  </acceptance_criteria>
  <done>Le workspace pnpm s'installe, ESLint charge sa config flat, le contrat `.env.example` est commité sans secret, la règle anti-import service_role est posée.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: packages/core — constantes temps (UTC, bougie clôturée, daily par source) + golden tests DATA-05</name>
  <read_first>
    - .planning/phases/01-fondations-s-curit/01-RESEARCH.md §Code Examples (constantes temps) + §Validation Architecture (DATA-05)
    - .planning/phases/01-fondations-s-curit/01-CONTEXT.md (D-09 daily natif par source, D-10 UTC + bougie clôturée)
    - ARCHITECTURE.md §5 (horaires sessions UTC) + §Principes (anti look-ahead)
    - tsconfig.base.json (créé Task 1)
  </read_first>
  <files>packages/core/package.json, packages/core/tsconfig.json, packages/core/src/index.ts, packages/core/src/time/constants.ts, packages/core/src/time/sessions.ts, packages/core/src/time/candle.ts, packages/core/src/time/candle.test.ts, packages/core/src/time/sessions.test.ts, vitest.config.ts</files>
  <behavior>
    - `lastClosedCandleStart(now, 60)` : pour `now = 2026-06-09T10:30:00Z`, retourne le début de la dernière bougie H1 CLÔTURÉE = `2026-06-09T09:00:00Z` (exclut la bougie 10:00 en cours).
    - `lastClosedCandleStart(now, 240)` (H4) : pour `now = 2026-06-09T10:30:00Z`, retourne `2026-06-09T04:00:00Z` (les buckets H4 sont alignés sur l'epoch UTC ; la bougie 08:00 est en cours).
    - `lastClosedCandleStart` ne retourne JAMAIS une borne ≥ `now` (anti look-ahead).
    - `DAILY_ANCHOR.oanda` = `{ zone: 'America/New_York', hour: 17 }` ; `DAILY_ANCHOR.binance` = `{ zone: 'UTC', hour: 0 }`.
    - Une fonction d'ancre daily (ex. `dailyAnchorStart(source, now)`) calcule la dernière clôture daily selon la source via luxon (gère DST NY).
  </behavior>
  <action>
    Créer le package `@app/core` (`package.json` avec `"type": "module"`, dépendances `luxon@3.7.2` + `zod@4.4.3`, devDep `@types/luxon`) et son `tsconfig.json` étendant `../../tsconfig.base.json`. Implémenter `constants.ts` (enum/objet `TIMEFRAMES` en minutes : H1=60, H4=240, D=1440 ; constante de fuseau canonique `UTC`). Implémenter `candle.ts` avec `lastClosedCandleStart(now: DateTime, tfMinutes: number): DateTime` — borne haute EXCLUSIVE via `floor(now.toUTC().toSeconds() / (tfMinutes*60)) - 1` (anti look-ahead D-10). Implémenter `sessions.ts` avec `DAILY_ANCHOR` (D-09, valeurs exactes ci-dessus) et une fonction d'ancre daily par source utilisant `DateTime.setZone`. Barrel `index.ts` re-exporte tout. Écrire les tests golden `candle.test.ts` et `sessions.test.ts` couvrant les cas du bloc `<behavior>` (RED d'abord : écrire les assertions, voir échouer, puis implémenter → GREEN). Créer `vitest.config.ts` racine (environnement node, include `packages/**/*.test.ts` et `apps/**/__tests__/**`). Forme exacte des constantes = discrétion D-Claude tant que le comportement ci-dessus tient.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/core` passe (golden values DATA-05 vertes).
    - `packages/core/src/time/candle.ts` exporte `lastClosedCandleStart`.
    - `packages/core/src/time/sessions.ts` exporte `DAILY_ANCHOR` avec `oanda.hour === 17` et `binance.zone === 'UTC'`.
    - Le test prouve que `lastClosedCandleStart` exclut la bougie en cours (borne < now).
    - `pnpm typecheck` passe sur `packages/core` (strict).
  </acceptance_criteria>
  <done>Les constantes temps sont déterministes, testées par golden values, et exportées via le barrel `@app/core`. DATA-05 satisfait au niveau code partagé.</done>
</task>

<task type="auto">
  <name>Task 3: playwright.config.ts + stubs de test Wave 0 (placeholders RLS / auth / runJob)</name>
  <read_first>
    - .planning/phases/01-fondations-s-curit/01-VALIDATION.md §Wave 0 Requirements + §Per-Task Verification Map
    - .planning/phases/01-fondations-s-curit/01-RESEARCH.md §Validation Architecture §Wave 0 Gaps
    - vitest.config.ts (créé Task 2)
  </read_first>
  <files>playwright.config.ts</files>
  <action>
    Créer `playwright.config.ts` racine (testDir `apps/web/e2e`, baseURL `http://localhost:3000`, un projet chromium, `webServer` optionnel commenté car le serveur dev est lancé manuellement en P1). NE PAS créer les fichiers de test métier ici (ils appartiennent à leurs plans : `rls.test.ts` et `auth.spec.ts` au plan 02, `runJob.test.ts` au plan 03) — ce plan se limite à poser les DEUX configs (Vitest fait en Task 2, Playwright ici) pour que les plans aval aient une infra de test prête. Installer le navigateur Playwright (`pnpm exec playwright install chromium`).
  </action>
  <verify>
    <automated>pnpm exec playwright test --list 2>&1 | head -5; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` existe avec `testDir` pointant `apps/web/e2e`.
    - `pnpm exec playwright test --list` s'exécute sans erreur de config (0 test listé est acceptable à ce stade).
    - `pnpm vitest run` (racine) s'exécute et ne trouve que les tests `packages/core` (verts).
  </acceptance_criteria>
  <done>Vitest et Playwright sont configurés et exécutables à la racine ; les plans 02/03 peuvent y déposer leurs tests sans recréer d'infra.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Scaffold monorepo, ESLint anti-import, `.env.example` contrat, `packages/core` testé, configs Vitest/Playwright. Le premier `pnpm install` a installé `server-only`, `luxon`, `zod`, `tsx` et l'outillage de test.</what-built>
  <how-to-verify>
    1. RESEARCH.md §Package Legitimacy Audit signale que `slopcheck` est indisponible et recommande une vérification humaine du publisher de `server-only@0.0.1` (numéro de version trompeur mais paquet officiel React/Next).
    2. Vérifier sur https://www.npmjs.com/package/server-only que le publisher est bien `vercel`/`react` et la version `0.0.1`.
    3. Vérifier que `pnpm install` n'a tiré aucun paquet inattendu (`pnpm why server-only`).
    4. Confirmer qu'aucune valeur de secret n'a été commitée dans `.env.example`.
  </how-to-verify>
  <resume-signal>Taper "approved" si `server-only` est légitime et aucun secret commité, sinon décrire le problème.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | Installation de paquets (chaîne d'approvisionnement) |
| repo → git remote | Risque de commit de secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pnpm install` (server-only et autres) | mitigate | Checkpoint humain bloquant vérifiant le publisher de `server-only@0.0.1` sur npmjs.com (slopcheck indisponible — RESEARCH §Package Audit). Tous les autres paquets sont des libs officielles >1M dl/sem. |
| T-01-01 | Information Disclosure | `.env.example` commité | mitigate | `.env.example` ne contient que des clés vides (contrat) ; `.gitignore` exclut `.env`/`.env.local` ; acceptance criteria vérifie l'absence de valeur. |
| T-01-02 | Elevation of Privilege | Import service_role depuis apps/web | mitigate | Règle ESLint `no-restricted-imports` posée dès maintenant (barrière lint-time), avant même la création du module au plan 02. |
</threat_model>

<verification>
- `pnpm install` réussit.
- `pnpm vitest run` → golden values `packages/core` vertes (DATA-05).
- `pnpm typecheck` et `pnpm lint` passent sur le workspace.
- `pnpm exec playwright test --list` sans erreur de config.
- `.env.example` commité sans secret ; `.gitignore` exclut les `.env`.
</verification>

<success_criteria>
- Monorepo pnpm installable avec les 4 packages déclarés (D-11), pas de coquille vide superflue.
- Constantes temps déterministes testées (DATA-05).
- Infra de test (Vitest + Playwright) prête pour les plans aval.
- Règle anti-import service_role et contrat `.env.example` en place.
</success_criteria>

<output>
Create `.planning/phases/01-fondations-s-curit/01-01-SUMMARY.md` when done
</output>
