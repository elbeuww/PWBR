---
phase: 16-reskin-transversal-de-toutes-les-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/__tests__/theme-scan.test.ts
  - apps/web/src/styles/__tests__/rls-unchanged.test.ts
  - apps/web/src/styles/__tests__/lwc-recolor-intact.test.ts
  - apps/web/src/styles/__tests__/volt-orphan-free.test.ts
  - apps/web/src/components/ui/glow.tsx
  - apps/web/src/components/ui/data-rain.tsx
  - apps/web/src/styles/globals.css
autonomous: true
requirements: [RESKIN-01, RESKIN-02, RESKIN-03, RESKIN-04, RESKIN-05, RESKIN-06]

must_haves:
  truths:
    - "theme-scan covers every surface reskinned in waves 2 (not just the 9 Phase-15 foundation files)"
    - "A structural scan fails if any reskin migrates an RLS fetch to the client or imports service_role on a non-admin page"
    - "A structural scan fails if CandleChart loses its lwc getComputedStyle/applyOptions recolor"
    - "A structural scan fails if any volt orphan (data-theme=volt / nxl-theme-toggle / nexa-landing-theme) survives under apps/web/src"
    - "A reusable tokenized glow primitive exists referencing --glow (box-shadow, never ring-*)"
    - "A reusable tokenized light data-rain primitive exists referencing --signal-bullish/--signal-bearish, reduced-motion guarded"
  artifacts:
    - path: "apps/web/src/styles/__tests__/theme-scan.test.ts"
      provides: "Extended FOUNDATION_FILES + FORBIDDEN_PALETTE covering wave-2 surfaces"
      contains: "FOUNDATION_FILES"
    - path: "apps/web/src/styles/__tests__/rls-unchanged.test.ts"
      provides: "Structural assertion: no service_role/admin-service in non-admin pages; gated reads stay server createClient"
    - path: "apps/web/src/styles/__tests__/lwc-recolor-intact.test.ts"
      provides: "Structural assertion: CandleChart recolor via getComputedStyle/applyOptions, zero candle color in CSS"
    - path: "apps/web/src/styles/__tests__/volt-orphan-free.test.ts"
      provides: "Structural assertion: zero volt orphan strings under apps/web/src"
    - path: "apps/web/src/components/ui/glow.tsx"
      provides: "Tier 2 reusable glow primitive (box-shadow var(--glow))"
    - path: "apps/web/src/components/ui/data-rain.tsx"
      provides: "Tier 2 reusable light data-rain primitive (tokenized, reduced-motion guarded)"
  key_links:
    - from: "apps/web/src/styles/__tests__/theme-scan.test.ts"
      to: "wave-2 reskinned surfaces"
      via: "FOUNDATION_FILES list extension"
      pattern: "FOUNDATION_FILES"
    - from: "apps/web/src/components/ui/glow.tsx"
      to: "globals.css --glow token"
      via: "box-shadow referencing var(--glow)"
      pattern: "var\\(--glow\\)"
---

<objective>
Wave 0 of the reskin: lock the guardrails BEFORE any surface is repainted, and extract the only "new code" the phase needs — reusable tokenized neon primitives (glow + light data-rain) — from the landing's `.nxl`-scoped CSS.

This plan does NOT reskin any page. It (1) extends the existing `theme-scan` guardrail to cover the surfaces waves 2 will touch, (2) adds three structural-assertion scans that fail if a reskin breaks an inviolable constraint (RLS fetch migrated, CandleChart recolor lost, volt orphan left live), and (3) generalizes the landing glow + data-rain into tokenized Tier-2 primitives (D-06) referencing the component token layer.

Purpose: every wave-2 task can then run against blocking automated gates instead of subjective review. The guardrails are the contract; this plan writes the contract.
Output: 1 extended scan + 3 new scans + 2 primitive components + (if needed) a component-layer token hook in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-CONTEXT.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-RESEARCH.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-PATTERNS.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-UI-SPEC.md

<interfaces>
<!-- Existing theme-scan structure the executor extends, not rewrites. Extracted from the file. -->

apps/web/src/styles/__tests__/theme-scan.test.ts (extend, do NOT rewrite):
- `FOUNDATION_FILES: readonly string[]` (lines 32-42) — 9 src-relative paths, the scanned set for Test 2/3.
- `FORBIDDEN_PALETTE: { token: string; re: RegExp }[]` (lines 48-62) — raw-palette regexes.
- `FORBIDDEN_RING = /ring-\[#2563EB\]/` (line 45) — applies tree-wide in Test 1.
- SANITY block (lines 144-165) — every FORBIDDEN_PALETTE token must match a planted fixture; keep in sync when adding tokens.
- helper `readFoundation(rel)` throws on missing path (no silent skip).

Landing neon source (apps/web/src/components/landing/nexa-landing.css):
- glow box-shadows: `.nxl .btn-primary { box-shadow: 0 8px 30px -10px var(--glow); }`, `.nxl .mark-tile { box-shadow: 0 0 22px -4px var(--glow); }`, `.nxl .nxl-progress { box-shadow: 0 0 12px var(--glow); }`
- data-rain: `.nxl .data-rain` / `.nxl .data-col` with `.u`/`.d` color-mix from `--buy`/`--sell`; reduced-motion → `animation: none`.

globals.css component token layer (reference these only): `--glow` `oklch(0.84 0.18 150 / 0.55)`, `--signal-bullish` `oklch(0.82 0.18 150)`, `--signal-bearish` `oklch(0.68 0.2 24)`.

admin-service usage (verified): imported only under `app/(admin)/**` PLUS two pre-existing server-action files `app/[locale]/affiliation/actions.ts` and `app/[locale]/(auth)/actions.ts`. The RLS-unchanged scan MUST allowlist these two existing actions files and only flag NEW non-admin page imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend theme-scan to cover wave-2 surfaces + author the 3 structural-assertion scans</name>
  <files>apps/web/src/styles/__tests__/theme-scan.test.ts, apps/web/src/styles/__tests__/rls-unchanged.test.ts, apps/web/src/styles/__tests__/lwc-recolor-intact.test.ts, apps/web/src/styles/__tests__/volt-orphan-free.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/theme-scan.test.ts (current state — extend the arrays, keep node:fs-only, keep SANITY in sync)
    - apps/web/src/components/signals/CandleChart.tsx (confirm readChartColors/getComputedStyle/applyOptions/MutationObserver names for the lwc-recolor assertion)
    - apps/web/src/components/landing/NexaLanding.tsx and NexaLandingEffects.tsx (confirm the exact orphan strings the volt scan must forbid)
    - apps/web/src/lib/supabase/admin-service.ts (confirm export name createAdminServiceClient for the RLS scan)
  </read_first>
  <behavior>
    - theme-scan Test 2: extend FOUNDATION_FILES with the wave-2 residual-offender surfaces — `app/(admin)/sante/page.tsx` (already present), `app/[locale]/dashboard/page.tsx`, `app/[locale]/(auth)/login/page.tsx`, plus the not-yet-verified surfaces waves 2 will tokenize: `app/(admin)/membres/page.tsx`, `app/(admin)/signaux/[id]/page.tsx`, `app/(admin)/affiliation/affilies/page.tsx`. Add `text-red-600`, `text-emerald-400`, `bg-emerald-500`, `bg-amber-500` (standalone) regexes to FORBIDDEN_PALETTE and a matching planted line in the SANITY fixture.
    - rls-unchanged.test.ts: walk `app/[locale]/(member)/**`, `app/[locale]/(account)/**`, `app/[locale]/(marketing)/**`, `app/[locale]/(auth)/**` `page.tsx`/`layout.tsx`; FAIL if any imports `admin-service` or references `service_role` or imports `createClient` from `lib/supabase/client` in a server (non-`'use client'`) gated read. ALLOWLIST the two pre-existing server-action files `app/[locale]/affiliation/actions.ts` and `app/[locale]/(auth)/actions.ts`. SANITY: a planted string `from '@/lib/supabase/admin-service'` is detected.
    - lwc-recolor-intact.test.ts: read `components/signals/CandleChart.tsx`; assert it contains `getComputedStyle`, `applyOptions`, and `MutationObserver`; assert zero candle-color CSS literal (no `upColor:` / `downColor:` assigned a hex/named color string). SANITY: a planted `upColor: '#26a69a'` would fail the no-literal check.
    - volt-orphan-free.test.ts: walk `apps/web/src/**` (.tsx + .css + .ts); FAIL if any of `data-theme="volt"`, `nxl-theme-toggle`, `nexa-landing-theme` appears. SANITY: planted fixture string matches each regex.
  </behavior>
  <action>Extend `theme-scan.test.ts` in place — append the listed src-relative paths to `FOUNDATION_FILES` and the listed regexes (`/text-red-600\b/`, `/text-emerald-400\b/`, `/bg-emerald-500(?!\/)\b/`, `/bg-amber-500(?!\/)\b/`) to `FORBIDDEN_PALETTE`, and add one planted line per new token to the SANITY fixture so the detector stays non-trivial. Keep the file dependency-free (`node:fs`/`node:path` only). Author the three new scans as siblings under `apps/web/src/styles/__tests__/` using the same `listTsxFiles` recursive-walk + `readFileSync` idiom. For `rls-unchanged.test.ts`, hardcode the two allowlisted actions paths so only NEW non-admin page service_role imports fail. These four suites are RED-then-GREEN gates: at this point in wave 1 the three structural scans are GREEN (tree currently compliant), theme-scan Test 2 is RED for the 3 residual offenders (`sante:65-66`, `dashboard:58`, `login:44`) — that RED is EXPECTED and proves the gate measures real wave-2 work. Do NOT fix the offenders here (waves 2 do). No fenced implementation beyond the regexes named above.</action>
  <verify>
    <automated>pnpm --filter web test -- rls-unchanged lwc-recolor-intact volt-orphan-free</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- rls-unchanged lwc-recolor-intact volt-orphan-free` exits 0 (all three GREEN against the current compliant tree).
    - `pnpm --filter web test -- theme-scan` shows Test 2 RED listing exactly `sante`, `dashboard`, `login` offenders, and SANITY + Test 1 + Test 3 GREEN (detector non-trivial, ring scan tree-wide passes).
    - `grep -n "FOUNDATION_FILES" apps/web/src/styles/__tests__/theme-scan.test.ts` shows the 3 added wave-2 paths present.
    - rls-unchanged.test.ts allowlists `app/[locale]/affiliation/actions.ts` and `app/[locale]/(auth)/actions.ts` (grep shows both literal paths).
    - Each new scan imports only from `vitest`, `node:fs`, `node:path` (no app imports) — grep shows no `@/` import.
  </acceptance_criteria>
  <done>Four guardrail suites exist; the three structural scans are GREEN; theme-scan Test 2 is RED for exactly the 3 known offenders (expected pre-wave-2); all SANITY blocks GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract glow + light data-rain into tokenized Tier-2 primitives</name>
  <files>apps/web/src/components/ui/glow.tsx, apps/web/src/components/ui/data-rain.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/components/landing/nexa-landing.css (glow box-shadow values + .data-rain/.data-col source to generalize)
    - apps/web/src/components/landing/NexaLandingEffects.tsx (data-rain injection: #dataRain host, .data-col columns, RAIN_POOL, reduced-motion guard — the logic to generalize)
    - apps/web/src/components/member/ExpiryBanner.tsx (Tier-2 token-bound component analog — logical props, no literal)
    - apps/web/src/styles/globals.css (component token layer: --glow, --signal-bullish, --signal-bearish)
  </read_first>
  <behavior>
    - glow.tsx: exports a tokenized glow affordance (utility component or `className` constant — discretion D-06) applying `box-shadow` derived from `var(--glow)` only. NEVER emits a `ring-*` utility (anti-collision C-3, theme-scan Test 3 must stay green). Usable on Tier-2 cards / primary buttons / focus states.
    - data-rain.tsx: a light ambient primitive generalizing the landing data-rain, referencing `--signal-bullish`/`--signal-bearish` (NOT `--buy`/`--sell` literals). Animation double-guarded under `@media (prefers-reduced-motion: no-preference)`; reduced-motion ⇒ static composition (no animation), composition preserved (D-14). Documented for calm surfaces only (auth, empty states, member overview header) — never dense data.
  </behavior>
  <action>Create `components/ui/glow.tsx` exposing the glow as a reusable tokenized affordance referencing `var(--glow)` via `box-shadow` (source the exact shadow recipe from `nexa-landing.css` `.btn-primary`/`.mark-tile`). Create `components/ui/data-rain.tsx` generalizing the landing's data-rain into a light primitive that maps `.u`/`.d` columns to `--signal-bullish`/`--signal-bearish` (via `color-mix` against the component layer, no raw literal), guards animation under `prefers-reduced-motion: no-preference`, and exposes an ambient/very-subtle variant. If a component-layer hook is needed (e.g. a `--glow`-based utility), add it to `globals.css` in the `@theme inline` component layer only — never re-declare a semantic color, never a literal. Keep both files small (<150 lines), token-only, RTL-safe (logical props), `data-testid`/ARIA-neutral. These primitives are NOT wired into any page here — waves 2 apply them. No fenced implementation in this action.</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "var(--glow)" apps/web/src/components/ui/glow.tsx` matches; `grep -nE "ring-\[|ring-[0-9]" apps/web/src/components/ui/glow.tsx` returns nothing (glow is box-shadow, never ring).
    - `grep -nE "#[0-9a-fA-F]{3,6}|--buy|--sell" apps/web/src/components/ui/data-rain.tsx` returns nothing (no literal, no legacy landing var); `grep -n "signal-bullish\|signal-bearish" apps/web/src/components/ui/data-rain.tsx` matches.
    - `grep -n "prefers-reduced-motion" apps/web/src/components/ui/data-rain.tsx` (or its referenced CSS) matches — animation is motion-guarded.
    - `pnpm --filter web test -- theme-scan` Test 3 (ring/--glow anti-collision) stays GREEN.
    - `pnpm --filter web typecheck` exits 0 (no new TS error introduced by the two new files).
  </acceptance_criteria>
  <done>Two tokenized, reduced-motion-safe, literal-free neon primitives exist under `components/ui/`, both referencing only the component token layer; theme-scan anti-collision green; typecheck clean. Not yet wired into pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → Postgres via anon RLS | Member/account/marketing pages read through `lib/supabase/server.ts` anon client; RLS is the data barrier. Reskin must not cross this to the client. |
| admin RSC → Postgres via service_role | `(admin)` pages only; `createAdminServiceClient()` is server-only and must never reach a non-admin page. |
| canvas (lwc) → CSS tokens | CandleChart reads tokens via `getComputedStyle`, not CSS cascade; a "CSS recolor" silently breaks direction colors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | member/account/marketing pages | mitigate | `rls-unchanged.test.ts` fails if a reskin imports `admin-service`/`service_role` on a non-admin page or moves a gated read to `lib/supabase/client` — written THIS plan as a blocking gate |
| T-16-02 | Tampering | CandleChart direction colors | mitigate | `lwc-recolor-intact.test.ts` asserts `getComputedStyle`/`applyOptions`/`MutationObserver` intact + zero candle color literal in CSS |
| T-16-03 | Tampering | landing theme branch | mitigate | `volt-orphan-free.test.ts` asserts zero `data-theme="volt"`/`nxl-theme-toggle`/`nexa-landing-theme` — green-only enforced before waves 2 touch the landing |
| T-16-SC | Tampering | npm/pnpm installs | mitigate | Zero new packages this phase (RESEARCH §Package Legitimacy: not applicable). Any install attempt is out-of-scope and must trigger a blocking-human checkpoint. |

**Assertion:** this plan touches ONLY test files, two new `ui/` primitive components, and (optionally) the `@theme inline` component layer of globals.css. It moves NO fetch from server to client and introduces NO service_role anywhere. The anon-RLS read vs admin service_role split (PATTERNS.md C-1/C-2) is preserved.
</threat_model>

<verification>
- `pnpm --filter web test -- rls-unchanged lwc-recolor-intact volt-orphan-free` → all GREEN.
- `pnpm --filter web test -- theme-scan` → Test 2 RED for exactly sante/dashboard/login; Test 1/3/SANITY GREEN.
- `pnpm --filter web typecheck` → 0 new errors.
- Two primitives exist, token-only, motion-guarded, not yet page-wired.
</verification>

<success_criteria>
- Guardrail set extended + 3 structural scans authored and (where expected) green.
- theme-scan RED state is exactly the 3 known residual offenders — proves the gate measures wave-2 work.
- Tokenized glow + light data-rain primitives extracted from landing source, literal-free, reduced-motion-safe.
- No page reskinned, no fetch touched, no service_role introduced.
</success_criteria>

<output>
Create `.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-01-SUMMARY.md` when done.
</output>
