---
phase: 16-reskin-transversal-de-toutes-les-pages
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/app/[locale]/(auth)/login/page.tsx
  - apps/web/src/app/[locale]/(auth)/signup/page.tsx
  - apps/web/src/app/[locale]/(account)/abonnement/page.tsx
  - apps/web/src/app/[locale]/(member)/signaux/page.tsx
  - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx
  - apps/web/src/app/[locale]/dashboard/page.tsx
  - apps/web/src/app/[locale]/affiliation/page.tsx
  - apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx
  - apps/web/src/components/signals/SignalCard.tsx
  - apps/web/src/components/signals/SignalList.tsx
  - apps/web/src/components/signals/FilterBar.tsx
  - apps/web/src/components/signals/SignalDetail.tsx
autonomous: true
requirements: [RESKIN-02, RESKIN-03, RESKIN-06]

must_haves:
  truths:
    - "Login, signup, abonnement, member signal list + trade detail, dashboard stub, affiliation, paiement-bientot are token-pure DS v3 with the Tier 2 discreet glow accent"
    - "The RLS fetch in every gated surface is unchanged: server createClient + RLS, no service_role on pages, no fetch moved client-side"
    - "Ambient data-rain appears only on calm surfaces (auth), never on signal lists / tables / trade detail"
    - "CandleChart recolor via lwc JS API is preserved (no CSS recolor introduced)"
  artifacts:
    - path: "apps/web/src/app/[locale]/(auth)/login/page.tsx"
      provides: "Tier 2 calm auth surface, text-primary (not legacy var), discreet glow, optional ambient data-rain"
      contains: "text-primary"
    - path: "apps/web/src/app/[locale]/dashboard/page.tsx"
      provides: "text-destructive replacing text-red-600 (residual offender line 58)"
    - path: "apps/web/src/app/[locale]/(member)/signaux/page.tsx"
      provides: "Token-pure dense list, readability-first, glow on cards only, fetch untouched"
  key_links:
    - from: "apps/web/src/app/[locale]/(member)/signaux/page.tsx"
      to: "lib/supabase/server.ts createClient + fetchActiveSignals"
      via: "server RLS read (DO NOT EDIT)"
      pattern: "createClient|fetchActiveSignals"
    - from: "apps/web/src/app/[locale]/(auth)/login/page.tsx"
      to: "components/ui/glow.tsx + data-rain.tsx"
      via: "Tier 2 accent on calm surface"
      pattern: "glow|DataRain"
---

<objective>
Reskin the **Tier 2 app surfaces** — auth (login/signup, RESKIN-02), compte/abonnement (RESKIN-02), espace membre (liste signaux + détail trade + chart, RESKIN-03), and paiement/funnel (paiement-bientot + dashboard stub + affiliation, RESKIN-06) — onto DS v3 dark néon with the **discreet Tier 2 accent**, while leaving every RLS fetch byte-for-byte untouched.

This is the highest-landmine plan: these are the files that contain gated reads. The reskin is className/markup ONLY — `createClient`, `fetchActiveSignals`, `requireUser/requireActiveSub`, `from(`/`.select(` are read-only do-not-edit zones (C-1/C-2). Dense surfaces (signal list, trade detail) get readability-first treatment: glow on cards only, NO data-rain. Calm surfaces (auth) are eligible for ambient data-rain. CandleChart is preserved verbatim (Pattern C / C-4).

Purpose: members and prospects experience the same green neon identity, with no weakening of the RLS barrier that protects the paid signals.
Output: 8 token-pure Tier 2 pages + 4 member components, RLS intact, CandleChart intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-CONTEXT.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-PATTERNS.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-UI-SPEC.md

<interfaces>
<!-- Read-only RLS zones (C-1) and residual offenders. Extracted from PATTERNS + grep. -->

DO-NOT-EDIT fetch zones in this plan's files:
- `(member)/signaux/page.tsx`: `const supabase = await createClient()` + `fetchActiveSignals(supabase, filters)` — anon RLS read, the paywall barrier.
- `(member)/signaux/[id]/page.tsx`: anon RLS read + hosts `CandleChartLazy`/`CandleChart`.
- `(member)/layout.tsx`, `(account)/layout.tsx`: `requireActiveSub()` / `requireUser()` gates — NOT in files_modified, do not touch.
- Never import `lib/supabase/admin-service.ts` (service_role) into any file here.

Residual offenders to fix (PATTERNS §Residual offenders):
- `app/[locale]/dashboard/page.tsx` line 58: `text-red-600` → `text-destructive`.
- `app/[locale]/(auth)/login/page.tsx` line 44: `text-[var(--accent-brand)]` → `text-primary`.

Pattern B (calm auth): swap legacy var → `text-primary`; add discreet glow on primary Button/card; ambient data-rain eligible (auth only); app titles flat tokens (no gradient, D-07).
Pattern D (Tier 2 glow): analog `components/member/ExpiryBanner.tsx` — token-bound shadcn primitives, logical props, no literal.
Pattern A Badge: `border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]` (posted/bullish), `variant="secondary" text-muted-foreground` (neutral).
CandleChart (C-4): preserve `readChartColors`/`getComputedStyle`/`applyOptions`/`MutationObserver` — change the token in globals.css if palette must change, NEVER the chart. NOT in files_modified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier 2 auth + account + funnel (login, signup, abonnement, paiement-bientot, dashboard, affiliation)</name>
  <files>apps/web/src/app/[locale]/(auth)/login/page.tsx, apps/web/src/app/[locale]/(auth)/signup/page.tsx, apps/web/src/app/[locale]/(account)/abonnement/page.tsx, apps/web/src/app/[locale]/(marketing)/paiement-bientot/page.tsx, apps/web/src/app/[locale]/dashboard/page.tsx, apps/web/src/app/[locale]/affiliation/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(auth)/login/page.tsx (current — confirm line 44 legacy var)
    - apps/web/src/app/[locale]/dashboard/page.tsx (current — confirm line 58 text-red-600)
    - apps/web/src/app/[locale]/(account)/abonnement/page.tsx (current — confirm requireUser gate is in layout, this page is render-only)
    - apps/web/src/app/[locale]/affiliation/dashboard/page.tsx (already-tokenized analog from Phase 15 — Pattern A)
    - apps/web/src/components/ui/glow.tsx and data-rain.tsx (plan 01 primitives)
    - apps/web/src/components/member/ExpiryBanner.tsx (Pattern D Tier 2 glow analog)
    - apps/web/src/styles/__tests__/rls-unchanged.test.ts (the gate; these files must not import service_role/migrate fetch)
  </read_first>
  <action>Fix the two residual offenders: `dashboard/page.tsx` line 58 `text-red-600`→`text-destructive`; `(auth)/login/page.tsx` line 44 `text-[var(--accent-brand)]`→`text-primary`. Apply the swap law to any other literal in these 6 files (`ring-[#2563EB]`→`ring-ring`, raw palette → semantic token). Apply the **Tier 2 discreet glow** (Pattern D) on the primary Button / card / focus of each surface via the glow primitive (box-shadow `var(--glow)`, never `ring-*`) so no page is "tokenized but fade" (C-6). On the **calm auth surfaces** (login, signup) add the **ambient, very-subtle data-rain** primitive (Pattern F) — reduced-motion guarded — but NOT on abonnement/dashboard if they show dense data. App titles use flat tokens (`text-foreground`/`text-primary`), never gradient (D-07). Keep `<Disclaimer />` where already rendered; any % via `applyThreshold` + N + provenance. These pages are render/markup only — do NOT touch the `requireUser`/`requireActiveSub` gates (they live in layouts) and import NO service_role. Logical RTL props only; preserve all `data-testid`/ARIA; inherit i18n copy unchanged.</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan rls-unchanged no-perf-claims no-mera-brand rtl-logical-props && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "text-destructive" apps/web/src/app/[locale]/dashboard/page.tsx` matches and `grep -n "text-red-600" …` returns nothing.
    - `grep -n "text-primary" apps/web/src/app/[locale]/(auth)/login/page.tsx` matches and `grep -n "var(--accent-brand)" …` returns nothing.
    - `pnpm --filter web test -- theme-scan` GREEN (login + dashboard now pass FOUNDATION_FILES checks added in plan 01).
    - `pnpm --filter web test -- rls-unchanged` GREEN (no service_role / admin-service / client-migrated fetch in these pages).
    - `pnpm --filter web test -- no-perf-claims no-mera-brand rtl-logical-props` GREEN; `pnpm lint:i18n` exits 0.
    - data-rain present on login/signup only — `grep -rln "DataRain\|data-rain" apps/web/src/app/[locale]/(auth)` matches; same grep on `(account)/abonnement` and member files returns nothing.
    - `git diff` shows no gate call (`requireUser`/`requireActiveSub`) added/removed and no `data-testid`/ARIA change.
  </acceptance_criteria>
  <done>6 auth/account/funnel surfaces are token-pure DS v3 with Tier 2 glow; ambient data-rain on auth only; residual offenders fixed; RLS/gates untouched; guard scans green.</done>
</task>

<task type="auto">
  <name>Task 2: Tier 2 member dense surfaces (signal list + trade detail + member components), readability-first, fetch + CandleChart untouched</name>
  <files>apps/web/src/app/[locale]/(member)/signaux/page.tsx, apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx, apps/web/src/components/signals/SignalCard.tsx, apps/web/src/components/signals/SignalList.tsx, apps/web/src/components/signals/FilterBar.tsx, apps/web/src/components/signals/SignalDetail.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (current — locate the createClient + fetchActiveSignals block to NOT touch; find any residual literal in markup)
    - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx (current — locate CandleChart host; do not touch fetch)
    - apps/web/src/components/signals/CandleChart.tsx (read-only reference — confirm getComputedStyle/applyOptions/MutationObserver intact; NOT modified)
    - apps/web/src/components/signals/SignalCard.tsx, SignalList.tsx, FilterBar.tsx, SignalDetail.tsx (current — find raw-palette literals; these render the dense list)
    - apps/web/src/app/(admin)/signaux/page.tsx (Pattern A token-pure analog + Badge pattern)
    - apps/web/src/styles/__tests__/lwc-recolor-intact.test.ts and rls-unchanged.test.ts (the gates)
  </read_first>
  <action>Tokenize the dense member surfaces: replace any raw-palette literal in the two pages and the four `signals/` components per the swap law — bullish/posted Badge → `border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]`, neutral → `variant="secondary" text-muted-foreground`, loss/negative → `text-destructive`, focus `ring-[#2563EB]`→`ring-ring`. Apply the **Tier 2 accent as readability-first** (C-6/D-05): discreet glow on **cards/CTA only** (SignalCard, the detail action button) via the glow primitive — **NO data-rain, NO gradient titles, NO section aura** on lists/tables/trade detail. The RLS fetch in `signaux/page.tsx` (`createClient` + `fetchActiveSignals`) and `signaux/[id]/page.tsx` is a **read-only zone — do not edit, do not move under `'use client'`, do not import service_role**. **Do NOT modify CandleChart.tsx** — it is already D-11 compliant (Pattern C); if a candle color must change, change the token in globals.css, never the chart. Any % via `applyThreshold` + N + provenance; render `<SignalsDisclaimerBanner />`/`<Disclaimer />` where already present. Logical RTL props only; numeric/symbol in `<bdi>`; preserve all `data-testid`/ARIA; inherit i18n copy.</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan rls-unchanged lwc-recolor-intact no-perf-claims rtl-logical-props && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- rls-unchanged` GREEN; `git diff apps/web/src/app/[locale]/(member)/signaux/page.tsx` shows the `createClient`/`fetchActiveSignals` lines unchanged.
    - `pnpm --filter web test -- lwc-recolor-intact` GREEN and `git diff apps/web/src/components/signals/CandleChart.tsx` is empty (chart untouched).
    - `pnpm --filter web test -- theme-scan` GREEN (member surfaces + components literal-free).
    - `grep -rln "DataRain\|data-rain" apps/web/src/components/signals apps/web/src/app/[locale]/(member)` returns nothing (no data-rain on dense data).
    - `pnpm --filter web test -- no-perf-claims rtl-logical-props` GREEN; `pnpm lint:i18n` exits 0.
    - `git diff` shows no `data-testid`/ARIA role removed or renamed (E2E gating/auth specs preserved).
  </acceptance_criteria>
  <done>Member list + trade detail + signal components are token-pure DS v3, readability-first with glow on cards only; RLS fetch and CandleChart verbatim-preserved; guard scans green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres anon RLS | Member/account pages read paid signals through `lib/supabase/server.ts` anon client; RLS `has_active_subscription()` is the paywall. Must not move client-side. |
| canvas (lwc) → CSS tokens | CandleChart reads `--signal-bullish`/`--signal-bearish` via `getComputedStyle`; a CSS recolor silently breaks direction colors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure / IDOR | `(member)/signaux` list + `[id]` detail | mitigate | `rls-unchanged.test.ts` (plan 01) fails on any service_role import or client-migrated fetch; this plan edits className/markup only, fetch block byte-identical in diff |
| T-16-03-02 | Tampering | CandleChart direction colors | mitigate | CandleChart NOT in files_modified; `lwc-recolor-intact.test.ts` asserts getComputedStyle/applyOptions/MutationObserver intact + zero candle color literal |
| T-16-03-03 | Information Disclosure | auth pages | mitigate | login/signup are render-only; gates live in layouts (not modified); no service_role imported |
| T-16-03-SC | Tampering | pnpm installs | mitigate | zero new packages; install attempt → blocking-human checkpoint |

**Assertion:** this plan touches the files that hold the gated reads but edits className/markup ONLY. The server-`createClient` + RLS read stays server-side; no fetch is moved to the client; no service_role reaches a member/account page. The anon-RLS read vs admin service_role split (C-1/C-2) is preserved, and CandleChart's lwc recolor (C-4) is untouched.
</threat_model>

<verification>
- rls-unchanged + lwc-recolor-intact + theme-scan + no-perf-claims + rtl-logical-props GREEN.
- `pnpm lint:i18n` + `pnpm --filter web typecheck` clean.
- CandleChart diff empty; member fetch diff empty; data-rain absent on dense surfaces.
</verification>

<success_criteria>
- RESKIN-02/03/06 met: auth, compte/abonnement, espace membre (list + detail + chart), paiement/funnel on DS v3 dark néon, gating RLS + anti-IDOR unchanged.
- Tier 2 discreet glow applied; data-rain only on calm auth; CandleChart preserved.
- All guard + structural scans green.
</success_criteria>

<output>
Create `.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-03-SUMMARY.md` when done.
</output>
