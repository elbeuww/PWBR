---
phase: 16-reskin-transversal-de-toutes-les-pages
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/components/landing/NexaLanding.tsx
  - apps/web/src/components/landing/NexaLandingEffects.tsx
  - apps/web/src/components/landing/nexa-landing.css
  - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx
  - apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx
  - apps/web/src/app/[locale]/(marketing)/legal/[doc]/page.tsx
autonomous: true
requirements: [RESKIN-01]

must_haves:
  truths:
    - "The landing renders green-only: data-theme=green hardcoded, Green/Volt toggle and theme-persistence logic removed"
    - "No volt orphan string survives under apps/web/src (data-theme=volt / nxl-theme-toggle / nexa-landing-theme)"
    - "Tarifs, méthodologie and légal are token-pure DS v3 with the Tier 1 vitrine accent applied"
    - "<Disclaimer /> and no-perf-claims preserved on every vitrine surface"
  artifacts:
    - path: "apps/web/src/components/landing/NexaLanding.tsx"
      provides: "Green-only landing root, toggle markup deleted"
      contains: "data-theme=\"green\""
    - path: "apps/web/src/components/landing/nexa-landing.css"
      provides: "Only the green branch live; volt block neutralized"
    - path: "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"
      provides: "Token-pure Tier 1 pricing surface, Disclaimer + measured % preserved"
  key_links:
    - from: "apps/web/src/components/landing/NexaLanding.tsx"
      to: "nexa-landing.css green branch"
      via: "data-theme=green selector"
      pattern: "data-theme=\"green\""
    - from: "apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx"
      to: "applyThreshold (@app/core)"
      via: "any % rendered with N + provenance"
      pattern: "applyThreshold"
---

<objective>
Reskin the **Tier 1 vitrine** (RESKIN-01) onto DS v3 dark néon, single pass, and reconcile the landing from the Green/Volt micro-world to **green-only** (D-01/D-02/D-03).

Two distinct jobs: (1) landing volt→green reconcile + orphan cleanup across `NexaLanding.tsx`, `NexaLandingEffects.tsx`, `nexa-landing.css` (Pattern E); (2) finish tokenizing the non-landing vitrine pages — tarifs, méthodologie, légal — and apply the Tier 1 full-neon accent (glow/gradient/aura already landing-scoped; pages get token purity + the rich vitrine treatment, not "tokenized but fade").

Purpose: one color identity (green) everywhere including the landing; the showcase surfaces carry the full neon signature while every guardrail (Disclaimer, no-perf-claims, RTL, data-testid) stays intact.
Output: green-only landing with zero volt orphans + 3 token-pure Tier 1 pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-CONTEXT.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-PATTERNS.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-UI-SPEC.md

<interfaces>
<!-- Exact orphan locations (grep-verified) the executor must remove. -->

apps/web/src/components/landing/NexaLanding.tsx:
- line 58: `<div className="nxl" data-theme="volt">` → `data-theme="green"`
- lines 84-86: `<div className="nxl-theme-toggle" aria-label="Green / Volt">` + two `<button data-theme=...>` → DELETE the whole toggle block.

apps/web/src/components/landing/NexaLandingEffects.tsx:
- lines 27-29, 34, 36: `root.setAttribute('data-theme', t)`, `localStorage.setItem/getItem('nexa-landing-theme', ...)`, `querySelectorAll('.nxl-theme-toggle button')` listeners, `savedTheme = ... || 'volt'` → DELETE theme logic.
- KEEP: data-rain injection, parallax, tilt, reveal/counters/gauge, progress/nav (none reference theme; all reduced-motion guarded).

apps/web/src/components/landing/nexa-landing.css:
- lines 40-58: `.nxl[data-theme="volt"] { … }` → DELETE.
- lines 83, 85: `.nxl[data-theme="volt"] .mark-tile { … }`, `… svg { … }` → DELETE.
- lines 96-98: `.nxl .nxl-theme-toggle { … }` + button rules → DELETE (toggle markup gone).
- KEEP lines 19-37: `.nxl[data-theme="green"] { … }` — the only live branch (D-03).

Pattern A token usage (analog `app/(admin)/signaux/page.tsx`): `text-foreground`, `text-muted-foreground`, `text-primary`, `bg-background`, `border-border`, Badge `border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]`, numeric/symbol in `<bdi>`.
Disclaimer single source: `components/Disclaimer.tsx` — render `<Disclaimer />`, never inline copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing volt→green reconcile + orphan cleanup (Pattern E)</name>
  <files>apps/web/src/components/landing/NexaLanding.tsx, apps/web/src/components/landing/NexaLandingEffects.tsx, apps/web/src/components/landing/nexa-landing.css</files>
  <read_first>
    - apps/web/src/components/landing/NexaLanding.tsx (current — confirm line 58 root + lines 84-86 toggle block)
    - apps/web/src/components/landing/NexaLandingEffects.tsx (current — confirm theme logic lines 25-40, identify which effects to KEEP)
    - apps/web/src/components/landing/nexa-landing.css (current — confirm volt block + toggle CSS + green branch boundaries)
    - apps/web/src/styles/__tests__/volt-orphan-free.test.ts (the gate this task must turn/keep green)
  </read_first>
  <action>In `NexaLanding.tsx` set the root to `data-theme="green"` (line 58) and DELETE the entire `nxl-theme-toggle` block (lines 84-86 incl. the two `<button>`). In `NexaLandingEffects.tsx` strip the theme branch: remove `setAttribute('data-theme', …)`, both `localStorage` calls for `nexa-landing-theme`, the `querySelectorAll('.nxl-theme-toggle button')` listeners + their cleanups, and the `savedTheme … || 'volt'` default; KEEP data-rain injection, parallax, tilt, reveal/counters/gauge, progress/nav untouched (they are reduced-motion guarded and theme-agnostic). In `nexa-landing.css` DELETE the `.nxl[data-theme="volt"]` rule block (40-58), the two volt `.mark-tile` overrides (83, 85), and the `.nxl .nxl-theme-toggle` rules (96-98); KEEP `.nxl[data-theme="green"]` (19-37) as the sole live branch (D-03). Remove the now-stale Green/Volt mention in the file header comment (line ~3) so it reads green-only. Do not retrofit landing internal spacing (self-contained scoped surface). green is the only live branch.</action>
  <verify>
    <automated>pnpm --filter web test -- volt-orphan-free && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- volt-orphan-free` exits 0 (zero `data-theme="volt"` / `nxl-theme-toggle` / `nexa-landing-theme` under apps/web/src).
    - `grep -rn 'data-theme="volt"\|nxl-theme-toggle\|nexa-landing-theme' apps/web/src` returns nothing.
    - `grep -n 'data-theme="green"' apps/web/src/components/landing/NexaLanding.tsx` matches the root.
    - `pnpm --filter web typecheck` exits 0; `pnpm lint:i18n` exits 0 (no new hardcoded text).
    - data-rain/parallax/tilt effects still present in NexaLandingEffects.tsx (grep shows their injection blocks retained).
  </acceptance_criteria>
  <done>Landing renders green-only; toggle markup, theme-persistence logic and volt CSS deleted; non-theme effects preserved; volt-orphan-free gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize + Tier-1-accent tarifs, méthodologie, légal</name>
  <files>apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx, apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx, apps/web/src/app/[locale]/(marketing)/legal/[doc]/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/signaux/page.tsx (Pattern A token-pure analog)
    - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx (current — find any raw-palette literal + where % is rendered)
    - apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx (current)
    - apps/web/src/app/[locale]/(marketing)/legal/[doc]/page.tsx (current)
    - apps/web/src/components/ui/glow.tsx (Tier-1/2 glow primitive from plan 01 — for popular price card / CTA accent)
    - apps/web/src/components/Disclaimer.tsx (single source — must render on tarifs)
    - apps/web/src/styles/__tests__/theme-scan.test.ts (the gate; these surfaces must stay/become literal-free)
  </read_first>
  <action>For each page, replace any raw-palette / color-literal utility with the DS v3 component-layer token equivalent per the swap law: `text-emerald-*`→`text-[--signal-bullish]`, loss/negative→`text-destructive`, `border-emerald-600/30 bg-emerald-500/10`→`border-border bg-muted`, any `text-[var(--accent-brand)]`/legacy var→`text-primary`, focus `ring-[#2563EB]`→`ring-ring`. Apply the **Tier 1 full-neon accent** (C-6): popular price card border + glow and primary CTAs use the glow primitive (box-shadow `var(--glow)`, never `ring-*`); vitrine eyebrow/accent links use `--primary`; pricing checkmarks/badges via the signal/primary tokens. Keep `<Disclaimer />` rendered on tarifs (and any vitrine page that already had it) — do NOT inline disclaimer copy. Any % stays routed through `applyThreshold` (`@app/core`) with N + provenance — introduce no naked %, no gain baseline. Preserve every `data-testid`/ARIA role and use logical RTL props only (`ps/pe/ms/me/text-start`). Inherit existing i18n copy unchanged (FR/EN/AR), introduce no hardcoded string.</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan no-perf-claims no-mera-brand rtl-logical-props && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- theme-scan` GREEN (these surfaces carry zero raw-palette literal).
    - `pnpm --filter web test -- no-perf-claims no-mera-brand` GREEN.
    - `pnpm --filter web test -- rtl-logical-props` GREEN (no physical-direction utility introduced).
    - `pnpm lint:i18n` exits 0 (no hardcoded text).
    - `grep -n "<Disclaimer" apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx` matches (Disclaimer preserved).
    - `grep -nE "text-(emerald|red|amber)-[0-9]|ring-\[#|var\(--accent-brand\)" apps/web/src/app/[locale]/(marketing)/{tarifs,methodologie,legal/[doc]}/page.tsx` returns nothing.
    - Tier 1 accent present: glow primitive referenced on at least the tarifs popular-card / CTA (grep for the glow import/className).
    - `git diff` shows no `data-testid`/ARIA role removed or renamed.
  </acceptance_criteria>
  <done>Tarifs, méthodologie, légal are token-pure DS v3 with the Tier 1 vitrine accent applied; Disclaimer + measured-% + RTL + i18n + E2E hooks preserved; guard scans green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres anon RLS | Vitrine pages are public/marketing; tarifs/légal read static or anon data. No gated fetch may move client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Information Disclosure | marketing pages | mitigate | `rls-unchanged.test.ts` (plan 01) fails if any marketing page imports service_role/admin-service or migrates a fetch to client; this plan touches className/markup only |
| T-16-02-02 | Tampering | landing theme branch | mitigate | `volt-orphan-free.test.ts` enforced in Task 1; green is the only live branch |
| T-16-02-03 | Spoofing/Repudiation | perf claims on tarifs | mitigate | `no-perf-claims` green; any % via `applyThreshold` + N + provenance (D-13), no gain baseline |
| T-16-02-SC | Tampering | pnpm installs | mitigate | zero new packages; any install triggers blocking-human checkpoint |

**Assertion:** reskin of the vitrine touches className/markup ONLY. No fetch migrated server→client, no service_role introduced. The anon-RLS vs admin service_role split (C-1/C-2) is preserved.
</threat_model>

<verification>
- volt-orphan-free + theme-scan + no-perf-claims + no-mera-brand + rtl-logical-props GREEN.
- `pnpm lint:i18n` + `pnpm --filter web typecheck` clean.
- Landing green-only; 3 vitrine pages token-pure with Tier 1 accent + Disclaimer preserved.
</verification>

<success_criteria>
- RESKIN-01 met: vitrine (accueil/landing, tarifs, méthodologie, légal) on DS v3 dark néon, trilingual, Disclaimer + no-perf-claims preserved.
- Green-only landing, zero volt orphan.
- Tier 1 accent applied (not fade); all guardrails green.
</success_criteria>

<output>
Create `.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-02-SUMMARY.md` when done.
</output>
