---
phase: 16-reskin-transversal-de-toutes-les-pages
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/app/[locale]/(marketing)/academie/page.tsx
  - apps/web/src/app/[locale]/(marketing)/academie/[slug]/page.tsx
  - apps/web/src/app/[locale]/(marketing)/academie/[slug]/[lesson]/page.tsx
  - apps/web/src/components/academie/ContentCard.tsx
  - apps/web/src/components/academie/FallbackBanner.tsx
  - apps/web/src/components/academie/Callout.tsx
  - apps/web/src/app/(admin)/sante/page.tsx
  - apps/web/src/app/(admin)/membres/page.tsx
  - apps/web/src/app/(admin)/signaux/[id]/page.tsx
  - apps/web/src/app/(admin)/affiliation/affilies/page.tsx
autonomous: true
requirements: [RESKIN-04, RESKIN-05]

must_haves:
  truths:
    - "D-04: Académie index + article + leçon are token-pure DS v3 Tier 2 (discreet accent); RTL and FR fallback preserved (FallbackBanner intact); D-08: component-layer var() only; D-09: per-surface DoD honored"
    - "D-10: verifier grid mechanical scans — admin residual offenders are tokenized: sante status dots no longer use bg-emerald-500/bg-amber-500"
    - "D-04: admin is Tier 3 sober; D-05: admin tables readability-first — tokens + components + minimal neon borders, zero effect (no glow/animation/data-rain)"
    - "D-12: service_role stays admin-only (admin service_role server-only); no admin-service import reaches Académie (marketing) pages"
  artifacts:
    - path: "apps/web/src/app/(admin)/sante/page.tsx"
      provides: "Status dots via --signal-bullish / --risk-moderate tokens (residual offender lines 65-66 fixed)"
    - path: "apps/web/src/app/[locale]/(marketing)/academie/page.tsx"
      provides: "Token-pure Tier 2 Académie index, RTL + FR fallback preserved"
    - path: "apps/web/src/components/academie/FallbackBanner.tsx"
      provides: "FR-fallback banner preserved, tokenized"
  key_links:
    - from: "apps/web/src/app/(admin)/sante/page.tsx"
      to: "globals.css signal/risk tokens"
      via: "status dot className using token utilities"
      pattern: "signal-bullish|risk-moderate|bg-primary"
    - from: "apps/web/src/app/[locale]/(marketing)/academie/[slug]/page.tsx"
      to: "components/academie/FallbackBanner.tsx"
      via: "FR fallback preserved (RESKIN-04)"
      pattern: "FallbackBanner"
---

<objective>
Reskin the **Académie** (Tier 2, RESKIN-04) and the **back-office `/admin`** (Tier 3 sober, RESKIN-05) onto DS v3 dark néon, single pass.

Académie is the trilingual content surface: tokenize index/article/leçon + key MDX components, apply the Tier 2 discreet accent, and rigorously preserve RTL logical properties + the FR fallback (`FallbackBanner`). Admin is **sober** (Phase 11 D-18, do NOT re-decide): tokens + components + minimal neon borders, **zero effect** — no hero, no glow, no data-rain. The net-new admin work is the 3 residual/likely offenders (sante status dots, membres, signaux/[id], affiliation/affilies); the other admin pages were already tokenized in Phase 15 (verify-only).

Purpose: the educational surface and the operator cockpit both carry the green identity at their correct intensity, with Académie's RTL/FR-fallback contract and admin's service_role-only barrier intact.
Output: 3 Académie pages + 3 Académie components (Tier 2) + 4 admin surfaces tokenized (Tier 3 sober).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-CONTEXT.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-PATTERNS.md
@.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-UI-SPEC.md

<interfaces>
<!-- Residual offenders + admin-only constraint. Extracted from PATTERNS + grep. -->

Admin residual offenders (PATTERNS §Residual offenders + File Classification):
- `app/(admin)/sante/page.tsx` lines 65-66: `bg-emerald-500`, `bg-amber-500` (status dots / DOT_CLASS) → `bg-[--signal-bullish]`, `bg-[--risk-moderate]` (or `bg-primary` / amber risk token).
- `app/(admin)/membres/page.tsx`, `app/(admin)/signaux/[id]/page.tsx`, `app/(admin)/affiliation/affilies/page.tsx`: likely-not-yet-tokenized (A1) — run the swap law for any raw-palette literal.
- Already tokenized Phase 15 (verify-only, NOT in files_modified): `(admin)/{signaux,page,sante header,affiliation,affiliation/payouts,file}` foundation entries — sante is included here ONLY for the dot offender.

Admin is service_role (admin-only, C-2): `createAdminServiceClient()` is correct here and stays. Do NOT touch the fetch; reskin className/markup only. Admin is mono-FR (no i18n key work), Tier 3 sober (Phase 11 D-18): zero effect.

Académie (Tier 2, RESKIN-04): preserve `FallbackBanner` + FR fallback; logical RTL props only (`ps/pe/ms/me/inset-inline-*/text-start`); MDX content rendered via `components/academie/*`. Pattern A analog: `app/(admin)/signaux/page.tsx`.
Pattern A token usage + Badge pattern as in plan 02/03. Disclaimer single source where applicable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize + Tier-2-accent Académie (index, article, leçon + ContentCard, FallbackBanner, Callout), RTL + FR fallback preserved</name>
  <files>apps/web/src/app/[locale]/(marketing)/academie/page.tsx, apps/web/src/app/[locale]/(marketing)/academie/[slug]/page.tsx, apps/web/src/app/[locale]/(marketing)/academie/[slug]/[lesson]/page.tsx, apps/web/src/components/academie/ContentCard.tsx, apps/web/src/components/academie/FallbackBanner.tsx, apps/web/src/components/academie/Callout.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/academie/page.tsx (current — find raw-palette literals)
    - apps/web/src/app/[locale]/(marketing)/academie/[slug]/page.tsx (current — confirm FallbackBanner usage + FR fallback path)
    - apps/web/src/app/[locale]/(marketing)/academie/[slug]/[lesson]/page.tsx (current)
    - apps/web/src/components/academie/{ContentCard,FallbackBanner,Callout}.tsx (current — find literals; these render content cards/callouts)
    - apps/web/src/app/(admin)/signaux/page.tsx (Pattern A token-pure analog)
    - apps/web/src/components/ui/glow.tsx (Tier 2 discreet glow for content cards)
    - apps/web/src/styles/__tests__/{theme-scan,rtl-logical-props,rls-unchanged}.test.ts (the gates)
  </read_first>
  <action>Apply the swap law to any raw-palette / color-literal utility in the 3 Académie pages and the 3 components: `text-emerald-*`→`text-[--signal-bullish]` or `text-primary` for accent, raw `border-*/bg-*` palette → `border-border`/`bg-muted`/`bg-secondary`, `ring-[#2563EB]`→`ring-ring`. Apply the **Tier 2 discreet accent** (C-6): glow on content cards / primary CTA via the glow primitive (box-shadow `var(--glow)`, never `ring-*`) — readability-first, NO section aura, NO gradient titles (D-07), NO data-rain on content lists. **Preserve `FallbackBanner` and the FR-fallback behaviour exactly** (RESKIN-04) — do not alter the fallback logic, only its tokens. Logical RTL props ONLY (`ps/pe/ms/me/inset-inline-*/text-start`) — no physical-direction utility may slip in (Pitfall 5). Keep `<Disclaimer />` where present; inherit i18n copy unchanged (no hardcoded text). Preserve all `data-testid`/ARIA (academie E2E spec). Do not touch any data fetch.</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan rtl-logical-props rls-unchanged no-perf-claims no-mera-brand && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- theme-scan` GREEN (Académie pages + components literal-free).
    - `pnpm --filter web test -- rtl-logical-props` GREEN (no physical-direction utility introduced).
    - `grep -n "FallbackBanner" apps/web/src/app/[locale]/(marketing)/academie/[slug]/page.tsx` matches (FR fallback preserved).
    - `grep -rnE "text-(emerald|red|amber)-[0-9]|ring-\[#|pl-|pr-|ml-|mr-|text-left|text-right" apps/web/src/app/[locale]/(marketing)/academie apps/web/src/components/academie` returns nothing (tokens + logical props only).
    - `pnpm --filter web test -- rls-unchanged no-perf-claims no-mera-brand` GREEN; `pnpm lint:i18n` exits 0.
    - Tier 2 accent present: glow primitive referenced on Académie content cards/CTA (grep matches).
    - `git diff` shows no `data-testid`/ARIA change and no fallback-logic change.
  </acceptance_criteria>
  <done>Académie index/article/leçon + components are token-pure DS v3 Tier 2 with discreet glow; RTL logical props + FR fallback preserved; guard scans green.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize admin residual offenders Tier 3 sober (sante dots, membres, signaux/[id], affiliation/affilies)</name>
  <files>apps/web/src/app/(admin)/sante/page.tsx, apps/web/src/app/(admin)/membres/page.tsx, apps/web/src/app/(admin)/signaux/[id]/page.tsx, apps/web/src/app/(admin)/affiliation/affilies/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/sante/page.tsx (current — confirm lines 65-66 bg-emerald-500/bg-amber-500 status dots)
    - apps/web/src/app/(admin)/membres/page.tsx (current — find raw-palette literals)
    - apps/web/src/app/(admin)/signaux/[id]/page.tsx (current — find literals)
    - apps/web/src/app/(admin)/affiliation/affilies/page.tsx (current — find literals)
    - apps/web/src/app/(admin)/signaux/page.tsx (already-tokenized Phase 15 analog — Pattern A; copy its token usage + Badge pattern)
    - apps/web/src/styles/__tests__/theme-scan.test.ts (these 3 surfaces are now in FOUNDATION_FILES from plan 01)
  </read_first>
  <action>Fix the sante status-dot offenders (lines 65-66): `bg-emerald-500`→`bg-[--signal-bullish]` (or `bg-primary` for healthy), `bg-amber-500`→`bg-[--risk-moderate]` (amber risk token) — semantic status, not raw palette. Apply the swap law to any raw-palette / color-literal utility in `membres`, `signaux/[id]`, `affiliation/affilies`: status pills → Pattern A Badge (`border-[--signal-bullish]/30 bg-[--signal-bullish]/10 text-[--signal-bullish]` or `variant="secondary" text-muted-foreground`), loss/destructive → `text-destructive`, `ring-[#2563EB]`→`ring-ring`. **Tier 3 sober (Phase 11 D-18, do NOT re-decide): tokens + components + minimal neon BORDER only — zero effect.** No glow primitive, no data-rain, no gradient, no animation on admin. The fetch is `createAdminServiceClient()` and is CORRECT here (admin-only, C-2) — do NOT touch it, do NOT remove the admin-service import. Admin is mono-FR — no i18n key work, but introduce no hardcoded text beyond existing. Preserve all `data-testid`/ARIA; logical RTL props (admin is FR-LTR but keep logical for consistency).</action>
  <verify>
    <automated>pnpm --filter web test -- theme-scan rls-unchanged no-perf-claims no-mera-brand && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "bg-emerald-500|bg-amber-500" apps/web/src/app/(admin)/sante/page.tsx` returns nothing; status dots use `--signal-bullish`/`--risk-moderate`/`bg-primary` tokens (grep matches).
    - `pnpm --filter web test -- theme-scan` GREEN — Test 2 now passes for sante, membres, signaux/[id], affilies (added to FOUNDATION_FILES in plan 01).
    - `pnpm --filter web test -- rls-unchanged` GREEN AND the admin-service import is still present in these admin pages (grep confirms — service_role stays admin-only, not removed).
    - No glow/data-rain on admin: `grep -rln "DataRain\|data-rain\|var(--glow)" apps/web/src/app/(admin)/{sante,membres,signaux/[id],affiliation/affilies}/page.tsx` returns nothing (Tier 3 sober).
    - `pnpm --filter web test -- no-perf-claims no-mera-brand` GREEN; `pnpm --filter web typecheck` exits 0.
    - `git diff` shows no `createAdminServiceClient`/fetch change and no `data-testid`/ARIA change.
  </acceptance_criteria>
  <done>Admin residual offenders tokenized Tier 3 sober (zero effect); sante status dots semantic; service_role admin-only fetch untouched; theme-scan + structural scans green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin RSC → Postgres service_role | `(admin)` pages use `createAdminServiceClient()` (server-only). Must stay admin-only; must never leak into a marketing/Académie page. |
| client → Postgres anon | Académie (marketing) is public/anon; no gated fetch may move client-side or pull service_role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Information Disclosure | Académie (marketing) pages | mitigate | `rls-unchanged.test.ts` (plan 01) fails if an Académie page imports admin-service/service_role; this task edits className/markup only |
| T-16-04-02 | Elevation of Privilege | admin back-office | mitigate | `requireRole('superadmin')`→`notFound()` lives in `(admin)/layout.tsx` (NOT modified); service_role fetch untouched; reskin is className-only |
| T-16-04-03 | Information Disclosure | admin existence leak | accept/preserve | 404-discret gate is pre-existing in the admin layout; this plan does not touch routing/gating |
| T-16-04-SC | Tampering | pnpm installs | mitigate | zero new packages; install attempt → blocking-human checkpoint |

**Assertion:** this plan touches className/markup ONLY. The admin `service_role` fetch stays admin-only and untouched; no service_role reaches an Académie/marketing page; no fetch is moved server→client. The anon-RLS read vs admin service_role split (C-1/C-2) is preserved.
</threat_model>

<verification>
- theme-scan + rls-unchanged + rtl-logical-props + no-perf-claims + no-mera-brand GREEN.
- `pnpm lint:i18n` + `pnpm --filter web typecheck` clean.
- Académie FR fallback preserved; admin Tier 3 sober (no glow/data-rain); service_role admin-only intact.
</verification>

<success_criteria>
- RESKIN-04 met: Académie (index + article + cours/leçon) on DS v3, RTL + FR fallback preserved.
- RESKIN-05 met: `/admin` on DS v3 Tier 3 sober, residual offenders tokenized, zero effect.
- service_role stays admin-only; all guard + structural scans green.
</success_criteria>

<output>
Create `.planning/phases/16-reskin-transversal-de-toutes-les-pages/16-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
