---
phase: 09-cms-cours-articles-vulgaris-s
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/app/[locale]/(marketing)/page.tsx
  - apps/web/src/components/signals/SignalDetail.tsx
  - apps/web/e2e/academie.spec.ts
autonomous: false
requirements: [CMS-01, LEGAL-01]
user_setup: []

must_haves:
  truths:
    - "L'entrée « Académie » figure en permanence dans la nav principale de la vitrine (D-08c)"
    - "La home marketing a un bloc « Apprenez les bases » → /academie (D-08a)"
    - "Le détail signal a un lien contextuel « Comment exécuter ce signal ? » → cours plateforme (D-08b)"
    - "L'E2E Académie prouve en Vercel preview : index lisible, article + leçon rendus, disclaimer présent, RTL arabe"
  artifacts:
    - path: "apps/web/src/app/[locale]/layout.tsx"
      provides: "entrée nav Académie (i18n, localisée)"
      contains: "navAcademy"
    - path: "apps/web/e2e/academie.spec.ts"
      provides: "E2E Playwright Académie (exécuté en preview)"
      contains: "academie"
  key_links:
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/i18n/navigation"
      via: "Link localisé vers /academie (préserve la locale)"
      pattern: "i18n/navigation"
    - from: "apps/web/src/components/signals/SignalDetail.tsx"
      to: "/academie"
      via: "lien contextuel funnel D-08b"
      pattern: "academie"
---

<objective>
Câbler l'Académie dans le funnel (D-08) : entrée permanente dans la nav vitrine, bloc « Apprenez les bases » sur la home, lien contextuel « Comment exécuter ce signal ? » dans le détail signal. Écrire l'E2E Playwright Académie et établir le checkpoint de vérification du RENDU MDX en Vercel preview (le seul moyen fiable — le `next build` local est non-viable, Pitfall 2).

Purpose: rend l'Académie atteignable depuis le produit et ferme la boucle de validation. Le rendu MDX réel (composants pédago, prose, RTL arabe, disclaimer) ne se prouve qu'en preview.
Output: 3 surfaces hôtes modifiées (liens i18n localisés) + E2E + vérification preview signée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-PATTERNS.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-VALIDATION.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-04-SUMMARY.md

<interfaces>
<!-- Analogs réels -->
From apps/web/src/app/[locale]/layout.tsx (header L52-58):
- `<header className="flex h-14 items-center justify-between bg-secondary px-4 md:px-6">`
- `<span className="font-semibold">Vétéran Trading</span> {/* i18n-ignore: marque */}`
- `<div className="ms-auto flex items-center gap-2"><ThemeToggle/><LanguageSwitcher/></div>`
- ATTENTION : ce layout est le SEUL `<html lang dir>` (Pitfall 7) — ajouter UNIQUEMENT l'entrée nav, ne pas toucher au reste.

From apps/web/src/i18n/navigation.ts : `Link` localisé (préserve locale, localePrefix always)
From Plan 04 : namespace `academy` posé (`navAcademy`, `learnBasicsCta`, `signalExecLink`) — consommer, ne pas redéclarer.
E2E : apps/web/e2e/*.spec.ts (analogs i18n.spec / gating.spec) ; pattern human-verify GREEN en preview (précédent D-01-04-C, D-07-04-C — E2E authoré, exécution = human-verify avec dev/preview).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liens funnel D-08 (nav + home + détail signal)</name>
  <files>apps/web/src/app/[locale]/layout.tsx, apps/web/src/app/[locale]/(marketing)/page.tsx, apps/web/src/components/signals/SignalDetail.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/layout.tsx (header L52-58 — point d'insertion nav ; NE PAS toucher au html/dir/providers)
    - apps/web/src/app/[locale]/(marketing)/page.tsx (home — point d'insertion bloc « Apprenez les bases »)
    - apps/web/src/components/signals/SignalDetail.tsx (point d'insertion lien contextuel)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md §Layout 6 (liens funnel) + §Copywriting (navAcademy/learnBasicsCta/signalExecLink)
  </read_first>
  <action>
    - `layout.tsx` (D-08c) : ajouter dans le `<header>` une entrée nav `<Link href="/academie">{t('navAcademy')}</Link>` via `@/i18n/navigation` (libellé i18n `academy.navAcademy`, classes logiques). Le nom de marque garde `// i18n-ignore: marque` ; l'entrée nav ne doit PAS être en dur (lint:i18n). Modification CHIRURGICALE — ne pas altérer `<html lang dir>`, providers, fonts (Pitfall 7).
    - home `(marketing)/page.tsx` (D-08a) : ajouter un bloc « Apprenez les bases » avec lien `academy.learnBasicsCta` → `/academie` (Link localisé). Lien accent `--primary` autorisé (UI-SPEC §Color, lien contextuel).
    - `SignalDetail.tsx` (D-08b) : ajouter un lien contextuel `academy.signalExecLink` (« Comment exécuter ce signal ? ») → cours plateforme `/academie/prendre-en-main-mt5` (Link localisé). i18n, propriétés logiques.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc -b --force 2>&1 | grep -v "forbidden-service-import" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers modifiés ; `tsc -b --force` n'ajoute aucune nouvelle erreur (baseline `forbidden-service-import` exclue).
    - `layout.tsx` : entrée nav via `@/i18n/navigation` `Link` + libellé `academy.navAcademy` ; `<html lang dir>`/providers/fonts INCHANGÉS (diff limité au header).
    - home + SignalDetail : liens via `@/i18n/navigation` (grep `next/navigation` Link == 0), libellés i18n.
    - `lint:i18n` exit 0 (aucune chaîne en dur hors marque).
  </acceptance_criteria>
  <done>Académie atteignable depuis nav + home + détail signal, liens localisés et i18n.</done>
</task>

<task type="auto">
  <name>Task 2: E2E Playwright Académie (authoré, exécuté en preview)</name>
  <files>apps/web/e2e/academie.spec.ts</files>
  <read_first>
    - apps/web/e2e/i18n.spec.ts (analog structure spec + locales) ; apps/web/e2e/gating.spec.ts (analog navigation)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-VALIDATION.md §Per-Task Map (CMS-01 lecture article+nav cours, LEGAL-01 disclaimer 100%)
  </read_first>
  <action>
    Écrire `academie.spec.ts` couvrant (cible = base URL preview via env, ex. `PLAYWRIGHT_BASE_URL`) :
    1. `/{fr,en,ar}/academie` : l'index charge, l'entrée nav Académie présente, ≥1 carte rendue.
    2. Ouvrir un article (`ratio-risque-rendement`) : titre, corps prose, composant pédago visible, `<Disclaimer>` présent en pied (LEGAL-01).
    3. Ouvrir une leçon du cours `prendre-en-main-mt5` : nav préc./suiv. présente, indicateur progression, disclaimer présent.
    4. `/ar/academie/<slug>` : `dir=rtl` sur `<html>` (RTL arabe).
    5. Fallback D-14 : `/ar/academie/comprendre-le-levier` (ar manquant) → version FR servie + bandeau fallback, PAS de 404.
    Le spec doit parser/`--list` sans erreur. L'EXÉCUTION GREEN se fait en Vercel preview (human-verify Task 3) — ne pas fabriquer un GREEN local (précédent D-01-04-C).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec playwright test e2e/academie.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test e2e/academie.spec.ts --list` liste les tests sans erreur de parse.
    - Le spec couvre les 5 cas ci-dessus (index trilingue, article+disclaimer, leçon+nav, RTL ar, fallback D-14).
    - Base URL paramétrable par env (preview), pas de localhost en dur si possible.
  </acceptance_criteria>
  <done>E2E Académie authoré et parseable, prêt à exécuter en preview.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérification du rendu MDX en Vercel preview</name>
  <what-built>
    Le système Académie complet : index filtré trilingue, articles/cours/leçons rendus via compileMDX, composants pédago, TOC, temps de lecture, disclaimer auto-injecté, fallback D-14, liens funnel, sitemap hreflang. Le rendu MDX NE se valide PAS en local (`next build` casse sur le `!`, dev tourne en turbopack — RESEARCH Pitfall 2). La validation autoritaire est en Vercel preview.
  </what-built>
  <how-to-verify>
    1. Déclencher un déploiement Vercel preview de la branche (push) ; récupérer l'URL de preview.
    2. Configurer `PLAYWRIGHT_BASE_URL` sur l'URL preview et lancer `pnpm --filter web exec playwright test e2e/academie.spec.ts` (E2E réel contre preview).
    3. Manuellement en preview : ouvrir `/fr/academie`, `/en/academie`, `/ar/academie` — l'index liste articles + cours, les filtres thème/niveau/plateforme fonctionnent.
    4. Ouvrir un article : callouts (⚠/💡), étapes numérotées, encadré « exemple de trade » (valeurs non inversées en arabe grâce à `<bdi>`), disclaimer en pied.
    5. Ouvrir une leçon du cours MT5 : nav préc./suiv., progression « Leçon X sur Y », disclaimer.
    6. `/ar/academie/<slug>` : la mise en page bascule en RTL (`dir=rtl`), prix/R:R restent LTR.
    7. Fallback D-14 : `/ar/academie/comprendre-le-levier` (ar manquant) → version FR + bandeau « traduction à venir », jamais 404.
    8. Confirmer : aucun crash 500, disclaimer présent sur 100% des articles/leçons (LEGAL-01).
  </how-to-verify>
  <acceptance_criteria>
    - E2E `academie.spec.ts` vert contre l'URL preview (ou écarts documentés).
    - Disclaimer visible sur chaque article ET chaque leçon ouverts (LEGAL-01).
    - Fallback D-14 vérifié (FR + bandeau, pas de 404).
    - RTL arabe correct, valeurs trade non inversées.
    - Aucune erreur 500 sur les routes Académie en preview.
  </acceptance_criteria>
  <resume-signal>Tape "approved" si l'index + article + leçon + RTL + fallback + disclaimer sont OK en preview, ou décris les écarts.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Liens funnel internes | navigation localisée vers /academie (aucune donnée non fiable franchie) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-LEGAL | Repudiation (légal) | vérif disclaimer en preview | mitigate | E2E + checkpoint humain confirment `<Disclaimer />` présent sur 100% des articles/leçons en preview (LEGAL-01). |
| T-09-I18N | Tampering | entrée nav / liens funnel | mitigate | Tous les libellés via namespace `academy`, `lint:i18n` exit 0 ; liens via `@/i18n/navigation` (préservent la locale, pas de chaîne en dur hors marque). |
| T-09-PATH | Tampering / Info Disclosure | vérif runtime fallback/404 | mitigate | Le checkpoint vérifie qu'un slug fallback sert FR (pas 404) et qu'aucune route ne fuit hors content/academie/ — confirmation runtime de T-09-PATH (mitigé code en Plan 02). |
</threat_model>

<verification>
- `cd apps/web && pnpm exec tsc -b --force` : aucune nouvelle erreur hors baseline ; `lint:i18n` exit 0.
- `playwright test e2e/academie.spec.ts --list` parse OK.
- **Phase gate : E2E vert en Vercel preview + checkpoint humain approuvé** (rendu MDX non validable en build local — Pitfall 2).
</verification>

<success_criteria>
- Académie atteignable depuis nav + home + détail signal (D-08a/b/c), liens localisés i18n.
- E2E Académie authoré et exécuté GREEN en Vercel preview.
- Disclaimer LEGAL-01 confirmé sur 100% des contenus en preview ; fallback D-14 et RTL arabe vérifiés.
</success_criteria>

<output>
Create `.planning/phases/09-cms-cours-articles-vulgaris-s/09-05-SUMMARY.md` when done.
</output>
