---
phase: 09-cms-cours-articles-vulgaris-s
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/academie/frontmatter.ts
  - apps/web/src/lib/academie/frontmatter.test.ts
  - apps/web/src/lib/academie/searchParams.ts
  - apps/web/src/lib/academie/searchParams.test.ts
  - apps/web/src/lib/academie/reading-time.ts
  - apps/web/src/lib/academie/reading-time.test.ts
  - apps/web/src/lib/academie/toc.ts
  - apps/web/src/lib/academie/toc.test.ts
autonomous: false
requirements: [CMS-01]
user_setup: []

must_haves:
  truths:
    - "Un frontmatter invalide est rejeté à la frontière (Zod), jamais rendu silencieusement"
    - "Une valeur de filtre hors-enum est ignorée (jamais propagée), le parse ne throw jamais"
    - "Le temps de lecture est déterministe et identique pour un même corps (golden values fr/en/ar) — D-10 (temps de lecture auto + sommaire TOC)"
    - "Les slugs du TOC sont identiques aux id émis par rehype-slug (accents/arabe)"
  artifacts:
    - path: "apps/web/src/lib/academie/frontmatter.ts"
      provides: "Schéma Zod FrontmatterSchema (D-11) + enums Theme/Niveau/Plateforme + type Frontmatter"
      contains: "FrontmatterSchema"
    - path: "apps/web/src/lib/academie/searchParams.ts"
      provides: "parseAcademyParams whitelist Zod multi-axes (D-05)"
      contains: "parseAcademyParams"
    - path: "apps/web/src/lib/academie/reading-time.ts"
      provides: "calcul déterministe du temps de lecture"
      contains: "computeReadingTime"
    - path: "apps/web/src/lib/academie/toc.ts"
      provides: "extraction titres → ancres via github-slugger (cohérent rehype-slug)"
      contains: "extractToc"
  key_links:
    - from: "apps/web/src/lib/academie/searchParams.ts"
      to: "apps/web/src/lib/academie/frontmatter.ts"
      via: "import des enums Theme/Niveau/Plateforme (source unique)"
      pattern: "from.*frontmatter"
---

<objective>
Poser le socle de logique pure et testable de l'Académie : le schéma Zod du frontmatter (frontière de données D-11), le parseur de filtres whitelist (D-05, calque signaux), le calcul déterministe du temps de lecture (D-10), et l'extraction du sommaire (D-10). Installer les dépendances MDX (next-mdx-remote/rsc + plugins + gray-matter + reading-time + github-slugger) — étape avec checkpoint de légitimité ET checkpoint d'install (le `!` du chemin casse `npm install` localement).

Purpose: ces 4 modules sont les briques pures que les routes RSC (Plan 04) et le scan fichiers (Plan 02) consomment. Tous testables localement en Vitest (le seul gate automatisé fiable de la phase — le rendu MDX ne se valide qu'en Vercel preview).
Output: 4 modules `lib/academie/*` + leurs tests Wave 0 verts + dépendances déclarées dans package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-PATTERNS.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-VALIDATION.md

<interfaces>
<!-- Analog whitelist parser — copier la structure 1:1, swap des enums. -->
From apps/web/src/lib/signals/searchParams.ts:
- `const StyleEnum = z.enum([...])` ; `SignalsParamsSchema = z.object({...})` ; `type SignalsParams = z.infer<...>`
- `function firstString(value: string | string[] | undefined): string | undefined`
- `export function parseSignalsParams(raw: RawParams): SignalsParams` — `pick(schema, value)` via `.safeParse`, hors-enum → undefined, jamais throw (threat T-03-05)
- `export function serializeSignalsParams(params): URLSearchParams`

Frontmatter cible (RESEARCH §Code Examples, D-11):
- ThemeEnum = ['usage-plateforme','bases-trading','gestion-risque','analyse-technique','comprendre-signaux']
- NiveauEnum = ['debutant','intermediaire'] ; PlateformeEnum = ['mt4','mt5','autre']
- champs: type ('article'|'cours'|'lecon'), titre, resume, theme, niveau, plateforme?, date, cover, course?, order?

Vitest glob (racine vitest.config.ts) capte déjà `apps/web/src/lib/**/*.test.ts` — aucune modif de config requise.
Alias `@/*` → `apps/web/src/*` (tsconfig web, posé P2).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Légitimité + install des dépendances MDX</name>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Package Legitimacy Audit (slopcheck 9/9 OK 2026-06-19) + §Standard Stack (versions épinglées)
    - apps/web/package.json (état actuel des deps)
  </read_first>
  <what-built>
    Installer dans le workspace web, versions épinglées : `next-mdx-remote@6.0.0`, `@mdx-js/mdx@3.1.1`, `@mdx-js/react@3`, `gray-matter@4.0.3`, `rehype-slug@6.0.0`, `remark-gfm@4.0.1`, `github-slugger@2.0.0`, `reading-time@1.5.0`. Toutes auditées slopcheck OK (RESEARCH §Package Legitimacy Audit). Commande : `pnpm --filter web add next-mdx-remote@6.0.0 @mdx-js/mdx@3.1.1 @mdx-js/react@3 gray-matter@4.0.3 rehype-slug@6.0.0 remark-gfm@4.0.1 github-slugger@2.0.0 reading-time@1.5.0`.
  </what-built>
  <how-to-verify>
    1. CONTEXTE CONNU : `npm/pnpm install` a échoué côté Windows à cause du `!` dans le chemin projet (RESEARCH §slopcheck note). Si l'install échoue localement, ne PAS la forcer ; déclarer les 8 deps + versions épinglées dans `apps/web/package.json` à la main et laisser Vercel résoudre au build (env propre).
    2. Confirmer que les 8 packages + versions exactes figurent dans `apps/web/package.json` (`dependencies`).
    3. Aucun de ces packages n'est `[ASSUMED]`/`[SUS]`/`[SLOP]` — tous `Approved` dans l'audit. Aucun nouveau package hors de cette liste.
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/package.json` contient les 8 dépendances aux versions épinglées exactes ci-dessus.
    - AUCUN `@next/mdx` ajouté (interdit — loader webpack `.mdx` = collision `!`, RESEARCH Pitfall 1).
    - Si install locale impossible (`!`), la déviation est notée dans le SUMMARY (résolution déléguée à Vercel) — ce n'est PAS un échec de tâche.
  </acceptance_criteria>
  <resume-signal>Tape "approved" si les 8 deps (versions exactes) sont déclarées et aucun `@next/mdx`, ou décris l'écart.</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: frontmatter.ts (Zod, frontière D-11) + searchParams.ts (whitelist D-05)</name>
  <files>apps/web/src/lib/academie/frontmatter.ts, apps/web/src/lib/academie/frontmatter.test.ts, apps/web/src/lib/academie/searchParams.ts, apps/web/src/lib/academie/searchParams.test.ts</files>
  <read_first>
    - apps/web/src/lib/signals/searchParams.ts (analog EXACT — structure 1:1, pick/safeParse/firstString, hors-enum→undefined)
    - apps/web/src/lib/signals/__tests__/searchParams.test.ts (analog de test)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Code Examples (FrontmatterSchema) + §Security Domain (T-09-01 searchParams, T-09-02 frontmatter)
  </read_first>
  <behavior>
    - frontmatter: un objet valide (D-11) parse ; `type` hors {article,cours,lecon} → erreur Zod ; `theme`/`niveau` hors enum → erreur ; `plateforme`/`course`/`order` absents OK (optionnels) ; `titre`/`resume`/`cover` vides → erreur (.min(1)).
    - searchParams: `parseAcademyParams` ignore une valeur hors-enum (undefined), ne throw jamais ; multi-select par axe (`theme=a&theme=b`) collecté en `string[]` filtré sur l'enum (les valeurs invalides du tableau sont retirées) ; round-trip `serializeAcademyParams` ∘ `parseAcademyParams` stable.
  </behavior>
  <action>
    Créer `frontmatter.ts` : exporter `ThemeEnum`/`NiveauEnum`/`PlateformeEnum` (valeurs RESEARCH §Code Examples), `FrontmatterSchema` (z.object champs D-11 : type/titre/resume/theme/niveau/plateforme?/date/cover/course?/order?), `type Frontmatter = z.infer<...>`. `date` accepte string ISO. C'est la SOURCE UNIQUE des enums d'axes (T-09-02 : frontmatter = frontière de données).
    Créer `searchParams.ts` (calque signals) : importer les 3 enums depuis `./frontmatter` (NE PAS redéclarer), `AcademyParamsSchema`, `parseAcademyParams(raw)` via `pick`/`.safeParse` (hors-enum ignoré, jamais throw — T-09-01), `serializeAcademyParams`. Gérer le multi-select (collecter le tableau, garder seulement les membres valides de l'enum). Aucune chaîne brute propagée.
    Écrire les tests AVANT (RED) puis implémenter (GREEN).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec vitest run src/lib/academie/frontmatter.test.ts src/lib/academie/searchParams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/academie/frontmatter.test.ts src/lib/academie/searchParams.test.ts` exit 0.
    - Test prouvé non trivial : un cas `theme=__invalid__` est ABSENT du résultat parsé (assertion explicite).
    - `searchParams.ts` importe les enums de `frontmatter.ts` (grep `from './frontmatter'` == 1), aucune redéclaration d'enum.
  </acceptance_criteria>
  <done>frontmatter + searchParams créés, enums centralisés, tests Wave 0 (frontmatter, searchParams) verts.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: reading-time.ts (déterministe) + toc.ts (slugs cohérents rehype-slug)</name>
  <files>apps/web/src/lib/academie/reading-time.ts, apps/web/src/lib/academie/reading-time.test.ts, apps/web/src/lib/academie/toc.ts, apps/web/src/lib/academie/toc.test.ts</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Open Questions Q4 (reading-time maison via lib `reading-time`, golden values) + Pitfall 5 (github-slugger = même algo que rehype-slug) + Pitfall 3 (gray-matter/reading-time = CJS → default import, jamais require)
    - apps/web/src/lib/signals/format.ts (style de fonction pure déterministe testée golden)
  </read_first>
  <behavior>
    - reading-time: `computeReadingTime(body: string)` retourne un nombre de minutes déterministe (golden) ; même entrée → même sortie ; un corps fr/en/ar de longueur connue donne la valeur attendue (3 golden values, une par langue).
    - toc: `extractToc(body: string)` retourne la liste {level, text, slug} pour les titres H2/H3 ; le `slug` est produit par `github-slugger` → identique à l'id qu'émet `rehype-slug` au render ; titres avec accents (fr) et caractères arabes produisent des slugs stables et non vides.
  </behavior>
  <action>
    Créer `reading-time.ts` : `import readingTime from 'reading-time'` (default import, CJS, Pitfall 3), `export function computeReadingTime(body: string): number` retournant `Math.ceil(readingTime(body).minutes)` (déterministe, indépendant du pipeline MDX — Q4). Pas de plugin remark.
    Créer `toc.ts` : `import GithubSlugger from 'github-slugger'` ; `export function extractToc(body: string): TocItem[]` — parser les titres markdown `## ` / `### ` du corps brut, produire le slug via une instance fraîche de `GithubSlugger` par appel (dédup cohérente avec rehype-slug). Type `TocItem = { level: 2 | 3; text: string; slug: string }`.
    Tests AVANT (RED) : golden values reading-time fr/en/ar ; cohérence slug toc (accents + arabe, assertion slug non vide et stable).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec vitest run src/lib/academie/reading-time.test.ts src/lib/academie/toc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/academie/reading-time.test.ts src/lib/academie/toc.test.ts` exit 0.
    - reading-time : 3 golden values (fr/en/ar) assertées en dur.
    - toc : un titre avec accents et un titre arabe produisent un slug non vide ; aucune dépendance à rehype au runtime du test (github-slugger pur).
    - Aucun `require(` dans les 2 modules (grep == 0) — default import CJS only (Pitfall 3).
  </acceptance_criteria>
  <done>reading-time + toc créés et golden-testés ; tests Wave 0 (reading-time, toc) verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → filtres | valeur de filtre non fiable issue de l'URL publique |
| Frontmatter fichier → modèle | en-tête YAML d'un fichier MDX (contenu repo, mais frontière de données typée) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | searchParams.ts (filtres) | mitigate | Zod whitelist par enum, `.safeParse` champ par champ, hors-enum → undefined, jamais throw ni propagation de string brute (calque signals T-03-05). Test assertant qu'une valeur invalide est absente. |
| T-09-02 | Tampering | frontmatter.ts (frontière de données) | mitigate | `FrontmatterSchema.parse` à la frontière ; frontmatter invalide → erreur Zod, jamais rendu silencieux. Test des cas d'enum/champs invalides. |
| T-09-SC | Tampering | install pnpm (8 deps MDX) | mitigate | 8 packages audités slopcheck OK (RESEARCH). Checkpoint blocking-human Task 1 : versions épinglées, aucun `@next/mdx`, aucun package hors liste. |
</threat_model>

<verification>
- Suite Vitest ciblée des 4 modules exit 0 : `cd apps/web && pnpm exec vitest run src/lib/academie/`.
- Aucun `@next/mdx` dans package.json (grep == 0).
- searchParams importe les enums de frontmatter (source unique).
</verification>

<success_criteria>
- 4 modules purs créés et leurs 4 tests Wave 0 verts localement.
- Dépendances MDX déclarées (versions épinglées), checkpoint légitimité passé.
- Threats T-09-01 / T-09-02 / T-09-SC mitigés et testés/vérifiés.
</success_criteria>

<output>
Create `.planning/phases/09-cms-cours-articles-vulgaris-s/09-01-SUMMARY.md` when done.
</output>
