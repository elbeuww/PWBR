---
phase: 09-cms-cours-articles-vulgaris-s
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/components/academie/Callout.tsx
  - apps/web/src/components/academie/Steps.tsx
  - apps/web/src/components/academie/Figure.tsx
  - apps/web/src/components/academie/TradeExample.tsx
  - apps/web/src/components/academie/Toc.tsx
  - apps/web/src/components/academie/mdx-components.tsx
  - apps/web/src/components/academie/ContentCard.tsx
  - apps/web/src/components/academie/FilterBar.tsx
  - apps/web/src/components/academie/FallbackBanner.tsx
autonomous: true
requirements: [CMS-01]
user_setup: []

must_haves:
  truths:
    - "Les composants pédago (callout, étapes, capture, exemple de trade) s'affichent sans couleur vert/rouge, distingués par icône + libellé i18n"
    - "Les valeurs prix/SL/TP/R:R de l'encadré exemple sont enveloppées dans <bdi> (anti-inversion RTL)"
    - "Le mapping MDX expose Callout/Steps/Figure/TradeExample + h2 ancré (id rehype-slug)"
    - "FilterBar met à jour l'URL via @/i18n/navigation (préserve la locale), multi-select par axe"
    - "Le bandeau fallback est un Alert neutre (jamais destructive/rouge) en tête de contenu"
  artifacts:
    - path: "apps/web/src/components/academie/mdx-components.tsx"
      provides: "MDX_COMPONENTS — mapping des composants pédago maison (D-09)"
      contains: "MDX_COMPONENTS"
    - path: "apps/web/src/components/academie/TradeExample.tsx"
      provides: "encadré exemple de trade, valeurs en <bdi>"
      contains: "bdi"
    - path: "apps/web/src/components/academie/FilterBar.tsx"
      provides: "chips filtres client → URL (multi-axes)"
      contains: "i18n/navigation"
  key_links:
    - from: "apps/web/src/components/academie/FilterBar.tsx"
      to: "apps/web/src/i18n/navigation"
      via: "useRouter/usePathname localisés (préserve la locale)"
      pattern: "i18n/navigation"
    - from: "apps/web/src/components/academie/mdx-components.tsx"
      to: "apps/web/src/components/academie/Callout.tsx"
      via: "import dans le mapping MDX"
      pattern: "Callout"
---

<objective>
Construire la bibliothèque de présentation de l'Académie : composants pédago maison (Callout ⚠/💡, Steps, Figure, TradeExample), mapping `MDX_COMPONENTS` injecté à compileMDX (D-09), composant TOC sticky, carte de contenu (article/cours), FilterBar (chips → URL, calque signaux), et FallbackBanner (D-14). Tous alignés sur l'UI-SPEC APPROUVÉE (palette héritée P2, pas de vert/rouge, propriétés logiques RTL, namespace i18n `academy`).

Purpose: ces composants sont consommés par les routes RSC (Plan 04). Fichiers entièrement disjoints de Plan 02 → exécutable en parallèle. Le namespace i18n `academy` est posé en Plan 04 (les libellés sont consommés ici via getTranslations/useTranslations, déclarés là).
Output: 9 composants sous `components/academie/`, conformes UI-SPEC, RTL-safe, sans chaîne en dur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-PATTERNS.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md

<interfaces>
<!-- Analogs réels du repo -->
From apps/web/src/components/signals/FilterBar.tsx (analog EXACT pour FilterBar):
- `'use client'` ; `import { useTranslations } from 'next-intl'`
- `import { useRouter, usePathname } from '../../i18n/navigation'` (localisé)
- `import { useSearchParams } from 'next/navigation'` (lecture seule)
- `setParam`/`toggleParam`/`resetAll` via `router.replace(..., { scroll: false })`

From apps/web/src/components/Disclaimer.tsx (analog FallbackBanner — RSC i18n minimal):
- `import { getTranslations } from 'next-intl/server'` ; `async function` ; classes logiques `ps-4 pe-4`

From apps/web/src/components/ui/badge.tsx — variants: `default`=bg-primary (RÉSERVÉ filtre actif), `outline`=border-border, `secondary`=bg-secondary
From apps/web/src/components/ui/{card,alert,button,progress,select}.tsx — déjà présents (Registry Safety, ne pas réinstaller)

Plan 01: apps/web/src/lib/academie/toc.ts → `extractToc`/`TocItem` (consommé par Toc.tsx)
i18n namespace cible = `academy` (posé en Plan 04 ; copy FR canonique = UI-SPEC §Copywriting Contract)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composants pédago RSC (Callout, Steps, Figure, TradeExample) + mapping MDX</name>
  <files>apps/web/src/components/academie/Callout.tsx, apps/web/src/components/academie/Steps.tsx, apps/web/src/components/academie/Figure.tsx, apps/web/src/components/academie/TradeExample.tsx, apps/web/src/components/academie/mdx-components.tsx</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md §Color (callouts spec, pas de vert/rouge, border-s-4) + §Layout 2 (Steps/Figure/TradeExample) + §Copywriting (libellés academy)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md Pattern 2 (mapping + Disclaimer hors MDX) + Pitfall 4 (bdi RTL)
    - apps/web/src/components/signals/SignalCard.tsx (style de composant RSC i18n du repo) ; apps/web/src/components/ui/card.tsx
  </read_first>
  <action>
    Créer les 4 composants pédago (RSC, libellés via `getTranslations('academy')`, classes logiques uniquement) :
    - `Callout.tsx` : prop `variant: 'attention' | 'astuce'`. attention = `bg-card border-s-4 border-primary` + icône lucide `AlertTriangle` `text-foreground` + libellé `academy.calloutAttention` ; astuce = `bg-card border-s-4 border-border` + `Lightbulb` `text-muted-foreground` + `academy.calloutAstuce`. PAS de couleur sémantique vert/rouge/ambre (UI-SPEC §Color). Distinction = icône + libellé.
    - `Steps.tsx` : `<ol>` stylé, compteur en `bg-secondary`.
    - `Figure.tsx` : `next/image` (ratio cohérent) + `<figcaption className="text-sm text-muted-foreground">`.
    - `TradeExample.tsx` : `Card bg-card border border-border p-6`, libellés `academy.tradeEntry/tradeStopLoss/tradeTakeProfit/tradeRr` en `text-sm text-muted-foreground`, valeurs prix/SL/TP/R:R **chacune enveloppée dans `<bdi>`** (Pitfall 4, précédent dashboard affilié D-02-03-A). Aucune couleur vert/rouge.
    - `mdx-components.tsx` : `export const MDX_COMPONENTS = { Callout, Steps, Figure, TradeExample, h2: HeadingWithAnchor }` où `HeadingWithAnchor` réutilise l'`id` posé par rehype-slug (titre cliquable/ancré). Le `<Disclaimer />` n'est PAS dans le mapping (injecté par la page — RESEARCH Pattern 2).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc -b --force 2>&1 | grep -v "forbidden-service-import" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - Les 5 fichiers existent ; `tsc -b --force` n'ajoute AUCUNE nouvelle erreur au-delà de la baseline connue (`forbidden-service-import.ts`).
    - Aucune classe `bg-green*`/`bg-red*`/`text-green*`/`text-red*`/`border-green*`/`border-red*`/`amber` dans les 5 fichiers (grep == 0, UI-SPEC §Color / D-04).
    - `TradeExample.tsx` enveloppe chaque valeur numérique dans `<bdi>` (grep `bdi` ≥ 1).
    - `mdx-components.tsx` exporte `MDX_COMPONENTS` mappant Callout/Steps/Figure/TradeExample + h2 ; aucun `<Disclaimer` dans le mapping (grep == 0).
    - `lint:i18n` exit 0 (aucune chaîne en dur ; libellés via `academy`).
  </acceptance_criteria>
  <done>4 composants pédago + mapping MDX créés, sans vert/rouge, bdi sur les valeurs, libellés i18n.</done>
</task>

<task type="auto">
  <name>Task 2: Toc + ContentCard + FallbackBanner (présentation index/détail)</name>
  <files>apps/web/src/components/academie/Toc.tsx, apps/web/src/components/academie/ContentCard.tsx, apps/web/src/components/academie/FallbackBanner.tsx</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md §Layout 1 (ContentCard : cover 16:9, titre text-xl, badges outline, méta temps de lecture) + §Layout 2 (TOC sticky, item courant text-primary) + §Layout 5 (FallbackBanner Alert neutre)
    - apps/web/src/components/Disclaimer.tsx (RSC i18n minimal — analog FallbackBanner) ; apps/web/src/components/ui/{alert,card,badge}.tsx
    - apps/web/src/lib/academie/toc.ts (TocItem — Plan 01) ; apps/web/src/lib/academie/frontmatter.ts (type Frontmatter)
  </read_first>
  <action>
    - `Toc.tsx` : reçoit `items: TocItem[]`, rend une liste de liens d'ancrage `text-sm`, item courant en `text-primary`. Titre « Sommaire » via `academy.tocTitle`. Sticky desktop (`lg:sticky`) côté `end` logique, en flux mobile. Navigation par ancres `#slug` (cohérentes avec rehype-slug, Plan 01 toc.ts).
    - `ContentCard.tsx` : compose `Card`+`Badge`. cover `next/image` ratio 16:9, titre `text-xl font-semibold`, résumé 2 lignes `text-sm text-muted-foreground`, rangée de badges thème/niveau/plateforme `variant="outline"` `gap-2`, méta temps de lecture `academy.readingTime` (ICU `{minutes}`). Variante cours : badge `outline` « Cours » (`academy.courseBadge`) + nombre de leçons + CTA `academy.startCourse` ; variante article : CTA `academy.readArticle`. Lien via `@/i18n/navigation` `Link`.
    - `FallbackBanner.tsx` : RSC, rend `Alert` shadcn `variant` NEUTRE (jamais destructive/rouge), icône lucide `Languages`/`Info`, copy `academy.fallbackBanner`, en tête de contenu (D-14).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc -b --force 2>&1 | grep -v "forbidden-service-import" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers existent ; `tsc -b --force` n'ajoute aucune nouvelle erreur (baseline `forbidden-service-import` exclue).
    - `FallbackBanner.tsx` n'utilise PAS `variant="destructive"` (grep == 0) ni couleur rouge.
    - `ContentCard.tsx` badges thème/niveau/plateforme en `variant="outline"` (jamais `default` au repos — `default`/bg-primary réservé au filtre actif, UI-SPEC §Color).
    - Tous les liens internes via `@/i18n/navigation` (grep `next/navigation` pour `Link`/`useRouter` == 0 dans ces 3 fichiers).
    - `lint:i18n` exit 0.
  </acceptance_criteria>
  <done>Toc + ContentCard + FallbackBanner créés, conformes UI-SPEC, RTL-safe, i18n.</done>
</task>

<task type="auto">
  <name>Task 3: FilterBar (chips multi-axes → URL, calque signaux)</name>
  <files>apps/web/src/components/academie/FilterBar.tsx</files>
  <read_first>
    - apps/web/src/components/signals/FilterBar.tsx (analog EXACT, 154 lignes — copier la mécanique de navigation localisée verbatim)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-UI-SPEC.md §Layout 1 (chips actif=Badge default, repos=outline, min-h-11) + §Copywriting (libellés filtres + « Réinitialiser les filtres »)
    - apps/web/src/lib/academie/searchParams.ts (Plan 01 — enums d'axes, multi-select)
  </read_first>
  <action>
    Créer `FilterBar.tsx` (`'use client'`) calqué sur signals/FilterBar : `useTranslations('academy')`, `useRouter`/`usePathname` depuis `../../i18n/navigation`, `useSearchParams` depuis `next/navigation`. Axes D-05 : thème / niveau / plateforme, **multi-select par axe** (toggle ajoute/retire la valeur du paramètre tableau `theme=a&theme=b`, pas un simple set). Chip actif = `Badge variant="default"` (bg-primary), repos = `variant="outline"`. Cible tactile `min-h-11`, propriétés logiques (`gap-2`, `ms-*/me-*`). Bouton « Réinitialiser les filtres » (`academy.resetFilters`) visible si au moins un filtre actif. `router.replace(..., { scroll: false })`. Toutes les chaînes via `academy`.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc -b --force 2>&1 | grep -v "forbidden-service-import" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - `FilterBar.tsx` existe ; `tsc -b --force` n'ajoute aucune nouvelle erreur.
    - `useRouter`/`usePathname` importés de `../../i18n/navigation` (grep == 1), JAMAIS de `next/navigation` (préservation locale).
    - Multi-select : le toggle d'une valeur déjà présente la RETIRE (logique tableau), une nouvelle l'ajoute (pas d'écrasement de l'axe).
    - `lint:i18n` exit 0 (libellés via `academy`).
  </acceptance_criteria>
  <done>FilterBar multi-axes créé, navigation localisée, chips conformes UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → FilterBar | la FilterBar écrit dans l'URL ; la lecture/validation est faite côté RSC (searchParams.ts, Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | FilterBar → URL | mitigate | La FilterBar n'écrit que des valeurs d'enum connues (toggle sur valeurs internes), et la frontière de confiance reste la lecture RSC `parseAcademyParams` (Plan 01, hors-enum ignoré). Aucune string libre injectée dans l'URL par les chips. |
| T-09-XSS | Tampering | mdx-components / composants pédago | accept | Composants maison à allowlist fixe passés à `compileMDX`. Aucun raw-HTML passthrough, aucune exécution de contenu distant. Contenu = repo revu en PR (RESEARCH §Security). |
| T-09-A11Y | (qualité/conformité) | Callout | mitigate | Distinction attention/astuce par icône + libellé i18n, jamais par la seule couleur (a11y + D-04 pas de vert/rouge). |
</threat_model>

<verification>
- `cd apps/web && pnpm exec tsc -b --force` : aucune nouvelle erreur hors baseline.
- `lint:i18n` exit 0 sur les 9 composants.
- grep vert/rouge == 0 ; grep `next/navigation` pour Link/useRouter == 0 ; `<bdi>` présent dans TradeExample.
</verification>

<success_criteria>
- 9 composants `components/academie/*` créés, conformes UI-SPEC (palette héritée, pas de vert/rouge, propriétés logiques, bdi).
- Mapping MDX prêt à être injecté à compileMDX (Plan 04).
- Aucune chaîne en dur (namespace `academy`, `lint:i18n` exit 0).
</success_criteria>

<output>
Create `.planning/phases/09-cms-cours-articles-vulgaris-s/09-03-SUMMARY.md` when done.
</output>
