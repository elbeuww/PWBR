---
phase: 09-cms-cours-articles-vulgaris-s
plan: 02
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/lib/academie/content.ts
  - apps/web/src/lib/academie/content.test.ts
  - apps/web/src/lib/academie/course-model.ts
  - apps/web/src/lib/academie/course-model.test.ts
  - apps/web/content/academie/articles/ratio-risque-rendement.fr.mdx
  - apps/web/content/academie/articles/ratio-risque-rendement.en.mdx
  - apps/web/content/academie/articles/ratio-risque-rendement.ar.mdx
  - apps/web/content/academie/articles/comprendre-le-levier.fr.mdx
  - apps/web/content/academie/articles/comprendre-le-levier.en.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.fr.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.en.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.ar.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/02-ouvrir-une-position.fr.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/02-ouvrir-une-position.en.mdx
  - apps/web/content/academie/cours/prendre-en-main-mt5/03-poser-tp-sl.fr.mdx
autonomous: true
requirements: [CMS-01, CMS-02]
user_setup: []

must_haves:
  truths:
    - "Résoudre (slug, locale) retourne le bon fichier sous content/academie/ — D-02 (fichiers MDX versionnés, pas de DB), D-12 (clé slug+locale, nommage {slug}.{locale}.mdx), D-13 (3 langues fr/en/ar publiées ensemble)"
    - "Une locale manquante pour un contenu existant en FR retourne la version FR + flag fallback (jamais null si FR existe) — D-14"
    - "Un slug inexistant dans TOUTES les langues retourne null (→ notFound côté route)"
    - "Un slug malveillant (../, slash, MAJ) est rejeté avant tout fs.readFile (path-traversal)"
    - "La dérivation d'un cours ordonne les leçons par `order` et calcule prev/next corrects — D-04 (deux types : Articles autonomes vs Cours ordonnés)"
    - "L'index liste le frontmatter SANS compiler les corps MDX (gray-matter seul)"
  artifacts:
    - path: "apps/web/src/lib/academie/content.ts"
      provides: "scan fs + gray-matter, résolution (slug,locale)+fallback D-14, validation path-traversal, catalogue index"
      contains: "resolveContent"
    - path: "apps/web/src/lib/academie/course-model.ts"
      provides: "dérivation cours/leçons (regroupement par course, tri order, prev/next, progression)"
      contains: "deriveCourse"
    - path: "apps/web/content/academie/articles/ratio-risque-rendement.fr.mdx"
      provides: "fixture de preuve article (D-03)"
      contains: "type: article"
  key_links:
    - from: "apps/web/src/lib/academie/content.ts"
      to: "apps/web/src/lib/academie/frontmatter.ts"
      via: "FrontmatterSchema.parse sur le frontmatter scanné (frontière D-11)"
      pattern: "FrontmatterSchema"
    - from: "apps/web/src/lib/academie/content.ts"
      to: "apps/web/src/lib/academie/reading-time.ts"
      via: "computeReadingTime sur le corps brut au scan"
      pattern: "computeReadingTime"
---

<objective>
Construire la couche fichiers de l'Académie : `content.ts` (scan `fs.readdir` + `gray-matter` frontmatter-seul pour l'index, résolution `(slug, locale)` → chemin avec fallback FR D-14, validation anti path-traversal) et `course-model.ts` (dérivation cours/leçons sans DB : regroupement par `course`, tri `order`, prev/next, progression). Livrer 3-5 fixtures MDX réelles de preuve (D-03), trilingues, dont un cours à leçons ordonnées et un article avec une locale manquante (pour prouver le fallback D-14).

Purpose: cette couche est consommée par toutes les routes RSC (Plan 04). C'est le point le plus sensible côté sécurité (path-traversal le plus haut, RESEARCH §Security Domain). Toute la logique est pure/fs et testable localement en Vitest.
Output: 2 services `lib/academie/*` + tests Wave 0 verts + jeu de fixtures MDX de preuve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-PATTERNS.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-02-PLAN.md
@.planning/phases/09-cms-cours-articles-vulgaris-s/09-01-SUMMARY.md

<interfaces>
<!-- Produits du Plan 01 (Wave 1) — importer, ne pas réécrire -->
From apps/web/src/lib/academie/frontmatter.ts:
- `FrontmatterSchema` (z.object D-11), `type Frontmatter`, enums Theme/Niveau/Plateforme
From apps/web/src/lib/academie/reading-time.ts:
- `computeReadingTime(body: string): number`

Convention de nommage tranchée (D-12, RESEARCH Open Q1):
- article : `content/academie/articles/{slug}.{locale}.mdx`
- leçon   : `content/academie/cours/{course}/{order}-{lesson}.{locale}.mdx`
locale ∈ routing.locales = {fr,en,ar} (import depuis apps/web/src/i18n/routing.ts)

Pattern 3 (RESEARCH): index = `fs.readdir` + `gray-matter` (frontmatter SEUL, ne compile pas le corps).
Pattern 4 (RESEARCH): fallback — exists(slug,locale) ? sinon (locale≠fr && exists(slug,'fr')) → FR+fallback ; sinon null.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: content.ts — scan, résolution (slug,locale), fallback D-14, anti path-traversal</name>
  <files>apps/web/src/lib/academie/content.ts, apps/web/src/lib/academie/content.test.ts</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Architecture (Pattern 3 gray-matter, Pattern 4 fallback) + §Security Domain (path-traversal = item de plus haute sévérité) + Pitfall 3 (gray-matter CJS default import)
    - apps/web/src/lib/academie/frontmatter.ts (FrontmatterSchema — frontière D-11) ; apps/web/src/lib/academie/reading-time.ts (computeReadingTime)
    - apps/web/src/i18n/routing.ts (routing.locales — whitelist locale)
  </read_first>
  <behavior>
    - `resolveContent(slug, locale)` : si fichier (slug,locale) existe → {absPath, locale, fallback:false} ; sinon si locale≠'fr' et (slug,'fr') existe → {absPath FR, locale:'fr', fallback:true} (D-14) ; sinon null.
    - path-traversal : `slug` ne matchant pas `^[a-z0-9-]+$` → rejet (null/throw contrôlé) AVANT tout accès disque ; `locale` hors routing.locales → rejet ; le chemin résolu DOIT rester sous `content/academie/` (assert via path.resolve + startsWith).
    - `listContent(locale)` : scanne articles + leçons, parse frontmatter via gray-matter (PAS de compile MDX), valide via FrontmatterSchema, calcule readingTime sur le corps, retourne le catalogue {slug, type, theme, niveau, plateforme, titre, resume, cover, date, readingMinutes, course?, order?}.
    - `listAllContent()` : énumère chaque slug avec la liste des locales RÉELLEMENT présentes (pour le sitemap Plan 04 — ne pas inclure les fallbacks).
  </behavior>
  <action>
    Créer `content.ts`. `import matter from 'gray-matter'` (default, CJS Pitfall 3), `import { promises as fs } from 'node:fs'`, `import path from 'node:path'`. Définir `CONTENT_ROOT = path.join(process.cwd(), 'content/academie')` (cwd = apps/web au build Vercel, RESEARCH §Project Structure).
    Implémenter la validation de chemin EN PREMIER (T-09-PATH) : regex slug `^[a-z0-9-]+$`, locale ∈ routing.locales, construire le chemin par `path.join` puis `path.resolve` et asserter `resolved.startsWith(CONTENT_ROOT)` — sinon rejeter. JAMAIS concaténer le slug brut.
    Implémenter `resolveContent` (fallback D-14, Pattern 4), `listContent(locale)` (Pattern 3 gray-matter + FrontmatterSchema + computeReadingTime), `listAllContent()` (locales réelles).
    Tests AVANT (RED) : résolution OK ; fallback FR quand ar manque ; null si slug absent partout ; rejet `../`, slug avec `/`, slug en MAJUSCULES, locale `xx` ; index ne contient que du frontmatter (assertion qu'aucun corps n'est compilé — vérifier que la valeur retournée n'a pas de champ "content compilé"). Les tests pointent vers les fixtures du Task 3 (créer les fixtures d'abord ou des fixtures de test dédiées sous un dossier de test).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec vitest run src/lib/academie/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/academie/content.test.ts` exit 0.
    - Path-traversal prouvé : `resolveContent('../../etc/passwd','fr')`, `resolveContent('a/b','fr')`, `resolveContent('Abc','fr')`, `resolveContent('x','xx')` → tous rejetés (null/erreur contrôlée), AUCUN `fs.readFile` atteint.
    - Fallback D-14 prouvé : un slug présent en fr+en mais pas ar → `resolveContent(slug,'ar')` retourne `{locale:'fr', fallback:true}`, jamais null.
    - grep `from './frontmatter'` == 1 et `computeReadingTime` référencé (key_links).
  </acceptance_criteria>
  <done>content.ts créé avec garde path-traversal en tête, fallback D-14, index frontmatter-seul ; test Wave 0 content vert.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: course-model.ts — dérivation cours/leçons (ordre, prev/next, progression)</name>
  <files>apps/web/src/lib/academie/course-model.ts, apps/web/src/lib/academie/course-model.test.ts</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Open Questions Q5 (modèle cours sans DB : type:'cours' regroupe les type:'lecon' même `course`, triés par `order` ; prev/next = voisins ; progression = order/total)
    - apps/web/src/lib/academie/content.ts (catalogue produit par listContent) ; apps/web/src/lib/academie/frontmatter.ts (type Frontmatter, champs course/order)
  </read_first>
  <behavior>
    - `deriveCourse(courseSlug, catalog)` : retourne les leçons du cours (filtre `type==='lecon' && course===courseSlug`) triées par `order` croissant, + le total.
    - `lessonNavigation(courseSlug, lessonOrder, catalog)` : retourne {prev, next, current, total} — prev/next sont les voisins dans la liste triée ; prev undefined à la 1ʳᵉ leçon, next undefined à la dernière.
    - progression : `current/total` (entier sur total).
    - tri stable et déterministe ; `order` dupliqués → ordre déterministe (tie-break par slug).
  </behavior>
  <action>
    Créer `course-model.ts` — fonctions PURES (aucune I/O ; reçoit le catalogue en argument, ne lit pas le disque). `deriveCourse`, `lessonNavigation`. Types `CourseLesson`, `CourseModel`, `LessonNav`. Tri par `order` puis `slug` (tie-break déterministe).
    Tests AVANT (RED) : 3+ leçons → ordre correct ; prev/next aux bornes (undefined) et au milieu ; total exact ; tie-break sur order dupliqué.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec vitest run src/lib/academie/course-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/lib/academie/course-model.test.ts` exit 0.
    - prev/next aux bornes assertés undefined ; ordre asserté sur ≥3 leçons.
    - `course-model.ts` est pur : aucun import `node:fs`/`gray-matter` (grep == 0).
  </acceptance_criteria>
  <done>course-model.ts pur créé ; test Wave 0 course-model vert.</done>
</task>

<task type="auto">
  <name>Task 3: Fixtures MDX de preuve (D-03) — articles + cours trilingues</name>
  <files>apps/web/content/academie/articles/ratio-risque-rendement.fr.mdx, apps/web/content/academie/articles/ratio-risque-rendement.en.mdx, apps/web/content/academie/articles/ratio-risque-rendement.ar.mdx, apps/web/content/academie/articles/comprendre-le-levier.fr.mdx, apps/web/content/academie/articles/comprendre-le-levier.en.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.fr.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.en.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/01-installer-mt5.ar.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/02-ouvrir-une-position.fr.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/02-ouvrir-une-position.en.mdx, apps/web/content/academie/cours/prendre-en-main-mt5/03-poser-tp-sl.fr.mdx</files>
  <read_first>
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-CONTEXT.md §Specifics (priorité = tutos pratiques MetaTrader, enablement produit) + D-09 (composants pédago) + D-11 (frontmatter)
    - .planning/phases/09-cms-cours-articles-vulgaris-s/09-RESEARCH.md §Code Examples (FrontmatterSchema = forme exacte du frontmatter)
    - apps/web/src/lib/academie/frontmatter.ts (schéma à respecter)
  </read_first>
  <action>
    Écrire 3-5 contenus de preuve réels (D-03), pas du lorem. Couvrir les 3 types et prouver les cas limites :
    1. Article `ratio-risque-rendement` en fr/en/ar (frontmatter type:article, theme:gestion-risque, niveau:debutant ; corps avec un `<Callout>` et un `<TradeExample>` pour exercer les composants pédago).
    2. Article `comprendre-le-levier` en fr/en SEULEMENT (pas d'ar) — sert de fixture du fallback D-14 (theme:bases-trading, niveau:intermediaire).
    3. Cours `prendre-en-main-mt5` : 3 leçons ordonnées (frontmatter type:lecon, course:'prendre-en-main-mt5', order 1/2/3, plateforme:mt5, theme:usage-plateforme) — leçon 1 (installer) fr/en/ar, leçon 2 (ouvrir une position) fr/en, leçon 3 (poser TP/SL) fr seul. Inclure `<Steps>` et `<Callout variant="astuce">` dans le corps.
    Tout frontmatter doit valider contre FrontmatterSchema. NE PAS écrire le `<Disclaimer />` dans le MDX (il est injecté par la page — Plan 04, RESEARCH Pattern 2). Corps en langue du fichier ; aucune promesse de gain (VITR-03).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec vitest run src/lib/academie/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Les 11 fichiers MDX existent aux chemins exacts ci-dessus, frontmatter conforme à FrontmatterSchema.
    - `comprendre-le-levier` a fr+en mais PAS ar (fixture fallback D-14) ; leçon 3 a fr seul.
    - Cours `prendre-en-main-mt5` : 3 leçons avec `order` 1,2,3 et `course` identique.
    - Aucun `<Disclaimer` dans les fichiers MDX (grep == 0) — injection par page seulement.
    - Aucune promesse de gain (pas de « profit garanti », « gains assurés » — VITR-03) ; `content.test.ts` (qui charge ces fixtures) reste vert.
  </acceptance_criteria>
  <done>Jeu de fixtures de preuve trilingues créé, validant le schéma et exerçant fallback D-14 + modèle cours.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `(slug, locale)` → fs.readFile | slug/locale issus de l'URL publique → chemin fichier |
| Fichier MDX frontmatter → modèle | en-tête YAML parsé → catalogue typé |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-PATH | Tampering / Info Disclosure | content.ts résolution (slug,locale) | mitigate | **(plus haute sévérité)** Valider slug `^[a-z0-9-]+$` + locale ∈ routing.locales AVANT tout accès disque ; `path.resolve` + assert `startsWith(CONTENT_ROOT)` ; jamais de concaténation brute. Tests : `../`, `/`, MAJ, locale invalide tous rejetés sans `fs.readFile`. |
| T-09-02 | Tampering | content.ts (frontmatter scanné) | mitigate | `FrontmatterSchema.parse` sur chaque frontmatter scanné — fichier au frontmatter invalide = erreur de frontière, jamais catalogué silencieusement. |
| T-09-MDX | Tampering | fixtures MDX | accept | Contenu repo écrit par l'agent, revu en PR git (RESEARCH §Security : surface de confiance interne). Aucun upload/MDX utilisateur compilé. Pas de route compilant du MDX fourni par l'utilisateur. |
</threat_model>

<verification>
- `cd apps/web && pnpm exec vitest run src/lib/academie/content.test.ts src/lib/academie/course-model.test.ts` exit 0.
- 11 fixtures MDX présentes, frontmatter valide, sans `<Disclaimer>`.
- Path-traversal et fallback D-14 prouvés par tests.
</verification>

<success_criteria>
- content.ts + course-model.ts créés et leurs tests Wave 0 verts localement.
- Fixtures de preuve trilingues livrées (article + article fallback + cours 3 leçons).
- T-09-PATH (path-traversal, plus haute sévérité) mitigé et testé ; T-09-02 mitigé.
</success_criteria>

<output>
Create `.planning/phases/09-cms-cours-articles-vulgaris-s/09-02-SUMMARY.md` when done.
</output>
