---
phase: 01-socle-transverse-i18n-rtl-r-les-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0008_profiles_role.sql
  - supabase/migrations/0009_subscriptions_gating.sql
  - packages/supabase/src/database.types.ts
  - packages/supabase/__tests__/gating-rls.test.ts
autonomous: false
requirements: [ACCESS-02, ACCESS-03, ACCESS-04]
user_setup:
  - service: supabase
    why: "Push migrations 0008/0009 sur le projet cloud + régénérer les types (non-TTY)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (pour push non-interactif)"
    dashboard_config:
      - task: "Confirm email OFF (déjà fait v1.0, à confirmer) + .env.test rempli (URL + ANON + SERVICE_ROLE) pour passer les tests RLS en GREEN"
        location: "Supabase Dashboard -> Authentication -> Providers ; .env.test local"

must_haves:
  truths:
    - "Un utilisateur authentifié SANS abonnement actif lit 0 ligne de trade_setups via l'anon-client (RLS, pas le gate UX)"
    - "Un utilisateur authentifié SANS abonnement actif lit 0 ligne de analyses via l'anon-client"
    - "profiles.role existe avec contrainte member/affiliate/superadmin, défaut member, lu côté DB"
    - "subscriptions est une table RÉELLE (pas un stub) ; has_active_subscription() interroge subscriptions réellement"
    - "Un utilisateur ne lit que ses propres subscriptions (isolation cross-user RLS)"
  artifacts:
    - path: "supabase/migrations/0008_profiles_role.sql"
      provides: "Colonne profiles.role + helper is_superadmin() security definer"
      contains: "is_superadmin"
    - path: "supabase/migrations/0009_subscriptions_gating.sql"
      provides: "Table subscriptions + has_active_subscription() + drop/recreate RLS trade_setups/analyses"
      contains: "has_active_subscription"
    - path: "packages/supabase/__tests__/gating-rls.test.ts"
      provides: "Test anon-client : non-abonné -> 0 setup, 0 analyses, isolation subscriptions"
      contains: "toHaveLength(0)"
    - path: "packages/supabase/src/database.types.ts"
      provides: "Types régénérés incluant role + subscriptions"
      contains: "subscriptions"
  key_links:
    - from: "supabase/migrations/0009_subscriptions_gating.sql"
      to: "public.subscriptions"
      via: "has_active_subscription() lit subscriptions"
      pattern: "from public.subscriptions"
    - from: "trade_setups RLS policy"
      to: "has_active_subscription()"
      via: "using (public.has_active_subscription())"
      pattern: "has_active_subscription\\(\\)"
---

<objective>
Poser la barrière de données réelle (RLS) qui conditionne l'accès aux signaux à un abonnement actif, et la source de vérité du rôle hors JWT. C'est la seule barrière non contournable (le front lit en anon-client).

Purpose: Sans cette couche, tout gate UX est cosmétique — un appel direct à l'anon-client aspirerait tous les signaux (Pitfall #5). Le rôle dans la DB (jamais le JWT) évite l'élévation de privilège par token périmé (Pitfall #7).
Output: migrations 0008 (role + is_superadmin) et 0009 (subscriptions + has_active_subscription + recreate des policies trade_setups/analyses), types Supabase régénérés, test d'intégration anon-client prouvant « non-abonné -> 0 ligne ».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-CONTEXT.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-PATTERNS.md
@supabase/migrations/0001_init_profiles_instruments_job_runs.sql
@supabase/migrations/0006_analyses_trade_setups.sql
@packages/supabase/__tests__/rls.test.ts

<interfaces>
<!-- Noms EXACTS des policies à drop (lus dans 0006) — un nom erroné fait échouer la migration : -->
From supabase/migrations/0006_analyses_trade_setups.sql:
- policy "trade_setups: lecture authentifiés" on public.trade_setups (l.78)
- policy "analyses: lecture authentifiés" on public.analyses (l.38)

<!-- Pattern security definer EXACT (miroir handle_new_user 0001 l.86-97) : -->
- helpers RLS SQL : `language sql stable security definer set search_path = public`
- revoke/grant défensif (pattern 0002_revoke_execute_trigger_functions.sql) : `revoke execute ... from public, anon; grant execute ... to authenticated;`

<!-- Pattern test (rls.test.ts) à répliquer : -->
- env guard : `process.env['NEXT_PUBLIC_SUPABASE_URL'] ?? ''` + `if (!SUPABASE_URL || !SUPABASE_ANON_KEY) return` dans chaque it
- signUpAndGetClient(email,password) + deleteUser(userId) via service_role en afterAll ; emails `@gmail.com` uniques par Date.now()
- assertion deny silencieux : `expect(error).toBeNull()` + `expect(data).toHaveLength(0)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 0008 (profiles.role + is_superadmin) et 0009 (subscriptions + has_active_subscription + recreate RLS)</name>
  <read_first>
    - supabase/migrations/0001_init_profiles_instruments_job_runs.sql (pattern text+check l.35-41 ; handle_new_user security definer l.86-97)
    - supabase/migrations/0006_analyses_trade_setups.sql (NOMS EXACTS des policies à drop ; table + RLS authenticated)
    - supabase/migrations/0002_revoke_execute_trigger_functions.sql (pattern revoke execute)
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-PATTERNS.md (sections 0008/0009 — DDL complet cité)
  </read_first>
  <action>
    Créer 0008_profiles_role.sql : `alter table public.profiles add column role text not null default 'member' check (role in ('member','affiliate','superadmin'))` (D-05 ; text+check cohérent 0001, PAS enum natif). Puis créer `public.is_superadmin()` returns boolean `language sql stable security definer set search_path = public` qui fait `select exists (select 1 from public.profiles p where p.id = auth.uid() and p.role = 'superadmin')` (Pitfall 6 : search_path figé obligatoire). Ajouter `revoke execute on function public.is_superadmin() from public, anon; grant execute on function public.is_superadmin() to authenticated;`. Aucune nouvelle policy profiles (l'user lit déjà sa ligne via 0001).

    Créer 0009_subscriptions_gating.sql : table `public.subscriptions` (id uuid pk default gen_random_uuid ; user_id uuid not null references public.profiles(id) on delete cascade ; status text not null default 'pending' check status in (pending/active/expired/canceled) ; plan text not null default 'standard' check plan in (discovery/standard) ; current_period_end timestamptz nullable ; created_at timestamptz not null default now). Colonne d'expiration = `current_period_end` (A6 TRANCHÉ : D-05/CONTEXT cite current_period_end, PAS expires_at d'ARCHITECTURE ; le helper RLS DOIT référencer ce même nom). `enable row level security`. Deux policies SELECT to authenticated : « subscriptions: lire les siennes » using (user_id = auth.uid()) ET « subscriptions: superadmin voit tout » using (public.is_superadmin()). AUCUNE policy insert/update/delete (activation = service_role en P4 — D-04). Index `subscriptions_active_idx on (user_id, status, current_period_end)`.

    Toujours dans 0009 : `public.has_active_subscription()` returns boolean `language sql stable security definer set search_path = public` qui fait `select exists (select 1 from public.subscriptions s where s.user_id = auth.uid() and s.status = 'active' and s.current_period_end > now())`. revoke/grant comme is_superadmin. Enfin DROP/RECREATE les policies de lecture : `drop policy "trade_setups: lecture authentifiés" on public.trade_setups;` puis recréer « trade_setups: abonnés actifs » for select to authenticated using (public.has_active_subscription()) ; idem `drop policy "analyses: lecture authentifiés" on public.analyses;` puis « analyses: abonnés actifs » using (public.has_active_subscription()). NE PAS toucher aux écritures (service_role bypass inchangé — invariant producteur-unique, aucune écriture front introduite).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0009_subscriptions_gating.sql | grep -c "has_active_subscription" | awk '$1>=3{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 0008 contient la colonne role text+check(member/affiliate/superadmin) default 'member' et la fonction is_superadmin() avec `security definer set search_path = public`.
    - 0009 crée subscriptions avec la colonne `current_period_end` (PAS expires_at), 2 policies SELECT, AUCUNE policy write, et has_active_subscription() référençant `current_period_end`.
    - 0009 droppe les policies aux noms EXACTS « trade_setups: lecture authentifiés » / « analyses: lecture authentifiés » et recrée des policies using(public.has_active_subscription()).
    - Les deux helpers ont un revoke from public,anon + grant to authenticated.
  </acceptance_criteria>
  <done>Les deux fichiers de migration existent, syntaxiquement valides, helpers security definer search_path figé, policies recréées sur les deux tables.</done>
</task>

<task type="auto">
  <name>Task 2: Test d'intégration anon-client gating-rls (Wave 0 — couvre ACCESS-02/03/04)</name>
  <read_first>
    - packages/supabase/__tests__/rls.test.ts (pattern intégral : env guard, signUpAndGetClient, deleteUser afterAll, assertion 0 ligne)
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-PATTERNS.md (section gating-rls.test.ts)
  </read_first>
  <action>
    Créer packages/supabase/__tests__/gating-rls.test.ts en répliquant le pattern de rls.test.ts (mêmes helpers signUpAndGetClient/deleteUser, mêmes env guards, cleanup afterAll via service_role, emails @gmail.com uniques par Date.now()). Tests : (1) « non-abonné lit 0 trade_setup via anon-client » — clientA.from('trade_setups').select('id') → expect(error).toBeNull() + expect(data).toHaveLength(0) (à ce stade 0 abonnement → 0 ligne via has_active_subscription, ACCESS-02). (2) « non-abonné lit 0 analyses via anon-client » → toHaveLength(0) (ACCESS-04). (3) « isolation subscriptions cross-user » : créer une subscription pour B via service_role (status active, current_period_end futur), prouver que clientA.from('subscriptions').select('id') ne voit PAS celle de B (toHaveLength(0)) — miroir isolation profiles de rls.test.ts l.120-129 (ACCESS-03/04). Inclure le it() RED documenté si les env vars manquent (pattern rls.test.ts l.94-106). Ce test est RED tant que 0008/0009 ne sont pas poussées + .env.test rempli — c'est l'état attendu jusqu'à la Task 3.
  </action>
  <verify>
    <automated>test -f packages/supabase/__tests__/gating-rls.test.ts && grep -c "toHaveLength(0)" packages/supabase/__tests__/gating-rls.test.ts | awk '$1>=3{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier existe et réplique env guard + signUpAndGetClient + deleteUser du pattern rls.test.ts.
    - Au moins 3 assertions toHaveLength(0) : trade_setups, analyses, isolation subscriptions cross-user.
    - `pnpm --filter @app/supabase test gating-rls` s'exécute (RED attendu sans .env.test ; GREEN après Task 3).
  </acceptance_criteria>
  <done>Test gating-rls.test.ts présent, exécutable par Vitest, couvrant ACCESS-02/03/04.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Push schéma 0008/0009 sur le cloud + régénération des types</name>
  <what-built>
    Migrations 0008/0009 écrites localement (Task 1). Le build/typecheck passe SANS push (types lus depuis database.types.ts généré, pas la DB live) — d'où le faux positif si on saute cette étape. Le push est obligatoire pour que les tests RLS passent et que database.types.ts reflète role+subscriptions.
  </what-built>
  <how-to-verify>
    1. S'assurer que SUPABASE_ACCESS_TOKEN est exporté et le projet linké (`supabase link`) pour un push non-interactif.
    2. Exécuter : `supabase db push` (applique 0008 puis 0009 dans l'ordre). Si des prompts interactifs ne peuvent pas être supprimés, appliquer 0008 puis 0009 via MCP apply_migration (canal utilisé pour 0006).
    3. Régénérer les types : `supabase gen types typescript --linked > packages/supabase/src/database.types.ts`. Vérifier que `subscriptions` et la colonne `role` apparaissent dans le fichier.
    4. Remplir `.env.test` (NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY + SUPABASE_SERVICE_ROLE_KEY) si absent.
    5. Lancer `pnpm --filter @app/supabase test gating-rls` → doit être GREEN (non-abonné lit 0 trade_setup/analyses ; isolation subscriptions).
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db push` (ou apply_migration MCP) appliqué sans erreur sur 0008 et 0009.
    - packages/supabase/src/database.types.ts contient `subscriptions` et le champ `role`.
    - `pnpm --filter @app/supabase test gating-rls` GREEN.
    - `pnpm --filter @app/supabase test rls` (test existant AUTH-02) reste GREEN (non régressé par le drop/recreate des policies).
  </acceptance_criteria>
  <resume-signal>Type "approved" une fois le push appliqué, les types régénérés et gating-rls GREEN, ou décris les erreurs rencontrées.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon-client public) → Postgres | Le front lit avec la clé anon publique ; un appel direct (devtools/script) franchit cette frontière sans passer par le gate UX. SEULE la RLS tranche. |
| JWT (claims) → décision d'accès | Le token peut être périmé/forgé ; le rôle ne doit JAMAIS en être déduit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | trade_setups/analyses via anon-client | mitigate | RLS `using(public.has_active_subscription())` sur les deux tables ; test anon-client « non-abonné → 0 ligne » (Task 2/3) |
| T-01-02 | Elevation of Privilege | profiles.role | mitigate | role en DB (jamais JWT) ; is_superadmin() lit profiles après auth.uid() ; aucune policy permettant à l'user d'UPDATE son role (write réservé service_role) |
| T-01-03 | Tampering / EoP | helpers RLS | mitigate | `security definer set search_path = public` (miroir handle_new_user 0001) ; tables référencées en public.xxx ; revoke from public,anon |
| T-01-04 | Information Disclosure | subscriptions cross-user | mitigate | policy SELECT using(user_id = auth.uid()) ; test isolation clientA ne voit pas les abos de B |
| T-01-SC | Tampering | npm installs | accept | Aucun nouveau package npm dans ce plan (migrations SQL + test sur Vitest existant) ; pas de surface supply-chain |
</threat_model>

<verification>
- `pnpm --filter @app/supabase test gating-rls` GREEN après push (ACCESS-02/03/04).
- `pnpm --filter @app/supabase test rls` non régressé.
- grep confirme has_active_subscription dans les policies trade_setups/analyses ET dans le helper.
- database.types.ts régénéré contient subscriptions + role.
</verification>

<success_criteria>
- Un utilisateur authentifié sans abonnement actif lit 0 trade_setup et 0 analyses via l'anon-client (prouvé par test).
- profiles.role est la source de vérité du rôle, jamais le JWT ; is_superadmin() et has_active_subscription() sont des fonctions RÉELLES security definer.
- Aucune écriture front introduite (invariant producteur-unique préservé).
</success_criteria>

<output>
Create `.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-01-SUMMARY.md` when done
</output>
