---
phase: 01-socle-transverse-i18n-rtl-r-les-gating
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - apps/web/e2e/i18n.spec.ts
  - apps/web/e2e/gating.spec.ts
  - apps/web/e2e/auth.spec.ts
  - scripts/check-i18n-hardcoded.mjs
  - package.json
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04, ACCESS-01, ACCESS-02, ACCESS-03]

must_haves:
  truths:
    - "Un E2E prouve la bascule fr/en/ar, la locale dans l'URL, sa persistance, et <html dir=\"rtl\"> en arabe"
    - "Un E2E prouve les redirections de gate : non-auth→login+returnTo, auth-sans-abo→/tarifs, non-superadmin→404 admin"
    - "Un E2E prouve que returnTo protocol-relative (//evil.com) ne provoque pas d'open-redirect après login"
    - "Un check statique échoue si du texte littéral en dur apparaît dans le JSX hors messages/ (I18N-03)"
    - "auth.spec.ts mis à jour vers les URLs localisées (ne casse plus après le déplacement [locale])"
  artifacts:
    - path: "apps/web/e2e/i18n.spec.ts"
      provides: "E2E I18N-01/02/04 (bascule locale, dir rtl, formatage)"
      contains: "rtl"
    - path: "apps/web/e2e/gating.spec.ts"
      provides: "E2E ACCESS-01/02/03 (redirections login/tarifs, 404 admin, returnTo same-origin)"
      contains: "returnTo"
    - path: "scripts/check-i18n-hardcoded.mjs"
      provides: "Grep CI anti-chaîne-dure (I18N-03)"
      contains: "messages"
  key_links:
    - from: "package.json"
      to: "scripts/check-i18n-hardcoded.mjs"
      via: "script lint:i18n"
      pattern: "lint:i18n"
    - from: "apps/web/e2e/i18n.spec.ts"
      to: "[locale]/layout dir attribute"
      via: "expect(html).toHaveAttribute('dir','rtl')"
      pattern: "dir.*rtl"
---

<objective>
Prouver de bout en bout les 8 success criteria de la phase via E2E Playwright + un check statique anti-chaîne-dure, et réparer auth.spec.ts cassé par le déplacement sous [locale].

Purpose: La phase est le cœur sécurité du milestone ; ses garanties (gating, i18n/RTL, aucune chaîne en dur) doivent être vérifiables automatiquement et le rester (régression). C'est le filet Nyquist de la phase.
Output: e2e/i18n.spec.ts, e2e/gating.spec.ts, auth.spec.ts localisé, script check-i18n-hardcoded.mjs + script npm lint:i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-PATTERNS.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-UI-SPEC.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-03-SUMMARY.md
@apps/web/e2e/auth.spec.ts

<interfaces>
<!-- Patterns Playwright réutilisables (auth.spec.ts) : -->
- uniqueEmail(tag) via Date.now() ; signUp(page,email) helper ; await expect(page).toHaveURL(...)
- ATTENTION : auth.spec.ts actuel cible '/signup','/dashboard','/login' NON localisés → cassés après le déplacement [locale]. Les URLs deviennent '/fr/signup', '/fr/dashboard', '/fr/login' (defaultLocale fr, localePrefix always).

<!-- Cibles à asserter (du Plan 03) : -->
- bascule de langue via LanguageSwitcher (dropdown header) → URL change /fr/… → /ar/… en restant sur la même page ; cookie NEXT_LOCALE
- <html dir="rtl"> uniquement en /ar ; <html lang="ar">
- non-auth sur /fr/<member> → redirigé /fr/login?returnTo=… (ACCESS-01)
- auth SANS abo sur /fr/<member> → redirigé /fr/tarifs (ACCESS-02 UX, D-07) — en P1 AUCUN user n'a d'abo actif, donc tout user fraîchement créé déclenche cette redirection
- non-superadmin sur (admin) (hors [locale]) → 404 (ACCESS-03, D-09)
- returnTo protocol-relative (//evil.com) → après login, ne redirige PAS vers //evil.com (T-01-07)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/i18n.spec.ts + mise à jour auth.spec.ts (URLs localisées)</name>
  <read_first>
    - apps/web/e2e/auth.spec.ts (helpers + URLs à localiser)
    - .planning/phases/01-...-gating/01-PATTERNS.md (section e2e ; bascule locale, assert html dir)
    - .planning/phases/01-...-gating/01-UI-SPEC.md (§Sélecteur de langue — comportement)
  </read_first>
  <action>
    Mettre à jour apps/web/e2e/auth.spec.ts : remplacer toutes les URLs nues par leur forme localisée (`/signup`→`/fr/signup`, `/dashboard`→`/fr/dashboard`, `/login`→`/fr/login`) dans goto et toHaveURL, sinon les tests existants AUTH-01 cassent après le déplacement [locale]. Garder la logique inchangée.

    Créer apps/web/e2e/i18n.spec.ts (réutiliser le pattern auth.spec.ts) : (1) I18N-01 bascule de langue — goto /fr/login, ouvrir le LanguageSwitcher, choisir العربية, assert `await expect(page).toHaveURL(/\/ar\//)` en restant sur la même page (login) ; recharger → la locale persiste (cookie NEXT_LOCALE). (2) I18N-02 RTL — sur /ar/…, `await expect(page.locator('html')).toHaveAttribute('dir','rtl')` et `toHaveAttribute('lang','ar')` ; sur /fr/…, dir='ltr'. (3) I18N-01 locale dans l'URL — goto '/' redirige vers '/fr' (defaultLocale). (4) I18N-04 (si une donnée formatée est rendue en P1, sinon documenter en manual-only) — assert le format d'un nombre/date locale-aware via `<bdi>` ; si aucune donnée numérique n'est rendue en P1, marquer ce point en Manual-Only (consommé P3+).
  </action>
  <verify>
    <automated>test -f apps/web/e2e/i18n.spec.ts && grep -q "dir" apps/web/e2e/i18n.spec.ts && grep -q "/fr/" apps/web/e2e/auth.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - auth.spec.ts cible les URLs localisées /fr/… (n'utilise plus de chemins nus).
    - i18n.spec.ts couvre : bascule fr→ar même page + persistance, html dir=rtl/lang=ar en arabe et dir=ltr en fr, redirection / → /fr.
    - I18N-04 couvert en E2E si une donnée formatée existe, sinon listé en Manual-Only avec justification.
  </acceptance_criteria>
  <done>i18n.spec.ts prouve I18N-01/02 (+04 ou manual) ; auth.spec.ts réparé pour les URLs localisées.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/gating.spec.ts (ACCESS-01/02/03 + returnTo same-origin)</name>
  <read_first>
    - apps/web/e2e/auth.spec.ts (helper signUp, assert toHaveURL ; cas non-auth→login l.55-59)
    - .planning/phases/01-...-gating/01-PATTERNS.md (section e2e gating)
    - .planning/phases/01-...-gating/01-CONTEXT.md (D-07/08/09)
  </read_first>
  <action>
    Créer apps/web/e2e/gating.spec.ts : (1) ACCESS-01 / D-08 — visiteur non auth qui goto une surface (member) (ex. /fr/<route member>) est redirigé vers /fr/login avec le param returnTo non vide (`await expect(page).toHaveURL(/\/fr\/login\?returnTo=/)`). (2) ACCESS-03 / D-09 — un utilisateur authentifié NON superadmin qui goto la route (admin) reçoit un 404 (assert le statut de réponse 404 via le retour de page.goto, ou la présence du marqueur 404 Next) ; un visiteur non auth idem 404 (discrétion). (3) ACCESS-02 / D-07 — MANDATORY : créer un user via signUp (uniqueEmail), puis goto une surface (member) /fr/<route member> ; comme AUCUN user n'a d'abonnement actif en P1, requireActiveSub redirige vers /fr/tarifs → `await expect(page).toHaveURL(/\/fr\/tarifs/)`. (4) T-01-07 open-redirect — goto `/fr/login?returnTo=//evil.com`, se connecter (signIn d'un user valide), puis assert que l'URL finale n'est PAS `//evil.com` ni un domaine externe (`expect(page.url()).not.toContain('evil.com')` ET reste sur l'origine de baseURL — par ex. redirigé vers /fr/dashboard ou la racine locale). Réutiliser uniqueEmail/signUp d'auth.spec.ts. Documenter les pré-requis GREEN (.env, dev server, migrations poussées) en tête comme auth.spec.ts.
  </action>
  <verify>
    <automated>test -f apps/web/e2e/gating.spec.ts && grep -q "returnTo" apps/web/e2e/gating.spec.ts && grep -q "404" apps/web/e2e/gating.spec.ts && grep -q "tarifs" apps/web/e2e/gating.spec.ts && grep -q "evil.com" apps/web/e2e/gating.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - gating.spec.ts asserte : non-auth sur (member) → /fr/login?returnTo=… (ACCESS-01) ; non-superadmin sur (admin) → 404 (ACCESS-03/D-09).
    - Le cas auth-sans-abo → /fr/tarifs (ACCESS-02/D-07) est couvert (MANDATORY, trivial en P1 : aucun user n'a d'abo).
    - Le cas returnTo=//evil.com → après login NE redirige PAS hors origine (T-01-07).
  </acceptance_criteria>
  <done>gating.spec.ts prouve les redirections (login/tarifs), le 404 admin, et la mitigation open-redirect de la couche UX (la barrière données est prouvée par gating-rls.test.ts du Plan 01).</done>
</task>

<task type="auto">
  <name>Task 3: Check statique anti-chaîne-dure (I18N-03) + script npm</name>
  <read_first>
    - .planning/phases/01-...-gating/01-RESEARCH.md (§Validation : I18N-03 grep anti-chaîne-dure)
    - apps/web/src/messages/fr.json (référence des chaînes autorisées)
    - package.json (racine — où ajouter le script lint:i18n)
  </read_first>
  <action>
    Créer scripts/check-i18n-hardcoded.mjs : un script Node qui scanne apps/web/src/app/**/*.tsx (et components/**/*.tsx) à la recherche de texte littéral visible dans le JSX hors messages/ — c.-à-d. du texte entre balises `>...texte...<` ou des attributs visibles (placeholder, aria-label, title, alt) contenant des lettres alphabétiques de plus de 1 mot, qui ne soit pas une expression `{t(...)}` / `{format...}` / une variable. Exclure : les fichiers de config i18n, les commentaires, les imports, les noms de classes Tailwind, les attributs techniques (name, type, autoComplete), et les autonymes connus (Français/English/العربية). Le script sort en code 1 (échec CI) avec la liste fichier:ligne des violations, code 0 sinon. Tolérance : viser zéro faux négatif sur le texte de paragraphe/titre ; accepter quelques faux positifs documentés via une liste d'exclusion explicite (commentaire `// i18n-ignore` en bout de ligne).

    Ajouter dans package.json (racine) un script `"lint:i18n": "node scripts/check-i18n-hardcoded.mjs"`. Lancer le script et corriger toute chaîne en dur résiduelle détectée dans le code livré par le Plan 03 (login/signup/dashboard/tarifs/LanguageSwitcher) en l'externalisant vers messages/.
  </action>
  <verify>
    <automated>node scripts/check-i18n-hardcoded.mjs; test $? -eq 0 && echo "i18n clean"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-i18n-hardcoded.mjs existe, sort 1 sur violation (avec fichier:ligne), 0 sinon.
    - package.json expose `lint:i18n`.
    - `node scripts/check-i18n-hardcoded.mjs` sort 0 sur le code livré (aucune chaîne en dur dans le JSX P1).
  </acceptance_criteria>
  <done>Check I18N-03 automatisé et vert ; toute chaîne en dur résiduelle externalisée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → app live | Les E2E s'exécutent contre l'app réelle (cookies/redirects réels) — c'est la vérification de la frontière de sécurité au niveau UX. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | (admin) enumeration | mitigate | gating.spec.ts asserte 404 (pas 403) pour non-superadmin — vérifie la mitigation D-09 du Plan 03 |
| T-01-07 | Open Redirect | returnTo | mitigate | gating.spec.ts prouve qu'un returnTo `//evil.com` ne redirige pas hors origine après login — vérifie la validation same-origin du gate (Plan 03) |
| T-01-10 | Information Disclosure | chaînes en dur | mitigate | check-i18n-hardcoded.mjs empêche la fuite de texte non traduit/non revu (I18N-03) ; barrière de régression CI |
| T-01-SC | Tampering | npm installs | accept | Aucun nouveau package npm (Playwright + Node natifs déjà présents) |
</threat_model>

<verification>
- `pnpm test:e2e` (i18n + gating + auth) GREEN avec dev server + .env (pré-requis documentés).
- `node scripts/check-i18n-hardcoded.mjs` sort 0.
- auth.spec.ts (AUTH-01) non régressé après localisation des URLs.
</verification>

<success_criteria>
- I18N-01/02 (+04) prouvés E2E ; ACCESS-01/02/03 prouvés E2E (redirections login/tarifs/404 + open-redirect) ; I18N-03 prouvé statiquement.
- La barrière données (ACCESS-02 RLS / ACCESS-04) reste prouvée par gating-rls.test.ts (Plan 01) — la phase entière a une couverture automatisée des 8 requirements.
</success_criteria>

<output>
Create `.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-04-SUMMARY.md` when done
</output>
</content>
