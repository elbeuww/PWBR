---
phase: 01-socle-transverse-i18n-rtl-r-les-gating
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/app/[locale]/(auth)/login/page.tsx
  - apps/web/src/app/[locale]/(auth)/signup/page.tsx
  - apps/web/src/app/[locale]/(auth)/actions.ts
  - apps/web/src/app/[locale]/(member)/layout.tsx
  - apps/web/src/app/[locale]/(member)/dashboard/page.tsx
  - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/components/LanguageSwitcher.tsx
  - apps/web/src/lib/auth/gate.ts
  - apps/web/src/lib/supabase/middleware.ts
  - apps/web/middleware.ts
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04, ACCESS-01, ACCESS-02, ACCESS-03]

must_haves:
  truths:
    - "Toutes les routes UI vivent sous /[locale]/… ; (admin) reste HORS [locale]"
    - "Un seul <html lang dir> (dans [locale]/layout) ; le root layout est un pass-through"
    - "En arabe <html dir=\"rtl\"> ; le sélecteur de langue change la locale en restant sur la même page"
    - "Le middleware compose handleI18n PUIS updateSession en MUTANT la response next-intl (pas de NextResponse.next recréée)"
    - "Un visiteur non auth sur (member) est redirigé vers login+returnTo ; un auth sans abo vers /tarifs ; un non-superadmin sur (admin) reçoit 404"
  artifacts:
    - path: "apps/web/src/app/[locale]/layout.tsx"
      provides: "Shell <html lang dir> + setRequestLocale + NextIntlClientProvider + header LanguageSwitcher"
      contains: "NextIntlClientProvider"
    - path: "apps/web/src/lib/auth/gate.ts"
      provides: "requireUser / requireActiveSub / requireRole"
      exports: ["requireUser", "requireActiveSub", "requireRole"]
    - path: "apps/web/src/components/LanguageSwitcher.tsx"
      provides: "Dropdown accessible de bascule de langue (cookie NEXT_LOCALE, même page)"
      contains: "usePathname"
    - path: "apps/web/middleware.ts"
      provides: "Composition handleI18n → updateSession + header x-pathname"
      contains: "handleI18n"
    - path: "apps/web/src/app/(admin)/layout.tsx"
      provides: "Gate superadmin → notFound (404, D-09)"
      contains: "requireRole"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/src/lib/supabase/middleware.ts"
      via: "updateSession(request, response)"
      pattern: "updateSession\\(request, response\\)"
    - from: "apps/web/src/app/[locale]/(member)/layout.tsx"
      to: "apps/web/src/lib/auth/gate.ts"
      via: "await requireActiveSub()"
      pattern: "requireActiveSub"
    - from: "apps/web/src/lib/auth/gate.ts"
      to: "public.subscriptions / public.profiles"
      via: "getUser() puis select role / subscriptions active"
      pattern: "getUser"
---

<objective>
Faire la jointure des deux fondations : déplacer l'arbre app/ sous le segment [locale] avec un seul <html lang dir> dynamique, composer le middleware locale→session, livrer la primitive d'accès (gate.ts : 3 portes) et le sélecteur de langue accessible. Câbler les redirections de gate (D-07/08/09) avec returnTo validé.

Purpose: C'est la couche UX du gating (défense en profondeur AU-DESSUS de la RLS du Plan 01) + l'activation effective de l'i18n du Plan 02 sur des pages réelles. Sans elle, l'i18n et le gating restent de la config inerte.
Output: middleware composé, segment [locale] peuplé (auth/member/dashboard/tarifs déplacés), (admin) hors locale, gate.ts, LanguageSwitcher, redirects auth localisés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-CONTEXT.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-PATTERNS.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-UI-SPEC.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-02-SUMMARY.md
@apps/web/src/app/layout.tsx
@apps/web/src/app/dashboard/page.tsx
@apps/web/src/app/(auth)/actions.ts
@apps/web/src/app/(auth)/login/page.tsx
@apps/web/middleware.ts
@apps/web/src/lib/supabase/middleware.ts
@apps/web/src/lib/supabase/server.ts

<interfaces>
<!-- API i18n livrée par Plan 02 (à importer, ne PAS recréer) : -->
From apps/web/src/i18n/navigation.ts: Link, redirect, usePathname, useRouter, getPathname (localisés)
From apps/web/src/i18n/routing.ts: routing { locales:['fr','en','ar'], defaultLocale:'fr', localePrefix:'always' }
From next-intl: NextIntlClientProvider, hasLocale, useTranslations, useFormatter
From next-intl/server: setRequestLocale, getMessages, getTranslations
From next-intl/middleware: createMiddleware

<!-- Schéma DB livré par Plan 01 (types régénérés) : subscriptions(user_id,status,current_period_end), profiles.role -->

<!-- Patterns existants à réutiliser : -->
From apps/web/src/lib/supabase/server.ts: createClient() async (cookies getAll/setAll)
From apps/web/src/app/dashboard/page.tsx: getUser()+redirect pattern (l.18-26) — getUser jamais getSession (T-03)
From apps/web/src/lib/supabase/middleware.ts: updateSession ACTUEL recrée NextResponse.next dans setAll (l.29) → À CORRIGER

<!-- Décisions : D-07 non-abo→/tarifs ; D-08 non-auth→login+returnTo ; D-09 non-superadmin→404 ; D-10 sélecteur cookie/même page -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware composé (handleI18n → updateSession en mutant la response + x-pathname)</name>
  <read_first>
    - apps/web/middleware.ts + apps/web/src/lib/supabase/middleware.ts (composition + correction NextResponse)
    - .planning/phases/01-...-gating/01-RESEARCH.md (Pattern 2 middleware ; Pitfall 2)
    - .planning/phases/01-...-gating/01-PATTERNS.md (sections middleware racine, middleware supabase)
  </read_first>
  <action>
    Modifier apps/web/src/lib/supabase/middleware.ts : `updateSession(request, response: NextResponse)` qui MUTE la response transmise (ne recrée JAMAIS NextResponse.next dans setAll — sinon le rewrite locale next-intl est perdu, Pitfall 2). Le setAll itère les cookies et fait `response.cookies.set(...)` sur la response reçue, sans appeler NextResponse.next(). Conserver getUser() (jamais getSession, invariant T-03).

    Modifier apps/web/middleware.ts : `import createMiddleware from 'next-intl/middleware'`, `const handleI18n = createMiddleware(routing)`, `export async function middleware(request){ const response = handleI18n(request); response.headers.set('x-pathname', request.nextUrl.pathname); return await updateSession(request, response) }` (le header x-pathname alimente returnTo du gate — RESEARCH Q2). Conserver le matcher existant (exclut assets/_next ; ajouter `api` à l'exclusion comme dans le pattern next-intl).
  </action>
  <verify>
    <automated>grep -q "updateSession(request, response)" apps/web/middleware.ts && grep -q "x-pathname" apps/web/middleware.ts && grep -q "handleI18n" apps/web/middleware.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts compose handleI18n PUIS updateSession(request, response) et pose le header x-pathname.
    - supabase/middleware.ts mute la response passée (response.cookies.set), ne recrée pas NextResponse.next dans setAll, conserve getUser().
    - Le matcher exclut assets/_next/api.
  </acceptance_criteria>
  <done>Middleware composé sans perte de locale ni de session ; header x-pathname posé pour le returnTo du gate.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure [locale] (un seul html lang/dir, root pass-through) + header LanguageSwitcher accessible</name>
  <read_first>
    - apps/web/src/app/layout.tsx (root <html lang="fr"> à réduire en pass-through — Pitfall 7)
    - .planning/phases/01-...-gating/01-RESEARCH.md (Pattern 1 layout ; Pitfalls 3/7)
    - .planning/phases/01-...-gating/01-PATTERNS.md (sections [locale]/layout, LanguageSwitcher)
    - .planning/phases/01-...-gating/01-UI-SPEC.md (§Shell, §Sélecteur de langue : a11y dropdown, autonymes, touch target 44px, RTL)
  </read_first>
  <action>
    Créer apps/web/src/app/[locale]/layout.tsx (le SEUL <html>) : `const { locale } = await params; if (!hasLocale(routing.locales, locale)) notFound(); setRequestLocale(locale)` (Pitfall 3 : sans setRequestLocale, rendu dynamique forcé), `const messages = await getMessages()`, retourner `<html lang={locale} dir={locale==='ar'?'rtl':'ltr'}><body><NextIntlClientProvider messages={messages}><header>…<LanguageSwitcher/></header>{children}</NextIntlClientProvider></body></html>` ; importer globals.css ; `export function generateStaticParams(){ return routing.locales.map(locale=>({locale})) }`. Header : barre horizontale hauteur 56px, padding inline md, fond secondary, LanguageSwitcher ancré à l'`end` logique (UI-SPEC §Shell) — utilities LOGIQUES uniquement (ms/me/ps/pe/text-start, JAMAIS ml/mr/pl/pr/text-left).

    Réduire apps/web/src/app/layout.tsx en pass-through : retirer <html>/<body> (déplacés), `return children` (ou conserver l'export metadata si root subsiste). Valider A5 : un SEUL <html> rendu (Pitfall 7).

    Créer apps/web/src/components/LanguageSwitcher.tsx (client component 'use client') : dropdown accessible — déclencheur bouton touch target ≥44×44px (globe lucide + autonyme actif Label 14/600 + chevron lucide), liste 3 items Français/English/العربية (autonymes via messages, jamais traduits), au choix garder la même page via `usePathname()` + `useRouter().replace(pathname, {locale})` de i18n/navigation (D-10, persistance cookie NEXT_LOCALE gérée par next-intl), a11y : aria-haspopup=listbox, aria-expanded, navigation flèches, Enter/Espace sélectionne, Échap ferme+refocus, aria-current/aria-selected sur l'item actif, focus-visible anneau accent #2563EB 2px. RTL : menu ancré à l'`end` (text-start sur items). Aucune chaîne en dur (useTranslations).
  </action>
  <verify>
    <automated>grep -rL "<html" apps/web/src/app/layout.tsx >/dev/null && grep -q "dir={locale" apps/web/src/app/[locale]/layout.tsx && grep -q "usePathname" apps/web/src/components/LanguageSwitcher.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - [locale]/layout.tsx rend le seul <html lang={locale} dir=…> avec setRequestLocale + generateStaticParams + NextIntlClientProvider + header LanguageSwitcher.
    - root layout.tsx ne contient plus de <html> (pass-through).
    - LanguageSwitcher : dropdown accessible (aria-haspopup/expanded, clavier, focus-visible), autonymes non traduits, même-page via usePathname/useRouter, utilities logiques uniquement.
  </acceptance_criteria>
  <done>Restructure [locale] avec un seul html lang/dir, root pass-through, sélecteur de langue accessible RTL-aware.</done>
</task>

<task type="auto">
  <name>Task 3: gate.ts (3 portes) + layouts gated (member, admin) + page tarifs placeholder</name>
  <read_first>
    - apps/web/src/app/dashboard/page.tsx (getUser+redirect l.18-26) ; apps/web/src/lib/supabase/server.ts (createClient)
    - packages/supabase/src/repositories/profiles.ts (select .eq('id',user.id).single() pattern)
    - .planning/phases/01-...-gating/01-RESEARCH.md (Pattern 3 layout gate, Pattern 4 gate.ts ; D-07/08/09)
    - .planning/phases/01-...-gating/01-PATTERNS.md (section gate.ts — code cité)
    - .planning/phases/01-...-gating/01-UI-SPEC.md (§États de gating ; §tarifs placeholder copy)
  </read_first>
  <action>
    Créer apps/web/src/lib/auth/gate.ts ('server-only') avec 3 portes. `requireUser()` : createClient() puis `getUser()` (JAMAIS getSession, Pitfall 4) ; si pas d'user, lire `(await headers()).get('x-pathname') ?? '/'`, VALIDER que c'est un chemin relatif same-origin (commence par '/', PAS '//' protocol-relative, PAS d'URL absolue — sinon fallback '/') puis `redirect({ href: { pathname: '/login', query: { returnTo: path } } })` (redirect localisé de i18n/navigation — D-08, open-redirect mitigé). `requireActiveSub()` : await requireUser(), select subscriptions status='active' & current_period_end > now() limit 1 ; si vide `redirect('/tarifs')` (D-07 funnel, PAS login). `requireRole('superadmin'|'affiliate')` : await requireUser(), select profiles.role .eq('id',user.id).single() ; si role ≠ attendu : superadmin→`notFound()` (404 discrétion, D-09), affiliate→redirect('/'). Le gate est la porte UX ; la non-fuite dépend de la RLS du Plan 01.

    Créer apps/web/src/app/[locale]/(member)/layout.tsx : `await requireActiveSub(); return <>{children}</>` (Pattern 3). Créer apps/web/src/app/(admin)/layout.tsx (HORS [locale], mono-langue) : `await requireRole('superadmin'); return <>{children}</>` (→ notFound si non superadmin, D-09). Créer apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx : page placeholder (setRequestLocale + getTranslations) — titre Heading « Tarifs » + corps « Le détail des offres arrive bientôt. » via messages pricing (I18N-03 ; contenu réel P2). Utilities logiques uniquement.
  </action>
  <verify>
    <automated>grep -q "getUser" apps/web/src/lib/auth/gate.ts && ! grep -q "getSession" apps/web/src/lib/auth/gate.ts && grep -q "notFound" apps/web/src/app/(admin)/layout.tsx && grep -q "requireActiveSub" apps/web/src/app/[locale]/(member)/layout.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - gate.ts exporte requireUser/requireActiveSub/requireRole, utilise getUser() (jamais getSession), valide returnTo comme chemin relatif same-origin (rejette // et URLs absolues).
    - requireActiveSub redirige vers /tarifs (D-07) ; requireUser vers login+returnTo (D-08) ; requireRole('superadmin') → notFound (D-09).
    - (member)/layout appelle requireActiveSub ; (admin)/layout (hors [locale]) appelle requireRole('superadmin').
    - tarifs/page : placeholder via messages, setRequestLocale présent, aucune chaîne en dur.
    - `pnpm --filter web typecheck` passe : gate.ts + layouts typés contre les types régénérés (subscriptions/role) du Plan 01.
  </acceptance_criteria>
  <done>Primitive d'accès (3 portes) + layouts gated + cible de redirection tarifs livrés ; redirections D-07/08/09 câblées avec returnTo validé ; typecheck vert contre database.types.ts régénéré.</done>
</task>

<task type="auto">
  <name>Task 4: Déplacer (auth) + dashboard sous [locale], localiser les redirects auth, externaliser les chaînes</name>
  <read_first>
    - apps/web/src/app/(auth)/actions.ts (redirects '/dashboard'/'/login'/'/signup' à localiser)
    - apps/web/src/app/(auth)/login/page.tsx + signup/page.tsx (markup à déplacer + externaliser)
    - apps/web/src/app/dashboard/page.tsx (à déplacer ; getUser inline à retirer si gated par layout)
    - .planning/phases/01-...-gating/01-PATTERNS.md (sections actions.ts, login/signup move, dashboard move — RESEARCH Q1/Q3)
  </read_first>
  <action>
    Déplacer (auth)/login/page.tsx, (auth)/signup/page.tsx, (auth)/actions.ts sous apps/web/src/app/[locale]/(auth)/ (supprimer les anciens). Dans actions.ts : remplacer les redirects nus (`/dashboard`, `/login`, `/signup`) par les redirects LOCALISÉS de i18n/navigation (RESEARCH Q3 — requis pour que le gating soit testable de bout en bout). Conserver la structure signUp/signIn/signOut + createClient() de server.ts. Externaliser toutes les chaînes des pages login/signup via getTranslations/useTranslations namespace auth (I18N-03) ; remplacer les styles inline et `textAlign:'left'` par utilities logiques (text-start). Ajouter setRequestLocale dans les pages statiques.

    RESEARCH Q1 (TRANCHÉ) : le dashboard actuel ne lit que `instruments` (lecture authenticated, NON conditionnée par abonnement) — il NE doit donc PAS vivre sous (member) (qui exige un abo actif et bloquerait tout le monde en P1). Le déplacer sous apps/web/src/app/[locale]/dashboard/page.tsx (authentifié simple, HORS (member)). Garder la lecture instruments via listActiveInstruments ; conserver le guard getUser()+redirect('/login') localisé inline (page authentifiée non gated par abo). Externaliser les chaînes (namespace common/nav) ; remplacer styles inline + tous les `textAlign:'left'` (l.68-71) par classes logiques text-start ; aucune classe physique ml/mr/pl/pr.
  </action>
  <verify>
    <automated>test -f "apps/web/src/app/[locale]/(auth)/actions.ts" && test -f "apps/web/src/app/[locale]/dashboard/page.tsx" && ! test -f "apps/web/src/app/(auth)/actions.ts" && ! grep -rn "textAlign" "apps/web/src/app/[locale]/dashboard/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - (auth)/login, signup, actions et dashboard vivent sous [locale]/ ; les anciens emplacements n'existent plus.
    - actions.ts redirige via les helpers localisés (plus de '/dashboard' nu).
    - dashboard sous [locale]/ (hors (member)), lit instruments, guard getUser localisé, chaînes externalisées, aucune classe/style directionnel physique.
    - login/signup : chaînes via namespace auth, utilities logiques.
  </acceptance_criteria>
  <done>Routes auth+dashboard déplacées sous [locale], redirects localisés, chaînes externalisées, primitive RTL appliquée (classes logiques) — gating testable de bout en bout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RSC layout gate | La décision d'accès UX est prise serveur avant rendu ; le client ne doit pas voir de contenu protégé (pas de flash). |
| returnTo param → redirect | Valeur contrôlée par l'attaquant ; risque open-redirect. |
| (admin) existence → visiteur | L'existence du back-office ne doit pas fuiter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Spoofing | gate.ts décision d'accès | mitigate | getUser() partout (revalide le token), jamais getSession() (Pitfall 4) |
| T-01-07 | Open Redirect | returnTo dans requireUser | mitigate | returnTo validé comme chemin relatif same-origin (rejette URLs absolues / `//`) avant redirect (D-08) |
| T-01-08 | Information Disclosure | (admin) enumeration | mitigate | non-superadmin → notFound() (404, pas 403) via requireRole (D-09) |
| T-01-09 | Information Disclosure | flash de contenu gated | mitigate | gate au RSC layout → redirect serveur AVANT rendu (Pattern 3) |
| T-01-02 | Elevation of Privilege | role lu via profiles | mitigate | requireRole lit profiles.role après getUser(), jamais un claim JWT (lié à T-01-02 du Plan 01) |
| T-01-SC | Tampering | npm installs | accept | Aucun nouveau package npm dans ce plan (consomme next-intl/tailwind du Plan 02 ; lucide-react à vérifier présent — si absent, l'install relève du Plan 02/UI-SPEC, sinon utiliser SVG inline) |
</threat_model>

<verification>
- `pnpm --filter web typecheck` passe : un seul <html>, imports i18n résolus, gate typé contre les types régénérés (subscriptions/role).
- grep : middleware compose handleI18n+updateSession(request,response) + x-pathname ; gate.ts sans getSession ; aucune classe physique directionnelle dans le nouveau JSX.
- E2E des redirections/RTL/bascule de langue : couvert par le Plan 04 (Wave 3).
</verification>

<success_criteria>
- Navigation /[locale]/… ; bascule fr/en/ar sur la même page ; <html dir="rtl"> en arabe (I18N-01/02).
- Chaînes UI externalisées (I18N-03) ; formatage locale-aware câblé via useFormatter dans les pages (I18N-04).
- Redirections de gate fonctionnelles : non-auth→login+returnTo (ACCESS-01), non-abo→/tarifs (ACCESS-02 UX), non-superadmin→404 (ACCESS-03).
</success_criteria>

<output>
Create `.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-03-SUMMARY.md` when done
</output>
</content>
</invoke>
