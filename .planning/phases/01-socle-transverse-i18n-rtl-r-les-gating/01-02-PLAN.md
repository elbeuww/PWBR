---
phase: 01-socle-transverse-i18n-rtl-r-les-gating
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/src/styles/globals.css
  - apps/web/src/i18n/routing.ts
  - apps/web/src/i18n/navigation.ts
  - apps/web/src/i18n/request.ts
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: true
requirements: [I18N-01, I18N-03]
user_setup: []

must_haves:
  truths:
    - "next-intl 4.13 et Tailwind v4 sont installés et configurés dans apps/web"
    - "Le routing définit locales fr/en/ar, defaultLocale fr, localePrefix always"
    - "Les messages existent dans les 3 langues avec les namespaces P1 (common, nav, auth, access, pricing)"
    - "globals.css importe Tailwind v4 et mappe la police arabe sur :lang(ar)"
  artifacts:
    - path: "apps/web/src/i18n/routing.ts"
      provides: "defineRouting locales/defaultLocale/localePrefix"
      contains: "localePrefix"
    - path: "apps/web/src/i18n/navigation.ts"
      provides: "Link/redirect/usePathname/useRouter localisés (createNavigation)"
      contains: "createNavigation"
    - path: "apps/web/src/i18n/request.ts"
      provides: "getRequestConfig chargeant messages/{locale}.json"
      contains: "getRequestConfig"
    - path: "apps/web/src/messages/ar.json"
      provides: "Traductions arabes des namespaces P1"
      contains: "langue"
    - path: "apps/web/src/styles/globals.css"
      provides: "Tailwind v4 import + --font-arabic sur :lang(ar)"
      contains: "@import \"tailwindcss\""
    - path: "apps/web/postcss.config.mjs"
      provides: "Plugin @tailwindcss/postcss"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "apps/web/src/i18n/request.ts"
      via: "createNextIntlPlugin('./src/i18n/request.ts')"
      pattern: "createNextIntlPlugin"
    - from: "apps/web/src/i18n/request.ts"
      to: "apps/web/src/messages/*.json"
      via: "import(`../messages/${locale}.json`)"
      pattern: "messages/\\$\\{locale\\}"
---

<objective>
Installer et câbler l'infrastructure i18n (next-intl 4.13) et le styling RTL natif (Tailwind v4) — la fondation réutilisée par toutes les pages des phases suivantes. Aucun déplacement de route ici (c'est la Task du Plan 03) : ce plan livre la config et les messages.

Purpose: Poser i18n/RTL AVANT toute UI évite deux refactors massifs (RTL après coup inverse tout le layout ; chaînes en dur à externaliser page par page). Pattern de câblage standard, doc complète.
Output: deps installées, plugin next-intl branché, routing/navigation/request, messages fr/en/ar namespacés, Tailwind v4 (postcss + globals.css avec police arabe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-CONTEXT.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md
@.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-UI-SPEC.md
@apps/web/next.config.ts

<interfaces>
<!-- Décisions verrouillées encodées dans la config : -->
- D-01 localePrefix: 'always' ; D-02 defaultLocale: 'fr' ; D-03 locales: ['fr','en','ar']
- next-intl 4.x : `defineRouting` (next-intl/routing), `createNavigation` (next-intl/navigation), `getRequestConfig` (next-intl/server), `hasLocale` (next-intl)
  [ASSUMED A1 : valider les chemins d'import exacts contre la doc next-intl 4.13 à l'implémentation — Context7/ctx7 si dispo]

<!-- next.config.ts existant à PRÉSERVER (ne pas écraser) : -->
- transpilePackages: ['@app/core', '@app/supabase'], outputFileTracingRoot, turbopack.root = monorepoRoot
  → wrapper avec `createNextIntlPlugin('./src/i18n/request.ts')(nextConfig)` SANS perdre ces clés.

<!-- Tailwind v4 (RESEARCH §globals.css + postcss) : -->
- postcss.config.mjs : `export default { plugins: { '@tailwindcss/postcss': {} } }`
- globals.css : `@import "tailwindcss";` + `@theme { --font-arabic: "Noto Sans Arabic", system-ui, sans-serif; }` + `:lang(ar){ font-family: var(--font-arabic); }`
- INTERDIT : tailwindcss-rtl, tailwindcss-logical (RTL = propriétés logiques natives v4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer next-intl + Tailwind v4 et câbler le plugin + postcss + globals.css</name>
  <read_first>
    - apps/web/next.config.ts (config existante à préserver : transpilePackages, turbopack.root)
    - apps/web/package.json (vérifier next-intl + tailwind ABSENTS avant install)
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md (§Installation, Pattern 1 next.config, §globals.css + postcss)
  </read_first>
  <action>
    Installer les dépendances verrouillées (versions exactes) : `pnpm --filter web add next-intl@4.13.0` puis `pnpm --filter web add -D tailwindcss@4.3.1 @tailwindcss/postcss@4.3.1`. NE PAS installer tailwindcss-rtl ni tailwindcss-logical (RTL natif v4). Créer apps/web/postcss.config.mjs avec `export default { plugins: { '@tailwindcss/postcss': {} } }`. Créer apps/web/src/styles/globals.css avec `@import "tailwindcss";` puis un bloc `@theme { --font-arabic: "Noto Sans Arabic", system-ui, sans-serif; }` et `:lang(ar) { font-family: var(--font-arabic); }` (D-11 : @theme minimal, design system complet reporté P2). Modifier apps/web/next.config.ts : importer `createNextIntlPlugin from 'next-intl/plugin'`, créer `const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')`, et exporter `export default withNextIntl(nextConfig)` en PRÉSERVANT toutes les clés existantes (transpilePackages, outputFileTracingRoot, turbopack.root).
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json');process.exit(p.dependencies['next-intl']&&p.devDependencies['@tailwindcss/postcss']?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contient next-intl@4.13.0 (deps) + tailwindcss@4.3.1 et @tailwindcss/postcss@4.3.1 (devDeps).
    - postcss.config.mjs exporte le plugin @tailwindcss/postcss.
    - globals.css importe tailwindcss et mappe --font-arabic sur :lang(ar).
    - next.config.ts est wrappé par withNextIntl et conserve transpilePackages + turbopack.root.
    - tailwindcss-rtl / tailwindcss-logical ABSENTS du package.json.
  </acceptance_criteria>
  <done>Deps installées, plugin next-intl branché sur request.ts, Tailwind v4 configuré, config existante préservée.</done>
</task>

<task type="auto">
  <name>Task 2: Créer routing.ts, navigation.ts, request.ts</name>
  <read_first>
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-RESEARCH.md (Pattern 1 : code des 3 fichiers)
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-CONTEXT.md (D-01/02/03)
  </read_first>
  <action>
    Créer apps/web/src/i18n/routing.ts : `defineRouting({ locales: ['fr','en','ar'], defaultLocale: 'fr', localePrefix: 'always' })` exporté en `routing` (D-01/02/03). Créer apps/web/src/i18n/navigation.ts : `createNavigation(routing)` exportant Link, redirect, usePathname, useRouter, getPathname (ces wrappers préfixent automatiquement la locale — D-10, utilisés partout au Plan 03). Créer apps/web/src/i18n/request.ts : `getRequestConfig(async ({ requestLocale }) => { const requested = await requestLocale; const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale; return { locale, messages: (await import(`../messages/${locale}.json`)).default } })`. Valider les chemins d'import next-intl 4.x contre la doc 4.13 (A1) ; si un import diffère, l'aligner sur la doc installée.
  </action>
  <verify>
    <automated>grep -q "localePrefix" apps/web/src/i18n/routing.ts && grep -q "createNavigation" apps/web/src/i18n/navigation.ts && grep -q "getRequestConfig" apps/web/src/i18n/request.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - routing.ts exporte `routing` avec locales fr/en/ar, defaultLocale fr, localePrefix always.
    - navigation.ts exporte Link/redirect/usePathname/useRouter via createNavigation(routing).
    - request.ts charge messages/{locale}.json avec fallback hasLocale → defaultLocale.
  </acceptance_criteria>
  <done>Les 3 fichiers i18n existent et exposent les API consommées par le layout/middleware/gate du Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Créer messages/{fr,en,ar}.json (namespaces P1, parité de clés)</name>
  <read_first>
    - .planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-UI-SPEC.md (§Copywriting Contract : libellés FR source + EN/AR ; autonymes Français/English/العربية)
  </read_first>
  <action>
    Créer apps/web/src/messages/fr.json (source), en.json, ar.json avec EXACTEMENT les mêmes clés (parité stricte — toute clé manquante casse une locale). Namespaces P1 minimaux : `common` (ex. errorGeneric « Une erreur est survenue. Réessayez ou rechargez la page. », emptyTitle « Rien à afficher », emptyBody), `nav`/`language` (label déclencheur sélecteur : FR « Langue » / EN « Language » / AR « اللغة » ; les noms de langue restent des AUTONYMES non traduits : « Français », « English », « العربية » dans les 3 fichiers), `auth` (titres login/signup, libellés champs email/password, boutons), `access` (libellés des redirections si nécessaires), `pricing` (titre « Tarifs »/« Pricing »/« الأسعار » + corps « Le détail des offres arrive bientôt. » et ses traductions). Toutes les chaînes UI de P1 doivent provenir d'ici (I18N-03 : aucune chaîne en dur — vérifié par grep au Plan 04).
  </action>
  <verify>
    <automated>node -e "const f=require('./apps/web/src/messages/fr.json'),e=require('./apps/web/src/messages/en.json'),a=require('./apps/web/src/messages/ar.json');const k=o=>Object.keys(o).flatMap(p=>typeof o[p]==='object'?Object.keys(o[p]).map(x=>p+'.'+x):[p]).sort();const kf=JSON.stringify(k(f)),ke=JSON.stringify(k(e)),ka=JSON.stringify(k(a));process.exit(kf===ke&&ke===ka?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers JSON existent et ont une parité de clés STRICTE (mêmes chemins de clés à tous les niveaux).
    - Les autonymes de langue (Français/English/العربية) sont identiques dans les 3 fichiers (jamais traduits).
    - Le namespace pricing contient titre + corps placeholder dans les 3 langues.
  </acceptance_criteria>
  <done>messages/{fr,en,ar}.json présents, parité de clés vérifiée, namespaces P1 couvrant les chaînes du shell/auth/pricing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (segment locale) → résolution serveur | La locale vient de l'URL ; une valeur non supportée ne doit pas casser le rendu. |
| npm registry → build | Installation de 3 nouveaux paquets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Input Validation / DoS | locale depuis l'URL | mitigate | `hasLocale(routing.locales, requested)` valide avant rendu ; fallback defaultLocale (V5 ASVS) |
| T-01-SC | Tampering | next-intl, tailwindcss, @tailwindcss/postcss | mitigate | Paquets verrouillés STACK.md/CLAUDE.md, vérifiés npm 2026-06-14, dépôts source connus (amannn/next-intl, tailwindlabs). slopcheck indisponible → traités [ASSUMED], faible risque (largement adoptés). Installer aux versions exactes 4.13.0 / 4.3.1 (pas de range). |
</threat_model>

<verification>
- `pnpm --filter web typecheck` (ou build) passe avec les 3 fichiers i18n + config.
- Parité de clés fr/en/ar prouvée par le script Node de la Task 3.
- next.config.ts conserve transpilePackages + turbopack.root après wrapping.
</verification>

<success_criteria>
- next-intl + Tailwind v4 installés/configurés ; routing fr/en/ar localePrefix always.
- Messages namespacés présents et à parité dans les 3 langues, source des chaînes UI (I18N-03).
- Police arabe mappée sur :lang(ar) ; primitive RTL prête à être consommée (le formatage locale-aware I18N-04 est câblé au Plan 03, vérifié au Plan 04).
</success_criteria>

<output>
Create `.planning/phases/01-socle-transverse-i18n-rtl-r-les-gating/01-02-SUMMARY.md` when done
</output>
</content>
