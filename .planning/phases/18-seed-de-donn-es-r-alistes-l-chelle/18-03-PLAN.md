---
phase: 18-seed-de-donn-es-r-alistes-l-chelle
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - apps/jobs/scripts/seed/signals.ts
  - apps/jobs/scripts/seed/affiliation.ts
  - apps/jobs/scripts/seed.ts
  - packages/supabase/src/repositories/__tests__/seed-rls.test.ts
  - packages/supabase/src/repositories/__tests__/seed.test.ts
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03]
user_setup: []

must_haves:
  truths:
    - "analyses → trade_setups → prediction_outcomes seedés FK-cohérents, outcomes BRUTS uniquement (outcome + realized_r), aucun win_rate stocké"
    - "La cohorte d'affiliés + referrals est seedée et les commissions sont calculées par le RPC compute_affiliate_commissions, jamais en JS"
    - "L'orchestrateur appelle refresh_mv_mrr() en fin de seed → MRR mesuré non vide"
    - "Un test d'intégration prouve qu'un re-run du seed laisse N identique (idempotence SEED-01)"
    - "Le test seed-rls prouve isolation cross-user + non-abonné=0 signal sur données seedées (SEED-03)"
  artifacts:
    - path: "apps/jobs/scripts/seed/signals.ts"
      provides: "analyses + trade_setups + prediction_outcomes bruts (outcome/realized_r)"
      contains: "realized_r"
    - path: "apps/jobs/scripts/seed/affiliation.ts"
      provides: "affiliates/codes/referrals + appel RPC compute_affiliate_commissions"
      contains: "compute_affiliate_commissions"
    - path: "packages/supabase/src/repositories/__tests__/seed.test.ts"
      provides: "test idempotence : COUNT stable entre 2 runs (skipIf)"
      contains: "describe.skipIf"
  key_links:
    - from: "apps/jobs/scripts/seed/affiliation.ts"
      to: "compute_affiliate_commissions(period) RPC"
      via: "client.rpc après seed referrals+payments"
      pattern: "compute_affiliate_commissions"
    - from: "apps/jobs/scripts/seed.ts"
      to: "mv_mrr matview"
      via: "refresh_mv_mrr() service_role en fin de seed"
      pattern: "refresh_mv_mrr"
    - from: "apps/jobs/scripts/seed/signals.ts"
      to: "pattern_stats (vue)"
      via: "outcomes bruts → % calculé par la vue, jamais seedé"
      pattern: "prediction_outcomes"
---

<objective>
Compléter le seed avec les signaux (analyses → trade_setups → prediction_outcomes en outcomes BRUTS), la cohorte d'affiliés dont les commissions sont calculées par le RPC existant, l'orchestration finale + refresh de la matview MRR, puis prouver l'idempotence (SEED-01) et finaliser la preuve RLS anon (SEED-03) sur les données seedées.

Purpose: Les outcomes bruts (sans win_rate stocké) garantissent VITR-03/SEED-02 structurellement ; le RPC `compute_affiliate_commissions` évite tout recalcul JS (double source de vérité) ; `refresh_mv_mrr()` rend le MRR mesuré visible (ADASH-02). Le test d'idempotence ferme SEED-01.
Output: seed complet et idempotent, MRR rafraîchi, tests seed (idempotence) + seed-rls (cross-user) verts/skip-propre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-CONTEXT.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-PATTERNS.md
@apps/jobs/scripts/seed/config.ts

<interfaces>
<!-- outcomes bruts (RESEARCH Pattern 3) — AUCUN win_rate -->
- prediction_outcomes : PK = setup_id (1 outcome / setup expiré ou invalidé)
- `outcome ∈ {hit_tp, hit_sl, flat}` via faker.helpers.weightedArrayElement (55/35/10)
- `realized_r` numeric : hit_tp [0.8..3.5], hit_sl [-1.2..-0.8], flat [-0.3..0.3]
- pattern_stats (vue 0014) calcule le % — JAMAIS de champ win_rate dans le seed

<!-- commissions via RPC (PATTERNS) -->
From apps/jobs/src/jobs/affiliate-commission.ts:
- `computeCommissions(client, period)` → `client.rpc('compute_affiliate_commissions', { period })` (L.62)
- period au format '2026-MM', DateTime.utc luxon (L.23/L.60)
- aucune boucle de calcul de commission en TS

<!-- refresh matview (RESEARCH Pitfall 5) -->
- fin de seed : `client.rpc('refresh_mv_mrr')` (service_role) ; exige index unique mv_mrr_month_idx (0017 Partie B) — vérifier LIVE

<!-- test idempotence : calque skipIf affiliate-rls.test.ts -->
- HAS_ENV gate, adminClient() service_role pour COUNT, describe.skipIf(!HAS_ENV)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signals.ts — analyses/trade_setups/prediction_outcomes (outcomes BRUTS)</name>
  <files>apps/jobs/scripts/seed/signals.ts, apps/jobs/scripts/seed.ts</files>
  <behavior>
    - Test (no-perf-seed-claims) : signals.ts ne contient AUCUN champ win_rate/success_rate/expectancy/% — seuls outcome, realized_r, status sont écrits
    - prediction_outcomes seedés UNIQUEMENT sur les setups status expired/invalidated (PK=setup_id)
    - analyses → trade_setups FK-cohérents (chaque setup référence une analyse seedée existante)
    - source='demo' sur analyses, trade_setups, prediction_outcomes
  </behavior>
  <read_first>
    - supabase/migrations/0006_analyses_trade_setups.sql (colonnes analyses/trade_setups, FK analysis_id, status, instrument_id)
    - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql (prediction_outcomes L.29-35 PK=setup_id, outcome, realized_r ; vue pattern_stats L.58-137)
    - supabase/migrations/0003_data_ingestion_tables.sql (instruments — réutiliser les id existants, NE PAS re-seeder)
    - apps/jobs/scripts/seed/config.ts (volumes analyses/setups/outcomes, distribution outcome)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Pattern 3" + §"Anti-Patterns" (re-seed instruments interdit)
  </read_first>
  <action>
    Créer `apps/jobs/scripts/seed/signals.ts` exportant `seedSignals(client, users)`. D'abord LIRE les `instruments.id` existants (seedés 0001/0003 — RÉUTILISER, ne JAMAIS re-seeder, anti-pattern). Seeder ~1500-3000 `analyses` (`source:'demo'`, `instrument_id` parmi les existants, `created_at` étalé luxon — jamais identiques, Pitfall 1 keyset). Puis ~3000-5000 `trade_setups` (`source:'demo'`, `analysis_id` = id d'analyse seedée réelle, mix `status` active/expired/invalidated). Puis `prediction_outcomes` (~2500) UNIQUEMENT sur les setups expired/invalidated (PK=setup_id, `source:'demo'`) : `outcome` via `faker.helpers.weightedArrayElement([{weight:55,value:'hit_tp'},{weight:35,value:'hit_sl'},{weight:10,value:'flat'}])` + `realized_r` borné par outcome (hit_tp 0.8..3.5, hit_sl -1.2..-0.8, flat -0.3..0.3). N≥30 outcomes par bucket de pattern pour rendre pattern_stats significatif. AUCUN champ win_rate/% — la distribution 55/35/10 est un poids de génération, PAS un % affiché ; le % émerge de la vue pattern_stats. Inserts batchés chunks 1000, throw sur erreur, garder les id retournés pour la cohérence FK. Câbler `seedSignals(client, users)` dans `seed.ts` après seedPayments.
  </action>
  <verify>
    <automated>pnpm test -- no-perf-seed-claims && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `signals.ts` écrit `outcome` + `realized_r` mais AUCUN champ `win_rate/success_rate/expectancy/pct` (source assertion + `pnpm test -- no-perf-seed-claims` vert)
    - trade_setups référencent des `analysis_id` d'analyses seedées (behavior : FK-cohérence, pas d'orphelin)
    - prediction_outcomes seedés uniquement sur setups expired/invalidated (behavior)
    - instruments NON re-seedés (source assertion : signals.ts lit instruments, n'insère pas)
    - `created_at` étalés (behavior : pas de constante)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>signals.ts seede analyses→trade_setups→prediction_outcomes FK-cohérents, outcomes BRUTS (outcome/realized_r), instruments réutilisés, source='demo', scan no-perf vert.</done>
</task>

<task type="auto">
  <name>Task 2: affiliation.ts (RPC commissions) + refresh mv_mrr final</name>
  <files>apps/jobs/scripts/seed/affiliation.ts, apps/jobs/scripts/seed.ts</files>
  <read_first>
    - supabase/migrations/0016_affiliation.sql (tables affiliates/affiliate_codes/referrals/commissions/payouts, RPC compute_affiliate_commissions, rate_bps grille jusqu'à 20%)
    - apps/jobs/src/jobs/affiliate-commission.ts (computeCommissions L.62 → rpc, period '2026-MM' luxon)
    - supabase/migrations/0017_scalable_foundation.sql (mv_mrr + refresh_mv_mrr + index unique mv_mrr_month_idx Partie B L.363-365)
    - apps/jobs/scripts/seed/config.ts (volume affiliés ~300, distribution referrals longue traîne)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Don't Hand-Roll" + §"Pitfall 5" + §"Ratios" (affiliation D-05)
  </read_first>
  <action>
    Créer `apps/jobs/scripts/seed/affiliation.ts` exportant `seedAffiliation(client, users)` : pour ~300 users `role='affiliate'`, insérer une ligne `affiliates` (`source:'demo'`), un `affiliate_codes`, et des `referrals` en distribution longue traîne (quelques affiliés à ~50 referrals, beaucoup à 0-5 ; referrals sans PII filleul). Les referrals pointent vers des users démo abonnés réels (cohérence FK avec subscriptions/payments du Plan 02). Puis APPELER le RPC pour chaque mois seedé : `client.rpc('compute_affiliate_commissions', { period })` (miroir affiliate-commission.ts L.62 ; periods '2025-07'..'2026-06' via luxon DateTime.utc). AUCUNE boucle de calcul de commission en JS (anti-pattern double source de vérité — RESEARCH §Don't Hand-Roll) ; la grille bps (jusqu'à 20% récurrent, D-05) est appliquée par le RPC. Un sous-ensemble des commissions `due` → `payouts` `paid` (`source` non requis sur payouts — cascade).
    Mettre à jour `seed.ts` : câbler `seedAffiliation(client, users)` après seedSignals, puis EN DERNIER appeler `client.rpc('refresh_mv_mrr')` (service_role — Pitfall 5 : sans refresh, MRR vide malgré payments seedés). Si le RPC échoue parce que `mv_mrr_month_idx` est absent LIVE, logger un avertissement explicite invitant à vérifier l'index 0017 Partie B (fallback : refresh non-concurrent documenté), sans masquer l'erreur silencieusement.
  </action>
  <verify>
    <automated>pnpm test -- no-perf-seed-claims && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `affiliation.ts` calcule les commissions via `client.rpc('compute_affiliate_commissions', ...)` — AUCUNE arithmétique de commission en TS (source assertion)
    - affiliates insérés avec `source: 'demo'`, referrals en distribution longue traîne sans PII (behavior)
    - `seed.ts` appelle `refresh_mv_mrr` en DERNIÈRE étape (source assertion)
    - `pnpm test -- no-perf-seed-claims` vert (affiliation.ts : rate_bps en liste blanche = commission, pas perf de trade)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>affiliation seedée (affiliates/codes/referrals longue traîne), commissions via RPC uniquement, payouts paid ; seed.ts rafraîchit mv_mrr en fin de course avec garde-fou index.</done>
</task>

<task type="auto">
  <name>Task 3: test idempotence seed.test.ts + finalisation seed-rls cross-user</name>
  <files>packages/supabase/src/repositories/__tests__/seed.test.ts, packages/supabase/src/repositories/__tests__/seed-rls.test.ts</files>
  <read_first>
    - packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts (calque HAS_ENV/adminClient/signUpAndGetClient/skipIf/afterAll)
    - packages/supabase/src/repositories/__tests__/seed-rls.test.ts (stub créé en 18-01 Task 3 — compléter l'assertion cross-user)
    - apps/jobs/scripts/seed/config.ts (SEED_SCALE pour mode N réduit en test)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-VALIDATION.md (per-task map SEED-01/SEED-03)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Code Examples" (test RLS anon) + §Pitfall 3
  </read_first>
  <action>
    Créer `packages/supabase/src/repositories/__tests__/seed.test.ts` (SEED-01 idempotence, `describe.skipIf(!HAS_ENV)`) calqué sur affiliate-rls : via `adminClient()` (service_role, lecture COUNT uniquement — pas une assertion RLS), compter les lignes `source='demo'` sur profiles/subscriptions/payments/trade_setups APRÈS un premier seed, puis (mode SEED_SCALE réduit pour tenir en CI/local) prouver qu'un second run laisse le COUNT IDENTIQUE (pas d'accumulation — D-06). Le re-run du seed lui-même reste une vérification Manual-Only à l'échelle (VALIDATION.md) ; le test automatisé valide la mécanique purge+reseed sur un N réduit. Asserter aussi `count WHERE source='demo' > 0` (SEED-02 labellisation requêtable) et `count WHERE source='live'` inchangé après purge (purge ne touche pas live).
    Compléter `packages/supabase/src/repositories/__tests__/seed-rls.test.ts` (créé en 18-01) : finaliser l'assertion cross-user user A / user B sur `payments` — seed via service_role un payment de B (`source:'demo'`), lire via clientA anon, `filter(r => r.user_id === userIdB)` → `toHaveLength(0)`. Confirmer que l'assertion non-abonné→0 trade_setups est complète. CRITIQUE (Pitfall 3) : toute lecture assertée passe par clientA anon, jamais service_role. `afterAll` nettoie users + lignes seedées.
  </action>
  <verify>
    <automated>pnpm test -- seed && pnpm test -- seed-rls && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `seed.test.ts` prouve COUNT `source='demo'` identique entre 2 runs en mode SEED_SCALE réduit (behavior : idempotence D-06)
    - `seed.test.ts` asserte `count WHERE source='demo' > 0` (SEED-02) et `source='live'` inchangé (purge n'efface pas live)
    - `seed-rls.test.ts` : non-abonné → 0 trade_setups ET user A ne lit aucun payment de B, lectures via anon uniquement (source assertion : aucun `SERVICE_ROLE_KEY` dans un `it`)
    - `pnpm test` (suite complète) vert ; `pnpm typecheck` vert
    - Tests skipés proprement sans `.env.test` (skipIf)
  </acceptance_criteria>
  <done>Idempotence prouvée (COUNT stable, live intact) + RLS anon finalisée (non-abonné=0 signal, isolation cross-user payments) ; suite complète verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code seed → INSERT | outcome brut autorisé, win_rate/% interdit (VITR-03) |
| commission calc | doit passer par le RPC DB, jamais recalcul JS |
| anon client → RLS | preuve cross-user lue via anon uniquement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | win_rate/% stocké dans le seed (VITR-03) | mitigate | signals.ts n'écrit que outcome+realized_r ; scan no-perf-seed-claims vert (Task 1) |
| T-18-11 | Info Disclosure | preuve RLS lue via service_role (faux vert) | mitigate | seed-rls assertions via clientA anon uniquement ; service_role réservé au seed des fixtures (Task 3) |
| T-18-12 | Tampering | commission recalculée en JS (double source de vérité) | mitigate | affiliation.ts appelle `compute_affiliate_commissions` RPC, 0 arithmétique commission TS (Task 2) |
| T-18-13 | Info Disclosure | referrals avec PII filleul | mitigate | referrals seedés sans PII (Task 2, miroir 0016) |
</threat_model>

<verification>
- `pnpm test` complète verte + `pnpm typecheck` vert
- `pnpm test -- seed` : idempotence (COUNT stable) + labellisation source>0 + live intact
- `pnpm test -- seed-rls` : non-abonné=0 signal + isolation cross-user (anon)
- seed.ts appelle refresh_mv_mrr en dernier ; commissions via RPC
- Manual-Only (VALIDATION.md) : re-run seed à l'échelle → N stable + MRR>0 après refresh
</verification>

<success_criteria>
- Seed complet FK-cohérent ~10k (SEED-01) : signaux + affiliés + outcomes bruts
- Aucun % de perf seedé (SEED-02) — outcomes bruts, % calculés par pattern_stats/mv_mrr
- MRR mesuré visible après refresh_mv_mrr (ADASH-02 alimenté)
- RLS re-testée anon à l'échelle (SEED-03) : non-abonné=0, isolation cross-user
- Idempotence prouvée : re-run → N stable
</success_criteria>

<output>
Create `.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-03-SUMMARY.md` when done
</output>
