---
phase: 18-seed-de-donn-es-r-alistes-l-chelle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - supabase/migrations/0018_seed_source_column.sql
  - packages/supabase/src/database.types.ts
  - apps/web/test/no-perf-seed-claims.test.ts
  - packages/supabase/src/repositories/__tests__/seed-rls.test.ts
  - apps/jobs/scripts/seed/config.ts
autonomous: false
requirements: [SEED-02, SEED-03]
user_setup: []

must_haves:
  truths:
    - "D-01: la colonne source ∈ {live,demo,backtest} existe LIVE sur les 8 tables seedées, default 'live' (migration 0018)"
    - "get_advisors(security) ne signale aucune nouvelle fuite RLS introduite par source"
    - "Un scan statique du code seed échoue si un champ de % de perf est inséré (SEED-02)"
    - "D-07: un test anon (jamais service_role) prouve que non-abonné lit 0 trade_setups et qu'un user A ne voit pas les données de B (SEED-03)"
    - "@faker-js/faker est en devDep racine et la config de volumes/ratios est isolée et testable"
  artifacts:
    - path: "supabase/migrations/0018_seed_source_column.sql"
      provides: "colonne source + CHECK borné sur 8 tables + index partiels Partie B commentés"
      contains: "source text not null default 'live'"
    - path: "apps/web/test/no-perf-seed-claims.test.ts"
      provides: "scan statique du code seed (SEED-02, sans DB)"
      contains: "FORBIDDEN_SEED_FIELDS"
    - path: "packages/supabase/src/repositories/__tests__/seed-rls.test.ts"
      provides: "preuve RLS anon (SEED-03), skipIf(!HAS_ENV)"
      contains: "describe.skipIf"
    - path: "apps/jobs/scripts/seed/config.ts"
      provides: "SEED_VERSION, volumes, ratios distribution démo, pricing atomic"
      contains: "PRICE_ATOMIC"
  key_links:
    - from: "supabase/migrations/0018_seed_source_column.sql"
      to: "packages/supabase/src/database.types.ts"
      via: "régénération manuelle MCP generate_typescript_types après apply LIVE"
      pattern: "source"
    - from: "seed-rls.test.ts"
      to: "has_active_subscription() (RLS trade_setups)"
      via: "anon client select → 0 ligne non-abonné"
      pattern: "trade_setups"
---

<objective>
Poser la fondation gatée de la phase 18 : la migration 0018 (colonne `source`), son application LIVE via MCP, la régénération des types, et les deux tests Wave 0 (scan no-perf-seed-claims statique + preuve RLS anon) plus la config de seed isolée. Rien ne peut être seedé tant que la colonne `source` n'existe pas LIVE et que les types ne la connaissent pas.

Purpose: La colonne `source` est l'ancre testable de SEED-02 (labellisation requêtable) et la clé de purge sûre de D-06 ; les tests Wave 0 doivent exister AVANT le seed pour que le sampling Nyquist mesure le seed dès sa première ligne.
Output: migration 0018 appliquée LIVE, `database.types.ts` à jour, 2 tests Wave 0, `seed/config.ts`, `@faker-js/faker` en devDep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-CONTEXT.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-PATTERNS.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-VALIDATION.md

<interfaces>
<!-- Analog migration : forme exacte colonne+CHECK (0008 L.20-22) -->
From supabase/migrations/0008_profiles_role.sql L.20-22:
```sql
alter table public.profiles
  add column role text not null default 'member'
  check (role in ('member', 'affiliate', 'superadmin'));
```

<!-- Analog test RLS anon : structure exacte à calquer -->
From packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts:
- `const HAS_ENV = Boolean(SUPABASE_URL && SUPABASE_ANON_KEY && SERVICE_ROLE_KEY)`
- `function adminClient()` → createClient(URL, SERVICE_ROLE_KEY, { auth:{persistSession:false,autoRefreshToken:false} })
- `async function signUpAndGetClient(email,password)` → anon client.auth.signUp, retourne { client, userId }
- `describe.skipIf(!HAS_ENV)(...)` ; lecture assertée TOUJOURS via clientA (anon), JAMAIS service_role
- `afterAll` : delete service_role + `adminClient().auth.admin.deleteUser(userId)`
- Env vars : NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY

<!-- Analog scan statique : no-perf-claims.test.ts -->
From apps/web/test/no-perf-claims.test.ts:
- squelette collecte récursive de fichiers + regex FORBIDDEN + liste d'offenders
- test de contrôle non-trivial : asserter que le détecteur attrape une valeur plantée (anti vacuous-green)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: faker devDep + migration 0018 (colonne source) + config seed</name>
  <files>package.json, pnpm-lock.yaml, supabase/migrations/0018_seed_source_column.sql, apps/jobs/scripts/seed/config.ts</files>
  <read_first>
    - supabase/migrations/0008_profiles_role.sql (forme colonne text + CHECK, L.20-22)
    - supabase/migrations/0017_scalable_foundation.sql (en-tête canonique L.1-19 + découpage Partie A/Partie B index CONCURRENTLY L.350-387)
    - supabase/migrations/0016_affiliation.sql (en-tête + convention text+check)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Détermination du périmètre exact de la colonne source" (8 tables) + §"La migration 0018 doit-elle indexer source ?"
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-CONTEXT.md (D-01, D-04)
  </read_first>
  <action>
    Installer faker en devDep racine : `pnpm add -D -w @faker-js/faker` (version 10.5.0, slopcheck [OK] confirmé RESEARCH §Package Legitimacy Audit ; `tsx`/`@supabase/supabase-js`/`vitest`/`luxon` déjà présents — ne pas réinstaller).
    Écrire `supabase/migrations/0018_seed_source_column.sql`. En-tête de commentaire canonique copié de 0017/0016 : numéro 0018 (0017 = dernière sur disque ; 0013 ABSENTE — ne pas réutiliser), mention apply via MCP `apply_migration` JAMAIS `supabase db push`, découpage Partie A (DDL transactionnel) / Partie B (CONCURRENTLY hors transaction).
    Partie A : sur chacune des 8 tables `profiles, subscriptions, payments, analyses, trade_setups, prediction_outcomes, affiliates, commissions`, ajouter la colonne via `add column source text not null default 'live' check (source in ('live','demo','backtest'))` (forme exacte 0008 L.20-22 ; `default 'live'` rend les lignes existantes `live`, future-proof). NE PAS colonner les tables filles cascadées (`affiliate_codes`, `referrals`, `payouts`) ni les tables volume (`candles`, `snapshots`, `job_runs`, `telegram_posts`) — décision : volume non seedé en P18 (réduit la surface 0018 ; tranche Open Question 1 / A4 vers le périmètre minimal 8 tables).
    Partie B (commentée en fin de fichier, exécutée hors apply_migration) : `create index concurrently` partiel `WHERE source = 'demo'` sur `analyses`, `trade_setups`, `payments` (accélère le delete D-06 ; miroir 0017 L.350-361).
    Anti-pattern bloquant (V4) : `source` est un LABEL de provenance, JAMAIS un gate de lecture — aucune policy `using (source=…)`. Ne créer/modifier AUCUNE policy RLS dans 0018.
    Créer `apps/jobs/scripts/seed/config.ts` exportant : `SEED_VERSION`, `faker.seed` value `42`, ratios distribution démo (abonnés actifs 37%, expirés 18%, annulés 5%, jamais payé 40%, affiliés ~3%, superadmin 2 comptes — RESEARCH §Ratios D-03), volumes cibles par table (RESEARCH §Volumes), `PRICE_ATOMIC = { standard: 9_000_000n, discovery: 3_000_000n }` (D-04, USDT 6 décimales bigint, PAS de float), domaine email démo `demo.nexa.invalid` (RFC 2606), mode N réduit paramétrable via env (`SEED_SCALE`). Config = données pures, aucune écriture DB ici.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0018_seed_source_column.sql | grep -c "default 'live'" | grep -qx 8 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `@faker-js/faker` apparaît dans `package.json` sous `devDependencies` (source assertion)
    - La migration ajoute exactement 8 colonnes `source` avec `check (source in ('live','demo','backtest'))` (8 occurrences hors commentaires)
    - La migration ne contient AUCUN `create policy` ni `using (source` (behavior : source n'est pas un gate)
    - Partie B contient 3 `create index concurrently` partiels commentés (analyses, trade_setups, payments)
    - `apps/jobs/scripts/seed/config.ts` exporte `PRICE_ATOMIC` avec valeurs bigint `9_000_000n`/`3_000_000n` et domaine `demo.nexa.invalid`
    - `pnpm typecheck` vert (aucune nouvelle erreur)
  </acceptance_criteria>
  <done>Migration 0018 écrite (8 colonnes source + index Partie B commentés, 0 policy), faker en devDep, config seed isolée, typecheck vert.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Appliquer 0018 LIVE via MCP + régénérer types + advisors</name>
  <files>packages/supabase/src/database.types.ts</files>
  <read_first>
    - supabase/migrations/0018_seed_source_column.sql (le fichier écrit en Task 1)
    - supabase/migrations/0017_scalable_foundation.sql (L.5-10 convention génération types manuelle ; L.389-422 gate advisors)
    - packages/supabase/src/database.types.ts (Row/Insert/Update existants des 8 tables + alias maison + override *_atomic string)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Runtime State Inventory" (Pitfall 6 types) + §Pitfall 6
  </read_first>
  <action>
    [BLOCKING — application LIVE, requiert MCP Supabase ; NON automatisable en suite Vitest, cf. VALIDATION.md §Manual-Only.]
    1. Appliquer la Partie A de `0018_seed_source_column.sql` via MCP Supabase `apply_migration` (PAS `supabase db push` — projet non link, pattern rodé 0011/0012/0014/0017).
    2. Exécuter les 3 `create index concurrently` partiels de la Partie B un statement à la fois via MCP `execute_sql` (CONCURRENTLY interdit en transaction, erreur 25001) ; vérifier `indisvalid=true` après chacun (miroir gate INVALID 0017).
    3. Régénérer via MCP `generate_typescript_types`, puis ré-éditer `packages/supabase/src/database.types.ts` à la MAIN (convention repo — PAS `gen types --linked`) : ajouter `source` aux `Row`/`Insert`/`Update` des 8 tables colonnées, réappliquer les alias maison et l'override string des champs `*_atomic`.
    4. Lancer MCP `get_advisors(security)` : confirmer que la colonne `source` n'introduit AUCUNE nouvelle alerte RLS (V4 — `source` n'est pas un gate). Les 2 WARN security-definer pré-existants (has_active_subscription/is_superadmin) restent EXPECTED BY DESIGN, non bloquants.
    Ce gate est obligatoire : build et typecheck passeraient sans le push live (les types viennent du fichier régénéré, pas de la DB live) → faux positif de vérification. Marqué `autonomous: false`.
  </action>
  <verify>
    <automated>grep -c "source" packages/supabase/src/database.types.ts | awk '$1>=8{exit 0}{exit 1}' && pnpm typecheck</automated>
    <human-check>MCP get_advisors(security) post-0018 : 0 nouvelle alerte RLS liée à source (2 WARN security-definer pré-existants tolérés)</human-check>
  </verify>
  <acceptance_criteria>
    - 0018 Partie A appliquée LIVE (CLI output MCP apply_migration succès)
    - 3 index partiels `source` créés `indisvalid=true` (MCP execute_sql sur pg_index)
    - `database.types.ts` contient `source` dans Row/Insert/Update des 8 tables (source assertion)
    - `pnpm typecheck` vert avec `source` reconnu dans les types Insert
    - MCP `get_advisors(security)` : aucune nouvelle alerte RLS introduite par 0018 (human-check)
  </acceptance_criteria>
  <resume-signal>Tapez "appliqué" après confirmation advisors verts, ou décrivez l'alerte RLS rencontrée.</resume-signal>
  <done>0018 LIVE + index partiels valides + types régénérés (source dans 8 tables) + advisors confirment 0 fuite RLS.</done>
</task>

<task type="auto">
  <name>Task 3: Tests Wave 0 — scan no-perf-seed-claims + preuve RLS anon</name>
  <files>apps/web/test/no-perf-seed-claims.test.ts, packages/supabase/src/repositories/__tests__/seed-rls.test.ts</files>
  <read_first>
    - apps/web/test/no-perf-claims.test.ts (squelette collecte + regex FORBIDDEN + test de contrôle, L.55-114)
    - packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts (calque EXACT : HAS_ENV, adminClient, signUpAndGetClient, skipIf, afterAll deleteUser)
    - vitest.config.ts (glob + chargement .env.test)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Scan no-perf-claims" (Volet A) + §"Code Examples" (test RLS anon)
  </read_first>
  <action>
    Créer `apps/web/test/no-perf-seed-claims.test.ts` (SEED-02, scan statique sans DB) en réutilisant le squelette `detectForbidden` + boucle d'offenders de `no-perf-claims.test.ts`. Cible = code seed `apps/jobs/scripts/seed/**` (lu via `node:fs`/glob, jamais de DB → toujours exécutable en CI). Regex `FORBIDDEN_SEED_FIELDS = /win_?rate|success_?rate|winRatePct|expectancy|hardcoded.*%/i`. Liste blanche : `realized_r`, `outcome`, `amount_atomic`, `rate_bps` (taux de commission affiliation, PAS perf de trade). Inclure un test de contrôle non-trivial (asserter que le détecteur attrape une valeur plantée `win_rate: 0.9`) pour interdire le vacuous-green. Le dossier `apps/jobs/scripts/seed/` ne contient pour l'instant que `config.ts` (Task 1) — le scan tolère l'absence d'offenders (0 = vert).
    Créer `packages/supabase/src/repositories/__tests__/seed-rls.test.ts` (SEED-03) en calquant EXACTEMENT `affiliate-rls.test.ts` : mêmes `SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY`, `HAS_ENV`, `adminClient()`, `signUpAndGetClient()`, `describe.skipIf(!HAS_ENV)`, `afterAll` deleteUser. Deux assertions (D-07) lues TOUJOURS via le client anon (Pitfall 3 — JAMAIS service_role dans une assertion) :
      (a) un non-abonné (signUp anon sans subscription active) lit `trade_setups` → `expect(data).toHaveLength(0)` (barrière `has_active_subscription()` = false) ;
      (b) un user A ne lit aucun `payments` de user B : seed via service_role un payment appartenant à B (`source:'demo'`, `amount_atomic` string, `status:'verified'`), puis lire `payments` via clientA et `filter(r => r.user_id === userIdB)` → `toHaveLength(0)`.
    Les deux tests sont skipés sans `.env.test` (skipIf) mais leur structure d'assertion est complète et exécutable dès que l'env LIVE est présent.
  </action>
  <verify>
    <automated>pnpm test -- no-perf-seed-claims && pnpm test -- seed-rls</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- no-perf-seed-claims` vert ; le test de contrôle prouve que `win_rate: 0.9` planté EST détecté (anti vacuous-green)
    - `no-perf-seed-claims.test.ts` scanne `apps/jobs/scripts/seed/**` via node:fs (behavior : aucune connexion DB)
    - `pnpm test -- seed-rls` exécutable : SKIP propre sans `.env.test`, structure d'assertion présente (skipIf)
    - La lecture assertée du test RLS passe par clientA anon, jamais `createClient(url, SERVICE_ROLE_KEY)` dans un `it` (source assertion)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>2 tests Wave 0 créés et verts/skip-propre : scan statique SEED-02 (avec test de contrôle) + preuve RLS anon SEED-03 calquée affiliate-rls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB live | DDL appliqué LIVE via MCP ; une policy mal formée ouvrirait une fuite |
| anon client → RLS | la preuve SEED-03 ne tranche QUE si lue via anon (service_role bypass = faux vert) |
| code seed → INSERT | un champ de % de perf inséré violerait VITR-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation | colonne `source` utilisée comme gate de lecture | mitigate | 0018 ne crée aucune policy ; aucun `using (source=…)` ; `get_advisors(security)` post-apply (Task 2) |
| T-18-02 | Info Disclosure | preuve RLS lue via service_role (faux vert) | mitigate | `seed-rls.test.ts` lit via clientA anon uniquement ; service_role réservé au seed des fixtures (Task 3) |
| T-18-03 | Tampering | input `source` non borné | mitigate | `check (source in ('live','demo','backtest'))` au niveau DB (Task 1) |
| T-18-04 | Tampering | montants en float | mitigate | `PRICE_ATOMIC` en bigint atomique, `amount_atomic` string (config Task 1) |
| T-18-SC | Tampering | install npm `@faker-js/faker` | mitigate | slopcheck [OK] + audit légitimité RESEARCH (16,7M dl/sem, repo officiel, 0 postinstall) — pas de checkpoint requis (verdict OK) |
</threat_model>

<verification>
- `pnpm typecheck` vert avec `source` dans les types des 8 tables
- `pnpm test -- no-perf-seed-claims` vert (test de contrôle inclus)
- `pnpm test -- seed-rls` exécutable (skip propre sans env)
- MCP `get_advisors(security)` : 0 nouvelle alerte RLS post-0018
- 0018 appliquée LIVE + 3 index partiels `indisvalid=true`
</verification>

<success_criteria>
- Colonne `source` LIVE sur 8 tables, default 'live', CHECK borné, aucune policy l'utilisant
- Types régénérés à la main, typecheck vert
- 2 tests Wave 0 en place (gate du sampling Nyquist du seed)
- faker en devDep, config seed isolée et testable
</success_criteria>

<output>
Create `.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-01-SUMMARY.md` when done
</output>
