---
phase: 18-seed-de-donn-es-r-alistes-l-chelle
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/jobs/scripts/seed.ts
  - apps/jobs/scripts/seed/purge.ts
  - apps/jobs/scripts/seed/users.ts
  - apps/jobs/scripts/seed/subscriptions.ts
  - apps/jobs/scripts/seed/payments.ts
  - apps/jobs/package.json
autonomous: true
requirements: [SEED-01]
user_setup: []

must_haves:
  truths:
    - "Le seed crée des users démo via auth.admin.createUser (emails .invalid, metadata seed:true) bornés par pLimit"
    - "Chaque ligne profiles/subscriptions/payments seedée porte source='demo'"
    - "D-03: distribution orientée démo (37% actifs/18% expirés/5% annulés/40% leads/~3% affiliés) ; D-04: pricing 9$/3$ atomique ; verified_at et current_period_end étalés sur ~12 mois (churn + MRR mesurés, jamais identiques ni inventés)"
    - "D-06: la purge supprime exclusivement WHERE source='demo' en ordre FK inverse, jamais TRUNCATE, jamais source='live'"
    - "L'orchestrateur seed.ts boote dotenv + fail-fast sur secrets manquants et enchaîne purge → users → subscriptions/payments"
  artifacts:
    - path: "apps/jobs/scripts/seed.ts"
      provides: "entrypoint tsx : dotenv, fail-fast, orchestration topologique"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "apps/jobs/scripts/seed/purge.ts"
      provides: "delete WHERE source='demo' ordre FK inverse + deleteUser par email pattern"
      contains: "source"
    - path: "apps/jobs/scripts/seed/users.ts"
      provides: "auth.admin.createUser + UPDATE profiles role/source, pLimit, faker multi-locale"
      contains: "createUser"
    - path: "apps/jobs/scripts/seed/payments.ts"
      provides: "payments verified amount_atomic, verified_at étalé luxon"
      contains: "amount_atomic"
  key_links:
    - from: "apps/jobs/scripts/seed/users.ts"
      to: "auth.users (trigger handle_new_user → profiles)"
      via: "serviceClient.auth.admin.createUser"
      pattern: "auth\\.admin\\.createUser"
    - from: "apps/jobs/scripts/seed/purge.ts"
      to: "source='demo' rows"
      via: "delete().eq('source','demo') ordre enfant→parent"
      pattern: "eq\\('source', 'demo'\\)"
---

<objective>
Construire le cœur idempotent du seed : la purge ciblée par `source='demo'`, la création déterministe des ~10k users (auth.admin.createUser bornée), et le seed des dimensions monétaires (subscriptions + payments étalés) avec labellisation `source='demo'`. L'orchestrateur `seed.ts` boote l'environnement et enchaîne ces étapes dans l'ordre topologique.

Purpose: SEED-01 exige un seed FK-cohérent et idempotent (re-run → N stable) ; la purge `WHERE source='demo'` (D-06) garantit N stable sans jamais toucher `source='live'`, et `auth.admin.createUser` est le seul chemin légal vers `profiles`.
Output: orchestrateur + 4 modules seed (purge, users, subscriptions, payments) ; le seed est exécutable mais incomplet (signaux/affiliation/market en Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-CONTEXT.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md
@.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-PATTERNS.md
@apps/jobs/scripts/seed/config.ts

<interfaces>
<!-- Entrypoint analog : freeze-nile-fixture.ts (même dossier scripts/) -->
From apps/jobs/scripts/freeze-nile-fixture.ts:
- dotenv : `config({ path: resolve(SCRIPT_DIR, '../.env') })` (apps/jobs/.env)
- `function fail(msg): never { console.error(...); process.exit(1) }`
- secrets : `process.env['SUPABASE_URL']`, `process.env['SUPABASE_SERVICE_ROLE_KEY']` validés au démarrage
- terminal : `main().catch((e) => fail((e as Error).message))`
- script npm : `"seed": "tsx scripts/seed.ts"` à ajouter dans apps/jobs/package.json (miroir freeze-nile)

<!-- service_role client : pattern lazy outcome-tracker.ts L.33-44 -->
From apps/jobs/src/jobs/outcome-tracker.ts:
```typescript
function getServiceClient() {
  const url = process.env['SUPABASE_URL']
  const key = process.env['SUPABASE_SERVICE_ROLE_KEY']
  if (!url || !key) throw new Error('seed: SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY requis')
  return createClient<Database>(url, key, { auth: { persistSession: false, autoRefreshToken: false } })
}
```

<!-- Création user (RESEARCH Pattern 1) -->
- `serviceClient.auth.admin.createUser({ email: 'seed-{i}@demo.nexa.invalid', password, email_confirm: true, user_metadata: { seed: true } })`
- trigger handle_new_user crée public.profiles automatiquement → puis UPDATE profiles SET role, source='demo' WHERE id = data.user.id

<!-- pricing (config.ts) -->
- PRICE_ATOMIC = { standard: 9_000_000n, discovery: 3_000_000n } ; amount_atomic transmis en string (override repo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrateur seed.ts + purge.ts (idempotence D-06)</name>
  <files>apps/jobs/scripts/seed.ts, apps/jobs/scripts/seed/purge.ts, apps/jobs/package.json</files>
  <read_first>
    - apps/jobs/scripts/freeze-nile-fixture.ts (dotenv L.17-33, fail-fast L.40-53, main().catch L.222, script npm L.7-8)
    - apps/jobs/src/jobs/outcome-tracker.ts (getServiceClient lazy L.33-44)
    - packages/supabase/src/service-client.ts (serviceClient + import 'server-only' L.4, persistSession:false)
    - apps/jobs/scripts/seed/config.ts (volumes, ratios, domaine email démo)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Ordre de purge FK-inverse" (L.268-286)
    - apps/jobs/package.json (scripts existants)
  </read_first>
  <action>
    Créer `apps/jobs/scripts/seed.ts` : bootstrap dotenv depuis `resolve(SCRIPT_DIR, '../.env')` (miroir freeze-nile), `fail(msg): never` qui `process.exit(1)`, validation fail-fast de `SUPABASE_URL` et `SUPABASE_SERVICE_ROLE_KEY` au démarrage (sécurité CLAUDE.md). Exposer un `main()` qui appelle dans l'ordre topologique : `purge()` → `seedUsers()` → `seedSubscriptions()` → `seedPayments()` (les étapes signals/affiliation/market et `refresh_mv_mrr` sont ajoutées en Plan 03 — laisser des appels stubés commentés ou des imports à compléter, ne pas inventer leur implémentation ici). Terminal `main().catch((e) => fail((e as Error).message))`. Ajouter `"seed": "tsx scripts/seed.ts"` dans `apps/jobs/package.json` (miroir freeze-nile-fixture).
    Créer `apps/jobs/scripts/seed/purge.ts` exportant `purge(client)` : `delete().eq('source','demo')` en ordre FK INVERSE strict (enfants d'abord, RESEARCH L.268-285) : `commissions` → `affiliates` → `prediction_outcomes` → `trade_setups` → `analyses` → `payments` → `subscriptions` → `profiles`, chacun `throw` sur erreur. JAMAIS `TRUNCATE` (détruirait `source='live'`, anti-pattern V4). Pour les users démo (auth.users n'a pas de colonne `source`) : `auth.admin.listUsers` paginé + filtrer les emails matchant le domaine `demo.nexa.invalid` (config) + `auth.admin.deleteUser` (cascade `on delete cascade` vers profiles). La suppression du profile démo via `source='demo'` couvre le résiduel ; la suppression auth nettoie auth.users. La purge doit être sûre à vide (0 ligne démo → no-op sans erreur).
  </action>
  <verify>
    <automated>pnpm --filter jobs exec tsc --noEmit -p tsconfig.json 2>/dev/null || pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/jobs/scripts/seed.ts` valide `SUPABASE_SERVICE_ROLE_KEY` au démarrage et `process.exit(1)` si absent (source assertion)
    - `apps/jobs/package.json` contient le script `"seed": "tsx scripts/seed.ts"` (source assertion)
    - `purge.ts` appelle `delete().eq('source','demo')` sur 8 tables dans l'ordre enfant→parent ; AUCUN `truncate` (source assertion)
    - `purge.ts` purge les users démo via `auth.admin.deleteUser` filtré sur le domaine `demo.nexa.invalid` (behavior)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>Orchestrateur boote/fail-fast + script npm seed ; purge idempotente WHERE source='demo' en ordre FK inverse + deleteUser par email pattern, sûre à vide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: users.ts (createUser borné, faker multi-locale, déterminisme)</name>
  <files>apps/jobs/scripts/seed/users.ts</files>
  <behavior>
    - Test (no-perf-seed-claims étendu) : users.ts ne contient aucun champ de % de perf (liste blanche : role, source, email)
    - faker.seed(42) appelé avant toute génération ; ordre de génération stable → dataset reproductible
    - Emails déterministes `seed-{i}@demo.nexa.invalid` (RFC 2606, jamais routable)
    - Distribution des rôles conforme config (member/affiliate/superadmin), source='demo' systématique
  </behavior>
  <read_first>
    - apps/jobs/src/jobs/outcome-tracker.ts (getServiceClient lazy L.33-44)
    - apps/jobs/scripts/seed/config.ts (volumes users, ratios rôles, SEED_SCALE, domaine email)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Pattern 1" + §"Code Examples" (multi-locale faker L.421-435, faker.seed L.438-446)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Pitfall 4" (pLimit createUser)
  </read_first>
  <action>
    Créer `apps/jobs/scripts/seed/users.ts` exportant `seedUsers(client)` retournant la liste des users créés (id, role, locale) pour les modules aval. Multi-locale faker : `new Faker({ locale: [ar, base] })`, `[fr, base]`, `[en, base]`, chaque instance `seed(42)` ; répartition déterministe par index (`localeOf(i)` ~50% ar / 30% fr / 20% en, fallback `base`). Pour chaque user : `auth.admin.createUser({ email: 'seed-{i}@demo.nexa.invalid', password: 'SeedDemo!2026', email_confirm: true, user_metadata: { seed: true } })` (seul chemin légal — JAMAIS INSERT direct dans auth.users ; le trigger crée profiles). Borner la concurrence via `pLimit(5)` (Pitfall 4 — éviter épuisement connexions/rate-limit Auth ; si p-limit absent du workspace, batch séquentiel par lots). Après création : `UPDATE profiles SET role=…, source='demo' WHERE id = data.user.id` (rôles selon ratios config : ~3% affiliate, 2 superadmin, reste member). Étaler les `created_at` des profiles (luxon) — jamais identiques (Pitfall 1, casse keyset). Respecter `SEED_SCALE` pour un mode N réduit en itération locale. AUCUN champ de % de perf.
  </action>
  <verify>
    <automated>pnpm test -- no-perf-seed-claims && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `users.ts` crée les users via `auth.admin.createUser` avec emails `*@demo.nexa.invalid` et `user_metadata: { seed: true }` (source assertion)
    - La concurrence createUser est bornée (`pLimit` ou batch séquentiel) — behavior anti Pitfall 4
    - faker instancié multi-locale avec `seed(42)` avant génération (source assertion : déterminisme)
    - Chaque profile mis à jour avec `source: 'demo'` (source assertion)
    - `pnpm test -- no-perf-seed-claims` reste vert (users.ts sans champ de % de perf)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>users.ts crée la cohorte démo déterministe via createUser borné, rôles + source='demo', emails .invalid, created_at étalés ; scan no-perf vert.</done>
</task>

<task type="auto">
  <name>Task 3: subscriptions.ts + payments.ts (MRR/churn étalés, source='demo')</name>
  <files>apps/jobs/scripts/seed/subscriptions.ts, apps/jobs/scripts/seed/payments.ts</files>
  <read_first>
    - supabase/migrations/0009_subscriptions_gating.sql + 0010_has_active_subscription_null_expiry.sql (colonnes subscriptions, status, current_period_end)
    - supabase/migrations/0012_payments.sql (colonnes payments, amount_atomic, status, verified_at)
    - apps/jobs/src/jobs/affiliate-commission.ts (DateTime.utc luxon L.23/L.60, batch insert pattern)
    - apps/jobs/scripts/seed/config.ts (ratios statuts, PRICE_ATOMIC, volumes, fenêtre 12 mois)
    - .planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-RESEARCH.md §"Pattern 2" + §"Pattern 4" + §"Fenêtre temporelle des paiements / churn"
  </read_first>
  <action>
    Créer `apps/jobs/scripts/seed/subscriptions.ts` exportant `seedSubscriptions(client, users)` : pour ~60% des users (config ratios), insérer une subscription `source:'demo'` avec `status` déterministe (active 37% / expired 18% / canceled 5% / reste users sans subscription = leads 40%), `plan` ('standard' ~75% / 'discovery' ~25%), `current_period_end` étalé via luxon ancré sur 2026-06-25 : actifs → futur (now+1..30j, une fraction en J-3/J-1 pour tester ExpiryBanner P19), expirés → passé (1-6 mois avant now). Insert batché en chunks de 1000, `throw` sur erreur.
    Créer `apps/jobs/scripts/seed/payments.ts` exportant `seedPayments(client, subscriptions)` : pour chaque abonné actif/expiré, 1-N `payments` `status:'verified'`, `source:'demo'`, `amount_atomic` = `PRICE_ATOMIC[plan].toString()` (string — override repo, USDT 6 décimales bigint, JAMAIS float), `verified_at` étalé sur 12 mois calendaires UTC (luxon `DateTime.utc()`), volume croissant vers les mois récents (courbe d'acquisition réaliste, RESEARCH §Fenêtre). Renouvellements : certains users 2-N payments successifs (MRR récurrent + LTV mesurable). AUCUN champ de % de perf ni de MRR stocké — le MRR émerge de la matview `mv_mrr` (calcul, jamais seedé). Insert batché chunks 1000, throw sur erreur.
    Mettre à jour `seed.ts` pour câbler `seedSubscriptions(client, users)` puis `seedPayments(client, subscriptions)` (consomment la sortie de seedUsers).
  </action>
  <verify>
    <automated>pnpm test -- no-perf-seed-claims && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `subscriptions.ts` insère avec `source: 'demo'`, mix status active/expired/canceled (source assertion) et `current_period_end` étalé luxon (behavior : pas de date constante)
    - `payments.ts` insère `amount_atomic` en string depuis `PRICE_ATOMIC` (bigint), jamais de float (source assertion)
    - `verified_at` étalé sur ~12 mois UTC, jamais identique (behavior : MRR/churn mesurables)
    - Ni subscriptions.ts ni payments.ts ne stockent un MRR/% calculé (source assertion — `pnpm test -- no-perf-seed-claims` vert)
    - `seed.ts` enchaîne users → subscriptions → payments (source assertion)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <done>subscriptions + payments seedés source='demo', statuts/plans/dates étalés (MRR & churn mesurables), montants atomiques bigint, aucun % stocké ; orchestrateur câblé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed (service_role) → DB | écriture en bypass RLS ; doit rester dans apps/jobs |
| code seed → INSERT | champ de % de perf interdit (VITR-03) |
| purge → DB | doit cibler source='demo' exclusivement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering / Destruction | purge TRUNCATE détruisant source='live' | mitigate | purge.ts `delete().eq('source','demo')` exclusif, ordre FK inverse ; aucun TRUNCATE (Task 1) |
| T-18-06 | Info Disclosure / spam | users démo avec emails routables | mitigate | domaine `.invalid` (RFC 2606) + `user_metadata.seed:true` (Task 2) |
| T-18-07 | Elevation | service_role key fuite hors apps/jobs | mitigate | seed reste dans apps/jobs ; import service-client `server-only` ; secrets via apps/jobs/.env fail-fast (Task 1) |
| T-18-08 | Tampering | montants en float | mitigate | `amount_atomic` string depuis PRICE_ATOMIC bigint (Task 3) |
| T-18-09 | DoS (self) | 10k createUser saturent le pooler/Auth | mitigate | `pLimit(5)` ou batch séquentiel (Task 2, Pitfall 4) |
</threat_model>

<verification>
- `pnpm typecheck` vert
- `pnpm test -- no-perf-seed-claims` vert (modules seed sans champ de perf)
- `seed.ts` boote, fail-fast sans secrets, enchaîne purge → users → subscriptions → payments
- purge `WHERE source='demo'` exclusif, ordre FK inverse, sûre à vide
</verification>

<success_criteria>
- Seed core idempotent (purge + reseed déterministe) — base de SEED-01
- ~10k users démo via createUser borné, source='demo' partout
- MRR/churn matérialisables (dates étalées, montants atomiques)
- Aucun % de perf seedé (scan vert)
</success_criteria>

<output>
Create `.planning/phases/18-seed-de-donn-es-r-alistes-l-chelle/18-02-SUMMARY.md` when done
</output>
