---
phase: 05-track-record-mesur-affich
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - apps/web/src/components/track-record/TrackRecordBlock.tsx
  - apps/web/src/lib/track-record/patternStats.ts
  - apps/web/src/app/[locale]/(marketing)/page.tsx
  - apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx
  - apps/web/src/app/[locale]/(member)/signaux/page.tsx
  - apps/web/messages/fr.json
  - apps/web/messages/en.json
  - apps/web/messages/ar.json
  - apps/web/src/__tests__/messages-parity-track-record.test.ts
autonomous: true
requirements: [TRACK-03]
user_setup: []
must_haves:
  truths:
    - "La vitrine (page marketing) affiche le bloc track record mesuré : slot D-08 débloqué (SHOW_PROOF→true)"
    - "Le bloc affiche win rate + R moyen + expectancy + N (toujours présent), all-time ET 90j (tabs), par catégorie (table)"
    - "Sous N=30 le bloc rend « échantillon insuffisant — N trades » (jamais de %)"
    - "L'espace membre miroite le même bloc depuis la même source"
    - "Une page méthodologie dédiée explique H1/distance/flat/TP1-avant-SL/R/expectancy/seuil"
    - "La lecture se fait via anon-client (jamais le repo service_role) ; namespaces trackRecord+methodology à parité fr/en/ar"
  artifacts:
    - path: "apps/web/src/components/track-record/TrackRecordBlock.tsx"
      provides: "Bloc d'affichage RSC (tabs périodes + stats + table catégories + tooltip + disclaimer)"
      min_lines: 60
    - path: "apps/web/src/lib/track-record/patternStats.ts"
      provides: "Lecture anon-client de pattern_stats"
      exports: ["getPatternStats"]
    - path: "apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx"
      provides: "Page méthodologie dédiée (D-14)"
    - path: "apps/web/src/__tests__/messages-parity-track-record.test.ts"
      provides: "Garde parité fr/en/ar + no-perf"
  key_links:
    - from: "apps/web/src/components/track-record/TrackRecordBlock.tsx"
      to: "pattern_stats (DB)"
      via: "anon-client via getPatternStats"
      pattern: "getPatternStats"
    - from: "apps/web/src/components/track-record/TrackRecordBlock.tsx"
      to: "apps/web/src/lib/track-record/threshold.ts"
      via: "applyThreshold (D-09)"
      pattern: "applyThreshold"
    - from: "apps/web/src/app/[locale]/(marketing)/page.tsx"
      to: "TrackRecordBlock"
      via: "SHOW_PROOF débloqué"
      pattern: "SHOW_PROOF"
---

<objective>
Livrer TRACK-03 : afficher un % TOUJOURS mesuré (jamais inventé) sur la vitrine ET dans l'espace membre, avec méthode (tooltip + page dédiée) et taille d'échantillon N toujours visible ; sous le seuil N≥30, « échantillon insuffisant — N trades ». Débloquer le slot vitrine masqué de P2/D-08.

Purpose: Le chiffre qui fonde la confiance — argument anti-arnaque (transparence = vente). Lecture publique via anon-client sur la vue pattern_stats (frontière producteur-unique : front read-only).
Output: TrackRecordBlock (RSC), lecture anon pattern_stats, slot vitrine activé, miroir membre, page méthodologie, i18n fr/en/ar à parité.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-track-record-mesur-affich/05-CONTEXT.md
@.planning/phases/05-track-record-mesur-affich/05-RESEARCH.md
@.planning/phases/05-track-record-mesur-affich/05-PATTERNS.md
@.planning/phases/05-track-record-mesur-affich/05-UI-SPEC.md

<interfaces>
<!-- Helper seuil (livré 05-01, apps/web/src/lib/track-record/threshold.ts) : -->
export function applyThreshold(row): { sufficient: true; n; winRatePct; expectancy; avgR } | { sufficient: false; n }
export const MIN_SAMPLE = 30

<!-- Vue pattern_stats (livrée 05-02, lisible anon) : -->
// colonnes : dimension, bucket, period ('all_time'|'90d'), n, win_rate, avg_r, expectancy
// dimensions : 'overall' | 'style' | 'asset' | 'asset_class' | 'score_band' | 'risk'

<!-- Lecture anon RSC (apps/web/.../anon-client.ts) : -->
const supabase = createServerSupabaseClient(await cookies())
const { data } = await supabase.from('pattern_stats').select('dimension, bucket, period, n, win_rate, avg_r, expectancy')

<!-- Slot vitrine (apps/web/src/app/[locale]/(marketing)/page.tsx:18, :56) :
     const SHOW_PROOF = false  →  true ; remplacer {SHOW_PROOF && null} par {SHOW_PROOF && <TrackRecordBlock ... />} -->

<!-- Composants ui/ existants à réutiliser (AUCUN ajout) : card, tabs, table, tooltip, badge, skeleton, separator. -->
<!-- Disclaimer existant : <Disclaimer> (LEGAL-01, P2) à placer adjacent (D-15). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lecture anon pattern_stats + TrackRecordBlock (RSC) + i18n + parité</name>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/page.tsx (analog RSC trilingue, setRequestLocale + getTranslations, container, SHOW_PROOF l.18/56)
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (surface membre, lecture anon-client + RLS)
    - apps/web/src/components/member/ExpiryBanner.tsx (exemple composant member, <bdi>/Intl)
    - .planning/phases/05-track-record-mesur-affich/05-UI-SPEC.md (Component Anatomy, Copywriting Contract, Color result-rule, Threshold-gated figure, Interaction & States)
    - .planning/phases/05-track-record-mesur-affich/05-PATTERNS.md (§TrackRecordBlock squelette RSC, §lecture anon)
    - apps/web/src/__tests__/messages-parity-payment.test.ts (analog garde parité récursive + no-perf)
  </read_first>
  <action>Créer apps/web/src/lib/track-record/patternStats.ts : `getPatternStats(supabase)` prenant un client anon `SupabaseClient<Database>`, SELECT `dimension, bucket, period, n, win_rate, avg_r, expectancy` from 'pattern_stats'. NE JAMAIS importer le service-client ni un repo @app/supabase service_role (frontière producteur-unique, UI-SPEC). Créer apps/web/src/components/track-record/TrackRecordBlock.tsx (RSC) : `setRequestLocale` non requis ici (composant), `getTranslations('trackRecord')` ; lit via createServerSupabaseClient(await cookies()) + getPatternStats ; applique `applyThreshold` (de threshold.ts, 05-01) sur chaque ligne (D-09/D-12). Structure UI-SPEC : titre Heading « Track record mesuré » ; tabs périodes « Tout l'historique »/« 90 jours » (D-11, défaut all_time) ; ligne agrégat (dimension 'overall') win rate `%` en Display NEUTRE (hero), R moyen + expectancy en Heading/Label, signe coloré vert/rouge UNIQUEMENT si ± explicite (jamais décoratif, retenue anti-arnaque) ; N toujours adjacent (Label muted « mesuré sur {N} trades clôturés », plural-aware) ; tooltip méthode (« Mesuré sur {N} trades réels clôturés. Gagnant = take-profit atteint avant le stop. » + lien « Voir la méthodologie ») ; table par catégorie (asset/asset_class/style/score_band/risk) avec colonnes Catégorie/Taux de réussite (ou « Échantillon insuffisant — {N} trades » si non-suffisant, NEUTRE muted, jamais rouge)/Trades(N) ; états loading (skeleton)/empty (« Track record en cours de mesure »)/error (retry). Toutes les valeurs en `<bdi>` + Intl.NumberFormat (RTL-safe). Classes logiques uniquement (ms/me/ps/pe/start/end). Placer le `<Disclaimer>` LEGAL-01 adjacent (D-15). Ajouter le namespace `trackRecord` aux 3 messages (apps/web/messages/{fr,en,ar}.json) à parité STRICTE depuis la copy FR canonique du Copywriting Contract (labels, tabs, sample-size plural, tooltip, insufficient plural, headers table, style/risk values, empty/error states) — les NOMBRES ne sont jamais des clés (formatés via Intl). EN no-perf : éviter le mot « profit » comme allégation (VITR-03, précédent D-02-02-D). Créer apps/web/src/__tests__/messages-parity-track-record.test.ts (miroir messages-parity-payment.test.ts) : parité récursive trackRecord fr/en/ar + sentinelles sous-clés + garde no-perf-claims sur trackRecord.</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run apps/web/src/__tests__/messages-parity-track-record.test.ts && pnpm --filter web exec node scripts/check-i18n-hardcoded.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/src/__tests__/messages-parity-track-record.test.ts` → vert (parité 3 langues + no-perf).
    - TrackRecordBlock importe applyThreshold (grep = 1) et getPatternStats (grep = 1) ; n'importe AUCUN service-client (grep `service` dans le fichier = 0 hors commentaire).
    - Le composant rend « Échantillon insuffisant » via une branche `!sufficient` (grep `insufficient` ou clé i18n correspondante).
    - N rendu adjacent à chaque chiffre (clé sample-size présente dans les 3 messages, parité stricte).
    - `check-i18n-hardcoded.mjs` exit 0 (aucune chaîne en dur).
  </acceptance_criteria>
  <done>Lecture anon pattern_stats + TrackRecordBlock complet (seuil D-09, N D-12, tooltip D-14, disclaimer D-15, RTL), i18n trackRecord à parité, test parité vert.</done>
</task>

<task type="auto">
  <name>Task 2: Débloquer le slot vitrine + miroir membre + page méthodologie</name>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/page.tsx (SHOW_PROOF l.18, slot l.56)
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (surface membre où miroiter le bloc)
    - apps/web/src/app/[locale]/(marketing)/legal/[doc]/page.tsx (analog route marketing statique, generateStaticParams, prose)
    - .planning/phases/05-track-record-mesur-affich/05-UI-SPEC.md (MethodologyPage D-14 : prose max-w-prose, sections H1/distance/flat/TP1/R/expectancy/seuil ; Copywriting methodology)
    - apps/web/messages/fr.json (structure namespaces existante)
  </read_first>
  <action>Modifier apps/web/src/app/[locale]/(marketing)/page.tsx : passer `const SHOW_PROOF = false` → `true` et remplacer `{SHOW_PROOF && null}` (l.56) par `{SHOW_PROOF && <TrackRecordBlock />}` avec l'import du composant. Conserver le commentaire D-08 mais le mettre à jour (« activé en Phase 5 »). Miroiter le bloc dans apps/web/src/app/[locale]/(member)/signaux/page.tsx : rendre `<TrackRecordBlock />` (même source/composant, plateforme payante — D-13) sans dupliquer la logique. Créer apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx (RSC, `setRequestLocale` + `getTranslations('methodology')`, prose `max-w-prose`) expliquant en langage vulgarisé : définition TP1-avant-SL (D-01), trade flat valorisé au close (D-02), granularité H1 (D-03), règle de distance sur heure ambiguë (D-04), définitions R et expectancy (D-10, A2 : R moyen = gagnants, expectancy = tous), périodes all-time + 90j (D-11), seuil N≥30 (D-09). Aucun nombre inventé — décrit la méthode uniquement. Réutiliser le `<Disclaimer>` (D-15). Ajouter le namespace `methodology` aux 3 messages à parité stricte (copy FR du Copywriting Contract methodology) et étendre le test de parité de Task 1 pour couvrir `methodology`. Le lien « Voir la méthodologie » du TrackRecordBlock pointe vers la route /methodologie (navigation localisée next-intl Link).</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run apps/web/src/__tests__/messages-parity-track-record.test.ts && pnpm typecheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx (marketing) : grep `SHOW_PROOF = true` = 1 et `<TrackRecordBlock` = 1 ; plus de `{SHOW_PROOF && null}`.
    - signaux/page.tsx rend `<TrackRecordBlock` (grep = 1) — miroir membre.
    - apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx existe (RSC, getTranslations('methodology')).
    - namespace `methodology` présent et à parité dans fr/en/ar (test parité étendu vert).
    - `pnpm typecheck` → aucune nouvelle erreur au-delà de la baseline P1.
  </acceptance_criteria>
  <done>Slot vitrine débloqué, bloc miroité côté membre, page méthodologie trilingue, lien « Voir la méthodologie » câblé, parité étendue verte.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [GATE PHASE]: Vérification RLS anon de bout en bout + rendu vitrine</name>
  <read_first>
    - .planning/phases/05-track-record-mesur-affich/05-VALIDATION.md (Manual-Only Verifications : lecture publique réelle, aucune fuite prediction_outcomes)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (Pitfall 1 : terrain RLS anon neuf)
  </read_first>
  <what-built>
    TrackRecordBlock branché sur pattern_stats via anon-client, slot vitrine débloqué (SHOW_PROOF=true), miroir membre, page méthodologie trilingue. Seuil N≥30 appliqué en TS, N toujours affiché, disclaimers adjacents.
  </what-built>
  <how-to-verify>
    1. Lancer le dev server (`pnpm --filter web dev`, :3000) avec `.env.local` Supabase configuré (pattern_stats appliquée en 05-02).
    2. Charger la vitrine en navigation privée (NON connecté, aucune session) : confirmer que le bloc track record s'affiche — soit les agrégats avec % et N (si N≥30 en base), soit « échantillon insuffisant — N trades » par figure, soit l'empty state « Track record en cours de mesure » si aucun outcome.
    3. Confirmer que N est TOUJOURS visible à côté de chaque chiffre, et qu'AUCUN % n'apparaît sous N=30.
    4. Sécurité : depuis un client anon (devtools ou script), tenter SELECT sur prediction_outcomes → doit échouer/0 ligne ; SELECT pattern_stats → OK. (Confirme la frontière validée par get_advisors en 05-02.)
    5. Vérifier le rendu RTL en /ar (propriétés logiques, nombres en <bdi>) et le lien « Voir la méthodologie » → page /ar/methodologie.
    6. Confirmer la présence du disclaimer LEGAL-01 adjacent au bloc.
  </how-to-verify>
  <resume-signal>`approved` + verdict (bloc rendu correctement, N toujours visible, aucun % sous seuil, prediction_outcomes inaccessible anon), OU description des écarts à corriger.</resume-signal>
  <acceptance_criteria>
    - Le bloc s'affiche en navigation privée (lecture anon réelle de pattern_stats fonctionne — première policy anon du projet validée de bout en bout).
    - Aucun % rendu sous N=30 ; N toujours adjacent.
    - prediction_outcomes inaccessible au client anon.
    - Rendu RTL correct et disclaimer présent.
  </acceptance_criteria>
  <done>Lecture publique réelle validée en navigateur, frontière anon confirmée, seuil/N/RTL/disclaimer conformes UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (visiteur public, sans compte) → vue pattern_stats | La vitrine lit les agrégats anonymes via anon-client. Front READ-ONLY ; jamais d'écriture, jamais d'import service_role. |
| authenticated (membre) → vue pattern_stats | Miroir membre lit la même vue (grant authenticated). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | composant front lit des données par setup | mitigate | TrackRecordBlock lit UNIQUEMENT pattern_stats (agrégats) via anon-client. getPatternStats n'importe aucun repo service_role (en-tête frontière). prediction_outcomes jamais requêté côté front. Gate manuel Task 3 + get_advisors (05-02). ASVS V4. |
| T-05-08 | Injection | couche d'affichage publique | mitigate | Aucune entrée utilisateur dans le pipeline d'affichage (les valeurs viennent de la vue DB ; les labels de next-intl). Nombres formatés via Intl, jamais concaténés en HTML brut. Pas de dangerouslySetInnerHTML. |
| T-05-09 | Tampering / promesse trompeuse | % affiché | mitigate | Seuil N≥30 (D-09) interdit tout % sous l'échantillon ; N toujours visible (D-12) ; méthode exposée (tooltip + page, D-14) ; disclaimer LEGAL-01 adjacent (D-15) ; « jamais inventé » — valeurs DB-derived, l'IA n'invente aucun chiffre (UI-SPEC). |
| T-05-SC | Tampering | npm/shadcn installs | accept | Aucun package npm ni bloc shadcn ajouté (UI-SPEC §Registry Safety : tous les composants existent). Pas de surface slopsquatting. |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/__tests__/messages-parity-track-record.test.ts` → vert (parité trackRecord + methodology + no-perf).
- `check-i18n-hardcoded.mjs` exit 0.
- `pnpm typecheck` vert (baseline P1 inchangée).
- Vérification manuelle Task 3 : lecture anon réelle + frontière prediction_outcomes + RTL.
- Suite complète `pnpm vitest run` verte avant /gsd:verify-work (sampling phase gate).
</verification>

<success_criteria>
- Vitrine et espace membre affichent le % mesuré + N + méthode, slot D-08 débloqué.
- Sous N=30 : « échantillon insuffisant — N trades », jamais de %.
- Page méthodologie trilingue explique la méthode (D-01/02/03/04/09/10/11).
- Lecture publique anon de pattern_stats fonctionne ; prediction_outcomes jamais exposé.
</success_criteria>

<output>
Create `.planning/phases/05-track-record-mesur-affich/05-03-SUMMARY.md` when done
</output>
