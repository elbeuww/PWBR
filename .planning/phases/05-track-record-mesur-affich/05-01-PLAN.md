---
phase: 05-track-record-mesur-affich
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/replay/outcome.ts
  - packages/core/src/replay/outcome.test.ts
  - packages/core/src/index.ts
  - apps/web/src/lib/track-record/threshold.ts
  - apps/web/src/lib/track-record/threshold.test.ts
autonomous: true
requirements: [TRACK-01, TRACK-03]
must_haves:
  truths:
    - "replayOutcome(setup, candlesH1) retourne hit_tp quand TP1 est touché avant SL (first-touch)"
    - "replayOutcome retourne hit_sl quand SL est touché avant TP1"
    - "Sur bougie ambiguë (TP1 ET SL dans la même H1), la règle de distance D-04 décide : niveau le plus proche de l'entrée touché en premier (tie ≤ → hit_tp)"
    - "Trade flat D-02 valorisé au close de la dernière bougie ≤ valid_until : R>0 si prix dans le sens, R<0 sinon, pour long ET short"
    - "Le helper de seuil retourne le % si N≥30, sinon { insufficient: true, n } — N toujours présent"
  artifacts:
    - path: "packages/core/src/replay/outcome.ts"
      provides: "Fonction pure replayOutcome + types Setup/Candle/Outcome"
      exports: ["replayOutcome"]
      min_lines: 40
    - path: "packages/core/src/replay/outcome.test.ts"
      provides: "Golden tests 5 cas TRACK-01 + déterminisme"
      contains: "describe('replayOutcome"
    - path: "apps/web/src/lib/track-record/threshold.ts"
      provides: "Helper d'affichage seuil N≥30 (D-09/D-12)"
      exports: ["applyThreshold"]
    - path: "apps/web/src/lib/track-record/threshold.test.ts"
      provides: "Test seuil N≥30 / N<30"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/replay/outcome.ts"
      via: "barrel export"
      pattern: "export \\{ replayOutcome \\}"
    - from: "packages/core/src/replay/outcome.ts"
      to: "packages/core/src/time/constants.ts"
      via: "import .js"
      pattern: "from './\\.\\./time/constants.js'"
---

<objective>
Livrer le cœur déterministe de TRACK-01 (logique pure `replayOutcome`, golden-testée, zéro I/O) et le helper d'affichage du seuil TRACK-03 (N≥30). Aucune DB, aucun réseau, aucun composant — uniquement de la logique pure testable.

Purpose: Isoler la simulation first-touch (« jamais inventé ») dans `packages/core`, golden-testée avant tout branchement I/O. C'est la brique sur laquelle le job (05-02) et l'affichage (05-03) s'appuient.
Output: `packages/core/src/replay/outcome.ts` (+ test golden 5 cas), barrel core étendu, `apps/web/src/lib/track-record/threshold.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-track-record-mesur-affich/05-CONTEXT.md
@.planning/phases/05-track-record-mesur-affich/05-RESEARCH.md
@.planning/phases/05-track-record-mesur-affich/05-PATTERNS.md
@.planning/phases/05-track-record-mesur-affich/05-VALIDATION.md

<interfaces>
<!-- Constantes core déjà exportées (packages/core/src/time/constants.ts), à réutiliser : -->
export const TIMEFRAMES = { H1: 60, H4: 240, D: 1440 } as const
export const UTC_ZONE = 'UTC' as const

<!-- Forme Setup dérivée du contrat §3 (packages/core/src/schemas/output.ts) :
     direction: 'long'|'short' ; entry_price: number ; stop_loss: number ;
     take_profits: { price: number; alloc_pct: number }[] (TP1 = index 0) ; valid_until: string -->

<!-- Algo de référence figé (RESEARCH §Code Examples l.264-290) — à porter tel quel :
     denom = isLong ? entry - sl : sl - entry  (> 0 par construction)
     hitTp = isLong ? c.high >= tp1 : c.low <= tp1
     hitSl = isLong ? c.low <= sl : c.high >= sl
     ambigu (hitTp && hitSl) → distTp <= distSl ? hit_tp : hit_sl   (tie-break ≤ = hit_tp, D-04)
     realized_r gagnant = Math.abs(tp1 - entry) / denom ; perdant = -1
     flat = close de la dernière bougie ≤ valid_until : long (close-entry)/denom ; short (entry-close)/denom -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Golden tests replayOutcome — 5 cas TRACK-01 + déterminisme</name>
  <read_first>
    - packages/core/src/time/candle.test.ts (analog EXACT structure golden Vitest core)
    - packages/core/src/time/candle.ts (convention anti look-ahead, import luxon, fn pure)
    - packages/core/src/schemas/output.ts (forme take_profits {price,alloc_pct}, direction, stop_loss)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.264-290 = algo figé ; Pitfall 2 ambigu D-04, Pitfall 6 flat D-02)
  </read_first>
  <behavior>
    - Test « first-touch hit_tp simple » (long) : séquence H1 où une bougie a high >= tp1 avant tout low <= sl → outcome 'hit_tp', realized_r = |tp1-entry|/(entry-sl) > 0.
    - Test « first-touch hit_sl simple » (long) : bougie low <= sl avant high >= tp1 → outcome 'hit_sl', realized_r = -1.
    - Test « cas ambigu D-04 » : une SEULE bougie touche TP1 ET SL (low<=sl ET high>=tp1) ; TP1 plus proche de l'entrée que SL → 'hit_tp' ; cas inverse (SL plus proche) → 'hit_sl' ; égalité distTp==distSl → 'hit_tp' (tie ≤).
    - Test « flat D-02 long » : aucune bougie ne touche TP1 ni SL ; close de la dernière bougie au-dessus de l'entrée → 'flat', realized_r > 0 ; en dessous → realized_r < 0.
    - Test « flat D-02 short » : miroir du cas long, signe du R correct (short : (entry-close)/denom).
    - Test « déterministe » : deux appels successifs sur les mêmes entrées retournent un résultat identique (copier candle.test.ts l.64-68).
  </behavior>
  <action>Créer packages/core/src/replay/outcome.test.ts avec import `{ replayOutcome }` depuis './outcome.js' (extension .js OBLIGATOIRE, convention core index.ts:6). Importer `{ describe, expect, it } from 'vitest'`. Définir des fixtures candles H1 synthétiques inline (séquences OHLC contrôlées) couvrant les 5 cas obligatoires (hit_tp, hit_sl, ambigu D-04 avec ses deux sous-branches + tie, flat long, flat short) plus le test de déterminisme. Les setups de test utilisent la forme du contrat §3 : direction 'long'|'short', entry_price, stop_loss, take_profits=[{price, alloc_pct}], valid_until. Le test DOIT échouer à l'exécution (RED) car outcome.ts n'existe pas encore. Ne PAS créer outcome.ts dans cette tâche.</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run packages/core/src/replay/outcome.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier packages/core/src/replay/outcome.test.ts existe.
    - `pnpm vitest run packages/core/src/replay/outcome.test.ts` échoue (RED) avec une erreur d'import/module manquant (outcome.js absent) OU des assertions rouges — JAMAIS « No test files found ».
    - Le fichier contient au moins 6 blocs `it(` couvrant : hit_tp, hit_sl, ambigu D-04 (2 sous-branches + tie), flat long, flat short, déterminisme.
    - Import en `./outcome.js` (grep `from './outcome.js'` = 1).
  </acceptance_criteria>
  <done>Tests golden authorés, RED confirmé (module manquant), 5 cas + déterminisme présents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implémenter replayOutcome pur + étendre le barrel core</name>
  <read_first>
    - packages/core/src/time/candle.ts (fn pure déterministe, en-tête JSDoc, import luxon)
    - packages/core/src/index.ts (barrel, blocs export l.6-21, imports .js obligatoires)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.264-290 — algo à porter tel quel ; §Anti-Patterns : realized_r = prix réalisés, JAMAIS via scoring)
    - packages/core/src/replay/outcome.test.ts (les tests écrits en Task 1, contrat à satisfaire)
  </read_first>
  <action>Créer packages/core/src/replay/outcome.ts. Exporter le type `Outcome = { outcome: 'hit_tp' | 'hit_sl' | 'flat'; realized_r: number }`, un type `ReplaySetup` (direction 'long'|'short', entry_price, stop_loss, take_profits: {price,alloc_pct}[], valid_until: string) et un type `ReplayCandle` (ts, open, high, low, close: number). Implémenter la fonction pure `replayOutcome(setup: ReplaySetup, candlesH1: ReplayCandle[]): Outcome` en portant l'algo figé du RESEARCH : tp1 = take_profits[0].price ; denom = isLong ? entry-sl : sl-entry ; boucle first-touch sur candlesH1 ; bougie ambiguë → règle de distance D-04 (distTp <= distSl → hit_tp, tie inclus) ; flat D-02 → R au close de la dernière bougie (long (close-entry)/denom, short (entry-close)/denom) ; aucune candle → flat realized_r 0 (edge A3). Calcul du R sur les PRIX uniquement, jamais via packages/core/scoring (anti-pattern). En-tête JSDoc citant TRACK-01 + D-01/D-02/D-04. Étendre packages/core/src/index.ts : ajouter `export { replayOutcome } from './replay/outcome.js'` et `export type { Outcome } from './replay/outcome.js'` sur le modèle des blocs existants.</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run packages/core/src/replay/outcome.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/core/src/replay/outcome.test.ts` → tous verts (GREEN), au moins 6 tests passants.
    - packages/core/src/index.ts contient `export { replayOutcome }` (grep = 1).
    - replayOutcome est une fonction pure : aucun import de '@supabase', 'fetch', './scoring' dans outcome.ts (grep `scoring` dans outcome.ts = 0).
    - Tie-break ambigu : le test distTp==distSl retourne 'hit_tp' (vert).
  </acceptance_criteria>
  <done>replayOutcome implémenté, 6/6 tests verts, barrel core étendu, aucune dépendance I/O.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Helper d'affichage seuil N≥30 (D-09/D-12) + test</name>
  <read_first>
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.330-337 — seuil en TS, n toujours exposé)
    - .planning/phases/05-track-record-mesur-affich/05-UI-SPEC.md (Threshold-gated figure D-09 : NO % sous N=30, N TOUJOURS visible)
    - .planning/phases/05-track-record-mesur-affich/05-CONTEXT.md (D-09 seuil 30, D-12 N adjacent)
  </read_first>
  <behavior>
    - applyThreshold({ n: 30, winRate: 0.64, ... }) retourne un objet affichable avec le % (sufficient) et n=30.
    - applyThreshold({ n: 29, ... }) retourne { insufficient: true, n: 29 } — AUCUN %.
    - applyThreshold({ n: 0, ... }) → insufficient, n=0 (jamais d'erreur, état honnête).
    - Le N est TOUJOURS présent dans la sortie, suffisant ou non (D-12).
  </behavior>
  <action>Créer apps/web/src/lib/track-record/threshold.ts. Définir la constante `MIN_SAMPLE = 30` (D-09) et la fonction `applyThreshold(row: { n: number; win_rate: number | null; expectancy: number | null; avg_r?: number | null })`. Retourner un type discriminé : si `row.n >= MIN_SAMPLE` → `{ sufficient: true; n; winRatePct: Math.round((win_rate ?? 0) * 100); expectancy; avgR }` ; sinon → `{ sufficient: false; n }`. Le `n` figure dans LES DEUX branches (D-12). Pas de formatage Intl ici (réservé au composant) — uniquement la décision seuil. Créer apps/web/src/lib/track-record/threshold.test.ts (sous le glob vitest racine `apps/web/**/__tests__/**` OU `apps/web/test/**` selon la config — voir D-02-02-B/D-02-03-C ; placer le test à un chemin couvert par le glob racine, sinon étendre `vitest.config.ts` include de `apps/web/src/lib/**`). Tester n=30 (sufficient + winRatePct), n=29 (insufficient), n=0 (insufficient, pas d'erreur), présence de n dans les deux cas.</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run apps/web/src/lib/track-record</automated>
  </verify>
  <acceptance_criteria>
    - Le test threshold passe vert (jamais « No test files found » — si le glob ne couvre pas le chemin, étendre `vitest.config.ts` include et le documenter dans le SUMMARY, précédent D-02-03-C).
    - `applyThreshold({ n: 29, ... }).sufficient === false` et la sortie contient `n === 29`.
    - `applyThreshold({ n: 30, ... }).sufficient === true` et `winRatePct` est un entier.
    - grep `MIN_SAMPLE = 30` dans threshold.ts = 1.
  </acceptance_criteria>
  <done>Helper seuil implémenté, test vert, N exposé dans les deux branches, 30 = seuil exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune) | Plan 100% logique pure (core + helper TS), zéro I/O, zéro entrée utilisateur, zéro réseau, zéro DB. Aucune frontière de confiance traversée. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | replayOutcome (calcul du R) | mitigate | Logique déterministe pure, golden-testée 5 cas (RESEARCH Threat « track record gonflé ») ; tie-break D-04 documenté et testé ; aucune entrée externe → pas de surface d'injection. realized_r calculé sur prix de candles, jamais via scoring (anti-pattern). |
| T-05-02 | Tampering | helper de seuil | accept | Décision d'affichage pure ; ne stocke rien, ne masque jamais N (D-12). Risque nul (pas de persistance, pas de privilège). |

> security_enforcement enabled (ASVS L1). Ce plan n'introduit AUCUNE persistance ni lecture ; les contrôles RLS/anon sont dans 05-02. Aucun package npm ajouté → pas de gate de légitimité.
</threat_model>

<verification>
- `pnpm vitest run packages/core/src/replay` → vert (golden 5 cas + déterminisme).
- `pnpm vitest run apps/web/src/lib/track-record` → vert (seuil N≥30).
- `pnpm typecheck` → aucune NOUVELLE erreur au-delà de la baseline P1 (`forbidden-service-import.ts`).
- Aucun nouveau package npm installé (`git diff package.json` vide).
</verification>

<success_criteria>
- replayOutcome couvre hit_tp / hit_sl / ambigu D-04 (avec tie ≤) / flat long / flat short, déterministe.
- Helper de seuil applique N≥30 et expose toujours N.
- Barrel core ré-exporte replayOutcome + type Outcome.
- Aucune dépendance I/O dans la logique pure.
</success_criteria>

<output>
Create `.planning/phases/05-track-record-mesur-affich/05-01-SUMMARY.md` when done
</output>
