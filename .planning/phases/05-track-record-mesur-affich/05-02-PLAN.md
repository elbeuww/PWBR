---
phase: 05-track-record-mesur-affich
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql
  - packages/supabase/src/repositories/predictionOutcomes.ts
  - packages/supabase/src/repositories/candles.ts
  - packages/supabase/src/index.ts
  - packages/supabase/src/database.types.ts
  - apps/jobs/src/jobs/outcome-tracker.ts
  - apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts
  - apps/jobs/src/dispatch.ts
autonomous: false
requirements: [TRACK-01, TRACK-02]
user_setup: []
must_haves:
  truths:
    - "Le job outcome-tracker sélectionne les setups status IN (expired,invalidated) AND valid_until<now() absents de prediction_outcomes, rejoue chacun via replayOutcome et persiste le résultat — un seul pipeline sur les setups réellement publiés, pas de backtest synthétique (D-07)"
    - "Le job est idempotent par setup_id : un 2e run consécutif insère 0 ligne (sélection bornée + UNIQUE(setup_id) onConflict ignoreDuplicates), réutilise les constantes anti look-ahead du cœur (D-05)"
    - "Le job est tracé dans job_runs via runJob (enregistré dans dispatch.ts)"
    - "La table prediction_outcomes est lisible par authenticated, AUCUNE policy write (service_role bypass)"
    - "La vue pattern_stats agrège win_rate + R moyen + expectancy + N (métriques D-10) par dimension (actif/classe, style, tranche de score, risk_level, global — catégories D-06) all-time ET 90j (périodes D-11), lisible par anon"
    - "prediction_outcomes n'est JAMAIS accessible à anon (get_advisors sans alerte d'exposition)"
  artifacts:
    - path: "supabase/migrations/0014_prediction_outcomes_pattern_stats.sql"
      provides: "Table prediction_outcomes + vue pattern_stats + RLS anon (première du projet)"
      contains: "create view public.pattern_stats"
    - path: "apps/jobs/src/jobs/outcome-tracker.ts"
      provides: "Job I/O de replay, miroir subscription-expiry"
      exports: ["outcomeTracker"]
    - path: "packages/supabase/src/repositories/predictionOutcomes.ts"
      provides: "Insert idempotent service_role"
      exports: ["insertOutcomes"]
    - path: "apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts"
      provides: "Test idempotence + sélection bornée"
      contains: "describe"
  key_links:
    - from: "apps/jobs/src/jobs/outcome-tracker.ts"
      to: "packages/core (replayOutcome)"
      via: "import @app/core"
      pattern: "replayOutcome"
    - from: "apps/jobs/src/dispatch.ts"
      to: "outcome-tracker.ts"
      via: "JOB_REGISTRY"
      pattern: "'outcome-tracker': outcomeTracker"
    - from: "apps/jobs/src/jobs/outcome-tracker.ts"
      to: "prediction_outcomes (DB)"
      via: "insertOutcomes onConflict setup_id"
      pattern: "insertOutcomes"
---

<objective>
Livrer le pipeline de données de TRACK-01 (I/O) et TRACK-02 : la migration 0014 (table `prediction_outcomes` + vue `pattern_stats` avec la PREMIÈRE policy/grant `anon` du projet), les repositories, le job `outcome-tracker` (miroir `subscription-expiry`), son enregistrement dispatch, et l'application LIVE de la migration via MCP avec régénération des types et gate sécurité `get_advisors`.

Purpose: Mesurer le track record réel — rejouer les setups expirés contre les candles H1 et agréger les statistiques publiquement lisibles, frontière producteur-unique stricte (écriture service_role, lecture publique des agrégats SEULEMENT).
Output: migration 0014 appliquée live, types régénérés, job idempotent enregistré, repos, test idempotence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-track-record-mesur-affich/05-CONTEXT.md
@.planning/phases/05-track-record-mesur-affich/05-RESEARCH.md
@.planning/phases/05-track-record-mesur-affich/05-PATTERNS.md
@.planning/phases/05-track-record-mesur-affich/05-VALIDATION.md

<interfaces>
<!-- Depuis @app/core (livré en 05-01) : -->
export function replayOutcome(setup: ReplaySetup, candlesH1: ReplayCandle[]): Outcome
// Outcome = { outcome: 'hit_tp'|'hit_sl'|'flat'; realized_r: number }

<!-- Pattern job idempotent (apps/jobs/src/jobs/subscription-expiry.ts) — getServiceClient lazy,
     fonction async retournant Json, enregistrée dans dispatch.ts JOB_REGISTRY, wrappée par runJob (trace job_runs). -->

<!-- Pattern repo idempotent (packages/supabase/src/repositories/candles.ts) :
     client.from(table).upsert(rows, { onConflict: 'setup_id', ignoreDuplicates: true }) -->

<!-- trade_setups (0006) : id, instrument_id, direction, entry_price, stop_loss,
     take_profits (jsonb [{price,alloc_pct}]), valid_until, created_at, status (active/invalidated/expired),
     style, opportunity_score, risk_level, instrument_class (classe d'actif). -->
<!-- candles (0003) : instrument_id, timeframe in (H1,H4,D), ts, open/high/low/close, volume. -->
</interfaces>

<migration_decisions>
<!-- VERROUILLÉ : migration Phase 5 = 0014 (PAS 0013, réservé cluster paiement P4 différé). -->
<!-- VERROUILLÉ : appliquer via MCP apply_migration, JAMAIS supabase db push. -->
<!-- A1 tranché (RESEARCH Open Q2) : setups 'invalidated' rejoués PLEINEMENT comme 'expired' — laisser replayOutcome décider hit_tp/hit_sl/flat, NE PAS présumer hit_sl. -->
<!-- A2 tranché (RESEARCH Open Q3) : expectancy = AVG(realized_r) sur TOUS les trades ; R moyen = AVG(realized_r) sur les GAGNANTS (outcome='hit_tp') uniquement. Figé dans la vue ET la page méthodologie (05-03). -->
<!-- D-09/D-12 : la vue retourne N BRUT, jamais masqué. Le seuil 30 est appliqué côté front (helper threshold.ts de 05-01). -->
</migration_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Test idempotence outcome-tracker + extension repo candles (lecture replay)</name>
  <read_first>
    - apps/jobs/src/jobs/__tests__/subscription-expiry.test.ts (analog EXACT : store mémoire, vi.mock @supabase/supabase-js, builder chaînable, test 2e run = 0)
    - apps/jobs/src/jobs/subscription-expiry.ts (squelette getServiceClient lazy, stats Json)
    - packages/supabase/src/repositories/candles.ts (getLastCandleTs comme modèle de lecture bornée ordonnée)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.242-249 sélection idempotente ; Pitfall 3 double comptage ; Pitfall 4 invalidated = A1)
  </read_first>
  <action>Créer apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts en miroir de subscription-expiry.test.ts : store en mémoire (tableaux setups[] + outcomes[]), builder chaînable simulant `.from('trade_setups').select().in('status',...).lt('valid_until',...)`, `.from('prediction_outcomes').select('setup_id')` et l'upsert onConflict ignoreDuplicates (incrémente outcomes seulement si setup_id absent), `vi.mock('@supabase/supabase-js', () => ({ createClient: () => makeClient() }))`. Fixtures : 2+ setups status 'expired'/'invalidated' avec valid_until passé ET des candles H1 dans la fenêtre (pour produire hit_tp/hit_sl/flat), plus 1 setup 'active' ou valid_until futur (NON éligible). Assertions : 1er run insère N outcomes et retourne stats `{ resolved, skipped }` ; 2e run consécutif insère 0 (idempotent) ; le setup non-éligible n'est jamais traité ; un setup 'invalidated' est rejoué pleinement (A1, pas présumé hit_sl). Étendre packages/supabase/src/repositories/candles.ts : ajouter `getCandlesForReplay(client, instrumentId, from, to)` (modèle getLastCandleTs) sélectionnant timeframe='H1', ts >= from AND ts <= to, ordre `ts asc`. Le test DOIT être RED (outcome-tracker.ts absent).</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts existe et échoue RED (module outcome-tracker absent), JAMAIS « No test files found ».
    - Le test contient une assertion explicite « 2e run = 0 insert » (idempotence).
    - getCandlesForReplay ajoutée à candles.ts (grep `getCandlesForReplay` = 1) avec ordre `ts asc` et filtre timeframe H1.
  </acceptance_criteria>
  <done>Test idempotence authoré RED, repo candles étendu pour le replay.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0014 (table + vue + RLS anon) + repos predictionOutcomes/patternStats + barrel</name>
  <read_first>
    - supabase/migrations/0006_analyses_trade_setups.sql (analog table + RLS authenticated + en-tête D-17 « jamais db push »)
    - supabase/migrations/0003_data_ingestion_tables.sql (candles RLS to authenticated)
    - packages/supabase/src/repositories/tradeSetups.ts (en-tête frontière producteur-unique D-07)
    - packages/supabase/src/repositories/candles.ts (insert idempotent onConflict ignoreDuplicates)
    - packages/supabase/src/index.ts (barrel, blocs export et types)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.293-324 esquisse SQL ; Pitfall 1 security_invoker=false + grant anon)
    - .planning/phases/05-track-record-mesur-affich/05-PATTERNS.md (§migration 0014, §pattern_stats RLS anon NEUF)
  </read_first>
  <action>Créer supabase/migrations/0014_prediction_outcomes_pattern_stats.sql. En-tête commentaire citant TRACK-01/TRACK-02, « appliquer via MCP apply_migration, JAMAIS db push », et « PREMIÈRE policy/grant anon du projet ». (1) Table `public.prediction_outcomes` : `setup_id uuid primary key references public.trade_setups(id) on delete cascade`, `outcome text not null check (outcome in ('hit_tp','hit_sl','flat'))`, `realized_r numeric not null`, `resolved_at timestamptz not null default now()`, `candle_count int` (minimisation données — pas plus de colonnes). `enable row level security` + policy `"prediction_outcomes: lecture authentifiés" for select to authenticated using (true)`. AUCUNE policy insert/update/delete (service_role bypass, frontière D-05). (2) Vue `public.pattern_stats with (security_invoker = false)` : une ligne par (dimension, bucket, period). Colonnes : `dimension text`, `bucket text`, `period text`, `n int` (count BRUT, jamais masqué — D-12), `win_rate numeric` (avg((outcome='hit_tp')::int)), `avg_r numeric` (avg(realized_r) FILTER WHERE outcome='hit_tp' — R moyen des GAGNANTS, A2), `expectancy numeric` (avg(realized_r) sur TOUS — A2). UNION ALL des dimensions D-06 : 'overall'/'all', 'style'/ts.style, 'asset'/instrument_id, 'asset_class'/instrument_class, 'score_band' (CASE opportunity_score >= 80 THEN '80-100' >=60 '60-79' ELSE '<60'), 'risk'/risk_level ; chaque dimension dupliquée pour period 'all_time' (sans filtre) ET '90d' (WHERE resolved_at > now() - interval '90 days'). `grant select on public.pattern_stats to anon, authenticated`. Créer packages/supabase/src/repositories/predictionOutcomes.ts : en-tête frontière (jamais importé depuis apps/web), `insertOutcomes(client, rows: PredictionOutcomeInsert[])` upsert onConflict 'setup_id' ignoreDuplicates true, et une lecture `getResolvedSetupIds(client)` (select setup_id from prediction_outcomes → Set) pour la sélection bornée. Étendre packages/supabase/src/index.ts : exporter insertOutcomes + getResolvedSetupIds + types PredictionOutcomeRow/Insert (ajoutés après régénération en Task 4). NE PAS créer de repo read service_role pour pattern_stats (lu en anon par 05-03).</action>
  <verify>
    <automated>cd "apps/.." && pnpm typecheck 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - 0014_prediction_outcomes_pattern_stats.sql existe ; grep `create view public.pattern_stats` = 1 ; grep `to anon` = 1 (première du projet) ; grep `security_invoker = false` = 1.
    - La table a `setup_id uuid primary key references public.trade_setups` et AUCUNE policy write (grep `for insert`/`for update`/`for delete` sur prediction_outcomes = 0).
    - La vue contient les dimensions overall/style/asset/asset_class/score_band/risk ET les deux périodes all_time + 90d.
    - `avg_r` filtre les gagnants (FILTER WHERE outcome='hit_tp'), `expectancy` non filtré (A2 figé).
    - insertOutcomes utilise onConflict 'setup_id' ignoreDuplicates true (grep = 1). Repo non importé par apps/web.
  </acceptance_criteria>
  <done>Migration SQL écrite (table + vue + RLS anon), repos service_role, barrel étendu, typecheck sans nouvelle erreur (types DB pleinement résolus après Task 4).</done>
</task>

<task type="auto">
  <name>Task 3 (GREEN): Job outcome-tracker + enregistrement dispatch</name>
  <read_first>
    - apps/jobs/src/jobs/subscription-expiry.ts (miroir DIRECT : getServiceClient lazy, signature async Json)
    - apps/jobs/src/dispatch.ts (JOB_REGISTRY, import jobs, runJob trace job_runs)
    - apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts (contrat à satisfaire — Task 1)
    - packages/supabase/src/repositories/candles.ts (getCandlesForReplay ajoutée Task 1)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (§Code Examples l.242-249 ; Pitfall 3/4)
  </read_first>
  <action>Créer apps/jobs/src/jobs/outcome-tracker.ts en miroir de subscription-expiry.ts. Importer `'dotenv/config'`, `{ createClient } from '@supabase/supabase-js'`, `{ replayOutcome } from '@app/core'`, `{ insertOutcomes, getResolvedSetupIds, getCandlesForReplay } from '@app/supabase'`, `type { Json, Database } from '@app/supabase'`. Copier `getServiceClient()` (message d'erreur « outcome-tracker: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set »). Exporter `async function outcomeTracker(): Promise<Json>` : (1) SELECT trade_setups (id, instrument_id, direction, entry_price, stop_loss, take_profits, valid_until, created_at, style, opportunity_score, risk_level) WHERE status IN ('expired','invalidated') AND valid_until < now() ; (2) charger getResolvedSetupIds → exclure ceux déjà traités (sélection bornée, idempotence niveau 1) ; (3) pour chaque setup éligible : getCandlesForReplay(client, instrument_id, created_at, valid_until), mapper vers ReplayCandle, appeler replayOutcome(setup, candles) → { outcome, realized_r }, accumuler une ligne PredictionOutcomeInsert { setup_id, outcome, realized_r, candle_count } ; (4) insertOutcomes (filet UNIQUE setup_id, idempotence niveau 2) ; retourner `{ resolved, skipped } as Json`. Les setups 'invalidated' sont rejoués pleinement (A1, jamais présumés hit_sl). Étendre apps/jobs/src/dispatch.ts : importer outcomeTracker et ajouter `'outcome-tracker': outcomeTracker` au JOB_REGISTRY avec un commentaire « TRACK-01 — replay déterministe des setups expirés. Windows Task Scheduler : run-job.cmd outcome-tracker ». NE PAS écrire job_runs à la main (runJob s'en charge).</action>
  <verify>
    <automated>cd "apps/.." && pnpm vitest run apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts` → vert, dont l'assertion « 2e run = 0 insert ».
    - dispatch.ts contient `'outcome-tracker': outcomeTracker` (grep = 1) et l'import.
    - outcome-tracker.ts importe replayOutcome depuis '@app/core' (grep = 1) et n'écrit jamais job_runs directement (grep `job_runs` dans outcome-tracker.ts = 0).
    - Sélection bornée présente : status IN ('expired','invalidated') + exclusion via getResolvedSetupIds.
  </acceptance_criteria>
  <done>Job outcome-tracker implémenté GREEN, idempotent à 2 niveaux, enregistré dispatch, tracé via runJob.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [BLOCKING]: Appliquer la migration 0014 LIVE via MCP + régénérer les types + gate get_advisors</name>
  <read_first>
    - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql (le fichier à appliquer)
    - .planning/STATE.md (B-04-02 / D-01-01-D : pattern apply_migration LIVE, JAMAIS db push)
    - .planning/phases/05-track-record-mesur-affich/05-RESEARCH.md (Pitfall 1 : get_advisors DOIT confirmer prediction_outcomes inaccessible à anon)
  </read_first>
  <what-built>
    Migration 0014 écrite sur disque (Task 2) : table prediction_outcomes (RLS authenticated, write service_role bypass) + vue pattern_stats (security_invoker=false, grant SELECT anon — PREMIÈRE policy anon du projet). Repos + job prêts et testés. La migration N'EST PAS encore dans la base live.
  </what-built>
  <how-to-verify>
    L'orchestrateur exécute via MCP Supabase (PAS `supabase db push`), après confirmation humaine :
    1. `apply_migration` name `0014_prediction_outcomes_pattern_stats` avec le contenu du fichier SQL.
    2. `generate_typescript_types` → écrire dans packages/supabase/src/database.types.ts (ajouter prediction_outcomes Row/Insert/Update + la vue pattern_stats dans le bloc Views). Retirer tout cast temporaire dans predictionOutcomes.ts (aucun stub de type inventé).
    3. `list_tables` → confirmer prediction_outcomes (PK setup_id, FK trade_setups, RLS enabled, AUCUNE policy write) + la vue pattern_stats.
    4. `get_advisors` type `security` → CONFIRMER qu'AUCUNE alerte n'expose prediction_outcomes à anon (seuls les agrégats pattern_stats sont publics). Une vue security_invoker=false sur table RLS peut déclencher un advisor « security definer view » — vérifier qu'il vise pattern_stats (attendu/voulu) et NON une fuite de prediction_outcomes.
    5. Test manuel : tenter un SELECT sur prediction_outcomes en client anon → doit retourner 0 ligne / refus ; SELECT pattern_stats en anon → doit retourner les lignes d'agrégat.
    6. Re-run `pnpm typecheck` → vert (types pleinement résolus, casts retirés).
  </how-to-verify>
  <resume-signal>`applied` + sortie list_tables + verdict get_advisors (aucune fuite prediction_outcomes), OU description du problème RLS à corriger.</resume-signal>
  <acceptance_criteria>
    - `list_tables` montre prediction_outcomes + pattern_stats appliquées.
    - `get_advisors` security : aucune alerte exposant prediction_outcomes à anon (gate phase obligatoire).
    - database.types.ts régénéré contient prediction_outcomes et pattern_stats ; `pnpm typecheck` vert.
    - SELECT anon : pattern_stats OK, prediction_outcomes refusé/0 ligne.
  </acceptance_criteria>
  <done>Migration appliquée LIVE, types régénérés, get_advisors confirme la frontière anon, typecheck vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job (service_role) → DB | Le job écrit prediction_outcomes via service_role (bypass RLS). Donnée d'entrée = candles internes déjà validées à l'ingestion. |
| anon (public, sans compte) → DB (vue pattern_stats) | PREMIÈRE lecture publique du projet. Seuls les agrégats anonymes traversent ; jamais les lignes par setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | prediction_outcomes exposé à anon | mitigate | prediction_outcomes reste `to authenticated` strict (aucune policy anon). Seule la vue agrégée pattern_stats a `grant select to anon`. Gate `get_advisors` (security) obligatoire en Task 4 confirme l'absence de fuite par setup. ASVS V4 (CRITIQUE). |
| T-05-04 | Elevation of Privilege | écriture front dans prediction_outcomes/pattern_stats | mitigate | AUCUNE policy insert/update/delete sur prediction_outcomes → écriture service_role bypass uniquement (frontière D-05, pattern 0006/0009). pattern_stats est une vue (non-writable). Repo predictionOutcomes JAMAIS importé par apps/web (en-tête frontière). |
| T-05-05 | Integrity / Tampering | double comptage faussant les stats | mitigate | Sélection bornée (getResolvedSetupIds, exclusion) + UNIQUE(setup_id) PK + insert onConflict ignoreDuplicates. Test idempotence « 2e run = 0 » (Task 1/3). |
| T-05-06 | Tampering | track record gonflé / inventé | mitigate | Seul le job service_role écrit ; replayOutcome déterministe golden-testé (05-01) ; la vue n'invente rien (agrégat SQL pur) ; N brut exposé (D-12). Core Value « jamais inventé ». |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé en Phase 5 (RESEARCH §Package Legitimacy Audit = N/A). Pas de surface slopsquatting. |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/src/jobs/__tests__/outcome-tracker.test.ts` → vert (idempotence).
- `pnpm vitest run packages/core/src/replay` → toujours vert (non-régression 05-01).
- Migration 0014 appliquée LIVE via MCP (jamais db push) ; `list_tables` confirme.
- `get_advisors` security : aucune fuite prediction_outcomes à anon.
- `pnpm typecheck` vert après régénération des types.
</verification>

<success_criteria>
- prediction_outcomes + pattern_stats existent en base, RLS correcte (authenticated read table, anon read vue agrégée).
- outcome-tracker rejoue les setups expirés (incl. invalidated, A1) idempotemment, tracé job_runs.
- pattern_stats agrège win_rate + R moyen (gagnants) + expectancy (tous) + N brut, all-time + 90j, par dimension D-06.
- Frontière producteur-unique respectée et prouvée par get_advisors.
</success_criteria>

<output>
Create `.planning/phases/05-track-record-mesur-affich/05-02-SUMMARY.md` when done
</output>
