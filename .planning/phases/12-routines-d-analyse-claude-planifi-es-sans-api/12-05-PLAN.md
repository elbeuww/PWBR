---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-03", "12-04"]
files_modified: []
autonomous: false
requirements: [ROUTINE-02, ROUTINE-03, ROUTINE-04]
user_setup:
  - service: claude-code-routines
    why: "Création des routines Remote (cron) via /schedule + dashboard ; le run réel tourne en cloud"
    dashboard_config:
      - task: "Créer les routines Remote `newyork` (30 12 * * 1-5 UTC) et `eod-swing` (00 21 * * 1-5 UTC), liées à l'Environment Custom"
        location: "/schedule (CLI) + claude.ai/code/routines (Environment binding)"
must_haves:
  truths:
    - "Deux routines Remote planifiées existent : `newyork` (day) et `eod-swing` (swing), crons UTC alignés sur sessions.ts"
    - "Un run réel de bout en bout (ingest→engines→combine→ANALYZE→persist) produit ≥1 trade_setups actif via persist.ts (frontière D-43 intacte, score recalculé)"
    - "Le run est tracé dans job_runs (status success, stats {written,rejected,reasons[]}) ; un re-run du même session_day ne crée pas de doublon"
    - "/admin/sante affiche le run et le flag stale dérivé de v_data_freshness, sous le budget quota (~15/j)"
  artifacts:
    - path: "docs/routines-claude.md"
      provides: "Trace des 2 routines activées + résultat du 1er run réel (date, session, setups écrits)"
      contains: "newyork"
  key_links:
    - from: "routine newyork (cron 30 12)"
      to: "trade_setups"
      via: "dispatch persist → frontière D-43"
      pattern: "session='newyork'"
    - from: "/admin/sante"
      to: "job_runs + v_data_freshness"
      via: "loadHealth (latestPerJob + is_stale)"
      pattern: "is_stale"
---

<objective>
Activer les 2 routines du rollout minimal (D-12-01 : `newyork` day + `eod-swing` swing), exécuter UN run réel de bout en bout, et confirmer qu'il produit ≥1 setup persisté via la frontière D-43 ET que le monitoring (`job_runs` + `stale` sur `/admin/sante`) le reflète. C'est le gate ROUTINE-03 qui dé-risque la phase avant d'élargir (plan 06).

Purpose: Le fondateur veut inspecter un setup persisté RÉEL avant de tout brancher. Ce plan prouve la chaîne complète en production (réseau confirmé en plan 03 + prompt revu en plan 04) sur le périmètre le plus riche (newyork = H1/H4/D actifs).
Output: 2 routines Remote créées + 1 run réel vert (≥1 trade_setups) + idempotence vérifiée + /admin/sante affichant le run et le flag stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md
@docs/routines-claude.md
@apps/jobs/config/sessions.ts
@apps/jobs/src/dispatch.ts
@apps/jobs/src/jobs/persist.ts
@apps/jobs/prompts/veteran.md
@apps/web/src/app/(admin)/sante/page.tsx

<interfaces>
<!-- Contrats que le run doit respecter — NE PAS éditer ces fichiers. -->

Séquence single-run (Pattern 1, RESEARCH) — UN SEUL run cloud, filesystem partagé :
  pnpm install
  tsx src/dispatch.ts market-ingest | news-ingest | macro-ingest
  tsx src/dispatch.ts technical-engine | fundamental-engine | news-engine
  tsx src/dispatch.ts combine-engine
  ANALYZE agent-native : lit snapshots kind='combined', applique veteran.md,
    écrit run-artifacts/<RUN_ID>/<instrument>_<style>.json (1 JSON §3 par fichier)
  RUN_ID=newyork-<YYYYMMDD>T1730Z tsx src/dispatch.ts persist

RUN_ID format strict (runArtifacts.ts RUN_ID_RE) : <session>-<YYYYMMDD>T<HHmm>Z
persist.ts : RUN_ID requis (throw sinon) ; expirePriorSetups(session_day) AVANT insert (idempotence D-45) ;
  WR-04 : 0 écrit + rejets>0 → throw ; vrai-vide (0 artefact) → ne pas appeler persist (no_artifacts).
job_runs (runJob) : status running→success/error, stats, finished_at — source du monitoring.
/admin/sante (loadHealth) : latestPerJob(job_runs) + v_data_freshness.is_stale (jamais re-dérivé côté JS).

Crons UTC (sessions.ts source de vérité) :
  newyork   30 12 * * 1-5   ← rollout #1 (day)
  eod-swing 00 21 * * 1-5   ← rollout #1 (swing)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Créer les routines Remote newyork + eod-swing (crons UTC alignés sessions.ts)</name>
  <files>Config cloud (hors git) : routines Remote `newyork` + `eod-swing` via /schedule + dashboard ; edit docs/routines-claude.md (liste routines actives).</files>
  <read_first>
    - apps/jobs/config/sessions.ts (crons UTC source de vérité — newyork 30 12, eod-swing 00 21)
    - docs/routines-claude.md (§7 checklist go-live, §8 séquence single-run — plan 02)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Code Examples création routine, § Architecture Pattern 2, min interval 1h, local→UTC)
  </read_first>
  <what-built>
    Deux routines Remote persistantes (PAS des Cron* session-scoped, D-12-11) créées via `/schedule` puis liées à l'Environment Custom (plan 03) au dashboard. Chaque routine porte le PROMPT d'orchestration single-run (la séquence complète de l'§interface dans UN run). Rollout minimal D-12-01 : SEULEMENT newyork + eod-swing (asia/london = plan 06).
  </what-built>
  <how-to-verify>
    1. Via `/schedule`, créer la routine `newyork` au cron `30 12 * * 1-5` (saisir en UTC ; min interval 1h respecté). Coller le prompt d'orchestration single-run (séquence de l'§interface, RUN_ID=`newyork-<YYYYMMDD>T1730Z` dérivé de la fenêtre). Lier à l'Environment `nexa-jobs`.
    2. Idem `eod-swing` au cron `00 21 * * 1-5`, RUN_ID=`eod-swing-<YYYYMMDD>T2100Z`, même Environment.
    3. Résolution day (Open Question 1, Claude's Discretion) : pour le rollout minimal, UNE fenêtre newyork (clôture, la plus riche — H1/H4/D actifs, cohérent D-12-03). La fenêtre d'ouverture est différée à l'élargissement si valeur prouvée. Documenter ce choix.
    4. Confirmer dans le dashboard que les 2 routines sont `Remote` (pas Local), persistantes, liées au bon Environment, avec le bon cron UTC.
    5. Mettre à jour docs/routines-claude.md : lister les 2 routines actives (nom, cron UTC, RUN_ID pattern).
  </how-to-verify>
  <action>
    Au dashboard/CLI : créer la routine Remote `newyork` (cron `30 12 * * 1-5` UTC, RUN_ID pattern `newyork-<YYYYMMDD>T1730Z`) et `eod-swing` (cron `00 21 * * 1-5` UTC, RUN_ID pattern `eod-swing-<YYYYMMDD>T2100Z`) via `/schedule`, liées à l'Environment Custom `nexa-jobs`. Chaque routine porte le prompt d'orchestration single-run COMPLET (la séquence de l'§interface : pnpm install → ingest×3 → engines×3 → combine-engine → ANALYZE agent-native → persist avec RUN_ID exporté), jamais une étape par routine. NE PAS créer asia/london (rollout minimal D-12-01). Documenter la résolution day = 1 fenêtre clôture (Open Question 1). Éditer docs/routines-claude.md avec la liste des 2 routines actives.
  </action>
  <verify>Dashboard : 2 routines Remote (`newyork` cron `30 12 * * 1-5`, `eod-swing` cron `00 21 * * 1-5`) liées à `nexa-jobs` ; asia/london absentes ; docs/routines-claude.md les liste.</verify>
  <done>2 routines Remote single-run créées avec crons UTC ⊆ sessions.ts, liées à l'Environment validé ; rollout minimal respecté.</done>
  <acceptance_criteria>
    - 2 routines Remote existent : `newyork` (cron `30 12 * * 1-5`) et `eod-swing` (cron `00 21 * * 1-5`), liées à l'Environment Custom.
    - Les crons sont en UTC et ⊆ ceux de sessions.ts (alignement ROUTINE-02).
    - Le prompt de chaque routine orchestre la séquence single-run complète (un run = ingest→…→persist), pas une étape par routine.
    - asia/london NE sont PAS créées (rollout minimal D-12-01).
    - docs/routines-claude.md liste les 2 routines (nom, cron, RUN_ID pattern).
  </acceptance_criteria>
  <resume-signal>Taper « routines-created » quand newyork + eod-swing existent en Remote avec les crons UTC corrects et le prompt single-run.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run réel de bout en bout newyork — ≥1 setup persisté via D-43 (gate ROUTINE-03)</name>
  <files>Run cloud réel (hors git) ; écrit trade_setups/analyses/job_runs en DB ; edit docs/routines-claude.md (résultat 1er run).</files>
  <read_first>
    - apps/jobs/src/jobs/persist.ts (frontière D-43 : RUN_ID requis, Zod §3, guardrails MIN_RR=1.2, scoreSetup recalculé, expirePriorSetups, WR-04)
    - apps/jobs/src/jobs/runArtifacts.ts (RUN_ID_RE, no_artifacts sur dir vide)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Architecture Pattern 1 handoff, § Pitfall P-EMPTY, P-HALLUC)
    - apps/jobs/run-artifacts/ (forme attendue des artefacts ANALYZE — référence)
  </read_first>
  <what-built>
    Un run réel one-off de la routine `newyork` (déclenchement manuel one-off, qui ne compte pas dans le daily cap) exécutant la séquence single-run complète. L'ANALYZE agent-native lit les snapshots combined, applique veteran.md, écrit les artefacts ; persist (RUN_ID exporté) valide/score/persiste via la frontière D-43.
  </what-built>
  <how-to-verify>
    1. Déclencher un run one-off de `newyork`. Suivre la séquence single-run jusqu'à `RUN_ID=newyork-<YYYYMMDD>T1730Z tsx src/dispatch.ts persist`.
    2. Vérifier le handoff : les artefacts `run-artifacts/<RUN_ID>/*.json` ont été écrits PUIS relus par persist dans le MÊME run (pas de clone frais entre ANALYZE et persist).
    3. GATE ROUTINE-03 — au moins un setup persisté : `select count(*) from trade_setups where session='newyork' and session_day = <jour UTC du run>` ≥ 1, status='active'.
    4. Frontière intacte : inspecter un trade_setups produit — `opportunity_score` provient de scoreSetup (code), PAS du JSON agent ; `entry_price` = bord conservateur (guard.entryCons) ; `risk_reward` ≥ 1.2. Confirmer via le payload vs colonnes (dualité D-43).
    5. Cas marché-calme acceptable : si l'ANALYZE n'a produit AUCUN setup discipliné (0 artefact), persist n'a PAS été appelé → `job_runs` success sans setup, PAS une erreur (D-12-02). Dans ce cas, RELANCER sur une fenêtre/jour où le marché offre une confluence, car le GATE exige ≥1 setup réel à inspecter (intention fondateur).
    6. Inspection fondateur : montrer un setup persisté réel (direction, entry/SL/TP, R:R, score) pour validation qualitative.
    7. Consigner dans docs/routines-claude.md : date du 1er run réel, session, nombre de setups écrits/rejetés.
  </how-to-verify>
  <action>
    Déclencher un run one-off de `newyork` exécutant la séquence single-run complète jusqu'à `RUN_ID=newyork-<YYYYMMDD>T1730Z tsx src/dispatch.ts persist`. Vérifier le handoff intra-run (artefacts écrits puis relus dans le même run). Confirmer `select count(*) from trade_setups where session='newyork' and session_day=<jour>` ≥1 (status='active'). Inspecter un setup : `opportunity_score` = scoreSetup (code, jamais le champ agent), `risk_reward` ≥1.2, `entry_price` = bord conservateur. Si marché calme (0 artefact, no_artifacts), relancer sur une fenêtre offrant une confluence (le gate exige ≥1 setup réel à inspecter). Présenter un setup réel au fondateur pour validation qualitative. Éditer docs/routines-claude.md (date du 1er run, session, setups écrits/rejetés). NE PAS modifier persist.ts (frontière intacte).
  </action>
  <verify>`select count(*) from trade_setups where session='newyork' and session_day=<jour>` ≥1, status='active' ; `opportunity_score` cohérent scoreSetup ; `risk_reward` ≥1.2 ; job_runs persist status='success', stats.written ≥1.</verify>
  <done>≥1 setup réel persisté via D-43 (score recalculé, R:R≥1.2), handoff single-run prouvé, inspecté et validé par le fondateur.</done>
  <acceptance_criteria>
    - `select count(*) from trade_setups where session='newyork' and session_day=<jour>` ≥ 1 (status='active').
    - Le `opportunity_score` du setup est cohérent avec scoreSetup (recalculé code), JAMAIS recopié d'un champ agent ; `risk_reward` ≥ 1.2.
    - Les artefacts run-artifacts/<RUN_ID>/ ont été écrits ET relus dans le même run (handoff single-run prouvé).
    - `job_runs` (job persist du run) : status='success', stats.written ≥ 1.
    - Le fondateur a inspecté ≥1 setup réel et validé qualitativement.
  </acceptance_criteria>
  <resume-signal>Taper « run-green » avec le nombre de setups persistés (≥1) quand le fondateur a inspecté un setup réel.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérifier idempotence (re-run) + monitoring stale sur /admin/sante (ROUTINE-04)</name>
  <files>Re-run cloud (hors git) ; lecture /admin/sante (RSC superadmin) ; aucune édition de fichier repo.</files>
  <read_first>
    - apps/jobs/src/jobs/persist.ts (expirePriorSetups AVANT insert, clé session_day — re-run sûr)
    - apps/web/src/app/(admin)/sante/page.tsx (loadHealth : latestPerJob(job_runs) + v_data_freshness.is_stale jamais re-dérivé)
    - apps/jobs/__tests__/routine-idempotence.test.ts (contrat unitaire d'idempotence — plan 01)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-QUOTA, § Validation Architecture ROUTINE-04)
  </read_first>
  <what-built>
    Confirmation que (a) re-lancer le run du même session_day n'accumule pas de doublons (expirePriorSetups), (b) le dashboard `/admin/sante` reflète l'état réel (run récent visible, flag stale correct), (c) la consommation quota reste sous le budget (~15/j).
  </what-built>
  <how-to-verify>
    1. Idempotence : noter `count(*)` de trade_setups pour (newyork, session_day). Re-déclencher le run one-off du MÊME session_day. Re-compter : le nombre de setups ACTIFS pour cette clé ne DOUBLE pas (les anciens sont expirés avant réinsertion). `select status, count(*) from trade_setups where session='newyork' and session_day=<jour> group by status` → au plus 1 jeu actif.
    2. Monitoring : ouvrir `/admin/sante` (superadmin). La table job_runs montre le dernier run (persist/combine/ingest) avec statut et durée. Le feu candles dérive de `v_data_freshness.is_stale` (jamais re-dérivé côté JS, T-08-12).
    3. Flag stale : confirmer que si une fenêtre est ratée (pas de MAJ dans la fenêtre), le feu passe à amber/red — soit via l'état réel, soit en notant le contrat (la vue gère week-end FX/DST). Le test `fault-isolation.test.ts` couvre déjà l'interrogeabilité de is_stale.
    4. Quota : vérifier sur `claude.ai/code/routines` / `claude.ai/settings/usage` que la conso reste sous le budget (~15/j) — 2 routines ≪ 15.
    5. Consigner dans le SUMMARY : résultat idempotence (pas de doublon), observation /admin/sante, conso quota.
  </how-to-verify>
  <action>
    Re-déclencher le run one-off newyork du MÊME session_day et vérifier `select status, count(*) from trade_setups where session='newyork' and session_day=<jour> group by status` → au plus 1 jeu ACTIF (expirePriorSetups, D-45). Ouvrir `/admin/sante` (superadmin) : confirmer que la table job_runs montre le dernier run (statut/durée) et que le feu de fraîcheur candles dérive de `v_data_freshness.is_stale` (lu de la vue, jamais re-dérivé côté JS, T-08-12). Vérifier la conso quota sur claude.ai/settings/usage < ~15/j (2 routines ≪ 15). Aucune édition de fichier repo.
  </action>
  <verify>`group by status` sur (newyork, session_day) → 1 seul jeu actif (pas de doublon) ; `/admin/sante` affiche le run + feu de fraîcheur dérivé de is_stale ; quota < ~15/j.</verify>
  <done>Idempotence run-level confirmée (pas de doublon), /admin/sante reflète run + stale (is_stale non re-dérivé), conso sous budget quota.</done>
  <acceptance_criteria>
    - Re-run même session_day : un seul jeu de setups ACTIFS pour (newyork, session_day) — pas de doublon (expirePriorSetups, D-45).
    - `/admin/sante` affiche le dernier run job_runs (statut/durée/dernier run) et le feu de fraîcheur dérivé de v_data_freshness.is_stale.
    - is_stale n'est PAS re-dérivé côté JS (lu de la vue, T-08-12) — contrat préservé.
    - Conso quota sous ~15/j (2 routines, pression faible, D-12-03).
  </acceptance_criteria>
  <resume-signal>Taper « monitoring-ok » quand l'idempotence est confirmée (pas de doublon) et /admin/sante reflète le run + le flag stale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefact ANALYZE (LLM) → persist.ts (DB) | la sortie non fiable franchit la frontière D-43 ; score recalculé, jamais celui de l'agent |
| run cloud → trade_setups | écriture seulement via persist service_role (jamais MCP) |
| /admin/sante → v_data_freshness | is_stale lu de la vue, jamais re-dérivé (intégrité du signal de fraîcheur) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Spoofing/Tampering | score/levels hallucinés | mitigate | persist.ts recalcule opportunity_score + R:R bord conservateur, rejette incohérences ; Task 2 vérifie le score ≠ champ agent |
| T-12-13 | Tampering | frontière D-43 contournée | mitigate | Aucune écriture hors persist ; le run n'utilise que dispatch→runJob→supabase-js (ROUTINE-05) |
| T-12-14 | Repudiation | doublons au re-run | mitigate | expirePriorSetups(session_day) AVANT insert ; Task 3 vérifie l'absence de doublon actif |
| T-12-15 | Denial of Service | épuisement quota partagé | accept | 2 routines ≪ ~15/j ; règle de dégradation D-12-03 (sacrifier asia/london) ; monitoring quota Task 3 |
| T-12-SC | Tampering | pnpm install dans le run | accept | install verrouillé par pnpm-lock du repo ; aucun nouveau package |
</threat_model>

<verification>
- `select count(*) from trade_setups where session='newyork' and session_day=<jour>` ≥ 1.
- Re-run même clé → pas de doublon actif (group by status).
- `/admin/sante` montre le run + feu de fraîcheur dérivé de v_data_freshness.is_stale.
- `pnpm --filter jobs exec vitest run` (suite jobs incluant routine-*.test.ts du plan 01) reste vert.
</verification>

<success_criteria>
- ROUTINE-02 : 2 routines Remote (newyork day + eod-swing swing) alignées sessions.ts.
- ROUTINE-03 : 1 run réel produit ≥1 setup persisté via D-43, frontière intacte, inspecté par le fondateur.
- ROUTINE-04 : idempotence confirmée + stale visible sur /admin/sante, sous budget quota.
- Gate franchi → élargissement asia/london autorisé (plan 06).
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-05-SUMMARY.md` when done
</output>
