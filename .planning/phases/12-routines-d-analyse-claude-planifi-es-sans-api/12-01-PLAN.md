---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/jobs/__tests__/routine-persist-empty.test.ts
  - apps/jobs/__tests__/routine-idempotence.test.ts
  - apps/jobs/__tests__/routine-no-mcp.test.ts
autonomous: true
requirements: [ROUTINE-03, ROUTINE-04, ROUTINE-05]
must_haves:
  truths:
    - "Un run au marché calme (0 artefact ANALYZE) ne déclenche PAS d'anomalie (no_artifacts est un non-événement, pas une erreur de pipeline)"
    - "Un run dont les setups sont TOUS rejetés (rejected>0, written=0) throw — visible dans job_runs.status=error (WR-04 préservé)"
    - "Un re-run du même session_day n'insère pas de doublon (expirePriorSetups AVANT insert)"
    - "Aucun import MCP ni clé ANTHROPIC_API_KEY dans apps/jobs/src (ROUTINE-05)"
  artifacts:
    - path: "apps/jobs/__tests__/routine-persist-empty.test.ts"
      provides: "Garde D-12-02 : true-empty vs all-rejected sémantique de persist/readRunArtifacts"
      contains: "no_artifacts"
    - path: "apps/jobs/__tests__/routine-idempotence.test.ts"
      provides: "Contrat idempotence run-level (expirePriorSetups appelé AVANT insert, clé session_day)"
      contains: "expirePriorSetups"
    - path: "apps/jobs/__tests__/routine-no-mcp.test.ts"
      provides: "Static-check ROUTINE-05 : pas de MCP, pas de clé Anthropic dans apps/jobs/src"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "apps/jobs/__tests__/routine-persist-empty.test.ts"
      to: "apps/jobs/src/jobs/persist.ts"
      via: "import persist + mock readRunArtifacts"
      pattern: "from '\\.\\./src/jobs/persist'"
    - from: "apps/jobs/__tests__/routine-no-mcp.test.ts"
      to: "apps/jobs/src"
      via: "readdirSync scan des imports"
      pattern: "readdirSync|readFileSync"
---

<objective>
Authorer les tests de validation Wave-0 manquants (RESEARCH § Wave 0 Gaps) qui verrouillent les invariants de la phase AVANT toute activation cloud : la sémantique « marché calme » (D-12-02), le contrat d'idempotence run-level, et le static-check ROUTINE-05 (pas de MCP, pas de clé Anthropic).

Purpose: Rendre les contrats ROUTINE-03/04/05 vérifiables en local (< 30 s) afin que le 1er run réel cloud (plans 03/05) ne soit pas la seule preuve. Ferme les trous Nyquist de 12-VALIDATION.md.
Output: 3 nouveaux fichiers de test sous apps/jobs/__tests__/, verts via `pnpm --filter jobs exec vitest run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md

<interfaces>
<!-- Contrats à respecter — extraits du code, NE PAS éditer les fichiers source. -->

persist.ts (apps/jobs/src/jobs/persist.ts) — frontière D-43, REUSED AS-IS :
  export async function persist(): Promise<Json>
  - lit process.env['RUN_ID'] (throw si absent), MODEL_LABEL (défaut 'claude-code-max'), PROMPT_VERSION (sinon computePromptVersion()).
  - boucle `for (const artifact of readRunArtifacts(runId))` → reject(code) | written++.
  - ligne 369 (WR-04) : `if (stats.written === 0 && stats.rejected > 0) throw new Error(...)`.
  - exports nommés réutilisables par les tests : runGuardrails, stripFence, sessionDayOf, computePromptVersion.

readRunArtifacts.ts (apps/jobs/src/jobs/runArtifacts.ts) — REUSED AS-IS :
  export function readRunArtifacts(runId: string): RunArtifact[]
  - ligne 80 : `if (artifacts.length === 0) throw new Error('no_artifacts')` — un répertoire VIDE throw 'no_artifacts' AVANT que persist atteigne la garde WR-04.
  - ⚠️ NUANCE D-12-02 : « true-empty = succès » NE veut PAS dire que persist retourne {written:0,rejected:0}. Cela veut dire que sur marché calme l'ANALYZE n'écrit AUCUN fichier ET la routine N'APPELLE PAS persist (runbook plan 02). Si persist EST appelé sur un dir vide → 'no_artifacts' (signal qu'aucun setup n'a été produit, PAS une erreur de pipeline). Le test doit prouver CETTE sémantique exacte, pas inventer un {written:0,rejected:0}.

expirePriorSetups (importé par persist depuis @app/supabase) — signature consommée :
  expirePriorSetups(client, { instrument_id, style, session, session_day }): Promise<void>
  appelée AVANT insertAnalysis/insertTradeSetups (persist.ts:319) → re-run sûr (D-45).
</interfaces>

<analog>
Analog test (à calquer) : apps/jobs/__tests__/persist.test.ts
  - mocks hoisted top-level : @supabase/supabase-js (createClient→{}), @app/supabase (getSnapshotByHash/insertAnalysis/insertTradeSetups/expirePriorSetups), ../src/jobs/runArtifacts (readRunArtifacts).
  - helper setEnv() pose SUPABASE_URL/SERVICE_ROLE_KEY/RUN_ID='london-20260614T0700Z'/MODEL_LABEL/PROMPT_VERSION.
  - fixtures réutilisables : ./__fixtures__/run-artifacts (validLongOutput, makeSnapshotRow, toRaw, INSTRUMENT_ID).
Analog static-scan : apps/jobs/__tests__/runArtifacts.test.ts (mock node:fs, readdirSync/readFileSync).
</analog>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test sémantique D-12-02 — true-empty (no_artifacts) vs all-rejected (throw)</name>
  <files>apps/jobs/__tests__/routine-persist-empty.test.ts</files>
  <read_first>
    - apps/jobs/__tests__/persist.test.ts (mocks hoisted, setEnv, withArtifact, fixtures __fixtures__/run-artifacts)
    - apps/jobs/src/jobs/persist.ts (lignes 257-374 : RUN_ID requis, boucle reject/written, garde WR-04 ligne 369)
    - apps/jobs/src/jobs/runArtifacts.ts (ligne 80 : throw 'no_artifacts' sur dir vide)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-EMPTY)
  </read_first>
  <behavior>
    - Test A (true-empty) : `readRunArtifacts` mocké pour throw `new Error('no_artifacts')` → `persist()` rejette avec message contenant `no_artifacts`. ASSERTION : ce throw provient de readRunArtifacts (0 fichier écrit = marché calme), PAS de la garde WR-04 — `insertAnalysis`/`insertTradeSetups` ne sont JAMAIS appelés (0 tentative d'écriture). Documenter en commentaire que la routine (plan 02 runbook) évite ce cas en n'appelant pas persist quand l'ANALYZE n'a rien produit.
    - Test B (all-rejected) : 1 artefact mocké dont le R:R < MIN_RR=1.2 (rejet rr_below_min) → `written=0 && rejected>0` → `persist()` throw avec message `0 setup écrit` (garde WR-04 ligne 369 INTACTE, comportement voulu, pas un bug). NE PAS affaiblir WR-04.
    - Test C (succès non-vide) : 1 artefact valide (toRaw(validLongOutput)) → `written=1, rejected=0`, pas de throw — sanity que le pipeline réussit toujours.
  </behavior>
  <action>
    Créer apps/jobs/__tests__/routine-persist-empty.test.ts en calquant les mocks hoisted de persist.test.ts (vi.mock @supabase/supabase-js, @app/supabase, ../src/jobs/runArtifacts), helper setEnv() identique (RUN_ID='newyork-20260622T1730Z'), et fixtures ./__fixtures__/run-artifacts. Pour Test A, configurer `mockReadRunArtifacts.mockImplementation(() => { throw new Error('no_artifacts') })` et asserter `await expect(persist()).rejects.toThrow('no_artifacts')` PLUS `expect(mockInsertAnalysis).not.toHaveBeenCalled()`. Pour Test B, mocker un artefact dont le take_profits produit un R:R sous 1.2 (réutiliser le pattern rrBad de persist.test.ts) et asserter `rejects.toThrow(/0 setup écrit/)`. Pour Test C, `withArtifact(toRaw(validLongOutput))` et asserter `{written:1,rejected:0}`. Commenter explicitement la nuance « no_artifacts ≠ WR-04 » et le lien runbook (plan 02). Aucune édition de persist.ts/runArtifacts.ts.
  </action>
  <verify>
    <automated>pnpm --filter jobs exec vitest run __tests__/routine-persist-empty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter jobs exec vitest run __tests__/routine-persist-empty.test.ts` passe (3 cas verts).
    - Test A prouve `mockInsertAnalysis` non appelé sur no_artifacts (source assertion).
    - Test B prouve le throw WR-04 sur all-rejected (rejected>0) — comportement préservé.
    - Aucune modification de apps/jobs/src/jobs/persist.ts ni runArtifacts.ts (git diff vide sur src/).
  </acceptance_criteria>
  <done>Les 3 cas verts ; la sémantique D-12-02 (calme≠erreur de pipeline, all-rejected=erreur voulue) est verrouillée par test sans toucher la frontière D-43.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test idempotence run-level (expirePriorSetups AVANT insert, clé session_day)</name>
  <files>apps/jobs/__tests__/routine-idempotence.test.ts</files>
  <read_first>
    - apps/jobs/__tests__/persist.test.ts (mocks + ordre des appels)
    - apps/jobs/src/jobs/persist.ts (lignes 317-357 : expirePriorSetups → insertAnalysis → insertTradeSetups, clé {instrument_id, style, session, session_day})
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Don't Hand-Roll : idempotence via expirePriorSetups D-45)
  </read_first>
  <behavior>
    - Test A (ordre) : 1 artefact valide → `mockExpirePriorSetups` est appelé AVANT `mockInsertTradeSetups` (vérifier l'ordre via `invocationCallOrder` vitest). La clé passée = `{ instrument_id, style, session, session_day }` (session_day dérivé de generated_at via sessionDayOf).
    - Test B (re-run = même clé) : appeler `persist()` deux fois avec le MÊME artefact (même generated_at) → `mockExpirePriorSetups` reçoit la MÊME `session_day` aux deux runs → un re-run efface l'ancien setup avant de réinsérer (pas de doublon au niveau contrat). Asserter que la `session_day` du 1er appel === celle du 2e.
  </behavior>
  <action>
    Créer apps/jobs/__tests__/routine-idempotence.test.ts avec les mêmes mocks hoisted que persist.test.ts. Capturer les arguments via `mockExpirePriorSetups.mock.calls`. Pour l'ordre, comparer `mockExpirePriorSetups.mock.invocationCallOrder[0] < mockInsertTradeSetups.mock.invocationCallOrder[0]`. Pour le re-run, `vi.clearAllMocks()` entre les deux `persist()` mais réutiliser le même `withArtifact(toRaw(validLongOutput))` ; extraire `session_day` de `mockExpirePriorSetups.mock.calls[0][1].session_day` aux deux runs et asserter l'égalité. Réutiliser `sessionDayOf` exporté de persist.ts uniquement pour calculer la valeur attendue, jamais la réimplémenter. Aucune édition de persist.ts.
  </action>
  <verify>
    <automated>pnpm --filter jobs exec vitest run __tests__/routine-idempotence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test A : `expirePriorSetups` invoqué strictement avant `insertTradeSetups` (assertion sur invocationCallOrder).
    - Test A : la clé d'expiration contient les 4 champs {instrument_id, style, session, session_day}.
    - Test B : la `session_day` est identique entre deux runs du même artefact (re-run sûr, D-45).
    - Aucune modification de apps/jobs/src (git diff vide sur src/).
  </acceptance_criteria>
  <done>Le contrat d'idempotence run-level est prouvé par test : expire-avant-insert + session_day stable ⇒ pas de doublon au re-run.</done>
</task>

<task type="auto">
  <name>Task 3: Static-check ROUTINE-05 — pas de MCP, pas de clé Anthropic dans apps/jobs/src</name>
  <files>apps/jobs/__tests__/routine-no-mcp.test.ts</files>
  <read_first>
    - apps/jobs/__tests__/runArtifacts.test.ts (mock node:fs — pattern de scan de fichiers)
    - apps/jobs/src/runJob.ts (commentaire « Pas de MCP » ligne ~12 — le scan doit IGNORER les commentaires, sinon faux positif)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-MCP, § Validation Architecture ROUTINE-05)
    - C:/Users/KOTEK Informatique/.claude/CLAUDE.md (règle grep-gate : filtrer les lignes de commentaire avant de compter un token)
  </read_first>
  <action>
    Créer apps/jobs/__tests__/routine-no-mcp.test.ts qui scanne RÉCURSIVEMENT le répertoire `apps/jobs/src` (via `readdirSync` réel, recursive:true, filtrer `.ts`) et lit chaque fichier. Pour CHAQUE fichier, retirer d'abord les lignes de commentaire (lignes dont le trim commence par `//`, `*`, `/*`) AVANT de chercher les tokens interdits — sinon le commentaire « Pas de MCP » de runJob.ts et « jamais MCP » génèrent des faux positifs (règle grep-gate hygiene). Asserter sur le CODE restant : (1) aucun `import ... from '@modelcontextprotocol/...'` ni `@anthropic-ai/sdk` ni `require('...mcp...')` ; (2) aucune occurrence de `ANTHROPIC_API_KEY` (ni process.env ni littéral) ; (3) sanity positive : runJob.ts et persist.ts contiennent bien `createClient` de `@supabase/supabase-js` (prouve la voie supabase-js). Le test doit chemin-résoudre `apps/jobs/src` via `path.resolve(__dirname, '../src')` (dotenv non requis ici — scan FS pur, offline).
  </action>
  <verify>
    <automated>pnpm --filter jobs exec vitest run __tests__/routine-no-mcp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le test passe : 0 import MCP, 0 import @anthropic-ai/sdk, 0 `ANTHROPIC_API_KEY` dans le CODE (commentaires exclus) de apps/jobs/src.
    - Sanity positive verte : `createClient` présent dans runJob.ts et persist.ts (voie supabase-js confirmée).
    - Le filtre-commentaires est prouvé non-trivial : un commentaire mentionnant « MCP » ne fait PAS échouer le test (la ligne `// Pas de MCP` de runJob.ts est ignorée).
    - Offline : le test ne touche ni réseau ni Supabase (skip-if-no-env non requis).
  </acceptance_criteria>
  <done>ROUTINE-05 est garanti par test statique automatisé : toute introduction future d'un MCP ou d'une clé Anthropic dans apps/jobs/src casse la CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefact ANALYZE (fichier) → persist.ts (DB) | sortie LLM non fiable franchit la frontière D-43 ; ces tests verrouillent que la frontière n'est ni affaiblie ni contournée |
| apps/jobs/src → Supabase | doit passer par supabase-js service_role uniquement, jamais MCP (ROUTINE-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | persist WR-04 guard | mitigate | Test all-rejected (Task 1B) prouve que written=0 && rejected>0 throw — un run all-rejected reste visible, jamais masqué en succès |
| T-12-02 | Elevation of Privilege | apps/jobs/src écriture DB | mitigate | Static-check (Task 3) bloque tout import MCP cloud qui contournerait persist.ts ; force supabase-js |
| T-12-03 | Repudiation | idempotence run | mitigate | Test idempotence (Task 2) prouve expire-avant-insert : un re-run ne fabrique pas de faux doublons non tracés |
| T-12-SC | Tampering | npm/pnpm installs | accept | Aucun install dans ce plan (tests purs, deps existantes vitest/dotenv) — pas de surface supply-chain nouvelle |
</threat_model>

<verification>
- `pnpm --filter jobs exec vitest run` — suite jobs complète verte (nouveaux tests inclus, aucune régression sur persist.test.ts / runArtifacts.test.ts / sessions-config.test.ts).
- `git diff --stat apps/jobs/src` — VIDE (aucun fichier source modifié ; frontière D-43 intacte).
</verification>

<success_criteria>
- 3 fichiers de test créés sous apps/jobs/__tests__/, tous verts en < 30 s.
- D-12-02 (true-empty vs all-rejected), idempotence run-level, et ROUTINE-05 static-check sont désormais couverts automatiquement.
- 12-VALIDATION.md Per-Task Verification Map peut référencer ces commandes (Dimension 8).
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-01-SUMMARY.md` when done
</output>
