---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/routines-claude.md
  - .gitignore
autonomous: true
requirements: [ROUTINE-01, ROUTINE-02, ROUTINE-05]
must_haves:
  truths:
    - "Le runbook documente que le réseau Custom + `*.supabase.co` est OBLIGATOIRE (corrige D-12-10 / §4 §7 stale) — Trusted ne couvre PAS Supabase"
    - "Le runbook documente le handoff single-run : ingest→engines→combine→ANALYZE→persist dans UN SEUL run cloud (clone frais perd run-artifacts/)"
    - "Le runbook documente la sémantique marché-calme : 0 artefact → la routine N'APPELLE PAS persist (no_artifacts ≠ erreur de pipeline)"
    - "Le runbook documente P-SECRET (SERVICE_ROLE_KEY visible aux éditeurs de l'Environment) et P-MCP (retirer tout connecteur Supabase)"
    - "Les horaires UTC du runbook sont alignés sur les crons de sessions.ts (30 12 newyork, 00 21 eod-swing)"
  artifacts:
    - path: "docs/routines-claude.md"
      provides: "Runbook go-live reproductible des routines Remote (réseau Custom, secrets, MCP off, single-run, RUN_ID, monitoring)"
      contains: "*.supabase.co"
    - path: ".gitignore"
      provides: "run-artifacts/ gitignoré (A3) — vérifié, déjà présent ligne 38"
      contains: "run-artifacts/"
  key_links:
    - from: "docs/routines-claude.md"
      to: "apps/jobs/config/sessions.ts"
      via: "crons UTC alignés (source de vérité)"
      pattern: "30 12|00 21"
    - from: "docs/routines-claude.md"
      to: "apps/jobs/src/jobs/persist.ts"
      via: "RUN_ID format + commande persist"
      pattern: "RUN_ID="
---

<objective>
Corriger et compléter `docs/routines-claude.md` pour en faire le runbook go-live reproductible de la Phase 12, et confirmer que `.gitignore` couvre `run-artifacts/`. Le runbook est la SEULE trace versionnée de la config (Environment/secrets/crons vivent hors git, dashboard Anthropic) — il doit refléter la réalité corrigée par la recherche, pas les hypothèses P1 périmées.

Purpose: La config cloud n'est pas committable ; sa reproductibilité dépend entièrement de ce doc. Trois sections sont stale (§4 « sans configuration spéciale », §7 « à confirmer A1 », §6 horaires placeholder) et doivent être corrigées AVANT que quiconque configure l'Environment (plan 03).
Output: docs/routines-claude.md mis à jour (réseau Custom obligatoire, single-run handoff, P-SECRET, P-MCP, RUN_ID, sémantique calme, crons alignés) + confirmation .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md
@docs/routines-claude.md
@apps/jobs/config/sessions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les 3 sections stale de docs/routines-claude.md (réseau, horaires, A1)</name>
  <files>docs/routines-claude.md</files>
  <read_first>
    - docs/routines-claude.md (intégralité — §2 quota, §4 MCP/réseau, §6 horaires, §7 TODO)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-NET, § State of the Art, § Architecture Pattern 2)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md (§ docs/routines-claude.md — liste exacte des lignes stale)
    - apps/jobs/config/sessions.ts (crons UTC source de vérité, lignes 27-32)
  </read_first>
  <action>
    Éditer docs/routines-claude.md. (1) §4 ligne 49 : remplacer « accessible depuis le réseau Anthropic sans configuration spéciale a priori » par la réalité corrigée — le profil Trusted par défaut N'INCLUT PAS `*.supabase.co` (liste vérifiée = api.anthropic.com, github.com, npm/pypi/crates/yarn, ubuntu) ; un appel hors allowlist échoue `403 x-deny-reason: host_not_allowed`. Documenter la procédure OBLIGATOIRE : Environment → Network access = `Custom` → ajouter `*.supabase.co` + cocher « inclure les package managers par défaut » (pour pnpm install). Fallback `Full` si Custom non fonctionnel (bug connu GitHub #30112, A2). (2) §4 ligne 50 + §7 ligne 122 : supprimer « à confirmer si nécessaire — A1 » et marquer l'étape réseau Custom comme REQUISE, gatée par un run de fumée (plan 03). (3) §6 lignes 110-113 : remplacer les horaires placeholder (22:30, 09:15, 00:15) par les crons UTC alignés sur sessions.ts pour les routines activées en rollout #1 : `newyork 30 12 * * 1-5` et `eod-swing 00 21 * * 1-5` (mentionner asia `00 23 * * 0-4` / london `00 07 * * 1-5` comme élargissement). Préciser : min interval 1h, heure saisie convertie local→UTC (saisir en UTC). Mettre à jour le bandeau de tête (« Statut : Phase 12, go-live ») et la ligne de bas de page. Préserver §3 (secrets), le diagramme §4 invariant (Routine→dispatch→runJob→supabase-js), §5 (Windows Task Scheduler = ingestion only).
  </action>
  <verify>
    <automated>grep -v '^#' docs/routines-claude.md | grep -c 'supabase.co'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/routines-claude.md` contient « Custom » + « *.supabase.co » + « host_not_allowed » (la procédure réseau corrigée).
    - Les chaînes stale « sans configuration spéciale a priori » et « à confirmer si nécessaire — A1 » sont SUPPRIMÉES (grep retourne 0).
    - §6 contient les crons `30 12` (newyork) et `00 21` (eod-swing) alignés sur sessions.ts.
    - Le diagramme invariant §4 (dispatch→runJob→supabase-js, jamais MCP) est préservé.
  </acceptance_criteria>
  <done>Les 3 sections stale sont corrigées ; le runbook reflète la réalité réseau (Custom obligatoire) et les horaires alignés sessions.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter au runbook le single-run handoff, RUN_ID, P-SECRET, P-MCP, sémantique calme</name>
  <files>docs/routines-claude.md</files>
  <read_first>
    - docs/routines-claude.md (après édition Task 1)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Architecture Pattern 1, § Code Examples, § Pitfalls P-SECRET / P-MCP / P-EMPTY)
    - apps/jobs/src/jobs/runArtifacts.ts (RUN_ID_RE format strict, throw no_artifacts sur dir vide)
    - apps/jobs/src/jobs/persist.ts (RUN_ID requis ligne 258, WR-04 ligne 369)
  </read_first>
  <action>
    Ajouter à docs/routines-claude.md une section « §8 Séquence d'un run (orchestration agent-native, single-run) » et convertir §7 en « §7 Runbook go-live (checklist) ». Documenter : (1) Single-run handoff — l'ANALYZE écrit `run-artifacts/<RUN_ID>/<instrument>_<style>.json` et `persist` les relit ; un clone frais entre runs PERD ces fichiers → toute la séquence (`pnpm install` → `tsx dispatch.ts market-ingest/news-ingest/macro-ingest` → `technical/fundamental/news-engine` → `combine-engine` → ANALYZE agent-native → `RUN_ID=<session>-<YYYYMMDD>T<HHmm>Z tsx dispatch.ts persist`) tourne dans UN SEUL run cloud, JAMAIS une routine par étape. (2) Format RUN_ID strict `<session>-<YYYYMMDD>T<HHmm>Z` (ex. `newyork-20260622T1730Z`), exporté sur la ligne de commande persist ; PROMPT_VERSION optionnel (sinon computePromptVersion()). (3) Sémantique marché-calme (D-12-02) : si l'ANALYZE ne produit AUCUN setup discipliné, il N'ÉCRIT AUCUN fichier et la routine NE LANCE PAS persist — `readRunArtifacts` throw `no_artifacts` sur dir vide, donc appeler persist sur rien = bruit ; un vrai-vide est un succès normal, pas une anomalie. Préciser que all-rejected (rejected>0) throw légitimement (WR-04). (4) P-MCP : retirer tout connecteur Supabase MCP de la routine (ROUTINE-05 ; écriture seulement via supabase-js service_role). (5) P-SECRET : `SUPABASE_SERVICE_ROLE_KEY` (bypass RLS, full DB) est stockée en clair dans l'Environment, visible par TOUT éditeur de l'Environment (pas de secrets store) → restreindre strictement qui peut éditer l'Environment ; ne jamais committer ; jamais echo dans les logs. Réécrire la checklist §7 avec la sous-étape « Network = Custom + *.supabase.co » marquée REQUISE et un item « run de fumée egress (gate ROUTINE-01) avant de planifier les fenêtres ».
  </action>
  <verify>
    <automated>grep -v '^#' docs/routines-claude.md | grep -cE 'RUN_ID|single-run|host_not_allowed|SERVICE_ROLE'</automated>
  </verify>
  <acceptance_criteria>
    - §8 présente la séquence single-run complète (ingest→engines→combine→ANALYZE→persist) et précise « UN SEUL run cloud ».
    - Le format `RUN_ID=<session>-<YYYYMMDD>T<HHmm>Z` est documenté avec un exemple concret.
    - La sémantique marché-calme (0 artefact → pas d'appel persist ; no_artifacts ≠ erreur de pipeline) est écrite explicitement.
    - P-SECRET (SERVICE_ROLE_KEY visible aux éditeurs) et P-MCP (retirer connecteur Supabase) sont documentés.
    - La checklist §7 marque l'étape réseau Custom REQUISE et liste le run de fumée comme gate ROUTINE-01.
  </acceptance_criteria>
  <done>Le runbook contient la procédure go-live complète et sûre : single-run, RUN_ID, sémantique calme, secrets restreints, MCP off.</done>
</task>

<task type="auto">
  <name>Task 3: Confirmer .gitignore couvre run-artifacts/ (A3 — vérification, déjà présent)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (lignes 37-38 : entrée run-artifacts/ déjà présente)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md (§ .gitignore — A3 CLOSED, no edit needed)
  </read_first>
  <action>
    Vérifier que `.gitignore` contient déjà `run-artifacts/` (ligne 38, sous le commentaire « Artefacts éphémères de l'ANALYZE »). A3 est CLOSED — aucune édition nécessaire si l'entrée est présente. Si et seulement si l'entrée est ABSENTE (régression), l'ajouter sous une ligne de commentaire dédiée. Ne pas réordonner ni reformater le reste du fichier (surgical). Confirmer aussi qu'aucun fichier sous run-artifacts/ n'est tracké par git.
  </action>
  <verify>
    <automated>grep -c '^run-artifacts/$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^run-artifacts/$' .gitignore` retourne 1 (entrée présente, exacte).
    - `git ls-files run-artifacts/` retourne 0 ligne (aucun artefact tracké) — un clone frais cloud ne portera pas d'artefact committé.
    - Aucune autre ligne de .gitignore modifiée (git diff minimal ou vide).
  </acceptance_criteria>
  <done>A3 confirmé fermé : run-artifacts/ gitignoré, aucun artefact tracké, handoff éphémère intra-run garanti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| éditeur de l'Environment → SERVICE_ROLE_KEY | la clé bypass-RLS est visible en clair aux éditeurs (pas de secrets store) — le runbook doit imposer la restriction d'accès |
| réseau Anthropic Remote → *.supabase.co | egress proxifié ; sans allowlist Custom, blocage 403 (P-NET) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | SERVICE_ROLE_KEY dans l'Environment | mitigate | Runbook §7 P-SECRET : restreindre l'édition de l'Environment, jamais committer, jamais logger ; rotation possible au lancement payant |
| T-12-05 | Elevation of Privilege | connecteur MCP Supabase cloud | mitigate | Runbook §8 P-MCP : retirer tout connecteur Supabase ; écriture uniquement via persist.ts service_role (ROUTINE-05) |
| T-12-06 | Denial of Service | egress non autorisé (403) | mitigate | Runbook §4 : Network Custom minimal `*.supabase.co` + defaults (moindre privilège), `Full` seulement en fallback documenté |
| T-12-SC | Tampering | npm/pnpm installs | accept | Doc-only ; aucun install dans ce plan |
</threat_model>

<verification>
- `grep -v '^#' docs/routines-claude.md | grep -c 'supabase.co'` ≥ 1 et chaînes stale absentes (0).
- `grep -c '^run-artifacts/$' .gitignore` = 1 ; `git ls-files run-artifacts/` vide.
- Lecture humaine rapide : le runbook se lit comme une checklist go-live exécutable de bout en bout.
</verification>

<success_criteria>
- docs/routines-claude.md corrigé (réseau Custom obligatoire) et complété (single-run, RUN_ID, P-SECRET, P-MCP, sémantique calme, crons alignés).
- .gitignore confirmé (A3 closed).
- Le doc seul suffit à reproduire la config cloud hors git.
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-02-SUMMARY.md` when done
</output>
