---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 06
type: execute
wave: 4
depends_on: ["12-05"]
files_modified: []
autonomous: false
requirements: [ROUTINE-02, ROUTINE-04]
user_setup:
  - service: claude-code-routines
    why: "Création des routines Remote asia + london (élargissement) via /schedule + dashboard"
    dashboard_config:
      - task: "Créer les routines Remote `asia` (00 23 * * 0-4 UTC) et `london` (00 07 * * 1-5 UTC), liées à l'Environment Custom"
        location: "/schedule (CLI) + claude.ai/code/routines"
must_haves:
  truths:
    - "Les 4 fenêtres sont couvertes : asia + london ajoutées APRÈS validation de newyork + eod-swing (D-12-01)"
    - "Les routines asia/london réutilisent l'Environment Custom validé (réseau + secrets) — pas de nouvelle config réseau"
    - "Chaque nouvelle routine tourne au moins une fois et trace job_runs ; le total reste sous le budget quota (~15/j)"
  artifacts:
    - path: "docs/routines-claude.md"
      provides: "Liste finale des 4 routines actives (asia/london/newyork/eod-swing) + crons UTC"
      contains: "london"
  key_links:
    - from: "routine london (cron 00 07)"
      to: "trade_setups"
      via: "dispatch persist → frontière D-43"
      pattern: "session='london'"
    - from: "routine asia (cron 00 23)"
      to: "job_runs"
      via: "runJob monitoring"
      pattern: "asia"
---

<objective>
Élargir le rollout aux 2 fenêtres restantes (asia + london) APRÈS que newyork + eod-swing ont prouvé la chaîne complète (D-12-01). Réutilise l'Environment Custom déjà validé (plan 03) — aucune nouvelle config réseau. Complète la couverture ROUTINE-02 (4 fenêtres) tout en restant sous le budget quota.

Purpose: D-12-01 impose un rollout staged : valider le risque (réseau + frontière + ≥1 setup) sur 2 routines avant de tout brancher. Ce plan n'est exécuté QUE si le plan 05 est vert. asia/london sont les fenêtres « sacrifiables en premier » (D-12-03) — ajoutées en dernier.
Output: 4 routines Remote actives, chacune ayant tracé ≥1 run dans job_runs, sous budget quota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@docs/routines-claude.md
@apps/jobs/config/sessions.ts
@apps/web/src/app/(admin)/sante/page.tsx

<interfaces>
<!-- Crons UTC (sessions.ts source de vérité) pour l'élargissement. -->
  asia    00 23 * * 0-4   (day)    — forex/metal/crypto (energy absente, D-49)
  london  00 07 * * 1-5   (day, swing) — forex/metal/energy/crypto
Même séquence single-run et même Environment Custom que newyork/eod-swing (plan 05).
RUN_ID format : <session>-<YYYYMMDD>T<HHmm>Z (asia-…T2300Z, london-…T0700Z).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Créer les routines Remote asia + london (réutiliser l'Environment validé)</name>
  <files>Config cloud (hors git) : routines Remote `asia` + `london` via /schedule + dashboard ; edit docs/routines-claude.md (liste finale 4 routines).</files>
  <read_first>
    - apps/jobs/config/sessions.ts (crons asia 00 23 * * 0-4, london 00 07 * * 1-5 ; univers asia sans energy)
    - docs/routines-claude.md (§7 checklist, §8 séquence single-run, liste des routines actives — plan 02/05)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-CONTEXT.md (D-12-01 staged, D-12-03 priorité)
  </read_first>
  <what-built>
    Deux routines Remote supplémentaires (`asia`, `london`) créées via `/schedule` + liées à l'Environment Custom EXISTANT (plan 03). Même prompt d'orchestration single-run que newyork/eod-swing (plan 05). Aucune nouvelle config réseau/secret (réutilisation).
  </what-built>
  <how-to-verify>
    1. PRÉREQUIS : confirmer que le plan 05 est vert (newyork a produit ≥1 setup réel, idempotence + monitoring OK). Sinon, NE PAS exécuter ce plan (D-12-01).
    2. Via `/schedule`, créer `asia` au cron `00 23 * * 0-4` (UTC), RUN_ID `asia-<YYYYMMDD>T2300Z`, prompt single-run, Environment Custom existant.
    3. Idem `london` au cron `00 07 * * 1-5` (UTC), RUN_ID `london-<YYYYMMDD>T0700Z`, swing+day couverts par l'univers.
    4. Confirmer : 2 routines Remote, liées au bon Environment, crons UTC ⊆ sessions.ts, aucun nouveau secret/réseau saisi.
    5. Mettre à jour docs/routines-claude.md : liste finale des 4 routines actives (nom, cron UTC, RUN_ID pattern).
  </how-to-verify>
  <action>
    PRÉREQUIS : ne procéder QUE si le plan 05 est vert (D-12-01). Via `/schedule`, créer la routine Remote `asia` (cron `00 23 * * 0-4` UTC, RUN_ID pattern `asia-<YYYYMMDD>T2300Z`) et `london` (cron `00 07 * * 1-5` UTC, RUN_ID pattern `london-<YYYYMMDD>T0700Z`), liées à l'Environment Custom `nexa-jobs` EXISTANT. Réutiliser le MÊME prompt single-run que newyork/eod-swing ; NE saisir AUCUN nouveau secret ni configurer de réseau (héritage de l'Environment validé plan 03). Éditer docs/routines-claude.md avec la liste finale des 4 routines actives (nom, cron UTC, RUN_ID pattern).
  </action>
  <verify>Dashboard : 4 routines Remote au total ; `asia` (cron `00 23 * * 0-4`) et `london` (cron `00 07 * * 1-5`) liées à `nexa-jobs` ; aucun nouveau secret/réseau ; docs/routines-claude.md liste les 4.</verify>
  <done>asia + london créées sur l'Environment validé (réutilisation), crons ⊆ sessions.ts, couverture 4 fenêtres documentée.</done>
  <acceptance_criteria>
    - Le plan 05 est confirmé vert AVANT toute action (gate D-12-01).
    - 2 routines Remote `asia` (cron `00 23 * * 0-4`) et `london` (cron `00 07 * * 1-5`) existent, liées à l'Environment Custom existant.
    - Aucune nouvelle config réseau/secret (réutilisation de l'Environment validé plan 03).
    - docs/routines-claude.md liste les 4 routines actives.
  </acceptance_criteria>
  <resume-signal>Taper « expansion-created » quand asia + london existent en Remote sur l'Environment validé.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Premier run de chaque nouvelle routine + monitoring 4 fenêtres sous quota</name>
  <files>Runs cloud one-off (hors git) asia + london ; lecture /admin/sante ; aucune édition de fichier repo.</files>
  <read_first>
    - apps/web/src/app/(admin)/sante/page.tsx (loadHealth : job_runs + v_data_freshness)
    - apps/jobs/src/jobs/persist.ts (idempotence session_day, WR-04)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-QUOTA, P-EMPTY)
  </read_first>
  <what-built>
    Un run one-off de `asia` et de `london` pour confirmer que les 2 nouvelles routines exécutent la chaîne complète et tracent job_runs, et que les 4 fenêtres ensemble restent sous le budget quota.
  </what-built>
  <how-to-verify>
    1. Déclencher un run one-off de `asia`, puis de `london` (one-off ne compte pas dans le daily cap).
    2. Pour chaque : `job_runs` trace le run (status success ou success-vide D-12-02 si marché calme ; un all-rejected throw=error est un signal légitime, pas un échec de config). Vérifier l'absence d'erreur d'egress (403/host_not_allowed) — l'Environment est déjà validé, donc aucun 403 attendu.
    3. Si un run produit des setups : `select count(*) from trade_setups where session in ('asia','london') and session_day=<jour>` reflète l'écriture ; idempotence préservée (re-run même clé → pas de doublon).
    4. `/admin/sante` : les 4 sessions apparaissent dans l'historique job_runs au fil des runs ; les feux de fraîcheur restent cohérents.
    5. Quota : confirmer sur `claude.ai/code/routines` / `claude.ai/settings/usage` que 4 routines + interactif restent sous ~15/j (pression faible attendue, D-12-03). Si tension : appliquer la dégradation D-12-03 (désactiver asia/london d'abord).
    6. Consigner dans le SUMMARY : runs asia/london (statut), setups éventuels, conso quota, état /admin/sante.
  </how-to-verify>
  <action>
    Déclencher un run one-off de `asia` puis de `london`. Confirmer que chacun trace une ligne `job_runs` sans erreur d'egress (Environment réutilisé → aucun 403/host_not_allowed attendu). Un run vide (marché calme) = success normal (D-12-02) ; un all-rejected (error) = signal légitime, pas un échec de config. Si setups produits, vérifier `select count(*) from trade_setups where session in ('asia','london') and session_day=<jour>` et l'absence de doublon au re-run. Ouvrir `/admin/sante` : confirmer que les 4 sessions apparaissent dans l'historique job_runs et que les feux de fraîcheur restent cohérents. Vérifier la conso quota < ~15/j ; documenter la dégradation D-12-03 comme disponible (asia/london sacrifiables en premier). Aucune édition de fichier repo.
  </action>
  <verify>`select distinct job_name from job_runs` couvre les runs asia + london sans erreur d'egress ; `/admin/sante` reflète les 4 sessions ; usage < ~15/j.</verify>
  <done>asia + london ont tourné (job_runs tracé, 0 erreur d'egress) ; /admin/sante reflète les 4 fenêtres ; conso sous budget quota.</done>
  <acceptance_criteria>
    - `asia` et `london` ont chacune tracé ≥1 run dans job_runs sans erreur d'egress (Environment réutilisé, pas de 403).
    - Un run vide (marché calme) est un success normal (D-12-02) ; un all-rejected (error) est un signal légitime, pas un échec de configuration.
    - `/admin/sante` reflète l'historique des 4 sessions ; feux de fraîcheur cohérents.
    - La conso quota reste sous ~15/j ; la règle de dégradation D-12-03 est documentée comme disponible.
  </acceptance_criteria>
  <resume-signal>Taper « expansion-ok » quand asia + london ont tourné (job_runs tracé, pas de 403) et le quota reste sous budget.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nouvelles routines → Environment Custom | réutilisation de l'allowlist + secrets déjà validés ; aucune nouvelle surface réseau |
| run cloud → trade_setups | écriture seulement via persist service_role (jamais MCP), inchangé |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Denial of Service | quota partagé (4 routines + interactif) | mitigate | 4 fenêtres ≪ ~15/j ; monitoring quota Task 2 ; dégradation D-12-03 (asia/london sacrifiables en premier) |
| T-12-17 | Tampering | frontière D-43 (nouvelles sessions) | mitigate | Même persist.ts service_role ; aucune écriture hors frontière ; idempotence session_day préservée |
| T-12-18 | Information Disclosure | secrets réutilisés | accept | Aucun nouveau secret saisi (Environment réutilisé) ; restriction d'accès héritée du plan 03 |
| T-12-SC | Tampering | pnpm install dans le run | accept | install verrouillé pnpm-lock ; aucun nouveau package |
</threat_model>

<verification>
- `job_runs` contient des runs asia et london sans erreur d'egress.
- `/admin/sante` reflète les 4 sessions.
- Conso quota < ~15/j.
</verification>

<success_criteria>
- ROUTINE-02 complète : 4 fenêtres (asia/london/newyork/eod-swing) couvertes, alignées sessions.ts.
- ROUTINE-04 : les 4 routines tracées dans job_runs, stale visible, sous budget quota.
- Rollout staged D-12-01 respecté (élargissement APRÈS validation du minimal).
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-06-SUMMARY.md` when done
</output>
