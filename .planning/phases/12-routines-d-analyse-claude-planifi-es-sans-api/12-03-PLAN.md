---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified: []
autonomous: false
requirements: [ROUTINE-01, ROUTINE-05]
user_setup:
  - service: claude-code-routines
    why: "Environment cloud + secrets + network allowlist sont dashboard-only (hors git)"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role secret (bypass RLS — accès restreint)"
    dashboard_config:
      - task: "Créer un Environment Custom, Network access = Custom + ajouter `*.supabase.co` + inclure les package managers par défaut"
        location: "claude.ai/code/routines → Environments"
      - task: "Retirer tout connecteur MCP Supabase de l'Environment/routine (ROUTINE-05)"
        location: "claude.ai/code/routines → Connectors"
must_haves:
  truths:
    - "Un Environment Custom existe avec SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY injectés"
    - "Le réseau Custom autorise `*.supabase.co` (+ defaults package managers)"
    - "Un run de fumée depuis le runtime Remote lit Supabase SANS 403 host_not_allowed (egress confirmé)"
    - "Aucun connecteur MCP Supabase n'est attaché ; aucune clé Anthropic n'est utilisée"
  artifacts:
    - path: "docs/routines-claude.md"
      provides: "Trace de la config Environment effective (valeurs non secrètes) + résultat du run de fumée"
      contains: "smoke"
  key_links:
    - from: "Cloud Environment (dashboard)"
      to: "*.supabase.co"
      via: "Network Custom allowlist"
      pattern: "host_not_allowed (absence dans job_runs.error)"
---

<objective>
Configurer l'Environment Claude Code Remote (secrets + réseau Custom `*.supabase.co`) et PROUVER l'egress par un run de fumée — le gate #1 de toute la phase (ROUTINE-01). Sans cette confirmation, les routines planifiées échoueraient toutes au 1er appel supabase-js (`403 host_not_allowed`).

Purpose: La recherche a INVALIDÉ D-12-10 (Trusted ne couvre PAS Supabase) — le réseau Custom est OBLIGATOIRE, pas optionnel. Ce plan transforme l'hypothèse en fait vérifié avant d'investir dans le scheduling (plans 05/06).
Output: Environment Custom opérationnel + run de fumée vert (lecture Supabase sans 403) + résultat consigné dans docs/routines-claude.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-PATTERNS.md
@docs/routines-claude.md
@apps/jobs/src/jobs/heartbeat.ts
@apps/jobs/src/dispatch.ts
@apps/jobs/src/runJob.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Créer l'Environment Custom (secrets + réseau *.supabase.co) — dashboard-only</name>
  <files>Config cloud (hors git) : Cloud Environment `nexa-jobs` au dashboard claude.ai/code/routines — aucun fichier repo.</files>
  <read_first>
    - docs/routines-claude.md (§3 secrets, §4 réseau Custom corrigé en plan 02, §7 checklist go-live)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Code Examples création routine, § Pitfall P-NET, P-SECRET, P-MCP)
  </read_first>
  <what-built>
    Rien de codé — étape de configuration cloud (hors git). L'agent ne peut pas créer l'Environment ni saisir des secrets via CLI : c'est dashboard-only (D-12-09).
  </what-built>
  <how-to-verify>
    1. Ouvrir `claude.ai/code/routines` → Environments → créer un Environment (ex. nom `nexa-jobs`).
    2. Network access = `Custom`. Ajouter l'entrée `*.supabase.co`. Cocher « inclure les package managers par défaut » (pour `pnpm install`).
    3. Environment variables : ajouter `SUPABASE_URL` (Supabase → Settings → API → Project URL) et `SUPABASE_SERVICE_ROLE_KEY` (Supabase → Settings → API → service_role secret). NE PAS committer ces valeurs ; NE PAS les coller dans le SUMMARY.
    4. Connectors : vérifier qu'AUCUN connecteur MCP Supabase n'est attaché (le retirer s'il existe). ROUTINE-05.
    5. Restreindre qui peut éditer l'Environment (P-SECRET : la SERVICE_ROLE_KEY y est visible en clair).
  </how-to-verify>
  <action>
    Au dashboard claude.ai/code/routines : créer un Cloud Environment nommé `nexa-jobs` ; régler Network access = `Custom` ; ajouter l'entrée d'allowlist `*.supabase.co` + cocher « inclure les package managers par défaut » ; ajouter les variables d'environnement `SUPABASE_URL` et `SUPABASE_SERVICE_ROLE_KEY` (valeurs depuis Supabase → Settings → API, jamais committées ni recopiées dans docs/SUMMARY/logs) ; retirer tout connecteur MCP Supabase (ROUTINE-05) ; restreindre l'accès en édition de l'Environment (P-SECRET). Étape dashboard-only (D-12-09) — aucun fichier repo touché.
  </action>
  <verify>Dashboard : Environment Custom `nexa-jobs` existe, allowlist contient `*.supabase.co`, 2 env vars définies (SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY), 0 connecteur MCP Supabase.</verify>
  <done>Environment Custom opérationnel avec secrets + allowlist `*.supabase.co`, sans connecteur MCP, accès édition restreint.</done>
  <resume-signal>Taper « environment-ready » quand l'Environment Custom existe avec les 2 secrets, `*.supabase.co` dans l'allowlist, et aucun connecteur MCP Supabase.</resume-signal>
  <acceptance_criteria>
    - Un Environment Custom existe ; Network access = Custom ; allowlist contient `*.supabase.co` + defaults.
    - `SUPABASE_URL` et `SUPABASE_SERVICE_ROLE_KEY` sont définis comme variables de l'Environment (valeurs jamais committées ni recopiées dans les docs/SUMMARY).
    - Aucun connecteur MCP Supabase attaché.
    - L'accès en édition de l'Environment est restreint (P-SECRET).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run de fumée egress — confirmer la lecture Supabase sans 403 (gate ROUTINE-01)</name>
  <files>Run cloud one-off (hors git) + edit docs/routines-claude.md (section « Résultat run de fumée »).</files>
  <read_first>
    - apps/jobs/src/jobs/heartbeat.ts (job le plus léger touchant Supabase — candidat run de fumée)
    - apps/jobs/src/runJob.ts (getServiceClient → 1er appel HTTPS *.supabase.co = point de 403 si allowlist absente)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Open Questions Q2, § Pitfall P-NET warning signs)
  </read_first>
  <what-built>
    Un run cloud one-off (NE compte PAS dans le daily cap) qui exécute un job trivial touchant Supabase via supabase-js service_role, pour prouver l'egress `*.supabase.co` depuis le runtime Remote. Le job `heartbeat` (le plus léger écrivant job_runs) sert de sonde.
  </what-built>
  <how-to-verify>
    1. Depuis l'Environment `nexa-jobs`, lancer un run one-off : `pnpm install` puis `pnpm --filter jobs exec tsx src/dispatch.ts heartbeat`.
    2. Observer la sortie : le job doit écrire une ligne `job_runs` (status `running` → `success`) — donc l'appel `getServiceClient()` a atteint `*.supabase.co`.
    3. GATE : vérifier l'absence de `403`, `host_not_allowed`, `ENOTFOUND`, `EAI_AGAIN`, `fetch failed` dans les logs du run ET dans `job_runs.error`. Confirmer via Supabase (`select * from job_runs order by started_at desc limit 1` → job_name='heartbeat', status='success').
    4. Si 403 host_not_allowed : l'allowlist Custom est non fonctionnelle sur Remote (bug A2 / GitHub #30112) → fallback documenté : passer Network access = `Full`, relancer le heartbeat, reconfirmer success. Consigner le fallback utilisé.
    5. Mettre à jour docs/routines-claude.md : section « Résultat run de fumée » = date + mode réseau effectif (Custom ou Full fallback) + statut egress confirmé (sans copier les secrets).
  </how-to-verify>
  <action>
    Déclencher un run cloud one-off sur l'Environment `nexa-jobs` exécutant `pnpm install` puis `pnpm --filter jobs exec tsx src/dispatch.ts heartbeat`. Vérifier que la dernière ligne `job_runs` (job_name='heartbeat') passe à `status='success'` et que les logs + `job_runs.error` ne contiennent AUCUNE chaîne `403`/`host_not_allowed`/`ENOTFOUND`/`EAI_AGAIN`/`fetch failed`. En cas de 403 (bug A2), basculer Network access = `Full`, relancer, reconfirmer success, et consigner le fallback. Éditer docs/routines-claude.md avec la section « Résultat run de fumée » (date + mode réseau effectif + egress confirmé), sans recopier de secret.
  </action>
  <verify>`select status, error from job_runs where job_name='heartbeat' order by started_at desc limit 1` → status='success', error null ; aucun `403`/`host_not_allowed` dans les logs.</verify>
  <done>Egress `*.supabase.co` confirmé depuis Remote (heartbeat success, 0 erreur réseau) ; mode réseau effectif consigné dans docs/routines-claude.md.</done>
  <resume-signal>Taper « smoke-green » quand le heartbeat Remote renvoie job_runs.status='success' sans aucune erreur d'egress (403/host_not_allowed/fetch failed).</resume-signal>
  <acceptance_criteria>
    - Le dernier `job_runs` (job_name='heartbeat') a `status='success'` après le run Remote.
    - Aucune chaîne `403` / `host_not_allowed` / `fetch failed` dans les logs du run ni `job_runs.error`.
    - Le mode réseau effectif (Custom, ou Full en fallback A2) est consigné dans docs/routines-claude.md avec la date.
    - Aucun secret n'apparaît dans les logs, le SUMMARY, ou le doc.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime Remote → *.supabase.co | egress proxifié ; allowlist Custom requise (P-NET) |
| Environment config → SERVICE_ROLE_KEY | clé bypass-RLS visible aux éditeurs (P-SECRET) |
| routine → connecteurs MCP | un MCP Supabase cloud contournerait persist.ts (P-MCP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | SERVICE_ROLE_KEY (Environment) | mitigate | Accès édition Environment restreint ; secrets jamais committés ni recopiés dans SUMMARY/logs (Task 1 §5, Task 2 §5) |
| T-12-08 | Denial of Service | egress bloqué 403 | mitigate | Network Custom `*.supabase.co` + run de fumée gate (Task 2) ; fallback Full documenté si A2 |
| T-12-09 | Elevation of Privilege | connecteur MCP Supabase | mitigate | Task 1 §4 retire tout connecteur Supabase ; écriture seulement via persist.ts service_role (ROUTINE-05) |
| T-12-SC | Tampering | pnpm install dans le run cloud | accept | `pnpm install` tire les deps verrouillées du repo (pnpm-lock) ; pas de nouveau package introduit par ce plan |
</threat_model>

<verification>
- `job_runs` : dernière ligne heartbeat = status 'success', error null (egress confirmé).
- Aucune occurrence d'erreur d'egress dans les logs du run de fumée.
- docs/routines-claude.md : « Résultat run de fumée » renseigné (date + mode réseau).
</verification>

<success_criteria>
- ROUTINE-01 prouvé : Environment Custom + secrets + `*.supabase.co` confirmé par un run de fumée vert depuis Remote.
- ROUTINE-05 préservé : aucun MCP, aucune clé Anthropic.
- Gate franchi : on peut planifier les routines (plan 05) sur une base réseau vérifiée.
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-03-SUMMARY.md` when done
</output>
