---
phase: 12-routines-d-analyse-claude-planifi-es-sans-api
plan: 04
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - apps/jobs/prompts/veteran.md
autonomous: false
requirements: [ROUTINE-03]
must_haves:
  truths:
    - "Le prompt vétéran (intelligence produit) a été revu par le fondateur avant que de vrais signaux partent aux membres (D-12-08)"
    - "Tout ajustement du prompt bumpe prompt_version (semver front-matter), jamais une refonte structurelle"
    - "Si le prompt reste inchangé, version=1.0.0 préservée ; la garde anti-injection <market_data> reste intacte"
  artifacts:
    - path: "apps/jobs/prompts/veteran.md"
      provides: "Prompt agent-native revu fondateur, versionné (semver+sha256, D-51)"
      contains: "version:"
  key_links:
    - from: "apps/jobs/prompts/veteran.md"
      to: "apps/jobs/src/jobs/persist.ts"
      via: "computePromptVersion (sha256 du fichier + semver front-matter)"
      pattern: "version:\\s*\\d+\\.\\d+\\.\\d+"
---

<objective>
Faire passer le prompt `apps/jobs/prompts/veteran.md` par la revue fondateur (Borhane) AVANT go-live (D-12-08) — c'est l'intelligence produit qui génère les signaux. Tout ajustement issu de la revue est appliqué et bumpe `prompt_version` (semver). Aucune refonte structurelle ; le mécanisme de versionnage est inchangé.

Purpose: Le code recalcule et écrase le score de l'agent (P10), mais la QUALITÉ du raisonnement vient du prompt. Le fondateur doit valider ce raisonnement avant que de vrais setups soient publiés aux membres. C'est un garde-fou qualité, pas un refactor.
Output: veteran.md revu (version préservée à 1.0.0 si inchangé, ou bumpée si ajusté), garde anti-injection intacte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-CONTEXT.md
@.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md
@apps/jobs/prompts/veteran.md
@apps/jobs/src/jobs/persist.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Revue fondateur du prompt vétéran (D-12-08) — valider ou ajuster</name>
  <files>apps/jobs/prompts/veteran.md (modifié uniquement si décision = adjust-and-bump)</files>
  <read_first>
    - apps/jobs/prompts/veteran.md (intégralité : mission, garde anti-injection <market_data>, schéma §3, discipline confluence)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-CONTEXT.md (D-12-08)
    - .planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-RESEARCH.md (§ Pitfall P-HALLUC : le code écrase le score agent ; la revue ne touche QUE le raisonnement)
    - apps/jobs/src/jobs/persist.ts (computePromptVersion : sha256 + semver front-matter — comment le bump fonctionne)
  </read_first>
  <decision>Le prompt vétéran v1.0.0 est-il prêt pour go-live tel quel, ou nécessite-t-il des ajustements de raisonnement (confluences, discipline, formulation) avant que de vrais signaux partent aux membres ?</decision>
  <context>
    Le prompt est l'intelligence produit. persist.ts ignore/écrase le score de l'agent (déterministe, D-43) — la revue NE concerne PAS le scoring (déjà verrouillé code) mais la QUALITÉ du raisonnement : empilement de confluences, discipline (rejeter les setups médiocres), clarté du schéma §3, robustesse de la garde anti-injection `<market_data>`. Tout changement bumpe simplement `prompt_version` (semver front-matter + sha256 recalculé automatiquement). Pas de refonte structurelle.
  </context>
  <options>
    <option id="approve-as-is">
      <name>Approuver v1.0.0 tel quel</name>
      <pros>Go-live immédiat ; le prompt a déjà la garde anti-injection et la discipline confluence ; version 1.0.0 préservée</pros>
      <cons>Premiers signaux réels sur un prompt non re-challengé par le fondateur</cons>
    </option>
    <option id="adjust-and-bump">
      <name>Ajuster le raisonnement et bumper la version</name>
      <pros>Le fondateur affine confluences/discipline avant publication ; traçabilité via prompt_version</pros>
      <cons>Itération supplémentaire ; bump version (1.0.0 → 1.x.0) à propager</cons>
    </option>
  </options>
  <action>
    Présenter le contenu de veteran.md au fondateur pour revue. Si décision = `approve-as-is` : ne rien modifier (version 1.0.0 inchangée, sha256 inchangé) — consigner « revue fondateur OK, v1.0.0 figée » dans le SUMMARY. Si décision = `adjust-and-bump` : appliquer UNIQUEMENT les ajustements de raisonnement demandés par le fondateur (mission/confluences/discipline/clarté §3), PRÉSERVER la garde anti-injection `<market_data>` et le schéma §3 (structure), puis bumper le champ `version:` du front-matter selon la nature du changement (patch pour reformulation, minor pour ajout de critère). NE PAS modifier le mécanisme de versionnage ni refondre la structure. Le sha256 est recalculé automatiquement par computePromptVersion() à la lecture — aucune action manuelle de hash.
  </action>
  <verify>
    <automated>grep -E '^version:\s*[0-9]+\.[0-9]+\.[0-9]+' apps/jobs/prompts/veteran.md && pnpm --filter jobs exec vitest run __tests__/persist.test.ts</automated>
  </verify>
  <done>Revue fondateur effectuée ; version semver valide (1.0.0 si approuvé tel quel, bumpée si ajusté) ; garde `<market_data>` intacte ; persist.test.ts vert.</done>
  <acceptance_criteria>
    - Le front-matter `version:` est un semver valide (1.0.0 si approuvé tel quel, ou bumpé si ajusté).
    - La garde anti-injection `<market_data>` est présente et intacte (grep `market_data` ≥ 1).
    - Le schéma §3 (structure de sortie) n'a pas été restructuré (seul le raisonnement/formulation peut changer).
    - Si ajusté : le bump de version reflète la nature du changement ; aucun changement du mécanisme de versionnage.
    - `pnpm --filter jobs exec vitest run __tests__/persist.test.ts` reste vert (computePromptVersion lit le fichier sans erreur).
  </acceptance_criteria>
  <resume-signal>Sélectionner : approve-as-is ou adjust-and-bump (avec la liste des ajustements demandés).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| données news tierces → prompt | headlines non contrôlées entrent via `<market_data>` ; la garde anti-injection les traite comme DONNÉE jamais instruction |
| raisonnement agent → persist.ts | le score/levels agent est recalculé/écrasé en code (le prompt ne peut pas franchir la frontière D-43) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | prompt injection via <market_data> | mitigate | La revue PRÉSERVE la garde `<market_data>` (donnée ≠ instruction) ; acceptance_criteria vérifie sa présence |
| T-12-11 | Spoofing | score halluciné par l'agent | mitigate | Déjà mitigé hors prompt : scoreSetup recalcule (D-43) ; la revue ne touche pas au scoring |
| T-12-SC | Tampering | — | accept | Aucun install ; édition de texte de prompt uniquement |
</threat_model>

<verification>
- `grep -E '^version:' apps/jobs/prompts/veteran.md` = semver valide.
- `grep -c 'market_data' apps/jobs/prompts/veteran.md` ≥ 1 (garde intacte).
- `pnpm --filter jobs exec vitest run __tests__/persist.test.ts` vert (versionnage lisible).
</verification>

<success_criteria>
- D-12-08 satisfait : revue fondateur effectuée avant go-live.
- Prompt figé (1.0.0) ou ajusté+bumpé, garde anti-injection préservée, mécanisme de versionnage inchangé.
</success_criteria>

<output>
Create `.planning/phases/12-routines-d-analyse-claude-planifi-es-sans-api/12-04-SUMMARY.md` when done
</output>
