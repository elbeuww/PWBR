---
phase: 08-superadmin-consolid-signaux-sant-affili-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/admin/signals.ts
  - apps/web/src/lib/admin/signals.test.ts
  - apps/web/src/lib/admin/freshness.ts
  - apps/web/src/lib/admin/freshness.test.ts
  - apps/web/src/lib/admin/jobs.ts
  - apps/web/src/lib/admin/jobs.test.ts
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/app/(admin)/_components/AdminSidebar.tsx
  - apps/web/src/messages/fr.json
autonomous: true
requirements: [ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "Pure function maps a set of telegram_posts dedupe_keys + a setup id to 'posted' | 'unpublished' (2-state, never 'failed')"
    - "Pure function maps candle is_stale / news age / macro age to green | amber | red with the founder-approved thresholds"
    - "Pure function reduces a flat job_runs list to the latest run per job_name with a computed duration"
    - "Every (admin) route renders inside a persistent sidebar shell listing the 7 nav items in D-01 order"
    - "Sidebar highlights the active route by prefix-match using usePathname()"
    - "All Phase 8 admin.* FR copy keys exist in one place so Wave 2 view plans never touch fr.json (zero file contention)"
    - "Non-superadmin still gets notFound() (404) — the existing layout gate is preserved, not weakened"
  artifacts:
    - path: "apps/web/src/lib/admin/signals.ts"
      provides: "telegramStatus pure mapper"
      exports: ["postedSetupIdSet", "telegramStatusFor"]
    - path: "apps/web/src/lib/admin/freshness.ts"
      provides: "freshness traffic-light pure mapper"
      exports: ["candleColor", "ageColor", "NEWS_THRESHOLDS", "MACRO_THRESHOLDS"]
    - path: "apps/web/src/lib/admin/jobs.ts"
      provides: "last-run-per-job reducer + duration"
      exports: ["latestPerJob", "runDurationMs"]
    - path: "apps/web/src/app/(admin)/_components/AdminSidebar.tsx"
      provides: "client sidebar nav with active-route detection"
    - path: "apps/web/src/app/(admin)/layout.tsx"
      provides: "two-column admin shell mounting the sidebar"
    - path: "apps/web/src/messages/fr.json"
      provides: "all Phase 8 admin.* copy (nav/dashboard/signals/health/affiliates)"
      contains: "admin"
  key_links:
    - from: "apps/web/src/app/(admin)/layout.tsx"
      to: "apps/web/src/app/(admin)/_components/AdminSidebar.tsx"
      via: "import + render inside flex row, gate preserved above"
      pattern: "AdminSidebar"
    - from: "apps/web/src/app/(admin)/_components/AdminSidebar.tsx"
      to: "next/navigation usePathname"
      via: "active prefix-match"
      pattern: "usePathname"
---

<objective>
Lay the Phase 8 foundation: the three pure, unit-tested logic functions that all admin views consume, the consolidated admin shell (sidebar) that every (admin) route renders inside, and ALL Phase 8 FR copy keys in one pass.

Purpose: Research §Wave 0 mandates extracting all genuine new logic into testable pure functions in `lib/admin/*` rather than testing RSC directly. The shell (D-01) is the structural prerequisite for the dashboard, signaux, santé and affiliés pages — they all mount into it. Centralizing every `admin.*` copy key here means the Wave 2 view plans (02/03/04) never touch `fr.json`, so they run fully parallel with zero file contention. This plan unblocks Wave 2.
Output: `lib/admin/{signals,freshness,jobs}.ts` (+ vitest tests), `(admin)/layout.tsx` edited to a two-column shell, `(admin)/_components/AdminSidebar.tsx`, and the complete `admin.{nav,dashboard,signals,health,affiliates}.*` FR copy block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-CONTEXT.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md

<interfaces>
<!-- Existing contracts the executor needs. Do NOT re-explore the codebase for these. -->

From apps/web/src/app/(admin)/layout.tsx (CURRENT — preserve gate + provider + Toaster verbatim):
The layout is an async RSC that calls `await requireRole('superadmin')` first, then renders
`<NextIntlClientProvider locale="fr" messages={frMessages}>` wrapping a single
`<div className="min-h-screen bg-background text-foreground">{children}</div>` and `<Toaster />`.
The gate line and the provider/Toaster MUST stay. Only the inner single `<div>` is replaced by the shell.

From apps/web/src/components/admin/PayoutRowAction.tsx (lines 1–13 — client island shape to mirror):
Starts with `'use client'`, imports `useTranslations` from `next-intl`. AdminSidebar mirrors this
directive + translation shape, and adds `usePathname` from `next/navigation`.

telegram_posts → trade_setups link convention (migration 0015 comment, A1 in research):
There is NO foreign key. A setup is "posté" iff a telegram_posts row exists with
`dedupe_key === 'notable:' + setup.id`. Failures are NOT persisted (releasePost deletes the
reservation) → only 2 states are derivable: 'posted' | 'unpublished'.

job_runs row shape (migration 0001): { job_name: string, status: 'running'|'success'|'error',
started_at: string (ISO), finished_at: string | null }.

v_data_freshness row shape (migrations 0003/0004): { canonical_symbol, timeframe, last_ts, is_stale: boolean }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure admin logic functions (signals / freshness / jobs) + vitest</name>
  <files>apps/web/src/lib/admin/signals.ts, apps/web/src/lib/admin/signals.test.ts, apps/web/src/lib/admin/freshness.ts, apps/web/src/lib/admin/freshness.test.ts, apps/web/src/lib/admin/jobs.ts, apps/web/src/lib/admin/jobs.test.ts</files>
  <read_first>
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md (§"No Analog Found" lines 294–298 — the 3 functions to extract; §"signaux/page.tsx" lines 134–143 telegram cross-ref; §"sante/page.tsx" lines 180–202 freshness + job_runs reducer)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md (§"Open Questions" Q1 lines 373–376 founder-approved thresholds; §"Code Examples" lines 282–327)
    - apps/web/src/app/(admin)/affiliation/payouts/page.tsx (any existing `*.test.ts` neighbour for the vitest import style and the workspace glob that picks up apps/web tests)
  </read_first>
  <behavior>
    signals.ts:
      - `postedSetupIdSet(posts: { dedupe_key: string }[]): Set<string>` — keeps only keys starting `notable:`, strips the prefix, returns the id set. Non-notable keys ignored. Empty/undefined input → empty set.
      - `telegramStatusFor(setupId: string, postedIds: Set<string>): 'posted' | 'unpublished'` — 'posted' iff present. NEVER returns 'failed' (Pitfall 3 — not persisted).
    freshness.ts:
      - `candleColor(isStale: boolean, ageHours: number, thresholdHours: number): 'green'|'amber'|'red'` — red iff isStale; amber iff !isStale && ageHours > 1.5 * thresholdHours; else green.
      - `ageColor(ageHours: number, amberHours: number, redHours: number): 'green'|'amber'|'red'` — red iff ageHours > redHours; amber iff ageHours > amberHours; else green. Used for news/macro.
      - `NEWS_THRESHOLDS = { amberHours: 6, redHours: 24 }` and `MACRO_THRESHOLDS = { amberHours: 36, redHours: 72 }` (founder-approved defaults).
    jobs.ts:
      - `runDurationMs(startedAt: string, finishedAt: string | null): number | null` — ms between, null if finishedAt null (running) or unparseable.
      - `latestPerJob(rows: { job_name: string; status: string; started_at: string; finished_at: string|null }[]): {...}[]` — input assumed sorted started_at desc (page passes it ordered); keep the FIRST occurrence per job_name; attach `durationMs`. Stable order = first-seen.
    Each test file: boundary cases (exact threshold, just-over, empty input, running job null duration, non-notable dedupe key ignored, duplicate job_name keeps most recent).
  </behavior>
  <action>Create `apps/web/src/lib/admin/` with three pure modules — zero I/O, no Supabase/next imports, no `Number()` on money (these handle no money). Implement `signals.ts`, `freshness.ts`, `jobs.ts` to the behavior contract above using the thresholds from RESEARCH Q1 (news amber>6h/red>24h, macro amber>36h/red>72h, candles amber band at 1.5× the timeframe-hours threshold). Write the matching `*.test.ts` files FIRST (RED), covering each boundary listed in behavior. Confirm the workspace vitest glob already includes `apps/web/src/lib/**/*.test.ts` (precedent D-05-01-D extended it); if a neighbour admin test under this glob already runs, no config change is needed — otherwise extend the root vitest include to cover `apps/web/src/lib/admin/**/*.test.ts` and note the deviation. Keep functions exported by name so RSC pages in Wave 2 import them directly.</action>
  <verify>
    <automated>pnpm vitest run apps/web/src/lib/admin/signals.test.ts apps/web/src/lib/admin/freshness.test.ts apps/web/src/lib/admin/jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three test files green; each covers at least: exact-boundary, just-over-boundary, empty-input.
    - `grep -v '^//' apps/web/src/lib/admin/signals.ts | grep -c "'failed'"` returns 0 (no failed state).
    - No import of `next`, `@supabase`, `server-only`, `fs`, `fetch` in any of the three modules (pure).
    - `pnpm tsc --noEmit` (or `pnpm typecheck`) adds zero new errors vs baseline.
  </acceptance_criteria>
  <done>signals/freshness/jobs pure functions exist, exported, unit-tested green at the boundaries; Wave 2 pages can import them.</done>
</task>

<task type="auto">
  <name>Task 2: Admin shell — layout edit + AdminSidebar island + ALL Phase 8 FR copy keys</name>
  <files>apps/web/src/app/(admin)/layout.tsx, apps/web/src/app/(admin)/_components/AdminSidebar.tsx, apps/web/src/messages/fr.json</files>
  <read_first>
    - apps/web/src/app/(admin)/layout.tsx (CURRENT 25 lines — gate line + provider + Toaster to preserve)
    - apps/web/src/components/admin/PayoutRowAction.tsx (lines 1–13 — 'use client' + useTranslations shape)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md (§layout.tsx lines 34–51; §AdminSidebar lines 55–76 active-state + ⚠ plain next/link)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md (§"Admin Shell / Sidebar" lines 68–86; §Color lines 200–223; §Copywriting Contract lines 227–245 — ALL copy)
    - apps/web/src/messages/fr.json (existing `admin` namespace — append, do not duplicate `admin.payouts.*` / `admin.members.*`)
  </read_first>
  <action>Edit `(admin)/layout.tsx`: keep `await requireRole('superadmin')`, the `NextIntlClientProvider locale="fr" messages={frMessages}` and `<Toaster />` EXACTLY as-is (the gate is the real barrier — T-04-ADMIN-ELEV, do not move or soften it). Replace only the inner single `<div className="min-h-screen ...">` with a two-column flex: an `<AdminSidebar/>` (logical inline-start, `w-60`, surface `bg-card text-card-foreground border-e border-border`) plus a scrollable content column rendering `{children}` (each page keeps its own `max-w-6xl main`). Create `(admin)/_components/AdminSidebar.tsx` as a `'use client'` island: import `usePathname` from `next/navigation`, `useTranslations` from `next-intl`, plain `Link` from `next/link` (NOT i18n navigation — admin is HORS [locale]) with literal hrefs, and the lucide icons `LayoutDashboard, Users, Inbox, Share2, Wallet, Radio, Activity`. Nav items in D-01 order with literal hrefs: Tableau de bord `/admin`, Membres `/admin/membres`, File `/admin/file`, Affiliation `/admin/affiliation/affilies`, Payouts `/admin/affiliation/payouts`, Signaux `/admin/signaux`, Santé `/admin/sante`. Active detection = prefix-match on `usePathname()` (so `/admin/affiliation/payouts` lights Payouts and `/admin` exact-matches dashboard only — handle the `/admin` exact case so it is not always active). Classes per UI-SPEC: idle `text-muted-foreground rounded-md px-3 py-2 text-sm`, hover `hover:bg-accent hover:text-accent-foreground`, active `bg-primary/10 text-primary font-medium`, icon 16px leading with `gap-2`. Labels via `useTranslations('admin')` reading `nav.*` keys. Accent (`--primary`) used ONLY for the active item — no green/red anywhere in the shell.

Then append the COMPLETE Phase 8 FR copy block to `fr.json` `admin` namespace (one pass — Wave 2 plans must NOT touch fr.json), all sourced verbatim from UI-SPEC §Copywriting Contract:
- `nav.*`: dashboard « Tableau de bord », members « Membres », queue « File », affiliates « Affiliation », payouts « Payouts », signals « Signaux », health « Santé ».
- `dashboard.*`: title « Tableau de bord », kpiActiveMembers « Membres actifs », kpiPendingQueue « File de validation en attente », kpiHealth « Santé des données », healthOk « OK », healthLimit « Limite », healthStale « Périmé ».
- `signals.*`: title « Signaux publiés », filterInstrument, filterStatus, statusPosted « Posté », statusUnpublished « Non publié », colDate, colInstrument, colDirection, colScore, colTelegram, emptyHeading « Aucun signal publié », emptyBody « Les signaux générés par le moteur apparaîtront ici dès leur publication. », errorLoad « Impossible de charger les données. Réessayez ou vérifiez les jobs dans Santé. », and `signals.detail.*` (field labels + back link « Retour aux signaux »).
- `health.*`: title « Santé jobs & données », sourceCandles, sourceNews, sourceMacro, freshUp « À jour », freshLimit « Limite », freshStale « Périmé », colJob, colStatus, colDuration, colLastRun, statusOk « OK », statusError « Échec », emptyHeading « Aucun run de job enregistré », emptyBody « Les exécutions de jobs apparaîtront ici après le premier run. ».
- `affiliates.*`: title « Affiliés & performances », colEmail « Affilié », colReferrals « Filleuls », colDue « Commissions dues », colPaid « Commissions payées », toPayouts « Gérer les payouts », emptyHeading « Aucun affilié », emptyBody.

Do not duplicate existing `admin.payouts.*` / `admin.members.*` keys.</action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireRole('superadmin')" apps/web/src/app/(admin)/layout.tsx` >= 1 (gate preserved).
    - `grep -c "from 'next/link'" apps/web/src/app/(admin)/_components/AdminSidebar.tsx` == 1 AND no import from any `i18n/navigation`/`@/i18n` module (admin uses plain next/link).
    - `grep -c "usePathname" apps/web/src/app/(admin)/_components/AdminSidebar.tsx` >= 1.
    - All 7 literal hrefs present in AdminSidebar.tsx covering the 7 routes in D-01 order.
    - fr.json contains all five key groups: `grep -c "\"nav\"\|\"dashboard\"\|\"signals\"\|\"health\"\|\"affiliates\"" apps/web/src/messages/fr.json` confirms each group present under admin.
    - `pnpm lint:i18n` exit 0 (no hardcoded JSX text in the sidebar); `pnpm tsc --noEmit` zero new errors.
  </acceptance_criteria>
  <done>Every (admin) route renders inside the sidebar shell; active route highlights by prefix-match; gate untouched; complete Phase 8 FR copy lives in fr.json so Wave 2 plans never touch it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → (admin) routes | Only superadmin may reach any (admin) surface; non-admin existence must not leak |
| client bundle ↔ server | service_role / secrets must never reach the AdminSidebar client island |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation | (admin)/layout.tsx gate | mitigate | Preserve `await requireRole('superadmin')` verbatim; do not move it below the shell render. Sidebar mounts UNDER the gate. |
| T-08-02 | Information Disclosure | non-superadmin reaching /admin | mitigate | Gate already calls `notFound()` (404, never 403) — T-04-ADMIN-ELEV. This plan must not introduce any pre-gate render. |
| T-08-03 | Information Disclosure | AdminSidebar client island | mitigate | Sidebar is `'use client'` and imports only next/link + next-intl + lucide — NO service_role, NO `createAdminServiceClient`, NO env. Pure nav. |
| T-08-04 | Tampering | pure lib functions (no money) | accept | signals/freshness/jobs handle no financial values; no `Number()` on atomic strings involved. Low risk. |
| T-08-SC | Tampering | npm installs | mitigate | Zero new packages this plan (RESEARCH §Package Legitimacy: N/A). lucide-react already locked. No install task → no slopcheck needed. |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/lib/admin` green (3 pure modules).
- `pnpm tsc --noEmit` zero new errors; `pnpm lint:i18n` exit 0.
- Manual smoke (human, optional): superadmin sees sidebar on `/admin/membres`; non-superadmin gets 404 (gate unchanged).
</verification>

<success_criteria>
- Three pure admin logic functions exist and are unit-tested at their boundaries (foundation for ADMIN-03/04 views).
- Consolidated sidebar shell (D-01) wraps every (admin) route with correct active-state and preserved gate.
- All Phase 8 FR copy keys centralized → Wave 2 view plans are file-disjoint (parallel-safe).
</success_criteria>

<output>
Create `.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-01-SUMMARY.md` when done.
</output>
