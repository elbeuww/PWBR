---
phase: 08-superadmin-consolid-signaux-sant-affili-s
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/app/(admin)/sante/page.tsx
  - apps/web/src/app/(admin)/page.tsx
autonomous: true
requirements: [ADMIN-04]
must_haves:
  truths:
    - "Superadmin sees green/orange/red freshness indicators per source: candles, news, macro"
    - "Candle freshness uses the existing v_data_freshness.is_stale (never re-derived in JS — weekends would break)"
    - "News freshness derives from max(published_at); macro from max(ts), via the Plan 01 ageColor mapper"
    - "Superadmin sees a table of the latest job_runs per job: statut (ok/échec), durée, dernier run"
    - "The /admin dashboard shows >=3 KPI cards (membres actifs, file en attente, santé globale) each linking to its page"
    - "No alerting/notification is wired (D-08, visibility only)"
  artifacts:
    - path: "apps/web/src/app/(admin)/sante/page.tsx"
      provides: "RSC santé: freshness traffic-lights + job_runs table"
      min_lines: 70
    - path: "apps/web/src/app/(admin)/page.tsx"
      provides: "RSC /admin dashboard KPI cards"
      min_lines: 40
  key_links:
    - from: "apps/web/src/app/(admin)/sante/page.tsx"
      to: "apps/web/src/lib/admin/freshness.ts"
      via: "import candleColor + ageColor + thresholds"
      pattern: "candleColor|ageColor"
    - from: "apps/web/src/app/(admin)/sante/page.tsx"
      to: "apps/web/src/lib/admin/jobs.ts"
      via: "import latestPerJob + runDurationMs"
      pattern: "latestPerJob|runDurationMs"
    - from: "apps/web/src/app/(admin)/page.tsx"
      to: "/admin/sante and /admin/membres and /admin/file"
      via: "KPI card links"
      pattern: "/admin/(sante|membres|file)"
---

<objective>
Build the Santé view (ADMIN-04, part 2) and the /admin dashboard landing (D-02): operational at-a-glance health (freshness traffic-lights per source + latest job_runs table) and KPI summary cards.

Purpose: Success Criterion 2 of Phase 8 — "le superadmin voit la santé des jobs/données". News/macro have no freshness view, so they are computed in-RSC from max() timestamps using the Plan 01 pure mappers (resolved decision 1 — NO migration). The dashboard is the back-office landing (D-02) with at least 3 KPI cards. All FR copy (`admin.health.*`, `admin.dashboard.*`) was added in Plan 01 — this plan does NOT touch fr.json.
Output: `(admin)/sante/page.tsx`, `(admin)/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-CONTEXT.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 created lib/admin/{freshness,jobs}.ts AND all admin.health.*/admin.dashboard.* fr.json keys. -->

From apps/web/src/lib/admin/freshness.ts (Plan 01):
- `candleColor(isStale: boolean, ageHours: number, thresholdHours: number): 'green'|'amber'|'red'`
- `ageColor(ageHours: number, amberHours: number, redHours: number): 'green'|'amber'|'red'`
- `NEWS_THRESHOLDS = { amberHours: 6, redHours: 24 }`, `MACRO_THRESHOLDS = { amberHours: 36, redHours: 72 }`

From apps/web/src/lib/admin/jobs.ts (Plan 01):
- `runDurationMs(startedAt: string, finishedAt: string|null): number|null`
- `latestPerJob(rows[]): rows with durationMs` — input MUST be ordered started_at desc.

Reads (RESEARCH Code Examples lines 306–327, PATTERNS lines 180–202):
- candles: `.from('v_data_freshness').select('canonical_symbol, timeframe, last_ts, is_stale')`
- news:    `.from('news').select('published_at').order('published_at',{ascending:false}).limit(1).maybeSingle()`
- macro:   `.from('macro_series').select('ts').order('ts',{ascending:false}).limit(1).maybeSingle()`
- jobs:    `.from('job_runs').select('job_name, status, started_at, finished_at').order('started_at',{ascending:false})`

KPI counts (PATTERNS dashboard lines 88–101): use `select('id',{count:'exact',head:true})` — cheap, no rows.
  - file en attente = payments where `status='ambiguous'` (the validation queue).
  - membres actifs = subscriptions with active period (mirror the membres/page.tsx active definition — read it, do not invent).

FR copy already in fr.json (Plan 01): `admin.health.*` and `admin.dashboard.*`. Read via `getTranslations('admin')`.
Do NOT add new keys here.

Established admin chrome + Card contract: UI-SPEC §Dashboard lines 90–101, §Santé lines 122–148.
searchParams/params are Promises in Next 15 (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Santé page — freshness traffic-lights (candles/news/macro) + job_runs table</name>
  <files>apps/web/src/app/(admin)/sante/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/file/page.tsx (RSC table analog: service_role read + shadcn Table + empty state)
    - apps/web/src/app/(admin)/affiliation/payouts/page.tsx (PostgREST select + manual map + badge tint)
    - apps/web/src/lib/admin/freshness.ts AND apps/web/src/lib/admin/jobs.ts (Plan 01 mappers — DO NOT re-derive staleness in JS)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md (§sante/page.tsx lines 176–208)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md (§"Santé View" lines 122–148 — dot/label/badge tints; §Color status exception lines 211–223)
    - apps/web/src/components/ui/{table,badge,card}.tsx (APIs)
  </read_first>
  <action>Create `(admin)/sante/page.tsx` as an RSC matching the established admin chrome. Read via `createAdminServiceClient()` all four sources from the interfaces block. Freshness: for candles, group v_data_freshness rows and map each to a color with `candleColor(is_stale, ageHours, thresholdHours)` where ageHours = (now - last_ts)/3600000 and thresholdHours = 2× the timeframe's hours (per RESEARCH Code Example) — NEVER recompute `is_stale` in JS (the view handles FX weekend/DST, Don't-Hand-Roll). For news use `ageColor(ageSinceMaxPublishedAt, NEWS_THRESHOLDS.amberHours, NEWS_THRESHOLDS.redHours)`; for macro `ageColor(ageSinceMaxTs, MACRO_THRESHOLDS.amberHours, MACRO_THRESHOLDS.redHours)`. Render per-source freshness as a row of cards / compact list: source name, colored dot `size-2.5 rounded-full` (green→`bg-emerald-500`, amber→`bg-amber-500`, red→`bg-red-500`), state label (`admin.health.freshUp/freshLimit/freshStale`), and last-fresh timestamp (`Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'short'})`, fallback '—'). If a source has no rows (null max) → treat as red « Périmé » with '—' timestamp. Jobs table (D-07): pass the started_at-desc ordered rows to `latestPerJob(rows)`, render shadcn Table columns Job · Statut · Durée · Dernier run. Statut badge: success → `admin.health.statusOk` emerald tint `border-emerald-600/30 bg-emerald-500/10 text-emerald-700 dark:text-emerald-400`; error → `admin.health.statusError` red tint `border-red-600/30 bg-red-500/10 text-red-700 dark:text-red-400`; running → neutral. Durée from `runDurationMs` formatted ms/s in `<bdi>`, '—' if null. Dernier run = started_at via `Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'short'})`. Empty jobs → dashed block `admin.health.emptyHeading` + `admin.health.emptyBody`. NO alerting, NO refresh polling, NO action island (D-08, read-only). NO inline requireRole. Use only the `admin.health.*` keys Plan 01 added — do NOT add new fr.json keys.</action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "candleColor\|ageColor" apps/web/src/app/(admin)/sante/page.tsx` >= 1 (uses Plan 01 freshness mapper).
    - `grep -c "latestPerJob\|runDurationMs" apps/web/src/app/(admin)/sante/page.tsx` >= 1 (uses Plan 01 jobs reducer).
    - `grep -c "is_stale\s*=\|isStale =" apps/web/src/app/(admin)/sante/page.tsx` == 0 (does NOT re-derive staleness; only reads/passes the view's boolean).
    - `grep -c "createAdminServiceClient" apps/web/src/app/(admin)/sante/page.tsx` >= 1.
    - All three sources present: `grep -c "v_data_freshness"`, `grep -c "news"`, `grep -c "macro_series"` each >= 1.
    - `grep -c "requireRole" apps/web/src/app/(admin)/sante/page.tsx` == 0 (layout owns gate).
    - `pnpm lint:i18n` exit 0; `pnpm tsc --noEmit` zero new errors.
  </acceptance_criteria>
  <done>Santé shows per-source green/amber/red freshness (candles via view, news/macro via age mapper) and a latest-per-job runs table; visibility only.</done>
</task>

<task type="auto">
  <name>Task 2: /admin dashboard landing with KPI cards</name>
  <files>apps/web/src/app/(admin)/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/membres/page.tsx (active-member definition + service_role read — mirror the active-subscription criterion, do NOT invent one)
    - apps/web/src/app/(admin)/file/page.tsx (ambiguous-payments queue criterion)
    - apps/web/src/lib/admin/freshness.ts (Plan 01 — for the global health summary dot)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md (§page.tsx dashboard lines 79–106)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md (§"Dashboard Landing" lines 90–101; Typography display 30px lines 184–194)
    - apps/web/src/components/ui/{card,skeleton}.tsx (APIs)
  </read_first>
  <action>Create `(admin)/page.tsx` as the `/admin` RSC landing. Read via `createAdminServiceClient()` at least 3 KPIs using cheap head counts (`select('id',{count:'exact',head:true})`): (1) membres actifs — reuse the active-subscription criterion from membres/page.tsx (read it; mirror exactly); (2) file de validation en attente — payments `status='ambiguous'`; (3) santé globale des données — derive a single worst-of traffic-light from the same freshness reads as the Santé page (reuse candleColor/ageColor on candles/news/macro; the global dot = the worst color among sources). Render a `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` of shadcn `Card`s: header = metric label `text-sm text-muted-foreground` (`admin.dashboard.kpiActiveMembers/kpiPendingQueue/kpiHealth`), body = big number `text-3xl font-semibold` formatted via `Intl.NumberFormat('fr-FR')` (the health card body = colored dot + word `admin.dashboard.healthOk/healthLimit/healthStale`). Each card is a link to its page: membres → `/admin/membres`, file → `/admin/file`, santé → `/admin/sante`. Use accent (`--primary`) only for links/focus per UI-SPEC reserved list. h1 `text-2xl font-semibold` = `admin.dashboard.title`. NO inline requireRole. Use only `admin.dashboard.*` keys Plan 01 added — do NOT add new fr.json keys.</action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "count: 'exact'" apps/web/src/app/(admin)/page.tsx` >= 2 (cheap head counts for KPIs).
    - `grep -Ec "/admin/(membres|file|sante)" apps/web/src/app/(admin)/page.tsx` >= 3 (each card links to its page).
    - `grep -c "Card" apps/web/src/app/(admin)/page.tsx` >= 3 (>= 3 KPI cards, UI-SPEC minimum).
    - `grep -c "candleColor\|ageColor" apps/web/src/app/(admin)/page.tsx` >= 1 (global health derived from the same mapper, not a new ad-hoc rule).
    - `grep -c "requireRole" apps/web/src/app/(admin)/page.tsx` == 0.
    - `pnpm lint:i18n` exit 0; `pnpm tsc --noEmit` zero new errors.
  </acceptance_criteria>
  <done>/admin renders >=3 KPI cards (active members, pending queue, global health) each linking to its full page; health derived from the shared freshness mapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin and /admin/sante | superadmin-only operational data over service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Elevation | non-admin reaching /admin or /admin/sante | mitigate | Layout gate `requireRole('superadmin')` → notFound() covers both routes (T-04-ADMIN-ELEV); pages add no pre-gate render. |
| T-08-11 | Information Disclosure | service_role reads in RSC | mitigate | Both files are pure RSC (no `'use client'`); `createAdminServiceClient()` is server-only, build fails if bundled. |
| T-08-12 | Tampering | freshness re-derivation getting weekends wrong | mitigate | Candle staleness READ from `v_data_freshness.is_stale` (view owns FX weekend/DST); JS only adds the amber band + news/macro age — never recomputes is_stale. |
| T-08-13 | Information Disclosure | raw DB error to client | mitigate | Load failures surface opaque dashboard/health copy; server-side throw logged, client sees Next error boundary (M-05). |
| T-08-SC | Tampering | npm installs | mitigate | Zero new packages (RESEARCH §Package Legitimacy N/A). No install → no slopcheck. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` zero new errors; `pnpm lint:i18n` exit 0; full `pnpm vitest run` green (Plan 01 mappers exercised by their own units).
- Manual smoke (human, optional): superadmin opens /admin → 3 KPI cards with real counts; opens /admin/sante → 3 source dots + jobs table; non-admin → 404.
</verification>

<success_criteria>
- ADMIN-04 (santé half): superadmin sees per-source freshness traffic-lights + latest job_runs, and a KPI dashboard landing — all visibility-only (D-08).
</success_criteria>

<output>
Create `.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-03-SUMMARY.md` when done.
</output>
