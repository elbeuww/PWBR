---
phase: 08-superadmin-consolid-signaux-sant-affili-s
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - apps/web/src/app/(admin)/affiliation/affilies/page.tsx
  - apps/web/e2e/gating.spec.ts
autonomous: true
requirements: [ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "Superadmin sees a list of all affiliates with per-affiliate performance: referral count, commissions due and paid"
    - "Affiliate performance is aggregated from BASE tables (affiliates/referrals/commissions), NOT from affiliate_dashboard (which is auth.uid()-scoped and empty under service_role)"
    - "Money cells render via formatAtomic(BigInt(str)) inside <bdi> — never Number() on atomic amounts"
    - "The existing payouts page (mark-paid + tx ref + paid history) is reachable from the shell and still renders due+paid commissions (D-09/D-10 — zero DB work)"
    - "An affiliate never sees another affiliate's data — this admin view is gated superadmin-only"
    - "A non-superadmin (and an unauthenticated visitor) requesting /admin, /admin/signaux, /admin/signaux/[id], or /admin/sante receives a 404 (notFound) — the back-office existence never leaks via 200/redirect (T-04-ADMIN-ELEV)"
  artifacts:
    - path: "apps/web/src/app/(admin)/affiliation/affilies/page.tsx"
      provides: "RSC affiliés + perfs aggregate list (ADMIN-03 part 1)"
      min_lines: 60
    - path: "apps/web/e2e/gating.spec.ts"
      provides: "Admin-gate E2E extended to assert 404 on every new Phase 8 admin route for non-superadmin + unauthenticated (T-04-ADMIN-ELEV)"
      contains: "/admin/signaux"
  key_links:
    - from: "apps/web/src/app/(admin)/affiliation/affilies/page.tsx"
      to: "createAdminServiceClient → affiliates/referrals/commissions"
      via: "service_role aggregate read of base tables"
      pattern: "from\\('affiliates'\\)"
    - from: "apps/web/src/app/(admin)/affiliation/affilies/page.tsx"
      to: "@app/core formatAtomic"
      via: "money cell string→BigInt"
      pattern: "formatAtomic\\(BigInt"
    - from: "apps/web/src/app/(admin)/_components/AdminSidebar.tsx"
      to: "/admin/affiliation/affilies and /admin/affiliation/payouts"
      via: "sidebar nav links (Affiliation default = affilies)"
      pattern: "/admin/affiliation/"
    - from: "apps/web/e2e/gating.spec.ts"
      to: "/admin/signaux, /admin/signaux/[id], /admin/sante (404 for non-superadmin)"
      via: "Playwright response.status() === 404 assertions per new route"
      pattern: "/admin/(signaux|sante)"
---

<objective>
Build the Affiliés-perfs view (ADMIN-03, part 1) — a superadmin list of all affiliates with their performance aggregated from base tables — confirm the existing payout workflow (ADMIN-03 part 2, D-09/D-10) is wired into the shell and renders the paid history, and extend the admin-gate E2E so the new Phase 8 admin routes all 404 for non-superadmins (ADMIN-04 phase-gate, T-04-ADMIN-ELEV).

Purpose: Success Criterion 1 of Phase 8 — "le superadmin voit les affiliés et leurs performances et gère les payouts". RESEARCH resolved that payouts already ship (migration 0016: `payouts` table + `commissions.status` + `mark_commission_paid` RPC; page already marks paid with tx ref + renders history) → D-09/D-10 need ZERO DB work. The genuine new surface is the affiliés-perfs aggregate, which MUST read base tables (not the auth.uid()-scoped `affiliate_dashboard` view — resolved decision 4 / assumption A3). FR copy (`admin.affiliates.*`) was added in Plan 01 — this plan does NOT touch fr.json. Task 2 closes the RESEARCH §Validation Architecture phase-gate: the existing admin-gate E2E (gating.spec.ts ACCESS-03) only covers the pre-existing `/admin` route; the new routes shipped by Plans 08-02 (signaux + detail) and 08-03 (sante + dashboard) must be proven to 404 for non-superadmins so the back-office never leaks its existence.
Output: `(admin)/affiliation/affilies/page.tsx`; payouts page confirmed reachable + intact; `e2e/gating.spec.ts` extended for the new admin routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-CONTEXT.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md
@.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 added admin.affiliates.* fr.json keys. -->

CRITICAL (resolved decision 4 / RESEARCH A3): do NOT read `affiliate_dashboard` via service_role.
It is security_invoker=true + auth.uid()-scoped → returns NOTHING under service_role (no auth.uid()).
Aggregate from BASE tables (RESEARCH Code Example lines 330–339, PATTERNS lines 216–224):
  `.from('affiliates').select('id, profiles!inner(email), referrals(count), commissions(amount_atomic, status)')`
then sum per-affiliate in JS: referral count plain; amounts string→BigInt (CR-02), split by status due vs paid.

From @app/core: `formatAtomic(BigInt(atomicString)): string` — money display, CR-02 (bigint > 2^53, never Number()).
Money unit: `getTranslations('payment')('amountUnit')` (existing key, mirrors payouts/page.tsx lines 104–108).

From apps/web/src/lib/supabase/admin-service.ts: `createAdminServiceClient()` (service_role, server-only).

FR copy already in fr.json (Plan 01): `admin.affiliates.title/colEmail/colReferrals/colDue/colPaid/toPayouts/
emptyHeading/emptyBody`. Read via `getTranslations('admin')`. Do NOT add new keys here.

Established admin chrome: main `mx-auto max-w-6xl px-4 py-8`; h1 `text-2xl font-semibold`; shadcn Table in
`mt-6 w-full overflow-x-auto`; empty state `rounded-lg border border-dashed p-10 text-center`;
money `<bdi>{formatAtomic(BigInt(x))} {unit}</bdi>`.

Existing payouts page (NO CHANGE, confirm only): apps/web/src/app/(admin)/affiliation/payouts/page.tsx +
actions.ts already mark commissions paid (tx_hash + atomic amount) via `mark_commission_paid` RPC and render
due+paid rows with TronScan link (D-15). Sidebar (Plan 01) links Payouts → /admin/affiliation/payouts.

Existing admin-gate E2E (Task 2 EXTENDS, does not replace): apps/web/e2e/gating.spec.ts.
The `ACCESS-03 / D-09` describe-block (lines ~73–87) already asserts `response.status() === 404` for both
non-auth and authenticated-non-superadmin against `ADMIN_SURFACE = '/admin'`. Helpers `signUp(page, email)`
(creates a default-"member" user) and `uniqueEmail(tag)` are already defined in-file. The new Phase 8 routes
to cover: `/admin/signaux`, `/admin/signaux/[id]` (any concrete id segment, e.g. `/admin/signaux/00000000-0000-0000-0000-000000000000`),
`/admin/sante`. (`/admin` is already covered.) Mirror the EXACT assertion style: `expect(response?.status()).toBe(404)`
— never assert a redirect/200 (that would leak existence). This spec runs against the LIVE app (`next dev` on
http://localhost:3000) per its header pre-reqs — same live-infra convention as the rest of e2e/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Affiliés-perfs aggregate page (base tables) + confirm payouts wiring</name>
  <files>apps/web/src/app/(admin)/affiliation/affilies/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/affiliation/payouts/page.tsx (EXACT analog — same dir, select+map+table+money-cell shape; lines ~104–108 money cell)
    - apps/web/src/app/(admin)/affiliation/page.tsx (existing candidature queue — confirm it is NOT the perfs view; perfs is the new affilies/ route)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-PATTERNS.md (§affilies/page.tsx lines 212–228 — base-table aggregate, money cell, nav placement)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md (§"Affiliés + perfs" lines 329–339 — A3 warning; §Don't Hand-Roll lines 241–249)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-UI-SPEC.md (§Established Admin Page Pattern lines 46–64; §Color lines 200–223; §Payout Workflow lines 152–161)
    - packages/supabase/src/database.types.ts (lines ~1062–1129 — AffiliateRow, CommissionRow, AffiliateDashboardRow aliases; confirm column names before query)
  </read_first>
  <action>Create `(admin)/affiliation/affilies/page.tsx` as an RSC matching the payouts/page.tsx pattern verbatim. Read via `createAdminServiceClient()` the BASE-table aggregate from the interfaces block — `.from('affiliates').select('id, profiles!inner(email), referrals(count), commissions(amount_atomic, status)')`. Do NOT query `affiliate_dashboard` (it returns empty under service_role — resolved decision 4; if a grep finds it, that is a bug). Aggregate per affiliate in JS: `referralCount` = the referrals count; `commissionsDueAtomic` = sum of `BigInt(c.amount_atomic)` where `c.status==='due'`; `commissionsPaidAtomic` = sum where `c.status==='paid'`. Use BigInt accumulation, NEVER `Number()` on amount_atomic. Render a shadcn Table: columns Affilié (email, `admin.affiliates.colEmail`), Filleuls (referralCount via `Intl.NumberFormat('fr-FR')`, `admin.affiliates.colReferrals`), Commissions dues (`<bdi>{formatAtomic(commissionsDueAtomic)} {unit}</bdi>`, `admin.affiliates.colDue`), Commissions payées (same with paid, `admin.affiliates.colPaid`). `unit` from `getTranslations('payment')('amountUnit')`. Sort affiliates by commissionsDue desc (most actionable first). Empty state: dashed block `admin.affiliates.emptyHeading` + `admin.affiliates.emptyBody`. Add a header link/button to the payouts page (`/admin/affiliation/payouts`) labelled `admin.affiliates.toPayouts` so the perfs view connects to the payout action. NO mutation/action island here (the mutation lives in the existing payouts page — D-09/D-10 already satisfied). NO inline requireRole. Use only `admin.affiliates.*` keys Plan 01 added — do NOT add new fr.json keys. CONFIRM (no edit): the existing payouts page still renders due+paid + tx link and is reachable from the sidebar Payouts item (Plan 01) — record this confirmation in the SUMMARY (D-09/D-10 zero-DB-work). The Plan 01 sidebar already points Affiliation → `/admin/affiliation/affilies`; confirm consistency.</action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from('affiliates')" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` >= 1 (base-table read).
    - `grep -c "affiliate_dashboard" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` == 0 (A3 — must NOT use the auth.uid()-scoped view).
    - `grep -c "formatAtomic(BigInt" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` >= 1 AND `grep -c "Number(" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` == 0 (CR-02 money).
    - `grep -c "/admin/affiliation/payouts" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` >= 1 (perfs view links to payout management).
    - `grep -c "createAdminServiceClient" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` >= 1.
    - `grep -c "requireRole" apps/web/src/app/(admin)/affiliation/affilies/page.tsx` == 0 (layout gate; this page has no mutation).
    - Payouts page unchanged: `git diff --quiet apps/web/src/app/(admin)/affiliation/payouts/` exits 0 (no modification).
    - `pnpm lint:i18n` exit 0; `pnpm tsc --noEmit` zero new errors.
  </acceptance_criteria>
  <done>Superadmin sees all affiliates with referral count + commissions due/paid (base-table aggregate, CR-02 money), links to the existing payout workflow which still marks-paid with tx ref + renders history.</done>
</task>

<task type="auto">
  <name>Task 2: Extend admin-gate E2E to the new Phase 8 routes (404 for non-superadmin) — T-04-ADMIN-ELEV</name>
  <files>apps/web/e2e/gating.spec.ts</files>
  <read_first>
    - apps/web/e2e/gating.spec.ts (the existing `ACCESS-03 / D-09` describe-block lines ~73–87 covering `/admin`; the `signUp`/`uniqueEmail` helpers lines ~38–48; the file header pre-reqs lines ~20–26 documenting the live `next dev` requirement — MIRROR this assertion style exactly)
    - apps/web/src/app/(admin)/layout.tsx (the `requireRole('superadmin')` → notFound() gate that produces the 404 — confirm it wraps ALL `(admin)/*` children, so every new route inherits the gate)
    - apps/web/src/lib/auth/gate.ts (`requireRole` → notFound() behavior; confirm it returns 404 not 403, never leaking existence)
    - .planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-RESEARCH.md (§Validation Architecture lines 423/429/435 — phase-gate requires Playwright admin-gate 404 on the new routes; §Security Domain V4 lines 448/460 — notFound() not 403, T-04-ADMIN-ELEV)
  </read_first>
  <action>Extend (do NOT rewrite) apps/web/e2e/gating.spec.ts. Add a new `test.describe` block — e.g. `ACCESS-03b / T-04-ADMIN-ELEV : nouvelles routes admin Phase 8 → 404` — that proves the new Phase 8 admin routes never leak their existence to non-superadmins. Define a constant list of the new routes: `/admin/signaux`, a concrete signal-detail path `/admin/signaux/00000000-0000-0000-0000-000000000000` (any UUID segment; the gate fires before any data lookup, so a non-existent id is fine and exercises the route segment), and `/admin/sante`. (`/admin` itself is already covered by the existing ACCESS-03 block — do NOT duplicate it.) For each route add TWO assertions mirroring the existing block verbatim: (1) an UNAUTHENTICATED visit — `const response = await page.goto(route); expect(response?.status()).toBe(404)`; and (2) an authenticated NON-superadmin visit — reuse the in-file `signUp(page, uniqueEmail('nonadmin-<routeTag>'))` helper (creates a default-"member" user), then `const response = await page.goto(route); expect(response?.status()).toBe(404)`. Use `for (const route of NEW_ADMIN_ROUTES)` with one `test()` per route per auth-state (or a parametrized `test.describe` loop) so each route path appears literally in the spec. The assertion MUST be `.toBe(404)` only — NEVER assert a redirect, a 200, or a 403 (a 403 or a 200 would leak the back-office existence; the gate's notFound() is the contract, T-04-ADMIN-ELEV / RESEARCH §Security Domain V4). Keep the existing file header comment accurate: append a line noting the new routes are covered as of Plan 08-04. Do NOT touch any other describe-block, helper, or import.</action>
  <verify>
    <automated>grep -Eq "/admin/signaux" apps/web/e2e/gating.spec.ts && grep -Eq "/admin/signaux/[0-9a-f]" apps/web/e2e/gating.spec.ts && grep -Eq "/admin/sante" apps/web/e2e/gating.spec.ts && grep -c "toBe(404)" apps/web/e2e/gating.spec.ts | grep -qE "[6-9]|[1-9][0-9]"</automated>
    <human-check>Live-infra E2E (requires `next dev` on :3000 + Supabase env + "Confirm email" disabled per the spec header). Run `pnpm playwright test gating` and confirm the new `ACCESS-03b` block passes: every new admin route returns 404 for both the unauthenticated and the authenticated-non-superadmin visit. Record PASS/SKIP (and why, if env-gated) in the SUMMARY — same manual-only convention as the other live-infra E2E in this project.</human-check>
  </verify>
  <acceptance_criteria>
    - The spec file contains literal assertions for each of the new routes: `grep -c "/admin/signaux" apps/web/e2e/gating.spec.ts` >= 1 (list route), `grep -Ec "/admin/signaux/[0-9a-fA-F]" apps/web/e2e/gating.spec.ts` >= 1 (detail route with a concrete id segment), `grep -c "/admin/sante" apps/web/e2e/gating.spec.ts` >= 1.
    - At least 6 `toBe(404)` assertions total in the file (2 existing for `/admin` + ≥2 each for the 3 new routes when iterating both auth states), and `grep -c "toBe(403)" apps/web/e2e/gating.spec.ts` == 0 AND `grep -c "toBe(200)" apps/web/e2e/gating.spec.ts` == 0 (no existence leak).
    - The existing `ACCESS-03 / D-09` block and the `signUp`/`uniqueEmail` helpers are unchanged (only additive code).
    - `pnpm exec tsc --noEmit -p apps/web` (or the e2e tsconfig) zero new type errors on the edited spec.
    - When live infra is available: `pnpm playwright test gating` exits 0 (new block green). When env-gated, the human-check records SKIP + reason (consistent with other live-infra specs).
  </acceptance_criteria>
  <done>The admin-gate E2E asserts that `/admin/signaux`, `/admin/signaux/[id]`, and `/admin/sante` all return 404 (notFound) for both unauthenticated and authenticated-non-superadmin requests — never 200/redirect/403 — closing the RESEARCH §Validation Architecture phase-gate (T-04-ADMIN-ELEV).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/affiliation/affilies | superadmin-only; exposes affiliate emails + financial aggregates |
| browser → /admin/signaux, /admin/signaux/[id], /admin/sante | superadmin-only Phase 8 routes; existence must not leak to non-admins |
| atomic money string → display | must not lose precision (bigint > 2^53) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Elevation | non-admin reaching affilies | mitigate | Layout gate `requireRole('superadmin')` → notFound() (T-04-ADMIN-ELEV); page adds no pre-gate render. Affiliates cannot reach another affiliate's data — superadmin-only surface. |
| T-08-15 | Information Disclosure | service_role read of emails/financials | mitigate | `createAdminServiceClient()` server-only; page is pure RSC (no client island), build fails if bundled. Superadmin is allowed to see emails (already does in membres/payouts, V8). |
| T-08-16 | Tampering | money precision loss via Number() | mitigate | Sum amounts as `BigInt(amount_atomic)`; display via `formatAtomic`; grep gate `Number(` == 0 (CR-02 / T-07-FLOAT). |
| T-08-17 | Tampering | reading affiliate_dashboard returns empty (silent data loss) | mitigate | Forbidden by grep gate; aggregate from base tables only (A3). |
| T-08-18 | Tampering | double-payout / payout integrity | accept | Payout mutation untouched this plan; integrity owned by existing `mark_commission_paid` RPC (atomic, raises if already paid — T-07-DOUBLEPAY, 0016). No new mutation introduced. |
| T-08-19 | Information Disclosure | new Phase 8 admin routes (/admin/signaux, /admin/signaux/[id], /admin/sante) leaking existence to non-admins (200/redirect/403 instead of 404) | mitigate | Task 2 E2E asserts `toBe(404)` for unauthenticated + authenticated-non-superadmin on every new route; grep gates forbid `toBe(403)`/`toBe(200)`. Real barrier is the layout `requireRole('superadmin')` → notFound() (T-04-ADMIN-ELEV); the E2E proves it covers the new routes too. |
| T-08-SC | Tampering | npm installs | mitigate | Zero new packages (RESEARCH §Package Legitimacy N/A). No install → no slopcheck. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` zero new errors; `pnpm lint:i18n` exit 0; full `pnpm vitest run` green (payout/affiliation guard tests not regressed).
- `git diff --quiet apps/web/src/app/(admin)/affiliation/payouts/` exits 0 (D-09/D-10 zero-DB-work confirmed by non-modification + existing tests).
- Admin-gate E2E phase-gate (T-04-ADMIN-ELEV): `apps/web/e2e/gating.spec.ts` contains `toBe(404)` assertions for `/admin/signaux`, `/admin/signaux/[id]`, `/admin/sante` for both auth states; no `toBe(403)`/`toBe(200)`. Against live infra (`next dev` + Supabase env), `pnpm playwright test gating` passes the new `ACCESS-03b` block; if env-gated, SKIP recorded in SUMMARY (live-infra convention).
- Manual smoke (human, optional): superadmin opens /admin/affiliation/affilies → affiliate rows with due/paid totals; clicks « Gérer les payouts » → existing payouts page marks a commission paid with tx ref and the paid row stays visible.
</verification>

<success_criteria>
- ADMIN-03: superadmin sees affiliates + their performance (referrals, due/paid commissions) and manages payouts (existing workflow reachable + intact, D-09/D-10).
- ADMIN-04 (phase-gate): every new Phase 8 admin route (/admin/signaux, /admin/signaux/[id], /admin/sante) returns 404 for non-superadmins — proven by the extended admin-gate E2E (T-04-ADMIN-ELEV).
</success_criteria>

<output>
Create `.planning/phases/08-superadmin-consolid-signaux-sant-affili-s/08-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
