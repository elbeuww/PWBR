---
phase: 02-ingestion-fiable-des-donn-es
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/data-sources/package.json
  - packages/data-sources/tsconfig.json
  - packages/data-sources/src/index.ts
  - packages/data-sources/src/binance/client.ts
  - packages/data-sources/src/binance/schema.ts
  - packages/data-sources/src/oanda/client.ts
  - packages/data-sources/src/oanda/schema.ts
  - packages/data-sources/src/oanda/instruments.ts
  - packages/data-sources/src/binance/schema.test.ts
  - packages/data-sources/src/oanda/schema.test.ts
  - packages/data-sources/src/__fixtures__/binance-klines.json
  - packages/data-sources/src/__fixtures__/oanda-candles.json
  - apps/jobs/src/jobs/market-ingest.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/__tests__/gap-fill.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-05, DATA-06]
user_setup: []

must_haves:
  truths:
    - "Le job market-ingest tire les klines crypto (Binance mainnet public) et les candles OANDA démo (forex+or+argent+pétrole), les normalise en UTC et les upserte sans doublon"
    - "Les parsers Zod Binance et OANDA transforment les payloads bruts en lignes candles typées (golden values vertes)"
    - "Le gap-fill reprend depuis la dernière bougie en base (1er run = backfill 2 ans D / 6 mois H4/H1, runs suivants = incrémental)"
    - "market-ingest est enregistré dans le dispatcher et exécutable via tsx src/dispatch.ts market-ingest"
  artifacts:
    - path: "packages/data-sources/src/binance/client.ts"
      provides: "getKlines via MainClient (mainnet public, sans clé)"
      exports: ["fetchBinanceKlines"]
    - path: "packages/data-sources/src/oanda/client.ts"
      provides: "fetch+Zod candles OANDA démo (count XOR from/to)"
      exports: ["fetchOandaCandles"]
    - path: "apps/jobs/src/jobs/market-ingest.ts"
      provides: "job gap-fill OHLCV multi-instrument/TF, isolation par instrument"
      exports: ["marketIngest"]
    - path: "packages/data-sources/src/oanda/instruments.ts"
      provides: "mapping symbole canonique <-> source_symbol OANDA"
      exports: ["OANDA_SYMBOLS"]
  key_links:
    - from: "apps/jobs/src/jobs/market-ingest.ts"
      to: "packages/supabase upsertCandles / getLastCandleTs"
      via: "import @app/supabase"
      pattern: "upsertCandles|getLastCandleTs"
    - from: "apps/jobs/src/jobs/market-ingest.ts"
      to: "packages/core lastClosedCandleStart / dailyAnchorStart"
      via: "import @app/core"
      pattern: "lastClosedCandleStart|dailyAnchorStart"
    - from: "apps/jobs/src/dispatch.ts"
      to: "marketIngest"
      via: "JOB_REGISTRY['market-ingest']"
      pattern: "'market-ingest'"
---

<objective>
Livrer la première tranche verticale complète d'ingestion : depuis le pull d'une source de marché jusqu'à l'upsert idempotent en base, bout-en-bout. Créer le package `packages/data-sources`, les clients Binance (SDK, mainnet public) et OANDA (fetch+Zod démo), leurs parsers de normalisation UTC, le job `market-ingest` auto-rattrapant (gap-fill) avec isolation des pannes par instrument, et son enregistrement dans le dispatcher.

Purpose: c'est le slice qui prouve la valeur de la phase — après ce plan, un `tsx src/dispatch.ts market-ingest` remplit la table candles pour les 12 instruments MVP en H1/H4/Daily, normalisés UTC, sans doublon au re-run.
Output: package data-sources, 2 clients + schemas, job market-ingest enregistré, golden tests + gap-fill test verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-01-SUMMARY.md

<interfaces>
<!-- Repositories disponibles depuis le plan 01 (import depuis @app/supabase) : -->
<!-- upsertCandles(client: ServiceClient, rows: CandleInsert[]): Promise<void>  // onConflict instrument_id,timeframe,ts -->
<!-- getLastCandleTs(client: ServiceClient, instrumentId: string, timeframe: string): Promise<string | null> -->

<!-- Constantes temps (import depuis @app/core, NE PAS recalculer) : -->
<!-- TIMEFRAMES = { H1: 60, H4: 240, D: 1440 } ; type Timeframe = 'H1'|'H4'|'D' -->
<!-- lastClosedCandleStart(now: DateTime, tfMinutes: number): DateTime  // borne haute EXCLUSIVE anti look-ahead (H1/H4) -->
<!-- dailyAnchorStart(source: 'oanda'|'binance', now: DateTime): DateTime  // borne haute Daily par source -->
<!-- DAILY_ANCHOR = { oanda: { zone:'America/New_York', hour:17 }, binance:{ zone:'UTC', hour:0 } } -->

<!-- Signature d'un job (copier heartbeat.ts) : -->
<!-- import type { Json } from '@app/supabase' -->
<!-- export async function marketIngest(): Promise<Json> { ... return { inserted, skipped, errors } } -->

<!-- Client service_role lazy (copier runJob.ts:30-41) : -->
<!-- const url = process.env['SUPABASE_URL']; const key = process.env['SUPABASE_SERVICE_ROLE_KEY'] -->
<!-- createClient<Database>(url, key, { auth: { persistSession:false, autoRefreshToken:false } }) -->

<!-- Binance SDK (mainnet public, klines non signé — testnet n'a PAS d'historique, Pitfall 1) : -->
<!-- import { MainClient } from 'binance' ; const c = new MainClient({}) -->
<!-- c.getKlines({ symbol, interval:'1h'|'4h'|'1d', startTime, endTime, limit:1000 }) -->
<!-- Kline[] = tuples [openTime, open, high, low, close, volume, closeTime, ...] -->

<!-- OANDA candles (fetch démo) — base https://api-fxpractice.oanda.com : -->
<!-- GET /v3/instruments/{src}/candles?granularity=H1|H4|D&from=<iso>&to=<iso>&price=M  (count XOR from/to — Pitfall 3) -->
<!-- réponse: { candles: [{ time, mid:{o,h,l,c}, volume, complete }] } -->

<!-- Rate limits (CLAUDE.md) : OANDA pLimit(2), Binance pLimit(3) ; p-retry respect Retry-After. p-retry/p-limit ESM only. -->

<!-- Univers seedé (instruments.source_symbol) : -->
<!-- crypto Binance: BTCUSDT ETHUSDT SOLUSDT BNBUSDT XRPUSDT -->
<!-- forex OANDA: EUR_USD GBP_USD USD_JPY AUD_USD -->
<!-- commodities OANDA: XAU_USD XAG_USD WTICO_USD -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package data-sources + clients Binance/OANDA + parsers Zod normalisation UTC + golden tests</name>
  <files>packages/data-sources/package.json, packages/data-sources/tsconfig.json, packages/data-sources/src/index.ts, packages/data-sources/src/binance/client.ts, packages/data-sources/src/binance/schema.ts, packages/data-sources/src/oanda/client.ts, packages/data-sources/src/oanda/schema.ts, packages/data-sources/src/oanda/instruments.ts, packages/data-sources/src/binance/schema.test.ts, packages/data-sources/src/oanda/schema.test.ts, packages/data-sources/src/__fixtures__/binance-klines.json, packages/data-sources/src/__fixtures__/oanda-candles.json</files>
  <behavior>
    - parseBinanceKlines(rawTuples) => CandleInsert[] avec ts en ISO UTC, open/high/low/close numériques, instrument_id/timeframe injectés.
    - parseOandaCandles(rawResponse) => CandleInsert[] : ne garde QUE les bougies `complete:true`, mid OHLC en numérique, time ISO UTC.
    - Golden : une fixture JSON connue => sortie déterministe attendue (valeurs exactes), exécutable hors-ligne.
  </behavior>
  <read_first>
    - packages/core/src/time/candle.ts + sessions.ts + constants.ts (signatures à consommer)
    - packages/core/src/time/candle.test.ts (style golden : describe/it, expect(...).toBe, fixture en tête)
    - apps/jobs/src/runJob.ts (lignes 30-41 : pattern secret depuis env + erreur explicite)
    - packages/supabase/src/database.types.ts (type CandleInsert — forme exacte des lignes à produire)
    - packages/data-sources/.. (n'existe pas encore — créer ; calquer package.json/tsconfig sur packages/core, ESM "type":"module")
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md (§Code Examples klines/OANDA ; §Pitfall 1 testnet ; §Pitfall 3 count XOR from/to)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md (§oanda/client.ts §binance/client.ts §schema.ts)
  </read_first>
  <action>
    Créer le package `packages/data-sources` (ESM, `"type":"module"`, nom `@app/data-sources`, tsconfig calqué sur packages/core moduleResolution, deps : `binance`, `zod`, `luxon`, `p-retry`, `p-limit` ; installer via `pnpm --filter @app/data-sources add binance p-retry p-limit` — zod/luxon déjà au workspace).

    - `binance/client.ts` : `fetchBinanceKlines(sourceSymbol, interval, startTimeMs, endTimeMs)` via `new MainClient({})` (AUCUNE clé — klines mainnet public ; testnet exclu, Pitfall 1), `getKlines({ symbol, interval, startTime, endTime, limit:1000 })`. Wrapper chaque appel dans `pLimit(3)` ∘ `pRetry` (respect Retry-After). Retourner les tuples bruts (la normalisation vit dans schema.ts).
    - `binance/schema.ts` : Zod parse du tuple kline `[openTime:number, open:string, high:string, low:string, close:string, volume:string, closeTime:number, ...]` => `parseBinanceKlines(tuples, instrumentId, timeframe): CandleInsert[]` : `ts` = ISO UTC depuis openTime (luxon `DateTime.fromMillis(openTime,{zone:'utc'}).toISO()`), OHLC `Number(...)`. Frontière Zod obligatoire (données externes non fiables).
    - `oanda/client.ts` : `fetchOandaCandles(sourceSymbol, granularity, fromIso, toIso)` — base `https://api-fxpractice.oanda.com`, token via `process.env['OANDA_API_TOKEN']` (throw si absent, pattern runJob.ts), URL `/v3/instruments/${src}/candles?granularity=${gran}&from=${fromIso}&to=${toIso}&price=M` (count XOR from/to — Pitfall 3), header `Authorization: Bearer`. `if (!res.ok) throw`. Wrapper `pLimit(2)` ∘ `pRetry`.
    - `oanda/schema.ts` : Zod schema `{ candles: [{ time, mid:{o,h,l,c}, volume, complete }] }` => `parseOandaCandles(resp, instrumentId, timeframe): CandleInsert[]` : filtre `complete===true` UNIQUEMENT (anti bougie en cours), `ts` = `time` normalisé ISO UTC, OHLC `Number(mid.*)`.
    - `oanda/instruments.ts` : exporter `OANDA_SYMBOLS` = mapping canonical_symbol -> source_symbol pour les 7 instruments OANDA (EUR_USD, GBP_USD, USD_JPY, AUD_USD, XAU_USD, XAG_USD, WTICO_USD). Le job lira plutôt `instruments.source_symbol` depuis la base, mais ce mapping documente/teste la convention D-18.
    - `index.ts` : barrel exportant fetchBinanceKlines, parseBinanceKlines, fetchOandaCandles, parseOandaCandles, OANDA_SYMBOLS.
    - Fixtures `__fixtures__/binance-klines.json` et `oanda-candles.json` : échantillons réels réduits (2-3 bougies, dont une OANDA `complete:false` pour prouver le filtrage).
    - Tests golden `binance/schema.test.ts` et `oanda/schema.test.ts` : charger la fixture, parser, assert valeurs exactes (ts ISO UTC, OHLC numériques) ; OANDA : assert que la bougie incomplete est exclue.
  </action>
  <verify>
    <automated>pnpm vitest run packages/data-sources</automated>
  </verify>
  <acceptance_criteria>
    - `packages/data-sources/package.json` a `"type":"module"`, nom `@app/data-sources`, dépend de binance/p-retry/p-limit.
    - `binance/client.ts` instancie `new MainClient({})` SANS clé (aucune référence à testnet pour les klines).
    - `oanda/client.ts` utilise la base `api-fxpractice.oanda.com` et lit `OANDA_API_TOKEN` depuis l'env (throw si absent).
    - `parseOandaCandles` exclut les bougies `complete:false` (prouvé par le golden test).
    - `pnpm vitest run packages/data-sources` passe (golden Binance + OANDA verts, déterministes hors-ligne).
  </acceptance_criteria>
  <done>Package data-sources créé, 2 clients + parsers normalisation UTC, golden tests verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job market-ingest (gap-fill + isolation par instrument) + enregistrement dispatcher + test gap-fill</name>
  <files>apps/jobs/src/jobs/market-ingest.ts, apps/jobs/src/dispatch.ts, apps/jobs/__tests__/gap-fill.test.ts</files>
  <behavior>
    - Avec un dernier ts en base = T, market-ingest calcule une fenêtre [since=T, until=lastClosedCandleStart/dailyAnchorStart) — jamais la bougie en cours.
    - Au 1er run (getLastCandleTs => null), since = backfillStart(tf) : ~2 ans pour D, ~6 mois pour H4/H1.
    - Une erreur sur UN instrument (ex symbole absent OANDA) est poussée dans stats.errors et n'interrompt PAS les autres instruments (DATA-07 partiel — preuve complète en plan 03).
    - market-ingest est résolvable via le dispatcher.
  </behavior>
  <read_first>
    - apps/jobs/src/jobs/heartbeat.ts (signature job () => Promise<Json>)
    - apps/jobs/src/dispatch.ts (registre JOB_REGISTRY à étendre)
    - apps/jobs/src/runJob.ts (pattern client service_role lazy depuis env)
    - packages/supabase/src/repositories/candles.ts (upsertCandles, getLastCandleTs — créés plan 01)
    - packages/supabase/src/repositories/instruments.ts (listActiveInstruments — pattern lecture ; le job lit les instruments actifs)
    - packages/core/src/time/candle.ts + sessions.ts (bornes hautes)
    - packages/data-sources/src/index.ts (fetch/parse créés Task 1)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md (§market-ingest gap-fill + isolation)
  </read_first>
  <action>
    Créer `apps/jobs/src/jobs/market-ingest.ts` exportant `marketIngest(): Promise<Json>` :
    - Crée son client service_role lazy depuis l'env (copier runJob.ts:30-41 ; ne PAS importer service-client.ts).
    - Lit les instruments actifs (`is_active`/`active` true) avec leur `source_symbol`, `broker`, `asset_class` — soit via un repository, soit via un select direct (le job a le service_role). Mapper broker -> source binance|oanda.
    - Pour chaque (instrument actif × timeframe H1/H4/D) DANS SON PROPRE try/catch :
      - `getLastCandleTs(client, inst.id, tf)`. `since = last ?? backfillStart(tf)` où backfillStart = `DateTime.utc().minus({years:2})` pour D, `.minus({months:6})` pour H1/H4.
      - `until` = `lastClosedCandleStart(DateTime.utc(), TIMEFRAMES[tf])` pour H1/H4 ; `dailyAnchorStart(broker, DateTime.utc())` pour D (borne par source — D-09).
      - Binance : pagine par `limit:1000` (startTime/endTime ms) ; OANDA : pagine par fenêtres `from`+`to` (count auto ≤5000), `includeFirst=false` entre pages pour éviter la bougie de jointure dupliquée.
      - parse via le schema correspondant => CandleInsert[] (instrument_id, timeframe injectés), `upsertCandles(client, rows)`.
      - `stats.inserted += rows.length`.
      - catch => `stats.errors.push({ instrument: inst.canonical_symbol ?? inst.source_symbol, timeframe: tf, msg })` ; CONTINUE (les autres cibles ingèrent — DATA-07).
    - Retourner `{ inserted, skipped, errors }` (runJob l'écrit dans job_runs.stats ; status reste 'success' même avec erreurs partielles).
    Enregistrer dans `apps/jobs/src/dispatch.ts` : `import { marketIngest } from './jobs/market-ingest'` + entrée `'market-ingest': marketIngest` dans JOB_REGISTRY (ne pas toucher heartbeat).
    Écrire `apps/jobs/__tests__/gap-fill.test.ts` (unit, repository mocké — pas de réseau) : étant donné un dernier ts simulé, assert que la fenêtre `since/until` calculée est correcte (until < now, since = dernier ts) et qu'au 1er run since = backfillStart. Style golden (candle.test.ts).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/gap-fill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `marketIngest` retourne un objet `{ inserted, skipped, errors }` (errors est un tableau).
    - Chaque cible (instrument × tf) est dans un try/catch individuel ; une erreur push dans stats.errors sans propager (vérifiable dans le source : pas de re-throw dans la boucle).
    - market-ingest consomme `lastClosedCandleStart`/`dailyAnchorStart` de @app/core (aucun floor maison) et `upsertCandles`/`getLastCandleTs` de @app/supabase.
    - `dispatch.ts` JOB_REGISTRY contient la clé `'market-ingest'`.
    - `pnpm vitest run apps/jobs/__tests__/gap-fill.test.ts` passe (fenêtre correcte, 1er run = backfill).
  </acceptance_criteria>
  <done>Job market-ingest livré, gap-fill + isolation, enregistré dans le dispatcher, test gap-fill vert. Slice OHLCV bout-en-bout opérationnelle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API externe (Binance/OANDA) -> job | réponse non fiable, parse Zod obligatoire avant upsert |
| job (service_role) -> Postgres | écriture bypass RLS, confinée apps/jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | payload Binance/OANDA malformé injecté en base | mitigate | parse Zod strict (binance/schema.ts, oanda/schema.ts) avant tout upsert ; OHLC validés numériques |
| T-02-05 | Information Disclosure | OANDA_API_TOKEN exposé | mitigate | token lu depuis apps/jobs/.env uniquement (throw si absent), jamais loggé, jamais NEXT_PUBLIC_ |
| T-02-06 | Denial of Service | rate limit tier gratuit OANDA/Binance | mitigate | p-limit(2)/p-limit(3) + p-retry Retry-After ; gap-fill borne les fenêtres (pas de re-pull du déjà en base) |
| T-02-07 | Tampering | look-ahead (bougie en cours ingérée) | mitigate | borne haute exclusive via lastClosedCandleStart/dailyAnchorStart (core P1) ; OANDA filtre complete:true |
| T-02-SC | Tampering | install binance/p-retry/p-limit | mitigate | paquets verrouillés CLAUDE.md + audités RESEARCH §Package Legitimacy (binance=tiagosiebler, p-retry/p-limit=sindresorhus) ; aucun [SUS]/[SLOP] => pas de checkpoint requis |
</threat_model>

<verification>
- `pnpm vitest run packages/data-sources` vert (golden Binance + OANDA).
- `pnpm vitest run apps/jobs/__tests__/gap-fill.test.ts` vert.
- `tsx apps/jobs/src/dispatch.ts market-ingest` résout le job (smoke ; remplissage réel dépend des clés OANDA + accès réseau).
</verification>

<success_criteria>
- DATA-01 (klines crypto H1/H4/D UTC) et DATA-02 (OANDA OHLCV UTC) ingérées via market-ingest.
- DATA-05 consommée (bornes core, jamais la bougie en cours).
- DATA-06 respectée (upsert idempotent du plan 01).
- Slice verticale : pull -> normalise UTC -> upsert candles, bout-en-bout, enregistrée dans le dispatcher.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-fiable-des-donn-es/02-02-SUMMARY.md` when done
</output>
