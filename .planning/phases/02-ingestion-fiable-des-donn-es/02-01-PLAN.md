---
phase: 02-ingestion-fiable-des-donn-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_data_ingestion_tables.sql
  - packages/supabase/src/database.types.ts
  - packages/supabase/src/repositories/candles.ts
  - packages/supabase/src/repositories/news.ts
  - packages/supabase/src/repositories/macroSeries.ts
  - packages/supabase/src/repositories/economicCalendar.ts
  - packages/supabase/src/index.ts
  - packages/supabase/__tests__/idempotency.test.ts
  - .env.example
autonomous: false
requirements: [DATA-06]
user_setup: []

must_haves:
  truths:
    - "Les tables candles, news, macro_series, economic_calendar existent sur le projet Supabase cloud avec RLS active et 0 alerte get_advisors"
    - "L'univers de 12 instruments MVP est seedé dans instruments (5 crypto, 4 forex, 3 commodities) sans dupliquer les 3 lignes existantes de Phase 1"
    - "Un re-run complet d'upsert ne crée aucun doublon (test idempotency vert)"
    - "La staleness est consultable via une vue qui tient compte des horaires de cotation (forex week-end != stale)"
  artifacts:
    - path: "supabase/migrations/0003_data_ingestion_tables.sql"
      provides: "DDL candles/news/macro_series/economic_calendar + RLS + index uniques + extension instruments + seed 12 + vue v_data_freshness"
      contains: "create table public.candles"
    - path: "packages/supabase/src/repositories/candles.ts"
      provides: "upsertCandles(onConflict instrument_id,timeframe,ts) + getLastCandleTs"
      exports: ["upsertCandles", "getLastCandleTs"]
    - path: "packages/supabase/src/repositories/news.ts"
      provides: "upsertNews(onConflict url_hash)"
      exports: ["upsertNews"]
    - path: "packages/supabase/src/repositories/macroSeries.ts"
      provides: "upsertMacroSeries(onConflict series_code,ts)"
      exports: ["upsertMacroSeries"]
    - path: "packages/supabase/src/repositories/economicCalendar.ts"
      provides: "upsertEconomicCalendar(onConflict event_key)"
      exports: ["upsertEconomicCalendar"]
    - path: "packages/supabase/__tests__/idempotency.test.ts"
      provides: "DATA-06 — upsert x2 => count identique contre cloud"
      contains: "upsertCandles"
  key_links:
    - from: "packages/supabase/src/repositories/candles.ts"
      to: "public.candles"
      via: ".upsert(rows, { onConflict: 'instrument_id,timeframe,ts' })"
      pattern: "onConflict.*instrument_id,timeframe,ts"
    - from: "supabase/migrations/0003_data_ingestion_tables.sql"
      to: "candles_uniq index"
      via: "create unique index"
      pattern: "unique index.*candles.*instrument_id, timeframe, ts"
---

<objective>
Poser la couche base de données et les repositories typés que tous les jobs d'ingestion de la Phase 2 consommeront : tables candles / news / macro_series / economic_calendar avec RLS et index uniques d'upsert, extension de `instruments` + seed des 12 instruments MVP, vue `v_data_freshness` (staleness consultable, horaires de cotation respectés), repositories d'upsert idempotent, et le contrat de secrets `.env.example`.

Purpose: l'idempotence (DATA-06) est définie au niveau base (contrainte unique + `upsert onConflict`) ; sans cette fondation, aucun job ne peut écrire sans risque de doublon. Les plans 02 et 03 se branchent dessus.
Output: migration 0003 appliquée sur le cloud, `database.types.ts` régénéré, 4 repositories exportés, test d'idempotence vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md

<interfaces>
<!-- Schéma canonique cible (ARCHITECTURE.md §171-173). Source de vérité du schéma. -->
<!-- candles — instrument_id FK, timeframe (H1|H4|D), ts, OHLCV ; unique (instrument_id, timeframe, ts) ; index (instrument_id, timeframe, ts desc). RLS lecture authenticated, écriture service_role. -->
<!-- news — source, url_hash (unique), title, summary, published_at, sentiment (-1..1), impact, instrument_ids uuid[]. RLS lecture authenticated. -->
<!-- macro_series — series_code (FRED), ts, value. Unique (series_code, ts). RLS lecture authenticated. -->

<!-- Pattern repository typé (à copier — packages/supabase/src/repositories/jobRuns.ts) : -->
<!-- import type { SupabaseClient } from '@supabase/supabase-js' -->
<!-- import type { Database, ... } from '../database.types' -->
<!-- type ServiceClient = SupabaseClient<Database> -->
<!-- if (error) throw new Error(`opName failed: ${error.message}`) -->

<!-- Pattern migration (à copier — supabase/migrations/0001_init_profiles_instruments_job_runs.sql) : -->
<!-- create table public.X (...) ; alter table public.X enable row level security ; -->
<!-- create policy "X: lecture authentifiés" on public.X for select to authenticated using (true) ; -->
<!-- AUCUNE policy insert/update/delete (écriture = service_role bypass). -->
<!-- Tout trigger security definer => set search_path = '' (cf. 0001 ligne 90). -->

<!-- État existant instruments (migration 0001) : colonnes symbol(unique)/broker/asset_class/display_name/pip_size/min_size/precision/active. 3 lignes seed : XAU_USD, EUR_USD, BTCUSDT. -->

<!-- Constantes temps réutilisables (packages/core, NE PAS redéfinir) : -->
<!-- DAILY_ANCHOR = { oanda: { zone: 'America/New_York', hour: 17 }, binance: { zone: 'UTC', hour: 0 } } -->
<!-- TIMEFRAMES = { H1: 60, H4: 240, D: 1440 } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0003 — tables d'ingestion, RLS, index uniques, extension instruments + seed 12, vue v_data_freshness</name>
  <files>supabase/migrations/0003_data_ingestion_tables.sql</files>
  <read_first>
    - supabase/migrations/0001_init_profiles_instruments_job_runs.sql (structure exacte à copier : table -> enable RLS -> policy select authenticated -> aucune policy write -> seed ; trigger security definer set search_path = '')
    - supabase/migrations/0002_revoke_execute_trigger_functions.sql (durcissement appliqué en P1)
    - ARCHITECTURE.md (§ lignes 171-173 : schéma canonique candles/news/macro_series — source de vérité)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md (D-14..D-20 univers/métadonnées ; D-26/D-27 stale ; D-31 calendrier)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md (§Pitfall 4 stale week-end ; §No Analog v_data_freshness ; séries FRED DFF/CPIAUCSL/DTWEXBGS/DFII10)
  </read_first>
  <action>
    Écrire la migration `0003_data_ingestion_tables.sql` qui crée 4 tables, étend `instruments`, re-seed l'univers et ajoute la vue de fraîcheur. Identifiants exacts à respecter (ils servent de `onConflict` aux repositories) :

    - Table `public.candles` : `instrument_id uuid not null references public.instruments(id)`, `timeframe text not null check (timeframe in ('H1','H4','D'))`, `ts timestamptz not null`, `open/high/low/close numeric not null`, `volume numeric`. Index unique nommé `candles_uniq` sur `(instrument_id, timeframe, ts)`. Index secondaire `(instrument_id, timeframe, ts desc)`.
    - Table `public.news` : `id uuid primary key default gen_random_uuid()`, `source text not null`, `url_hash text not null`, `title text not null`, `summary text`, `published_at timestamptz not null`, `sentiment numeric check (sentiment >= -1 and sentiment <= 1)`, `impact text`, `instrument_ids uuid[] not null default '{}'`. Index unique nommé `news_uniq` sur `(url_hash)`.
    - Table `public.macro_series` : `series_code text not null`, `ts timestamptz not null`, `value numeric not null`. Index unique `macro_series_uniq` sur `(series_code, ts)`.
    - Table `public.economic_calendar` : `event_key text not null`, `title text not null`, `country text`, `event_at timestamptz not null`, `impact text check (impact in ('High','Medium','Low'))`, `forecast text`, `previous text`, `source text not null`. Index unique `economic_calendar_uniq` sur `(event_key)`.
    - Sur CHAQUE table : `enable row level security` + policy `select to authenticated using (true)` + AUCUNE policy write (écriture service_role bypass).
    - Étendre `public.instruments` (ne PAS recréer la table) via `alter table` : ajouter `canonical_symbol text` (ex 'BTC/USD','EUR/USD','XAU/USD','WTI/USD'), `source_symbol text` (symbole technique par source : 'BTCUSDT','EUR_USD','WTICO_USD'…), `price_decimals int`, `quote_hours text` (ex '24/7' pour crypto, 'fx' pour forex/commodities — interprété par v_data_freshness). Élargir le check `asset_class` pour inclure 'commodity' si absent (la P1 a 'metal'/'energy' — ajouter 'commodity' OU réutiliser metal/energy de façon cohérente avec D-19 ; documenter le choix en commentaire SQL).
    - Re-seed les 12 instruments MVP (D-14/15/16) via `insert ... on conflict (symbol) do update set ...` pour ne PAS dupliquer XAU_USD/EUR_USD/BTCUSDT existants : crypto Binance BTCUSDT/ETHUSDT/SOLUSDT/BNBUSDT/XRPUSDT, forex OANDA EUR_USD/GBP_USD/USD_JPY/AUD_USD, commodities OANDA XAU_USD/XAG_USD/WTICO_USD. Renseigner canonical_symbol + source_symbol + asset_class + quote_hours + price_decimals pour chaque.
    - Vue `public.v_data_freshness` (D-26/D-27, Pitfall 4) : pour chaque (instrument_id, timeframe) avec la dernière candle, calculer `is_stale` = (age depuis dernière candle > 2× le timeframe attendu) ET (marché ouvert selon quote_hours). Crypto (24/7) toujours évaluable ; forex/commodities (quote_hours='fx') => `is_stale=false` pendant la fermeture week-end FX (vendredi 17:00 NY -> dimanche 17:00 NY). Exposer colonnes `instrument_id, timeframe, last_ts, is_stale`. La vue doit être consultable par les utilisateurs authentifiés (RLS héritée des tables sous-jacentes ; ne pas exposer de données sensibles).
    Ne PAS recalculer les conventions de bougie côté SQL ; la vue ne mesure que la fraîcheur (âge), pas l'alignement.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0003_data_ingestion_tables.sql | grep -c "enable row level security" # attendu >= 4</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier contient `create table public.candles`, `public.news`, `public.macro_series`, `public.economic_calendar`.
    - Chaque nouvelle table a `enable row level security` ET une policy `for select to authenticated using (true)` ; aucune policy `for insert`/`for update`/`for delete`.
    - Index uniques présents et nommés : `candles_uniq` (instrument_id, timeframe, ts), `news_uniq` (url_hash), `macro_series_uniq` (series_code, ts), `economic_calendar_uniq` (event_key).
    - `instruments` est étendu par `alter table` (pas recréé) ; le seed utilise `on conflict (symbol) do update` ; les 12 symboles sources sont présents (BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT, XRPUSDT, EUR_USD, GBP_USD, USD_JPY, AUD_USD, XAU_USD, XAG_USD, WTICO_USD).
    - `create view public.v_data_freshness` existe et référence `quote_hours` (logique marché fermé != stale).
  </acceptance_criteria>
  <done>Migration écrite, DDL + RLS + index uniques + extension instruments + seed 12 + vue freshness présents. Pas encore appliquée (Task 2).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Appliquer la migration sur le cloud + régénérer database.types.ts + saisir les clés API</name>
  <read_first>
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md (§Pitfall 6 types périmés ; §Environment Availability clés requises)
    - .env.example (contrat de secrets existant à étendre)
  </read_first>
  <what-built>
    La migration 0003 est écrite. Elle doit être appliquée sur le projet Supabase cloud (project_ref `csotpitrjxryjkadyiml`) via MCP `apply_migration`, puis `database.types.ts` doit être régénéré (MCP `generate_typescript_types`) pour que les repositories typés compilent contre le schéma live. Build/type-check passeraient SANS ce push (les types viennent du fichier généré, pas du live) — d'où le caractère bloquant.

    De plus, de nouvelles clés API doivent être saisies par l'utilisateur lui-même dans `apps/jobs/.env` (jamais manipulées par l'exécuteur) : `OANDA_API_TOKEN`, `OANDA_ACCOUNT_ID`, `FINNHUB_API_KEY`, `MARKETAUX_API_KEY`, `FRED_API_KEY`. (BINANCE_API_KEY/SECRET optionnels — les klines sont en mainnet public sans clé. FairEconomy = aucune clé.)
  </what-built>
  <action>
    1. L'exécuteur applique la migration via MCP Supabase `apply_migration` (project_ref csotpitrjxryjkadyiml), name `0003_data_ingestion_tables`.
    2. L'exécuteur régénère les types via MCP `generate_typescript_types` et écrit le résultat dans `packages/supabase/src/database.types.ts` (committer). Vérifier que les nouveaux types `CandleRow/CandleInsert`, `NewsRow/NewsInsert`, `MacroSeriesRow/MacroSeriesInsert`, `EconomicCalendarRow/EconomicCalendarInsert` sont présents.
    3. L'exécuteur étend `.env.example` (valeurs VIDES, jamais préfixées NEXT_PUBLIC_, sous la section JOBS) avec : OANDA_API_TOKEN=, OANDA_ACCOUNT_ID=, BINANCE_API_KEY=, BINANCE_API_SECRET=, FINNHUB_API_KEY=, MARKETAUX_API_KEY=, FRED_API_KEY= (commentaire : FairEconomy = aucune clé ; Binance optionnel).
    4. L'exécuteur vérifie `get_advisors` (type security) = 0 alerte RLS sur les nouvelles tables.
    5. CHECKPOINT HUMAIN : l'utilisateur colle ses propres clés OANDA/FRED/Finnhub/Marketaux dans `apps/jobs/.env` (fichier gitignoré). L'exécuteur ne lit ni n'écrit jamais les valeurs de clés.
  </action>
  <how-to-verify>
    1. MCP `list_tables` montre candles, news, macro_series, economic_calendar avec RLS active.
    2. MCP `get_advisors` (security) ne retourne aucune alerte "RLS disabled" sur les nouvelles tables.
    3. `apps/jobs/.env` contient OANDA_API_TOKEN, OANDA_ACCOUNT_ID, FINNHUB_API_KEY, MARKETAUX_API_KEY, FRED_API_KEY renseignées par l'utilisateur (vérification visuelle par l'utilisateur ; ne jamais echo les valeurs).
  </how-to-verify>
  <acceptance_criteria>
    - `list_tables` liste les 4 nouvelles tables avec rls_enabled=true.
    - `get_advisors` security = 0 alerte sur candles/news/macro_series/economic_calendar.
    - `packages/supabase/src/database.types.ts` contient les types des 4 nouvelles tables.
    - `.env.example` contient les 7 nouvelles clés (valeurs vides, aucune préfixée NEXT_PUBLIC_).
  </acceptance_criteria>
  <resume-signal>Tape "clés saisies" une fois les clés OANDA/FRED/Finnhub/Marketaux collées dans apps/jobs/.env (ou "ignorer X" pour les sources sans clé pour l'instant).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repositories d'upsert idempotent + getLastCandleTs + test d'idempotence (DATA-06)</name>
  <files>packages/supabase/src/repositories/candles.ts, packages/supabase/src/repositories/news.ts, packages/supabase/src/repositories/macroSeries.ts, packages/supabase/src/repositories/economicCalendar.ts, packages/supabase/src/index.ts, packages/supabase/__tests__/idempotency.test.ts</files>
  <behavior>
    - upsertCandles(client, rows) avec rows en doublon => 0 nouvelle ligne au 2e appel (count stable).
    - getLastCandleTs(client, instrumentId, 'H1') => ISO du dernier ts ou null si aucune candle.
    - upsertNews / upsertMacroSeries / upsertEconomicCalendar : même garantie d'idempotence sur leurs clés uniques respectives.
    - Re-run complet (upsert ×2 du même lot) => count identique en base (DATA-06).
  </behavior>
  <read_first>
    - packages/supabase/src/repositories/jobRuns.ts (pattern exact : ServiceClient type, erreur explicite, select/order/limit)
    - packages/supabase/src/repositories/instruments.ts (pattern lecture)
    - packages/supabase/src/index.ts (barrel — y ajouter les exports)
    - packages/supabase/__tests__/rls.test.ts (pattern intégration cloud : env SUPABASE_URL/SERVICE_ROLE_KEY, client service_role persistSession:false, cleanup afterAll via delete().in)
    - apps/jobs/__tests__/runJob.test.ts (pattern cleanup createdIds + delete().in)
    - packages/supabase/src/database.types.ts (types régénérés en Task 2 — CandleInsert, NewsInsert, etc.)
  </read_first>
  <action>
    Créer 4 repositories typés sur `type ServiceClient = SupabaseClient<Database>`, chacun calqué sur `jobRuns.ts` (erreur explicite `throw new Error(\`opName failed: ${error.message}\`)`):
    - `candles.ts` : `upsertCandles(client, rows: CandleInsert[]): Promise<void>` avec `.upsert(rows, { onConflict: 'instrument_id,timeframe,ts', ignoreDuplicates: false })` (early-return si rows vide) ; `getLastCandleTs(client, instrumentId: string, timeframe: string): Promise<string | null>` via `select('ts').eq(...).eq(...).order('ts',{ascending:false}).limit(1).maybeSingle()`.
    - `news.ts` : `upsertNews(client, rows: NewsInsert[]): Promise<void>` onConflict `'url_hash'`.
    - `macroSeries.ts` : `upsertMacroSeries(client, rows: MacroSeriesInsert[]): Promise<void>` onConflict `'series_code,ts'`.
    - `economicCalendar.ts` : `upsertEconomicCalendar(client, rows: EconomicCalendarInsert[]): Promise<void>` onConflict `'event_key'`.
    Exporter les 6 fonctions + les types Insert/Row associés depuis `packages/supabase/src/index.ts` (étendre le barrel existant, ne pas retirer les exports P1).
    Écrire `idempotency.test.ts` (intégration cloud, analogue `rls.test.ts`) : prend un instrument seedé réel (ex BTCUSDT) via son id, upsert un petit lot de candles synthétiques 2×, assert que `count` est identique après les deux passes (DATA-06). Cleanup en afterAll via `delete()` sur les ts de test (service_role). Charger l'env comme rls.test.ts.
  </action>
  <verify>
    <automated>pnpm vitest run packages/supabase/__tests__/idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/supabase/src/index.ts` exporte upsertCandles, getLastCandleTs, upsertNews, upsertMacroSeries, upsertEconomicCalendar.
    - Chaque repository utilise le `onConflict` exact correspondant à l'index unique de la migration (candles: instrument_id,timeframe,ts ; news: url_hash ; macro_series: series_code,ts ; economic_calendar: event_key).
    - `pnpm vitest run packages/supabase/__tests__/idempotency.test.ts` passe : count stable après double upsert.
    - Aucune fonction n'avale d'erreur silencieusement (chaque `error` => throw).
  </acceptance_criteria>
  <done>4 repositories exportés, test d'idempotence vert contre le cloud (DATA-06 prouvée).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs (service_role) -> Postgres | écriture via service_role bypass RLS ; jamais exposée au front |
| migration -> schéma live | DDL appliqué directement sur le cloud (apply_migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | nouvelles tables sans RLS | mitigate | `enable row level security` + policy select-only sur candles/news/macro_series/economic_calendar ; vérifié par get_advisors=0 (Task 2) |
| T-02-02 | Information Disclosure | service_role/clés API vers le front | mitigate | service_role confiné apps/jobs (lint no-restricted-imports P1) ; clés en apps/jobs/.env gitignoré, jamais NEXT_PUBLIC_ ; .env.example valeurs vides |
| T-02-03 | Tampering | trigger security definer détournable via search_path | mitigate | tout nouveau trigger => `set search_path = ''` (convention 0001) ; pas de nouveau trigger prévu ici |
| T-02-SC | Tampering | npm installs | accept | Plan 01 n'installe aucun paquet (repositories sur supabase-js déjà présent). Audit de légitimité = plans 02/03. |
</threat_model>

<verification>
- Migration appliquée : `list_tables` montre les 4 tables RLS active.
- `get_advisors` security = 0 alerte.
- `pnpm vitest run packages/supabase` vert (idempotency + rls P1 toujours verts).
</verification>

<success_criteria>
- 4 tables d'ingestion + extension instruments + seed 12 + vue freshness sur le cloud.
- database.types.ts régénéré, repositories typés compilent.
- DATA-06 prouvée par test (double upsert => 0 doublon).
- .env.example étendu (contrat de secrets), clés réelles saisies par l'utilisateur.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-fiable-des-donn-es/02-01-SUMMARY.md` when done
</output>
