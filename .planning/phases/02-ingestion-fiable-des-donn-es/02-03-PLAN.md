---
phase: 02-ingestion-fiable-des-donn-es
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - packages/data-sources/src/finnhub/client.ts
  - packages/data-sources/src/finnhub/schema.ts
  - packages/data-sources/src/marketaux/client.ts
  - packages/data-sources/src/fred/client.ts
  - packages/data-sources/src/faireconomy/client.ts
  - packages/data-sources/src/index.ts
  - packages/data-sources/src/finnhub/schema.test.ts
  - packages/data-sources/src/fred/schema.test.ts
  - packages/data-sources/src/faireconomy/schema.test.ts
  - packages/data-sources/src/__fixtures__/finnhub-news.json
  - packages/data-sources/src/__fixtures__/fred-observations.json
  - packages/data-sources/src/__fixtures__/faireconomy-calendar.json
  - apps/jobs/src/jobs/news-ingest.ts
  - apps/jobs/src/jobs/macro-ingest.ts
  - apps/jobs/src/jobs/calendar-ingest.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/__tests__/fault-isolation.test.ts
autonomous: true
requirements: [DATA-03, DATA-04, DATA-07]
user_setup: []

must_haves:
  truths:
    - "Le job news-ingest tire les news par catégorie (Finnhub primaire, Marketaux fallback), stocke le sentiment provider tel quel, dédoublonne par url_hash"
    - "Le job macro-ingest tire les séries FRED (DFF, CPIAUCSL, DTWEXBGS, DFII10) sur 2 ans et upserte par (series_code, ts)"
    - "Le job calendar-ingest tire le calendrier FairEconomy (caché 1x/jour), upserte par event_key (alimente le news_risk de Phase 3)"
    - "Une source en échec (clé API invalide) n'interrompt pas les autres et la staleness reste exposée (DATA-07 prouvée par test)"
  artifacts:
    - path: "apps/jobs/src/jobs/news-ingest.ts"
      provides: "Finnhub primaire -> Marketaux fallback, dédup url_hash, mapping par catégorie"
      exports: ["newsIngest"]
    - path: "apps/jobs/src/jobs/macro-ingest.ts"
      provides: "FRED DFF/CPIAUCSL/DTWEXBGS/DFII10 -> upsert macro_series"
      exports: ["macroIngest"]
    - path: "apps/jobs/src/jobs/calendar-ingest.ts"
      provides: "FairEconomy JSON caché -> upsert economic_calendar"
      exports: ["calendarIngest"]
    - path: "apps/jobs/__tests__/fault-isolation.test.ts"
      provides: "DATA-07 — une source en échec n'interrompt pas les autres"
      contains: "stats.errors"
  key_links:
    - from: "apps/jobs/src/jobs/news-ingest.ts"
      to: "marketaux fallback"
      via: "catch Finnhub -> fetchMarketaux"
      pattern: "marketaux|fallback"
    - from: "apps/jobs/src/jobs/macro-ingest.ts"
      to: "packages/supabase upsertMacroSeries"
      via: "import @app/supabase"
      pattern: "upsertMacroSeries"
    - from: "apps/jobs/src/dispatch.ts"
      to: "news/macro/calendar jobs"
      via: "JOB_REGISTRY"
      pattern: "news-ingest|macro-ingest|calendar-ingest"
---

<objective>
Compléter la couverture d'ingestion avec les tranches verticales news, macro et calendrier économique, puis prouver la tolérance aux pannes (DATA-07). Créer les clients Finnhub (SDK), Marketaux/FRED/FairEconomy (fetch+Zod maison), les 3 jobs correspondants, leur enregistrement dans le dispatcher, et le test d'isolation des pannes qui définit — avec l'idempotence du plan 01 — la fiabilité de la phase.

Purpose: Phase 3 (news_risk, fundamental_context) consommera des tables déjà remplies ; le contrat de fiabilité de la phase exige qu'une source coupée (mauvaise clé) laisse les autres ingérer.
Output: 4 clients news/macro/calendrier, 3 jobs enregistrés, golden tests + fault-isolation (DATA-07) verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-01-SUMMARY.md
@.planning/phases/02-ingestion-fiable-des-donn-es/02-02-SUMMARY.md

<interfaces>
<!-- Repositories disponibles (plan 01, import @app/supabase) : -->
<!-- upsertNews(client, rows: NewsInsert[]): Promise<void>  // onConflict url_hash -->
<!-- upsertMacroSeries(client, rows: MacroSeriesInsert[]): Promise<void>  // onConflict series_code,ts -->
<!-- upsertEconomicCalendar(client, rows: EconomicCalendarInsert[]): Promise<void>  // onConflict event_key -->

<!-- Pattern client fetch+Zod maison (établi plan 02, oanda/client.ts) : secret depuis env (throw si absent), -->
<!-- fetch -> if(!res.ok) throw -> Schema.parse(await res.json()) -> map ligne typée. pLimit ∘ pRetry par source. -->

<!-- Finnhub SDK (free) : marketNews('general'|'crypto'|'forex', {}) — PAS companyNews/news-sentiment (premium/US-only, Pitfall 2). Clé via FINNHUB_API_KEY. -->
<!-- Marketaux (fallback, fetch) : GET /v1/news/all?api_token=KEY&symbols=... (free 100 req/j, 3 articles/req). Clé MARKETAUX_API_KEY. -->
<!-- FRED (fetch) : GET https://api.stlouisfed.org/fred/series/observations?series_id=DFF&api_key=KEY&file_type=json&observation_start=<2y>. Séries : DFF (Fed funds), CPIAUCSL (CPI), DTWEXBGS (USD broad ~ proxy DXY — A5), DFII10 (real yield 10Y). Clé FRED_API_KEY. -->
<!-- FairEconomy (fetch, AUCUNE clé) : GET https://nfs.faireconomy.media/ff_calendar_thisweek.json — { title, country, date(ISO), impact(High|Medium|Low), forecast, previous }. Rate limit 2/5min => cacher, re-fetch 1x/jour max (Pitfall 5). -->

<!-- Rate limits (CLAUDE.md) : Finnhub pLimit(1)+Retry-After, FRED pLimit(2). -->

<!-- D-28 mapping news = par catégorie/symbole ciblé (la news arrive déjà mappée) — JAMAIS heuristique mots-clés maison. -->
<!-- D-29 Finnhub primaire, Marketaux tiré SEULEMENT si Finnhub échoue/rate-limité. -->
<!-- D-30 sentiment = celui du provider, stocké tel quel avec la source. -->

<!-- Signature job + client service_role lazy : voir plan 02 (heartbeat.ts / runJob.ts:30-41). -->
<!-- Dispatcher déjà étendu par 'market-ingest' (plan 02) — y ajouter news/macro/calendar. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clients Finnhub/Marketaux/FRED/FairEconomy + parsers Zod + golden tests</name>
  <files>packages/data-sources/src/finnhub/client.ts, packages/data-sources/src/finnhub/schema.ts, packages/data-sources/src/marketaux/client.ts, packages/data-sources/src/fred/client.ts, packages/data-sources/src/faireconomy/client.ts, packages/data-sources/src/index.ts, packages/data-sources/src/finnhub/schema.test.ts, packages/data-sources/src/fred/schema.test.ts, packages/data-sources/src/faireconomy/schema.test.ts, packages/data-sources/src/__fixtures__/finnhub-news.json, packages/data-sources/src/__fixtures__/fred-observations.json, packages/data-sources/src/__fixtures__/faireconomy-calendar.json</files>
  <behavior>
    - parseFinnhubNews(rawArticles, category) => NewsInsert[] : url_hash dérivé déterministe de l'url, sentiment provider tel quel (D-30), source='finnhub'.
    - parseFredObservations(raw, seriesCode) => MacroSeriesInsert[] : (series_code, ts ISO UTC, value numérique), ignore les observations '.' (FRED manquant).
    - parseFairEconomyCalendar(raw) => EconomicCalendarInsert[] : event_key déterministe (title+country+date), event_at ISO UTC, impact High|Medium|Low.
    - Golden : fixtures connues => sorties déterministes attendues, hors-ligne.
  </behavior>
  <read_first>
    - packages/data-sources/src/oanda/client.ts (pattern fetch+Zod maison établi plan 02 — secret env, frontière, erreur explicite, pLimit∘pRetry)
    - packages/data-sources/src/oanda/schema.ts (pattern parse -> ligne typée)
    - packages/data-sources/src/index.ts (barrel à étendre)
    - packages/supabase/src/database.types.ts (NewsInsert, MacroSeriesInsert, EconomicCalendarInsert)
    - packages/core/src/time/candle.test.ts (style golden)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md (§Pitfall 2 Finnhub premium ; §Pitfall 5 FairEconomy cache ; §A5 DTWEXBGS proxy DXY ; §Code Examples FRED/FairEconomy/Finnhub)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md (§finnhub/client §clients fetch maux)
  </read_first>
  <action>
    Ajouter 4 clients au package `packages/data-sources` (installer le SDK : `pnpm --filter @app/data-sources add finnhub`) :
    - `finnhub/client.ts` : SDK Finnhub, `marketNews('crypto'|'forex'|'general')` UNIQUEMENT (D-28 ; PAS companyNews/news-sentiment — Pitfall 2). Clé via `FINNHUB_API_KEY` (throw si absent). pLimit(1) + pRetry (respect Retry-After). `finnhub/schema.ts` : Zod parse article + `parseFinnhubNews(articles, category): NewsInsert[]` — `url_hash` = hash déterministe de l'url (crypto.createHash sha256 hex), `sentiment` = champ provider tel quel ou null (D-30), `source='finnhub'`, `instrument_ids` mappé depuis la catégorie (laisser vide ou par convention catégorie->instruments si trivial ; ne PAS faire d'heuristique mots-clés — D-28).
    - `marketaux/client.ts` : fetch+Zod maison (squelette oanda/client.ts), `GET /v1/news/all?api_token=KEY&symbols=...`, clé `MARKETAUX_API_KEY`. Parse -> NewsInsert[] (`source='marketaux'`, même logique url_hash/sentiment). Sert de fallback (appelé par le job, pas en parallèle).
    - `fred/client.ts` : fetch+Zod maison, `GET https://api.stlouisfed.org/fred/series/observations?series_id=${code}&api_key=${KEY}&file_type=json&observation_start=${twoYearsAgoIso}`, clé `FRED_API_KEY`. `parseFredObservations(raw, seriesCode): MacroSeriesInsert[]` — ignorer `value === '.'` (manquant FRED), `value` Number, `ts` ISO UTC depuis date. Documenter en commentaire DTWEXBGS = proxy DXY (A5).
    - `faireconomy/client.ts` : fetch+Zod maison SANS clé, `GET https://nfs.faireconomy.media/ff_calendar_thisweek.json`. **Cache** : re-fetch 1×/jour max (Pitfall 5, rate limit 2/5min) — implémenter un cache simple (table `economic_calendar` sert déjà de cache de données, mais éviter de re-télécharger : garder un timestamp de dernier fetch en mémoire/fichier ou via la donnée existante ; documenter la stratégie). `parseFairEconomyCalendar(raw): EconomicCalendarInsert[]` — `event_key` déterministe (sha256 de title|country|date), `event_at` ISO UTC, `impact` validé dans High|Medium|Low (Zod), `source='faireconomy'`. Schema TOLÉRANT (flux communautaire, A3) : champs inattendus ignorés, parse défensif.
    - Étendre `index.ts` : exporter fetchFinnhubNews, parseFinnhubNews, fetchMarketauxNews, parseMarketauxNews, fetchFredSeries, parseFredObservations, fetchFairEconomyCalendar, parseFairEconomyCalendar.
    - Fixtures réelles réduites + golden tests `finnhub/schema.test.ts`, `fred/schema.test.ts`, `faireconomy/schema.test.ts` (déterministes hors-ligne ; FRED : prouver l'exclusion d'une observation '.').
  </action>
  <verify>
    <automated>pnpm vitest run packages/data-sources</automated>
  </verify>
  <acceptance_criteria>
    - `finnhub/client.ts` appelle `marketNews(category)` et JAMAIS `companyNews`/`news-sentiment`.
    - `parseFredObservations` exclut les observations `value === '.'` (prouvé par golden test).
    - `parseFairEconomyCalendar` produit un `event_key` déterministe et valide `impact` ∈ {High,Medium,Low} via Zod.
    - `faireconomy/client.ts` n'utilise aucune clé API et documente la stratégie de cache (re-fetch ≤ 1×/jour).
    - `pnpm vitest run packages/data-sources` passe (golden Finnhub + FRED + FairEconomy verts, + Binance/OANDA du plan 02 toujours verts).
  </acceptance_criteria>
  <done>4 clients news/macro/calendrier + parsers, golden tests verts, barrel étendu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jobs news/macro/calendar-ingest + enregistrement dispatcher</name>
  <files>apps/jobs/src/jobs/news-ingest.ts, apps/jobs/src/jobs/macro-ingest.ts, apps/jobs/src/jobs/calendar-ingest.ts, apps/jobs/src/dispatch.ts</files>
  <behavior>
    - news-ingest : tire Finnhub par catégorie ; si Finnhub échoue (clé invalide/rate-limit), bascule sur Marketaux (D-29) ; dédoublonne par url_hash (upsertNews) ; isolation par catégorie dans stats.errors.
    - macro-ingest : pour chaque série FRED dans son try/catch, upsertMacroSeries ; une série en échec n'interrompt pas les autres.
    - calendar-ingest : tire FairEconomy (caché), upsertEconomicCalendar par event_key.
    - Les 3 jobs sont résolvables via le dispatcher.
  </behavior>
  <read_first>
    - apps/jobs/src/jobs/market-ingest.ts (squelette exact à répliquer : client lazy, boucle try/catch par cible, retour stats — créé plan 02)
    - apps/jobs/src/jobs/heartbeat.ts (signature)
    - apps/jobs/src/dispatch.ts (registre — déjà étendu par market-ingest au plan 02)
    - packages/supabase/src/repositories/news.ts + macroSeries.ts + economicCalendar.ts (upsert — plan 01)
    - packages/data-sources/src/index.ts (clients Task 1)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-PATTERNS.md (§news/macro/calendar-ingest)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md (D-23 macro 2 ans / news au fil de l'eau ; D-28/29/30)
  </read_first>
  <action>
    Créer 3 jobs calqués sur `market-ingest.ts` (client service_role lazy, isolation par cible, retour `{ inserted, skipped, errors }`) :
    - `news-ingest.ts` `newsIngest()` : pour chaque catégorie pertinente ('crypto','forex','general') dans son try/catch : tenter Finnhub (`fetchFinnhubNews`+`parseFinnhubNews`) ; en cas d'échec/rate-limit, fallback Marketaux (D-29) dans le même bloc ; `upsertNews` (dédup url_hash). Erreur d'une catégorie => stats.errors, les autres continuent.
    - `macro-ingest.ts` `macroIngest()` : pour chaque série ['DFF','CPIAUCSL','DTWEXBGS','DFII10'] dans son try/catch : `fetchFredSeries`(2 ans)+`parseFredObservations` => `upsertMacroSeries`. Une série en échec n'interrompt pas les autres.
    - `calendar-ingest.ts` `calendarIngest()` : `fetchFairEconomyCalendar` (caché 1×/jour) + `parseFairEconomyCalendar` => `upsertEconomicCalendar`. try/catch global (source unique) => stats.errors si indisponible (Phase 3 dégradée, pas bloquant — A3).
    - Étendre `dispatch.ts` : importer les 3 jobs + ajouter `'news-ingest': newsIngest`, `'macro-ingest': macroIngest`, `'calendar-ingest': calendarIngest` au JOB_REGISTRY (conserver heartbeat + market-ingest).
  </action>
  <verify>
    <automated>grep -E "news-ingest|macro-ingest|calendar-ingest" apps/jobs/src/dispatch.ts | grep -c ":" # attendu >= 3</automated>
  </verify>
  <acceptance_criteria>
    - `newsIngest` tente Finnhub d'abord puis Marketaux en fallback (présence d'un chemin catch -> Marketaux dans le source).
    - `macroIngest` itère les 4 séries DFF/CPIAUCSL/DTWEXBGS/DFII10, chacune dans un try/catch.
    - Les 3 jobs retournent `{ inserted, skipped, errors }` et créent leur client service_role lazy (pas d'import service-client.ts).
    - `dispatch.ts` JOB_REGISTRY contient les clés 'news-ingest', 'macro-ingest', 'calendar-ingest' (+ heartbeat + market-ingest préservés).
  </acceptance_criteria>
  <done>3 jobs d'ingestion livrés et enregistrés ; DATA-03 et DATA-04 couvertes ; calendrier D-31 alimente Phase 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test d'isolation des pannes (DATA-07) — une source coupée n'interrompt pas les autres + staleness exposée</name>
  <files>apps/jobs/__tests__/fault-isolation.test.ts</files>
  <behavior>
    - Avec UNE source dont la clé est invalide (ex FRED_API_KEY bidon), le job concerné peuple stats.errors pour cette source MAIS les autres cibles ingèrent et le statut runJob reste 'success'.
    - La vue v_data_freshness est interrogeable et retourne is_stale (la staleness est exposée — contrat Phase 4).
  </behavior>
  <read_first>
    - apps/jobs/__tests__/runJob.test.ts (pattern intégration : dotenvConfig apps/jobs/.env, client service_role verify, cleanup createdIds delete().in)
    - packages/supabase/__tests__/rls.test.ts (pattern env + client service_role + afterAll cleanup)
    - apps/jobs/src/jobs/macro-ingest.ts (job à exercer ; le test simule une clé invalide pour UNE source)
    - apps/jobs/src/jobs/market-ingest.ts (isolation par instrument — référence)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-RESEARCH.md (§Validation Architecture DATA-07 ; §Pattern 2 isolation)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-VALIDATION.md (Wave 0 isolation des pannes)
  </read_first>
  <action>
    Écrire `apps/jobs/__tests__/fault-isolation.test.ts` (intégration, analogue runJob.test.ts) prouvant DATA-07 :
    - Charger `apps/jobs/.env` via dotenvConfig (pattern runJob.test.ts:15-21).
    - Cas 1 — isolation : exécuter un job multi-cible (ex macro-ingest, ou market-ingest sur un sous-ensemble) en injectant une clé/source invalide pour UNE cible (ex override `process.env['FRED_API_KEY']` temporairement à une valeur invalide, ou mocker un client qui throw pour une série) ; assert que `stats.errors` contient l'entrée de la source en échec ET que `stats.inserted`/les autres cibles ne sont pas nuls (les autres ont continué) ET que le job ne throw pas (status resterait 'success' dans runJob).
    - Cas 2 — staleness exposée : SELECT sur `v_data_freshness` retourne des lignes avec une colonne `is_stale` (booléen) consultable (le contrat que Phase 4 utilisera). Si l'environnement n'a pas de candles, asserter au minimum que la vue est interrogeable sans erreur.
    - Cleanup en afterAll via service_role (delete des lignes de test), restaurer les env vars modifiées (try/finally).
    - Skip propre si SUPABASE_URL/SERVICE_ROLE_KEY absents (pattern runJob.test.ts beforeAll).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/fault-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le test assert que `stats.errors` est peuplé pour la source en échec ET que les autres cibles ont ingéré (inserted > 0 ou skipped cohérent) ET que le job ne propage pas l'erreur.
    - Le test interroge `v_data_freshness` et lit une colonne `is_stale`.
    - Les env vars modifiées sont restaurées (try/finally) ; cleanup afterAll via service_role.
    - `pnpm vitest run apps/jobs/__tests__/fault-isolation.test.ts` passe (DATA-07 prouvée).
  </acceptance_criteria>
  <done>DATA-07 prouvée par test : source coupée => les autres ingèrent + staleness exposée. Avec l'idempotence du plan 01, les deux tests qui définissent la fiabilité de la phase sont verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API news/macro/calendrier -> job | réponses non fiables (dont flux communautaire FairEconomy), parse Zod tolérant obligatoire |
| job (service_role) -> Postgres | écriture bypass RLS, confinée apps/jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | empoisonnement via source FairEconomy compromise | mitigate | schéma Zod tolérant + champ `source='faireconomy'` ; impact borné (calendrier informatif) ; impact validé enum |
| T-02-09 | Information Disclosure | FINNHUB/MARKETAUX/FRED_API_KEY exposées | mitigate | clés lues depuis apps/jobs/.env (throw si absentes), jamais loggées, jamais NEXT_PUBLIC_ |
| T-02-10 | Denial of Service | rate limit FairEconomy (2/5min) / Finnhub (60/min) | mitigate | cache FairEconomy 1×/jour ; Finnhub pLimit(1)+Retry-After ; FRED pLimit(2) ; Marketaux en fallback seulement (D-29) |
| T-02-11 | Tampering | payload news/macro malformé en base | mitigate | parse Zod à chaque frontière (finnhub/fred/faireconomy/marketaux schema) avant upsert |
| T-02-SC | Tampering | install finnhub | mitigate | paquet officiel verrouillé CLAUDE.md + audité RESEARCH §Package Legitimacy (Finnhub-Stock-API officiel) ; aucun [SUS]/[SLOP] => pas de checkpoint requis |
</threat_model>

<verification>
- `pnpm vitest run packages/data-sources` vert (golden des 5 sources).
- `pnpm vitest run apps/jobs/__tests__/fault-isolation.test.ts` vert (DATA-07).
- `pnpm vitest run` complet vert (idempotency DATA-06 + fault-isolation DATA-07 = contrat de fiabilité de la phase).
- Les 4 jobs (`market/news/macro/calendar-ingest`) résolvables via le dispatcher.
</verification>

<success_criteria>
- DATA-03 (news + sentiment par catégorie, Finnhub primaire / Marketaux fallback) ingérée.
- DATA-04 (FRED DFF/CPIAUCSL/DTWEXBGS/DFII10 sur 2 ans) ingérée.
- D-31 (calendrier économique FairEconomy) alimente Phase 3.
- DATA-07 prouvée : source en échec => les autres ingèrent + staleness exposée.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-fiable-des-donn-es/02-03-SUMMARY.md` when done
</output>
