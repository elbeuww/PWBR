---
phase: 19-dashboard-utilisateur
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
  - apps/web/src/components/dash/DashShell.tsx
  - apps/web/src/app/[locale]/(dash)/layout.tsx
autonomous: true
requirements: [UDASH-04]
user_setup: []

must_haves:
  truths:
    - "Le groupe (dash) est gardé par requireUser (auth seul, PAS requireActiveSub) — un abonné expiré entre pour renouveler (D-03)"
    - "ExpiryBanner (J-3/J-1) est rendu dans le shell (dash) — dette WIRING-01 levée (UDASH-04)"
    - "DashShell rend une sidebar persistante (desktop) + bottom-nav (mobile) avec 6 onglets, item actif en accent fill/underline SANS glow (D-01)"
    - "Le shell (dash) monte les composants existants (SignalList, RevenueTabs, ExpiryBanner) sans réimplémentation, et remplace le stub /dashboard (D-02 : agrégation, zéro réimplémentation)"
    - "Le namespace i18n `dash` existe à parité STRICTE fr/en/ar (toutes les clés de la phase : nav, overview, suivis, historique, watchlist, settings, affiliation, états)"
    - "RTL-safe : propriétés logiques uniquement (ms/me/ps/pe/start/end), aucune classe directionnelle physique"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dash)/layout.tsx"
      provides: "gate requireUser + lecture subscriptions.current_period_end + ExpiryBanner + DashShell"
      contains: "requireUser"
    - path: "apps/web/src/components/dash/DashShell.tsx"
      provides: "sidebar + bottom-nav 6 onglets, item actif sans glow"
      contains: "usePathname"
    - path: "apps/web/src/messages/fr.json"
      provides: "namespace dash complet (canonique FR, UI-SPEC Copywriting)"
      contains: "dash"
  key_links:
    - from: "(dash)/layout.tsx"
      to: "ExpiryBanner"
      via: "lecture subscriptions.current_period_end anon-client + rendu"
      pattern: "ExpiryBanner"
    - from: "(dash)/layout.tsx"
      to: "requireUser"
      via: "gate au layout (PAS requireActiveSub)"
      pattern: "requireUser"
---

<objective>
Poser le shell `(dash)` : layout gardé `requireUser`, montage de l'`ExpiryBanner` (résout la dette WIRING-01), composant `DashShell` (sidebar desktop + bottom-nav mobile, 6 onglets), et l'INTÉGRALITÉ du namespace i18n `dash` à parité fr/en/ar (consommé par tous les plans aval).

Purpose: Le shell est la fondation UI agrégeante (D-02 : monter, jamais réimplémenter). Le gate `requireUser` (et non `requireActiveSub`) laisse entrer un abonné EXPIRÉ pour renouveler (D-03). Définir tout le namespace i18n ici évite que plusieurs plans aval éditent les mêmes fichiers messages (parité stricte préservée).
Output: layout (dash) + DashShell + namespace dash fr/en/ar complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md

<interfaces>
<!-- gate.ts (lib/auth/gate.ts) — porte UX, server-only -->
export async function requireUser(): Promise<User>          // (dash) : auth seul (D-03)
export async function requireActiveSub(): Promise<User>     // NE PAS utiliser ici
export async function requireRole(role: 'superadmin' | 'affiliate'): Promise<User> // REDIRIGE

<!-- ExpiryBanner (components/member/ExpiryBanner.tsx) — 'use client' -->
interface ExpiryBannerProps { currentPeriodEnd: string | null; windowDays?: number }
// n'affiche rien hors fenêtre J-3/J-1 ou si null ; CTA payment.renew → /tarifs

<!-- i18n navigation (i18n/navigation.ts) -->
import { Link, usePathname, useRouter } from '@/i18n/navigation'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace i18n `dash` complet (parité stricte fr/en/ar)</name>
  <files>apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/messages/fr.json (structure existante des namespaces ; ne pas casser les clés présentes)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Copywriting Contract §104-118 — copy FR canonique : CTA renouveler, empty Suivis/Historique, renewal state, affiliation summary, error, watchlist aria)
    - apps/web/src/components/LanguageSwitcher.tsx (référence parité/usage des clés)
  </read_first>
  <action>
    Ajouter un namespace `dash` aux 3 fichiers `messages/{fr,en,ar}.json` à parité de clés STRICTE (P01 D-01-02-A). Source FR canonique = UI-SPEC Copywriting Contract. Clés à couvrir (toutes définies ICI pour que les plans aval n'éditent plus ces fichiers) :
    - `dash.nav.*` : overview, suivis, historique, watchlist, abonnement, affiliation, parametres (labels d'onglets).
    - `dash.overview.*` : title (« cockpit »), subscriptionStatus, latestSignals, shortcuts.
    - `dash.suivis.emptyTitle` = « Aucun signal suivi », `dash.suivis.emptyBody` (toucher l'étoile…), `dash.historique.emptyTitle` = « Aucun signal clôturé », `dash.historique.emptyBody`.
    - `dash.renewal.title` = « Votre abonnement a expiré », `dash.renewal.body`, `dash.renewal.cta` = « Renouveler l'abonnement » (état RENOUVELLEMENT, jamais upsell gratuit, D-03).
    - `dash.watchlist.add` / `dash.watchlist.remove` (aria-label toggle étoile), `dash.watchlist.error` (toast rollback).
    - `dash.affiliate.summary` (« X abonnés ramenés · Y revenus mesurés »), `dash.affiliate.link` (« Voir mon tableau d'affiliation »).
    - `dash.error.title` = « Impossible de charger vos données », `dash.error.body`, `dash.error.retry` = « Réessayer ».
    - `dash.settings.*` : account, email, changePassword, language, notifications, manageSubscription, signOut, savePreferences.
    Pas de toggle thème (forcedTheme dark, D-11). AR = traduction réelle (pas de placeholder), Noto Sans Arabic via :lang(ar).
  </action>
  <acceptance_criteria>
    - Les 3 fichiers contiennent un namespace `dash` avec EXACTEMENT le même ensemble de clés (parité)
    - Le test de parité des messages passe : `pnpm vitest run apps/web/src/__tests__/*parity* 2>/dev/null || pnpm test` ne signale aucune clé manquante dans une langue
    - `dash.renewal.cta` contient « Renouveler » (FR) — état renouvellement, aucune occurrence de « gratuit »/« free » dans le namespace dash
    - `lint:i18n` (script `pnpm lint:i18n`) exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint:i18n</automated>
  </verify>
  <done>Namespace dash complet, à parité fr/en/ar, copy canonique UI-SPEC respectée.</done>
</task>

<task type="auto">
  <name>Task 2: Composant DashShell (sidebar desktop + bottom-nav mobile, sans glow)</name>
  <files>apps/web/src/components/dash/DashShell.tsx</files>
  <read_first>
    - apps/web/src/components/LanguageSwitcher.tsx (nav client a11y + propriétés logiques L.183/200 ; usePathname/useRouter depuis i18n/navigation L.20 ; hit-area min-h-11 min-w-11 L.168)
    - apps/web/src/components/signals/SignalCard.tsx (CONTRASTE : glowClass autorisé sur surface de valeur L.79-81 — NE PAS reproduire sur la nav, D-01)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Color §86-93 accent réservé : item actif = fill/underline subtil, focus ring ; Spacing/Typography)
  </read_first>
  <action>
    Créer `apps/web/src/components/dash/DashShell.tsx` (`'use client'`). Sidebar latérale persistante en desktop (`md:`+) + bottom-nav fixe en mobile, listant les 6 onglets via `dash.nav.*`. Liens via `Link` de `@/i18n/navigation` ; item actif détecté par `usePathname()` (comparaison de segment). Item actif = accent `--primary` en fill/underline SUBTIL (ex. `bg-[var(--primary)]/10 text-[var(--primary)]` ou bordure inline), JAMAIS de glow/néon (D-01, Tier App lisibilité d'abord). Focus ring = `--ring`. Propriétés logiques uniquement (`ms`/`me`/`ps`/`pe`/`start`/`end`, `inset-inline`), aucune classe physique left/right. Hit-area ≥44px par item en mobile. Tokens Tailwind v4 `bg-[var(--token)]` (jamais `bg-[--token]`, CR-01 P16). Le composant reçoit `{ children }` et place `{children}` dans la zone de contenu. Icônes : `lucide-react` si l'import se résout (vérifier), sinon SVG inline (précédent LanguageSwitcher).
  </action>
  <acceptance_criteria>
    - `grep -c "glow" apps/web/src/components/dash/DashShell.tsx` == 0 (aucun glow sur la nav, D-01)
    - `grep -E "text-(left|right)|[mp][lr]-" apps/web/src/components/dash/DashShell.tsx` == 0 (aucune classe directionnelle physique)
    - `grep -c "bg-\[--" apps/web/src/components/dash/DashShell.tsx` == 0 (token correct `bg-[var(--…)]`)
    - DashShell importe `usePathname` et `Link` depuis `@/i18n/navigation` et liste 6 onglets (`dash.nav.`)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>DashShell rendu sidebar+bottom-nav, item actif sans glow, RTL-safe, 6 onglets i18n.</done>
</task>

<task type="auto">
  <name>Task 3: Layout (dash) — requireUser + ExpiryBanner (WIRING-01) + DashShell</name>
  <files>apps/web/src/app/[locale]/(dash)/layout.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/layout.tsx (calque EXACT : requireActiveSub→requireUser, lecture subscriptions.current_period_end L.22-30, montage ExpiryBanner L.33-34)
    - apps/web/src/lib/auth/gate.ts (requireUser L.78-81 ; NE PAS utiliser requireActiveSub — ne pas affaiblir le gate des signaux (member))
    - apps/web/src/components/member/ExpiryBanner.tsx (props currentPeriodEnd)
    - apps/web/src/components/dash/DashShell.tsx (le shell de Task 2)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/(dash)/layout.tsx` en calquant `(member)/layout.tsx` MAIS en swappant `requireActiveSub` → `requireUser` (D-03 : l'abonné expiré entre pour renouveler ; la RLS reste la vraie barrière sur les pages enfant). Lire `subscriptions.current_period_end` via `createClient()` anon-client RSC (verbatim du member layout, scope user_id = auth.uid()), passer à `<ExpiryBanner currentPeriodEnd={sub?.current_period_end ?? null} />` rendu EN TÊTE (D-08 — résout WIRING-01). Envelopper `{children}` dans `<DashShell>`. Aucun service_role. Le gate `requireActiveSub` du groupe `(member)` (signaux) reste INTACT — ne pas déplacer ces routes ici.
  </action>
  <acceptance_criteria>
    - `grep -c "requireUser" apps/web/src/app/[locale]/(dash)/layout.tsx` >= 1 ET `grep -c "requireActiveSub" .../(dash)/layout.tsx` == 0
    - `grep -c "ExpiryBanner" apps/web/src/app/[locale]/(dash)/layout.tsx` >= 1 (WIRING-01 levée)
    - le layout n'importe aucun client service_role (`grep -c "admin-service\|service_role\|createAdminServiceClient" .../(dash)/layout.tsx` == 0)
    - `apps/web/src/app/[locale]/(member)/layout.tsx` reste inchangé (toujours `requireActiveSub`)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Layout (dash) gardé requireUser, ExpiryBanner câblé (WIRING-01), DashShell monté, gate (member) intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → RSC (dash) | requête membre franchit le gate UX puis la RLS |
| RSC (dash) → Postgres RLS | lecture subscriptions scopée auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Elevation | Accès (dash) sans session | mitigate | requireUser au layout (redirect login + returnTo same-origin) ; RLS = vraie barrière sur enfants |
| T-19-06 | Elevation | Affaiblir le gate des signaux en déplaçant (member) sous requireUser | mitigate | (member)/layout.tsx INTACT (requireActiveSub) ; (dash) n'héberge pas les routes signaux gated |
| T-19-07 | Information Disclosure | Lire l'abonnement d'un autre | mitigate | lecture subscriptions via anon-client RLS scope user_id = auth.uid(), jamais service_role |
| T-19-08 | Tampering | Injection texte JSX hors i18n | mitigate | toutes chaînes via namespace dash next-intl ; lint:i18n exit 0 |
| T-19-SC | Tampering | npm installs | accept | Aucun package installé (réutilisation stack verrouillée) |
</threat_model>

<verification>
- `pnpm lint:i18n` exit 0 (parité dash + aucun texte en dur).
- `pnpm typecheck` vert.
- Greps : 0 glow nav, 0 classe physique, 0 service_role, requireUser présent + ExpiryBanner présent.
</verification>

<success_criteria>
Shell (dash) gardé requireUser avec ExpiryBanner câblé (WIRING-01), DashShell sidebar+bottom-nav sans glow RTL-safe, namespace i18n dash complet à parité fr/en/ar.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-02-SUMMARY.md` when done
</output>
