---
phase: 19-dashboard-utilisateur
plan: 07
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - apps/web/src/components/dash/KeysetList.tsx
  - apps/web/src/app/[locale]/(dash)/suivis/page.tsx
  - apps/web/src/app/[locale]/(dash)/historique/page.tsx
  - apps/web/src/app/[locale]/(dash)/watchlist/page.tsx
autonomous: true
requirements: [UDASH-02]
user_setup: []

must_haves:
  truths:
    - "La page Suivis liste les setups ouverts (status=active) bookmarkés, paginés keyset par curseur (D-04/D-05, UDASH-02)"
    - "La page Historique liste les mêmes setups une fois clôturés (invalidated/expired) avec leur issue (prediction_outcomes), paginés keyset"
    - "Le lien « page suivante » porte ?cursor=<opaque> (nextCursor de fetchFollowedSetups) ; pas d'OFFSET"
    - "Chaque surface a 4 états : loading (Skeleton), empty (copy contrat), error (retry), renewal (abonné expiré → 0 ligne via RLS)"
    - "watchlist (route nav) pointe sur la vue Suivis (fusion D-04) — pas de module séparé lourd"
  artifacts:
    - path: "apps/web/src/components/dash/KeysetList.tsx"
      provides: "liste paginée curseur (lignes followed + lien page suivante)"
      contains: "cursor"
    - path: "apps/web/src/app/[locale]/(dash)/suivis/page.tsx"
      provides: "Suivis (status=active) keyset"
      contains: "fetchFollowedSetups"
    - path: "apps/web/src/app/[locale]/(dash)/historique/page.tsx"
      provides: "Historique (clôturés + issue) keyset"
      contains: "fetchFollowedSetups"
  key_links:
    - from: "suivis/page.tsx"
      to: "fetchFollowedSetups(status='active', cursor)"
      via: "anon-client RSC + parse cursor"
      pattern: "fetchFollowedSetups"
    - from: "KeysetList"
      to: "?cursor=nextCursor"
      via: "lien page suivante (pas OFFSET)"
      pattern: "cursor"
---

<objective>
Livrer les surfaces Suivis et Historique (UDASH-02) : source unique `user_followed_setups ⋈ trade_setups` filtrée par statut, paginée par curseur keyset, avec les 4 états (loading/empty/error/renewal). Le `KeysetList` rend les lignes + le lien « page suivante » par curseur opaque.

Purpose: D-04 (source unique, pas de table view-log) + D-05 (keyset, pas OFFSET → Index Scan). Le `!inner` sous RLS donne la barrière renouvellement gratuite (abonné expiré → 0 ligne). La route nav « watchlist » fusionne avec Suivis (geste léger, pas un module lourd).
Output: KeysetList + suivis/page + historique/page + watchlist/page (→ Suivis).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-utilisateur/19-RESEARCH.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md
@.planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md

<interfaces>
<!-- watchlist queries (lib/watchlist/queries.ts — 19-03) -->
export async function fetchFollowedSetups(supabase, { status, cursor }): Promise<{ data: FollowedRow[]; nextCursor: string | null; error: string | null }>
// Suivis : status='active' ; Historique : status in ('invalidated','expired') + outcome/realized_r

<!-- cursor (lib/keyset/cursor.ts — 19-03) + searchParams (cursor/tab) -->
import { decodeCursor } from '@/lib/keyset/cursor'

<!-- i18n (namespace dash — 19-02) -->
// dash.suivis.emptyTitle/emptyBody, dash.historique.emptyTitle/emptyBody, dash.error.*, dash.renewal.*

<!-- états (UI-SPEC §130/135) : loading Skeleton, empty copy, error retry, renewal expired -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KeysetList (liste paginée curseur + lien page suivante)</name>
  <files>apps/web/src/components/dash/KeysetList.tsx</files>
  <read_first>
    - apps/web/src/components/signals/SignalList.tsx (structure liste + Skeleton L.154-166)
    - apps/web/src/components/ui/skeleton.tsx, apps/web/src/components/ui/card.tsx
    - apps/web/src/app/[locale]/affiliation/dashboard/page.tsx (rendu issue/montants <bdi>, table simple si besoin)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Suivis/Historique §130 ; états §135 ; typo numerals font-mono §71)
  </read_first>
  <action>
    Créer `apps/web/src/components/dash/KeysetList.tsx` (server-renderable ; pas de react-query nécessaire — pagination par lien URL). Props : `{ rows: FollowedRow[]; nextCursor: string | null; basePath: string; locale: string; variant: 'suivis' | 'historique' }`. Rendre une grille/table de lignes : symbole (font-mono), direction (token --signal-* — direction SEULE, D-05 P10), score/risque, et pour `historique` l'issue (`outcome` hit_tp/hit_sl/flat + `realized_r` via <bdi>, JAMAIS un % de gain agrégé fabriqué). Si `nextCursor` non null → un `<Link href={basePath + '?cursor=' + nextCursor}>` « page suivante » (PAS d'OFFSET, D-05). Empty (rows.length===0) → bloc copy selon `variant` (dash.suivis.*/dash.historique.*). RTL-safe (propriétés logiques), tokens `bg-[var(--token)]`. Lignes « followed » → toutes par définition (option : monter WatchlistToggle pour désuivre, mais non requis ici — éviter le couplage ; lien vers le détail où vit le toggle).
  </action>
  <acceptance_criteria>
    - `grep -c "cursor" apps/web/src/components/dash/KeysetList.tsx` >= 1 (lien page suivante par curseur)
    - `grep -Eic "offset|range\(" KeysetList.tsx` == 0 (pas d'OFFSET — D-05)
    - aucune classe directionnelle physique : `grep -E "text-(left|right)|[mp][lr]-" KeysetList.tsx` == 0
    - variant historique rend l'issue (`grep -c "outcome\|realized_r" KeysetList.tsx` >= 1)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>KeysetList rend lignes + lien page suivante par curseur, issue en historique, RTL-safe, pas d'OFFSET.</done>
</task>

<task type="auto">
  <name>Task 2: Pages Suivis + Historique (keyset, 4 états)</name>
  <files>apps/web/src/app/[locale]/(dash)/suivis/page.tsx, apps/web/src/app/[locale]/(dash)/historique/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (structure RSC : setRequestLocale+getTranslations+createClient+parse searchParams L.32-39 ; 3 états error/empty/contenu L.54-75)
    - apps/web/src/lib/watchlist/queries.ts (fetchFollowedSetups — 19-03)
    - apps/web/src/lib/keyset/cursor.ts + apps/web/src/lib/signals/searchParams.ts (decodeCursor + param cursor — 19-03)
    - apps/web/src/components/dash/KeysetList.tsx (Task 1)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (états §135 dont renewal)
  </read_first>
  <action>
    Créer `(dash)/suivis/page.tsx` et `(dash)/historique/page.tsx` (RSC, gate déjà au layout). Lire `cursor` depuis `searchParams` (param whitelisté 19-03), `createClient()` anon-client RSC. Appeler `fetchFollowedSetups(supabase, { status, cursor })` : Suivis → `status: 'active'` ; Historique → `status: ['invalidated','expired']` (+ issue). Rendre `<KeysetList rows nextCursor basePath variant locale>`. 4 ÉTATS (UI-SPEC §135) : loading (Skeleton — RSC : pendant le fetch, ou suspense/skeleton fallback), empty (copy dash.suivis/historique.*), error (dash.error.* + retry), RENEWAL : si l'user est abonné expiré, le `!inner` RLS renvoie 0 ligne → afficher l'état `dash.renewal.*` (« Renouvelle pour réaccéder ») au lieu d'un simple empty (distinguer expiré vs jamais suivi — vérifier le statut d'abonnement pour choisir empty vs renewal). setRequestLocale + getTranslations('dash').
  </action>
  <acceptance_criteria>
    - `grep -c "fetchFollowedSetups" apps/web/src/app/[locale]/(dash)/suivis/page.tsx` >= 1 ET dans historique/page.tsx >= 1
    - suivis filtre `status` actif ; historique filtre clôturés (invalidated/expired)
    - `grep -c "decodeCursor\|cursor" suivis/page.tsx` >= 1 (curseur lu de l'URL)
    - chaque page gère renewal : `grep -c "renewal" suivis/page.tsx` >= 1 (état abonné expiré, D-03)
    - aucune réimplémentation de la requête (passe par fetchFollowedSetups, pas de from('user_followed_setups') inline) : `grep -c "from('user_followed_setups')\|from(\"user_followed_setups\")" suivis/page.tsx` == 0
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Suivis/Historique keyset, 4 états dont renewal, requête déléguée à fetchFollowedSetups.</done>
</task>

<task type="auto">
  <name>Task 3: Route watchlist → vue Suivis (fusion D-04)</name>
  <files>apps/web/src/app/[locale]/(dash)/watchlist/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(dash)/suivis/page.tsx (Task 2 — la vue à réutiliser/rediriger)
    - apps/web/src/app/[locale]/(auth)/actions.ts (pattern redirect localisé)
  </read_first>
  <action>
    Créer `(dash)/watchlist/page.tsx`. DÉCISION (D-04 fusion, Claude's Discretion) : la « watchlist » = les Suivis (setups ouverts bookmarkés). Faire de cette route soit une redirection localisée vers `/suivis`, soit un ré-export de la page Suivis pour éviter la duplication. NE PAS créer un module watchlist séparé lourd (le geste watchlist se vit via l'étoile — 19-06). Le but : la nav `dash.nav.watchlist` mène à une vue cohérente sans réimplémenter la liste. Documenter le choix (redirection vs alias) dans le SUMMARY.
  </action>
  <acceptance_criteria>
    - `(dash)/watchlist/page.tsx` ne duplique pas la requête : soit `redirect` vers /suivis (`grep -c "redirect" watchlist/page.tsx` >= 1), soit ré-export de la page Suivis (pas de second `fetchFollowedSetups` inline divergent)
    - aucune réimplémentation de KeysetList/liste
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Route watchlist = vue Suivis (redirection ou alias), zéro duplication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (cursor) → parse → requête | param non fiable franchit ici |
| RSC (dash) → Postgres RLS | lecture suivis/historique scopée auth.uid() + has_active_subscription() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-26 | Information Disclosure | Abonné expiré accède à ses signaux suivis | mitigate | trade_setups!inner sous RLS has_active_subscription() → 0 ligne ; état renewal affiché (D-03) ; barrière = RLS, jamais le gate UX |
| T-19-27 | Tampering | Curseur falsifié pour exfiltrer d'autres lignes | accept | curseur opaque ; RLS scope toujours à auth.uid() → offset incohérent au pire, pas d'élévation |
| T-19-28 | DoS | Seq scan à l'échelle (OFFSET) | mitigate | keyset uniquement (grep offset == 0) ; index 19-01 → Index Scan |
| T-19-29 | Integrity | % de gain fabriqué dans l'historique | mitigate | issue = outcome/realized_r mesurés ; aucun agrégat de gain (D-09) |
| T-19-30 | Tampering | Réimplémentation de la liste (divergence source) | mitigate | délégué à fetchFollowedSetups ; KeysetList unique ; pas de from() inline |
| T-19-SC | Tampering | npm installs | accept | Aucun package ajouté |
</threat_model>

<verification>
- `pnpm typecheck` vert.
- Greps : fetchFollowedSetups présent (pas de from() inline), 0 offset, renewal présent, watchlist sans duplication.
- Gate Manual-Only (cf. 19-VALIDATION) : EXPLAIN keyset Index Scan + RLS abonné expiré → 0 ligne (A2) via MCP en fin de phase.
</verification>

<success_criteria>
Suivis/Historique source unique keyset (4 états dont renewal), KeysetList unique sans OFFSET, route watchlist = Suivis sans duplication, requêtes déléguées à fetchFollowedSetups.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-07-SUMMARY.md` when done
</output>
