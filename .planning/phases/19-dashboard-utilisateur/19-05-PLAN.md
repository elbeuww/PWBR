---
phase: 19-dashboard-utilisateur
plan: 05
type: execute
wave: 2
depends_on: ["19-02"]
files_modified:
  - apps/web/src/app/[locale]/(dash)/parametres/page.tsx
  - apps/web/src/components/dash/PasswordChangeForm.tsx
  - apps/web/src/app/[locale]/dashboard/page.tsx
autonomous: true
requirements: [UDASH-06]
user_setup: []

must_haves:
  truths:
    - "La page (dash)/parametres rend : Compte (email affiché + changement mdp via Supabase auth) + Langue (LanguageSwitcher existant, PAS de toggle thème) + Notifications (préférences UI seules, aucune delivery) + lien gestion abonnement + déconnexion (UDASH-06, D-11)"
    - "Le changement de mot de passe utilise supabase.auth.updateUser({password}) côté client (D-11)"
    - "Les préférences notifications sont UI seules — aucune infra d'envoi (D-12, hors scope)"
    - "L'ancien stub /dashboard (liste instruments) est remplacé par une redirection localisée vers l'overview (dash)"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dash)/parametres/page.tsx"
      provides: "écran paramètres (compte/langue/notifications/abonnement/déconnexion)"
      contains: "LanguageSwitcher"
    - path: "apps/web/src/components/dash/PasswordChangeForm.tsx"
      provides: "changement mdp client supabase.auth.updateUser"
      contains: "updateUser"
    - path: "apps/web/src/app/[locale]/dashboard/page.tsx"
      provides: "redirection localisée vers (dash) (stub remplacé)"
      contains: "redirect"
  key_links:
    - from: "(dash)/parametres/page.tsx"
      to: "LanguageSwitcher"
      via: "montage du composant existant"
      pattern: "LanguageSwitcher"
    - from: "PasswordChangeForm"
      to: "supabase.auth.updateUser"
      via: "client @/lib/supabase/client"
      pattern: "updateUser"
---

<objective>
Livrer l'écran Paramètres de compte (UDASH-06) et remplacer le stub `/dashboard` (liste `instruments`) par une redirection vers l'overview `(dash)`.

Purpose: Paramètres = Compte (email + mdp) + Langue + Notifications (UI seule, D-12) + lien abonnement + déconnexion (D-11). Aucune infra de delivery notifications (hors scope, déféré). Le stub `/dashboard` est obsolète (remplacé par le shell agrégé).
Output: parametres/page.tsx + PasswordChangeForm + redirection stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md

<interfaces>
<!-- signOut server action ((auth)/actions.ts) -->
import { signOut } from '../(auth)/actions'   // <form action={signOut}>

<!-- changement mdp (client) -->
import { createClient } from '@/lib/supabase/client'
const { error } = await supabase.auth.updateUser({ password: newPassword })

<!-- LanguageSwitcher existant (Don't Hand-Roll) -->
import { LanguageSwitcher } from '@/components/LanguageSwitcher'

<!-- redirection localisée (i18n/navigation) -->
import { redirect } from '../../../i18n/navigation'   // redirect({ href, locale })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PasswordChangeForm (client, supabase.auth.updateUser)</name>
  <files>apps/web/src/components/dash/PasswordChangeForm.tsx</files>
  <read_first>
    - apps/web/src/components/signals/SignalList.tsx (pattern 'use client' + createClient browser L.1-33/51)
    - apps/web/src/lib/supabase/client.ts (createClient navigateur)
    - apps/web/src/components/ui/input.tsx, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/form.tsx (primitives vendored)
    - .planning/phases/19-dashboard-utilisateur/19-RESEARCH.md (§362-365 updateUser)
  </read_first>
  <action>
    Créer `apps/web/src/components/dash/PasswordChangeForm.tsx` (`'use client'`). Formulaire : nouveau mot de passe (+ confirmation), validation côté client (longueur min, égalité), soumission via `createClient()` navigateur (`@/lib/supabase/client`) `await supabase.auth.updateUser({ password })`. Succès → toast/message i18n ; erreur → message i18n (narrow `error`). Chaînes via `dash.settings.*`. Propriétés logiques, tokens `bg-[var(--token)]`. Aucun secret, aucun service_role.
  </action>
  <acceptance_criteria>
    - `grep -c "auth.updateUser" apps/web/src/components/dash/PasswordChangeForm.tsx` >= 1
    - `grep -c "'use client'" PasswordChangeForm.tsx` >= 1
    - importe `createClient` depuis `@/lib/supabase/client` (navigateur), aucun import service_role
    - chaînes via `dash.settings` (aucun texte JSX en dur) — `pnpm lint:i18n` exit 0
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Formulaire mdp client via updateUser, i18n, sans service_role.</done>
</task>

<task type="auto">
  <name>Task 2: Page (dash)/parametres (compte/langue/notifications/abonnement/déconnexion)</name>
  <files>apps/web/src/app/[locale]/(dash)/parametres/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(account)/abonnement/page.tsx (shell RSC main max-width + text-start + header + filet --primary L.55-63 ; requireUser L.44 ; lecture sub pour lien gestion)
    - apps/web/src/components/LanguageSwitcher.tsx (composant à monter — Don't Hand-Roll)
    - apps/web/src/app/[locale]/dashboard/page.tsx (pattern <form action={signOut}> L.47-54)
    - apps/web/src/components/dash/PasswordChangeForm.tsx (Task 1)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Settings §132 contrat)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/(dash)/parametres/page.tsx` (RSC, gate déjà au layout (dash)). Sections via `dash.settings.*` :
    - **Compte** : email affiché (lu via `getUser()` anon-client) + `<PasswordChangeForm />`.
    - **Langue & préférences** : monter `<LanguageSwitcher />` existant. PAS de toggle thème (forcedTheme dark, D-11).
    - **Notifications** : préférences UI SEULES (cases/switches), AUCUNE delivery, aucune écriture d'infra d'envoi (D-12 — hors scope ; possible stockage local/préférence sans moteur). Indiquer clairement que c'est un écran de préférences.
    - **Abonnement** : lien gestion → `/abonnement` (ou onglet (dash)/abonnement).
    - **Déconnexion** : `<form action={signOut}>` (action existante (auth)/actions.ts), bouton direct (réversible, pas de modale — UI-SPEC §116).
    Propriétés logiques, tokens corrects, header avec filet `--primary`.
  </action>
  <acceptance_criteria>
    - `grep -c "LanguageSwitcher" apps/web/src/app/[locale]/(dash)/parametres/page.tsx` >= 1
    - `grep -c "PasswordChangeForm" parametres/page.tsx` >= 1
    - `grep -c "signOut" parametres/page.tsx` >= 1
    - aucun toggle thème : `grep -Eic "theme.?toggle|ThemeToggle|setTheme" parametres/page.tsx` == 0 (D-11)
    - aucune infra d'envoi notifications (pas d'appel email/push/edge function) — préférences UI seules
    - `pnpm lint:i18n` exit 0 ; `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Écran paramètres complet (compte/langue/notifications UI/abonnement/déconnexion), sans thème, sans delivery.</done>
</task>

<task type="auto">
  <name>Task 3: Remplacer le stub /dashboard par une redirection vers (dash)</name>
  <files>apps/web/src/app/[locale]/dashboard/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/dashboard/page.tsx (stub actuel : liste instruments L.16-93 — à remplacer)
    - apps/web/src/app/[locale]/(auth)/actions.ts (pattern redirect localisé redirect({ href, locale }))
  </read_first>
  <action>
    Remplacer le corps de `apps/web/src/app/[locale]/dashboard/page.tsx` (liste `instruments`) par une redirection localisée vers l'overview `(dash)` (note : `(dash)` est un groupe de routes → l'URL cible est `/` du segment dash, soit la racine localisée du shell). Utiliser `redirect({ href: '<route overview (dash)>', locale })` (pattern (auth)/actions.ts). Auditer les `href="/dashboard"` internes pointant vers l'ancien stub : si l'URL `/dashboard` doit rester l'entrée du dashboard, faire pointer le groupe `(dash)` sur `/dashboard` (route racine du groupe) plutôt qu'une redirection en boucle — DÉCISION planner : si le groupe `(dash)` produit déjà `/dashboard` comme racine, supprimer ce stub et laisser `(dash)/page.tsx` (19-04) servir `/dashboard` ; sinon rediriger. Documenter le choix dans le SUMMARY. Ne pas laisser de boucle de redirection.
  </action>
  <acceptance_criteria>
    - `apps/web/src/app/[locale]/dashboard/page.tsx` ne contient plus la liste instruments : `grep -c "listActiveInstruments\|instrumentsTitle" dashboard/page.tsx` == 0
    - soit redirection (`grep -c "redirect" dashboard/page.tsx` >= 1) soit fichier supprimé/remplacé par l'overview (dash) sans boucle
    - aucun lien interne ne mène à une page « liste instruments » (audit href="/dashboard")
    - `pnpm typecheck` vert ; aucune route en double conflictuelle (`/dashboard` servi par une seule source)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Stub /dashboard remplacé (redirection ou racine (dash)), aucune boucle, aucun lien orphelin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → Supabase Auth | changement mdp / déconnexion |
| RSC (dash) → Postgres RLS | lecture email/abonnement scopée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-17 | Spoofing/Tampering | Changement mdp d'un autre compte | mitigate | supabase.auth.updateUser opère sur la session courante (token revalidé) ; aucun user_id reçu |
| T-19-18 | Information Disclosure | Fuite email d'autrui | mitigate | email lu via getUser() (session courante), jamais service_role |
| T-19-19 | Tampering | Texte JSX en dur (i18n bypass) | mitigate | toutes chaînes via dash.settings ; lint:i18n exit 0 |
| T-19-20 | Confusion scope | Notifications « delivery » non livrée prise pour active | accept | UI préférences seules, libellé clair ; infra d'envoi déférée (D-12) |
| T-19-SC | Tampering | npm installs | accept | Aucun package ajouté |
</threat_model>

<verification>
- `pnpm typecheck` vert ; `pnpm lint:i18n` exit 0.
- Greps : updateUser présent, LanguageSwitcher monté, signOut présent, 0 toggle thème, stub remplacé sans boucle.
</verification>

<success_criteria>
Écran paramètres complet (compte/langue/notifications UI/abonnement/déconnexion) sans toggle thème ni delivery, mdp via updateUser ; stub /dashboard remplacé proprement.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-05-SUMMARY.md` when done
</output>
