---
phase: 19-dashboard-utilisateur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0020_user_followed_setups.sql
  - packages/supabase/src/database.types.ts
  - packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts
autonomous: true
requirements: [UDASH-03, UDASH-02]
user_setup: []

must_haves:
  truths:
    - "La table user_followed_setups existe LIVE avec RLS enabled (UDASH-03, D-07 : migration 0020 via MCP apply_migration, policies scopées auth.uid(), index keyset CONCURRENTLY)"
    - "Un non-propriétaire (client anon/auth A) ne peut PAS insérer/supprimer une ligne au nom de B : with check (user_id = (select auth.uid())) rejette (erreur 42501 ou 0 ligne) — anti-IDOR"
    - "Un abonné peut insérer/lire/supprimer SA propre watchlist (user_id auto = default auth.uid())"
    - "L'index keyset (user_id, created_at desc, id desc) est CONCURRENTLY, indisvalid=false, et un EXPLAIN keyset rend Index Scan (pas Seq Scan + Sort) — support UDASH-02"
    - "get_advisors(performance) ne signale aucun auth_rls_initplan sur user_followed_setups ; get_advisors(security) aucune nouvelle alerte"
  artifacts:
    - path: "supabase/migrations/0020_user_followed_setups.sql"
      provides: "table + 3 policies RLS scopées auth.uid() + entête convention apply_migration"
      contains: "with check (user_id = (select auth.uid()))"
    - path: "packages/supabase/src/database.types.ts"
      provides: "types régénérés incluant user_followed_setups + alias UserFollowedSetup{Row,Insert}"
      contains: "user_followed_setups"
    - path: "packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts"
      provides: "preuve anti-IDOR insert/delete/select via anon-client (skipIf no env)"
      contains: "user_followed_setups"
  key_links:
    - from: "0020 policy insert"
      to: "auth.uid()"
      via: "with check non corrélé wrappé (select auth.uid())"
      pattern: "with check \\(user_id = \\(select auth.uid\\(\\)\\)\\)"
    - from: "user-followed-rls.test.ts"
      to: "clientA anon-client"
      via: "insert user_id=B → error attendu"
      pattern: "user_followed_setups"
---

<objective>
Poser la fondation données de la watchlist membre : la table `user_followed_setups` (1re écriture front membre du milestone), ses policies RLS anti-IDOR scopées `auth.uid()`, et l'index keyset CONCURRENTLY qui supportera la pagination des suivis/historique (UDASH-02).

Purpose: La watchlist est la SEULE écriture front membre — la RLS `with check (user_id = (select auth.uid()))` est la vraie barrière anti-IDOR (V4 Access Control prioritaire). Sans cette table poussée LIVE, build+types passent en faux-positif (types viennent de database.types.ts, pas de la DB live).
Output: Migration 0020 appliquée LIVE via MCP, types régénérés, test anti-IDOR vert (ou skip propre sans env), advisors verts, EXPLAIN keyset = Index Scan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-dashboard-utilisateur/19-RESEARCH.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md
@.planning/phases/19-dashboard-utilisateur/19-VALIDATION.md

<interfaces>
<!-- Forme de policy à COPIER de 0017 (RLS InitPlan wrap, Pitfall 3) -->
<!-- INSERT = with check SEUL ; SELECT/DELETE = using ; colonne JAMAIS wrappée -->
for insert to authenticated with check (user_id = (select auth.uid()))
for select to authenticated using      (user_id = (select auth.uid()))
for delete to authenticated using      (user_id = (select auth.uid()))

<!-- Tiebreaker keyset (open question RESEARCH §389 tranchée ci-dessous) :
     ajouter une colonne id uuid not null default gen_random_uuid() unique
     pour aligner le helper keyset générique (created_at desc, id desc). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration 0020 (table + RLS) + le test anti-IDOR (RED scaffold)</name>
  <files>supabase/migrations/0020_user_followed_setups.sql, packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts</files>
  <read_first>
    - supabase/migrations/0017_scalable_foundation.sql (entête convention L.5-19 ; policies payments/subscriptions L.110-134 ; découpage Partie A / Partie B CONCURRENTLY L.350-413 ; gate indisvalid L.393-407)
    - supabase/migrations/0006_analyses_trade_setups.sql (forme create table + enable RLS L.55-76)
    - packages/supabase/src/repositories/__tests__/seed-rls.test.ts (calque EXACT : bootstrap HAS_ENV + describe.skipIf L.26-52 ; adminClient/signUpAndGetClient/deleteUser L.32-49 ; lecture assertée via clientA anon L.98-114)
    - packages/supabase/src/repositories/__tests__/affiliate-rls.test.ts (pattern isolation cross-user existant)
  </read_first>
  <action>
    Créer `supabase/migrations/0020_user_followed_setups.sql`. Entête = copier le bloc convention de 0017 (apply_migration jamais db push ; numéro 0020 ; édition manuelle database.types.ts après generate_typescript_types). Partie A (transactionnelle, apply_migration) : `create table public.user_followed_setups` avec colonnes `id uuid not null default gen_random_uuid()` (tiebreaker keyset — DÉCISION tranchée : id dédié, aligne le helper générique), `user_id uuid not null default (select auth.uid()) references auth.users(id) on delete cascade`, `setup_id uuid not null references public.trade_setups(id) on delete cascade`, `created_at timestamptz not null default now()`, `primary key (user_id, setup_id)` (idempotence : un seul follow par couple), plus `unique (id)`. `alter table ... enable row level security`. 3 policies `to authenticated` : « ufs: lire les siens » `for select using (user_id = (select auth.uid()))`, « ufs: insérer la sienne » `for insert with check (user_id = (select auth.uid()))`, « ufs: supprimer la sienne » `for delete using (user_id = (select auth.uid()))`. La colonne `user_id` n'est JAMAIS wrappée (Pitfall 3 : `user_id = (select auth.uid())`, jamais `(select user_id = auth.uid())`). En commentaire `-- Partie B (execute_sql, hors tx)` : `create index concurrently user_followed_setups_keyset_idx on public.user_followed_setups (user_id, created_at desc, id desc);` (NON exécuté dans apply_migration — Pitfall 2, erreur 25001). Aucune policy write service_role (contrairement à trade_setups) : l'user EST le producteur de sa watchlist.

    Créer `packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts` en calquant `seed-rls.test.ts` : `describe.skipIf(!HAS_ENV)`, `adminClient()` service_role réservé au SEEDING (créer un trade_setup actif + 2 users A/B), puis assertions via anon-clients. Cas anti-IDOR (UDASH-03) : `clientA.from('user_followed_setups').insert({ user_id: userIdB, setup_id })` → `expect(error).not.toBeNull()` (usurpation rejetée par with check). Cas nominal : A insère SANS passer user_id (default auth.uid()) → succès ; A lit sa ligne ; B ne lit pas la ligne de A (0 ligne) ; A supprime la sienne. Test RED attendu tant que la table n'est pas poussée LIVE (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -c "with check (user_id = (select auth.uid()))" supabase/migrations/0020_user_followed_setups.sql` == 1
    - `grep -c "create index concurrently" supabase/migrations/0020_user_followed_setups.sql` >= 1 (en commentaire Partie B)
    - `0020_*.sql` ne contient AUCUN `(select user_id = auth.uid())` (Pitfall 3) : `grep -c "select user_id" 0020_*.sql` == 0
    - `0020_*.sql` ne contient aucune policy `for ... using/with check` référençant service_role pour l'écriture
    - `user-followed-rls.test.ts` contient `describe.skipIf` et un cas `insert({ user_id: userIdB` asserté `error` non null
    - Sans `.env.test` : `pnpm vitest run packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts` SKIP proprement (0 échec)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts</automated>
  </verify>
  <done>Migration 0020 écrite (Partie A + commentaire Partie B), test anti-IDOR scaffoldé et SKIP/RED selon env.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Pousser le schéma 0020 LIVE (MCP apply_migration + index CONCURRENTLY) + régénérer les types + advisors</name>
  <files>packages/supabase/src/database.types.ts</files>
  <read_first>
    - supabase/migrations/0020_user_followed_setups.sql (le fichier écrit en Task 1 — source de vérité de l'apply)
    - supabase/migrations/0017_scalable_foundation.sql (script gate indisvalid L.393-407 ; gabarit EXPLAIN keyset (b) L.409-413)
    - packages/supabase/src/database.types.ts (état réel ; bloc d'alias de convenance maintenu à la main en fin de fichier — convention D-05-02-F à réappliquer après régénération)
  </read_first>
  <action>
    [BLOCKING] Cette tâche pousse le schéma APRÈS l'écriture du fichier (Task 1) mais AVANT toute vérification — sans elle, types+build passent en faux-positif (Supabase non `link`é local, D-01-01-D). Convention projet : MCP `apply_migration` UNIQUEMENT (jamais `supabase db push` ni `supabase link`).

    1. MCP `apply_migration` avec le contenu Partie A de 0020 (table + 3 policies RLS). Vérifier succès.
    2. MCP `execute_sql` pour la Partie B, UN statement à la fois (CONCURRENTLY interdit en transaction, Pitfall 2) : `create index concurrently user_followed_setups_keyset_idx on public.user_followed_setups (user_id, created_at desc, id desc);`.
    3. MCP `execute_sql` gate `indisvalid` (script (a) de 0017) → confirmer `indisvalid = true` sur `user_followed_setups_keyset_idx`.
    4. MCP `execute_sql` EXPLAIN keyset (gabarit (b) de 0017) sur une requête `select ... where user_id = '<uuid>' order by created_at desc, id desc limit 21` → confirmer `Index Scan using user_followed_setups_keyset_idx`, PAS `Seq Scan` + `Sort`.
    5. MCP `generate_typescript_types` → écraser `packages/supabase/src/database.types.ts`, puis RÉAPPLIQUER manuellement le bloc d'alias de convenance en fin de fichier (CandleInsert/TradeSetupInsert/ProfileRow/… détruits par la régénération, D-05-02-F) ET ajouter `UserFollowedSetupRow/Insert/Update` (Row/Insert/Update de la table).
    6. MCP `get_advisors(performance)` → 0 `auth_rls_initplan` sur user_followed_setups. MCP `get_advisors(security)` → 0 NOUVELLE alerte (les 2 WARN security-definer préexistants has_active_subscription/is_superadmin sont EXPECTED, non bloquants).
  </action>
  <acceptance_criteria>
    - `apply_migration` 0020 retourne succès (table + 3 policies créées)
    - `execute_sql` indisvalid : `user_followed_setups_keyset_idx` a `indisvalid = true`
    - EXPLAIN sur la requête keyset montre `Index Scan using user_followed_setups_keyset_idx` (chaîne présente), AUCUN `Seq Scan` ni `Sort` sur cette table
    - `grep -c "user_followed_setups" packages/supabase/src/database.types.ts` >= 1
    - `database.types.ts` contient à nouveau les alias de convenance (`grep -c "UserFollowedSetupRow" database.types.ts` >= 1)
    - `get_advisors(performance)` : 0 entrée `auth_rls_initplan` mentionnant `user_followed_setups`
    - `get_advisors(security)` : aucune nouvelle alerte au-delà des 2 préexistantes
    - `pnpm tsc --noEmit` (ou `pnpm typecheck`) reste vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>0020 LIVE, index keyset valide + Index Scan prouvé par EXPLAIN, types régénérés + alias réappliqués, advisors verts.</done>
</task>

<task type="auto">
  <name>Task 3: Verdir le test anti-IDOR contre la DB live + sceller le gate keyset</name>
  <files>packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts</files>
  <read_first>
    - packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts (le scaffold Task 1)
    - packages/supabase/src/repositories/__tests__/seed-rls.test.ts (cleanup deleteUser + ordre des assertions)
  </read_first>
  <action>
    Avec la table LIVE (Task 2), faire passer le test au VERT si `.env.test` (URL + ANON_KEY + SERVICE_ROLE_KEY) est présent ; sinon SKIP propre (jamais de vert fabriqué — la lecture/écriture assertée passe TOUJOURS par un anon-client, jamais service_role qui bypasserait la RLS = faux vert). Ajuster les assertions pour couvrir : (a) anti-IDOR insert usurpé → error ; (b) nominal insert/select/delete A ; (c) isolation : B ne lit pas la ligne de A. Nettoyer les users créés (deleteUser) en teardown. Consigner dans le SUMMARY que le gate EXPLAIN keyset + advisors (Task 2) est Manual-Only-via-MCP (non automatisable en Vitest, cf. 19-VALIDATION Manual-Only).
  </action>
  <acceptance_criteria>
    - Avec `.env.test` : `pnpm vitest run packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts` exits 0, le cas anti-IDOR (insert user_id d'autrui) assert `error` non null
    - Sans `.env.test` : le fichier SKIP (0 échec)
    - Le test n'utilise JAMAIS le service_role pour une assertion de lecture/écriture (service_role uniquement pour le seeding du trade_setup + create/delete users)
    - `pnpm test` (suite complète) reste vert ou skip propre
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/supabase/src/repositories/__tests__/user-followed-rls.test.ts</automated>
  </verify>
  <done>Anti-IDOR prouvé (ou skip propre sans env), gate keyset+advisors consigné, suite non régressée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (navigateur/anon) → Postgres RLS | L'user écrit sa watchlist ; entrée non fiable franchit ici |
| job/seed (service_role) → Postgres | Seeding de test uniquement, jamais côté page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering/Elevation | INSERT/DELETE user_followed_setups au nom d'autrui (IDOR) | mitigate | `with check (user_id = (select auth.uid()))` + colonne `default (select auth.uid())` ; `user_id` jamais reçu du client ; test anon-client insert usurpé → error 42501 |
| T-19-02 | Information Disclosure | Lire la watchlist d'un autre user | mitigate | policy SELECT `using (user_id = (select auth.uid()))` ; test isolation B ne lit pas A |
| T-19-03 | Elevation | Faux vert de test via service_role (bypass RLS) | mitigate | assertions TOUJOURS via anon-client ; service_role = seeding seul |
| T-19-04 | Denial of Service | Seq scan à l'échelle sur suivis/historique | mitigate | index keyset CONCURRENTLY (user_id, created_at desc, id desc) ; EXPLAIN = Index Scan |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan (réutilisation stack verrouillée) — audit RESEARCH §Package Legitimacy sans objet |
</threat_model>

<verification>
- `apply_migration` 0020 LIVE + index CONCURRENTLY indisvalid=true.
- EXPLAIN keyset = Index Scan ; advisors perf/security verts.
- Test anti-IDOR vert (env) ou skip propre ; suite complète non régressée ; typecheck vert.
</verification>

<success_criteria>
Table user_followed_setups LIVE avec RLS anti-IDOR prouvée par test anon-client ; index keyset valide et utilisé (Index Scan) ; types régénérés ; aucune nouvelle alerte advisor.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-01-SUMMARY.md` when done
</output>
