---
phase: 19-dashboard-utilisateur
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - apps/web/src/lib/keyset/cursor.ts
  - apps/web/src/lib/keyset/__tests__/cursor.test.ts
  - apps/web/src/lib/watchlist/queries.ts
  - apps/web/src/lib/signals/searchParams.ts
autonomous: true
requirements: [UDASH-02]
user_setup: []

must_haves:
  truths:
    - "Le helper keyset encode/decode un curseur (created_at, id) en base64url opaque ; un curseur corrompu → null (première page), jamais throw (UDASH-02)"
    - "lib/watchlist/queries.ts lit user_followed_setups ⋈ trade_setups!inner filtré par statut (Suivis=active ; Historique=invalidated/expired + prediction_outcomes pour l'issue) via anon-client (D-04)"
    - "La pagination keyset utilise le tuple-compare PostgREST .or(created_at.lt.X,and(created_at.eq.X,id.lt.Y)) — pas de trou/doublon sur timestamps égaux (Pitfall 4)"
    - "searchParams expose cursor (string opaque) + tab whitelistés Zod, valeur invalide ignorée (jamais throw, jamais d'injection)"
    - "fetchFollowedSetupIds(supabase) retourne le Set des setup_id suivis par l'user (pour l'état initial du toggle)"
  artifacts:
    - path: "apps/web/src/lib/keyset/cursor.ts"
      provides: "encodeCursor/decodeCursor base64url, decode tolérant (corrompu→null)"
      contains: "base64url"
    - path: "apps/web/src/lib/watchlist/queries.ts"
      provides: "fetchFollowedSetups(status, cursor) keyset + fetchFollowedSetupIds"
      contains: "user_followed_setups"
    - path: "apps/web/src/lib/signals/searchParams.ts"
      provides: "param cursor + tab whitelistés (extension)"
      contains: "cursor"
  key_links:
    - from: "watchlist/queries.ts"
      to: "user_followed_setups ⋈ trade_setups"
      via: "!inner join + .or() tuple-compare keyset"
      pattern: "user_followed_setups"
    - from: "queries.ts keyset"
      to: "cursor.ts"
      via: "decodeCursor(raw) → .or(created_at.lt…)"
      pattern: "decodeCursor"
---

<objective>
Construire les 2 artefacts data front réellement neufs : le helper de curseur keyset (`lib/keyset/cursor.ts`, aucun code n'existe) et les requêtes watchlist (`lib/watchlist/queries.ts`) qui lisent la source unique `user_followed_setups ⋈ trade_setups` filtrée par statut, paginée par curseur. Étendre `searchParams.ts` (param `cursor`+`tab`) plutôt que d'ajouter `nuqs`.

Purpose: La pagination keyset (D-05) est la barrière de scalabilité (Index Scan, pas OFFSET). La source unique (D-04) évite une table view-log. Le `!inner` sous RLS `has_active_subscription()` donne GRATUITEMENT la barrière renouvellement (abonné expiré → 0 ligne, A2 à vérifier live).
Output: cursor.ts + test, watchlist/queries.ts, searchParams.ts étendu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-utilisateur/19-RESEARCH.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md

<interfaces>
<!-- queries existantes (lib/signals/queries.ts) — entête frontière producteur-unique à copier -->
export interface SignalRow { id; instrument_id; direction; opportunity_score; risk_level; risk_reward; style; valid_until; created_at; instruments: SignalInstrument }
export async function fetchActiveSignals(supabase: SupabaseClient<Database>, params: SignalsParams): Promise<{ data: SignalRow[]; error: string | null }>

<!-- searchParams.ts existant — étendre le schéma Zod, ne pas réécrire -->
export const SignalsParamsSchema = z.object({ style?, risk?, class?, asset?, sort })
export function parseSignalsParams(raw): SignalsParams  // safeParse champ par champ, jamais throw

<!-- Statuts (vérifiés) : trade_setups.status ∈ ('active','invalidated','expired') (0006)
     prediction_outcomes.outcome ∈ ('hit_tp','hit_sl','flat') keyé setup_id (0014) -->

<!-- Gabarit keyset PostgREST (RESEARCH §237-245) -->
.order('created_at', { ascending: false }).order('id', { ascending: false }).limit(PAGE_SIZE + 1)
// if cursor: .or(`created_at.lt.${c.createdAt},and(created_at.eq.${c.createdAt},id.lt.${c.id})`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper keyset cursor.ts (encode/decode tolérant) + test</name>
  <files>apps/web/src/lib/keyset/cursor.ts, apps/web/src/lib/keyset/__tests__/cursor.test.ts</files>
  <read_first>
    - apps/web/src/lib/signals/searchParams.ts (PHILOSOPHIE : safeParse champ par champ, valeur corrompue → fallback, JAMAIS throw L.39-48)
    - .planning/phases/19-dashboard-utilisateur/19-RESEARCH.md (gabarit base64url §219-246)
  </read_first>
  <behavior>
    - encodeCursor({createdAt, id}) → string base64url ; decodeCursor(encodeCursor(c)) === c (round-trip)
    - decodeCursor(undefined) → null (première page)
    - decodeCursor('@@corrompu@@') → null (jamais throw)
    - decodeCursor('') → null
  </behavior>
  <action>
    Créer `apps/web/src/lib/keyset/cursor.ts` : `interface Cursor { createdAt: string; id: string }`, `encodeCursor(c): string` = `Buffer.from(JSON.stringify([c.createdAt, c.id])).toString('base64url')`, `decodeCursor(raw: string | undefined): Cursor | null` qui retourne `null` si `!raw`, sinon `try { JSON.parse(Buffer.from(raw,'base64url').toString()) → {createdAt,id} } catch { return null }`. Le curseur est OPAQUE (pas un secret) ; même falsifié, la RLS scope toujours à auth.uid() → dégradation gracieuse, pas d'élévation. Écrire `cursor.test.ts` couvrant les cas du bloc behavior (round-trip + undefined + corrompu + vide). Le glob vitest `apps/web/src/lib/**` couvre déjà ce chemin (précédent D-08-01-A).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/src/lib/keyset/__tests__/cursor.test.ts` exits 0
    - le test contient un cas round-trip ET un cas curseur corrompu → null (assert `toBeNull`)
    - `grep -c "throw" apps/web/src/lib/keyset/cursor.ts` == 0 (decode ne throw jamais)
    - `grep -c "base64url" apps/web/src/lib/keyset/cursor.ts` >= 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/web/src/lib/keyset/__tests__/cursor.test.ts</automated>
  </verify>
  <done>cursor.ts encode/decode tolérant, test round-trip + corrompu vert.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre searchParams.ts (cursor + tab whitelistés)</name>
  <files>apps/web/src/lib/signals/searchParams.ts</files>
  <read_first>
    - apps/web/src/lib/signals/searchParams.ts (état réel : SignalsParamsSchema, parseSignalsParams safeParse champ par champ, serializeSignalsParams)
  </read_first>
  <action>
    Étendre `SignalsParamsSchema` (ou ajouter un schéma frère `WatchlistParamsSchema` si plus propre) avec `cursor: z.string().min(1).optional()` (valeur opaque, validée non-vide seulement — le décodage réel se fait via `decodeCursor`, jamais d'injection car passée à `.or()` paramétré après décodage) et `tab: z.enum(['suivis','historique']).default('suivis')`. Parser via `.safeParse` champ par champ (valeur invalide → undefined/défaut, jamais throw — T-03-05). Mettre à jour `serialize*` pour omettre le tab par défaut et le curseur absent (URL propre, round-trip stable). NE PAS ajouter nuqs (Claude's Discretion tranchée : réutiliser le pattern maison).
  </action>
  <acceptance_criteria>
    - `grep -c "cursor" apps/web/src/lib/signals/searchParams.ts` >= 1 ET `grep -c "tab" searchParams.ts` >= 1
    - `tab` est un `z.enum(['suivis','historique'])` (whitelist)
    - aucune valeur de param n'est concaténée dans une requête (parse via safeParse)
    - aucun import de `nuqs`
    - `pnpm typecheck` vert ; tests searchParams existants non régressés
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>searchParams expose cursor+tab whitelistés, jamais throw, round-trip stable.</done>
</task>

<task type="auto">
  <name>Task 3: lib/watchlist/queries.ts — source unique keyset + followed-ids</name>
  <files>apps/web/src/lib/watchlist/queries.ts</files>
  <read_first>
    - apps/web/src/lib/signals/queries.ts (EXACT : entête frontière producteur-unique L.1-17 ; signature (supabase, params) → {data,error} ; pattern !inner join + normalisation type L.49-50/136-138 ; jamais throw)
    - apps/web/src/lib/keyset/cursor.ts (decodeCursor — Task 1)
    - packages/supabase/src/database.types.ts (types user_followed_setups + prediction_outcomes régénérés en 19-01)
    - .planning/phases/19-dashboard-utilisateur/19-RESEARCH.md (Pattern 4 source unique §249-254 ; Pitfall 4 tuple-compare §311-314 ; A2 barrière expiré §384)
  </read_first>
  <action>
    Créer `apps/web/src/lib/watchlist/queries.ts`. Copier l'entête frontière producteur-unique de `lib/signals/queries.ts` (jamais service_role, RLS = vraie barrière). Exporter :
    1. `fetchFollowedSetups(supabase, { status, cursor }): Promise<{ data: FollowedRow[]; nextCursor: string | null; error: string | null }>` — source unique `from('user_followed_setups').select('id, created_at, setup_id, trade_setups!inner(<colonnes SignalRow>)')`. Filtre statut : Suivis = `trade_setups.status = 'active'` ; Historique = `status in ('invalidated','expired')` + join `prediction_outcomes(outcome, realized_r)` pour l'issue. Tri keyset `.order('created_at',{ascending:false}).order('id',{ascending:false}).limit(PAGE_SIZE+1)`. Si curseur décodé : `.or('created_at.lt.<X>,and(created_at.eq.<X>,id.lt.<Y>)')` (tuple complet, tiebreaker id — Pitfall 4). Détecter la page suivante via la PAGE_SIZE+1-ième ligne → `nextCursor = encodeCursor(...)`. Normaliser le type front. Jamais throw → `{data:[], nextCursor:null, error: msg}`.
    2. `fetchFollowedSetupIds(supabase): Promise<Set<string>>` — lit `from('user_followed_setups').select('setup_id')` (RLS scope auto), retourne le Set des setup_id (pour l'état initial du toggle, consommé par 19-05). Jamais throw → Set vide en cas d'erreur.
    Le `!inner` sous RLS `has_active_subscription()` filtre tout pour un abonné expiré (barrière renouvellement D-03 gratuite). NE PAS dupliquer fetchActiveSignals (D-02, Pitfall 5).
  </action>
  <acceptance_criteria>
    - `grep -c "user_followed_setups" apps/web/src/lib/watchlist/queries.ts` >= 1
    - `grep -c "trade_setups!inner" queries.ts` >= 1 (source unique join D-04)
    - le tuple-compare keyset présent : `grep -c "and(created_at.eq" queries.ts` >= 1 (Pitfall 4 — pas de simple .lt seul)
    - aucun import service_role : `grep -c "admin-service\|createAdminServiceClient\|service_role" queries.ts` == 0
    - `grep -c "fetchFollowedSetupIds" queries.ts` >= 1
    - `grep -c "throw" queries.ts` == 0 (retourne {error}, jamais throw)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>watchlist/queries.ts : source unique keyset + followed-ids, anon-client RLS, jamais throw, jamais service_role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (cursor/tab) → parse | param non fiable franchit ici |
| RSC → Postgres RLS | lecture watchlist scopée auth.uid() + has_active_subscription() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | Injection via param cursor/tab dans la requête | mitigate | Zod whitelist (tab enum) ; cursor décodé puis passé à `.or()` paramétré PostgREST, jamais concaténé SQL ; curseur corrompu → null |
| T-19-10 | Tampering | Curseur falsifié pour sauter des lignes d'autrui | accept | curseur opaque ; même falsifié, RLS scope à auth.uid() → pas d'élévation, juste un offset incohérent (dégradation gracieuse) |
| T-19-11 | Information Disclosure | Abonné expiré lit ses signaux suivis | mitigate | trade_setups!inner sous RLS has_active_subscription() → 0 ligne (A2 à vérifier live, fallback filtre serveur) |
| T-19-12 | Elevation | service_role côté lecture front | mitigate | anon-client uniquement ; grep service_role == 0 |
| T-19-SC | Tampering | npm installs (nuqs/react-table) | accept | Aucun package ajouté (décision no-new-lib RESEARCH) |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/lib/keyset/__tests__/cursor.test.ts` vert.
- `pnpm typecheck` vert.
- Greps : tuple-compare présent, 0 service_role, 0 throw, 0 nuqs.
</verification>

<success_criteria>
Helper keyset tolérant testé ; watchlist/queries source unique keyset + followed-ids via anon-client RLS ; searchParams étendu cursor+tab whitelistés sans nouvelle lib.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-03-SUMMARY.md` when done
</output>
