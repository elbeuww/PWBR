---
phase: 19-dashboard-utilisateur
plan: 06
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - apps/web/src/components/dash/WatchlistToggle.tsx
  - apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx
  - apps/web/src/components/signals/SignalCard.tsx
  - apps/web/src/components/signals/SignalDetail.tsx
  - apps/web/src/app/[locale]/(member)/signaux/page.tsx
  - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx
autonomous: true
requirements: [UDASH-03]
user_setup: []

must_haves:
  truths:
    - "WatchlistToggle écrit user_followed_setups via anon-client navigateur (insert/delete), JAMAIS user_id envoyé du client (default auth.uid() + with check = anti-IDOR) (UDASH-03)"
    - "Le toggle est optimiste (react-query) : flip immédiat de l'icône, rollback + toast sonner si l'écriture échoue"
    - "L'étoile est rendue sur SignalCard (liste) ET dans SignalDetail, hors du <Link> (sibling, sinon clic capturé), hit-area ≥44px (D-06)"
    - "L'état initial suivi/non-suivi vient d'un Set de setup_id fetché côté page (fetchFollowedSetupIds), pas N requêtes"
    - "Star lucide si l'import se résout, sinon SVG inline (≥44px)"
  artifacts:
    - path: "apps/web/src/components/dash/WatchlistToggle.tsx"
      provides: "toggle étoile optimiste, insert/delete anon-client, rollback+toast"
      contains: "useMutation"
    - path: "apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx"
      provides: "preuve optimiste + rollback sur erreur"
      contains: "rollback"
    - path: "apps/web/src/components/signals/SignalCard.tsx"
      provides: "étoile câblée hors du Link"
      contains: "WatchlistToggle"
  key_links:
    - from: "WatchlistToggle"
      to: "user_followed_setups (insert/delete)"
      via: "anon-client navigateur, default auth.uid(), with check"
      pattern: "user_followed_setups"
    - from: "(member)/signaux/page.tsx"
      to: "fetchFollowedSetupIds"
      via: "Set passé en prop followed aux cartes"
      pattern: "fetchFollowedSetupIds"
---

<objective>
Livrer la SEULE écriture front membre du milestone : le `WatchlistToggle` (étoile optimiste) câblé sur `SignalCard` et `SignalDetail`, écrivant `user_followed_setups` via anon-client RLS-scopé (anti-IDOR). L'état initial vient d'un Set de setup_id fetché côté page.

Purpose: UDASH-03 = 1re policy insert/delete scopée auth.uid(). L'écriture EST la donnée de l'user → anon-client RLS-scopé (PAS service_role, contrairement aux paiements). `with check (user_id = (select auth.uid()))` + `default auth.uid()` = anti-IDOR ; on n'envoie JAMAIS user_id du client.
Output: WatchlistToggle + test + câblage cartes/detail + fetch followed-ids dans les pages membre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-utilisateur/19-RESEARCH.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md
@.planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md

<interfaces>
<!-- écriture optimiste (RESEARCH §190-217) — calque SignalList browser-client -->
import { createClient } from '@/lib/supabase/client'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
// insert : supabase.from('user_followed_setups').insert({ setup_id })   // PAS de user_id (default auth.uid())
// delete : supabase.from('user_followed_setups').delete().eq('setup_id', setupId)
// onMutate flip optimiste ; onError rollback + toast.error(t('dash.watchlist.error'))

<!-- état initial (lib/watchlist/queries.ts — 19-03) -->
export async function fetchFollowedSetupIds(supabase): Promise<Set<string>>

<!-- SignalCard : corps = <Link> (L.72-76) ; placer l'étoile en SIBLING hors du Link -->
<!-- i18n : dash.watchlist.add / dash.watchlist.remove (aria), dash.watchlist.error (toast) — 19-02 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WatchlistToggle (optimiste + rollback) + test</name>
  <files>apps/web/src/components/dash/WatchlistToggle.tsx, apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx</files>
  <read_first>
    - apps/web/src/components/signals/SignalList.tsx (calque : 'use client' + entête sécurité L.1-29 ; useMemo createClient browser L.33/51 ; usage @tanstack/react-query L.32)
    - apps/web/src/components/LanguageSwitcher.tsx (SVG inline quand lucide absent L.13-14/25-43 ; hit-area min-h-11 min-w-11 L.168)
    - apps/web/src/components/ui/sonner.tsx (toast vendored)
    - .planning/phases/19-dashboard-utilisateur/19-RESEARCH.md (Pattern 2 §190-217 ; Pitfall 1 with check §294-298 ; Pitfall 7 lucide §326-328)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Watchlist toggle §110/131 — aria-label add/remove, ≥44px)
  </read_first>
  <behavior>
    - clic sur étoile non-suivie → flip immédiat vers « suivie » (optimiste) puis insert
    - insert échoue → rollback de l'icône + toast.error(dash.watchlist.error)
    - clic sur étoile suivie → flip vers « non-suivie » puis delete
    - delete échoue → rollback + toast
    - aria-label = dash.watchlist.add quand non-suivie / dash.watchlist.remove quand suivie
  </behavior>
  <action>
    Créer `apps/web/src/components/dash/WatchlistToggle.tsx` (`'use client'`). Props : `{ setupId: string; initialFollowed?: boolean }`. `const supabase = useMemo(() => createClient(), [])` (navigateur, porte la session → RLS). État local `followed` initialisé sur `initialFollowed`. `useMutation` : `mutationFn` insert (`{ setup_id: setupId }` — JAMAIS user_id, default auth.uid() + with check = anti-IDOR) si on suit, sinon delete `.eq('setup_id', setupId)`. `onMutate` flip optimiste de l'état local ; `onError` rollback de l'état + `toast.error(t('watchlist.error'))` (namespace dash). Icône `Star` de `lucide-react` SI l'import se résout (vérifier), sinon SVG étoile inline. Bouton hit-area ≥44px (`min-h-11 min-w-11`), `aria-label` dash.watchlist.add/remove selon l'état, état actif = remplissage accent `--primary` (l'étoile EST une surface de valeur, accent autorisé — distinct de la nav). Écrire `WatchlistToggle.test.tsx` (mock supabase.from(...).insert/delete + mock toast) couvrant le bloc behavior (optimiste + rollback sur erreur). Glob vitest : placer sous `__tests__/` (couvert par `apps/web/**/__tests__/**`).
  </action>
  <acceptance_criteria>
    - `grep -c "user_id" apps/web/src/components/dash/WatchlistToggle.tsx` == 0 (JAMAIS envoyé — anti-IDOR, default auth.uid())
    - `grep -c "useMutation" WatchlistToggle.tsx` >= 1 ET `grep -c "onError" WatchlistToggle.tsx` >= 1 (rollback)
    - `grep -c "createClient" WatchlistToggle.tsx` >= 1 importé de `@/lib/supabase/client`, aucun service_role
    - hit-area : `grep -Ec "min-h-11|min-w-11|h-11|w-11" WatchlistToggle.tsx` >= 1 (≥44px)
    - `pnpm vitest run apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx` exits 0, couvre rollback sur erreur
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx</automated>
  </verify>
  <done>Toggle optimiste anti-IDOR (sans user_id), rollback+toast testés, ≥44px, lucide/SVG.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler l'étoile dans SignalCard + SignalDetail (hors du Link)</name>
  <files>apps/web/src/components/signals/SignalCard.tsx, apps/web/src/components/signals/SignalDetail.tsx</files>
  <read_first>
    - apps/web/src/components/signals/SignalCard.tsx (corps = <Link> L.72-76 ; server-renderable PAS 'use client' L.5 — l'étoile = îlot client autonome en SIBLING)
    - apps/web/src/components/signals/SignalDetail.tsx (en-tête sémantique, server-renderable composant d'enfants client)
    - apps/web/src/components/dash/WatchlistToggle.tsx (Task 1)
    - .planning/phases/19-dashboard-utilisateur/19-PATTERNS.md (Pattern d'insertion §207-209 : étoile FRÈRE du Link, jamais imbriquée)
  </read_first>
  <action>
    Modifier `SignalCard.tsx` pour rendre `<WatchlistToggle setupId={signal.id} initialFollowed={followed} />` en ÉLÉMENT FRÈRE du `<Link>` (PAS à l'intérieur — sinon le clic est capturé par le Link / hydratation imbriquée). Ajouter une prop optionnelle `followed?: boolean` à `SignalCardProps` (défaut false). Envelopper Link + toggle dans un conteneur relatif (positionner l'étoile en `end-0 top-0` via propriétés logiques, hit-area ≥44px). SignalCard reste server-renderable (le toggle est l'unique îlot client). Idem pour `SignalDetail.tsx` : insérer `<WatchlistToggle setupId={...} initialFollowed={followed} />` dans l'en-tête (ajouter prop `followed?`). Conserver `<Disclaimer />` et toute la surface existante (D-02 : agréger, ne pas retirer).
  </action>
  <acceptance_criteria>
    - `grep -c "WatchlistToggle" apps/web/src/components/signals/SignalCard.tsx` >= 1 ET dans SignalDetail.tsx >= 1
    - l'étoile n'est PAS dans le `<Link>` : structurellement WatchlistToggle est un sibling (revue : le tag <Link> se ferme avant <WatchlistToggle> ou le toggle est hors du JSX du Link)
    - `SignalCardProps` et SignalDetail props exposent `followed?: boolean` optionnel (rétro-compatible)
    - SignalCard reste sans `'use client'` (server-renderable) : `grep -c "'use client'" SignalCard.tsx` == 0
    - `<Disclaimer` non retiré de SignalDetail
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Étoile câblée hors du Link sur carte + détail, prop followed optionnelle, surfaces préservées.</done>
</task>

<task type="auto">
  <name>Task 3: Fetch des followed-ids dans les pages membre + passage en prop</name>
  <files>apps/web/src/app/[locale]/(member)/signaux/page.tsx, apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (lecture RSC anon-client + montage SignalList ; ne pas casser le gate (member) requireActiveSub au layout)
    - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx (détail : montage SignalDetail + en-tête ScoreRing)
    - apps/web/src/lib/watchlist/queries.ts (fetchFollowedSetupIds — 19-03)
  </read_first>
  <action>
    Dans `(member)/signaux/page.tsx` : appeler `fetchFollowedSetupIds(supabase)` (anon-client RSC déjà instancié) UNE fois, obtenir le `Set<string>`. Passer `followed={followedIds.has(signal.id)}` à chaque `SignalCard` (via `SignalList`/initialData → propager la prop `followed` par carte, ou calculer côté liste). Idem dans `[id]/page.tsx` : déterminer `followed` pour le setup affiché et le passer à `SignalDetail`. Une seule requête (pas N+1). Si `fetchFollowedSetupIds` échoue → Set vide (dégradation gracieuse, étoiles non remplies). Ne pas affaiblir le gate `(member)` (requireActiveSub reste au layout).
  </action>
  <acceptance_criteria>
    - `grep -c "fetchFollowedSetupIds" apps/web/src/app/[locale]/(member)/signaux/page.tsx` >= 1 (1 seule requête)
    - `[id]/page.tsx` détermine et passe `followed` à SignalDetail
    - aucune requête par carte (pas de fetchFollowedSetupIds dans une boucle/map)
    - le gate (member) reste requireActiveSub (layout (member) inchangé)
    - `pnpm typecheck` vert ; tests existants (signaux) non régressés
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Followed-ids fetché 1×, propagé en prop followed aux cartes/détail, gate (member) intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur (anon-client) → Postgres RLS | écriture watchlist franchit ici (untrusted) |
| RSC (member) → Postgres RLS | lecture followed-ids scopée auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-21 | Tampering/Elevation | INSERT/DELETE watchlist d'autrui (IDOR) — CENTRAL | mitigate | user_id JAMAIS envoyé du client (grep user_id == 0) ; default (select auth.uid()) + with check (RLS 19-01) ; insert ne porte que setup_id |
| T-19-22 | Elevation | service_role côté écriture front | mitigate | createClient navigateur (anon, session cookies) ; aucun service_role/admin-service importé |
| T-19-23 | Tampering | Clic étoile capturé par le Link parent (mauvais câblage) | mitigate | toggle = SIBLING hors du <Link> (Pattern d'insertion) |
| T-19-24 | DoS | N+1 requêtes followed-status | mitigate | fetchFollowedSetupIds 1× → Set, prop par carte |
| T-19-25 | Tampering | Texte/aria en dur | mitigate | aria via dash.watchlist.* ; lint:i18n exit 0 |
| T-19-SC | Tampering | npm installs | accept | Aucun package ajouté (react-query/sonner/lucide déjà présents) |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/components/dash/__tests__/WatchlistToggle.test.tsx` vert (optimiste + rollback).
- `pnpm typecheck` vert ; suite signaux non régressée.
- Greps : user_id == 0 dans le toggle, 0 service_role, étoile hors Link, fetchFollowedSetupIds 1×.
</verification>

<success_criteria>
Watchlist write anti-IDOR (anon-client, sans user_id) optimiste avec rollback testé, étoile câblée hors du Link sur carte+détail, followed-ids fetché 1×, gate (member) intact.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-06-SUMMARY.md` when done
</output>
