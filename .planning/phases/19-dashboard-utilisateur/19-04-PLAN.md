---
phase: 19-dashboard-utilisateur
plan: 04
type: execute
wave: 2
depends_on: ["19-02"]
files_modified:
  - apps/web/src/components/dash/AffiliateSummaryCard.tsx
  - apps/web/src/app/[locale]/(dash)/page.tsx
  - apps/web/src/app/[locale]/(dash)/abonnement/page.tsx
  - apps/web/test/no-perf-seed-claims.test.ts
autonomous: true
requirements: [UDASH-01, UDASH-05]
user_setup: []

must_haves:
  truths:
    - "L'overview (dash)/page.tsx rend l'ordre figé D-08 : statut abonnement (ExpiryBanner déjà au layout) → 3-4 derniers signaux → raccourcis (watchlist, affiliation si affilié, paramètres) (UDASH-01)"
    - "L'overview ne rend AUCUN chiffre de performance fabriqué : ni equity curve, ni P&L, ni ROI (D-09/VITR-03)"
    - "AffiliateSummaryCard n'est rendue QUE si l'user est affilié, lit la vue affiliate_dashboard (no-PII, agrégats), montants via formatAtomic(BigInt) jamais Number, et lie vers /affiliation/dashboard (D-10, UDASH-05)"
    - "L'abonnement est géré dans (dash)/abonnement (réhébergement ou redirection de la surface (account)/abonnement existante, sans cloner PlanCard)"
    - "Le test no-perf-seed-claims couvre l'overview (dash) — aucun % non mesuré"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dash)/page.tsx"
      provides: "overview cockpit : sub + 3-4 signaux + raccourcis"
      contains: "fetchActiveSignals"
    - path: "apps/web/src/components/dash/AffiliateSummaryCard.tsx"
      provides: "carte résumé conditionnelle affiliation, formatAtomic BigInt, lien dédié"
      contains: "formatAtomic"
    - path: "apps/web/test/no-perf-seed-claims.test.ts"
      provides: "garde no-perf étendue à l'overview"
      contains: "dash"
  key_links:
    - from: "(dash)/page.tsx"
      to: "AffiliateSummaryCard"
      via: "rendu conditionnel si affilié"
      pattern: "AffiliateSummaryCard"
    - from: "AffiliateSummaryCard"
      to: "affiliate_dashboard (vue)"
      via: "anon-client RSC, agrégats no-PII"
      pattern: "affiliate_dashboard"
---

<objective>
Livrer la vue d'ensemble « cockpit personnel » (UDASH-01) et la carte résumé d'affiliation conditionnelle (UDASH-05), plus le réhébergement de la gestion d'abonnement dans `(dash)`. Étendre la garde no-perf à l'overview.

Purpose: L'overview aligne le membre sur la valeur abonnement (rétention/renouvellement = le business) sans aucun chiffre de gain fabriqué (D-09). L'affiliation est un RENVOI (3 dashboards mentalement séparés, D-10), pas un module intégré lourd.
Output: AffiliateSummaryCard + overview + abonnement (dash) + no-perf étendu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md
@.planning/phases/19-dashboard-utilisateur/19-PATTERNS.md

<interfaces>
<!-- fetchActiveSignals (lib/signals/queries.ts) — pour les 3-4 derniers signaux -->
export async function fetchActiveSignals(supabase, params: SignalsParams): Promise<{ data: SignalRow[]; error: string | null }>
// params.sort='recent' + slice(0,4) pour « derniers signaux »

<!-- vue affiliate_dashboard (security_invoker, no-PII) — colonnes lues par affiliation/dashboard/page.tsx -->
.from('affiliate_dashboard').select('total_signups, active_referrals, revenue_total_atomic, revenue_current_month_atomic, commissions_due_atomic, commissions_paid_atomic').maybeSingle()
import { formatAtomic } from '@app/core'  // formatAtomic(BigInt(atomic ?? '0')) — JAMAIS Number

<!-- gate -->
requireRole('affiliate') REDIRIGE — NE PAS l'utiliser pour la carte ; lire profiles.role / affiliate row et conditionner le rendu
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AffiliateSummaryCard (carte résumé conditionnelle, no-PII)</name>
  <files>apps/web/src/components/dash/AffiliateSummaryCard.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/affiliation/dashboard/page.tsx (EXACT pour le rendu montants : lecture vue affiliate_dashboard L.65-72 ; formatAtomic(BigInt(...)) + <bdi> L.47-50/191-193 ; import @app/core L.25)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Copywriting affiliation summary §114 ; Color montants mesurés D-09)
    - apps/web/src/components/ui/card.tsx (Card vendored)
  </read_first>
  <action>
    Créer `apps/web/src/components/dash/AffiliateSummaryCard.tsx` (server-renderable RSC). Le composant reçoit le client anon (ou lit via `createClient()`), vérifie d'abord que l'user est affilié SANS rediriger (lire `profiles.role === 'affiliate'` ou l'existence d'une ligne `affiliates` — `requireRole('affiliate')` REDIRIGE, interdit ici). Si NON affilié → retourne `null` (rendu conditionnel D-10). Si affilié → lit la vue `affiliate_dashboard` (security_invoker, no-PII, agrégats seuls) via anon-client, rend une Card résumé : `dash.affiliate.summary` avec `active_referrals` (X abonnés ramenés) et `revenue_total_atomic` formaté `formatAtomic(BigInt(atomic ?? '0'))` (JAMAIS Number — perte précision >2^53, <bdi>), + lien `dash.affiliate.link` → `/affiliation/dashboard`. AUCUNE ligne nominative, aucun % de performance. Tokens `bg-[var(--token)]`, propriétés logiques.
  </action>
  <acceptance_criteria>
    - `grep -c "formatAtomic" apps/web/src/components/dash/AffiliateSummaryCard.tsx` >= 1 ET `grep -c "Number(" AffiliateSummaryCard.tsx` == 0
    - `grep -c "requireRole" AffiliateSummaryCard.tsx` == 0 (ne redirige pas ; conditionne le rendu)
    - le composant retourne `null` quand l'user n'est pas affilié (présence d'un `return null`)
    - `grep -c "/affiliation/dashboard" AffiliateSummaryCard.tsx` >= 1 (lien vers le dashboard dédié)
    - `grep -c "affiliate_dashboard" AffiliateSummaryCard.tsx` >= 1 (vue no-PII)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Carte résumé affiliation conditionnelle, no-PII, formatAtomic BigInt, lien dédié.</done>
</task>

<task type="auto">
  <name>Task 2: Overview (dash)/page.tsx + abonnement (dash)/abonnement</name>
  <files>apps/web/src/app/[locale]/(dash)/page.tsx, apps/web/src/app/[locale]/(dash)/abonnement/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (structure RSC complète : entête lecture gated L.1-15 ; setRequestLocale+getTranslations+createClient+parse L.32-39 ; 3 états error/empty/contenu L.54-75 ; montage QueryProvider>SignalList sans réimplémenter L.72-74)
    - apps/web/src/app/[locale]/(account)/abonnement/page.tsx (surface gestion abonnement à réhéberger ou rediriger — ne PAS cloner PlanCard/QueryProvider)
    - apps/web/src/lib/signals/queries.ts (fetchActiveSignals signature)
    - apps/web/src/components/dash/AffiliateSummaryCard.tsx (Task 1)
    - .planning/phases/19-dashboard-utilisateur/19-UI-SPEC.md (Overview §129 ordre D-08 ; états required §135 ; renewal copy §113)
  </read_first>
  <action>
    Créer `apps/web/src/app/[locale]/(dash)/page.tsx` (overview RSC, gate déjà au layout). Ordre vertical FIGÉ (D-08) — l'ExpiryBanner et le statut sont au layout (19-02) ; l'overview rend : (1) un bloc statut abonnement (actif/expiré — lire subscriptions.current_period_end/status via anon-client ; si expiré → état RENOUVELLEMENT `dash.renewal.*` + CTA « Renouveler » → /tarifs, jamais upsell gratuit D-03) ; (2) les 3-4 DERNIERS signaux via `fetchActiveSignals(supabase, {sort:'recent', ...})` puis slice(0,4) — AGRÉGER (monter SignalCard/QueryProvider existants, ne pas réimplémenter D-02) ; (3) raccourcis (cartes liens) vers watchlist, paramètres, et `<AffiliateSummaryCard />` (rendu conditionnel si affilié). États soignés loading(Skeleton)/empty/error/renewal. INTERDIT (D-09/Pitfall 6) : aucune equity curve, aucun P&L, aucun ROI, aucun % non issu d'applyThreshold. setRequestLocale + getTranslations('dash').

    Créer `apps/web/src/app/[locale]/(dash)/abonnement/page.tsx` : réhéberger la surface `(account)/abonnement` (monter ses composants) OU rediriger vers `/abonnement` existant (discrétion planner — DÉCISION : réhéberger en montant le contenu pour rester dans le shell ; NE PAS cloner PlanCard/PaymentPanel). Gate déjà assuré par le layout (dash) requireUser.
  </action>
  <acceptance_criteria>
    - `grep -c "fetchActiveSignals" apps/web/src/app/[locale]/(dash)/page.tsx` >= 1 (3-4 derniers signaux agrégés)
    - l'overview ne contient AUCUN terme interdit : `grep -Eic "equity|P&L|PnL|ROI" .../(dash)/page.tsx` == 0
    - `grep -c "AffiliateSummaryCard" .../(dash)/page.tsx` >= 1 (raccourci conditionnel)
    - état renouvellement présent : `grep -c "renewal" .../(dash)/page.tsx` >= 1 (D-03)
    - aucune réimplémentation de liste : `grep -c "SignalList\|SignalCard\|QueryProvider" .../(dash)/page.tsx` >= 1 (montage, pas clone)
    - (dash)/abonnement ne clone pas PlanCard : `grep -c "PlanCard" .../(dash)/abonnement/page.tsx` == 0 OU import depuis (account)
    - `pnpm typecheck` vert
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Overview cockpit ordre D-08 sans chiffre fabriqué, abonnement réhébergé, surfaces agrégées.</done>
</task>

<task type="auto">
  <name>Task 3: Étendre no-perf-seed-claims à l'overview (dash)</name>
  <files>apps/web/test/no-perf-seed-claims.test.ts</files>
  <read_first>
    - apps/web/test/no-perf-seed-claims.test.ts (état réel : scan statique des fichiers seed/UI pour % non mesuré, VITR-03)
    - apps/web/src/app/[locale]/(dash)/page.tsx (la surface overview à couvrir — Task 2)
  </read_first>
  <action>
    Étendre `apps/web/test/no-perf-seed-claims.test.ts` pour inclure l'overview `(dash)/page.tsx` (et AffiliateSummaryCard) dans le périmètre de scan : aucun terme de perf fabriquée (equity/P&L/ROI/% non mesuré) ne doit apparaître hors d'un usage `applyThreshold`+N+provenance. Réutiliser le détecteur existant ; ajouter le(s) chemin(s) (dash) à la liste scannée. Sanity : prouver le détecteur non trivial (une chaîne « +12% ce mois » plantée déclenche un échec).
  </action>
  <acceptance_criteria>
    - `grep -c "dash" apps/web/test/no-perf-seed-claims.test.ts` >= 1 (overview dans le périmètre)
    - `pnpm vitest run apps/web/test/no-perf-seed-claims.test.ts` exits 0
    - le test échoue si une chaîne de perf fabriquée est injectée dans l'overview (détecteur non trivial)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/web/test/no-perf-seed-claims.test.ts</automated>
  </verify>
  <done>Garde no-perf couvre l'overview (dash), détecteur non trivial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC (dash) → Postgres RLS | lecture signaux/abonnement/vue affiliation scopée |
| vue affiliate_dashboard → UI | agrégats no-PII franchissent ici |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Information Disclosure | Fuite PII filleuls dans le résumé affiliation | mitigate | vue affiliate_dashboard security_invoker no-PII, agrégats seuls (active_referrals, revenue atomic) ; aucune ligne nominative |
| T-19-14 | Repudiation/Integrity | Chiffre de performance fabriqué (ROI/equity) | mitigate | D-09 : overview = sub+signaux+raccourcis ; test no-perf étendu ; revenus affiliation MESURÉS via formatAtomic |
| T-19-15 | Elevation | Carte affiliation rendue à un non-affilié | mitigate | rendu conditionnel (lire role, return null) ; requireRole NON utilisé (redirige) |
| T-19-16 | Tampering | Perte de précision montants (Number > 2^53) | mitigate | formatAtomic(BigInt(...)) ; grep Number( == 0 |
| T-19-SC | Tampering | npm installs | accept | Aucun package ajouté |
</threat_model>

<verification>
- `pnpm vitest run apps/web/test/no-perf-seed-claims.test.ts` vert + détecteur non trivial.
- `pnpm typecheck` vert.
- Greps : fetchActiveSignals présent, 0 equity/ROI, AffiliateSummaryCard conditionnelle, 0 Number( dans la carte.
</verification>

<success_criteria>
Overview cockpit (ordre D-08) sans chiffre fabriqué, carte affiliation conditionnelle no-PII mesurée, abonnement réhébergé, garde no-perf étendue.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-utilisateur/19-04-SUMMARY.md` when done
</output>
