---
phase: 21-tests-e2e-audit-de-scalabilit
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/web/e2e/admin/cockpit.spec.ts
  - apps/web/e2e/admin/actions.spec.ts
  - apps/web/e2e/isolation/anon-admin.spec.ts
  - apps/web/e2e/isolation/free-admin.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02]

must_haves:
  truths:
    - "Un superadmin voit le cockpit 4 axes, les tables membres/file keyset, et les pages santé/signaux en lecture seule"
    - "Les actions grant-temps / suspend-reactivate / marquer-commission-payée fonctionnent via leurs RPC gated, réversibles"
    - "Un visiteur anon reçoit 404 sur toutes les routes /admin (jamais 403, jamais 200)"
    - "Un authentifié non-superadmin (free) reçoit 404 sur /admin et est redirigé /fr/tarifs sur surface member"
  artifacts:
    - path: "apps/web/e2e/admin/cockpit.spec.ts"
      provides: "Couverture E2E-01 cockpit superadmin (20-UAT items 2-6,10,11)"
      contains: "/admin"
    - path: "apps/web/e2e/admin/actions.spec.ts"
      provides: "Couverture E2E-01 actions superadmin (20-UAT items 7,8,9)"
      contains: "/admin/membres"
    - path: "apps/web/e2e/isolation/anon-admin.spec.ts"
      provides: "E2E-02 404 discret anon sur /admin"
      contains: "toBe(404)"
    - path: "apps/web/e2e/isolation/free-admin.spec.ts"
      provides: "E2E-02 404 non-superadmin + redirection tarifs non-abonné"
      contains: "toBe(404)"
  key_links:
    - from: "apps/web/e2e/admin/*.spec.ts"
      to: "playwright/.auth/superadmin.json"
      via: "projet superadmin (config 21-01) storageState"
      pattern: "page.goto\\('/admin"
    - from: "apps/web/e2e/isolation/anon-admin.spec.ts"
      to: "gating layout (admin) requireRole → notFound()"
      via: "assertion response.status()===404"
      pattern: "status\\(\\)\\).toBe\\(404\\)"
---

<objective>
Couvrir en Playwright le cockpit superadmin Phase 20 (groupe `(admin)`) — happy-path sous le
rôle `superadmin` (conversion ~1:1 des 11 items de `20-UAT.md`, l'UAT manuel ayant été sauté)
— ET l'isolation E2E-02 : 404 discret pour anon / non-superadmin, redirection tarifs pour
non-abonné.

Purpose: La Phase 20 n'a aucune preuve (UAT sauté). L'E2E DEVIENT la preuve (D-01, specifics).
La barrière DONNÉES cross-user reste prouvée par Vitest admin-rls/seed-rls (NE PAS réécrire, D-02) ;
ici on prouve la frontière HTTP/DOM (404, dialogs, rendu).
Output: e2e/admin/cockpit.spec.ts, e2e/admin/actions.spec.ts, e2e/isolation/{anon,free}-admin.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UAT.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-05-SUMMARY.md
@.planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-04-SUMMARY.md

<interfaces>
<!-- Routes (admin) figées (21-PATTERNS L.9) -->
/admin                       (cockpit 4 axes : Revenus→Ops→Acquisition→Conformité, 20-05)
/admin/membres               (table keyset + filtres statut/source/q, actions grant/suspend, 20-03/04)
/admin/file                  (file paiements keyset)
/admin/affiliation/payouts   (marquer commission payée via admin_mark_commission_paid, 20-04)
/admin/affiliation/affilies  /admin/affiliation
/admin/sante  /admin/signaux  /admin/signaux/[id]   (lecture seule, anon-client)

<!-- Pattern 404 discret prouvé (gating.spec.ts L.80-92) — JAMAIS 403 (threat T-01-08) -->
const response = await page.goto(route)
expect(response?.status()).toBe(404)

<!-- Pattern redirection non-abonné (gating.spec.ts L.73-75) -->
await page.goto('/fr/signaux')
await expect(page).toHaveURL(/\/fr\/tarifs/, { timeout: 5000 })

<!-- Mapping 20-UAT → assertions (21-PATTERNS section admin specs) -->
item2 gating · item3 4 axes+provenance « Mesuré · N=… » · item4 sidebar 4 axes · item5 membres keyset+filtres+colonne source
item6 file keyset · item7 dialog grant presets 7j/1m/3m+toast · item8 suspend motif requis→/login?suspended=1
item9 payout RPC gated+toast · item10 conformité feu rouge défaut read-only · item11 santé/signaux lecture seule
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec cockpit superadmin (UAT 2-6,10,11)</name>
  <files>apps/web/e2e/admin/cockpit.spec.ts</files>
  <read_first>
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UAT.md (items 2,3,4,5,6,10,11 — texte exact attendu)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-05-SUMMARY.md (4 cartes d'axe, ordre, ligne provenance, conformité read-only)
    - apps/web/src/app/(admin)/page.tsx + _components/AxisSummary*.tsx (libellés/landmarks réels)
    - apps/web/src/app/(admin)/membres/page.tsx (filtres + bouton « Charger la page suivante » + colonne source)
    - apps/web/e2e/gating.spec.ts (conventions)
  </read_first>
  <action>
    Créer `e2e/admin/cockpit.spec.ts`, projet `superadmin` (storageState injecté). `test.describe('E2E-01 cockpit superadmin')`. Un test par item observable de 20-UAT (2,3,4,5,6,10,11), chacun cité en commentaire `// UAT item N`. Cibler les libellés/landmarks RÉELS lus dans les composants (jamais inventés) :
      • item3 : `/admin` rend 4 sections dans l'ordre Revenus→Ops→Acquisition→Conformité + au moins une ligne « Mesuré · N = » visible (provenance honnête).
      • item4 : sidebar avec 4 en-têtes d'axe + liens vers `/admin/*` ; item actif surligné.
      • item5 : `/admin/membres` rend la table + un contrôle de filtre statut/source/recherche + bouton « Charger la page suivante » + une colonne « source » (asserter présence d'en-tête source, pas « dernier paiement »).
      • item6 : `/admin/file` rend la file paginée keyset + email membre par ligne.
      • item10 : section Conformité = feu ROUGE par défaut (LEGAL_REVIEW_DONE non posé) + version/date ; aucun lien drill-down.
      • item11 : `/admin/sante`, `/admin/signaux`, `/admin/signaux/[id]` rendent en lecture seule (asserter ABSENCE de bouton édition/création — ex. `expect(locator).toHaveCount(0)` sur un bouton « Créer/Éditer signal »).
    Assertions web-first uniquement, pas de `waitForTimeout`. Données : rendu/structure, pas un volume exact (seed 10k présent mais non assertable item-par-item, D-05).
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=superadmin e2e/admin/cockpit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --project=superadmin e2e/admin/cockpit.spec.ts` exit 0
    - Le fichier contient un test pour chacun des items 2,3,4,5,6,10,11 (commentaires `UAT item N` présents)
    - L'ordre des 4 axes (Revenus→Ops→Acquisition→Conformité) est asserté sur /admin
    - item11 asserte l'absence (count 0) d'un bouton édition/création sur /admin/signaux
  </acceptance_criteria>
  <done>Cockpit superadmin happy-path couvert (7 items 20-UAT), vert sous projet superadmin.</done>
</task>

<task type="auto">
  <name>Task 2: Spec actions superadmin réversibles (UAT 7,8,9)</name>
  <files>apps/web/e2e/admin/actions.spec.ts</files>
  <read_first>
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-UAT.md (items 7,8,9)
    - .planning/phases/20-dashboard-superadmin-cockpit-4-axes/20-04-SUMMARY.md (dialogs grant/suspend/reactivate, payout, RPC gated, toasts sonner, bouton désactivé pendant pending)
    - apps/web/src/app/(admin)/membres/_components/MemberRowActions.tsx (libellés dialog/CTA exacts)
    - apps/web/src/app/(admin)/affiliation/payouts (flux marquer payée)
    - apps/web/e2e/fixtures/roles.ts (cible réversible : le fixture `free` sert de victime suspend→reactivate)
  </read_first>
  <action>
    Créer `e2e/admin/actions.spec.ts`, projet `superadmin`. Trois tests, RÉVERSIBLES (ne pas casser les autres specs) :
      • item7 « Offrir du temps gratuit » : sur une ligne membre, ouvrir le dialog, asserter les presets 7j/1mois/3mois + CTA « Confirmer la prolongation », confirmer, asserter le toast de succès et que le bouton est désactivé pendant l'envoi. Action additive non destructive (prolonge un abonnement) — cibler le fixture `e2e-fixture-free` ou un membre seedé démo.
      • item8 « Suspendre puis réactiver » : cibler EXPLICITEMENT le fixture `e2e-fixture-free` (jamais l'abonné utilisé par 21-02). Ouvrir la confirmation destructive (motif requis, CTA rouge), confirmer ; asserter l'état suspendu. PUIS réactiver dans le même test (ou `afterEach`/`afterAll`) pour rendre le fixture réutilisable. Vérifier le message/redirection `/login?suspended=1` est documenté par le flux (l'assertion de redirection effective peut se faire en re-login du compte suspendu si non-flaky ; sinon asserter l'état suspendu côté table). Garantir la réactivation finale.
      • item9 « Marquer commission payée » : sur `/admin/affiliation/payouts`, marquer une commission (hash de transaction) ; asserter le toast + passage à l'état payé via le RPC gated `admin_mark_commission_paid` ; asserter qu'un second clic ne double-paie pas (bouton désactivé / état payé). Cibler une commission seedée démo (non critique).
    Web-first assertions, pas de `waitForTimeout`. Toute mutation destructive doit être réversible avant la fin de la suite.
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=superadmin e2e/admin/actions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --project=superadmin e2e/admin/actions.spec.ts` exit 0
    - Le test suspend cible le fixture `e2e-fixture-free` et le réactive avant la fin (état final = actif/non-suspendu)
    - Le test payout asserte l'absence de double-paiement (bouton désactivé ou état payé après 1er clic)
    - Re-run de la suite reste vert (idempotence des effets)
  </acceptance_criteria>
  <done>Les 3 actions superadmin sont couvertes et réversibles, vertes sous projet superadmin.</done>
</task>

<task type="auto">
  <name>Task 3: Specs isolation E2E-02 (anon + non-superadmin)</name>
  <files>apps/web/e2e/isolation/anon-admin.spec.ts, apps/web/e2e/isolation/free-admin.spec.ts</files>
  <read_first>
    - apps/web/e2e/gating.spec.ts (patterns 404 L.80-92 + redirection tarifs L.73-75 — réutiliser verbatim)
    - apps/web/test/admin-rls.test.ts (NE PAS réécrire — reste la preuve barrière données ; l'E2E prouve l'effet HTTP/DOM)
    - 21-PATTERNS.md (Shared Patterns : 404 discret)
  </read_first>
  <action>
    Créer `e2e/isolation/anon-admin.spec.ts` (projet `anon`, pas de storageState) : un `test()` déroulé par route `(admin)` — `/admin`, `/admin/membres`, `/admin/file`, `/admin/affiliation/payouts`, `/admin/sante`, `/admin/signaux`, `/admin/signaux/00000000-0000-0000-0000-000000000000` — chacun asserte `(await page.goto(route))?.status()` égal `404` (JAMAIS 403/200/redirect — threat T-01-08, l'existence du back-office ne fuit pas). Chaque chemin littéral apparaît dans le fichier.
    Créer `e2e/isolation/free-admin.spec.ts` (projet `free`, storageState non-superadmin) : (a) les mêmes routes `(admin)` → `status()===404` pour un authentifié-non-superadmin ; (b) un test E2E-02 données : `page.goto('/fr/signaux')` (surface member) → `expect(page).toHaveURL(/\/fr\/tarifs/)` car le fixture `free` n'a pas de subscription active (requireActiveSub). Web-first assertions.
    NE PAS dupliquer les tests `/admin`/`/admin/signaux`/`/admin/sante` déjà dans gating.spec.ts pour les comptes jetables — ici la valeur ajoutée = couverture sous storageState rôle stable + routes cockpit supplémentaires (/admin/membres, /file, /affiliation/payouts).
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=anon e2e/isolation/anon-admin.spec.ts --project=free e2e/isolation/free-admin.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --project=anon e2e/isolation/anon-admin.spec.ts` exit 0
    - `playwright test --project=free e2e/isolation/free-admin.spec.ts` exit 0
    - anon-admin.spec.ts asserte `toBe(404)` sur au moins 6 routes /admin distinctes
    - free-admin.spec.ts contient une assertion `toHaveURL(/\/fr\/tarifs/)` pour le non-abonné sur /fr/signaux
    - Aucune assertion n'accepte 403 ou 200 sur une route /admin
  </acceptance_criteria>
  <done>Isolation E2E-02 prouvée : 404 discret pour anon et non-superadmin, redirection tarifs pour non-abonné.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/non-superadmin → routes (admin) | requête HTTP non privilégiée franchit la garde requireRole du layout (admin) |
| superadmin → RPC d'écriture gated | actions grant/suspend/payout via RPC SECURITY DEFINER is_superadmin() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Information Disclosure | énumération back-office | mitigate | Specs anon/free assertent 404 systématique (jamais 403/200/redirect) sur toutes les routes /admin — preuve T-01-08 |
| T-21-08 | Elevation of Privilege | RPC écriture admin | mitigate | Actions passent par les RPC gated 0021 ; la garde is_superadmin() DANS le RPC reste la barrière (admin-rls.test Vitest inchangé) |
| T-21-09 | Tampering | mutation fixtures par actions.spec | mitigate | Suspend ciblé sur fixture `free` + réactivation garantie ; payout sur commission démo non critique ; effets réversibles/idempotents |
| T-21-10 | Tampering | rls-unchanged.test.ts | accept | Aucune page/policy modifiée par ce plan (specs seules) — garde service_role reste verte |
| T-21-SC | Tampering | npm installs | accept | Aucun install net-new |
</threat_model>

<verification>
- `pnpm exec playwright test --project=superadmin` (cockpit + actions) exit 0
- `pnpm exec playwright test --project=anon --project=free` (isolation) exit 0
- État final des fixtures inchangé (free réactivé) — re-run vert
- `pnpm typecheck` exit 0
</verification>

<success_criteria>
Cockpit superadmin Phase 20 couvert (11 items 20-UAT) + isolation E2E-02 (404 discret anon/non-superadmin, redirection tarifs non-abonné) — verts, effets réversibles. La barrière données reste prouvée par Vitest (non réécrit).
</success_criteria>

<output>
Create `.planning/phases/21-tests-e2e-audit-de-scalabilit/21-03-SUMMARY.md` when done
</output>
