---
phase: 21-tests-e2e-audit-de-scalabilit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/fixtures/roles.ts
  - apps/jobs/scripts/seed-fixtures.ts
  - apps/web/e2e/auth.setup.ts
  - playwright.config.ts
  - .gitignore
autonomous: true
requirements: [E2E-01, E2E-02]
user_setup:
  - service: supabase
    why: "Provisioning des 5 comptes fixtures déterministes sur le cloud partagé via service_role"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "apps/jobs/.env (déjà présent — réutilisé par seed.ts)"
      - name: E2E_FIXTURE_PW
        source: "Optionnel. Mot de passe partagé des fixtures (défaut 'TestPassword123!' dans roles.ts). À poser en GitHub Secret en 21-04."

must_haves:
  truths:
    - "Le script seed-fixtures provisionne 5 comptes déterministes (free/abonné/affilié/superadmin + anon implicite) de façon idempotente"
    - "Le projet Playwright `setup` se connecte une fois par rôle authentifié et écrit playwright/.auth/<role>.json"
    - "playwright.config.ts expose un projet par rôle avec storageState + dependencies sur setup + webServer activé pour CI"
    - "playwright/.auth/ est gitignoré (cookies de session impersonnables)"
    - "Décisions couvertes : D-04 (comptes fixtures déterministes un par rôle, seedés à part, CI-friendly), D-02 (specs smoke existantes préservées en projet `smoke`, non réécrites)"
  artifacts:
    - path: "apps/web/e2e/fixtures/roles.ts"
      provides: "Constantes FIXTURES déterministes (emails préfixés e2e-fixture- domaine .invalid) + FIXTURE_PASSWORD"
      contains: "e2e-fixture-"
    - path: "apps/jobs/scripts/seed-fixtures.ts"
      provides: "Provisioning idempotent service_role des comptes fixtures + promotion superadmin + subscription active pour abonné"
      contains: "auth.admin.createUser"
    - path: "apps/web/e2e/auth.setup.ts"
      provides: "Setup-project login par rôle → storageState"
      contains: "storageState"
    - path: "playwright.config.ts"
      provides: "Projets par rôle + dependencies + webServer + retries CI"
      contains: "dependencies"
  key_links:
    - from: "playwright.config.ts"
      to: "playwright/.auth/<role>.json"
      via: "use.storageState par projet"
      pattern: "storageState"
    - from: "apps/web/e2e/auth.setup.ts"
      to: "apps/web/e2e/fixtures/roles.ts"
      via: "import FIXTURES"
      pattern: "from './fixtures/roles'"
---

<objective>
Poser l'infrastructure E2E multi-rôles (Wave 0) dont dépendent toutes les specs aval :
comptes fixtures déterministes provisionnés sur le cloud partagé, setup-project Playwright
qui authentifie une fois par rôle et persiste l'état (storageState), et refonte de
`playwright.config.ts` en projets par rôle avec `webServer` activé pour la CI.

Purpose: Sans cette fondation, les specs `e2e/dash/*` et `e2e/admin/*` (21-02/21-03) ne
peuvent pas réutiliser une session par rôle — elles retomberaient sur le pattern lent
re-signUp par test. C'est le préalable strict des vagues 2-3.
Output: roles.ts, seed-fixtures.ts, auth.setup.ts, playwright.config.ts refondu, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md

<interfaces>
<!-- Sélecteurs de login prouvés (apps/web/e2e/auth.spec.ts L.73-77) — réutiliser verbatim -->
page.goto('/fr/login')
input[name="email"] / input[name="password"] / button[type="submit"]
await expect(page).toHaveURL('/fr/dashboard', { timeout: 10000 })

<!-- Provisioning service_role (apps/web/test/admin-rls.test.ts L.56-100 + apps/jobs/scripts/seed.ts L.61-63) -->
createClient(URL, SERVICE_ROLE_KEY, { auth: { persistSession: false, autoRefreshToken: false } })
admin.auth.admin.createUser({ email, password, email_confirm: true })
admin.from('profiles').update({ role: 'superadmin' }).eq('id', userId)
admin.auth.admin.deleteUser(userId)   // teardown / purge idempotente

<!-- playwright.config.ts actuel : projet unique chromium (L.33-38) ; webServer commenté (L.43-47) ;
     testDir 'apps/web' + testMatch ['e2e/**/*.spec.ts','tests/**/*.spec.ts'] (L.16-17) à préserver -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures déterministes + seed-fixtures idempotent</name>
  <files>apps/web/e2e/fixtures/roles.ts, apps/jobs/scripts/seed-fixtures.ts</files>
  <read_first>
    - apps/jobs/scripts/seed.ts (structure complète : boot dotenv apps/jobs/.env, fail-fast secrets, client service_role stateless, purge idempotente → recreate)
    - apps/web/test/admin-rls.test.ts (L.39-100 : bloc constantes figées + createUser + promotion role='superadmin' via service_role + insertion subscription)
    - apps/jobs/scripts/seed/ (sous-modules : subscriptions, config) pour la forme d'une subscription active déterministe
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md (sections roles.ts + seed-fixtures.ts)
  </read_first>
  <action>
    Créer `apps/web/e2e/fixtures/roles.ts` : exporter `FIXTURE_PASSWORD` (= `process.env['E2E_FIXTURE_PW'] ?? 'TestPassword123!'`) et un objet `FIXTURES` const-assert avec 4 entrées authentifiées — `free`, `abonne`, `affilie`, `superadmin` — chacune `{ email, role }`. Emails préfixés `e2e-fixture-<role>@nexa-e2e.invalid` (domaine `.invalid` RFC 2606, clé d'isolation Pitfall 2). `superadmin.role='superadmin'`, les 3 autres `role='member'`. PAS d'entrée `anon` (état non authentifié). NE PAS utiliser `@gmail.com` (réservé aux comptes jetables interactifs des specs existantes).
    Créer `apps/jobs/scripts/seed-fixtures.ts` en copiant la STRUCTURE de `seed.ts` : boot `dotenv` sur `apps/jobs/.env`, `fail()` fail-fast sur `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` manquants (logger le NOM jamais la valeur, sécurité CLAUDE.md), client `createClient<Database>` service_role stateless. Importer `FIXTURES`/`FIXTURE_PASSWORD` (chemin relatif vers apps/web/e2e/fixtures/roles.ts, ou redéfinir les mêmes constantes côté jobs si l'import cross-app casse le tsconfig — au choix de l'exécuteur, mais valeurs IDENTIQUES). Pour chaque fixture : idempotence purge→recreate calquée sur `seed.ts` — lister/supprimer le user existant par email (préfixe `e2e-fixture-`) via `auth.admin`, puis `auth.admin.createUser({ email, password: FIXTURE_PASSWORD, email_confirm: true })`. Post-création : `superadmin` → `profiles.update({ role:'superadmin' })`, `abonne` → insérer une subscription ACTIVE déterministe (statut/forme empruntés à seed/subscriptions). Ne JAMAIS toucher les lignes `source='demo'`/`source='live'` (purge bornée au préfixe fixture). Ajouter le script à `apps/jobs/package.json` (ex. `"seed:fixtures": "tsx scripts/seed-fixtures.ts"`).
  </action>
  <verify>
    <automated>cd "apps/jobs" && pnpm tsx scripts/seed-fixtures.ts && pnpm tsx scripts/seed-fixtures.ts</automated>
  </verify>
  <acceptance_criteria>
    - seed-fixtures.ts s'exécute 2× de suite sans erreur "User already registered" (idempotence prouvée)
    - apps/web/e2e/fixtures/roles.ts exporte FIXTURES avec exactement 4 clés et 0 occurrence de '@gmail.com'
    - Après run, les 4 comptes existent dans auth.users ; le fixture superadmin a profiles.role='superadmin' ; le fixture abonne a une subscription active (vérifiable via MCP execute_sql sur profiles/subscriptions filtré email like 'e2e-fixture-%')
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>5 fixtures provisionnées de façon idempotente, superadmin promu, abonné avec subscription active, aucun chevauchement avec source=demo/live.</done>
</task>

<task type="auto">
  <name>Task 2: Refonte playwright.config.ts (projets par rôle + webServer) + auth.setup.ts + .gitignore</name>
  <files>playwright.config.ts, apps/web/e2e/auth.setup.ts, .gitignore</files>
  <read_first>
    - playwright.config.ts (état actuel : projet unique chromium L.33-38, webServer commenté L.43-47, testDir/testMatch L.16-17)
    - apps/web/e2e/auth.spec.ts (sélecteurs login L.73-77, redirection /fr/dashboard)
    - apps/web/e2e/fixtures/roles.ts (créé Task 1)
    - .gitignore (vérifier ce qui est déjà ignoré : .next/, test-results/, playwright-report/ présents)
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md (section playwright.config.ts + Shared Patterns) + 21-RESEARCH.md (Pattern 1/2, Pitfall 6)
  </read_first>
  <action>
    Refondre `playwright.config.ts` SANS casser l'existant. Conserver `testDir: 'apps/web'`, `timeout`, `expect.timeout`, `use.baseURL`, `use.trace`. Ajouter `retries: process.env['CI'] ? 2 : 0`. Remplacer le projet unique `chromium` par la liste de projets :
      • `setup` — `testMatch: /.*\.setup\.ts/`.
      • `smoke` — pas de storageState, pas de dependencies, `testMatch: ['e2e/auth.spec.ts','e2e/gating.spec.ts','e2e/academie.spec.ts','e2e/i18n.spec.ts','e2e/affiliation-attribution.spec.ts','tests/**/*.spec.ts']` (préserve la non-régression D-02 + les gardes Vitest-playwright existantes signals-rls/no-flash/no-cdn-fonts).
      • `anon` — pas de storageState, `testMatch: ['e2e/isolation/anon-*.spec.ts']`.
      • `free` — `dependencies:['setup']`, `use.storageState:'playwright/.auth/free.json'`, `testMatch:['e2e/isolation/free-*.spec.ts']`.
      • `abonne` — `dependencies:['setup']`, `storageState:'playwright/.auth/abonne.json'`, `testMatch:['e2e/dash/**/*.spec.ts']`.
      • `affilie` — `dependencies:['setup']`, `storageState:'playwright/.auth/affilie.json'`, `testMatch:['e2e/**/affilie-*.spec.ts']`.
      • `superadmin` — `dependencies:['setup']`, `storageState:'playwright/.auth/superadmin.json'`, `testMatch:['e2e/admin/**/*.spec.ts']`.
    Activer `webServer` (décommenter + adapter) : `command:'pnpm --filter web build && pnpm --filter web start'`, `url:'http://localhost:3000'`, `reuseExistingServer: !process.env['CI']`, `timeout:120_000`.
    Créer `apps/web/e2e/auth.setup.ts` : importer `test as setup` de @playwright/test + `FIXTURES`/`FIXTURE_PASSWORD`. Boucler sur `['free','abonne','affilie','superadmin']` ; chaque `setup(\`auth ${role}\`, …)` répète les sélecteurs login EXACTS d'auth.spec.ts (goto /fr/login, fill email FIXTURES[role].email, fill password, click submit), attend `page.waitForURL` sur la cible post-login (/fr/dashboard), puis `page.context().storageState({ path: \`playwright/.auth/${role}.json\` })`.
    Éditer `.gitignore` : ajouter `playwright/.auth/` (cookies de session — V3 ASVS). Vérifier que `playwright-report/`, `test-results/` y sont déjà ; ajouter si absents.
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=setup && ls playwright/.auth/free.json playwright/.auth/abonne.json playwright/.auth/affilie.json playwright/.auth/superadmin.json</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --list --project=setup` énumère 4 tests setup (un par rôle authentifié)
    - playwright.config.ts contient les projets setup/smoke/anon/free/abonne/affilie/superadmin et un bloc webServer non commenté
    - `.gitignore` contient la ligne `playwright/.auth/`
    - Après `playwright test --project=setup`, les fichiers playwright/.auth/{free,abonne,affilie,superadmin}.json existent
    - `pnpm typecheck` exit 0 ; les specs smoke existantes restent collectées (non supprimées)
  </acceptance_criteria>
  <done>Config par rôle opérationnelle, setup génère 4 storageState, webServer prêt pour CI, .auth gitignoré, smoke préservé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script seed-fixtures → Supabase cloud partagé | service_role écrit dans auth.users/profiles/subscriptions d'une base PARTAGÉE avec le seed 10k et la prod-démo |
| storageState fichiers → repo git | cookies de session authentifiés (5 rôles) persistés sur disque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | seed-fixtures purge | mitigate | Purge BORNÉE au préfixe `e2e-fixture-`/domaine `.invalid` ; ne jamais toucher source=demo/live (assertion dans le code, calque seed.ts) |
| T-21-02 | Spoofing | playwright/.auth/*.json | mitigate | `.gitignore playwright/.auth/` obligatoire — cookies impersonnables jamais committés |
| T-21-03 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | fail-fast logge le NOM jamais la valeur ; clé lue depuis apps/jobs/.env (non commité) ; jamais en clair |
| T-21-04 | Tampering | rls-unchanged.test.ts (garde permanente) | accept | Cette infra ne touche aucune page/policy ; la garde service_role reste verte (aucun nouvel usage côté web) |
| T-21-SC | Tampering | npm installs | accept | Aucun install net-new (Playwright/Vitest déjà verrouillés CLAUDE.md) — voir 21-RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm tsx apps/jobs/scripts/seed-fixtures.ts` idempotent (2 runs OK)
- `pnpm exec playwright test --project=setup` produit 4 storageState JSON
- `git status` ne montre AUCUN fichier sous playwright/.auth/ (gitignoré)
</verification>

<success_criteria>
Infra E2E par rôle posée : fixtures déterministes idempotentes, setup-project générant storageState par rôle, config multi-projets avec webServer CI, .auth gitignoré. Les vagues 2-3 peuvent consommer storageState sans re-signUp.
</success_criteria>

<output>
Create `.planning/phases/21-tests-e2e-audit-de-scalabilit/21-01-SUMMARY.md` when done
</output>
