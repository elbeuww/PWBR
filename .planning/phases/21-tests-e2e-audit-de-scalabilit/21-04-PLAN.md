---
phase: 21-tests-e2e-audit-de-scalabilit
plan: 04
type: execute
wave: 3
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [E2E-01, E2E-02]
user_setup:
  - service: github
    why: "La CI a besoin des secrets Supabase pour builder l'app et exécuter E2E/Vitest contre le cloud partagé"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "GitHub repo -> Settings -> Secrets and variables -> Actions"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "GitHub repo -> Settings -> Secrets and variables -> Actions"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "GitHub repo -> Settings -> Secrets and variables -> Actions (JAMAIS committe)"
      - name: E2E_FIXTURE_PW
        source: "GitHub Secret (mot de passe partage des fixtures, doit matcher seed-fixtures)"
    dashboard_config:
      - task: "Ajouter les 4 secrets au repo"
        location: "GitHub -> Settings -> Secrets and variables -> Actions -> New repository secret (ou gh secret set <NAME>)"

must_haves:
  truths:
    - "Un workflow GitHub Actions execute lint -> typecheck -> vitest -> e2e a chaque PR et sur push master"
    - "Le job e2e est bloquant (echec rouge bloque la PR) — garde anti-regression durable D-09"
    - "Les secrets Supabase sont fournis via GitHub Secrets, jamais committes ni logges"
    - "Le rapport Playwright est uploade en artefact meme en cas d'echec"
    - "Decisions couvertes : D-11 (definition de done — tous les flux E2E verts, prouve par le job e2e bloquant a chaque PR)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pipeline CI complet (D-09) — n'existait pas (.github absent)"
      contains: "playwright install"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm test:e2e"
      via: "step bloquant avec secrets en env"
      pattern: "test:e2e"
---

<objective>
Creer le pipeline CI GitHub Actions (D-09) — INEXISTANT a date (`.github/` absent, hypothese
CONTEXT corrigee par RESEARCH). Le workflow execute lint + typecheck + vitest + e2e, le job e2e
bloquant la PR, fournissant la garde anti-regression durable du milestone.

Purpose: Sans CI, rien n'empeche une regression de casser les flux E2E. D-09 exige que les E2E
bloquent la PR. Depend des specs (21-02/03) et de la config webServer (21-01) deja en place.
Output: .github/workflows/ci.yml + secrets GitHub poses (checkpoint humain).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md

<interfaces>
<!-- Scripts npm racine verifies (21-PATTERNS L.214) -->
lint      = eslint .
typecheck = tsc -b --noEmit
test      = vitest run
test:e2e  = playwright test
packageManager = pnpm@9.15.9

<!-- Actions officielles approuvees (21-RESEARCH Package Legitimacy Audit) -->
actions/checkout@v4 . pnpm/action-setup@v4 . actions/setup-node@v4 (node 20, cache pnpm) . actions/upload-artifact@v4
chromium : pnpm exec playwright install --with-deps chromium
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md (section Code Examples CI GitHub Actions + Pitfall 1 + Pitfall 6)
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md (section ci.yml — pas d'analog, suivre l'exemple)
    - package.json (racine — confirmer les scripts lint/typecheck/test/test:e2e et packageManager)
    - playwright.config.ts (webServer build && start active en 21-01 — la CI compte dessus pour demarrer l'app)
  </read_first>
  <action>
    Creer `.github/workflows/ci.yml`. `name: CI`, declencheurs `on: { pull_request: {}, push: { branches: [master] } }`. Un job `quality` sur `ubuntu-latest`, steps sequences : actions/checkout@v4 -> pnpm/action-setup@v4 (lit packageManager pnpm@9.15.9) -> actions/setup-node@v4 (node-version 20, cache pnpm) -> `pnpm install --frozen-lockfile` -> `pnpm lint` -> `pnpm typecheck` -> `pnpm test` (vitest, contrats RLS) -> `pnpm exec playwright install --with-deps chromium` -> `pnpm test:e2e` (BLOQUANT, D-09). Le step test:e2e recoit en `env` les secrets : NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, E2E_FIXTURE_PW (tous via la syntaxe secrets.* de GitHub Actions). Les memes NEXT_PUBLIC_* doivent etre en env des steps build/Vitest (le webServer `pnpm --filter web build` en a besoin). Dernier step actions/upload-artifact@v4 avec `if: ${{ !cancelled() }}`, name playwright-report, path `playwright-report/`. NE JAMAIS ecrire de valeur de secret en clair ; uniquement des references secrets.NOM. NE PAS ajouter de step qui logge l'environnement (`env`/`printenv`).
    Note executeur : si le seed des fixtures (21-01) doit tourner en CI avant E2E, ajouter un step `pnpm --filter jobs seed:fixtures` AVANT test:e2e avec SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY en env (lire 21-01-SUMMARY pour le nom exact du script).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); ['pnpm lint','pnpm typecheck','playwright install','test:e2e','upload-artifact'].forEach(s=>{if(!y.includes(s))throw new Error('manque '+s)}); console.log('ci.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` existe et contient les steps lint, typecheck, vitest (pnpm test), playwright install --with-deps chromium, test:e2e, upload-artifact
    - Le step test:e2e reference les 4 secrets via la syntaxe GitHub `secrets.*` (aucune valeur en clair)
    - `on:` declenche sur pull_request ET push branches master
    - Aucune chaine ressemblant a une cle (40+ chars) en clair dans le fichier
  </acceptance_criteria>
  <done>Workflow CI complet cree, e2e bloquant, secrets references jamais en clair, rapport uploade en artefact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Poser les secrets GitHub et confirmer un run CI vert</name>
  <what-built>Le workflow .github/workflows/ci.yml est cree et committe (Task 1). Il reference 4 secrets repo que seul l'humain peut poser (valeurs sensibles, notamment service_role).</what-built>
  <how-to-verify>
    1. Ajouter les 4 secrets au repo GitHub (Settings -> Secrets and variables -> Actions, ou `gh secret set <NAME>`) :
       - NEXT_PUBLIC_SUPABASE_URL (valeur de .env.local / apps/web/.env)
       - NEXT_PUBLIC_SUPABASE_ANON_KEY
       - SUPABASE_SERVICE_ROLE_KEY (depuis Supabase Dashboard -> Project Settings -> API ; ne jamais committer)
       - E2E_FIXTURE_PW (la meme valeur que celle utilisee par seed-fixtures en 21-01)
    2. Pousser la branche et ouvrir/declencher une PR (ou push master).
    3. Verifier dans l'onglet Actions que le run `CI / quality` passe au VERT (lint + typecheck + vitest + e2e tous OK). L'artefact playwright-report est telechargeable.
    4. Verifier que le verdict e2e est bloquant : un echec e2e marque le check PR en rouge.
  </how-to-verify>
  <read_first>
    - .github/workflows/ci.yml (Task 1)
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md (Runtime State Inventory — Secrets/env vars)
  </read_first>
  <acceptance_criteria>
    - Les 4 secrets sont presents dans GitHub Actions (l'humain le confirme)
    - Un run CI complet est VERT (lint/typecheck/vitest/e2e), confirme par l'utilisateur
  </acceptance_criteria>
  <resume-signal>Tapez "approuve" une fois le run CI vert observe, ou decrivez l'echec (job + log) pour gap-closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> Supabase cloud | secrets (dont service_role) injectes en env de CI publique |
| workflow yml -> repo public | fichier versionne pouvant exposer des secrets si mal ecrit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY en CI | mitigate | Uniquement via GitHub Secrets (secrets.*), jamais en clair dans le yml ; aucun step ne logge l'environnement |
| T-21-12 | Information Disclosure | logs CI / artefacts | mitigate | upload-artifact limite a playwright-report/ ; pas de dump d'env ; traces sans secrets |
| T-21-13 | Tampering | dependances CI | mitigate | `pnpm install --frozen-lockfile` (lockfile fige) ; actions officielles epinglees @v4 (audit legitimite 21-RESEARCH) |
| T-21-SC | Tampering | npm/actions installs | accept | Actions officielles GitHub/pnpm, hors perimetre slopcheck npm (21-RESEARCH) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` present, steps complets, secrets references jamais en clair
- Checkpoint humain : run CI vert observe (lint+typecheck+vitest+e2e), e2e bloquant
</verification>

<success_criteria>
Pipeline CI GitHub Actions operationnel et bloquant (D-09) : lint + typecheck + vitest + e2e a chaque PR, secrets securises, rapport Playwright en artefact. Garde anti-regression durable du milestone v3.0.
</success_criteria>

<output>
Create `.planning/phases/21-tests-e2e-audit-de-scalabilit/21-04-SUMMARY.md` when done
</output>
