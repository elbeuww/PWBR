---
phase: 21-tests-e2e-audit-de-scalabilit
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md
autonomous: true
requirements: [SCALE-06]

must_haves:
  truths:
    - "Le seed ~10k (source=demo) est present au moment de l'audit"
    - "Chaque requete cle (paginations keyset, RPC KPI gated, policies RLS wrappees) a un plan EXPLAIN ANALYZE consigne"
    - "Les paginations keyset montrent Index Scan (jamais Seq Scan + tri sur requete chaude)"
    - "Les policies RLS wrappees montrent un noeud InitPlan (1x) — is_superadmin()/auth.uid() non reevaluee par ligne"
    - "0 nouvel advisor perf/secu vs baseline (delta), consigne avec le verdict D-06"
    - "Decisions couvertes : D-07 (requetes cles auditees — paginations keyset, RPC KPI gated, policies RLS wrappees), D-08 (RPC de la migration 0022 HORS audit, check cible reporte au solde de dette), D-10 (audit documente en rapport chiffre versionne 21-AUDIT.md), D-05 (seed ~10k = support du volume realiste de l'audit)"
  artifacts:
    - path: ".planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md"
      provides: "Rapport chiffre SCALE-06 (plans EXPLAIN + advisors baseline->delta + medianes N=5 + verdict D-06)"
      contains: "Index Scan"
  key_links:
    - from: "21-AUDIT.md"
      to: "supabase/migrations/0017 + 0021"
      via: "audit des policies RLS wrappees (select ...) + RPC KPI gated"
      pattern: "InitPlan"
---

<objective>
Produire le rapport d'audit DB chiffre SCALE-06 (D-10) sur le seed ~10k : `EXPLAIN (ANALYZE, BUFFERS)`
sur chaque requete cle (D-07), `get_advisors` baseline->delta, et medianes de temps (N=5, run froid
jete). Verdict triple D-06 : Index Scan (jamais Seq Scan chaude) + 0 nouvel advisor + temps borne ;
preuve InitPlan 1x pour les RLS wrappees.

Purpose: La scalabilite du milestone = conception (Phase 17/0017/0021) + AUDIT (ici), pas de load-test
(hors scope). Ce rapport est la preuve mesuree que les index/keyset/RLS tiennent a l'echelle.
Output: .planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md.
Note: independant des specs E2E — ne touche que la DB (lecture) + un fichier doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-CONTEXT.md

<interfaces>
<!-- Canal d'audit : MCP Supabase (web-only) -->
mcp__supabase__execute_sql      -> EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT) ...
mcp__supabase__get_advisors     -> baseline puis delta (type perf + security)
mcp__supabase__list_tables / get_logs

<!-- Cibles D-07 (lectures uniquement, Pitfall 4) -->
keyset : trade_setups (created_at desc, id desc) ; membres profiles keyset ; file paiements keyset ;
         user_followed_setups (suivis) ; historique user
RPC KPI gated (lecture) : get_mrr, get_acquisition_funnel, get_churn, get_plan_mix
RLS wrappees : policies (select is_superadmin()) / (select auth.uid()) de 0017_scalable_foundation.sql + 0021_admin_cockpit.sql

<!-- Lecture du plan (21-RESEARCH Pattern 3) -->
"Index Scan"/"Index Only Scan" = OK (D-06.1) ; "Seq Scan" sur requete chaude = ECHEC
policy wrappee correcte => noeud "InitPlan 1" en tete (evaluee 1x) ; si "SubPlan"/Filter par ligne => regression
temps : mediane des 4 runs apres avoir jete le run froid (D-06.3) ; seuil garde-fou = mediane x3 relatif
advisors : 0 NOUVEAU vs baseline (Pitfall 5 : faux positif auth_rls_initplan connu => comparer delta, pas zero absolu)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparer l'environnement d'audit + capturer la baseline</name>
  <files>.planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md</files>
  <read_first>
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md (Pattern 3, Methodologie des seuils, Pitfalls 3/4/5, Runtime State Inventory)
    - apps/jobs/scripts/seed.ts (commande de reseed + label source=demo + volume ~10k)
    - supabase/migrations/0017_scalable_foundation.sql (policies wrappees + index keyset cibles)
    - supabase/migrations/0021_admin_cockpit.sql (policies superadmin + RPC KPI gated cibles)
  </read_first>
  <action>
    Verifier la presence du seed ~10k : via `mcp__supabase__execute_sql`, compter les lignes source='demo' sur profiles/trade_setups/payments (attendu ~10k profils). Si absent/insuffisant, relancer le seed (`pnpm --filter jobs seed`) puis re-verifier. Verifier `pg_stat_statements` : `select 1 from pg_extension where extname='pg_stat_statements'` ; si absent, le noter comme fallback (EXPLAIN + advisors seuls, A3). Capturer la BASELINE advisors : `mcp__supabase__get_advisors` type=performance ET type=security — consigner la liste integrale (sert de reference au delta D-06.2 ; Pitfall 5 : l'advisor auth_rls_initplan peut etre un faux positif post-fix, d'ou delta vs zero absolu).
    Initialiser `21-AUDIT.md` : en-tete (date, projet csotpitrjxryjkadyiml, contexte seed 10k source=demo, note compute partagee/variance), section « Environnement » (compte seed par table, pg_stat_statements oui/non), section « Baseline advisors » (dump perf + security). Lister la table des requetes cibles D-07 a auditer en Task 2.
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('.planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md','utf8'); ['Baseline','advisor','seed'].forEach(s=>{if(!a.toLowerCase().includes(s.toLowerCase()))throw new Error('manque '+s)}); console.log('audit env OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 21-AUDIT.md contient le compte de lignes seed (source=demo) par table cle, prouvant ~10k present
    - La baseline get_advisors (perf + security) est consignee integralement
    - Le statut pg_stat_statements (active/fallback) est documente
  </acceptance_criteria>
  <done>Environnement d'audit verifie (seed 10k present), baseline advisors capturee, 21-AUDIT.md initialise.</done>
</task>

<task type="auto">
  <name>Task 2: EXPLAIN ANALYZE des requetes cles + verdict D-06</name>
  <files>.planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md</files>
  <read_first>
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md (Task 1 : baseline + cibles)
    - supabase/migrations/0017_scalable_foundation.sql + 0021_admin_cockpit.sql (forme exacte des index keyset + signatures RPC + ordre des policies)
    - .planning/phases/21-tests-e2e-audit-de-scalabilit/21-RESEARCH.md (Pattern 3 lecture du plan : Index Scan vs Seq Scan, InitPlan 1x)
  </read_first>
  <action>
    Pour CHAQUE requete cible D-07 (lectures uniquement, Pitfall 4 : pas d'ANALYZE sur RPC d'ecriture grant/suspend/payout — s'en tenir a get_advisors ou begin;...;rollback), via `mcp__supabase__execute_sql` :
      • Paginations keyset : trade_setups `order by created_at desc, id desc limit N` (aligne trade_setups_keyset_idx) ; profiles membres keyset (created_at desc, id desc) ; file paiements keyset ; user_followed_setups (signaux suivis) ; historique user. Asserter Index Scan/Index Only Scan, JAMAIS Seq Scan + tri.
      • RPC KPI gated (lecture) : get_mrr, get_acquisition_funnel, get_churn, get_plan_mix — EXPLAIN ANALYZE, lire le plan.
      • Policies RLS wrappees : requetes declenchant les policies (select is_superadmin())/(select auth.uid()) de 0017/0021 — VERIFIER la presence d'un noeud `InitPlan` (evalue 1x) ; absence de reevaluation par ligne (pas de SubPlan/Filter par tuple). C'est la preuve « non reevaluee par ligne ».
    Methodologie temps (D-06.3) : pour chaque requete, N=5 runs, JETER le 1er (cache froid/JIT), prendre la MEDIANE des 4 (Execution Time). Consigner mediane + seuil garde-fou (mediane x3). Cibler aussi `Buffers` (shared hit vs read).
    Re-capturer `get_advisors` (perf + security) et calculer le DELTA vs baseline Task 1 : 0 NOUVEL advisor = D-06.2 OK.
    Completer `21-AUDIT.md` : par requete -> plan EXPLAIN (extrait), verdict Index Scan O/N, InitPlan O/N (pour RLS), mediane ms, seuil ; section delta advisors ; (si dispo) top requetes pg_stat_statements (mean_exec_time/calls). VERDICT FINAL D-06 : PASS si (Index Scan partout + InitPlan present sur RLS wrappees + 0 nouvel advisor + temps bornes), sinon FAIL avec liste des regressions. Si un seuil ms exact est fixe, le documenter (A2/[ASSUMED] leve).
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('.planning/phases/21-tests-e2e-audit-de-scalabilit/21-AUDIT.md','utf8'); ['Index Scan','InitPlan','get_mrr','delta','VERDICT'].forEach(s=>{if(!a.includes(s))throw new Error('manque '+s)}); console.log('audit complet OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 21-AUDIT.md contient un noeud de plan « Index Scan » pour les paginations keyset auditees
    - 21-AUDIT.md contient « InitPlan » pour au moins une policy RLS wrappee (preuve non-reevaluation par ligne)
    - Les 4 RPC KPI (get_mrr/get_acquisition_funnel/get_churn/get_plan_mix) ont chacun un plan consigne
    - Le delta advisors (baseline->apres) est calcule et = 0 nouvel advisor (ou les nouveaux sont justifies comme faux positifs Pitfall 5)
    - Une ligne VERDICT D-06 (PASS/FAIL) clot le rapport, avec medianes N=5 par requete
  </acceptance_criteria>
  <done>Rapport SCALE-06 complet et chiffre : plans EXPLAIN, InitPlan prouve, delta advisors, medianes, verdict D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Supabase -> DB cloud partagee | execute_sql EXPLAIN ANALYZE execute reellement les requetes auditees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | Tampering | ANALYZE sur RPC d'ecriture | mitigate | D-07 cible les LECTURES uniquement ; pour toute RPC d'ecriture, get_advisors seul ou begin;...;rollback (Pitfall 4 — pas de ligne parasite dans admin_audit_log/subscriptions) |
| T-21-15 | Information Disclosure | RLS affaiblie pour « faire passer » l'audit | mitigate | L'audit MESURE, ne modifie aucune policy ; rls-unchanged.test.ts reste vert ; verdict honnete (FAIL si regression, jamais contournee) |
| T-21-16 | Repudiation | faux verdict PASS | mitigate | Rapport chiffre versionne (plans + medianes + delta) — preuve auditable, pas un « c'est vert » (specifics CONTEXT) |
| T-21-SC | Tampering | npm installs | accept | Aucun install net-new (audit via MCP) |
</threat_model>

<verification>
- 21-AUDIT.md present avec plans EXPLAIN, InitPlan, delta advisors, medianes N=5, VERDICT D-06
- Aucune policy/migration modifiee (audit en lecture) ; rls-unchanged.test.ts reste vert
- Seed ~10k confirme present au moment de l'audit
</verification>

<success_criteria>
Rapport d'audit DB chiffre SCALE-06 produit (D-10) : verdict D-06 (Index Scan + InitPlan 1x sur RLS wrappee + 0 nouvel advisor + temps bornes) sur le seed ~10k. Cloture la preuve de scalabilite du milestone v3.0.
</success_criteria>

<output>
Create `.planning/phases/21-tests-e2e-audit-de-scalabilit/21-05-SUMMARY.md` when done
</output>
