---
phase: 21-tests-e2e-audit-de-scalabilit
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/web/e2e/dash/overview.spec.ts
  - apps/web/e2e/dash/navigation.spec.ts
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "Un abonné (storageState) atteint /fr/dashboard et voit la vue d'ensemble rendue"
    - "Les 6 sous-routes du groupe (dash) répondent et rendent leur en-tête sans crash"
    - "Les specs n'effectuent AUCUN re-signUp (réutilisent storageState abonne du projet)"
    - "Décisions couvertes : D-01 (effort profond sur le dashboard neuf Phase 19), D-03 (flux navigation membre + dashboard utilisateur), D-05 (seed ~10k NON utilisé pour ces assertions E2E — données fixtures stables)"
  artifacts:
    - path: "apps/web/e2e/dash/overview.spec.ts"
      provides: "Couverture E2E-01 de la home dashboard /fr/dashboard"
      contains: "/fr/dashboard"
    - path: "apps/web/e2e/dash/navigation.spec.ts"
      provides: "Couverture E2E-01 des 6 sous-routes (dash)"
      contains: "/fr/dashboard/"
  key_links:
    - from: "apps/web/e2e/dash/*.spec.ts"
      to: "playwright/.auth/abonne.json"
      via: "projet abonne (config 21-01) storageState"
      pattern: "page.goto\\('/fr/dashboard"
---

<objective>
Couvrir en Playwright les flux du dashboard utilisateur Phase 19 (groupe `(dash)`) sous le
rôle `abonne` (storageState), prouvant E2E-01 sur les surfaces neuves jamais testées.

Purpose: La Phase 19 n'a aucune couverture E2E. Cet effort va où est le risque (D-01).
Output: e2e/dash/overview.spec.ts + e2e/dash/navigation.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tests-e2e-audit-de-scalabilit/21-PATTERNS.md
@.planning/phases/19-dashboard-utilisateur/19-04-SUMMARY.md
@.planning/phases/19-dashboard-utilisateur/19-05-SUMMARY.md

<interfaces>
<!-- Routes (dash) figées (21-PATTERNS L.8) — préfixe locale /fr -->
/fr/dashboard               (overview, 19-04)
/fr/dashboard/abonnement    (19-04)
/fr/dashboard/affiliation   (AffiliateSummaryCard conditionnelle no-PII, 19-04)
/fr/dashboard/historique    (historique keyset, 19)
/fr/dashboard/suivis        (signaux suivis keyset, 19)
/fr/dashboard/watchlist     (user_followed_setups, 19-06)
/fr/dashboard/parametres    (compte/langue/notifs, 19-05)

<!-- Assertions web-first prouvées (auth.spec.ts L.86-89) — pas de waitForTimeout -->
const rows = page.locator('table tbody tr')
await expect(rows.first()).toBeVisible({ timeout: 5000 })
expect(await rows.count()).toBeGreaterThanOrEqual(1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec vue d'ensemble /fr/dashboard (abonné)</name>
  <files>apps/web/e2e/dash/overview.spec.ts</files>
  <read_first>
    - apps/web/e2e/auth.spec.ts (structure test.describe + web-first assertions)
    - .planning/phases/19-dashboard-utilisateur/19-04-SUMMARY.md (sections d'overview, ordre D-08, AffiliateSummaryCard conditionnelle)
    - apps/web/src/app/[locale]/(dash)/dashboard/page.tsx (en-têtes/landmarks réellement rendus, pour cibler des sélecteurs stables : getByRole heading / texte de section)
    - apps/web/e2e/fixtures/roles.ts
  </read_first>
  <action>
    Créer `e2e/dash/overview.spec.ts`. AUCUN login/signUp (le projet `abonne` injecte storageState). `test.describe('E2E-01 dashboard overview (abonné)')`. Test 1 : `page.goto('/fr/dashboard')` puis asserter via web-first qu'au moins un `heading` de la vue d'ensemble est visible (`page.getByRole('heading')` first toBeVisible) et que l'URL reste `/fr/dashboard` (pas de redirection /tarifs ni /login — l'abonné a une subscription active). Test 2 : asserter la présence des sections clés de l'overview en ciblant des libellés/landmarks lus dans page.tsx (ne PAS inventer de texte — utiliser ce que rend réellement le composant). Si une section liste des données seedées (ex. signaux suivis récents), asserter le rendu via `getByRole`/locator stable, jamais un compte exact dépendant du seed 10k (D-05 : assertions sur fixtures, pas sur volume). Pas de `waitForTimeout`.
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=abonne e2e/dash/overview.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --project=abonne e2e/dash/overview.spec.ts` exit 0
    - overview.spec.ts ne contient ni `signUp` ni `input[name="password"]` (réutilise storageState)
    - Une assertion vérifie que l'URL reste /fr/dashboard (abonné non redirigé)
  </acceptance_criteria>
  <done>La vue d'ensemble abonné est couverte et verte sous le projet abonne.</done>
</task>

<task type="auto">
  <name>Task 2: Spec navigation 6 sous-routes (dash)</name>
  <files>apps/web/e2e/dash/navigation.spec.ts</files>
  <read_first>
    - apps/web/e2e/dash/overview.spec.ts (Task 1, mêmes conventions)
    - .planning/phases/19-dashboard-utilisateur/19-05-SUMMARY.md (paramètres) + 19-04-SUMMARY.md (abonnement/affiliation)
    - apps/web/src/app/[locale]/(dash)/dashboard/{abonnement,affiliation,historique,suivis,watchlist,parametres} (en-têtes rendus pour sélecteurs stables)
  </read_first>
  <action>
    Créer `e2e/dash/navigation.spec.ts`, projet `abonne`. Un `test()` par sous-route (abonnement, affiliation, historique, suivis, watchlist, parametres) — chaque chemin littéral `/fr/dashboard/<seg>` apparaît dans le spec (déroulé, pas de boucle opaque). Chaque test : `page.goto` puis web-first assertion qu'un heading/landmark spécifique à la page est visible ET que la réponse n'est ni 404 ni redirigée hors /fr/dashboard. Pour `historique`/`suivis` (paginations keyset) : si un bouton « Charger la page suivante » existe (cf. 19 SUMMARY), asserter sa présence (couvre le rendu keyset côté UI ; l'audit perf est en 21-05). Cibler des libellés réels lus dans les pages. Pas de `waitForTimeout`.
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=abonne e2e/dash/navigation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --project=abonne e2e/dash/navigation.spec.ts` exit 0
    - Les 6 chemins /fr/dashboard/{abonnement,affiliation,historique,suivis,watchlist,parametres} apparaissent littéralement dans le fichier
    - Chaque test asserte un élément visible spécifique à sa page (pas un simple goto sans assertion)
  </acceptance_criteria>
  <done>Les 6 sous-routes (dash) sont couvertes et vertes sous le projet abonne.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur abonné → pages (dash) anon-client | session abonné réelle (storageState) lit ses propres données via RLS auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Information Disclosure | specs dash lisant des données | mitigate | Assertions bornées au rendu/fixtures déterministes — ne dépendent pas du volume seed 10k (D-05), pas de fuite cross-user testée ici (couverte 21-03/Vitest) |
| T-21-06 | Spoofing | réutilisation storageState abonne | accept | storageState généré par setup (21-01), gitignoré ; spec ne fabrique pas de session |
| T-21-SC | Tampering | npm installs | accept | Aucun install net-new |
</threat_model>

<verification>
- `pnpm exec playwright test --project=abonne` (les 2 specs) exit 0
- `pnpm typecheck` exit 0
</verification>

<success_criteria>
Le dashboard utilisateur Phase 19 (7 routes) est couvert par Playwright sous le rôle abonné, vert, sans re-signUp — E2E-01 prouvé sur les surfaces neuves.
</success_criteria>

<output>
Create `.planning/phases/21-tests-e2e-audit-de-scalabilit/21-02-SUMMARY.md` when done
</output>
