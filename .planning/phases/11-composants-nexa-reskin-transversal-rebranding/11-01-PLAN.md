---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/globals.css
autonomous: true
requirements: [DESIGN-05]
must_haves:
  truths:
    - "Un token component-layer purple accent existe et flippe correctement clair/sombre"
    - "Un token component-layer amber (risque modéré) existe en respectant les 3 couches"
    - "Aucun composant ne consomme jamais une primitive --nexa-* directement"
  artifacts:
    - path: "apps/web/src/styles/globals.css"
      provides: "tokens purple accent + amber risque en 3 couches (primitive -> semantic -> component)"
      contains: "--accent-brand"
  key_links:
    - from: "globals.css couche component (@theme inline)"
      to: "globals.css couche sémantique (:root/.dark)"
      via: "var() repointage"
      pattern: "--color-accent-brand:\\s*var\\(--accent-brand\\)"
---

<objective>
Ajouter les tokens component-layer manquants identifiés par la recherche (RESEARCH §Pitfall 4, PATTERNS §Tokens MANQUANTS) : un accent **purple** (consommé par Eyebrow / gradients) et un **amber** « risque modéré » (consommé par ScoreRing). Ces tokens DOIVENT respecter l'architecture 3 couches verrouillée en Phase 10 (primitive `--nexa-*` -> sémantique `:root`/`.dark` -> component `@theme inline`), **sans re-décider la palette**.

Purpose : débloquer toute la Wave 2 (composants NEXA + recoloration ExpiryBanner) qui référence ces tokens. Sans eux, Eyebrow/ScoreRing/ExpiryBanner devraient consommer des primitives en dur (anti-pattern HARD).
Output : `globals.css` étendu de 2 familles de tokens (purple accent + amber risque) en 3 couches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md

<interfaces>
<!-- État ACTUEL de globals.css (source de vérité, lu cette session). -->
<!-- Couche 1 @theme : primitives existantes pertinentes -->
--nexa-purple-500: oklch(0.4233 0.1772 302.98);  /* royal purple light */
--nexa-purple-400: oklch(0.5400 0.1900 302.98);  /* eyebrow/accent en thème sombre */
--nexa-ink: oklch(0.1663 0.0262 269.37);
<!-- amber : ABSENT même en primitive -> à AJOUTER couche 1 -->
<!-- Couche 2 :root / .dark : tokens sémantiques (FLIPPENT). Purple/amber ABSENTS ici -->
<!-- Couche 3 @theme inline : --color-* = var(--token). Purple/amber ABSENTS ici -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Ajouter primitives + tokens sémantiques purple accent & amber risque</name>
  <files>apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/styles/globals.css (les 3 couches : @theme primitives lignes 30-60, @theme inline component lignes 67-91, :root lignes 104-128, .dark lignes 134-153)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (§Couche component des tokens + §Tokens MANQUANTS — règle d'or var()-only)
    - .planning/phases/10-fondation-design-system-nexa/10-PATTERNS.md (architecture 3 couches verrouillée)
  </read_first>
  <action>
    Dans la **couche 1 @theme** (après `--nexa-purple-400`, ligne ~44) : ajouter une primitive amber absente — `--nexa-amber-500: oklch(0.7686 0.1647 70.08);` (ambre standard, hue ~70, ne PAS toucher au hue 155 brand ni au hue 27/149 signal). Le purple primitive existe déjà (`--nexa-purple-500`/`--nexa-purple-400`) — ne PAS le dupliquer.
    Dans la **couche 2 `:root`** (après `--signal-bearish`, ligne ~127) : ajouter `--accent-brand: var(--nexa-purple-500);` (purple accent light) et `--risk-moderate: var(--nexa-amber-500);` (amber risque modéré). Ces deux tokens repointent UNIQUEMENT sur des primitives via var().
    Dans la **couche 2 `.dark`** (après `--ring`, ligne ~152) : overrider `--accent-brand: var(--nexa-purple-400);` (variante sombre éclaircie, primitive dédiée existante). `--risk-moderate` GARDE sa teinte amber dans les deux thèmes (comme `--signal-bearish`) -> NE PAS le redéclarer dans `.dark`.
    Dans la **couche 3 `@theme inline`** (après `--color-ring`, ligne ~86) : exposer `--color-accent-brand: var(--accent-brand);` et `--color-risk-moderate: var(--risk-moderate);` pour que les composants puissent consommer `text-accent-brand` / `bg-risk-moderate` ou `var(--accent-brand)`.
    Ajouter un commentaire au-dessus de chaque ajout référençant « Phase 11 : token component ajouté (DESIGN-05), sans re-décision de palette (Pitfall 4) ». Aucune valeur HEX/OKLCH littérale hors de la couche 1.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -v '^[[:space:]]*\*' apps/web/src/styles/globals.css | grep -c -- '--color-accent-brand\|--color-risk-moderate\|--accent-brand\|--risk-moderate\|--nexa-amber-500'</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contient `--nexa-amber-500:` dans le bloc `@theme` (couche 1)
    - `globals.css` contient `--accent-brand: var(--nexa-purple-500)` sous `:root` ET `--accent-brand: var(--nexa-purple-400)` sous `.dark`
    - `globals.css` contient `--risk-moderate: var(--nexa-amber-500)` sous `:root` UNIQUEMENT (absent de `.dark`)
    - `globals.css` contient `--color-accent-brand: var(--accent-brand)` ET `--color-risk-moderate: var(--risk-moderate)` dans `@theme inline`
    - Aucune valeur OKLCH/HEX littérale n'apparaît dans `:root`, `.dark` ou `@theme inline` (grep `oklch(` hors bloc `@theme` couche 1 = 0 nouvelle occurrence ajoutée par ce plan, hors les `oklch(0.95...)`/`oklch(0.72...)` foreground dark préexistants)
  </acceptance_criteria>
  <done>Les 5 lignes de tokens (1 primitive amber + 2 sémantiques :root + 1 sémantique .dark + 2 component) sont présentes, en 3 couches, var()-only hors couche 1.</done>
</task>

<task type="auto">
  <name>Task 2 : Vérifier non-régression du garde RTL fondation</name>
  <files>apps/web/src/styles/globals.css</files>
  <read_first>
    - apps/web/src/styles/__tests__/rtl-logical-props.test.ts (scanne globals.css pour classes physiques)
  </read_first>
  <action>
    Aucune classe utilitaire Tailwind n'est ajoutée par ce plan (uniquement des déclarations de tokens CSS). Lancer le garde RTL fondation Phase 10 pour confirmer que l'édition de `globals.css` n'introduit aucune classe physique (`ml-/mr-/pl-/pr-/left-/right-`). Ce plan touche un fichier fondation surveillé -> le garde DOIT rester vert.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts` exit 0 (2 tests verts)
  </acceptance_criteria>
  <done>Garde RTL fondation vert après l'ajout des tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build CSS -> runtime | Les tokens CSS sont du contenu de build statique, aucune entrée utilisateur ne traverse cette frontière. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | globals.css token layers | accept | Édition de tokens CSS statiques ; aucun input dynamique. Risque négligeable. |
| T-11-01-LIT | Tampering | flip .dark cassé par littéral HEX | mitigate | Acceptance interdit toute valeur littérale hors couche 1 ; var()-only enforced (Pitfall 3 globals.css). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Aucun package installé dans ce plan (phase 100% in-repo, RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts` vert.
- `pnpm --filter web exec tsc -b --noEmit` (ou typecheck racine) sans nouvelle erreur (CSS pur n'affecte pas tsc, sanity).
</verification>

<success_criteria>
- Tokens purple accent + amber risque disponibles en couche component, consommables par Eyebrow/ScoreRing/ExpiryBanner en Wave 2.
- Architecture 3 couches préservée (primitive -> semantic -> component), aucune palette re-décidée.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-01-SUMMARY.md` when done.
</output>
