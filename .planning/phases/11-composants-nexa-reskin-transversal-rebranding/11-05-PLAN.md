---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-04"]
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/icon.tsx
  - apps/web/src/app/apple-icon.tsx
  - apps/web/src/app/opengraph-image.tsx
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/components/Footer.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]
must_haves:
  truths:
    - "La marque affichée partout est NEXA (header + footer = Logo, metadata = NEXA, favicon/OG = mark)"
    - "Aucune trace de MERA / Make Everybody Rich Again / Vétéran Trading dans apps/web/src + messages"
    - "La baseline NEXA trilingue est affichée au header et est sans promesse de gain"
  artifacts:
    - path: "apps/web/src/app/opengraph-image.tsx"
      provides: "image OG NEXA générée via next/og"
      contains: "ImageResponse"
    - path: "apps/web/src/app/icon.tsx"
      provides: "favicon mark hexagonal"
      contains: "ImageResponse"
  key_links:
    - from: "app/[locale]/layout.tsx header"
      to: "components/nexa/Logo.tsx"
      via: "import + rendu Logo"
      pattern: "<Logo"
    - from: "app/layout.tsx metadata"
      to: "NEXT_PUBLIC_SITE_URL"
      via: "metadataBase"
      pattern: "metadataBase"
---

<objective>
Achever le rebranding MERA / Vétéran Trading -> NEXA (BRAND-01), poser la baseline trilingue (BRAND-02) et les assets de marque metadata files (BRAND-03 : favicon/icon/apple-icon/OG via next/og). Cibles exactes (UI-SPEC §Rebranding targets, PATTERNS) : header [locale]/layout.tsx:56, root metadata layout.tsx:11-12, placeholders i18n fr.json:210 et fr.json:489, + Footer logo, + 3 metadata files greenfield.

Purpose : fait virer au vert le garde no-mera-brand (11-02) et livre l'identité visible. Dépend de 11-04 (Logo) et 11-02 (scan de marque).
Output : Logo câblé header+footer, metadata NEXA + OG, icon/apple-icon/opengraph-image générés, MERA->NEXA dans les messages, baseline au header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-04-SUMMARY.md
@apps/web/src/components/nexa/Logo.tsx

<interfaces>
Logo (11-04) : Logo variant full|mark className — full = mark + wordmark, mark = hexagone seul.
Cibles rebranding (UI-SPEC §Rebranding targets) :
  [locale]/layout.tsx:55-56 : header span font-semibold Vétéran Trading (i18n-ignore)
  app/layout.tsx:10-13 : metadata { title 'Vétéran Trading Platform', description } — root pass-through, AUCUN html ici (Pitfall 7)
  fr.json:210 : codePlaceholder "Ex : MERA2026" -> "Ex : NEXA2026"
  fr.json:489 : "...Vétéran Trading" -> NEXA
Convention metadata Next 15 (RESEARCH Pattern 8) : fichiers RACINE app/ (indépendant de [locale]) ; export default function Image() retournant new ImageResponse(...) ; exports size/contentType/alt. OG défaut 1200x630, apple-icon 180x180, icon ex 32x32. favicon.ico non générable.
metadataBase : process.env.NEXT_PUBLIC_SITE_URL (precedent app/sitemap.ts:17 BASE_URL).
baseline (11-04) : namespace i18n baseline.text "Nouvelle Ère · Alliance d'Échange" (FR/EN/AR).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Câbler Logo + baseline au header et footer (BRAND-01/02)</name>
  <files>apps/web/src/app/[locale]/layout.tsx, apps/web/src/components/Footer.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/app/[locale]/layout.tsx (header l.55-67 — span marque, ms-6/ms-auto, ThemeToggle, LanguageSwitcher, Footer ; un seul html lang dir)
    - apps/web/src/components/Footer.tsx (logo/marque actuel + Disclaimer)
    - apps/web/src/components/nexa/Logo.tsx (API variant full/mark)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (section layout.tsx header)
    - apps/web/src/messages/fr.json (l.489 Vétéran Trading, namespace baseline ajouté en 11-04)
  </read_first>
  <action>
    Dans [locale]/layout.tsx, remplacer le span Vétéran Trading (l.56) par Logo variant full + la baseline rendue via t baseline.text (sous/à côté du wordmark, D-16). Récupérer la traduction baseline via getTranslations dans ce RSC. Préserver intégralement : ms-6/ms-auto, ThemeToggle, LanguageSwitcher, le slot Footer, le html lang dir unique ; retirer le commentaire i18n-ignore devenu inutile. Propriétés logiques only.
    Dans Footer.tsx, remplacer toute marque texte par Logo variant mark (ou full selon densité), conserver Disclaimer et liens légaux.
    Dans les 3 messages : renommer toute occurrence Vétéran Trading -> NEXA (fr.json:489 + clés EN/AR équivalentes). Vérifier que baseline (11-04) est présente et utilisée. Parité STRICTE.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "Logo" apps/web/src/app/[locale]/layout.tsx && test $(grep -rc "Vétéran Trading" apps/web/src/app/[locale]/layout.tsx apps/web/src/messages/fr.json) -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - [locale]/layout.tsx contient Logo et la baseline via t('baseline.text') ; le span Vétéran Trading a disparu
    - grep "Vétéran Trading" sur layout.tsx + fr.json retourne 0
    - Footer.tsx rend Logo et conserve Disclaimer
    - Un seul html lang dir conservé ; ms-6/ms-auto/ThemeToggle/LanguageSwitcher intacts
  </acceptance_criteria>
  <done>Header et footer affichent le Logo NEXA + baseline trilingue, zéro Vétéran Trading restant.</done>
</task>

<task type="auto">
  <name>Task 2 : Metadata root NEXA + OG/icon/apple-icon via next/og (BRAND-03)</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/icon.tsx, apps/web/src/app/apple-icon.tsx, apps/web/src/app/opengraph-image.tsx</files>
  <read_first>
    - apps/web/src/app/layout.tsx (metadata l.10-13 ; root pass-through, aucun html, Pitfall 7)
    - apps/web/src/app/sitemap.ts (precedent metadata route + BASE_URL via NEXT_PUBLIC_SITE_URL l.17)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 8 metadata files — ImageResponse, size/contentType/alt, racine app/, défauts dimensions)
    - apps/web/src/components/nexa/Logo.tsx (path SVG du mark à réutiliser)
  </read_first>
  <action>
    layout.tsx : remplacer title -> NEXA (descripteur baseline, sans promesse de gain), description NEXA neutre éducative ; ajouter metadataBase new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000') (precedent sitemap.ts) ; ajouter openGraph { title, description, type website }. Root reste pass-through (aucun html).
    Créer app/icon.tsx : export size width 32 height 32, contentType image/png, export default function Image() retournant new ImageResponse(div fond #0a0e1a hexagone N gradient #03d87f vers #63279b). Mark seul (D-09).
    Créer app/apple-icon.tsx : size 180x180, même mark, fond ink.
    Créer app/opengraph-image.tsx : alt NEXA baseline, size 1200x630, contentType image/png, ImageResponse fond #0a0e1a + mark hexagonal + wordmark NEXA + baseline, gradient #03d87f vers #63279b. Hex exacts autorisés (asset de marque). Aucune promesse de gain, aucun %.
    Tous via next/og (next@15), aucun package installé.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "metadataBase" apps/web/src/app/layout.tsx && grep -c "ImageResponse" apps/web/src/app/opengraph-image.tsx && grep -c "ImageResponse" apps/web/src/app/icon.tsx && test $(grep -c "Vétéran Trading" apps/web/src/app/layout.tsx) -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contient metadataBase et openGraph ; aucun Vétéran Trading
    - icon.tsx, apple-icon.tsx, opengraph-image.tsx contiennent chacun ImageResponse + size + contentType
    - opengraph-image.tsx contient #03d87f ET #63279b (hex de marque exacts)
    - Root layout reste pass-through (aucun html ajouté)
  </acceptance_criteria>
  <done>Metadata NEXA + 3 metadata files générés via next/og, hex de marque exacts, zéro promesse de gain.</done>
</task>

<task type="auto">
  <name>Task 3 : Renommer placeholders MERA i18n + faire virer no-mera-brand au vert (BRAND-01)</name>
  <files>apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/messages/fr.json (codePlaceholder l.210 "Ex : MERA2026" ; clés EN/AR équivalentes)
    - apps/web/test/no-mera-brand.test.ts (garde créé en 11-02 — scan apps/web/src + messages)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Rebranding targets)
  </read_first>
  <action>
    Renommer codePlaceholder "Ex : MERA2026" -> "Ex : NEXA2026" dans fr.json (l.210) ET ses équivalents en.json/ar.json (parité STRICTE). Scanner les 3 messages pour toute autre occurrence MERA / Make Everybody Rich Again et les retirer/renommer. Ne PAS toucher .planning/** (hors périmètre, non livré). Aucune nouvelle clé ; édition de valeurs uniquement (parité préservée).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/test/no-mera-brand.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - pnpm vitest run apps/web/test/no-mera-brand.test.ts exit 0 (scan réel désormais GREEN, plus aucun MERA dans src+messages)
    - grep -ri "MERA2026" apps/web/src ne retourne rien
    - Parité i18n préservée (mêmes clés FR/EN/AR)
  </acceptance_criteria>
  <done>no-mera-brand vert : zéro MERA / slogan dans le code et le contenu livrés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code source -> build livré | La marque MERA/slogan ne doit pas franchir vers le build. |
| metadata files build -> head HTML | Les ImageResponse sont du build statique theme-aware, pas d'input utilisateur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-BRAND | (intégrité marque/légal) | messages + code | mitigate | no-mera-brand vert (Task 3) ; aucun slogan de gain réintroduit. |
| T-11-OG-XSS | Tampering | opengraph-image/icon | mitigate | ImageResponse construit à partir de contenu authored statique ; aucune entrée dynamique injectée. |
| T-11-LEGAL | (légal) | title/OG/baseline | mitigate | title + baseline sans promesse de gain ; no-perf-claims couvre baseline namespace. |
| T-11-SC | Tampering | npm installs | mitigate | next/og natif next@15 ; aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- pnpm vitest run apps/web/test/no-mera-brand.test.ts vert.
- pnpm vitest run apps/web/test/no-perf-claims.test.ts vert (baseline sans promesse).
- pnpm --filter web exec tsc -b --noEmit sans nouvelle erreur (layout.tsx + 3 metadata files).
- Build sanity : pnpm --filter web build compile les metadata files (icon/apple-icon/opengraph-image).
</verification>

<success_criteria>
- BRAND-01 : zéro MERA/Vétéran Trading dans code+contenu ; header/footer/metadata = NEXA.
- BRAND-02 : baseline trilingue affichée, sans promesse de gain.
- BRAND-03 : favicon + apple-icon + OG + Logo header/footer livrés.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-05-SUMMARY.md` when done.
</output>
