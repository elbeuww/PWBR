---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 08
type: execute
wave: 6
depends_on: ["11-03", "11-04", "11-05", "11-06", "11-07"]
files_modified:
  - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx
  - apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx
  - apps/web/src/app/[locale]/(marketing)/academie/page.tsx
  - apps/web/src/app/[locale]/(auth)/login/page.tsx
  - apps/web/src/app/[locale]/(auth)/signup/page.tsx
  - apps/web/src/app/[locale]/(account)/abonnement/page.tsx
  - apps/web/src/app/(admin)/page.tsx
autonomous: true
requirements: [UI-01, UI-04, UI-05, UI-06, UI-07]
must_haves:
  truths:
    - "La vitrine (tarifs, méthodologie), l'Académie, l'auth (login/signup), le compte/abonnement et l'admin sont au design NEXA"
    - "L'ExpiryBanner est visible sur (account)/abonnement (dette WIRING-01/PAY-05 levée), via fetch RLS anon-client serveur (jamais service_role client)"
    - "Le <Disclaimer />, le gating RLS et les data-testid/ARIA des 5 specs E2E sont préservés sur chaque surface"
    - "L'admin est reskiné sobre (tokens + composants), sans hero ni animations (D-18)"
  artifacts:
    - path: "apps/web/src/app/[locale]/(account)/abonnement/page.tsx"
      provides: "ExpiryBanner câblé via fetch RLS serveur"
      contains: "ExpiryBanner"
  key_links:
    - from: "(account)/abonnement/page.tsx"
      to: "subscriptions.current_period_end (RLS anon-client serveur)"
      via: "createClient serveur, precedent (member)/layout.tsx:23-34"
      pattern: "current_period_end"
    - from: "surfaces reskinées"
      to: "<Disclaimer /> + RLS gating"
      via: "préservés inchangés"
      pattern: "Disclaimer"
---

<objective>
Achever le reskin transversal sur les surfaces restantes (UI-01 vitrine hors hero, UI-04 Académie, UI-05 auth + compte, UI-06 admin sobre) et lever la dette UI-07 (câbler l'ExpiryBanner sur (account)/abonnement). Le reskin = composition NEXA via tokens + composants (Eyebrow, Logo, ScoreRing/ConfidenceStat le cas échéant) ; les 20 primitifs shadcn se reskinnent automatiquement par re-point de tokens (ne PAS les forker). Gate de phase : suite unit + 5 specs E2E vertes.

Purpose : compléter tous les Success Criteria UI restants en préservant gating RLS, <Disclaimer /> et data-testid/ARIA. Dépend de tous les plans amont (composants, recoloration, rebranding, hero, membre).
Output : surfaces vitrine/académie/auth/compte/admin reskinées + ExpiryBanner sur abonnement + gate E2E vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-04-SUMMARY.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-05-SUMMARY.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-06-SUMMARY.md

<interfaces>
Reskin = swap tokens + composition NEXA (Eyebrow, Logo, ScoreRing, ConfidenceStat). Primitifs ui/ auto-reskinés — NE PAS forker.
ExpiryBanner (11-03 tokenisé) : ExpiryBanner currentPeriodEnd={iso|null} — composant client recevant la prop fetchée serveur.
Fetch RLS anon-client serveur (precedent (member)/layout.tsx:23-34) :
  const supabase = await createClient()
  const { data: sub } = await supabase.from('subscriptions').select('current_period_end').eq('user_id', user.id).eq('status','active').order('current_period_end',{ascending:false}).limit(1).maybeSingle()
  -> ExpiryBanner currentPeriodEnd={sub?.current_period_end ?? null}
(account)/layout.tsx fait déjà requireUser() ; abonnement/page.tsx est RSC, n'a PAS encore current_period_end (vérifié : actuellement reçoit receiveAddress/discoveryAvailable seulement).
Admin sobre (D-18) : tokens + primitifs, AUCUN hero/animation. (admin) hors [locale], FR-only.
5 specs E2E (préserver) : i18n, affiliation-attribution, gating, auth, academie. Divergence ROADMAP "6 specs" = COQUILLE (5 existent, vérifié) -> préserver les 5, ne pas créer de 6e.
<Disclaimer /> : RSC clé disclaimer.footer, global via Footer + chaque page Académie — préserver.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Câbler ExpiryBanner sur (account)/abonnement via fetch RLS serveur (UI-07)</name>
  <files>apps/web/src/app/[locale]/(account)/abonnement/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(account)/abonnement/page.tsx (intégral — RSC, reçoit receiveAddress/discoveryAvailable, pas de current_period_end)
    - apps/web/src/app/[locale]/(member)/layout.tsx (l.23-34 — precedent fetch RLS anon-client serveur + ExpiryBanner)
    - apps/web/src/components/member/ExpiryBanner.tsx (API currentPeriodEnd, tokenisé en 11-03)
    - apps/web/src/app/[locale]/(account)/layout.tsx (requireUser déjà posé)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (§Préservation frontière de confiance + §ExpiryBanner câblage)
  </read_first>
  <action>
    Dans abonnement/page.tsx (RSC), ajouter le fetch RLS anon-client SERVEUR du current_period_end en réutilisant EXACTEMENT le pattern (member)/layout.tsx:23-34 : createClient() serveur, getUser() (ou réutiliser l'user déjà résolu par requireUser du layout — récupérer via le helper existant), select subscriptions.current_period_end status active limit 1 maybeSingle. Rendre <ExpiryBanner currentPeriodEnd={sub?.current_period_end ?? null} /> en haut du main (au-dessus du PlanCard). NE JAMAIS utiliser service_role côté client ; le composant reste client recevant la prop. Ne pas dupliquer la logique de jours (déjà dans ExpiryBanner). Préserver QueryProvider/PlanCard/i18n/text-start.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "ExpiryBanner" apps/web/src/app/[locale]/(account)/abonnement/page.tsx && grep -c "current_period_end" apps/web/src/app/[locale]/(account)/abonnement/page.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - abonnement/page.tsx rend ExpiryBanner avec currentPeriodEnd issu d'un select subscriptions serveur
    - Le fetch utilise createClient serveur + status active (pattern member layout) ; aucun service_role, aucun fetch client ajouté
    - PlanCard/QueryProvider/i18n préservés
  </acceptance_criteria>
  <done>UI-07 levé : ExpiryBanner visible sur abonnement ET membre, via RLS serveur, dette WIRING-01/PAY-05 close.</done>
</task>

<task type="auto">
  <name>Task 2 : Reskin vitrine + Académie + auth + admin sobre (composition NEXA)</name>
  <files>apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx, apps/web/src/app/[locale]/(marketing)/methodologie/page.tsx, apps/web/src/app/[locale]/(marketing)/academie/page.tsx, apps/web/src/app/[locale]/(auth)/login/page.tsx, apps/web/src/app/[locale]/(auth)/signup/page.tsx, apps/web/src/app/(admin)/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/tarifs/page.tsx + methodologie/page.tsx (structure, Disclaimer, ConfidenceStat/track-record si présent)
    - apps/web/src/app/[locale]/(marketing)/academie/page.tsx (index, Disclaimer, RTL/fallback FR)
    - apps/web/src/app/[locale]/(auth)/login/page.tsx + signup/page.tsx (forms shadcn)
    - apps/web/src/app/(admin)/page.tsx (back-office, FR-only)
    - apps/web/src/components/nexa/Eyebrow.tsx (en-têtes de section)
    - apps/web/e2e/academie.spec.ts + auth.spec.ts (sélecteurs/data-testid à préserver)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Reskin Scope, D-18 admin sobre)
  </read_first>
  <action>
    Appliquer la composition NEXA via tokens + Eyebrow en-têtes + espacement 8pt + surfaces bg-card/border-border sur chaque page. Les primitifs ui/ se reskinnent seuls — NE PAS les forker ni ajouter de CSS bespoke par page.
    Vitrine (tarifs, methodologie) : Eyebrow sur sections ; track-record/ConfidenceStat (si présent) reste via applyThreshold (jamais % nu) ; conserver Disclaimer.
    Académie (index) : design NEXA, RTL + fallback FR préservés, <Disclaimer /> sur la page (UI-SPEC), data-testid academie spec intacts.
    Auth (login/signup) : forms NEXA tokenisés (primitifs ui/form), data-testid auth spec intacts ; aucun changement de logique auth.
    Admin (page.tsx) : reskin SOBRE (D-18) — tokens + primitifs, AUCUN hero/animation/Eyebrow purple flashy ; peut rester dépouillé.
    Propriétés logiques only partout. Aucun % nu, aucune promesse de gain. NE PAS toucher les fetch RLS / gating.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/test/no-perf-claims.test.ts && pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Chaque page reskinée conserve son <Disclaimer /> si déjà présent et ses data-testid (diff contre academie.spec/auth.spec)
    - Admin page.tsx : aucun import hero/* ni keyframe d'animation (D-18 sobre)
    - pnpm vitest run no-perf-claims exit 0
    - pnpm vitest run rtl-logical-props exit 0
    - Aucun forking de primitif ui/ (aucun nouveau fichier dans components/ui/ créé par ce plan)
  </acceptance_criteria>
  <done>Vitrine + Académie + auth + admin sobre au design NEXA, Disclaimer/RLS/testids préservés.</done>
</task>

<task type="auto">
  <name>Task 3 : Gate de phase — suite unit + 5 specs E2E (préservation)</name>
  <files>apps/web/src/app/[locale]/(marketing)/academie/page.tsx</files>
  <read_first>
    - apps/web/e2e/i18n.spec.ts, affiliation-attribution.spec.ts, gating.spec.ts, auth.spec.ts, academie.spec.ts (5 specs — sélecteurs)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (§Validation Architecture — Phase gate)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-VALIDATION.md (§Sampling Rate Phase gate)
  </read_first>
  <action>
    Exécuter le gate de phase complet. Lancer la suite unit Vitest entière (no-perf-claims étendu, no-mera-brand, rtl-logical-props étendu, parité i18n) — TOUT doit être vert. Puis lancer les 5 specs E2E Playwright (nécessite next dev :3000 + .env.local Supabase) ; si un sélecteur casse à cause du reskin, restaurer le data-testid/role manquant sur la surface fautive (fix surgical, pas de refonte). Aucune 6e spec à créer (divergence ROADMAP = coquille, 5 specs préservés). Documenter dans le SUMMARY le statut des 5 specs (vert ou human-verify si env Supabase indisponible en session, precedent D-01-04-C). Ce task ne modifie un fichier que si un testid doit être restauré (sinon zéro diff fonctionnel — fichier listé par défaut, à ne toucher que si nécessaire).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run && echo unit-green</automated>
  </verify>
  <acceptance_criteria>
    - pnpm vitest run (suite unit complète) exit 0 : no-perf-claims, no-mera-brand, rtl-logical-props, parité i18n tous verts
    - Les 5 specs E2E passent OU sont documentées human-verify (env Supabase requis) avec sélecteurs préservés vérifiés par grep
    - Aucun data-testid des 5 specs supprimé sur les surfaces reskinées (vérif grep par spec)
  </acceptance_criteria>
  <done>Gate de phase : unit verte, 5 specs E2E préservées (vertes ou human-verify documenté).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serveur (RLS anon-client) -> abonnement page | current_period_end fetché serveur sous RLS ; jamais service_role côté client. |
| surfaces reskinées -> gating/Disclaimer | Le reskin ne doit jamais retirer le Disclaimer ni affaiblir le gating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-RLS | Information Disclosure | abonnement ExpiryBanner | mitigate | Fetch RLS anon-client serveur (pattern member layout) ; aucun service_role client (acceptance). |
| T-11-DISC | (légal) | surfaces reskinées | mitigate | <Disclaimer /> préservé sur chaque page (acceptance grep) ; pas de fuite de contenu membre via reskin. |
| T-11-E2E | (régression sécurité) | data-testid gating/auth | mitigate | Gate E2E (Task 3) rejoue les 5 specs ; data-testid/role préservés. |
| T-11-PERF | (légal) | vitrine/track-record | mitigate | no-perf-claims vert ; ConfidenceStat via applyThreshold (jamais % nu). |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- pnpm vitest run (suite unit complète) vert.
- 5 specs Playwright vertes OU human-verify documenté (env Supabase), sélecteurs préservés.
- pnpm --filter web exec tsc -b --noEmit sans nouvelle erreur.
- Manuel (VALIDATION §Manual-Only) : RTL AR sur surfaces reskinées, cohérence visuelle.
</verification>

<success_criteria>
- UI-01/04/05/06 : toutes les surfaces au design NEXA, trilingues, Disclaimer + RLS + testids préservés.
- UI-07 : ExpiryBanner visible membre ET compte/abonnement (dette WIRING-01/PAY-05 levée).
- Gate de phase vert (unit + E2E préservés).
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-08-SUMMARY.md` when done.
</output>
