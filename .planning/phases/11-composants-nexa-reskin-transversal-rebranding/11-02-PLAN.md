---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/test/no-perf-claims.test.ts
  - apps/web/test/no-mera-brand.test.ts
  - apps/web/src/styles/__tests__/rtl-logical-props.test.ts
autonomous: true
requirements: [BRAND-04, BRAND-01, DESIGN-04]
must_haves:
  truths:
    - "Le test no-perf-claims scanne les namespaces i18n des nouveaux composants (hero, marquee, gauges/scoreRing, baseline)"
    - "Un test garde-fou prouve l'absence de MERA / Make Everybody Rich Again dans apps/web/src + messages"
    - "Le scan RTL logique couvre les nouveaux fichiers composant/hero"
  artifacts:
    - path: "apps/web/test/no-perf-claims.test.ts"
      provides: "scan no-perf étendu aux namespaces composant"
      contains: "hero"
    - path: "apps/web/test/no-mera-brand.test.ts"
      provides: "garde absence de marque MERA dans code+messages livrés"
      contains: "Make Everybody Rich Again"
  key_links:
    - from: "no-perf-claims.test.ts"
      to: "messages/{fr,en,ar}.json namespaces composant"
      via: "scan récursif"
      pattern: "hero|marquee|scoreRing|baseline"
---

<objective>
Wave-0 garde-fous (VALIDATION §Wave 0 Requirements). Avant que les composants/copy n'existent, étendre/créer les tests text-scan qui verrouillent les invariants légaux et de marque de toute la phase :
1. **no-perf-claims** étendu à la couverture composant (BRAND-04) — ajouter les namespaces i18n des nouveaux composants au scan.
2. **no-mera-brand** (nouveau, BRAND-01) — assert que `MERA` / `Make Everybody Rich Again` est absent de `apps/web/src` (code) et des messages livrés (hors `.planning/**`).
3. **rtl-logical-props** étendu (DESIGN-04) à la liste des nouveaux fichiers composant/hero.

Purpose : les gardes existent AVANT le code qu'ils protègent (Nyquist) — chaque tâche/wave ultérieure les fait tourner. Ce plan est greenfield-test : il définit le contrat que les copy/composants devront satisfaire.
Output : 3 fichiers de test prêts, verts à l'état actuel (namespaces composant pas encore créés -> scan tolérant à l'absence ; MERA déjà absent du code -> vert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md

<interfaces>
<!-- no-perf-claims.test.ts ACTUEL (à étendre) -->
const MARKETING_NAMESPACES = ['home', 'pricing', 'paiement'] as const
const FORBIDDEN = /%|\d+\s*%|garanti|guaranteed|\bprofit\b|rentable/i
function stripAllowed(value: string): string { return value.replace(/take-profits?/gi, '') } // whitelist take-profit(s), precedent D-02-03-D
<!-- rtl-logical-props.test.ts ACTUEL -->
const FOUNDATION_FILES = [ '../globals.css', '../../app/[locale]/layout.tsx' ]
const PHYSICAL_CLASS = /\b(ml-|mr-|pl-|pr-|left-|right-)/
<!-- vitest.config.ts include couvre : apps/web/test/**/*.test.ts ET apps/web/src/styles/__tests__/** -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Étendre no-perf-claims aux namespaces composant (BRAND-04)</name>
  <files>apps/web/test/no-perf-claims.test.ts</files>
  <behavior>
    - Le scan inclut les namespaces composant `hero`, `marquee`, `scoreRing`, `baseline`, `confidenceStat` en plus de `home`/`pricing`/`paiement`.
    - Un namespace absent du JSON (pas encore créé) est ignoré sans erreur (scan tolérant), pas un faux échec.
    - Le cas de contrôle `90% de trades gagnants` reste détecté (non trivial préservé).
    - `take-profit(s)` reste whitelisté (precedent D-02-03-D).
  </behavior>
  <read_first>
    - apps/web/test/no-perf-claims.test.ts (intégral — structure collectStrings/detectForbidden/FORBIDDEN/stripAllowed)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Copywriting Contract — règle BRAND-04 + whitelist take-profit)
  </read_first>
  <action>
    Étendre la constante de namespaces : renommer `MARKETING_NAMESPACES` -> `SCANNED_NAMESPACES` (ou conserver le nom et ajouter) en ajoutant `'hero'`, `'marquee'`, `'scoreRing'`, `'baseline'`, `'confidenceStat'`. Dans la boucle de scan, garder l'accès `(messages)[ns]` qui renvoie `undefined` si le namespace n'existe pas encore -> `collectStrings(undefined, ns)` retourne `[]` (déjà le cas via le garde `typeof obj !== 'object'`), donc tolérant à l'absence : ne RIEN ajouter qui jette sur namespace manquant. Conserver `FORBIDDEN`, `stripAllowed` (whitelist `take-profits?`), l'exclusion `legal`/`disclaimer`, et les 2 cas de contrôle non triviaux existants. Ajouter un commentaire d'en-tête « Phase 11 : couverture composant BRAND-04 (hero/marquee/scoreRing/baseline/confidenceStat) ». Comme `grep -c` compte les commentaires, ne pas dépendre d'un compteur de commentaires dans la vérif.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/test/no-perf-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/test/no-perf-claims.test.ts` exit 0
    - `grep -v '^[[:space:]]*//' apps/web/test/no-perf-claims.test.ts | grep -c "hero\|marquee\|scoreRing\|baseline\|confidenceStat"` >= 1 (namespaces ajoutés dans du code, pas un commentaire)
    - Le test reste vert même si `hero`/`marquee` sont absents des JSON (tolérance à l'absence)
  </acceptance_criteria>
  <done>no-perf-claims scanne 8 namespaces, tolérant à l'absence, contrôles non triviaux préservés, vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Créer no-mera-brand garde-fou (BRAND-01)</name>
  <files>apps/web/test/no-mera-brand.test.ts</files>
  <behavior>
    - Le test FAIL si `MERA` (mot entier, insensible casse, hors substrings légitimes comme "numera"... -> ancrer en `\bMERA\b`) OU `Make Everybody Rich Again` apparaît dans un fichier de `apps/web/src/**` ou dans `messages/{fr,en,ar}.json`.
    - Cas de contrôle : la regex détecte bien une chaîne plantée `MERA2026` et `Make Everybody Rich Again` (non trivial).
    - Le test ne scanne PAS `.planning/**` (docs non livrées).
  </behavior>
  <read_first>
    - apps/web/test/no-perf-claims.test.ts (style text-scan + cas de contrôle à mirrorer)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Rebranding targets — 4 cibles exactes ; tout MERA hors .planning doit disparaître)
    - apps/web/src/messages/fr.json (occurrences actuelles : codePlaceholder "Ex : MERA2026" l.~210, "Vétéran Trading" l.~489)
  </read_first>
  <action>
    Créer `apps/web/test/no-mera-brand.test.ts` (placement couvert par le glob `apps/web/test/**`). Utiliser `node:fs` + parcours récursif de `apps/web/src` (filtrer extensions `.ts/.tsx/.json/.css`, exclure `node_modules`/`__tests__`/`.next`). Regex `BRAND_FORBIDDEN = /\bMERA\b|Make Everybody Rich Again/i`. Scanner aussi explicitement les 3 messages JSON. Pour chaque offender, pousser `chemin:ligne`. `expect(offenders).toEqual([])`. Ajouter un `it` de contrôle non trivial : `expect(BRAND_FORBIDDEN.test('Ex : MERA2026')).toBe(true)` et `expect(BRAND_FORBIDDEN.test('Make Everybody Rich Again'))` true, et un négatif `expect(BRAND_FORBIDDEN.test('numérateur'))` false. NOTE : à l'état actuel `fr.json` contient ENCORE `MERA2026` (l.210) -> ce test sera **RED** jusqu'à ce que le plan 11-05 (rebranding i18n) renomme. C'est attendu (RED Wave-0). Documenter en commentaire d'en-tête que le GREEN arrive avec 11-05. Marquer ces deux assertions sur fichiers réels via `it` séparé pour que le contrôle non-trivial reste vérifiable indépendamment.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/test/no-mera-brand.test.ts 2>&1 | grep -E "non trivial|control|MERA" | head -5; echo "exit-aware: le cas de controle doit passer"</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `apps/web/test/no-mera-brand.test.ts` existe
    - L'`it` de contrôle non trivial (regex détecte `MERA2026` + `Make Everybody Rich Again`) PASSE
    - L'`it` de scan réel est RED tant que `fr.json:210` n'est pas renommé (attendu, GREEN livré en 11-05) — documenté en en-tête
    - `grep -c "Make Everybody Rich Again" apps/web/test/no-mera-brand.test.ts` >= 1
  </acceptance_criteria>
  <done>Garde no-mera-brand créé, contrôle non trivial vert, scan réel prêt à virer au vert dès le rebranding i18n (11-05).</done>
</task>

<task type="auto">
  <name>Task 3 : Étendre rtl-logical-props aux fichiers composant/hero (DESIGN-04)</name>
  <files>apps/web/src/styles/__tests__/rtl-logical-props.test.ts</files>
  <read_first>
    - apps/web/src/styles/__tests__/rtl-logical-props.test.ts (intégral — FOUNDATION_FILES + PHYSICAL_CLASS + scanFile)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (§Propriétés logiques RTL — fichiers cibles)
  </read_first>
  <action>
    Élargir `FOUNDATION_FILES` (ou ajouter une constante `RESKIN_FILES`) avec un scan par glob du dossier `apps/web/src/components/nexa/` et `apps/web/src/components/hero/` (lire récursivement les `.tsx` existants au moment du run via `node:fs` `readdirSync` avec garde « dossier absent -> liste vide »). Conserver `PHYSICAL_CLASS = /\b(ml-|mr-|pl-|pr-|left-|right-)/` et le cas de contrôle non trivial. La tolérance « dossier absent -> [] » garde le test vert à l'état actuel (dossiers nexa/hero pas encore créés) et le rend protecteur dès qu'ils existent. NE PAS retirer globals.css/layout.tsx du scan.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts` exit 0
    - Le test référence `components/nexa` et `components/hero` (grep le confirme dans du code non commenté)
    - Tolérance « dossier absent » : vert à l'état actuel
  </acceptance_criteria>
  <done>Scan RTL couvre fondation + futurs composants/hero, vert, protecteur dès création des dossiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy i18n -> rendu UI | La copy traverse de l'i18n vers l'écran ; un % nu ou une promesse de gain = exposition légale (guardrail). |
| code source -> build livré | La marque MERA ne doit pas franchir vers le build livré. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LEGAL | (légal/Information Disclosure) | copy composant | mitigate | no-perf-claims étendu aux namespaces composant (BRAND-04) — % nu/promesse bloqués en CI. |
| T-11-BRAND | (intégrité marque) | code+messages livrés | mitigate | no-mera-brand scanne apps/web/src + messages (hors .planning) — MERA/slogan bloqués. |
| T-11-RTL | Tampering (layout RTL) | composants reskinés | mitigate | rtl-logical-props étendu aux nouveaux dossiers — classes physiques bloquées. |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `pnpm vitest run apps/web/test/no-perf-claims.test.ts` vert.
- `pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts` vert.
- no-mera-brand : `it` de contrôle vert ; `it` de scan réel documenté RED jusqu'à 11-05.
</verification>

<success_criteria>
- Les 3 gardes-fous existent et tournent avant le code qu'ils protègent (Nyquist Wave-0).
- Tolérance à l'absence : aucun faux échec pour des namespaces/dossiers pas encore créés.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-02-SUMMARY.md` when done.
</output>
