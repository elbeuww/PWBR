---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/web/src/components/nexa/Eyebrow.tsx
  - apps/web/src/components/nexa/ScoreRing.tsx
  - apps/web/src/components/nexa/Marquee.tsx
  - apps/web/src/components/nexa/ConfidenceStat.tsx
  - apps/web/src/components/nexa/Logo.tsx
  - apps/web/src/styles/globals.css
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: true
requirements: [DESIGN-05, BRAND-03, BRAND-04]
must_haves:
  truths:
    - "Une bibliothèque NEXA tokenisée existe (Eyebrow, ScoreRing, Marquee, ConfidenceStat, Logo) suivant le moule cva + data-slot + cn ; wordmark Archivo bold tracking large majuscules (D-07) ; Marquee = instruments couverts + sessions de marché, neutre, zéro % (D-14)"
    - "ScoreRing rend un anneau radial SVG, chiffre au centre, lisible liste+détail (D-11), couleur = RISQUE neutre->amber->bearish jamais vert=gagnant (D-12), jamais --primary/green, role=meter accessible"
    - "ConfidenceStat passe TOUJOURS par applyThreshold (jamais un % nu) et affiche win-rate mesuré + N + provenance (D-13)"
    - "Logo = SVG mark emblème hexagonal N (D-06) + wordmark Archivo, gradient green->purple #03d87f->#63279b (D-08, seule exception var()-only sanctionnée)"
  artifacts:
    - path: "apps/web/src/components/nexa/ScoreRing.tsx"
      provides: "anneau radial SVG accessible, couleur risque"
      contains: "role=\"meter\""
    - path: "apps/web/src/components/nexa/ConfidenceStat.tsx"
      provides: "stat de confiance via applyThreshold"
      contains: "applyThreshold"
    - path: "apps/web/src/components/nexa/Logo.tsx"
      provides: "SVG mark hexagonal + wordmark"
      contains: "linearGradient"
  key_links:
    - from: "ConfidenceStat.tsx"
      to: "lib/track-record/threshold applyThreshold"
      via: "import + appel"
      pattern: "applyThreshold"
    - from: "Marquee.tsx anim"
      to: "globals.css keyframes nexa-scroll"
      via: "classe CSS + prefers-reduced-motion"
      pattern: "nexa-scroll"
---

<objective>
Construire la bibliothèque de composants NEXA tokenisée (DESIGN-05) : 5 composants in-repo suivant le moule cva + data-slot + cn des primitifs existants, référençant UNIQUEMENT la couche component des tokens (var()), jamais de primitive ni de littéral — sauf le SVG du Logo (identité chromatique fixe, exception sanctionnée). Ajouter les keyframes Marquee dans globals.css et la copy i18n associée (3 langues à parité).

Purpose : ces composants sont consommés par le hero (11-07), le reskin membre (11-06), le rebranding header (11-05) et la vitrine (11-08). Interface-first : ce plan livre les contrats que les plans aval consomment.
Output : components/nexa/{Eyebrow,ScoreRing,Marquee,ConfidenceStat,Logo}.tsx + keyframes globals.css + namespaces i18n marquee/scoreRing/confidenceStat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-01-SUMMARY.md
@apps/web/src/components/ui/badge.tsx

<interfaces>
Moule cva + data-slot + cn (precedent badge.tsx) :
  import { cva, type VariantProps } from "class-variance-authority"
  import { cn } from "@/lib/utils"
  data-slot sur racine ; React.ComponentProps + VariantProps ; cn(variants(...), className) en dernier
Tokens consommables (couche component, après 11-01) :
  bg-primary text-foreground text-muted-foreground border-border bg-card
  text-accent-brand / var(--accent-brand) (purple, 11-01)
  var(--risk-moderate) (amber, 11-01)  var(--signal-bearish)
Polices (Phase 10, CSS vars) :
  font-display (Archivo wordmark)  font-accent (Chakra Petch eyebrow)  font-mono (JetBrains chiffres)
applyThreshold source unique (D-13) :
  import { applyThreshold } from '@/lib/track-record/threshold'
  applyThreshold({ n, win_rate, expectancy, avg_r }) -> { sufficient:true, winRatePct, n, ... } | { sufficient:false, n }
  MIN_SAMPLE=30 ; sous le seuil -> état "en construction", N toujours visible
Hex de marque EXACTS (Logo SVG seulement) : #03d87f green -> #63279b purple
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Eyebrow + Marquee (cva + keyframes CSS + i18n)</name>
  <files>apps/web/src/components/nexa/Eyebrow.tsx, apps/web/src/components/nexa/Marquee.tsx, apps/web/src/styles/globals.css, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <behavior>
    - Eyebrow rend un span data-slot eyebrow, font-accent uppercase, variante purple consommant var(--accent-brand), variante muted consommant text-muted-foreground.
    - Marquee rend une bande défilante instruments+sessions (zéro %), aria-label i18n, items dupliqués aria-hidden ; sous prefers-reduced-motion -> liste statique ; RTL -> sens inversé.
    - no-perf-claims reste vert sur les namespaces marquee ajoutés.
  </behavior>
  <read_first>
    - apps/web/src/components/ui/badge.tsx (moule cva + data-slot + cn)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 1 Eyebrow, Pattern 5 Marquee — keyframes nexa-scroll/nexa-scroll-rtl)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (sections Eyebrow, Marquee, Reduced-motion double-garde)
    - apps/web/src/styles/globals.css (où ajouter keyframes — fin de fichier après @layer base)
    - apps/web/src/messages/fr.json (structure namespaces pour parité)
  </read_first>
  <action>
    Eyebrow.tsx : copier le moule badge (cva eyebrowVariants), classes inline-flex items-center font-accent text-sm font-semibold uppercase tracking-wide ; variants tone purple = text-[var(--accent-brand)], muted = text-muted-foreground ; defaultVariants tone purple ; racine span data-slot eyebrow ; props React.ComponentProps span + VariantProps. Export Eyebrow + eyebrowVariants.
    Marquee.tsx : RSC ; div.nexa-marquee.overflow-hidden role marquee aria-label depuis t marquee.label, contenant ul.nexa-marquee-track.flex.gap-6 avec items + clone aria-hidden. Items via i18n marquee.items. Propriétés logiques only.
    globals.css : ajouter keyframes nexa-scroll (translateX -50%) et nexa-scroll-rtl (translateX 50%), .nexa-marquee-track animation none par défaut, sous @media (prefers-reduced-motion: no-preference) animation nexa-scroll 30s linear infinite, et [dir="rtl"] .nexa-marquee-track animation-name nexa-scroll-rtl. Aucune classe utilitaire physique.
    messages : ajouter aux 3 fichiers (parité STRICTE) namespace marquee { label, items } FR/EN/AR. Sans slop, zéro %, zéro promesse. AR lisible en miroir.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "data-slot" apps/web/src/components/nexa/Eyebrow.tsx && grep -c "nexa-scroll" apps/web/src/styles/globals.css && pnpm vitest run apps/web/test/no-perf-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Eyebrow.tsx contient data-slot eyebrow et var(--accent-brand) ; aucun --nexa-purple- primitive
    - globals.css contient keyframes nexa-scroll ET nexa-scroll-rtl ET prefers-reduced-motion no-preference
    - Marquee.tsx contient aria-label et aucune classe physique
    - Les 3 messages JSON ont le namespace marquee à parité
    - pnpm vitest run no-perf-claims exit 0
  </acceptance_criteria>
  <done>Eyebrow + Marquee livrés tokenisés, keyframes RTL-aware reduced-motion-safe, i18n à parité, no-perf vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : ScoreRing (SVG radial accessible, couleur = risque)</name>
  <files>apps/web/src/components/nexa/ScoreRing.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <behavior>
    - ScoreRing rend un anneau SVG via stroke-dasharray/stroke-dashoffset (offset = c*(1 - score/100), c = 2*pi*r), chiffre centré font-mono tabular-nums dans un bdi.
    - Couleur stroke = RISQUE : faible -> var(--muted-foreground), modéré -> var(--risk-moderate), élevé -> var(--signal-bearish). JAMAIS --primary ni --signal-bullish (D-12).
    - role meter aria-valuenow/min/max + aria-label i18n parlant (score + risque) ; svg décoratif aria-hidden ; hit-area >= 44px en taille liste.
    - score hors [0,100] clampé.
  </behavior>
  <read_first>
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 2 ScoreRing — code SVG complet, RISK_STROKE, role meter)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (section ScoreRing — D-11/D-12, hit-area 44px, bdi font-mono)
    - apps/web/src/components/signals/SignalCard.tsx (l.62-74 sémantique score neutre, precedent tabular-nums)
    - apps/web/src/styles/globals.css (tokens --muted-foreground / --risk-moderate / --signal-bearish)
  </read_first>
  <action>
    Créer ScoreRing.tsx en RSC : faire fournir le aria-label déjà traduit par l'appelant via prop label (precedent dialog closeLabel D-02-01-F) pour éviter use client. Props { score, risk: faible|modere|eleve, size?, label }. Calculer r=(size-6)/2, c=2*pi*r, offset=c*(1 - clamp(score,0,100)/100). Table RISK_STROKE { faible: var(--muted-foreground), modere: var(--risk-moderate), eleve: var(--signal-bearish) }. Conteneur div role meter aria-valuenow score aria-valuemin 0 aria-valuemax 100 aria-label label style inlineSize/blockSize size minInlineSize/minBlockSize 44 ; svg aria-hidden avec 2 circle (track stroke var(--border) ; progress stroke RISK_STROKE risk strokeLinecap round strokeDasharray c strokeDashoffset offset transform rotate -90) ; span font-mono tabular-nums avec bdi score. Ajouter aux 3 messages namespace scoreRing { riskLabels: { faible, modere, eleve }, ariaTemplate } à parité, zéro %.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "role=\"meter\"" apps/web/src/components/nexa/ScoreRing.tsx && test $(grep -cE "var\(--primary\)|signal-bullish" apps/web/src/components/nexa/ScoreRing.tsx) -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - ScoreRing.tsx contient role="meter", aria-valuenow, aria-valuemax, aria-label
    - grep -cE "var\(--primary\)|signal-bullish" sur ScoreRing.tsx retourne 0 (jamais green=gagnant)
    - ScoreRing.tsx référence var(--risk-moderate) ET var(--signal-bearish) ET var(--muted-foreground)
    - minInlineSize/minBlockSize 44 présents (hit-area)
    - namespace scoreRing à parité dans les 3 JSON
  </acceptance_criteria>
  <done>ScoreRing radial accessible, couleur=risque jamais green, hit-area 44px, i18n à parité.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : ConfidenceStat (via applyThreshold) + Logo SVG</name>
  <files>apps/web/src/components/nexa/ConfidenceStat.tsx, apps/web/src/components/nexa/Logo.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <behavior>
    - ConfidenceStat appelle applyThreshold ; si sufficient false -> état "en construction (N insuffisant)" SANS chiffre de %, N toujours visible + provenance (backtest/réel) ; si sufficient true -> winRatePct + N + provenance, chiffres font-mono dans bdi. Jamais un % nu hors applyThreshold.
    - Logo rend un SVG inline : mark hexagonal N + wordmark NEXA (font-display Archivo bold uppercase tracking large), linearGradient #03d87f->#63279b, variantes clair/sombre ; jamais dangerouslySetInnerHTML.
  </behavior>
  <read_first>
    - apps/web/src/components/track-record/TrackRecordBlock.tsx (precedent applyThreshold — import + usage)
    - apps/web/src/components/ui/card.tsx (multi-parts data-slot pour ConfidenceStat composé)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (sections ConfidenceStat + Logo)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 9 Logo — exception var() pour hex de marque)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (Copywriting — état < N=30 "Track record en construction (N insuffisant)")
  </read_first>
  <action>
    ConfidenceStat.tsx : props { n, win_rate, expectancy, avg_r, provenance: 'backtest'|'reel', labels } ; importer applyThreshold depuis '@/lib/track-record/threshold' ; const r = applyThreshold({ n, win_rate, expectancy, avg_r }) ; rendu composé via Card/CardHeader/CardContent (data-slot par sous-élément) ; si r.sufficient false -> afficher le texte i18n "en construction" + N (r.n) + badge provenance, AUCUN winRatePct ; si true -> winRatePct en font-mono bdi + N + provenance. Toute valeur numérique en font-mono + bdi, propriétés logiques. Couleur neutre (text-foreground/muted), jamais green pour encoder la perf.
    Logo.tsx : RSC, SVG inline statique authored (PAS dangerouslySetInnerHTML). defs linearGradient id nexa-grad de #03d87f à #63279b ; path/polygon hexagone + glyphe N rempli url(#nexa-grad) ; wordmark via texte <span className="font-display font-bold uppercase tracking-[0.2em]">NEXA</span> à côté du SVG (ou <text> SVG). Props { variant?: 'full'|'mark', className } : mark = hexagone seul (pour favicon/footer), full = mark + wordmark. Variantes clair/sombre via currentColor sur les traits neutres si besoin ; le gradient reste fixe (identité). Commenter "exception sanctionnée au var()-only : asset de marque (RESEARCH Pattern 9 / A2)".
    messages : ajouter aux 3 fichiers namespace confidenceStat { inConstruction, sampleLabel, provenanceBacktest, provenanceReel } + baseline { text } "Nouvelle Ère · Alliance d'Échange" FR + EN + AR (D-15, sans slop, sans promesse). Parité STRICTE.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "applyThreshold" apps/web/src/components/nexa/ConfidenceStat.tsx && grep -c "linearGradient" apps/web/src/components/nexa/Logo.tsx && test $(grep -c "dangerouslySetInnerHTML" apps/web/src/components/nexa/Logo.tsx) -eq 0 && pnpm vitest run apps/web/test/no-perf-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ConfidenceStat.tsx importe et appelle applyThreshold ; aucun winRatePct rendu hors la branche sufficient true
    - ConfidenceStat affiche N et provenance dans les deux branches
    - Logo.tsx contient linearGradient avec #03d87f ET #63279b ; aucun dangerouslySetInnerHTML
    - baseline + confidenceStat namespaces à parité dans les 3 JSON
    - pnpm vitest run no-perf-claims exit 0 (baseline/confidenceStat sans % nu)
  </acceptance_criteria>
  <done>ConfidenceStat passe par applyThreshold (jamais % nu), Logo SVG statique gradient marque, i18n baseline à parité.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| données mesurées (pattern_stats) -> ConfidenceStat | Tout % affiché doit transiter par applyThreshold (source unique) — jamais un nombre inventé. |
| SVG authored -> DOM | Le Logo/ScoreRing sont des SVG statiques authored, pas du contenu utilisateur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-PERF | (légal) | ConfidenceStat | mitigate | applyThreshold obligatoire ; no-perf-claims couvre confidenceStat/baseline (% nu bloqué). |
| T-11-XSS | Tampering | Logo / ScoreRing SVG | mitigate | SVG statique authored ; interdiction dangerouslySetInnerHTML (grep = 0). |
| T-11-SCORE | (intégrité visuelle) | ScoreRing | mitigate | Couleur=risque only ; grep interdit --primary/signal-bullish (green=gagnant impossible). |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé ; composants in-repo (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- Tous les greps d'acceptance verts (data-slot, role meter, applyThreshold, linearGradient, no dangerouslySetInnerHTML).
- pnpm vitest run no-perf-claims vert (marquee/baseline/confidenceStat/scoreRing sans % nu).
- Parité i18n : check-i18n-hardcoded.mjs + tests de parité existants verts.
- pnpm --filter web exec tsc -b --noEmit sans nouvelle erreur sur components/nexa.
</verification>

<success_criteria>
- Bibliothèque NEXA (DESIGN-05) complète et tokenisée, consommable par hero/membre/vitrine.
- ScoreRing accessible couleur=risque ; ConfidenceStat jamais % nu ; Logo gradient marque exact.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-04-SUMMARY.md` when done.
</output>
