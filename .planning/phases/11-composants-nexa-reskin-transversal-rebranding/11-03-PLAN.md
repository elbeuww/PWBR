---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/web/src/components/signals/CandleChart.tsx
  - apps/web/src/components/signals/SignalCard.tsx
  - apps/web/src/components/member/ExpiryBanner.tsx
  - apps/web/src/components/ui/alert.tsx
autonomous: true
requirements: [UI-03, UI-07, DESIGN-05]
must_haves:
  truths:
    - "CandleChart rend ses couleurs depuis les tokens NEXA résolus (signal-bullish/bearish/foreground), pas des HEX en dur, et se re-colore au changement de thème"
    - "SignalCard encode la direction via --signal-* tokens (flip-safe), zéro HEX direction en dur"
    - "ExpiryBanner utilise une variante warning tokenisée (amber via token component), zéro littéral amber-*"
  artifacts:
    - path: "apps/web/src/components/signals/CandleChart.tsx"
      provides: "recoloration lwc via getComputedStyle + applyOptions au theme toggle"
      contains: "getComputedStyle"
    - path: "apps/web/src/components/ui/alert.tsx"
      provides: "variante warning tokenisée pour ExpiryBanner"
      contains: "warning"
  key_links:
    - from: "CandleChart.tsx"
      to: "globals.css tokens --signal-bullish/--signal-bearish/--foreground"
      via: "getComputedStyle getPropertyValue"
      pattern: "getPropertyValue\\(.--signal"
    - from: "ExpiryBanner.tsx"
      to: "alert.tsx variante warning"
      via: "variant warning"
      pattern: "variant=.warning"
---

<objective>
Recoloration résiduelle des HEX/littéraux couleur hardcodés (RESEARCH §Pitfall 1, PATTERNS §recoloration) vers les tokens NEXA. Trois cibles : CandleChart.tsx:49-53 (HEX v2.0, lwc ne lit pas les CSS vars -> Anti-Pattern 5), SignalCard.tsx:37-39 (HEX direction + variantes dark: manuelles), ExpiryBanner.tsx:49 (border-amber-* -> token amber de 11-01).

Purpose : supprimer toute couleur qui ne flippe pas avec le thème ; livrer UI-07 (tokenisation ExpiryBanner) et la moitié couleur de UI-03. Recoloration UNIQUEMENT — ne JAMAIS toucher le fetch RLS serveur (Anti-Pattern 3, HARD).
Output : 3 composants recolorés via tokens + 1 variante warning ajoutée à alertVariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-01-SUMMARY.md

<interfaces>
Tokens disponibles après 11-01 (couche component, flip-safe) :
  --signal-bullish (#16a34a sobre)  --signal-bearish (#dc2626)  --foreground  --border
  --risk-moderate (amber, 11-01)  --color-risk-moderate
CandleChart.tsx:49-53 ACTUEL :
  UP_COLOR=#15803D  DOWN_COLOR=#B91C1C  ENTRY_COLOR=#1E5FBF  SL_COLOR=#B91C1C  TP_COLOR=#15803D
  createChart ~l.81 ; addSeries(CandlestickSeries,{upColor,downColor,...}) ~96-104 ; createPriceLine({color}) ~117-142
SignalCard.tsx:37-39 ACTUEL :
  isLong ? 'bg-[#15803D]/10 text-[#15803D] dark:bg-[#22C55E]/15 dark:text-[#22C55E]' : 'bg-[#B91C1C]/10 ...'
ExpiryBanner.tsx:49 ACTUEL (composant use client, logique J-3/J-1 intacte) :
  Alert className border-amber-600/30 bg-amber-500/10 text-amber-800 dark:text-amber-300
alert.tsx alertVariants : variant { default, destructive } -- ajouter 'warning'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Recolorer CandleChart via tokens résolus lwc + re-color au theme toggle</name>
  <files>apps/web/src/components/signals/CandleChart.tsx</files>
  <read_first>
    - apps/web/src/components/signals/CandleChart.tsx (intégral — HEX l.49-53, createChart, addSeries, createPriceLine, useEffect deps)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (section CandleChart — mapping UP/TP vers bullish, DOWN/SL vers bearish, ENTRY vers foreground ; Pitfall 5 theme toggle)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 7 recoloration lwc, readToken)
    - apps/web/src/styles/globals.css (tokens --signal-bullish/--signal-bearish/--foreground)
  </read_first>
  <action>
    Supprimer les 5 const HEX (l.49-53). Dans le useEffect existant, AVANT createChart, ajouter readToken(name) = getComputedStyle(containerRef.current).getPropertyValue(name).trim() et lire UP/TP via --signal-bullish, DOWN/SL via --signal-bearish, ENTRY via --foreground. Injecter ces valeurs dans addSeries(CandlestickSeries, { upColor, downColor, wickUpColor, wickDownColor, borderVisible:false }) et dans chaque createPriceLine({ color }). Theme toggle (Pitfall 5) : importer useTheme de next-themes, ajouter resolvedTheme aux dépendances du useEffect OU attacher un MutationObserver sur document.documentElement.classList qui relit les tokens et appelle series.applyOptions plus mise à jour des priceLines à chaque flip .dark. NE PAS toucher le fetch RLS ni la frontière serveur (composant reste use client ssr:false, aucune requête déplacée). Conserver les data-testid et rôles existants.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && test $(grep -cE "#(15803D|B91C1C|1E5FBF)" apps/web/src/components/signals/CandleChart.tsx) -eq 0 && grep -c getPropertyValue apps/web/src/components/signals/CandleChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "#(15803D|B91C1C|1E5FBF)" sur CandleChart.tsx retourne 0
    - CandleChart.tsx contient getComputedStyle et getPropertyValue('--signal-bullish')
    - CandleChart.tsx contient applyOptions OU MutationObserver (re-color au flip thème)
    - Aucun createClient / from( / supabase ajouté (fetch RLS intact, vérif diff)
  </acceptance_criteria>
  <done>CandleChart lit ses couleurs depuis les tokens résolus, se re-colore au theme toggle, fetch RLS intact.</done>
</task>

<task type="auto">
  <name>Task 2 : Tokeniser SignalCard direction (HEX -> --signal-*)</name>
  <files>apps/web/src/components/signals/SignalCard.tsx</files>
  <read_first>
    - apps/web/src/components/signals/SignalCard.tsx (intégral — directionClass l.37-39, bloc score l.62-74 qui reste neutre)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (section SignalCard — direction via --signal-*, score reste neutre text-primary)
  </read_first>
  <action>
    Remplacer directionClass (l.37-39) par des classes basées sur les tokens flip-safe : long -> bg-[var(--signal-bullish)]/10 text-[var(--signal-bullish)] ; short -> bg-[var(--signal-bearish)]/10 text-[var(--signal-bearish)]. Supprimer les variantes dark: manuelles (le token flippe seul). NE PAS toucher le bloc score (l.62-74) neutre (text-primary, déjà conforme D-12). Conserver bdi/font-mono/propriétés logiques et tout data-testid. Couleur = direction UNIQUEMENT (D-03).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && test $(grep -cE "#(15803D|B91C1C|22C55E|EF4444)" apps/web/src/components/signals/SignalCard.tsx) -eq 0 && grep -c "signal-bullish\|signal-bearish" apps/web/src/components/signals/SignalCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "#(15803D|B91C1C|22C55E|EF4444)" sur SignalCard.tsx retourne 0
    - SignalCard.tsx contient var(--signal-bullish) ET var(--signal-bearish)
    - Aucune classe dark: manuelle de direction restante
    - Bloc score inchangé (text-primary toujours présent)
  </acceptance_criteria>
  <done>Direction encodée par tokens flip-safe, score neutre intact, zéro HEX direction.</done>
</task>

<task type="auto">
  <name>Task 3 : Variante warning tokenisée dans alert.tsx + tokeniser ExpiryBanner</name>
  <files>apps/web/src/components/ui/alert.tsx, apps/web/src/components/member/ExpiryBanner.tsx</files>
  <read_first>
    - apps/web/src/components/ui/alert.tsx (alertVariants — variants default/destructive l.6-22 ; NOTE: ligne 6 utilise text-left physique préexistant — NE PAS y toucher, hors périmètre)
    - apps/web/src/components/member/ExpiryBanner.tsx (intégral — Alert l.49 amber hardcodé ; logique daysUntil/fenêtre J-3/J-1 l.29-42 INTACTE)
    - apps/web/src/styles/globals.css (token --risk-moderate / --color-risk-moderate ajouté en 11-01)
  </read_first>
  <action>
    Dans alert.tsx alertVariants, ajouter une variante warning à côté de destructive : utiliser le token amber component-layer de 11-01 — par ex. border-[var(--risk-moderate)]/30 bg-[var(--risk-moderate)]/10 text-[var(--risk-moderate)] et *:data-[slot=alert-description]:text-[var(--risk-moderate)]. Aucun littéral amber-* / couleur Tailwind nommée. Dans ExpiryBanner.tsx l.49, remplacer le className amber hardcodé par variant="warning" sur Alert (retirer border-amber-*/bg-amber-*/text-amber-*/dark:text-amber-*). Conserver intégralement la logique J-3/J-1 (daysUntil, fenêtre, coupe nette), l'ICU payment.expiryBanner, le CTA Renouveler, les propriétés logiques (text-start/mx-auto). NE PAS modifier la lecture RLS (le composant reçoit currentPeriodEnd en prop, fetché serveur — Anti-Pattern 3).
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && test $(grep -c "amber-" apps/web/src/components/member/ExpiryBanner.tsx) -eq 0 && grep -c "warning" apps/web/src/components/ui/alert.tsx && grep -c "variant=\"warning\"" apps/web/src/components/member/ExpiryBanner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "amber-" sur ExpiryBanner.tsx retourne 0
    - alert.tsx alertVariants contient une variante warning consommant var(--risk-moderate) (aucun littéral amber-*)
    - ExpiryBanner.tsx utilise variant="warning" sur Alert
    - Logique J-3/J-1 (daysUntil, windowDays, remaining <= 0 return null) inchangée
  </acceptance_criteria>
  <done>ExpiryBanner stylé par variante warning tokenisée, zéro amber littéral, logique d'alerte intacte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serveur (fetch RLS) -> composant client | currentPeriodEnd et données signaux sont fetchées serveur sous RLS ; le reskin ne doit jamais migrer ce fetch au client. |
| token CSS résolu -> API JS lwc | getComputedStyle lit des tokens de build, pas d'input utilisateur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-RLS | Information Disclosure | CandleChart / ExpiryBanner | mitigate | Recoloration uniquement ; acceptance interdit tout createClient/from(/supabase ajouté. Fetch RLS serveur préservé (Anti-Pattern 3). |
| T-11-FLIP | Tampering | couleurs ne flippant pas | mitigate | Tokens component-layer flip-safe ; HEX/amber littéraux supprimés (grep = 0). |
| T-11-XSS | Tampering | lwc applyOptions | accept | Valeurs de couleur lues depuis tokens authored (getComputedStyle), aucune entrée dynamique injectée dans le chart. |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- Les 3 greps HEX/amber retournent 0.
- `pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts` vert (composants signals non régressés sur classes physiques — note: alert.tsx text-left préexistant n'est PAS scanné par ce garde).
- `pnpm --filter web exec tsc -b --noEmit` sans nouvelle erreur sur les 4 fichiers.
</verification>

<success_criteria>
- UI-07 livré côté tokenisation (ExpiryBanner warning tokenisé) — le câblage (account)/abonnement est traité en 11-08.
- Moitié couleur de UI-03 livrée (CandleChart + SignalCard tokenisés, theme-flip correct).
- Frontière de confiance RLS intacte (Anti-Pattern 3).
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-03-SUMMARY.md` when done.
</output>
