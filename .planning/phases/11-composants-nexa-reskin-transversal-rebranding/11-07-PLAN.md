---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 07
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - apps/web/src/components/hero/Hero.tsx
  - apps/web/src/components/hero/WireframeGlobe.tsx
  - apps/web/src/components/hero/DataRain.tsx
  - apps/web/src/components/hero/FloatingCards.tsx
  - apps/web/src/components/hero/HeroTilt.tsx
  - apps/web/src/styles/globals.css
  - apps/web/src/app/[locale]/(marketing)/page.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: true
requirements: [UI-02, DESIGN-05, BRAND-04]
must_haves:
  truths:
    - "Le hero animé complet (globe filaire rotatif green/purple CSS (D-02) + cartes flottantes + data-rain subtil fond lointain (D-03) + tilt/parallaxe) est livré sur la home"
    - "Toute animation est gardée par prefers-reduced-motion (CSS @media + garde JS) ; sous reduced-motion la composition est 100% statique mais visible ; CSS + vanilla TS uniquement, GSAP/three.js/WebGL interdits (D-05)"
    - "Les cartes flottantes affichent des setups anonymisés éducatifs (instrument · direction · score /100 · niveau de risque) avec ZÉRO % et aucun chiffre de gain (D-01)"
    - "Le hero a un fond sombre fixe ink (--nexa-ink) indépendant du thème même en clair (D-04)"
  artifacts:
    - path: "apps/web/src/components/hero/Hero.tsx"
      provides: "composition hero RSC + îlots client"
      contains: "FloatingCards"
    - path: "apps/web/src/components/hero/HeroTilt.tsx"
      provides: "tilt vanilla TS avec garde reduced-motion"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "(marketing)/page.tsx"
      to: "components/hero/Hero.tsx"
      via: "import + rendu remplaçant le hero statique l.33-44"
      pattern: "<Hero"
    - from: "hero anim"
      to: "globals.css keyframes + prefers-reduced-motion"
      via: "classes CSS gardées"
      pattern: "prefers-reduced-motion"
---

<objective>
Livrer le hero animé greenfield (UI-02) en CSS + vanilla TS UNIQUEMENT (D-05, GSAP/three.js/WebGL INTERDITS) : globe filaire rotatif (D-02), cartes flottantes de setups anonymisés zéro % (D-01), data-rain subtil de fond (D-03), fond ink fixe indépendant du thème (D-04), tilt/parallaxe au pointer. Tout est doublement gardé par prefers-reduced-motion (CSS @media + garde JS avant listener) -> composition statique mais visible sous reduced-motion.

Purpose : remplir UI-02. Remplace le hero statique de (marketing)/page.tsx:33-44. Dépend de 11-04 (Eyebrow, baseline i18n, ScoreRing, keyframes pattern).
Output : components/hero/* + keyframes globe/data-rain dans globals.css + page.tsx câblée + copy hero i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-04-SUMMARY.md

<interfaces>
Contraintes D-01..05 (HARD) :
  CSS + vanilla TS only. GSAP/three.js/WebGL/motion INTERDITS.
  Globe filaire SVG/CSS rotate green->purple (D-02). Data-rain repeating-linear-gradient + keyframes, aria-hidden, derrière contenu (D-03). Cartes setups anonymisés instrument·direction·score·risque, ZÉRO % (D-01). Fond --nexa-ink FIXE même en thème clair (D-04). Tilt pointermove vanilla -> CSS custom props, garde reduced-motion (D-05).
  prefers-reduced-motion => composition statique, visible (pas vide).
Patterns RESEARCH : Pattern 3 globe, Pattern 4 data-rain, Pattern 6 tilt vanilla.
Composants 11-04 : Eyebrow, baseline i18n baseline.text, ScoreRing, Button asChild + Link localisé.
Hero statique actuel (page.tsx:33-44) : section h1 heroTitle, p heroLede, Button -> /tarifs. CTA UI-SPEC : "Découvrir la méthode" + "Voir les tarifs".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Globe filaire + data-rain (CSS-only, keyframes gardés reduced-motion)</name>
  <files>apps/web/src/components/hero/WireframeGlobe.tsx, apps/web/src/components/hero/DataRain.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 3 globe SVG/CSS rotate, Pattern 4 data-rain)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (section hero, Reduced-motion double-garde)
    - apps/web/src/styles/globals.css (où ajouter keyframes — fin fichier après nexa-scroll de 11-04 ; tokens --nexa-ink, --primary, --accent-brand)
  </read_first>
  <action>
    WireframeGlobe.tsx : RSC, SVG inline filaire (méridiens/parallèles = ellipses + lignes), gradient via linearGradient consommant var(--primary)/var(--accent-brand) (globe décoratif, var() autorisé). aria-hidden true. Classe .nexa-globe animée par keyframes nexa-globe-spin (rotate 1turn) UNIQUEMENT sous @media (prefers-reduced-motion: no-preference) ; sinon globe figé mais rendu (D-05). ~8-12 lignes max, transform/opacity only, pas de box-shadow animé.
    DataRain.tsx : RSC, div.nexa-datarain position absolute inset-0 z-index 0 opacity ~0.06 pointer-events none aria-hidden, background repeating-linear-gradient(to bottom, var(--primary) 0 2px, transparent 2px 18px), background-size 100% 200%. Animé nexa-rain (background-position 0 200%) sous @media no-preference ; figé sinon.
    globals.css : ajouter keyframes nexa-globe-spin et nexa-rain + classes .nexa-globe / .nexa-datarain avec garde @media (prefers-reduced-motion: no-preference). Aucune classe utilitaire physique.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "nexa-globe-spin" apps/web/src/styles/globals.css && grep -c "aria-hidden" apps/web/src/components/hero/DataRain.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contient keyframes nexa-globe-spin ET nexa-rain, chacun sous @media (prefers-reduced-motion: no-preference)
    - WireframeGlobe.tsx et DataRain.tsx contiennent aria-hidden
    - Aucun import three/gsap/webgl/motion dans hero/* (grep = 0)
  </acceptance_criteria>
  <done>Globe filaire + data-rain CSS-only, animations gardées reduced-motion, décoratifs aria-hidden.</done>
</task>

<task type="auto">
  <name>Task 2 : Cartes flottantes anonymisées (zéro %) + tilt vanilla TS</name>
  <files>apps/web/src/components/hero/FloatingCards.tsx, apps/web/src/components/hero/HeroTilt.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <behavior>
    - FloatingCards rend des cartes setups anonymisés : instrument · direction · score /100 · niveau de risque (ex EUR/USD · Long · Score 82 · Risque modéré). ZÉRO %, aucun chiffre de gain (D-01).
    - HeroTilt n'attache le listener pointermove QUE si prefers-reduced-motion n'est PAS reduce ; sinon aucun mouvement (Pitfall 2). Le tilt écrit dans des CSS custom props.
    - no-perf-claims reste vert sur le namespace hero ajouté.
  </behavior>
  <read_first>
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-RESEARCH.md (Pattern 6 tilt vanilla — garde matchMedia avant listener)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Hero Contract cartes zéro %, §Copywriting)
    - apps/web/src/components/nexa/ScoreRing.tsx (score des cartes, optionnel)
    - apps/web/src/messages/fr.json (parité)
  </read_first>
  <action>
    FloatingCards.tsx : RSC, données via i18n hero.cards (liste { instrument, direction, score, risk } ÉDUCATIVES anonymisées, zéro %). Rendu en surfaces bg-card/border-border flottantes positionnées en logique (start/end). Le score peut réutiliser ScoreRing (couleur=risque). Aucun %, aucune promesse.
    HeroTilt.tsx : 'use client', île minimale. Dans useEffect : if window.matchMedia('(prefers-reduced-motion: reduce)').matches return AVANT addEventListener. Sinon attacher pointermove qui set --tilt-x/--tilt-y en deg (clamp ~6deg). Cleanup removeEventListener. Wrapper applique transform perspective(800px) rotateX(var(--tilt-x,0)) rotateY(var(--tilt-y,0)).
    messages : ajouter namespace hero { title, lede, ctaPrimary "Découvrir la méthode", ctaSecondary "Voir les tarifs", cards } aux 3 fichiers, parité STRICTE, ZÉRO %, sans slop, AR en miroir.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "prefers-reduced-motion" apps/web/src/components/hero/HeroTilt.tsx && test $(grep -ric "three\|gsap\|framer-motion" apps/web/src/components/hero) -eq 0 && pnpm vitest run apps/web/test/no-perf-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - HeroTilt.tsx teste matchMedia prefers-reduced-motion reduce AVANT addEventListener
    - grep three/gsap/framer-motion dans hero/ retourne 0
    - FloatingCards données via i18n hero.cards, zéro %
    - namespace hero à parité dans les 3 JSON ; ctaPrimary = "Découvrir la méthode"
    - pnpm vitest run no-perf-claims exit 0
  </acceptance_criteria>
  <done>Cartes anonymisées zéro %, tilt vanilla gardé reduced-motion, copy hero à parité.</done>
</task>

<task type="auto">
  <name>Task 3 : Composition Hero + câblage dans (marketing)/page.tsx</name>
  <files>apps/web/src/components/hero/Hero.tsx, apps/web/src/app/[locale]/(marketing)/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/page.tsx (hero statique l.33-44 à remplacer ; sections suivantes à conserver)
    - apps/web/src/components/hero/WireframeGlobe.tsx, DataRain.tsx, FloatingCards.tsx, HeroTilt.tsx (créés Task 1-2)
    - apps/web/src/components/nexa/Eyebrow.tsx
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Hero Contract — fond ink fixe D-04, baseline D-16)
  </read_first>
  <action>
    Hero.tsx : RSC shell composant Eyebrow (accroche), h1 title, p lede, CTA (Button asChild + Link localisé vers /tarifs et méthode), et les îlots décoratifs WireframeGlobe + DataRain (z-index bas) + FloatingCards enveloppées dans HeroTilt (z-index haut). Conteneur FOND INK FIXE via background var(--nexa-ink) ou classe dédiée — indépendant du thème (D-04), texte clair pour contraste. Propriétés logiques only. Baseline affichée (D-16).
    page.tsx : remplacer la section hero statique (l.33-44) par Hero (traductions via getTranslations hero). Conserver les sections suivantes (méthode, funnel académie) inchangées. Aucun % nu, aucune promesse.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "Hero" apps/web/src/app/[locale]/(marketing)/page.tsx && grep -c "nexa-ink" apps/web/src/components/hero/Hero.tsx && pnpm vitest run apps/web/src/styles/__tests__/rtl-logical-props.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - (marketing)/page.tsx rend Hero ; le hero statique l.33-44 (h1 heroTitle inline) a disparu
    - Hero.tsx applique un fond var(--nexa-ink) fixe (D-04)
    - pnpm vitest run rtl-logical-props exit 0 (hero/* scanné depuis 11-02, aucune classe physique)
  </acceptance_criteria>
  <done>Hero composé et câblé sur la home, fond ink fixe, RTL logique vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy hero i18n -> rendu | Les cartes/copy hero traversent l'i18n ; un % nu/promesse = exposition légale. |
| pointer events client -> CSS | Le tilt lit des events pointer, écrit des CSS vars ; aucun fetch, aucune donnée sensible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-PERF | (légal) | FloatingCards / copy hero | mitigate | Cartes anonymisées zéro % (D-01) ; no-perf-claims couvre namespace hero. |
| T-11-RM | (a11y) | hero animations | mitigate | Double-garde reduced-motion (CSS @media + JS matchMedia avant listener, Pitfall 2). |
| T-11-LIB | Tampering | hero deps | mitigate | CSS + vanilla TS only ; grep interdit three/gsap/framer-motion (D-05). |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- pnpm vitest run no-perf-claims vert (hero sans % nu).
- pnpm vitest run rtl-logical-props vert (hero/* scanné).
- grep three/gsap/framer-motion dans hero/ = 0.
- pnpm --filter web exec tsc -b --noEmit sans nouvelle erreur sur hero/*.
- Manuel (VALIDATION §Manual-Only) : rendu visuel + reduced-motion OS dans les 3 locales.
</verification>

<success_criteria>
- UI-02 : hero animé complet livré, reduced-motion respecté, fond ink fixe, zéro % / promesse.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-07-SUMMARY.md` when done.
</output>
