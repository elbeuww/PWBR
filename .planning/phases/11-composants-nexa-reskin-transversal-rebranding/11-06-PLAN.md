---
phase: 11-composants-nexa-reskin-transversal-rebranding
plan: 06
type: execute
wave: 4
depends_on: ["11-03", "11-04", "11-05"]
files_modified:
  - apps/web/src/app/[locale]/(member)/signaux/page.tsx
  - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx
  - apps/web/src/components/signals/SignalCard.tsx
  - apps/web/src/messages/fr.json
  - apps/web/src/messages/en.json
  - apps/web/src/messages/ar.json
autonomous: true
requirements: [UI-03, DESIGN-05]
must_haves:
  truths:
    - "La liste signaux et le détail trade sont au design NEXA et intègrent ScoreRing (couleur=risque)"
    - "Le gating RLS et le fetch serveur des surfaces membre sont inchangés (Anti-Pattern 3)"
    - "Les data-testid et rôles ARIA utilisés par les specs E2E gating sont préservés"
  artifacts:
    - path: "apps/web/src/components/signals/SignalCard.tsx"
      provides: "carte signal NEXA avec ScoreRing"
      contains: "ScoreRing"
  key_links:
    - from: "SignalCard.tsx"
      to: "components/nexa/ScoreRing.tsx"
      via: "import + rendu"
      pattern: "ScoreRing"
    - from: "(member)/signaux/page.tsx"
      to: "fetch RLS serveur existant"
      via: "inchangé"
      pattern: "createClient"
---

<objective>
Reskin de l'espace membre (UI-03) : liste signaux + détail trade au design NEXA, en intégrant les composants NEXA (ScoreRing pour le score=risque, Eyebrow pour les en-têtes de section, états vides tokenisés). Le CandleChart et la direction SignalCard ont déjà été recolorés en 11-03 ; ce plan compose la mise en page NEXA et branche ScoreRing.

Purpose : livrer UI-03 (design + score visuel) en préservant strictement la frontière RLS (Anti-Pattern 3, HARD) et les data-testid/ARIA des specs E2E gating. Dépend de 11-03 (chart/card recolorés), 11-04 (ScoreRing/Eyebrow) et 11-05 (i18n marque, file-ordering messages partagés).
Output : signaux/page.tsx + signaux/[id]/page.tsx reskinés, SignalCard intègre ScoreRing, états vides i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-03-SUMMARY.md
@.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-04-SUMMARY.md

<interfaces>
ScoreRing (11-04) : ScoreRing { score, risk: faible|modere|eleve, size?, label } — label déjà traduit fourni par l'appelant (RSC-safe).
Eyebrow (11-04) : Eyebrow tone purple|muted — en-tête de section.
SignalCard (11-03) : direction déjà tokenisée --signal-* ; bloc score actuel = text-primary neutre (l.62-74) à remplacer par ScoreRing.
Frontière RLS (HARD, Anti-Pattern 3) : (member)/layout.tsx fait le fetch RLS anon-client SERVEUR ; signaux/page.tsx + [id]/page.tsx lisent côté serveur (createClient). NE PAS migrer au client.
E2E gating spec : préserver tout data-testid/role référencé (apps/web/e2e/gating.spec.ts).
Mapping risque : déterminer faible/modere/eleve depuis le champ risque du setup (UI-SPEC §ScoreRing — colorblind-safe, label parlant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Intégrer ScoreRing dans SignalCard + en-têtes Eyebrow (liste)</name>
  <files>apps/web/src/components/signals/SignalCard.tsx, apps/web/src/app/[locale]/(member)/signaux/page.tsx, apps/web/src/messages/fr.json, apps/web/src/messages/en.json, apps/web/src/messages/ar.json</files>
  <read_first>
    - apps/web/src/components/signals/SignalCard.tsx (intégral — bloc score l.62-74 à remplacer, direction déjà tokenisée en 11-03, data-testid existants)
    - apps/web/src/app/[locale]/(member)/signaux/page.tsx (fetch RLS serveur, structure liste, état vide, data-testid)
    - apps/web/src/components/nexa/ScoreRing.tsx (API + risk levels)
    - apps/web/src/components/nexa/Eyebrow.tsx
    - apps/web/e2e/gating.spec.ts (sélecteurs à préserver)
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-UI-SPEC.md (§Copywriting — états vides signaux)
  </read_first>
  <action>
    SignalCard.tsx : remplacer le bloc score neutre (l.62-74, span text-2xl + barre bg-muted) par ScoreRing { score: opportunity_score, risk: mapping du champ risque, size: 40, label } (taille liste, hit-area 44px gérée par ScoreRing). Composer le label traduit via les clés scoreRing (11-04). Garder direction tokenisée, bdi/font-mono, propriétés logiques, et TOUS les data-testid existants. Conserver le moule shadcn card.
    signaux/page.tsx : appliquer la mise en page NEXA (Eyebrow en en-tête de section, espacement scale 8pt, surfaces bg-card/border-border). État vide via i18n signals "Aucun signal pour le moment" + corps (UI-SPEC copy, sans promesse de fréquence/gain). NE PAS toucher le fetch RLS serveur (createClient conservé). Préserver data-testid/role des specs gating.
    messages : ajouter/compléter les clés états vides signaux aux 3 fichiers à parité (si absentes), sans %.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "ScoreRing" apps/web/src/components/signals/SignalCard.tsx && grep -c "createClient" apps/web/src/app/[locale]/(member)/signaux/page.tsx && pnpm vitest run apps/web/test/no-perf-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SignalCard.tsx importe et rend ScoreRing avec risk + label ; l'ancien bloc score text-2xl/barre a disparu
    - signaux/page.tsx conserve createClient (fetch RLS serveur intact, aucune migration client)
    - Aucun data-testid existant supprimé (diff vérifié contre gating.spec)
    - pnpm vitest run no-perf-claims exit 0
  </acceptance_criteria>
  <done>Liste signaux NEXA, score affiché en ScoreRing couleur=risque, RLS et testids préservés.</done>
</task>

<task type="auto">
  <name>Task 2 : Reskin détail trade (ScoreRing large + chart recoloré + Eyebrow)</name>
  <files>apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx (intégral — fetch RLS serveur, rendu CandleChart, plan de trade, data-testid)
    - apps/web/src/components/nexa/ScoreRing.tsx (taille détail ~96px)
    - apps/web/src/components/signals/CandleChart.tsx (déjà recoloré en 11-03 — consommé tel quel)
    - apps/web/src/components/nexa/Eyebrow.tsx
    - .planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-PATTERNS.md (préservation frontière de confiance)
  </read_first>
  <action>
    Reskin [id]/page.tsx au design NEXA : ScoreRing size 96 pour le score du setup (couleur=risque, label traduit), Eyebrow sur les en-têtes (Plan de trade, Analyse), surfaces bg-card/border-border, chiffres entrée/SL/TP/R:R en font-mono + bdi (déjà pattern). Consommer le CandleChart recoloré tel quel. Conserver le <Disclaimer /> si présent sur la page, le fetch RLS serveur (createClient inchangé) et tous les data-testid/role. Propriétés logiques only. Aucun % nu ni promesse de gain.
  </action>
  <verify>
    <automated>cd "C:/Users/KOTEK Informatique/Desktop/Developpement/NEXA" && grep -c "ScoreRing" apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx && grep -c "createClient" apps/web/src/app/[locale]/(member)/signaux/[id]/page.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - [id]/page.tsx rend ScoreRing size 96 + Eyebrow ; consomme le CandleChart recoloré
    - createClient conservé (fetch RLS serveur intact)
    - <Disclaimer /> préservé si déjà présent ; data-testid/role inchangés
    - Aucune classe physique RTL ajoutée
  </acceptance_criteria>
  <done>Détail trade NEXA, ScoreRing large, chart recoloré, RLS + disclaimer + testids préservés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serveur (fetch RLS gated) -> rendu membre | Les setups sont gated par RLS has_active_subscription ; le reskin ne doit jamais migrer ce fetch au client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-RLS | Information Disclosure | signaux/page.tsx + [id]/page.tsx | mitigate | createClient serveur conservé (acceptance grep) ; aucune migration de fetch au client (Anti-Pattern 3). |
| T-11-E2E | (régression sécurité) | data-testid gating | mitigate | Préservation des data-testid/role ; suite gating E2E rejouée en 11-08. |
| T-11-PERF | (légal) | états vides/score | mitigate | no-perf-claims vert ; ScoreRing couleur=risque (pas de % nu). |
| T-11-SC | Tampering | npm installs | mitigate | Aucun package installé (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- Greps createClient présents (RLS intact) ; ScoreRing intégré.
- pnpm vitest run no-perf-claims vert ; tests de parité i18n verts.
- Suite E2E gating exécutée en 11-08 (gate de phase) — préservation testids vérifiée là.
- pnpm --filter web exec tsc -b --noEmit sans nouvelle erreur.
</verification>

<success_criteria>
- UI-03 : espace membre (liste + détail + chart) au design NEXA, ScoreRing couleur=risque.
- Frontière RLS et data-testid/ARIA E2E préservés.
</success_criteria>

<output>
Create `.planning/phases/11-composants-nexa-reskin-transversal-rebranding/11-06-SUMMARY.md` when done.
</output>
