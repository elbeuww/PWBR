---
phase: 17-fondation-db-scalable-perf-avant-charge
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - packages/supabase/database.types.ts
autonomous: false
requirements: [SCALE-04, SCALE-01, SCALE-02, SCALE-03, SCALE-05]
must_haves:
  truths:
    - "0017 Partie A est appliquee LIVE via apply_migration ; Partie B (CONCURRENTLY) via execute_sql UN PAR UN hors transaction"
    - "get_advisors(performance) ne signale 0 auth_rls_initplan ; get_advisors(security) sans NOUVELLE alerte"
    - "EXPLAIN des 3 listes keyset montre un Index Scan (pas Seq Scan + Sort)"
    - "REFRESH MATERIALIZED VIEW CONCURRENTLY mv_mrr reussit ; aucun index INVALID restant"
    - "database.types.ts reconcilie a la main (mv_mrr / get_mrr, override string *_atomic) ; typecheck vert"
    - "Tests RLS anon-client (signals-rls / gating-rls / mrr-gating) verts post-migration"
  artifacts:
    - path: "packages/supabase/database.types.ts"
      provides: "Types regeneres + reconcilies a la main pour mv_mrr / get_mrr"
      contains: "mv_mrr"
  key_links:
    - from: "apply_migration 0017 Partie A"
      to: "get_advisors(performance)"
      via: "gate D-01 : 0 auth_rls_initplan"
      pattern: "auth_rls_initplan"
    - from: "execute_sql CONCURRENTLY (Partie B)"
      to: "pg_index.indisvalid"
      via: "detection/recovery INVALID apres chaque statement"
      pattern: "indisvalid"
---

<objective>
**[BLOQUANT — autonomous:false]** Appliquer LIVE la migration `0017` via le MCP Supabase (JAMAIS `db push`), modele 0011/0012/0014/0016, puis verifier tous les gates de la phase (SCALE-04 + validation de SCALE-01/02/03/05).

Sequence D-05 : (1) `apply_migration` pour la **Partie A** transactionnelle ; (2) `execute_sql` per-statement pour la **Partie B** (`CREATE INDEX CONCURRENTLY`, hors transaction), avec detection/recovery INVALID apres CHAQUE statement ; (3) retrait differe de la publication / REPLICA IDENTITY apres verif A3 ; (4) `generate_typescript_types` -> reconciliation manuelle de `database.types.ts` ; (5) `get_advisors(performance)` + `get_advisors(security)` ; (6) `EXPLAIN` des 3 listes keyset + `REFRESH CONCURRENTLY` + requete indisvalid ; (7) re-run des tests RLS anon-client.

Purpose : faire passer la fondation DB du fichier de migration a l'etat LIVE verifie, sans faux-positif (le build/typecheck passent SANS l'apply — donc cet apply est obligatoire pour valider reellement la phase).
Output : 0017 LIVE, advisors verts, `database.types.ts` reconcilie, gates documentes (dont Manual-Only Broadcast en 17-HUMAN-UAT).

**Prerequis BLOQUANT :** le checkpoint A1 (definition mv_mrr, plan 17-01 Task 5) doit etre RESOLU avant d'appliquer ; sinon re-travail (re-ouvrir 0017).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-RESEARCH.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-PATTERNS.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-VALIDATION.md
@supabase/migrations/0017_scalable_foundation.sql

<interfaces>
Outils MCP Supabase (project_ref=csotpitrjxryjkadyiml, .mcp.json) :
  apply_migration(name, query)        — Partie A (DDL transactionnel)
  execute_sql(query)                  — Partie B (CONCURRENTLY, 1 statement/appel, hors tx) + EXPLAIN + indisvalid + verif publication
  generate_typescript_types()         — regen types apres migration
  get_advisors(type=performance|security)
  list_tables()                       — confirmer l'etat

Procedure types (en-tete 0016 L.7-11) : projet NON linke -> generate_typescript_types donne le brut ; reappliquer a la main les alias maison + override *_atomic -> string. mv_mrr ajoute month / active_subs / mrr_atomic (override string) + le type de retour de get_mrr().

Tests RLS anon-client a re-run post-migration :
  apps/web/tests/signals-rls.spec.ts (non-abonne -> 0 trade_setup)
  apps/web/tests/mrr-gating.test.ts (non-superadmin -> get_mrr() 0 ligne) [cree plan 17-02]
  toute spec gating-rls existante
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>0017 Partie A (transactionnel) prete a apply_migration ; Partie B (CONCURRENTLY) prete a execute_sql per-statement.</what-built>
  <how-to-verify>
    Confirmer AVANT toute ecriture LIVE :
    1. Le checkpoint A1 (definition mv_mrr, plan 17-01 Task 5) est resolu et le SQL de mv_mrr reflete la formule confirmee.
    2. Vous autorisez l'application LIVE de 0017 sur le projet Supabase csotpitrjxryjkadyiml via MCP (apply_migration + execute_sql), JAMAIS db push.
    3. Vous comprenez que les CREATE INDEX CONCURRENTLY s'executent un-par-un hors transaction (Pitfall 1) et qu'un echec laisse un index INVALID a nettoyer.
  </how-to-verify>
  <resume-signal>Repondre "go" pour lancer l'application LIVE, ou lister les ajustements requis sur 0017 avant.</resume-signal>
</task>

<task type="auto">
  <name>Task 1 : Appliquer Partie A (apply_migration) puis Partie B (execute_sql per-statement) + recovery INVALID</name>
  <read_first>
    - supabase/migrations/0017_scalable_foundation.sql (Partie A active + Partie B commentee + script de gate)
    - 17-RESEARCH.md section Execution CONCURRENTLY via MCP (L.297-308), Pitfall 1 (L.262-266), Pitfall 2 (L.268-282), Runtime State Inventory (L.246-258, retrait publication A3)
    - 17-PATTERNS.md Partie B (L.142-166)
  </read_first>
  <action>
    1. apply_migration(name="0017_scalable_foundation", query=Partie A transactionnelle UNIQUEMENT — sans les blocs commentes Partie B). Verifier le succes (pas d'erreur de NOM EXACT de policy ; si erreur -> corriger 0017 et reappliquer).
    2. Pour CHAQUE statement de la Partie B (dans l'ordre du fichier : mv_mrr_month_idx UNIQUE en premier, puis les 3 keyset, puis les 2 colonnes de policy) : un appel execute_sql isole (1 statement, JAMAIS groupe, JAMAIS via apply_migration — Pitfall 1 / erreur 25001). Avant payments_user_id_idx et subscriptions_user_id_idx, verifier via execute_sql (pg_indexes) si la colonne de policy a deja un index dedie — sauter le create si redondant (Assumption A5). APRES chaque create : execute_sql de la requete de detection INVALID (script de gate du fichier) ; si un index sort indisvalid=false -> execute_sql drop index concurrently if exists <name> puis relancer le create.
    3. Retrait publication / REPLICA IDENTITY (D-04, A3) : execute_sql sur pg_publication_tables (pubname=supabase_realtime, tablename=trade_setups) + grep client (postgres_changes + trade_setups) pour confirmer qu'aucun autre consommateur n'en depend. SI confirme seul le badge (deja migre Broadcast plan 17-02) -> execute_sql alter publication supabase_realtime drop table public.trade_setups; puis execute_sql alter table public.trade_setups replica identity default; SINON conserver et noter dans le SUMMARY.
  </action>
  <verify>
    <automated>echo "verification LIVE manuelle (MCP) — gate consolide en Task 3"</automated>
  </verify>
  <acceptance_criteria>
    - apply_migration(0017_scalable_foundation) reussit sans erreur de policy/nom.
    - Les 6 (ou moins, si redondants) CREATE INDEX CONCURRENTLY sont appliques chacun via un execute_sql isole.
    - La requete pg_index indisvalid=false renvoie 0 ligne en fin de Partie B (aucun index INVALID restant).
    - Le retrait publication/REPLICA IDENTITY n'est execute QUE si la verif A3 confirme l'absence d'autre consommateur ; sinon conserve et documente.
  </acceptance_criteria>
  <done>0017 Partie A + Partie B appliquees LIVE ; 0 index INVALID ; decision publication tracee.</done>
</task>

<task type="auto">
  <name>Task 2 : generate_typescript_types puis reconciliation manuelle de database.types.ts</name>
  <read_first>
    - packages/supabase/database.types.ts (etat actuel — alias maison + overrides *_atomic a preserver)
    - supabase/migrations/0016_affiliation.sql (en-tete L.7-11 : procedure generate_typescript_types -> edition main + override string *_atomic)
    - 17-RESEARCH.md Build artifacts (L.256)
  </read_first>
  <action>
    Appeler generate_typescript_types() (MCP). Reconcilier a la main packages/supabase/database.types.ts : integrer les nouveaux types de la matview public.mv_mrr (month, active_subs, mrr_atomic) et du type de retour de la fonction get_mrr() ; reappliquer les overrides maison des montants atomiques *_atomic -> string (en-tete 0016 L.10-11) ; preserver les alias maison existants. NE PAS ecraser betement le fichier avec le brut MCP (le projet non linke exige la reconciliation). Lancer pnpm typecheck (tsc -b --noEmit) et corriger toute NOUVELLE erreur introduite par les nouveaux types.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - database.types.ts contient les types de mv_mrr (month, active_subs, mrr_atomic) et le retour de get_mrr().
    - Les montants *_atomic restent typed string (override maison preserve).
    - Alias maison existants preserves (pas d'ecrasement brut).
    - pnpm typecheck vert (aucune NOUVELLE erreur vs baseline).
  </acceptance_criteria>
  <done>database.types.ts reconcilie post-migration ; typecheck vert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Gate D-05 consolide : advisors, EXPLAIN keyset, REFRESH CONCURRENTLY, indisvalid, tests RLS anon-client, Broadcast live.</what-built>
  <how-to-verify>
    Executer et confirmer chaque gate (17-VALIDATION.md Manual-Only) :
    1. get_advisors(type=performance) -> 0 auth_rls_initplan (critere d'arret D-01). Si une policy reste rouge -> revenir au plan 17-01 corriger le wrap puis reappliquer.
    2. get_advisors(type=security) -> aucune NOUVELLE alerte (les 2 WARN security-definer helpers has_active_subscription/is_superadmin sont EXPECTED, cf. STATE D-01-01-D).
    3. execute_sql EXPLAIN sur les 3 listes keyset (trade_setups / profiles / payments, order by created_at desc, id desc limit 20) -> Index Scan, PAS Seq Scan + Sort.
    4. execute_sql refresh materialized view concurrently public.mv_mrr; -> succes (prouve que mv_mrr_month_idx UNIQUE est present).
    5. execute_sql requete pg_index indisvalid=false -> 0 ligne.
    6. pnpm test (Vitest) avec env Supabase rempli : signals-rls + mrr-gating verts (non-abonne -> 0 trade_setup ; non-superadmin -> get_mrr() 0 ligne). Si env absent -> SKIP attendu, le noter.
    7. Broadcast live (Manual-Only) : session abonnee active recoit l'event badge ; session anon NE recoit RIEN. Documenter le shape reel du payload (payload.payload.record, A4) et le resultat dans 17-HUMAN-UAT.md.
  </how-to-verify>
  <resume-signal>Repondre "approuve" si tous les gates 1-7 passent (ou Broadcast documente en UAT), sinon decrire le gate en echec pour gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operateur MCP -> DB LIVE | l'application passe par apply_migration/execute_sql ; jamais db push ; jamais service_role cote pages |
| client anon -> RLS post-migration | la regression RLS est detectee par get_advisors(security) + tests anon-client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-RLS-LIVE | Elevation/Info Disclosure | drop/recreate policies appliquees LIVE | mitigate | get_advisors(security) sans nouvelle alerte + tests anon-client (signals-rls/gating-rls) verts post-migration ; rollback via correction 0017 si rouge |
| T-17-MV-LIVE | Info Disclosure | mv_mrr en prod | mitigate | mrr-gating test (non-superadmin -> 0 ligne) vert + get_mrr() revoke public/anon confirme |
| T-17-BC-LIVE | Info Disclosure | Broadcast realtime.messages en prod | mitigate | UAT manuel : session anon ne recoit aucun event ; policy realtime.messages active |
| T-17-IDX | Availability | index CONCURRENTLY echoue (INVALID) | mitigate | detection indisvalid=false apres chaque execute_sql + drop concurrently if exists + recreate ; 0 INVALID en fin |
| T-17-PUB | Availability | retrait publication casse un autre flux | mitigate | verif A3 (pg_publication_tables + grep client) AVANT drop ; conserver si doute |
| T-17-SC | Tampering | installs npm/pip | accept | aucun install (phase SQL/MCP) |
</threat_model>

<verification>
- Gate D-05 (Task 3) : get_advisors(performance)=0 auth_rls_initplan + get_advisors(security) sans nouvelle alerte + EXPLAIN index scan sans tri (3 listes) + REFRESH CONCURRENTLY OK + 0 index INVALID + tests RLS anon-client verts.
- pnpm typecheck vert apres reconciliation des types.
- Broadcast live = Manual-Only, documente en 17-HUMAN-UAT.md.
</verification>

<success_criteria>
- 0017 applique LIVE (Partie A apply_migration + Partie B execute_sql per-statement), 0 index INVALID.
- get_advisors(performance) 0 auth_rls_initplan ; get_advisors(security) sans nouvelle alerte.
- EXPLAIN 3 listes keyset = Index Scan sans Sort ; REFRESH CONCURRENTLY mv_mrr OK.
- database.types.ts reconcilie ; typecheck vert ; tests RLS anon-client verts.
- Decision retrait publication/REPLICA IDENTITY tracee ; Broadcast UAT documente.
</success_criteria>

<output>
Creer `.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-03-SUMMARY.md` quand termine.
Creer/mettre a jour `.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-HUMAN-UAT.md` (gate Broadcast Manual-Only).
</output>
