---
phase: 17-fondation-db-scalable-perf-avant-charge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0017_scalable_foundation.sql
autonomous: false
requirements: [SCALE-01, SCALE-02, SCALE-03, SCALE-05]
must_haves:
  truths:
    - "Toutes les policies RLS referencant has_active_subscription()/is_superadmin()/auth.uid() sont reecrites en wrap (select ...) dans 0017 Partie A"
    - "La matview mv_mrr existe avec un wrapper get_mrr() gated is_superadmin() (SECURITY DEFINER) et une fonction refresh_mv_mrr() lockee service_role"
    - "Le flux Realtime trade_setups est servi par un trigger Broadcast (realtime.broadcast_changes) + policy RLS sur realtime.messages repliquant has_active_subscription()"
    - "Les statements CONCURRENTLY (UNIQUE matview + keyset (created_at desc, id desc) + colonnes de policy) sont documentes en Partie B avec script de gate INVALID/EXPLAIN"
    - "La definition metier de mv_mrr est confirmee par le fondateur avant gel (A1)"
  artifacts:
    - path: "supabase/migrations/0017_scalable_foundation.sql"
      provides: "Partie A transactionnelle (wraps RLS, matview MRR + wrapper gated + refresh, trigger Broadcast + policy realtime.messages) + Partie B documentee (index CONCURRENTLY + script de gate)"
      contains: "create materialized view public.mv_mrr"
  key_links:
    - from: "public.get_mrr()"
      to: "public.is_superadmin()"
      via: "garde where (select public.is_superadmin())"
      pattern: "is_superadmin"
    - from: "realtime.messages policy"
      to: "public.has_active_subscription()"
      via: "using ((select public.has_active_subscription()))"
      pattern: "has_active_subscription"
    - from: "trigger trg_trade_setups_broadcast"
      to: "realtime.broadcast_changes"
      via: "broadcast_trade_setup_changes()"
      pattern: "broadcast_changes"
---

<objective>
Ecrire l'integralite de la migration `0017_scalable_foundation.sql` (authoring uniquement — application LIVE en plan `17-03`, BLOQUANT) :
- **Partie A transactionnelle** (destinee a `apply_migration`) : (1) reecriture de TOUTES les policies RLS existantes en wrap `(select ...)` (D-01/SCALE-01), (2) infra matview de reference MRR — `mv_mrr` + wrapper `get_mrr()` gated `is_superadmin()` + `refresh_mv_mrr()` lockee service_role (D-02/SCALE-03), (3) bascule du flux Realtime `trade_setups` vers Broadcast — trigger DB + policy RLS sur `realtime.messages` (D-04/SCALE-05, cote DB).
- **Partie B documentee en commentaire** (destinee a `execute_sql` per-statement, plan `17-03`) : index `CREATE INDEX CONCURRENTLY` — UNIQUE matview + keyset `(created_at desc, id desc)` + colonnes de policy (D-02/D-03/SCALE-01/SCALE-02), plus le script de gate reutilisable (detection index INVALID + EXPLAIN des listes keyset).

Purpose : poser la fondation perf RLS + l'infra matview + la fan-out Broadcast par conception, avant l'exposition a l'echelle (seed Phase 18, dashboards 19/20).
Output : `0017_scalable_foundation.sql` complet (Partie A active + Partie B commentee + script de gate commente).

Plan **non autonome** : la definition metier de `mv_mrr` (Assumption A1) n'est pas verrouillee dans CONTEXT.md -> checkpoint fondateur avant gel du SQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-RESEARCH.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-PATTERNS.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-CONTEXT.md

<interfaces>
Helpers SECURITY DEFINER existants — A REUTILISER, ne pas reecrire.
From supabase/migrations/0008_profiles_role.sql (forme canonique L.29-47) :
  public.is_superadmin() returns boolean — language sql stable security definer set search_path=public
  revoke execute from public, anon ; grant execute to authenticated
From supabase/migrations/0009_subscriptions_gating.sql + 0010 :
  public.has_active_subscription() returns boolean — security definer, reference subscriptions.current_period_end (PAS expires_at, cf. D-01-01-B)

NOMS EXACTS de policy a drop/recreate (un nom errone FAIT ECHOUER la migration). Source PATTERNS.md L.55-65 :
  trade_setups        : "trade_setups: abonnes actifs"            (0009 L.88)  -> wrap has_active_subscription()
  analyses            : "analyses: abonnes actifs"                (0009 L.94)  -> wrap has_active_subscription()
  candles             : "candles: abonnes actifs"                 (0011 L.63)  -> wrap has_active_subscription()
  payments            : "payments: inserer la sienne en pending"  (0012 L.92)  -> wrap auth.uid()
  payments            : "payments: lire les siennes"              (0012 L.97)  -> user_id = (select auth.uid())
  payments            : "payments: superadmin voit tout"          (0012 L.102) -> wrap is_superadmin()
  subscriptions       : "subscriptions: lire les siennes"         (0009 L.41)  -> wrap auth.uid()
  subscriptions       : "subscriptions: superadmin voit tout"     (0009 L.47)  -> wrap is_superadmin()
  profiles            : "profiles: lire le sien"                  (0001, ref 0008 L.11) -> wrap auth.uid()
  prediction_outcomes : "prediction_outcomes: lecture authentifies" (0014 L.39, using(true)) -> rien a wrapper, VERIFIER advisor
  affiliation (0016 L.68-227, 6 tables) : "affiliates: lire la sienne", "... superadmin voit tout", "affiliate_codes: lire les siens", "affiliate_applications: superadmin voit tout", "referrals: lire les siens", "commissions: lire les siennes", "payouts: lire les siens" + variantes superadmin -> wrap auth.uid() dans sous-requetes where user_id = auth.uid() ; wrap is_superadmin()

Index existants a NE PAS dupliquer (verifier au plan 17-03) :
  0006 L.86-87 : trade_setups_score_idx (opportunity_score desc, created_at desc) — SANS id tiebreaker
  0012 L.82-83 : payments_status_created_idx (status, created_at desc) — SANS id tiebreaker
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : En-tete 0017 + wrap RLS InitPlan de TOUTES les policies (Partie A section 1)</name>
  <read_first>
    - supabase/migrations/0016_affiliation.sql (en-tete le plus complet L.1-54 : bloc « via MCP apply_migration JAMAIS db push », section Decisions, section STRIDE ; + policies affiliation L.68-227 a wrapper)
    - supabase/migrations/0009_subscriptions_gating.sql (L.41-98 : NOMS EXACTS subscriptions/trade_setups/analyses ; pattern drop/recreate)
    - supabase/migrations/0012_payments.sql (L.91-105 : NOMS EXACTS payments, policy correlee colonne user_id = auth.uid())
    - supabase/migrations/0011_realtime_trade_setups.sql (L.60-66 : NOM EXACT candles + commentaire « NOM EXACT — un nom errone fait echouer la migration »)
    - supabase/migrations/0008_profiles_role.sql (L.11 : reference policy "profiles: lire le sien")
    - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql (L.39 : "prediction_outcomes: lecture authentifies" using(true))
    - 17-PATTERNS.md section 1 (L.25-67) + 17-RESEARCH.md Pattern 1 (L.133-151)
  </read_first>
  <action>
    Creer supabase/migrations/0017_scalable_foundation.sql. En-tete : calquer le bloc 0016 (L.1-11) — « appliquee via MCP apply_migration, JAMAIS db push ; projet non linke -> database.types.ts edite a la main apres generate_typescript_types, override string *_atomic » ; section « Decisions couvertes » D-01..D-05 ; section STRIDE (calque 0016 L.45-54). Annoter explicitement : numero = 0017 (0013 ABSENTE, NE PAS reutiliser) ; Partie A = transactionnel (apply_migration) ; Partie B = CONCURRENTLY documentee en commentaire mais executee via execute_sql (plan 17-03).
    Section 1 — Pour CHAQUE policy de l'inventaire des interfaces : drop policy "NOM EXACT" on table puis recreer a l'identique en wrappant UNIQUEMENT la fonction non correlee a la ligne dans un (select ...). Regle Pitfall 3 (17-RESEARCH L.284-286) : has_active_subscription() -> (select public.has_active_subscription()) ; is_superadmin() -> (select public.is_superadmin()) ; user_id = auth.uid() -> user_id = (select auth.uid()) (NE JAMAIS wrapper la comparaison de colonne). Pour prediction_outcomes (using(true)) : pas de fonction a wrapper — laisser tel quel, commenter que l'advisor sera verifie au gate (plan 17-03). Pour les policies affiliation avec sous-requete where user_id = auth.uid() : wrapper (select auth.uid()) a l'interieur de la sous-requete. Conserver a l'identique : to authenticated, for select/insert, le reste de l'expression using/with check.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0017_scalable_foundation.sql | grep -cE 'select public\.(has_active_subscription|is_superadmin)\(\)\)|select auth\.uid\(\)\)'</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier 0017_scalable_foundation.sql existe avec en-tete conforme (mention apply_migration / JAMAIS db push, D-01..D-05, STRIDE).
    - Chaque NOM EXACT de l'inventaire apparait dans un drop policy ET un create policy (aucun nom invente).
    - Toute occurrence de has_active_subscription() / is_superadmin() dans une policy recreee est dans un (select ...) ; toute comparaison user_id = reste hors (select ...) (la fonction auth.uid() seule est wrappee).
    - prediction_outcomes reste using (true) (rien wrappe), commente.
    - Aucun CREATE INDEX ni CREATE INDEX CONCURRENTLY ACTIF dans la zone Partie A.
  </acceptance_criteria>
  <done>Partie A section 1 ecrite ; toutes les policies de l'inventaire drop/recreate en wrap (select ...) ; en-tete conforme convention MCP.</done>
</task>

<task type="auto">
  <name>Task 2 : Matview MRR + wrapper get_mrr() gated + refresh_mv_mrr() (Partie A section 2)</name>
  <read_first>
    - supabase/migrations/0008_profiles_role.sql (L.29-47 : forme canonique SECURITY DEFINER set search_path=public + revoke/grant — calque EXACT pour get_mrr())
    - supabase/migrations/0014_prediction_outcomes_pattern_stats.sql (L.58-140 : forme d'une vue d'agregat, source de la forme MRR)
    - supabase/migrations/0016_affiliation.sql (L.236-362 : RPC SECURITY DEFINER + revoke execute ; en-tete L.10-11 override string *_atomic)
    - supabase/migrations/0012_payments.sql (colonnes payments : status, user_id, expected_amount_atomic / montant atomique bigint)
    - supabase/migrations/0009_subscriptions_gating.sql (colonnes subscriptions : status, current_period_end, user_id)
    - 17-PATTERNS.md section 2 (L.71-106) + 17-RESEARCH.md Pattern 3 (L.163-188, Assumption A1 L.326)
  </read_first>
  <action>
    Dans 0017, Partie A section 2 : create materialized view public.mv_mrr as <agregat> sur subscriptions (left join payments), squelette de 17-RESEARCH Pattern 3 — colonnes : month (date_trunc('month', s.current_period_end)), active_subs (count(*) filter (where s.status='active' and (s.current_period_end is null or s.current_period_end > now()))), mrr_atomic (coalesce(sum(p.expected_amount_atomic) filter (where p.status='verified'),0)::bigint) group by month. Montants *_atomic en bigint (override TS string au plan 17-03). NE PAS poser le UNIQUE index ici (il est en Partie B, Task 4 : mv_mrr_month_idx). Ajouter un commentaire : REFRESH CONCURRENTLY exige mv_mrr_month_idx (UNIQUE) pose en Partie B.
    Wrapper de lecture (les matviews N'ONT PAS de RLS — wrapper OBLIGATOIRE, jamais GRANT direct) : create function public.get_mrr() returns setof public.mv_mrr language sql stable security definer set search_path = public, corps select * from public.mv_mrr where (select public.is_superadmin()) ; puis revoke execute on function public.get_mrr() from public, anon ; grant execute on function public.get_mrr() to authenticated (miroir 0008 L.46-47).
    Fonction de refresh lockee service_role : create function public.refresh_mv_mrr() returns void language plpgsql security definer set search_path = public, corps begin refresh materialized view concurrently public.mv_mrr ; end ; puis revoke execute on function public.refresh_mv_mrr() from public, anon, authenticated (seul service_role bypass — miroir 0016 L.362). NE PAS ordonnancer le refresh (Open Question 1 — hors scope P17).
    AVANT de finaliser ce SQL, declencher le checkpoint Task 5 (validation A1 par le fondateur).
  </action>
  <verify>
    <automated>grep -qE 'create materialized view public\.mv_mrr' supabase/migrations/0017_scalable_foundation.sql; grep -qE 'where \(select public\.is_superadmin\(\)\)' supabase/migrations/0017_scalable_foundation.sql; grep -qE 'revoke execute on function public\.refresh_mv_mrr\(\) from public, anon, authenticated' supabase/migrations/0017_scalable_foundation.sql</automated>
  </verify>
  <acceptance_criteria>
    - create materialized view public.mv_mrr present, colonnes month / active_subs / mrr_atomic::bigint.
    - get_mrr() est SECURITY DEFINER, set search_path = public, garde where (select public.is_superadmin()), revoke public+anon, grant authenticated.
    - refresh_mv_mrr() est SECURITY DEFINER, set search_path = public, REFRESH MATERIALIZED VIEW CONCURRENTLY, revoke execute from public, anon, authenticated.
    - Aucun GRANT SELECT direct sur mv_mrr a authenticated/anon.
    - Aucun unique index dans cette zone (deplace en Partie B) ; commentaire le rappelle.
  </acceptance_criteria>
  <done>Partie A section 2 ecrite ; mv_mrr + get_mrr() gated + refresh_mv_mrr() lockee, conformes a la forme canonique 0008, sous reserve de la validation A1 (Task 5).</done>
</task>

<task type="auto">
  <name>Task 3 : Broadcast from Database — trigger + policy realtime.messages (Partie A section 3)</name>
  <read_first>
    - supabase/migrations/0011_realtime_trade_setups.sql (le flux a REMPLACER : REPLICA IDENTITY FULL L.37 + DO-block publication idempotent L.44-55)
    - supabase/migrations/0008_profiles_role.sql (L.29-47 : forme fonction SECURITY DEFINER search_path fige)
    - supabase/migrations/0009_subscriptions_gating.sql (L.79-80 : has_active_subscription() reutilise dans la policy realtime.messages)
    - 17-PATTERNS.md section 3 (L.110-138) + 17-RESEARCH.md Pattern 4 (L.190-227), Pitfall 4 (L.288-291), Assumption A3 (L.328)
  </read_first>
  <action>
    Dans 0017, Partie A section 3 : create or replace function public.broadcast_trade_setup_changes() returns trigger security definer language plpgsql set search_path = public ; corps : perform realtime.broadcast_changes('topic:new-signals', tg_op, tg_op, tg_table_name, tg_table_schema, new, old) ; return null. Topic FIXE partage 'topic:new-signals' (fan-out global du badge, Open Question 2 tranchee : topic fixe + canal prive). Trigger : create trigger trg_trade_setups_broadcast after insert or update on public.trade_setups for each row execute function public.broadcast_trade_setup_changes().
    Parite securite (REMPLACE le filtre RLS de postgres_changes retire) : create policy "broadcast: abonnes actifs ecoutent new-signals" on realtime.messages for select to authenticated using ((select public.has_active_subscription())) — wrap InitPlan coherent avec la section 1.
    Reevaluation REPLICA IDENTITY FULL + publication (D-04) : NE PAS executer le DROP dans la Partie A. Documenter en commentaire les statements alter publication supabase_realtime drop table public.trade_setups et alter table public.trade_setups replica identity default, a executer SEULEMENT au plan 17-03 APRES verification LIVE (execute_sql sur pg_publication_tables) qu'aucun autre consommateur postgres_changes ne depend de la publication (Assumption A3).
  </action>
  <verify>
    <automated>grep -qE "realtime\.broadcast_changes\('topic:new-signals'" supabase/migrations/0017_scalable_foundation.sql; grep -qE 'create trigger trg_trade_setups_broadcast' supabase/migrations/0017_scalable_foundation.sql; grep -qE 'on realtime\.messages' supabase/migrations/0017_scalable_foundation.sql</automated>
  </verify>
  <acceptance_criteria>
    - broadcast_trade_setup_changes() est SECURITY DEFINER, set search_path = public, appelle realtime.broadcast_changes avec topic fixe 'topic:new-signals'.
    - trigger trg_trade_setups_broadcast after insert or update on public.trade_setups for each row.
    - policy "broadcast: abonnes actifs ecoutent new-signals" on realtime.messages for select to authenticated using ((select public.has_active_subscription())).
    - Le DROP de la publication / REPLICA IDENTITY n'est PAS execute dans la Partie A — uniquement documente en commentaire pour le plan 17-03 (apres verif A3).
  </acceptance_criteria>
  <done>Partie A section 3 ecrite ; trigger Broadcast + policy realtime.messages poses ; retrait publication differe au plan 17-03 avec garde A3.</done>
</task>

<task type="auto">
  <name>Task 4 : Partie B (index CONCURRENTLY) + script de gate INVALID/EXPLAIN documentes en commentaire</name>
  <read_first>
    - supabase/migrations/0012_payments.sql (L.55-83 : 3 create index dont composite payments_status_created_idx (status, created_at desc) — analog des index, SANS id ni CONCURRENTLY)
    - supabase/migrations/0006_analyses_trade_setups.sql (L.86-87 : trade_setups_score_idx (opportunity_score desc, created_at desc) — composite existant sans id tiebreaker)
    - 17-PATTERNS.md Partie B (L.142-166) + 17-RESEARCH.md Pattern 2 (L.153-161), Pitfall 1 (L.262-266), Pitfall 2 (L.268-282), Open Question 3 (L.342-346)
  </read_first>
  <action>
    Ajouter a la FIN de 0017_scalable_foundation.sql un bloc commente intitule « Partie B — CONCURRENTLY hors transaction (NE PAS dans apply_migration — Pitfall 1, erreur 25001). Executer via execute_sql UN PAR UN au plan 17-03 ». Y consigner, en SQL commente (chaque statement sur sa ligne, prefixe par --), exactement les statements a executer :
    1. create unique index concurrently mv_mrr_month_idx on public.mv_mrr (month); -- requis pour REFRESH CONCURRENTLY (D-02) ; doit etre cree AVANT le premier refresh.
    2. create index concurrently trade_setups_keyset_idx on public.trade_setups (created_at desc, id desc); -- keyset feed plus recents (D-03/SCALE-02) ; trade_setups_score_idx 0006 n'a pas id en tiebreaker.
    3. create index concurrently profiles_keyset_idx on public.profiles (created_at desc, id desc); -- keyset users (D-03/SCALE-02).
    4. create index concurrently payments_keyset_idx on public.payments (created_at desc, id desc); -- keyset paiements (D-03/SCALE-02) ; payments_status_created_idx 0012 n'a pas id.
    5. create index concurrently payments_user_id_idx on public.payments (user_id); -- colonne de policy (D-01/SCALE-01, Assumption A5) si non couverte.
    6. create index concurrently subscriptions_user_id_idx on public.subscriptions (user_id); -- colonne de policy (D-01/SCALE-01) si non couverte.
    Ajouter une note : confirmer LIVE au plan 17-03, avant chaque create, si une colonne de policy a deja un index dedie (FK auto-index ? Assumption A5) — ne pas dupliquer ; l'advisor get_advisors(performance) rattrape les manquants. Le sens DESC de chaque index DOIT matcher l'ORDER BY reel des listes Phase 19/20 (Open Question 3) ; EXPLAIN au gate valide l'absence de Sort.
    Ajouter ENSUITE un bloc commente « Script de gate reutilisable (execute via execute_sql au plan 17-03) » contenant : (a) la requete de detection d'index INVALID — select c.relname from pg_index i join pg_class c on c.oid=i.indexrelid join pg_class t on t.oid=i.indrelid where not i.indisvalid and c.relnamespace='public'::regnamespace; et la remediation drop index concurrently if exists <name>; (b) un EXPLAIN gabarit par liste keyset — select ... from <table> order by created_at desc, id desc limit 20; attendu : Index Scan, PAS Seq Scan + Sort.
  </action>
  <verify>
    <automated>grep -q 'create unique index concurrently mv_mrr_month_idx' supabase/migrations/0017_scalable_foundation.sql</automated>
  </verify>
  <acceptance_criteria>
    - Bloc Partie B present en commentaire, listant les 6 statements CONCURRENTLY (mv_mrr_month_idx UNIQUE + 3 keyset (created_at desc, id desc) + 2 colonnes de policy), chacun annote du requirement (D-01/D-02/D-03).
    - Bloc « Script de gate » present : detection indisvalid=false + drop index concurrently if exists + EXPLAIN gabarit par liste.
    - Aucun de ces create index n'est ACTIF (tous prefixes par --) dans le corps transactionnel — ils sont documentes pour execution via execute_sql (plan 17-03).
    - Mention explicite « NE PAS dans apply_migration — Pitfall 1 / 25001 ».
  </acceptance_criteria>
  <done>Partie B + script de gate documentes en commentaire dans 0017 ; prets pour execution per-statement au plan 17-03.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Definition metier de la matview de reference mv_mrr (Assumption A1)</decision>
  <context>La formule du MRR n'est pas verrouillee dans CONTEXT.md. Le squelette (Task 2) agrege sum(payments.expected_amount_atomic) filter status='verified' par mois sur abonnes actifs. Avant de figer 0017 et de l'appliquer LIVE (plan 17-03), le fondateur doit confirmer la source de verite du montant mensuel : montant attendu (expected) vs montant reellement constate ; periode (current_period_end vs date de paiement) ; plans inclus (standard 9$ / decouverte 3$, PROJECT.md). Une formule erronee = re-travail (re-ouvrir 0017).</context>
  <options>
    <option id="expected-by-period-end">
      <name>MRR = sum(expected_amount_atomic verified) groupe par mois de current_period_end</name>
      <pros>Aligne sur l'engagement d'abonnement (recurrent) ; squelette deja ecrit ; coherent avec le gating has_active_subscription() base sur current_period_end.</pros>
      <cons>Compte le montant attendu, pas le cash reellement encaisse on-chain.</cons>
    </option>
    <option id="constated-by-payment-date">
      <name>MRR = sum(amount constate) groupe par mois de date de paiement verified</name>
      <pros>Reflete l'encaissement reel.</pros>
      <cons>Diverge du modele recurrent MRR ; exige une colonne montant constate distincte de expected ; potentiellement double-comptage si plusieurs paiements/mois.</cons>
    </option>
  </options>
  <resume-signal>Confirmer : "expected-by-period-end", "constated-by-payment-date", ou fournir la formule exacte (colonnes + periode + plans inclus). Apres reponse, ajuster le SQL de Task 2 puis valider 0017 complet.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client anon -> RLS Postgres | tout fetch front passe en client anon ; la RLS est la seule barriere non contournable |
| matview -> wrapper get_mrr() | les matviews n'ont PAS de RLS ; la garde est dans la fonction SECURITY DEFINER |
| trade_setups change -> realtime.messages | fan-out Broadcast ; l'ecoute est filtree par RLS sur realtime.messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-RLS | Elevation/Info Disclosure | drop/recreate policies (section 1) | mitigate | drop/recreate par NOM EXACT (un nom errone fait echouer la migration) ; wrap n'ouvre jamais la lecture ; re-run tests anon-client + get_advisors(security) au gate (plan 17-03) |
| T-17-MV | Info Disclosure | mv_mrr (pas de RLS) | mitigate | wrapper get_mrr() SECURITY DEFINER gated (select is_superadmin()) + revoke public/anon + grant authenticated ; jamais de GRANT SELECT direct |
| T-17-BC | Info Disclosure | realtime.messages (Broadcast) | mitigate | policy RLS sur realtime.messages repliquant (select has_active_subscription()) — parite avec le filtre postgres_changes retire ; un non-abonne ne recoit aucun event |
| T-17-SP | Tampering/Elevation | nouvelles fonctions SECURITY DEFINER | mitigate | set search_path = public fige sur get_mrr / refresh_mv_mrr / broadcast_trade_setup_changes (advisor function_search_path_mutable) |
| T-17-RF | Tampering | refresh_mv_mrr() appelable par un client | mitigate | revoke execute from public, anon, authenticated (service_role/job uniquement) |
| T-17-SC | Tampering | installs npm/pip | accept | aucun install dans cette phase (SQL natif) ; surface nulle |
</threat_model>

<verification>
- Aucun nouvel install npm (phase SQL native).
- pnpm test (Vitest) non regresse apres ecriture (le SQL n'affecte pas les tests unitaires existants).
- Le SQL n'est PAS applique dans ce plan (application LIVE = plan 17-03 apres checkpoint humain).
- Verification LIVE (get_advisors, EXPLAIN, REFRESH, indisvalid) = plan 17-03.
</verification>

<success_criteria>
- 0017_scalable_foundation.sql complet : Partie A active (policies wrappees, mv_mrr + get_mrr() + refresh_mv_mrr(), trigger Broadcast + policy realtime.messages) + Partie B commentee (6 index CONCURRENTLY + script de gate).
- En-tete conforme convention MCP (apply_migration, JAMAIS db push, D-01..D-05, STRIDE).
- Checkpoint A1 (definition MRR) resolu avant gel.
</success_criteria>

<output>
Creer `.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-01-SUMMARY.md` quand termine.
</output>
