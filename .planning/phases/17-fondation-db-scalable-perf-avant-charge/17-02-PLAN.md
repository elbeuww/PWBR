---
phase: 17-fondation-db-scalable-perf-avant-charge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/signals/SignalList.tsx
  - apps/web/tests/mrr-gating.test.ts
autonomous: true
requirements: [SCALE-05, SCALE-03]
must_haves:
  truths:
    - "SignalList s'abonne a un canal prive Broadcast 'topic:new-signals' (setAuth + config.private) au lieu de postgres_changes"
    - "Le mapping client lit payload.payload.record (forme Broadcast), pas payload.new"
    - "Un test anon-client mrr-gating verifie qu'un non-superadmin recoit 0 ligne via get_mrr()"
  artifacts:
    - path: "apps/web/src/components/signals/SignalList.tsx"
      provides: "Abonnement Broadcast canal prive ; etats newCount/removedIds/realtimeLost conserves"
      contains: "topic:new-signals"
    - path: "apps/web/tests/mrr-gating.test.ts"
      provides: "Wave-0 : gating get_mrr() via client anon -> 0 ligne"
      contains: "get_mrr"
  key_links:
    - from: "SignalList.tsx"
      to: "supabase.channel topic:new-signals"
      via: "setAuth + on('broadcast')"
      pattern: "broadcast"
    - from: "mrr-gating.test.ts"
      to: "rpc('get_mrr')"
      via: "client anon nu"
      pattern: "get_mrr"
---

<objective>
Cote client / test du flux Broadcast et du gating MRR : (1) reecrire l'abonnement Realtime de `SignalList.tsx` — remplacer `postgres_changes` par un **canal prive Broadcast** `topic:new-signals` (`setAuth` + `config.private`), en conservant exactement les etats et handlers existants (D-04/SCALE-05, cote client) ; (2) creer le test Wave-0 `mrr-gating.test.ts` — un client anon nu appelant `get_mrr()` recoit 0 ligne (D-02/SCALE-03 gating).

Ces deux fichiers sont disjoints du SQL (plan 17-01) -> meme vague, ownership exclusif, parallelisable. Le test ne passera GREEN qu'apres l'application LIVE de 0017 (plan 17-04) ; il est ecrit en Wave-0 avec le garde test.skip standard du projet.

Purpose : poser le consommateur Broadcast de reference + le filet de regression gating MRR.
Output : `SignalList.tsx` reecrit, `mrr-gating.test.ts` cree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-RESEARCH.md
@.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-PATTERNS.md

<interfaces>
Etat actuel SignalList.tsx (a CONSERVER lors de la reecriture) :
  - createClient() de @/lib/supabase/client (L.44) — porte la session pour Realtime Authorization
  - useState newCount (badge), removedIds (Set<string>), realtimeLost (repli refetch)
  - revealNew() : setNewCount(0) + refetch (L.121-124)
  - bloc subscribe (L.99-109) : etats CHANNEL_ERROR / TIMED_OUT / CLOSED -> setRealtimeLost(true) ; SUBSCRIBED -> setRealtimeLost(false) ; transitoires no-op
  - cleanup return () => supabase.removeChannel(channel) (L.111-113)
  - RealtimeBadge (count/onReveal) : INCHANGE

Bloc a REMPLACER (L.74-98) : .channel('signals-active').on('postgres_changes', INSERT filter status=eq.active, ...).on('postgres_changes', UPDATE sans filtre, ...)

Forme Broadcast cible (17-RESEARCH L.213-226, 17-PATTERNS L.174-188) :
  await supabase.realtime.setAuth()
  supabase.channel('topic:new-signals', { config: { private: true } })
    .on('broadcast', { event: 'INSERT' }, () => setNewCount(c => c+1))
    .on('broadcast', { event: 'UPDATE' }, (payload) => { const next = payload.payload?.record as {id?:string; status?:string}; if (next?.id && next.status && next.status !== 'active') setRemovedIds(prev => new Set(prev).add(next.id)) })
    .subscribe(MEME callback etats)

Analog test : apps/web/tests/signals-rls.spec.ts L.48-76 (header test.skip env, client anon nu createClient(URL, ANON_KEY), auth.signUp, assert error toBeNull + resultat vide)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Reecrire l'abonnement Realtime de SignalList en canal prive Broadcast</name>
  <read_first>
    - apps/web/src/components/signals/SignalList.tsx (integralite — surtout L.73-114 le bloc useEffect a reecrire ; conserver L.41-72 et L.116-151)
    - apps/web/src/components/signals/RealtimeBadge.tsx (INCHANGE — confirmer l'interface props count/onReveal)
    - apps/web/src/lib/supabase/client.ts (createClient = createBrowserSupabaseClient, porte la session cookie)
    - 17-PATTERNS.md section SignalList (L.170-188) + 17-RESEARCH.md Pattern 4 client (L.213-227), Pitfall 4 (L.288-291, A4 L.329)
  </read_first>
  <action>
    Dans le useEffect (actuellement L.73-114), remplacer UNIQUEMENT le bloc .channel('signals-active').on('postgres_changes', ...) par un abonnement canal prive Broadcast. Avant la creation du canal : await supabase.realtime.setAuth() (requis pour Realtime Authorization sur canal prive). Le useEffect devient async-safe : encapsuler la mise en place dans une fonction async appelee dans l'effet, garder la reference du channel pour le cleanup via une variable mutable + un flag annule-si-demonte (le removeChannel doit fonctionner meme si le channel est cree dans une promesse). Channel : supabase.channel('topic:new-signals', { config: { private: true } }). Handler INSERT : .on('broadcast', { event: 'INSERT' }, () => setNewCount((c) => c + 1)) (remplace l'ancien INSERT filter status=eq.active L.79-82). Handler UPDATE : .on('broadcast', { event: 'UPDATE' }, (payload) => { const next = payload.payload?.record as { id?: string; status?: string }; if (next?.id && next.status && next.status !== 'active') setRemovedIds((prev) => new Set(prev).add(next.id as string)) }) (remplace L.84-98 — ATTENTION shape : payload.payload.record, PAS payload.new, Pitfall 4/A4). Conserver a l'identique : le bloc .subscribe((status) => ...) (L.99-109, etats CHANNEL_ERROR/TIMED_OUT/CLOSED/SUBSCRIBED -> realtimeLost), le cleanup removeChannel, les etats newCount/removedIds/realtimeLost, revealNew, le rendu JSX et RealtimeBadge. Mettre a jour le commentaire d'en-tete du fichier (L.12-19) pour decrire le flux Broadcast (canal prive, RLS realtime.messages) au lieu de postgres_changes/0011. Aucun nouvel import autre que ce qui est deja present.
  </action>
  <verify>
    <automated>grep -q "channel('topic:new-signals'" apps/web/src/components/signals/SignalList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SignalList.tsx s'abonne a channel('topic:new-signals', { config: { private: true } }) apres supabase.realtime.setAuth().
    - Plus aucune occurrence de 'postgres_changes' dans le fichier (grep -c = 0 hors commentaires).
    - Le handler UPDATE lit payload.payload.record (jamais payload.new).
    - Etats newCount/removedIds/realtimeLost, revealNew, bloc subscribe, cleanup removeChannel et RealtimeBadge conserves a l'identique.
    - pnpm typecheck (tsc -b --noEmit) ne produit aucune NOUVELLE erreur dans SignalList.tsx.
  </acceptance_criteria>
  <done>SignalList consomme le canal prive Broadcast ; postgres_changes retire ; mapping payload.payload.record ; etats/handlers/cleanup preserves ; typecheck propre.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Wave-0 test mrr-gating.test.ts (anon-client get_mrr() -> 0 ligne)</name>
  <read_first>
    - apps/web/tests/signals-rls.spec.ts (L.1-76 — structure complete a mirrorer : header securite, test.skip env, client anon nu, auth.signUp, assert)
    - vitest.config.ts (racine — confirmer que apps/web/tests/**/*.test.ts est inclus dans le glob, cf. 17-RESEARCH L.369)
    - 17-PATTERNS.md section mrr-gating (L.192-196) + 17-VALIDATION.md Wave 0 (L.60)
  </read_first>
  <behavior>
    - Un client anon nu (NEXT_PUBLIC ANON_KEY, jamais service_role), authentifie via auth.signUp (donc non-superadmin), appelant supabase.rpc('get_mrr') recoit error === null ET data de longueur 0 (la garde where (select is_superadmin()) filtre silencieusement).
    - Si NEXT_PUBLIC_SUPABASE_URL / ANON_KEY absents -> le test SKIP (ne pas fabriquer de GREEN).
  </behavior>
  <action>
    Creer apps/web/tests/mrr-gating.test.ts (extension .test.ts pour matcher le glob Vitest racine, PAS .spec.ts qui est Playwright). Calquer la structure de signals-rls.spec.ts L.48-76 mais en Vitest (import describe/it/expect depuis vitest, pas @playwright/test) : garde de skip si NEXT_PUBLIC_SUPABASE_URL ou NEXT_PUBLIC_SUPABASE_ANON_KEY absent ; client anon nu createClient(URL, ANON_KEY) de @supabase/supabase-js (JAMAIS service_role — commentaire securite en tete) ; auth.signUp d'un email unique -> user authentifie sans privilege superadmin ; appel await supabase.rpc('get_mrr') ; assertions : expect(error).toBeNull() (la garde filtre SILENCIEUSEMENT, pas une erreur) ET expect(data ?? []).toHaveLength(0). En-tete documentant : depend de 0017 LIVE (plan 17-04) ; tant que non applique, le test SKIP si env absent — ne PAS fabriquer un GREEN.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/tests/mrr-gating.test.ts existe, importe depuis vitest (pas @playwright/test).
    - Le test appelle rpc('get_mrr') via un client anon nu (createClient avec ANON_KEY) ; aucun service_role.
    - Assertions : error toBeNull + data toHaveLength(0).
    - Garde de skip env presente (NEXT_PUBLIC_SUPABASE_URL / ANON_KEY).
    - pnpm test reste vert (le test SKIP si env absent, ne casse pas la suite existante).
  </acceptance_criteria>
  <done>mrr-gating.test.ts cree, miroir Vitest de signals-rls ; gating get_mrr() couvert ; suite non regressee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur -> canal prive Broadcast | l'ecoute du badge passe par un canal prive ; l'autorisation est filtree par RLS sur realtime.messages (cote DB, plan 17-01) |
| client anon -> get_mrr() | la garde is_superadmin() est cote fonction ; le test prouve qu'un non-superadmin lit 0 ligne |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-BC-CLI | Info Disclosure | SignalList abonnement Broadcast | mitigate | canal prive + setAuth (porte la session) ; le filtrage abonne est garanti par la policy realtime.messages (plan 17-01) ; client n'utilise jamais service_role |
| T-17-MV-TEST | Info Disclosure | mrr-gating test | mitigate | test anon-client nu (ANON_KEY) prouve get_mrr() -> 0 ligne pour non-superadmin ; jamais de service_role cote front (commentaire securite obligatoire, calque signals-rls L.11-17) |
| T-17-A4 | Tampering | mapping payload Broadcast | mitigate | lecture payload.payload.record (forme Broadcast) ; verif runtime du shape au 1er event (HUMAN-UAT, plan 17-04) |
| T-17-SC | Tampering | installs npm | accept | aucun install (@supabase/supabase-js et vitest deja presents) |
</threat_model>

<verification>
- pnpm test reste vert (mrr-gating SKIP si env absent).
- pnpm typecheck sans nouvelle erreur sur SignalList.tsx.
- Verification Broadcast live (badge abonne recoit / anon non) = Manual-Only, documentee en 17-HUMAN-UAT au plan 17-04.
</verification>

<success_criteria>
- SignalList consomme le canal prive Broadcast topic:new-signals ; postgres_changes retire ; mapping payload.payload.record.
- mrr-gating.test.ts cree (Vitest, anon-client, get_mrr -> 0 ligne).
- Suite Vitest + typecheck non regressees.
</success_criteria>

<output>
Creer `.planning/phases/17-fondation-db-scalable-perf-avant-charge/17-02-SUMMARY.md` quand termine.
</output>
