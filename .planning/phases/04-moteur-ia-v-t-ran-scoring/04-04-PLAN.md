---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - apps/jobs/config/sessions.ts
  - apps/jobs/src/jobs/sessionUniverse.ts
  - apps/jobs/__tests__/sessions-config.test.ts
  - apps/jobs/prompts/veteran.md
  - apps/jobs/src/jobs/persist.ts
  - apps/jobs/src/dispatch.ts
autonomous: false
requirements: [JOB-01, JOB-02, SCORE-01]
user_setup:
  - service: claude-code-scheduled-agents
    why: "Routines planifiées aux ouvertures de session (l'ANALYZE exige l'agent Claude Code Max — pas d'API en P1)"
    dashboard_config:
      - task: "Enregistrer/mettre à jour les routines planifiées par session (crons UTC §5)"
        location: "Claude Code app — scheduled agents (config hors git)"
must_haves:
  truths:
    - "L'univers d'une session = SESSIONS[session].asset_classes ∩ instruments WHERE active, styles depuis la config ; crypto incluse dans chaque session (D-49)"
    - "Le prompt vétéran versionné (veteran.md) instruit l'agent de produire UN fichier JSON par instrument×style conforme à §3, sans calculer score/R:R, sans fence markdown"
    - "prompt_version = semver front-matter + sha256(veteran.md) ; persist enregistré dans JOB_REGISTRY ; un run réel produit ≥1 setup persisté ; traçabilité de version enregistrée sur analyses (D-51)"
  artifacts:
    - path: "apps/jobs/config/sessions.ts"
      provides: "mapping session → asset_classes + styles (config versionnée D-49)"
      exports: ["SESSIONS"]
    - path: "apps/jobs/prompts/veteran.md"
      provides: "runbook vétéran versionné (D-47)"
      contains: "version:"
    - path: "apps/jobs/src/jobs/sessionUniverse.ts"
      provides: "résolution univers = config ∩ instruments actifs"
      exports: ["resolveSessionUniverse"]
  key_links:
    - from: "apps/jobs/src/jobs/sessionUniverse.ts"
      to: "@app/supabase listActiveInstruments"
      via: "intersection asset_class"
      pattern: "listActiveInstruments"
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "apps/jobs/prompts/veteran.md"
      via: "sha256 prompt_version"
      pattern: "createHash\\('sha256'\\)"
    - from: "apps/jobs/src/dispatch.ts"
      to: "persist"
      via: "JOB_REGISTRY"
      pattern: "persist,"
---

<objective>
Câbler le scheduling et le contrat-agent du run de session (JOB-01/02, SCORE-01) : la config versionnée `apps/jobs/config/sessions.ts` (D-49) + le résolveur d'univers (config ∩ instruments actifs), le prompt vétéran versionné `apps/jobs/prompts/veteran.md` (D-47, schéma-en-prompt + 1 exemple §3, discipline fichier JSON), le calcul `prompt_version` (semver + sha256, D-51) branché dans `persist.ts`, l'enregistrement de `persist` dans `JOB_REGISTRY`, et le checkpoint human-action pour configurer les routines planifiées + valider un run réel.

Purpose : ferme la boucle de bout en bout — l'agent sait quoi produire, le run sait quels instruments couvrir par session, et persist trace la version exacte du prompt pour l'itération/calibration future.
Output : config + prompt + dispatch câblés, golden test univers, routines enregistrées et 1 run réel produisant ≥1 setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@ARCHITECTURE.md

<interfaces>
<!-- Config analog — packages/core/src/time/sessions.ts:17-22 (objet as const + keyof typeof) -->
<!-- listActiveInstruments(client) existe déjà (@app/supabase), importé dans technical-engine.ts:38 -->
<!-- dispatch.ts:33-42 JOB_REGISTRY — ajouter 1 import + 1 entrée `persist` -->
<!-- hash.ts:10 createHash('sha256') — patron prompt_version (node:crypto, T-03-07) -->

<!-- Crons UTC §5 (ARCHITECTURE lignes 196-203) — référence pour la config agent (hors git) :
session-asia 00 23 * * 0-4 ; session-london 00 07 * * 1-5 ; session-newyork 30 12 * * 1-5 ; eod-swing 00 21 * * 1-5 -->

<!-- Contrat JSON §3 (exemple XAU_USD) — ARCHITECTURE lignes 124-162 : à intégrer DANS veteran.md comme exemple complet -->
<!-- A1 : veteran.md peut montrer score/risk dans l'exemple pour lisibilité MAIS le code les recalcule/écrase ; OutputSchema ne les exige pas -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config sessions + résolveur d'univers (config ∩ actifs)</name>
  <files>apps/jobs/config/sessions.ts, apps/jobs/src/jobs/sessionUniverse.ts, apps/jobs/__tests__/sessions-config.test.ts</files>
  <read_first>
    - packages/core/src/time/sessions.ts:17-24 (patron `as const` + keyof typeof)
    - apps/jobs/src/jobs/technical-engine.ts:38 (import listActiveInstruments) + 04-PATTERNS.md lignes 285-297
    - D-49 (04-CONTEXT.md) + 04-RESEARCH.md Pattern 4 (lignes 239-250)
  </read_first>
  <behavior>
    - SESSIONS figé selon D-49 : asia {forex,metal,crypto}/[day], london {forex,metal,energy,crypto}/[day,swing], newyork {forex,metal,energy,crypto}/[day], eod-swing {…}/[swing]
    - Test : `resolveSessionUniverse('london', activeInstruments)` retourne les paires (instrument×style) dont asset_class ∈ asset_classes(london), pour chaque style de london
    - Test : crypto présente dans CHAQUE session (asia/london/newyork/eod-swing)
    - Test : un instrument inactif (active=false) est exclu
    - Test : un asset_class hors session (ex. energy pour asia) est exclu
  </behavior>
  <action>
    Créer `apps/jobs/config/sessions.ts` : `SESSIONS = { asia:{asset_classes:['forex','metal','crypto'],styles:['day']}, london:{asset_classes:['forex','metal','energy','crypto'],styles:['day','swing']}, newyork:{asset_classes:['forex','metal','energy','crypto'],styles:['day']}, 'eod-swing':{asset_classes:['forex','metal','energy','crypto'],styles:['swing']} } as const` + `type SessionName = keyof typeof SESSIONS`. Créer `sessionUniverse.ts` : `resolveSessionUniverse(session: SessionName, instruments: InstrumentRow[]): Array<{ instrument: InstrumentRow; style: 'day'|'swing' }>` = produit cartésien des instruments filtrés par `asset_class ∈ SESSIONS[session].asset_classes` (et `active`) × `SESSIONS[session].styles`. Pur (instruments injectés) pour testabilité offline (patron D-23). Test golden avec un set d'instruments figé couvrant les 4 asset_classes + 1 inactif.
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/sessions-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `SESSIONS` exporté `as const`, `resolveSessionUniverse` pur
    - crypto dans chaque session (test), inactifs exclus (test), asset_class hors session exclu (test)
    - `pnpm vitest run apps/jobs/__tests__/sessions-config.test.ts` vert
  </acceptance_criteria>
  <done>Univers session→instruments versionné et golden-testé (JOB-02, D-49).</done>
</task>

<task type="auto">
  <name>Task 2: Prompt vétéran versionné + prompt_version (sha256) branché dans persist</name>
  <files>apps/jobs/prompts/veteran.md, apps/jobs/src/jobs/persist.ts</files>
  <read_first>
    - ARCHITECTURE.md §3 lignes 76-162 (philosophie confluence + contrat JSON exemple XAU_USD)
    - 04-RESEARCH.md "Veteran prompt — techniques de robustesse" (lignes 345-355) + 04-PATTERNS.md lignes 316-318
    - packages/indicators/src/snapshots/hash.ts:10 (createHash('sha256') — patron, T-03-07) + D-51
  </read_first>
  <action>
    Écrire `apps/jobs/prompts/veteran.md` (markdown, pas de code) avec front-matter `version: 1.0.0`. Contenu (RESEARCH §robustesse) : rôle "trader 50 ans, philosophie de confluence §3" ; le schéma EN-prompt comme bloc JSON (champs §3 — PAS en prose) + UN exemple complet XAU_USD (dérivé d'ARCHITECTURE §3) ; discipline de sortie "Pour CHAQUE instrument×style, écris UN fichier `<instrument>_<style>.json` contenant UNIQUEMENT l'objet JSON — pas de prose, pas de fence markdown, pas de commentaire" ; boucle batch "traite les instruments un par un, un échec n'en bloque pas un autre" ; bornes "propose 1 à 3 TP avec alloc_pct ; NE calcule PAS le score ni le R:R (le code s'en charge)". Dans `persist.ts`, calculer `prompt_version` = `${frontMatterVersion}+${sha256(veteran.md)}` via `node:crypto` `createHash('sha256')` (lire le fichier au démarrage du run, jamais de hash maison) ; passer cette valeur à `insertAnalysis` (remplace l'injection de test de 04-03). Exposer une fonction `computePromptVersion(promptPath): string` testable.
  </action>
  <verify>
    <automated>grep -q "version:" apps/jobs/prompts/veteran.md && grep -q "createHash('sha256')" apps/jobs/src/jobs/persist.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `veteran.md` a un front-matter `version:` + contient le bloc JSON schéma + exemple XAU_USD + interdiction de fence/calcul score
    - `persist.ts` calcule `prompt_version` via `node:crypto` sha256 (D-51), pas de hash maison
    - `pnpm --filter @app/jobs exec tsc --noEmit` vert
  </acceptance_criteria>
  <done>Prompt versionné (D-47) + prompt_version traçable (D-51, SCORE-01).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Enregistrer persist dans dispatch + configurer routines + run réel</name>
  <what-built>
    `persist` est ajouté à `JOB_REGISTRY` (dispatch.ts). Cette étape configure les routines planifiées Claude Code (crons UTC §5 — config hors git, exige l'agent Max) et valide un run réel produisant ≥1 setup persisté.
  </what-built>
  <read_first>
    - apps/jobs/src/dispatch.ts:33-42 (JOB_REGISTRY — ajouter import + 1 entrée)
    - ARCHITECTURE.md §5 lignes 182-205 (anatomie INGEST→PREP→ANALYZE→PERSIST + crons UTC par session)
    - 04-RESEARCH.md Runtime State Inventory (lignes 273-283) + Manual-Only Verifications (04-VALIDATION.md lignes 64-70)
  </read_first>
  <how-to-verify>
    1. Ajouter `import { persist } from './jobs/persist'` + l'entrée `persist,` au `JOB_REGISTRY` de `dispatch.ts`. Lancer `pnpm --filter @app/jobs exec tsc --noEmit` (vert) et `pnpm vitest run` (suite complète verte).
    2. Configurer les routines planifiées dans l'app Claude Code (scheduled agents, hors git) selon les crons UTC §5 : session-asia `00 23 * * 0-4`, session-london `00 07 * * 1-5`, session-newyork `30 12 * * 1-5`, eod-swing `00 21 * * 1-5`. Chaque routine : INGEST + PREP (jobs P2/P3 existants) → ANALYZE (l'agent lit les snapshots, raisonne avec veteran.md, écrit un fichier JSON par instrument×style) → `tsx src/dispatch.ts persist`.
    3. Lancer manuellement UN run de session démo : produire au moins un fichier JSON agent valide (snapshot existant en base), puis `tsx src/dispatch.ts persist`.
    4. Vérifier en base : ≥1 ligne `trade_setups` status='active' liée à une `analyses` avec snapshot/model/prompt_version/run_id renseignés.
    5. Inspecter `job_runs.stats` du run : `{ written, rejected, reasons[] }` présents ; noter le taux de rejet (métrique d'itération du prompt, non bloquante en P1).
  </how-to-verify>
  <acceptance_criteria>
    - `persist` présent dans `JOB_REGISTRY` ; `pnpm vitest run` + `tsc --noEmit` verts
    - routines planifiées enregistrées (crons §5) dans l'app Claude Code
    - ≥1 trade_setup actif persisté lié à une analyse traçable (snapshot + prompt_version + run_id)
    - job_runs.stats du run contient written/rejected/reasons
  </acceptance_criteria>
  <resume-signal>Tapez "approved" une fois dispatch câblé, routines configurées et ≥1 setup réel persisté (ou décrivez le blocage).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled agent → fichiers JSON | l'agent produit des artefacts non fiables consommés par persist.ts (frontière en 04-03) |
| veteran.md → prompt_version | version du prompt tracée par hash infalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Repudiation / Tampering | prompt_version spoofing | mitigate | prompt_version = sha256(veteran.md) via node:crypto (infalsifiable) + semver lisible ; traçable par run (D-51) |
| T-04-11 | Tampering | injection via JSON agent | mitigate | toute la validation est en aval dans persist.ts (04-03) : OutputSchema + garde-fous ; le prompt interdit fence/prose mais le code ne fait JAMAIS confiance au prompt (strip + parse défensifs) |
| T-04-12 | Elevation of Privilege | routine fait insert direct | mitigate | l'ANALYZE écrit des fichiers uniquement ; le seul job d'écriture est `persist` (service_role, D-43) — aucune tâche OS/MCP d'insert |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/__tests__/sessions-config.test.ts` vert.
- `pnpm vitest run` (suite complète) vert + `pnpm --filter @app/jobs exec tsc --noEmit` vert.
- Run réel : ≥1 trade_setup actif + job_runs.stats renseigné (checkpoint).
</verification>

<success_criteria>
Univers session versionné + prompt vétéran versionné + prompt_version traçable + persist enregistré ; routines planifiées configurées et un run réel produit ≥1 setup persisté immuable (JOB-01/02, SCORE-01).
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-04-SUMMARY.md` à la fin.
</output>
