---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/core/src/scoring/weights.ts
  - packages/core/src/scoring/rr.ts
  - packages/core/src/scoring/risk.ts
  - packages/core/src/scoring/confidence.ts
  - packages/core/src/scoring/score.ts
  - packages/core/src/scoring/index.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/scoring/rr.test.ts
  - packages/core/__tests__/scoring/risk.test.ts
  - packages/core/__tests__/scoring/confidence.test.ts
  - packages/core/__tests__/scoring/score.test.ts
  - packages/core/__tests__/scoring/__fixtures__/setups.ts
autonomous: true
requirements: [SCORE-02, SCORE-03]
must_haves:
  truths:
    - "Le R:R par TP et global est recalculé par le code sur le bord conservateur de la zone d'entrée (long: zone.max, short: zone.min), identique pour les deux directions golden ; l'agent propose 1–3 TP + alloc_pct, le code borne (somme alloc=100) (D-50)"
    - "L'opportunity_score /100 est dérivé du barème §3 day/swing (pondération décomposable via breakdown) ; HTF contredit + pas de catalyseur → score plafonné à 45 ; logique de scoring pure et golden-testée dans packages/core (D-42, D-52)"
    - "risk_level ∈ {low,medium,high,extreme} et confidence ∈ {low,moderate,high} sont dérivés par règle code reproductible (golden) (D-46, D-48)"
  artifacts:
    - path: "packages/core/src/scoring/score.ts"
      provides: "scoreSetup(snapshot, output, style, now) → {opportunity_score, breakdown, risk_level, confidence}"
      exports: ["scoreSetup"]
    - path: "packages/core/src/scoring/rr.ts"
      provides: "recompute R:R bord conservateur (pur, réutilisable web)"
      exports: ["computeRiskReward"]
    - path: "packages/core/src/scoring/weights.ts"
      provides: "barème §3 day/swing (constantes nommées as const)"
      exports: ["WEIGHTS", "PENALTIES"]
  key_links:
    - from: "packages/core/src/scoring/score.ts"
      to: "packages/core/src/scoring/weights.ts"
      via: "import WEIGHTS/PENALTIES"
      pattern: "WEIGHTS\\[style\\]"
    - from: "packages/core/src/scoring/index.ts"
      to: "packages/core/src/index.ts"
      via: "re-export barrel @app/core"
      pattern: "scoreSetup"
---

<objective>
Construire le cœur déterministe de scoring dans `packages/core/scoring` (pur, sans IO, sans Date.now) : `weights.ts` (barème §3 day/swing), `rr.ts` (recalcul R:R bord conservateur D-50), `risk.ts` (risk_level D-46), `confidence.ts` (D-48), `score.ts` (opportunity_score /100 décomposable D-42 + règle dure cap 45). Verrouillé par golden tests (fixtures `snapshot + output → {score, risk_level, confidence, rr}`), même patron que les golden values indicateurs P3.

Purpose : ces fonctions sont LE remplacement déterministe du jugement chiffré de l'agent (anti-hallucination, reproductibilité, calibration §6). `persist.ts` (04-03) les appellera.
Output : `scoreSetup` + helpers purs exportés du barrel `@app/core`, golden suite verte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@ARCHITECTURE.md

<interfaces>
<!-- Entrée : snapshot §3 (TechnicalSnapshot/FundamentalContext/NewsContext) — types depuis @app/indicators -->
TechnicalSnapshot: trend_htf/trend_ltf enum(bullish|bearish|range), momentum{rsi,macd_hist,slope}, volatility{atr,atr_percentile}, key_levels[{price,type,strength}], structure{last_swing_high,last_swing_low,bos_choch}, volume_state.
FundamentalContext: macro_bias, rate_environment, asset_specific_drivers[].
NewsContext: net_sentiment(-1..1), news_risk:boolean.

<!-- Output agent : type Output depuis @app/core (créé en 04-01) — direction, entry{price,zone:[min,max]}, stop_loss, take_profits[{price,alloc_pct}] -->

<!-- Barème ARCHITECTURE §3 (lignes 103-118) — source de vérité -->
day:   trendAlign 25, keyLevel 20, momentum 15, fundamental 15, news 10, rr 15
swing: trendAlign 30, keyLevel 20, momentum 10, fundamental 20, news 10, rr 10
Pénalités: news high-impact imminente -15, volatilité extrême -10, structure cassée contre = rejet (persist).
Règles dures: R:R<1.2 rejet (persist) ; HTF contredit + pas de catalyseur → cap 45 ; R:R cible ≥1.5 day / ≥2 swing (qualité rr).

<!-- Patron déterminisme — packages/indicators/src/snapshots/hash.ts:13 HASH_DECIMALS=6 + technical-engine.ts:136 Math.round(x*1e6)/1e6 -->
<!-- Patron percentile — technical-engine.ts:75-81 (réutilisable risk.ts ATR distance) -->
<!-- Patron constantes as const — packages/core/src/time/sessions.ts:17-22 DAILY_ANCHOR -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: rr.ts — recompute R:R bord conservateur (RED→GREEN→REFACTOR)</name>
  <files>packages/core/src/scoring/rr.ts, packages/core/__tests__/scoring/rr.test.ts, packages/core/__tests__/scoring/__fixtures__/setups.ts</files>
  <read_first>
    - 04-RESEARCH.md Pitfall 1 (lignes 287-292) + D-50 (bord conservateur)
    - packages/indicators/src/snapshots/snapshots.test.ts (patron golden : valid fixture figée + bad = {...valid, champ})
    - packages/indicators/src/snapshots/hash.ts (HASH_DECIMALS=6, toFixed — arrondi déterministe)
    - 04-PATTERNS.md lignes 117-134 (anti-float, percentile)
  </read_first>
  <behavior>
    - RED : `computeRiskReward(output)` retourne `{ perTp: [{rr}], global }` calculé sur le bord conservateur : long → entrée = zone.max (pire prix), short → entrée = zone.min
    - Test long : zone [2316,2320], SL 2305, TP 2335 → rr = (2335-2320)/(2320-2305) arrondi 6 déc ; vérifier valeur figée
    - Test short : zone [min,max], SL au-dessus, TP en dessous → rr sur zone.min ; valeur figée
    - Test : R:R global = somme pondérée par alloc_pct des rr par TP (bord conservateur), jamais > si on swap min/max (Pitfall 1 warning sign)
    - Test : distance SL nulle (SL == entrée conservateur) → throw ou rr=0 documenté (cohérence)
  </behavior>
  <action>
    Implémenter `computeRiskReward(output: Output): { perTp: Array<{ price: number; alloc_pct: number; rr: number }>; global: number }` pur. Sélectionner l'entrée conservatrice selon `direction` (long ⇒ `entry.zone[1]`, short ⇒ `entry.zone[0]`, D-50). `rr_tp = |tp.price - entryCons| / |entryCons - stop_loss|`. `global` = somme(rr_tp × alloc_pct/100). Arrondir toutes les sorties à 6 décimales (`Math.round(x*1e6)/1e6`, patron hash.ts). Aucun `Date.now()`. Créer les fixtures partagées `setups.ts` (un output long + un short figés, dérivés de l'exemple §3 XAU_USD).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/scoring/rr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `computeRiskReward` exporté, pur (aucun import IO/Date)
    - golden long ET short verts avec valeurs figées
    - R:R calculé sur bord conservateur (test prouve que swap min/max ne baisse pas le rr)
  </acceptance_criteria>
  <done>R:R recalculé par code, bord conservateur, golden long/short.</done>
</task>

<task type="tdd">
  <name>Task 2: risk.ts + confidence.ts — dérivations code (RED→GREEN→REFACTOR)</name>
  <files>packages/core/src/scoring/risk.ts, packages/core/src/scoring/confidence.ts, packages/core/__tests__/scoring/risk.test.ts, packages/core/__tests__/scoring/confidence.test.ts</files>
  <read_first>
    - ARCHITECTURE.md §3 lignes 120-121 (facteurs risk_level) + D-46/D-48 (04-CONTEXT.md)
    - apps/jobs/src/jobs/technical-engine.ts:75-81 (patron atrPercentile réutilisable)
    - packages/indicators/src/snapshots/snapshots.test.ts (patron golden)
  </read_first>
  <behavior>
    - risk RED : `deriveRiskLevel({snapshot, output, rr})` → enum. Facteurs §3 : distance SL en ATR (trop serrée = high), atr_percentile élevé (vol extrême = relève), news_risk true (relève), contre-tendance HTF (relève)
    - Test golden risk : 4 fixtures → low / medium / high / extreme attendus (figés)
    - Test : SL distance < seuil ATR → au moins 'high'
    - confidence RED : `deriveConfidence({opportunity_score, alignedConfluences, news_risk})` → low|moderate|high (D-48 : mapping depuis score + nb confluences alignées + flag news_risk)
    - Test golden confidence : score haut + confluences alignées + pas de news_risk → high ; news_risk dégrade ; score bas → low
  </behavior>
  <action>
    `deriveRiskLevel` : règles nommées sur facteurs §3 (constantes de seuils ATR/percentile dans `weights.ts` ou en tête de fichier, data-not-magic). Réutiliser le patron percentile de technical-engine. `deriveConfidence` : mapping reproductible depuis `opportunity_score` + nombre de confluences alignées (tendance/niveau/momentum/fondamental/news dans le sens) + flag `news_risk` (dégrade d'un cran). Tout pur, sorties enum stables, aucun `Date.now()`. Fixtures golden figées pour les 4 niveaux de risque + 3 de confidence.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/scoring/risk.test.ts packages/core/__tests__/scoring/confidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deriveRiskLevel` couvre les 4 enums (golden), `deriveConfidence` les 3 (golden)
    - news_risk relève le risque ET dégrade la confidence (tests dédiés)
    - aucune dépendance IO/Date — fonctions pures
  </acceptance_criteria>
  <done>risk_level + confidence dérivés par code, golden-testés (SCORE-03, D-48).</done>
</task>

<task type="tdd">
  <name>Task 3: score.ts — opportunity_score /100 décomposable + cap 45 + barrel (RED→GREEN→REFACTOR)</name>
  <files>packages/core/src/scoring/weights.ts, packages/core/src/scoring/score.ts, packages/core/src/scoring/index.ts, packages/core/src/index.ts, packages/core/__tests__/scoring/score.test.ts</files>
  <read_first>
    - ARCHITECTURE.md §3 lignes 103-118 (barème + pénalités + règles dures) ; 04-RESEARCH.md Pattern 2 (lignes 221-233)
    - packages/core/src/time/sessions.ts:17-22 (patron `as const`) + packages/core/src/index.ts (barrel `.js`)
    - 04-PATTERNS.md lignes 136-152 (weights as const + barrel)
  </read_first>
  <behavior>
    - WEIGHTS day/swing figés selon §3 (somme blocs = 100 avant pénalités)
    - RED : `scoreSetup(snapshot, output, style, opts)` → `{ opportunity_score, breakdown, risk_level, confidence }` ; breakdown = points par bloc (trendAlign/keyLevel/momentum/fundamental/news/rr) → SCORE-02 décomposable
    - Test golden : fixture confluence forte day → score figé (≈78 type §3) + breakdown sommant au score
    - Test règle dure : HTF contredit la direction + aucun catalyseur fort → opportunity_score plafonné à 45 (cap)
    - Test pénalité : news high-impact imminente → -15 appliqué au score (breakdown le montre)
    - Test : score borné [0,100], arrondi entier ; reproductible (2 appels = même résultat)
    - Test : scoreSetup câble deriveRiskLevel + deriveConfidence (sortie cohérente avec Tasks 1-2)
  </behavior>
  <action>
    Créer `weights.ts` : `WEIGHTS = { day: {...}, swing: {...} } as const` (valeurs §3) + `PENALTIES = { newsHighImpact: -15, extremeVol: -10 } as const` + seuils R:R cible (≥1.5 day, ≥2 swing). `score.ts` : `scoreSetup(snapshot, output, style, opts: { now?: string })` pur — calcule chaque bloc (alignement tendance HTF/LTF, confluence niveau clé via proximité+strength des key_levels, momentum RSI/MACD dans le sens, fondamental macro/taux aligné, sentiment news, qualité R:R via `computeRiskReward`), applique pénalités, applique cap 45 (règle dure HTF contredit sans catalyseur), borne [0,100], arrondit entier. Retourne `breakdown` (objet bloc→points) + appelle `deriveRiskLevel`/`deriveConfidence`. `index.ts` re-exporte ; ajouter `export { scoreSetup, computeRiskReward } from './scoring/index.js'` au barrel core (`.js`). Aucun `Date.now()` — `now` injecté via opts.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/scoring</automated>
  </verify>
  <acceptance_criteria>
    - `scoreSetup` exporté du barrel `@app/core`, signature `(snapshot, output, style, opts) → {opportunity_score, breakdown, risk_level, confidence}`
    - golden : breakdown somme au score ; cap 45 vérifié ; pénalité news vérifiée
    - score entier dans [0,100], reproductible (golden stable)
    - suite `pnpm vitest run packages/core/__tests__/scoring` verte
  </acceptance_criteria>
  <done>opportunity_score décomposable + risk + confidence dérivés code, golden-testés (SCORE-02/03, D-42/46/48).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (interne) snapshot §3 → scoring | Snapshot déjà validé Zod en P3 ; scoring est pur, pas de frontière externe ici |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | non-déterminisme scoring | mitigate | fonctions pures, pas de Date.now/Math.random, arrondi 6 déc (golden stable, Pitfall 5) — sinon calibration §6 faussée |
| T-04-05 | Tampering | R:R surestimé entrée en zone | mitigate | bord conservateur D-50 golden-testé long ET short (Pitfall 1) |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package (zéro install) |
</threat_model>

<verification>
- `pnpm vitest run packages/core/__tests__/scoring` vert (rr + risk + confidence + score).
- `pnpm --filter @app/core exec tsc --noEmit` vert.
</verification>

<success_criteria>
scoreSetup + helpers purs golden-testés, déterministes, exportés du barrel ; barème §3 day/swing + cap 45 + R:R bord conservateur verrouillés.
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-02-SUMMARY.md` à la fin.
</output>
