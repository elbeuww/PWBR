---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/jobs/src/jobs/persist.ts
  - apps/jobs/__tests__/persist.test.ts
  - apps/jobs/__tests__/__fixtures__/run-artifacts.ts
autonomous: true
requirements: [SCORE-04, SCORE-05]
must_haves:
  truths:
    - "persist.ts lit les fichiers JSON par instrument×style, JSON.parse défensif (strip fence), OutputSchema.parse, résout le snapshot exact par raw_indicators_ref, applique les garde-fous, score, et upsert via service_role"
    - "Un setup avec R:R<1.2 ou SL/entry/TP incohérents par direction est rejeté + raison normalisée loggée dans job_runs.stats (jamais de valeur de clé)"
    - "À l'insert, les setups antérieurs de la même clé (instrument,style,session,jour) sont marqués expired ; l'analyse stocke le snapshot exact et n'est jamais mutée"
  artifacts:
    - path: "apps/jobs/src/jobs/persist.ts"
      provides: "frontière de confiance unique (Zod + garde-fous + immuabilité + upsert service_role)"
      exports: ["persist"]
      min_lines: 60
  key_links:
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "@app/core scoreSetup"
      via: "appel scoring déterministe"
      pattern: "scoreSetup\\("
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "@app/supabase expirePriorSetups"
      via: "immuabilité D-45"
      pattern: "expirePriorSetups\\("
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "OutputSchema.parse"
      via: "Zod à la frontière avant tout upsert"
      pattern: "OutputSchema\\.parse"
---

<objective>
Construire la **frontière de confiance unique** `apps/jobs/src/jobs/persist.ts` (D-43, SCORE-04/05) — le slice end-to-end qui prouve : un fichier JSON agent valide → JSON.parse défensif + `OutputSchema.parse` + résolution snapshot exact + garde-fous déterministes (recalcul R:R bord conservateur, cohérence SL/entry/TP par direction, règles dures, borne TP) + scoring (`scoreSetup`) + immuabilité (expiry clé D-45) + upsert service_role. JSON non conforme → rejet + raison normalisée loggée dans `job_runs.stats` (T-02-13), pas de retry en P1.

Purpose : c'est LE point unique où la sortie qualitative non fiable de l'agent devient une donnée vérifiée et immuable. Aucun autre chemin d'écriture n'existe (l'agent n'insère jamais — D-43).
Output : `persist` enregistré, garde-fous + expiry + immuabilité golden-testés avec client Supabase mocké.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@ARCHITECTURE.md

<interfaces>
<!-- Patron job frontière — extrait apps/jobs/src/jobs/technical-engine.ts (analog exact) -->
export async function technicalEngine(): Promise<Json> { const stats = {inserted, skipped, errors:[]}; const client = getServiceClient(); for (item) { try { const validated = Schema.parse(x); await upsert(client,row); stats.inserted++ } catch (err) { stats.errors.push({msg: err instanceof Error ? err.message : String(err)}) } } if (stats.inserted===0 && stats.errors.length>0) throw; return stats as Json }
<!-- getServiceClient lazy local (technical-engine.ts:162-175) : url/key depuis process.env, throw si absent, createClient<Database> auth persistSession:false -->

<!-- Consommé depuis @app/core (04-01/04-02) : OutputSchema, type Output, scoreSetup, computeRiskReward -->
<!-- Consommé depuis @app/supabase (04-01) : getSnapshotByHash, insertAnalysis, insertTradeSetups, expirePriorSetups, types AnalysisInsert/TradeSetupInsert/SnapshotRow/Json/Database -->

<!-- runJob (apps/jobs/src/runJob.ts) appelle persist via dispatch (câblé en 04-04) ; stats retournées → job_runs.stats -->
<!-- Raisons normalisées (T-02-13, Pitfall 4) : json_parse, zod_shape, snapshot_not_found, rr_below_min, sl_coherence, tp_bounds -->
<!-- run_id résolu (discrétion Open Question #1) : <session>_<YYYYMMDD>T<HHmm>Z ; artefacts lus depuis run-artifacts/<run_id>/<instrument>_<style>.json -->
<!-- model résolu (discrétion Open Question #3) : process.env['MODEL_LABEL'] ?? 'claude-code-max' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Garde-fous + fixtures + rejets normalisés (cœur frontière)</name>
  <files>apps/jobs/src/jobs/persist.ts, apps/jobs/__tests__/persist.test.ts, apps/jobs/__tests__/__fixtures__/run-artifacts.ts</files>
  <read_first>
    - apps/jobs/src/jobs/technical-engine.ts (analog exact : boucle isolée, Zod.parse avant upsert, stats normalisées, throw si 0 produit, getServiceClient lazy)
    - apps/jobs/src/runJob.ts (wrapper : persist retourne stats → job_runs)
    - 04-RESEARCH.md Pattern 1 (lignes 185-219) + Pitfalls 2/3/4/6 (lignes 293-313)
    - 04-PATTERNS.md lignes 35-107 (assignations persist.ts) + Shared Patterns (lignes 324-346)
  </read_first>
  <behavior>
    - Test : fichier JSON avec fence markdown ```json ... ``` → strip défensif → parse réussit
    - Test : JSON malformé → rejet, reason 'json_parse', stats.rejected++
    - Test : JSON dérivé de §3 (enum hors §3) → OutputSchema.parse throw → reason 'zod_shape'
    - Test : raw_indicators_ref introuvable (getSnapshotByHash mock → null) → reason 'snapshot_not_found'
    - Test garde-fou : R:R global recalculé < 1.2 → rejet, reason 'rr_below_min' (règle dure §3)
    - Test garde-fou : long avec SL > entrée conservatrice OU un TP du mauvais côté → rejet, reason 'sl_coherence'
    - Test garde-fou : take_profits somme alloc_pct ≠ 100 → borné/rejet, reason 'tp_bounds'
    - Test : stats.reasons ne contient JAMAIS de valeur de prix/clé, seulement les codes normalisés (T-02-13)
  </behavior>
  <action>
    Créer `persist(): Promise<Json>` calquée sur `technical-engine.ts`. `getServiceClient` lazy local (copier le patron, message d'erreur "persist: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set"). `stats = { written: 0, rejected: 0, reasons: [] as string[] }`. Boucle sur les artefacts lus (`readRunArtifacts(runId)` — fonction exportée séparément pour testabilité, patron D-23). Dans le `try` par fichier : (a) `stripFence` + `JSON.parse` ; (b) `OutputSchema.parse` ; (c) `getSnapshotByHash(client, output.raw_indicators_ref)` → si null `reject('snapshot_not_found')` ; (d) garde-fous via helpers purs : `computeRiskReward(output)` puis `if (rr.global < 1.2) reject('rr_below_min')`, cohérence SL/entry/TP par direction (long ⇒ `SL < entryCons < tous TP` ; short ⇒ inverse) sinon `reject('sl_coherence')`, borne TP (1–3 + somme alloc=100) sinon `reject('tp_bounds')`. Sortir la logique de garde-fou pure dans une fonction `runGuardrails(output)` testable hors-ligne. `reject(reason)` = push code normalisé dans `stats.reasons` (jamais de valeur) + `stats.rejected++` + `continue`. Mocker le client Supabase (vi.mock @supabase/supabase-js + @app/supabase) dans le test, patron vi.mock top-level hoisted (D-27). Throw si `written===0 && rejected>0` (WR-04/Pitfall 6).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `persist` exporté ; `runGuardrails` testable séparément
    - ≥6 cas de rejet golden (json_parse, zod_shape, snapshot_not_found, rr_below_min, sl_coherence, tp_bounds)
    - stats.reasons = codes normalisés uniquement (test asserte aucune valeur numérique de prix présente)
    - strip fence markdown fonctionne
  </acceptance_criteria>
  <done>Frontière Zod + garde-fous déterministes + rejets normalisés (SCORE-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scoring + immuabilité (expiry clé) + upsert (chemin succès)</name>
  <files>apps/jobs/src/jobs/persist.ts, apps/jobs/__tests__/persist.test.ts</files>
  <read_first>
    - 04-PATTERNS.md lignes 344-346 (immuabilité par expiry à l'insert) + D-45 (04-CONTEXT.md)
    - packages/supabase/src/repositories/snapshots.ts (patron repo) — analyses.ts/tradeSetups.ts créés en 04-01
    - 04-RESEARCH.md Pattern 3 (lignes 235-238) + lignes 144-152 (étapes e/f/g)
  </read_first>
  <behavior>
    - Test succès : output valide → scoreSetup appelé → expirePriorSetups(key) appelé AVANT insert → insertAnalysis → insertTradeSetups(status='active', valid_until) → stats.written++
    - Test ordre immuabilité : le mock vérifie que expirePriorSetups est invoqué avant insertTradeSetups (même clé instrument/style/session/jour)
    - Test valid_until : day → ≈ now + 24h ; swing → quelques jours (via luxon, now injecté)
    - Test SCORE-05 : la ligne analyses reçoit le snapshot exact (snapshot du getSnapshotByHash) + model + prompt_version + schema_version + run_id ; aucun champ d'analyse n'est jamais ré-écrit (pas d'UPDATE analyses dans le code)
    - Test : opportunity_score/risk_level/confidence du trade_setup proviennent de scoreSetup (pas du JSON agent)
  </behavior>
  <action>
    Compléter le chemin succès dans `persist`. Après garde-fous : `const scored = scoreSetup(snapshot.payload, output, output.style, { now })`. Construire la clé `key = { instrument_id, style: output.style, session: output.session }` (jour borné par luxon sur `generated_at`). Appeler `expirePriorSetups(client, key)` (D-45) AVANT l'insert. `insertAnalysis(client, { run_id, session, style, instrument_id, snapshot: snapshot.payload, model: process.env['MODEL_LABEL'] ?? 'claude-code-max', prompt_version, schema_version: output.schema_version })` → récupère `id`. `insertTradeSetups(client, [{ analysis_id: id, instrument_id, style, session, direction, opportunity_score: scored.opportunity_score, risk_level: scored.risk_level, confidence: scored.confidence, entry_price: entryCons, stop_loss, take_profits: <perTp avec rr>, risk_reward: rr.global, payload: output, status: 'active', valid_until }])`. `valid_until` via luxon (`DateTime.fromISO(generated_at).plus(...)` — day 24h, swing quelques jours, jamais `Date` maison, T-03-17). `prompt_version` passé en argument (résolu en 04-04 ; ici lire depuis env/constante injectable pour le test). Étendre les mocks pour asserter l'ordre des appels.
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - chemin succès : expirePriorSetups invoqué avant insertTradeSetups (assertion d'ordre sur mock)
    - opportunity_score/risk_level/confidence proviennent de scoreSetup, jamais du JSON agent
    - aucune UPDATE des champs d'analyse (grep : pas de `.update(` sur analyses)
    - valid_until day≈24h / swing>24h (test sur now injecté)
    - suite `pnpm vitest run apps/jobs/__tests__/persist.test.ts` verte
  </acceptance_criteria>
  <done>Scoring câblé + immuabilité par expiry + upsert immuable traçable (SCORE-05, D-45).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent JSON file → persist.ts | UNTRUSTED jusqu'à JSON.parse + OutputSchema.parse + garde-fous passés |
| persist.ts service_role → DB | seul chemin d'écriture analyses/trade_setups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering / Input Validation | JSON IA dérive de forme / valeurs hors borne | mitigate | `OutputSchema.parse` + garde-fous (R:R, cohérence SL/TP par direction, règles dures, borne TP) → rejet+log avant tout upsert (V5 ASVS, SCORE-04) |
| T-04-07 | Elevation of Privilege | agent contourne la frontière (insert direct) | mitigate | D-43 : l'agent écrit des fichiers ; SEUL persist.ts (service_role) insère ; aucun chemin MCP insert dans le code |
| T-04-08 | Information Disclosure | fuite de valeurs dans logs de rejet | mitigate | stats.reasons = codes normalisés uniquement, jamais le payload ni valeurs de clé (T-02-13, Pitfall 4) — testé |
| T-04-09 | Tampering | mutation prédiction historique | mitigate | expirePriorSetups ne touche que status ; aucune UPDATE des champs analyse (SCORE-05) — vérifié par grep |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/__tests__/persist.test.ts` vert (garde-fous + rejets + succès + immuabilité).
- `pnpm vitest run` (suite complète) vert.
- `pnpm --filter @app/jobs exec tsc --noEmit` vert.
</verification>

<success_criteria>
Frontière de confiance unique opérationnelle et testée : JSON agent → validé/scoré/persisté immuablement, ou rejeté+loggé normalisé. Aucun autre chemin d'écriture.
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-03-SUMMARY.md` à la fin.
</output>
