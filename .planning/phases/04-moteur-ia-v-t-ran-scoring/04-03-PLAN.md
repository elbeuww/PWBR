---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/jobs/src/jobs/persist.ts
  - apps/jobs/src/jobs/runArtifacts.ts
  - apps/jobs/__tests__/persist.test.ts
  - apps/jobs/__tests__/runArtifacts.test.ts
  - apps/jobs/__tests__/__fixtures__/run-artifacts.ts
autonomous: true
requirements: [SCORE-04, SCORE-05]
must_haves:
  truths:
    - "persist.ts lit les fichiers JSON par instrument×style via readRunArtifacts(runId) (run_id validé regex stricte + path.resolve + startsWith(BASE_DIR) — anti path traversal), JSON.parse défensif (strip fence), OutputSchema.parse, résout le snapshot exact par raw_indicators_ref, applique les garde-fous, score, et upsert via service_role ; frontière unique — l'agent n'insère jamais directement (D-43) ; liste d'artefacts vide = échec explicite (throw), jamais succès silencieux"
    - "Un setup avec R:R<1.2, SL/entry/TP incohérents par direction, structure cassée contre le trade (bos_choch contredit la direction → reject('structure_against')), ou alloc_pct≠100 (reject('tp_bounds')) est rejeté + raison normalisée loggée dans job_runs.stats (jamais de valeur de clé) ; tous les setups passant les règles dures (R:R≥1.2) sont écrits, aucun seuil de score à l'écriture (D-44)"
    - "À l'insert, les setups antérieurs de la même clé (instrument_id, style, session, session_day) sont marqués expired via expirePriorSetups (session_day dérivé déterministe de generated_at en UTC) ; l'analyse stocke le snapshot exact et n'est jamais mutée ; snapshot.partial=true → traité explicitement (scoré avec risk_level relevé, voir Task 2)"
  artifacts:
    - path: "apps/jobs/src/jobs/persist.ts"
      provides: "frontière de confiance unique (Zod + garde-fous + immuabilité + upsert service_role)"
      exports: ["persist"]
      min_lines: 60
    - path: "apps/jobs/src/jobs/runArtifacts.ts"
      provides: "readRunArtifacts(runId) + validation run_id anti path traversal + RUN_ARTIFACTS_DIR"
      exports: ["readRunArtifacts", "RUN_ID_RE", "RUN_ARTIFACTS_DIR"]
  key_links:
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "@app/core scoreSetup"
      via: "appel scoring déterministe"
      pattern: "scoreSetup\\("
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "@app/supabase expirePriorSetups"
      via: "immuabilité D-45 (clé avec session_day)"
      pattern: "expirePriorSetups\\("
    - from: "apps/jobs/src/jobs/persist.ts"
      to: "OutputSchema.parse"
      via: "Zod à la frontière avant tout upsert"
      pattern: "OutputSchema\\.parse"
    - from: "apps/jobs/src/jobs/runArtifacts.ts"
      to: "path.resolve startsWith"
      via: "anti path traversal run_id"
      pattern: "startsWith"
---

<objective>
Construire la **frontière de confiance unique** `apps/jobs/src/jobs/persist.ts` (D-43, SCORE-04/05) — le slice end-to-end qui prouve : un fichier JSON agent valide → lecture via `readRunArtifacts(runId)` (run_id sanitisé) → JSON.parse défensif + `OutputSchema.parse` + résolution snapshot exact + garde-fous déterministes (recalcul R:R bord conservateur, cohérence SL/entry/TP par direction, structure cassée contre = rejet, règles dures, borne TP) + scoring (`scoreSetup`) + immuabilité (expiry clé D-45 avec session_day) + upsert service_role. JSON non conforme → rejet + raison normalisée loggée dans `job_runs.stats` (T-02-13), pas de retry en P1.

Purpose : c'est LE point unique où la sortie qualitative non fiable de l'agent devient une donnée vérifiée et immuable. Aucun autre chemin d'écriture n'existe (l'agent n'insère jamais — D-43).
Output : `persist` + `readRunArtifacts` enregistrés, garde-fous + expiry + immuabilité golden-testés avec client Supabase mocké.

Concerns revue tranchés ici :
- **#2 (HIGH) readRunArtifacts + path traversal** : module dédié `runArtifacts.ts` — `RUN_ARTIFACTS_DIR = run-artifacts/` (créé par l'ANALYZE, 04-04), `RUN_ID_RE = /^[a-z]+-\d{8}T\d{4}Z$/`, validation `path.resolve` + assertion `startsWith(BASE_DIR)` ; liste vide = throw explicite.
- **#1 (HIGH) clé d'immuabilité avec session_day** : `expirePriorSetups({instrument_id, style, session, session_day})` ; `session_day = DateTime.fromISO(output.generated_at, {zone:'utc'}).toISODate()`.
- **#3 (HIGH) valid_until chiffré** : `SWING_VALID_HOURS = 72`, `DAY_VALID_HOURS = 24` (constantes nommées).
- **#4 (MEDIUM) snapshot.partial** : tranché (scorer avec risk_level relevé, Task 2).
- **structure cassée contre = rejet** (§3, concern quality [HIGH]) : `reject('structure_against')`.
- **alloc_pct≠100** : rejet `reject('tp_bounds')` (concern quality [MEDIUM]).
- **dualité entry_price** : colonne `entry_price` = bord conservateur ; `payload.entry.price` = prix médian agent (documenté).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-REVIEWS.md
@ARCHITECTURE.md

<interfaces>
<!-- Patron job frontière — extrait apps/jobs/src/jobs/technical-engine.ts (analog exact) -->
export async function technicalEngine(): Promise<Json> { const stats = {inserted, skipped, errors:[]}; const client = getServiceClient(); for (item) { try { const validated = Schema.parse(x); await upsert(client,row); stats.inserted++ } catch (err) { stats.errors.push({msg: err instanceof Error ? err.message : String(err)}) } } if (stats.inserted===0 && stats.errors.length>0) throw; return stats as Json }
<!-- getServiceClient lazy local (technical-engine.ts:162-175) : url/key depuis process.env, throw si absent, createClient<Database> auth persistSession:false -->

<!-- Consommé depuis @app/core (04-01/04-02) : OutputSchema, type Output, scoreSetup, computeRiskReward -->
<!-- Consommé depuis @app/supabase (04-01) : getSnapshotByHash, insertAnalysis, insertTradeSetups, expirePriorSetups (clé {instrument_id, style, session, session_day}), types AnalysisInsert/TradeSetupInsert/SnapshotRow/Json/Database -->

<!-- runJob (apps/jobs/src/runJob.ts) appelle persist via dispatch (câblé en 04-04) ; stats retournées → job_runs.stats -->
<!-- Raisons normalisées (T-02-13, Pitfall 4) : json_parse, zod_shape, snapshot_not_found, rr_below_min, sl_coherence, tp_bounds, structure_against, insufficient_data -->

<!-- readRunArtifacts (concern #2 HIGH) — apps/jobs/src/jobs/runArtifacts.ts :
RUN_ARTIFACTS_DIR = path.resolve(process.cwd(), 'run-artifacts')  (répertoire créé par l'ANALYZE en 04-04 ; chaque fichier = <instrument>_<style>.json)
RUN_ID_RE = /^[a-z]+-\d{8}T\d{4}Z$/  (ex. london-20260614T0700Z)
readRunArtifacts(runId): { instrument: string; style: string; raw: string }[]
  - if (!RUN_ID_RE.test(runId)) throw new Error('invalid_run_id')
  - dir = path.resolve(RUN_ARTIFACTS_DIR, runId) ; if (!dir.startsWith(RUN_ARTIFACTS_DIR + path.sep)) throw new Error('invalid_run_id')  (anti path traversal)
  - list *.json ; for each, re-valider basename (pas de séparateur) ; lire le contenu
  - if (files.length === 0) throw new Error('no_artifacts')  (concern security [LOW] : liste vide = échec explicite, jamais succès silencieux)
-->
<!-- run_id résolu (discrétion Open Question #1) : <session>-<YYYYMMDD>T<HHmm>Z ; passé à persist via process.env['RUN_ID'] (résolu/exporté par l'ANALYZE en 04-04) -->
<!-- model résolu (discrétion Open Question #3) : process.env['MODEL_LABEL'] ?? 'claude-code-max' -->
<!-- dualité entry_price (concern archi [MEDIUM]) : colonne entry_price = bord conservateur (entryCons, anti-surestimation R:R) ; payload.entry.price = prix médian proposé par l'agent (conservé tel quel dans payload jsonb). Documenter ce contrat dans un commentaire au-dessus de insertTradeSetups. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: readRunArtifacts (anti path traversal) + garde-fous + rejets normalisés (cœur frontière)</name>
  <files>apps/jobs/src/jobs/runArtifacts.ts, apps/jobs/src/jobs/persist.ts, apps/jobs/__tests__/runArtifacts.test.ts, apps/jobs/__tests__/persist.test.ts, apps/jobs/__tests__/__fixtures__/run-artifacts.ts</files>
  <read_first>
    - apps/jobs/src/jobs/technical-engine.ts (analog exact : boucle isolée, Zod.parse avant upsert, stats normalisées, throw si 0 produit, getServiceClient lazy)
    - apps/jobs/src/runJob.ts (wrapper : persist retourne stats → job_runs)
    - 04-RESEARCH.md Pattern 1 (lignes 185-219) + Pitfalls 2/3/4/6 (lignes 293-313)
    - 04-PATTERNS.md lignes 35-107 (assignations persist.ts) + Shared Patterns (lignes 324-346)
    - 04-REVIEWS.md (concern #2 HIGH readRunArtifacts + path traversal ; structure_against ; tp_bounds ; liste vide = échec)
  </read_first>
  <behavior>
    - readRunArtifacts RED : run_id valide 'london-20260614T0700Z' → lit les fichiers du répertoire (mock fs)
    - Test path traversal : runId '../../etc/passwd' OU 'london-20260614T0700Z/../..' → throw 'invalid_run_id' (RUN_ID_RE échoue OU startsWith échoue)
    - Test liste vide (concern security) : répertoire valide mais 0 fichier *.json → throw 'no_artifacts' (jamais succès silencieux)
    - Test : fichier JSON avec fence markdown ```json ... ``` → strip défensif → parse réussit
    - Test : JSON malformé → rejet, reason 'json_parse', stats.rejected++
    - Test : JSON dérivé de §3 (enum hors §3) → OutputSchema.parse throw → reason 'zod_shape'
    - Test : raw_indicators_ref introuvable (getSnapshotByHash mock → null) → reason 'snapshot_not_found'
    - Test garde-fou : R:R global recalculé < 1.2 → rejet, reason 'rr_below_min' (règle dure §3)
    - Test garde-fou : long avec SL > entrée conservatrice OU un TP du mauvais côté → rejet, reason 'sl_coherence'
    - Test garde-fou : take_profits somme alloc_pct ≠ 100 → rejet, reason 'tp_bounds' (concern : rejet, pas borné silencieux)
    - Test garde-fou STRUCTURE (concern quality [HIGH]) : snapshot.structure.bos_choch contredit output.direction (ex. bos_choch='bearish_bos' avec direction='long') → rejet, reason 'structure_against' (règle dure §3)
    - Test : stats.reasons ne contient JAMAIS de valeur de prix/clé, seulement les codes normalisés (T-02-13)
  </behavior>
  <action>
    Créer `apps/jobs/src/jobs/runArtifacts.ts` (concern #2) : `RUN_ARTIFACTS_DIR = path.resolve(process.cwd(), 'run-artifacts')`, `RUN_ID_RE = /^[a-z]+-\d{8}T\d{4}Z$/`, `readRunArtifacts(runId): { instrument: string; style: string; raw: string }[]`. Valider : `if (!RUN_ID_RE.test(runId)) throw new Error('invalid_run_id')` ; `const dir = path.resolve(RUN_ARTIFACTS_DIR, runId)` ; `if (!dir.startsWith(RUN_ARTIFACTS_DIR + path.sep)) throw new Error('invalid_run_id')` (anti path traversal) ; lister les `*.json`, re-valider chaque basename (pas de `path.sep`), lire le contenu ; `if (files.length === 0) throw new Error('no_artifacts')`. Parser `<instrument>_<style>.json` pour instrument/style.
    Créer `persist(): Promise<Json>` calquée sur `technical-engine.ts`. `getServiceClient` lazy local (copier le patron, message d'erreur "persist: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set"). `const runId = process.env['RUN_ID']` (throw si absent). `stats = { written: 0, rejected: 0, reasons: [] as string[] }`. Boucle sur `readRunArtifacts(runId)`. Dans le `try` par fichier : (a) `stripFence` + `JSON.parse` ; (b) `OutputSchema.parse` ; (c) `getSnapshotByHash(client, output.raw_indicators_ref)` → si null `reject('snapshot_not_found')` ; (d) garde-fous via `runGuardrails(output, snapshot)` pur testable hors-ligne : `computeRiskReward(output)` puis `if (rr.global < 1.2) reject('rr_below_min')`, cohérence SL/entry/TP par direction (long ⇒ `SL < entryCons < tous TP` ; short ⇒ inverse) sinon `reject('sl_coherence')`, **structure cassée contre** (si `snapshot.technical.structure.bos_choch` indique une cassure de direction opposée au `output.direction` → `reject('structure_against')`, règle dure §3), borne TP (1–3 + somme alloc=100) sinon `reject('tp_bounds')`. `reject(reason)` = push code normalisé dans `stats.reasons` (jamais de valeur) + `stats.rejected++` + `continue`. Mocker le client Supabase (vi.mock @supabase/supabase-js + @app/supabase) et `node:fs` dans le test, patron vi.mock top-level hoisted (D-27). Throw si `written===0 && rejected>0` (WR-04/Pitfall 6).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/runArtifacts.test.ts apps/jobs/__tests__/persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `readRunArtifacts` exporté ; run_id validé par `RUN_ID_RE` + `path.resolve`+`startsWith(RUN_ARTIFACTS_DIR + path.sep)` ; path traversal → throw 'invalid_run_id' (test) ; liste vide → throw 'no_artifacts' (test)
    - `persist` exporté ; `runGuardrails(output, snapshot)` testable séparément
    - ≥7 cas de rejet golden (json_parse, zod_shape, snapshot_not_found, rr_below_min, sl_coherence, tp_bounds, structure_against)
    - `reject('structure_against')` déclenché quand bos_choch contredit la direction (test dédié)
    - `tp_bounds` = REJET quand somme alloc_pct≠100 (test dédié, pas de borne silencieuse)
    - stats.reasons = codes normalisés uniquement (test asserte aucune valeur numérique de prix présente)
    - strip fence markdown fonctionne
  </acceptance_criteria>
  <done>readRunArtifacts sécurisé + frontière Zod + garde-fous déterministes (structure_against inclus) + rejets normalisés (SCORE-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scoring + snapshot.partial + immuabilité (expiry clé session_day) + valid_until chiffré + upsert (chemin succès)</name>
  <files>apps/jobs/src/jobs/persist.ts, apps/jobs/__tests__/persist.test.ts</files>
  <read_first>
    - 04-PATTERNS.md lignes 344-346 (immuabilité par expiry à l'insert) + D-45 (04-CONTEXT.md)
    - packages/supabase/src/repositories/snapshots.ts (patron repo) — analyses.ts/tradeSetups.ts créés en 04-01 (expirePriorSetups prend {instrument_id, style, session, session_day})
    - 04-RESEARCH.md Pattern 3 (lignes 235-238) + lignes 144-152 (étapes e/f/g)
    - 04-REVIEWS.md (concern #1 session_day, #3 SWING_VALID_HOURS=72, #4 snapshot.partial, dualité entry_price)
  </read_first>
  <behavior>
    - Test succès : output valide → scoreSetup appelé → expirePriorSetups(key avec session_day) appelé AVANT insert → insertAnalysis → insertTradeSetups(status='active', valid_until) → stats.written++
    - Test ordre immuabilité : le mock vérifie que expirePriorSetups est invoqué avant insertTradeSetups, avec key = {instrument_id, style, session, session_day}
    - Test session_day déterministe : generated_at='2026-06-14T07:05:00Z' → session_day='2026-06-14' (DateTime.fromISO zone utc, toISODate) ; un generated_at à 23:30Z reste le jour UTC (pas de glissement de fuseau)
    - Test valid_until chiffré (concern #3) : day → now + DAY_VALID_HOURS(24h) ; swing → now + SWING_VALID_HOURS(72h) (via luxon, now injecté) — valeurs exactes
    - Test snapshot.partial (concern #4) : snapshot.partial===true → le setup EST scoré et persisté MAIS risk_level est relevé d'au moins un cran (jamais 'low' si partial) ; un commentaire/flag documente le choix (scorer-avec-risque-relevé plutôt que rejeter)
    - Test SCORE-05 : la ligne analyses reçoit le snapshot exact (snapshot du getSnapshotByHash) + model + prompt_version + schema_version + run_id ; aucun champ d'analyse n'est jamais ré-écrit (pas d'UPDATE analyses dans le code)
    - Test dualité entry_price : trade_setup.entry_price === entryCons (bord conservateur) ; payload.entry.price === prix médian agent (inchangé)
    - Test : opportunity_score/risk_level/confidence du trade_setup proviennent de scoreSetup (pas du JSON agent)
  </behavior>
  <action>
    Compléter le chemin succès dans `persist`. Définir constantes `DAY_VALID_HOURS = 24` et `SWING_VALID_HOURS = 72` (concern #3, nommées en tête de fichier). Après garde-fous : `const scored = scoreSetup(snapshot.payload, output, output.style, { now })`. **snapshot.partial (concern #4)** : si `snapshot.payload.partial === true` (snapshot <200 bougies produit par P3), relever `scored.risk_level` d'au moins un cran (jamais 'low' ; mapping low→medium, medium→high, high→extreme) et documenter en commentaire « partial snapshot → risk relevé, pas de rejet (D-44 : on conserve tout pour calibration) ». Calculer `session_day = DateTime.fromISO(output.generated_at, { zone: 'utc' }).toISODate()` (concern #1, dérivation déterministe UTC). Construire `key = { instrument_id, style: output.style, session: output.session, session_day }`. Appeler `expirePriorSetups(client, key)` (D-45) AVANT l'insert. `insertAnalysis(client, { run_id: runId, session: output.session, style: output.style, instrument_id, snapshot: snapshot.payload, model: process.env['MODEL_LABEL'] ?? 'claude-code-max', prompt_version, schema_version: output.schema_version })` → récupère `id`. `insertTradeSetups(client, [{ analysis_id: id, instrument_id, style: output.style, session: output.session, session_day, direction, opportunity_score: scored.opportunity_score, risk_level: scored.risk_level, confidence: scored.confidence, entry_price: entryCons /* bord conservateur, dualité */, stop_loss, take_profits: <perTp avec rr>, risk_reward: rr.global, payload: output /* payload.entry.price = médian agent inchangé */, status: 'active', valid_until }])`. `valid_until` via luxon : `DateTime.fromISO(generated_at, {zone:'utc'}).plus({ hours: style==='swing' ? SWING_VALID_HOURS : DAY_VALID_HOURS })` (jamais `Date` maison, T-03-17). `prompt_version` passé en argument (résolu en 04-04 ; ici lire depuis env/constante injectable pour le test). Étendre les mocks pour asserter l'ordre des appels + la clé session_day.
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - chemin succès : expirePriorSetups invoqué avant insertTradeSetups, avec key incluant session_day (assertion d'ordre + de clé sur mock)
    - session_day = DateTime.fromISO(generated_at,{zone:'utc'}).toISODate() (test déterministe, pas de glissement fuseau)
    - valid_until : day = now+24h (DAY_VALID_HOURS), swing = now+72h (SWING_VALID_HOURS) — valeurs exactes testées
    - snapshot.partial===true → risk_level relevé (jamais 'low'), setup quand même persisté (test dédié, concern #4)
    - entry_price = bord conservateur ; payload.entry.price = médian agent (test dualité)
    - opportunity_score/risk_level/confidence proviennent de scoreSetup, jamais du JSON agent
    - aucune UPDATE des champs d'analyse (grep : pas de `.update(` sur analyses)
    - suite `pnpm vitest run apps/jobs/__tests__/persist.test.ts` verte
  </acceptance_criteria>
  <done>Scoring câblé + snapshot.partial tranché + immuabilité par expiry (clé session_day) + valid_until chiffré (24h/72h) + upsert immuable traçable (SCORE-05, D-45).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_id (env) → readRunArtifacts | UNTRUSTED : run_id sanitisé (regex + path.resolve + startsWith) avant tout accès fichier (anti path traversal) |
| agent JSON file → persist.ts | UNTRUSTED jusqu'à JSON.parse + OutputSchema.parse + garde-fous passés |
| persist.ts service_role → DB | seul chemin d'écriture analyses/trade_setups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering / Input Validation | JSON IA dérive de forme / valeurs hors borne / structure contre le trade | mitigate | `OutputSchema.parse` + garde-fous (R:R, cohérence SL/TP par direction, structure_against, règles dures, rejet alloc≠100) → rejet+log avant tout upsert (V5 ASVS, SCORE-04) |
| T-04-07 | Elevation of Privilege | agent contourne la frontière (insert direct) | mitigate | D-43 : l'agent écrit des fichiers ; SEUL persist.ts (service_role) insère ; aucun chemin MCP insert dans le code |
| T-04-08 | Information Disclosure | fuite de valeurs dans logs de rejet | mitigate | stats.reasons = codes normalisés uniquement, jamais le payload ni valeurs de clé (T-02-13, Pitfall 4) — testé |
| T-04-09 | Tampering | mutation prédiction historique | mitigate | expirePriorSetups ne touche que status (clé session_day) ; aucune UPDATE des champs analyse (SCORE-05) — vérifié par grep |
| T-04-15 | Tampering / Path Traversal | run_id non sanitisé → lecture fichiers arbitraires | mitigate | `RUN_ID_RE = /^[a-z]+-\d{8}T\d{4}Z$/` + `path.resolve` + `startsWith(RUN_ARTIFACTS_DIR + path.sep)` ; basename re-validé ; liste vide → throw (concern security [HIGH] #2) |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package (luxon/zod déjà présents) |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/__tests__/runArtifacts.test.ts apps/jobs/__tests__/persist.test.ts` vert (path traversal + garde-fous + structure_against + rejets + succès + immuabilité session_day + valid_until 24/72h + snapshot.partial).
- `pnpm vitest run` (suite complète) vert.
- `pnpm --filter @app/jobs exec tsc --noEmit` vert.
</verification>

<success_criteria>
Frontière de confiance unique opérationnelle et testée : run_id sanitisé → JSON agent → validé/scoré/persisté immuablement (clé session_day, valid_until 24/72h, partial géré), ou rejeté+loggé normalisé (structure_against inclus). Aucun autre chemin d'écriture.
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-03-SUMMARY.md` à la fin.
</output>
