---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0006_analyses_trade_setups.sql
  - packages/core/src/schemas/output.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/schemas/output-schema.test.ts
  - packages/supabase/src/repositories/analyses.ts
  - packages/supabase/src/repositories/tradeSetups.ts
  - packages/supabase/src/index.ts
  - packages/supabase/src/database.types.ts
autonomous: false
requirements: [SCORE-01, SCORE-05]
must_haves:
  truths:
    - "Les tables analyses et trade_setups existent dans la base live avec RLS select-only authenticated et aucune write policy (service_role bypass)"
    - "Un JSON conforme au contrat §3 (exemple XAU_USD) passe OutputSchema.parse ; un JSON dérivé (enum hors §3, take_profits vide ou >3) est rejeté"
    - "Les repos insertAnalysis / insertTradeSetups / expirePriorSetups existent et sont typés depuis database.types régénérés"
  artifacts:
    - path: "supabase/migrations/0006_analyses_trade_setups.sql"
      provides: "DDL analyses + trade_setups + RLS + index score + index expiry"
      contains: "create table public.analyses"
    - path: "packages/core/src/schemas/output.ts"
      provides: "OutputSchema Zod §3 (contrat JSON IA) + type Output"
      exports: ["OutputSchema", "Output"]
    - path: "packages/supabase/src/repositories/analyses.ts"
      provides: "insertAnalysis service_role retournant id"
      exports: ["insertAnalysis"]
    - path: "packages/supabase/src/repositories/tradeSetups.ts"
      provides: "insertTradeSetups + expirePriorSetups (immuabilité D-45)"
      exports: ["insertTradeSetups", "expirePriorSetups"]
  key_links:
    - from: "packages/supabase/src/repositories/analyses.ts"
      to: "public.analyses"
      via: "insert service_role"
      pattern: "from\\('analyses'\\)"
    - from: "packages/supabase/src/repositories/tradeSetups.ts"
      to: "public.trade_setups"
      via: "update status=expired sur clé active"
      pattern: "status.*expired"
---

<objective>
Poser la fondation persistante + le contrat de sortie de la Phase 4 : la migration `0006` crée `analyses` + `trade_setups` (DDL §4 + RLS calquée sur 0005), appliquée à la base live via MCP `apply_migration` (D-17) ; le `OutputSchema` Zod §3 (contrat JSON de l'agent) et les repos typés `analyses`/`trade_setups` (upsert service_role + expiry immuable D-45) sont créés.

Purpose : sans tables ni contrat, aucun setup ne peut être validé ni persisté. C'est le socle des slices 04-02 (scoring) et 04-03 (persist).
Output : migration appliquée, types régénérés, OutputSchema + repos exportés du barrel `@app/supabase`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@ARCHITECTURE.md

<interfaces>
<!-- Conventions Zod v4 à répliquer — extraites de packages/indicators/src/snapshots/schema.ts -->
KeyLevelSchema/TechnicalSnapshotSchema : z.object, z.enum, z.number().min(-1).max(1), type via z.infer.
ARCHITECTURE §3 contrat JSON sortie (source de vérité) : schema_version "1.0", generated_at ISO, session enum(asia|london|newyork|eod-swing), style enum(day|swing), instrument string, direction enum(long|short), timeframe_analysis string, entry {type:limit|market|stop, price:number, zone:[number,number]}, stop_loss number, take_profits[{price, alloc_pct}] min1 max3, technical_reasons string[], fundamental_reasons string[], news_catalysts[{headline,impact,direction,ts}], upcoming_risk_events[{event,ts,note}], invalidation string, veteran_note string, raw_indicators_ref string.

<!-- A1 RÉSOLU (discrétion CONTEXT) : opportunity_score/risk_level/confidence/risk_reward/atr_distance_sl NE figurent PAS dans OutputSchema — le code les produit/recalcule (D-42/46/48/50). -->

<!-- Patron repository — extrait de packages/supabase/src/repositories/snapshots.ts -->
type ServiceClient = SupabaseClient<Database>
upsertSnapshot(client, row): Promise<void> ; if (error) throw new Error(`x failed: ${error.message}`)
getSnapshotByHash(client, hash): select('*').eq('content_hash', hash).maybeSingle()

<!-- Barrel @app/supabase — zone Phase 3 (index.ts:58-61) où ajouter les exports Phase 4. D-07 : ne JAMAIS exporter de service-client. -->

<!-- Patron migration RLS — extrait de supabase/migrations/0005_snapshots.sql -->
enable row level security + policy select to authenticated using(true) + AUCUNE write policy (D-05/T-03-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutputSchema Zod §3 (contrat JSON IA) + fixtures Wave 0</name>
  <files>packages/core/src/schemas/output.ts, packages/core/src/index.ts, packages/core/__tests__/schemas/output-schema.test.ts</files>
  <read_first>
    - packages/indicators/src/snapshots/schema.ts (analog Zod v4 exact — conventions z.enum/z.object/z.infer)
    - ARCHITECTURE.md §3 lignes 123-163 (contrat JSON sortie = source de vérité champ par champ)
    - packages/core/src/index.ts (barrel — pattern export ./...js extension ESM obligatoire)
    - 04-RESEARCH.md lignes 317-343 (Code Example OutputSchema + note A1) + 04-PATTERNS.md lignes 181-199
  </read_first>
  <behavior>
    - Test : fixture exemple §3 complète (XAU_USD, dérivée d'ARCHITECTURE §3) → OutputSchema.parse ne throw pas
    - Test : session enum hors §3 ('tokyo') → throw
    - Test : take_profits vide (0) → throw ; take_profits avec 4 entrées → throw ; 1 à 3 → ok
    - Test : entry.zone non-tuple (1 seul nombre) → throw
    - Test : OutputSchema NE contient PAS opportunity_score/risk_level/confidence (A1) — parse réussit même si ces champs absents
  </behavior>
  <action>
    Créer `OutputSchema` (Zod v4) dans `packages/core/src/schemas/output.ts` encodant le contrat §3 champ par champ. Utiliser `z.iso.datetime()` pour `generated_at`/`ts` (Zod v4), `z.tuple([z.number(), z.number()])` pour `entry.zone`, `z.array(...).min(1).max(3)` pour `take_profits` (objets `{price:z.number(), alloc_pct:z.number()}` — `rr` recalculé code, PAS dans le schéma), `z.enum` pour session/style/direction/entry.type. Exclure opportunity_score/risk_level/confidence/risk_reward/atr_distance_sl du schéma (A1 — le code les produit, D-42/46/48). Exporter `type Output = z.infer<typeof OutputSchema>`. Ajouter `export { OutputSchema } from './schemas/output.js'` et `export type { Output } from './schemas/output.js'` au barrel `packages/core/src/index.ts` (extension `.js`). Créer la fixture XAU_USD figée + tests valides/invalides (patron `valid… / bad = { ...valid, champ: mauvais }` de snapshots.test.ts).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/schemas/output-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/schemas/output.ts` exporte `OutputSchema` + `type Output`
    - `OutputSchema` n'a pas de clé `opportunity_score`/`risk_level`/`confidence` (vérifiable : un parse d'un objet §3 sans ces clés réussit)
    - `pnpm vitest run packages/core/__tests__/schemas/output-schema.test.ts` passe (fixture valide + ≥4 cas de rejet)
    - barrel `@app/core` réexporte `OutputSchema` et `Output`
  </acceptance_criteria>
  <done>Contrat JSON §3 verrouillé par Zod + golden-testé, réutilisable web+jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0006 SQL (DDL analyses + trade_setups + RLS + index)</name>
  <files>supabase/migrations/0006_analyses_trade_setups.sql</files>
  <read_first>
    - supabase/migrations/0005_snapshots.sql (analog exact : table + FK cascade + check enums + RLS select-only + AUCUNE write policy + index nommés)
    - ARCHITECTURE.md §4 lignes 174-175 (colonnes analyses/trade_setups) + 04-RESEARCH.md lignes 357-399 (DDL §4 forme + note A2)
    - 04-PATTERNS.md lignes 246-281 (patron migration 0005)
  </read_first>
  <action>
    Écrire `supabase/migrations/0006_analyses_trade_setups.sql`. Table `public.analyses` : id uuid pk default gen_random_uuid(), run_id text not null, session text not null check (session in ('asia','london','newyork','eod-swing')), style text not null check (style in ('day','swing')), instrument_id uuid not null references public.instruments(id) on delete cascade, snapshot jsonb not null, model text not null, prompt_version text not null, schema_version text not null, created_at timestamptz not null default now(). Table `public.trade_setups` : id uuid pk, analysis_id uuid not null references public.analyses(id) on delete cascade, instrument_id uuid not null references public.instruments(id) on delete cascade, **style text not null check (style in ('day','swing'))** et **session text not null check (session in ('asia','london','newyork','eod-swing'))** dénormalisés (A2 — D-45, index d'expiry direct), direction text check (direction in ('long','short')), opportunity_score int not null, risk_level text not null check (risk_level in ('low','medium','high','extreme')), confidence text not null check (confidence in ('low','moderate','high')), entry_price numeric not null, stop_loss numeric not null, take_profits jsonb not null, risk_reward numeric not null, payload jsonb not null, status text not null default 'active' check (status in ('active','invalidated','expired')), valid_until timestamptz not null, created_at timestamptz not null default now(). RLS : `enable row level security` sur les deux + une policy `for select to authenticated using (true)` chacune, AUCUNE policy insert/update/delete (commenter `-- service_role bypass D-05`). Index : `trade_setups_score_idx (opportunity_score desc, created_at desc)` (§4) et `trade_setups_versionkey_idx (instrument_id, style, session, status) where status = 'active'` (clé d'expiry D-45).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0006_analyses_trade_setups.sql | grep -c "for select" | grep -qx 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fichier contient `create table public.analyses` et `create table public.trade_setups`
    - 2 policies `for select to authenticated`, 0 policy `for insert`/`for update`/`for delete`
    - `trade_setups` a les colonnes `style` et `session` (A2 dénormalisation)
    - index `trade_setups_score_idx` et `trade_setups_versionkey_idx` présents
  </acceptance_criteria>
  <done>SQL versionné prêt à appliquer (source de vérité), non encore appliqué à la base.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Appliquer migration 0006 via MCP + régénérer types + repos</name>
  <what-built>
    Migration 0006 écrite (SQL versionné). Cette étape l'applique à la base LIVE via MCP `apply_migration` (D-17, PAS `supabase db push`), régénère les types TS, puis crée les repos typés.
  </what-built>
  <read_first>
    - packages/supabase/src/repositories/snapshots.ts (patron repo service_role complet : ServiceClient, insert().select('id').single(), throw on error)
    - packages/supabase/src/index.ts (barrel zone Phase 3 — où ajouter exports + types)
    - 04-PATTERNS.md lignes 203-242 (patron analyses.ts/tradeSetups.ts + barrel)
  </read_first>
  <how-to-verify>
    1. Appliquer la migration : invoquer MCP Supabase `apply_migration` avec le contenu de `supabase/migrations/0006_analyses_trade_setups.sql` (nom `0006_analyses_trade_setups`).
    2. Vérifier `get_advisors` (security) → 0 alerte RLS sur analyses/trade_setups (RLS active, pas de table exposée).
    3. Régénérer les types : MCP `generate_typescript_types` (ou `supabase gen types typescript --linked`) → écrire dans `packages/supabase/src/database.types.ts`. Vérifier que `analyses`/`trade_setups` y apparaissent.
    4. Ajouter au barrel `index.ts` les types `AnalysisRow/Insert`, `TradeSetupRow/Insert` (bloc export type) et préparer la zone d'export des repos (Phase 4).
    5. Créer `packages/supabase/src/repositories/analyses.ts` : `insertAnalysis(client: ServiceClient, row: AnalysisInsert): Promise<{ id: string }>` via `.insert([row]).select('id').single()`, throw `insertAnalysis failed: ${error.message}`. Docstring "JAMAIS importé depuis apps/web".
    6. Créer `packages/supabase/src/repositories/tradeSetups.ts` : `insertTradeSetups(client, rows: TradeSetupInsert[]): Promise<void>` + `expirePriorSetups(client, key: {instrument_id, style, session}): Promise<void>` (UPDATE status='expired' WHERE instrument_id=… AND style=… AND session=… AND status='active' — seul status transitionne, D-45/SCORE-05). Exporter les deux du barrel.
    7. Lancer `pnpm --filter @app/supabase exec tsc --noEmit` (types compilent contre la base live).
  </how-to-verify>
  <acceptance_criteria>
    - `get_advisors` ne reporte aucune table sans RLS pour analyses/trade_setups
    - `packages/supabase/src/database.types.ts` contient les types `analyses` et `trade_setups`
    - `insertAnalysis`, `insertTradeSetups`, `expirePriorSetups` exportés du barrel `@app/supabase`
    - `tsc --noEmit` du package supabase passe
  </acceptance_criteria>
  <resume-signal>Tapez "approved" une fois la migration appliquée, les types régénérés et les repos créés (ou décrivez les erreurs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs service_role → DB | Écriture analyses/trade_setups : seul chemin d'écriture, jamais le web |
| web authenticated → DB | Lecture seule via RLS select-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | RLS analyses/trade_setups | mitigate | `enable row level security` + policy select-only to authenticated, AUCUNE write policy (service_role bypass D-05) ; vérifié par `get_advisors` (Task 3) |
| T-04-02 | Information Disclosure | service-client export | mitigate | repos importent le client par paramètre ; barrel ne ré-exporte JAMAIS service-client (D-07) |
| T-04-03 | Tampering | mutation prédiction historique | mitigate | expirePriorSetups ne touche QUE `status` (active→expired) ; aucune UPDATE des champs de prédiction (SCORE-05/D-45) |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package (RESEARCH : zéro install) — slopcheck non requis |
</threat_model>

<verification>
- `pnpm vitest run packages/core` vert (OutputSchema).
- Migration 0006 appliquée (MCP) + `get_advisors` propre + types régénérés.
- `pnpm --filter @app/supabase exec tsc --noEmit` vert.
</verification>

<success_criteria>
Tables analyses/trade_setups en base live avec RLS correcte, OutputSchema §3 golden-testé, repos insert/expiry typés et exportés.
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-01-SUMMARY.md` à la fin.
</output>
