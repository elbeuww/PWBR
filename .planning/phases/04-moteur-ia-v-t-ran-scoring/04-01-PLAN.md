---
phase: 04-moteur-ia-v-t-ran-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0006_analyses_trade_setups.sql
  - packages/core/src/schemas/output.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/schemas/output-schema.test.ts
  - packages/core/__tests__/schemas/migration-0006.test.ts
  - packages/supabase/src/repositories/analyses.ts
  - packages/supabase/src/repositories/tradeSetups.ts
  - packages/supabase/src/index.ts
  - packages/supabase/src/database.types.ts
autonomous: false
requirements: [SCORE-01, SCORE-05]
must_haves:
  truths:
    - "Les tables analyses et trade_setups existent dans la base live avec RLS select-only authenticated et aucune write policy (service_role bypass) ; analyses porte les colonnes de traçabilité de version model/prompt_version/schema_version/run_id (D-51)"
    - "Un JSON conforme au contrat §3 (exemple XAU_USD) passe OutputSchema.parse ; un JSON dérivé (enum hors §3, take_profits vide ou >3) est rejeté ; OutputSchema est PERMISSIF (z.object, PAS .strict()) — les clés en trop émises par l'agent (l'exemple §3 contient opportunity_score/risk_level/confidence) NE provoquent PAS de rejet Zod ; ces 3 champs ne figurent PAS dans le schéma et sont produits par le code, jamais par l'agent (D-42, A1)"
    - "Les repos insertAnalysis / insertTradeSetups / expirePriorSetups existent et sont typés depuis database.types régénérés ; la clé d'immuabilité inclut session_day (instrument_id, style, session, session_day) — strictement cohérente entre l'index unique partiel SQL et la signature expirePriorSetups (D-45, concern revue #1)"
  artifacts:
    - path: "supabase/migrations/0006_analyses_trade_setups.sql"
      provides: "DDL analyses + trade_setups (avec session_day) + RLS + index score + index unique partiel d'expiry"
      contains: "create table public.analyses"
    - path: "packages/core/src/schemas/output.ts"
      provides: "OutputSchema Zod §3 PERMISSIF (contrat JSON IA) + type Output"
      exports: ["OutputSchema", "Output"]
    - path: "packages/supabase/src/repositories/analyses.ts"
      provides: "insertAnalysis service_role retournant id"
      exports: ["insertAnalysis"]
    - path: "packages/supabase/src/repositories/tradeSetups.ts"
      provides: "insertTradeSetups + expirePriorSetups (immuabilité D-45, clé avec session_day)"
      exports: ["insertTradeSetups", "expirePriorSetups"]
  key_links:
    - from: "packages/supabase/src/repositories/analyses.ts"
      to: "public.analyses"
      via: "insert service_role"
      pattern: "from\\('analyses'\\)"
    - from: "packages/supabase/src/repositories/tradeSetups.ts"
      to: "public.trade_setups"
      via: "update status=expired sur clé active (incluant session_day)"
      pattern: "status.*expired"
---

<objective>
Poser la fondation persistante + le contrat de sortie de la Phase 4 : la migration `0006` crée `analyses` + `trade_setups` (DDL §4 + RLS calquée sur 0005), appliquée à la base live via MCP `apply_migration` (D-17) ; le `OutputSchema` Zod §3 PERMISSIF (contrat JSON de l'agent) et les repos typés `analyses`/`trade_setups` (upsert service_role + expiry immuable D-45) sont créés.

Purpose : sans tables ni contrat, aucun setup ne peut être validé ni persisté. C'est le socle des slices 04-02 (scoring) et 04-03 (persist).
Output : migration appliquée, types régénérés, OutputSchema + repos exportés du barrel `@app/supabase`.

Concern revue #1 (HIGH consensus) tranché ici : la clé d'immuabilité D-45 « (instrument, style, session, jour) » est matérialisée par une colonne **`session_day date`** (dérivée déterministe de `generated_at` en UTC) — incluse dans l'index unique partiel ET dans la signature de `expirePriorSetups`. Plus de contradiction avec 04-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-CONTEXT.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-RESEARCH.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-PATTERNS.md
@.planning/phases/04-moteur-ia-v-t-ran-scoring/04-REVIEWS.md
@ARCHITECTURE.md

<interfaces>
<!-- Conventions Zod v4 à répliquer — extraites de packages/indicators/src/snapshots/schema.ts -->
KeyLevelSchema/TechnicalSnapshotSchema : z.object, z.enum, z.number().min(-1).max(1), type via z.infer.
ARCHITECTURE §3 contrat JSON sortie (source de vérité) : schema_version "1.0", generated_at ISO, session enum(asia|london|newyork|eod-swing), style enum(day|swing), instrument string, direction enum(long|short), timeframe_analysis string, entry {type:limit|market|stop, price:number, zone:[number,number]}, stop_loss number, take_profits[{price, alloc_pct}] min1 max3, technical_reasons string[], fundamental_reasons string[], news_catalysts[{headline,impact,direction,ts}], upcoming_risk_events[{event,ts,note}], invalidation string, veteran_note string, raw_indicators_ref string.

<!-- A1 RÉSOLU (concern revue, quality [HIGH]) : OutputSchema PERMISSIF = z.object({...}) SANS .strict(). L'exemple §3 dans veteran.md contient opportunity_score:78/risk_level/confidence ; si l'agent les émet, le schéma permissif les ignore (Zod strip par défaut) au lieu de rejeter. opportunity_score/risk_level/confidence/risk_reward/atr_distance_sl NE figurent PAS dans OutputSchema — le code les produit/recalcule (D-42/46/48/50). -->

<!-- Patron repository — extrait de packages/supabase/src/repositories/snapshots.ts -->
type ServiceClient = SupabaseClient<Database>
upsertSnapshot(client, row): Promise<void> ; if (error) throw new Error(`x failed: ${error.message}`)
getSnapshotByHash(client, hash): select('*').eq('content_hash', hash).maybeSingle()

<!-- Barrel @app/supabase — zone Phase 3 (index.ts:58-61) où ajouter les exports Phase 4. D-07 : ne JAMAIS exporter de service-client. -->

<!-- Patron migration RLS — extrait de supabase/migrations/0005_snapshots.sql -->
enable row level security + policy select to authenticated using(true) + AUCUNE write policy (D-05/T-03-01).
Note RLS using(true) : laissé tel quel en P1 (perso) ; au pivot payant P5 ajouter une policy par abonnement (concern security [MEDIUM], non bloquant P1 — noté pour P5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutputSchema Zod §3 PERMISSIF (contrat JSON IA) + fixtures Wave 0</name>
  <files>packages/core/src/schemas/output.ts, packages/core/src/index.ts, packages/core/__tests__/schemas/output-schema.test.ts</files>
  <read_first>
    - packages/indicators/src/snapshots/schema.ts (analog Zod v4 exact — conventions z.enum/z.object/z.infer)
    - ARCHITECTURE.md §3 lignes 123-163 (contrat JSON sortie = source de vérité champ par champ ; noter que l'exemple contient opportunity_score/risk_level/confidence)
    - packages/core/src/index.ts (barrel — pattern export ./...js extension ESM obligatoire) — créer le répertoire packages/core/src/schemas/ s'il n'existe pas
    - 04-RESEARCH.md lignes 317-343 (Code Example OutputSchema + note A1) + 04-PATTERNS.md lignes 181-199
    - 04-REVIEWS.md (concern A1 quality [HIGH] : permissif vs strict)
  </read_first>
  <behavior>
    - Test : fixture exemple §3 complète (XAU_USD, dérivée d'ARCHITECTURE §3) → OutputSchema.parse ne throw pas
    - Test (A1) : objet §3 valide AVEC clés en trop opportunity_score:78 + risk_level:'medium' + confidence:'high' → parse RÉUSSIT (permissif, pas .strict()) et le résultat ne contient PAS ces clés (Zod les strip)
    - Test : session enum hors §3 ('tokyo') → throw
    - Test : take_profits vide (0) → throw ; take_profits avec 4 entrées → throw ; 1 à 3 → ok
    - Test : entry.zone non-tuple (1 seul nombre) → throw
    - Test : OutputSchema NE contient PAS opportunity_score/risk_level/confidence (A1) — parse réussit même si ces champs absents
  </behavior>
  <action>
    Créer `OutputSchema` (Zod v4) dans `packages/core/src/schemas/output.ts` encodant le contrat §3 champ par champ. Utiliser `z.object({...})` PERMISSIF — NE PAS appeler `.strict()` (A1, concern revue quality [HIGH]) : l'agent peut émettre des clés supplémentaires (l'exemple §3 inclut opportunity_score/risk_level/confidence) sans déclencher de rejet Zod ; Zod strip les clés inconnues par défaut. Utiliser `z.iso.datetime()` pour `generated_at`/`ts` (Zod v4), `z.tuple([z.number(), z.number()])` pour `entry.zone`, `z.array(...).min(1).max(3)` pour `take_profits` (objets `{price:z.number(), alloc_pct:z.number()}` — `rr` recalculé code, PAS dans le schéma), `z.enum` pour session/style/direction/entry.type. Exclure opportunity_score/risk_level/confidence/risk_reward/atr_distance_sl du schéma (A1 — le code les produit, D-42/46/48). Exporter `type Output = z.infer<typeof OutputSchema>`. Ajouter `export { OutputSchema } from './schemas/output.js'` et `export type { Output } from './schemas/output.js'` au barrel `packages/core/src/index.ts` (extension `.js`). Créer la fixture XAU_USD figée + tests valides/invalides (patron `valid… / bad = { ...valid, champ: mauvais }` de snapshots.test.ts) + le test « parse réussit avec clés supplémentaires » (A1).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/schemas/output-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/schemas/output.ts` exporte `OutputSchema` + `type Output`
    - `OutputSchema` utilise `z.object` sans `.strict()` (vérifiable : `OutputSchema.parse({ ...exempleValide, opportunity_score: 78, risk_level: 'medium', confidence: 'high' })` RÉUSSIT et le retour n'a aucune de ces 3 clés)
    - `OutputSchema` n'a pas de clé `opportunity_score`/`risk_level`/`confidence` (vérifiable : un parse d'un objet §3 sans ces clés réussit)
    - `pnpm vitest run packages/core/__tests__/schemas/output-schema.test.ts` passe (fixture valide + test clés-en-trop A1 + ≥4 cas de rejet)
    - barrel `@app/core` réexporte `OutputSchema` et `Output`
  </acceptance_criteria>
  <done>Contrat JSON §3 verrouillé par Zod PERMISSIF (A1) + golden-testé, réutilisable web+jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0006 SQL (DDL analyses + trade_setups + session_day + RLS + index) + test SQL cross-platform</name>
  <files>supabase/migrations/0006_analyses_trade_setups.sql, packages/core/__tests__/schemas/migration-0006.test.ts</files>
  <read_first>
    - supabase/migrations/0005_snapshots.sql (analog exact : table + FK cascade + check enums + RLS select-only + AUCUNE write policy + index nommés)
    - ARCHITECTURE.md §4 lignes 174-175 (colonnes analyses/trade_setups) + 04-RESEARCH.md lignes 357-399 (DDL §4 forme + note A2)
    - 04-PATTERNS.md lignes 246-281 (patron migration 0005)
    - 04-REVIEWS.md (concern #1 HIGH : la clé d'immuabilité doit inclure une dimension jour → colonne session_day ; concern nettoyage : grep Windows-hostile → remplacé par un test cross-platform)
  </read_first>
  <behavior>
    - Test (cross-platform, remplace le grep Windows-hostile) : le test lit `supabase/migrations/0006_analyses_trade_setups.sql` via `fs.readFileSync`, strip les lignes commençant par `--`, et assert :
      - exactement 2 occurrences de `for select to authenticated`
      - 0 occurrence de `for insert`, `for update`, `for delete`
      - présence de `create table public.analyses` et `create table public.trade_setups`
      - présence de `session_day date not null`
      - `trade_setups_versionkey_idx` est déclaré `unique` et contient `(instrument_id, style, session, session_day)` + `where status = 'active'`
  </behavior>
  <action>
    Écrire `supabase/migrations/0006_analyses_trade_setups.sql`. Table `public.analyses` : id uuid pk default gen_random_uuid(), run_id text not null, session text not null check (session in ('asia','london','newyork','eod-swing')), style text not null check (style in ('day','swing')), instrument_id uuid not null references public.instruments(id) on delete cascade, snapshot jsonb not null, model text not null, prompt_version text not null, schema_version text not null, created_at timestamptz not null default now(). Table `public.trade_setups` : id uuid pk, analysis_id uuid not null references public.analyses(id) on delete cascade, instrument_id uuid not null references public.instruments(id) on delete cascade, **style text not null check (style in ('day','swing'))** et **session text not null check (session in ('asia','london','newyork','eod-swing'))** dénormalisés (A2 — D-45, index d'expiry direct), **session_day date not null** (concern revue #1 — dimension « jour » de la clé d'immuabilité D-45, dérivée déterministe de generated_at en UTC côté persist.ts), direction text check (direction in ('long','short')), opportunity_score int not null, risk_level text not null check (risk_level in ('low','medium','high','extreme')), confidence text not null check (confidence in ('low','moderate','high')), entry_price numeric not null, stop_loss numeric not null, take_profits jsonb not null, risk_reward numeric not null, payload jsonb not null, status text not null default 'active' check (status in ('active','invalidated','expired')), valid_until timestamptz not null, created_at timestamptz not null default now(). RLS : `enable row level security` sur les deux + une policy `for select to authenticated using (true)` chacune, AUCUNE policy insert/update/delete (commenter `-- service_role bypass D-05`). Index : `trade_setups_score_idx (opportunity_score desc, created_at desc)` (§4) et **`create unique index trade_setups_versionkey_idx on public.trade_setups (instrument_id, style, session, session_day) where status = 'active'`** (clé d'immuabilité D-45 AVEC session_day, UNIQUE partiel = filet DB contre la race expire→insert, concern archi [HIGH] race ; documenter en commentaire `-- filet DB race expire→insert P1 (D-43) ; RPC atomique reportée`). Créer `packages/core/__tests__/schemas/migration-0006.test.ts` (cross-platform via fs, voir behavior) — remplace le grep Windows-hostile du plan précédent.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/__tests__/schemas/migration-0006.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fichier SQL contient `create table public.analyses` et `create table public.trade_setups`
    - `trade_setups` a la colonne `session_day date not null` (concern revue #1)
    - test assert 2 policies `for select to authenticated` + 0 policy insert/update/delete (assertion via fs, indépendante de l'OS — plus de grep)
    - `trade_setups` a les colonnes `style` et `session` (A2 dénormalisation)
    - index `trade_setups_score_idx` présent ; `trade_setups_versionkey_idx` est `unique` et porte `(instrument_id, style, session, session_day)` avec `where status = 'active'`
    - `pnpm vitest run packages/core/__tests__/schemas/migration-0006.test.ts` vert
  </acceptance_criteria>
  <done>SQL versionné prêt à appliquer (source de vérité, clé d'immuabilité avec session_day + unique partiel filet race), vérifié par test cross-platform, non encore appliqué à la base.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Appliquer migration 0006 via MCP + régénérer types + repos</name>
  <what-built>
    Migration 0006 écrite (SQL versionné, avec session_day + unique partiel). Cette étape l'applique à la base LIVE via MCP `apply_migration` (D-17, PAS `supabase db push`), régénère les types TS, puis crée les repos typés.
  </what-built>
  <read_first>
    - packages/supabase/src/repositories/snapshots.ts (patron repo service_role complet : ServiceClient, insert().select('id').single(), throw on error)
    - packages/supabase/src/index.ts (barrel zone Phase 3 — où ajouter exports + types)
    - 04-PATTERNS.md lignes 203-242 (patron analyses.ts/tradeSetups.ts + barrel)
    - 04-REVIEWS.md (concern #1 : expirePriorSetups doit prendre session_day dans sa clé)
  </read_first>
  <how-to-verify>
    1. Appliquer la migration : invoquer MCP Supabase `apply_migration` avec le contenu de `supabase/migrations/0006_analyses_trade_setups.sql` (nom `0006_analyses_trade_setups`).
    2. Vérifier `get_advisors` (security) → 0 alerte RLS sur analyses/trade_setups (RLS active, pas de table exposée).
    3. Régénérer les types : MCP `generate_typescript_types` (ou `supabase gen types typescript --linked`) → écrire dans `packages/supabase/src/database.types.ts`. Vérifier que `analyses`/`trade_setups` (avec `session_day`) y apparaissent.
    4. Ajouter au barrel `index.ts` les types `AnalysisRow/Insert`, `TradeSetupRow/Insert` (bloc export type) et préparer la zone d'export des repos (Phase 4).
    5. Créer `packages/supabase/src/repositories/analyses.ts` : `insertAnalysis(client: ServiceClient, row: AnalysisInsert): Promise<{ id: string }>` via `.insert([row]).select('id').single()`, throw `insertAnalysis failed: ${error.message}`. Docstring "JAMAIS importé depuis apps/web".
    6. Créer `packages/supabase/src/repositories/tradeSetups.ts` : `insertTradeSetups(client, rows: TradeSetupInsert[]): Promise<void>` + **`expirePriorSetups(client, key: { instrument_id: string; style: string; session: string; session_day: string }): Promise<void>`** (UPDATE status='expired' WHERE instrument_id=… AND style=… AND session=… AND session_day=… AND status='active' — seul status transitionne, D-45/SCORE-05 ; la clé inclut OBLIGATOIREMENT session_day, miroir exact de l'index unique partiel). Exporter les deux du barrel.
    7. Lancer `pnpm --filter @app/supabase exec tsc --noEmit` (types compilent contre la base live).
  </how-to-verify>
  <acceptance_criteria>
    - `get_advisors` ne reporte aucune table sans RLS pour analyses/trade_setups
    - `packages/supabase/src/database.types.ts` contient les types `analyses` et `trade_setups` (incluant `session_day`)
    - `insertAnalysis`, `insertTradeSetups`, `expirePriorSetups` exportés du barrel `@app/supabase`
    - `expirePriorSetups` accepte une clé `{ instrument_id, style, session, session_day }` (les 4 dimensions D-45) et filtre `where session_day = key.session_day AND status = 'active'`
    - `tsc --noEmit` du package supabase passe
  </acceptance_criteria>
  <resume-signal>Tapez "approved" une fois la migration appliquée, les types régénérés et les repos créés (ou décrivez les erreurs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs service_role → DB | Écriture analyses/trade_setups : seul chemin d'écriture, jamais le web |
| web authenticated → DB | Lecture seule via RLS select-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | RLS analyses/trade_setups | mitigate | `enable row level security` + policy select-only to authenticated, AUCUNE write policy (service_role bypass D-05) ; vérifié par `get_advisors` (Task 3) |
| T-04-02 | Information Disclosure | service-client export | mitigate | repos importent le client par paramètre ; barrel ne ré-exporte JAMAIS service-client (D-07) |
| T-04-03 | Tampering | mutation prédiction historique | mitigate | expirePriorSetups ne touche QUE `status` (active→expired) ; aucune UPDATE des champs de prédiction (SCORE-05/D-45) |
| T-04-13 | Tampering / concurrency | race expire→insert (deux runs chevauchants, même clé) | mitigate | index UNIQUE partiel `(instrument_id, style, session, session_day) where status='active'` = filet DB (deux active même clé impossible) ; RPC atomique reportée P1 (risque résiduel documenté, concern archi [HIGH]) |
| T-04-SC | Tampering | npm installs | accept | aucun nouveau package (RESEARCH : zéro install) — slopcheck non requis |
</threat_model>

<verification>
- `pnpm vitest run packages/core` vert (OutputSchema permissif + cas A1 + migration-0006 cross-platform).
- Migration 0006 appliquée (MCP) + `get_advisors` propre + types régénérés (avec session_day).
- `pnpm --filter @app/supabase exec tsc --noEmit` vert.
</verification>

<success_criteria>
Tables analyses/trade_setups en base live avec RLS correcte et colonne session_day + index unique partiel, OutputSchema §3 PERMISSIF golden-testé (A1), repos insert/expiry typés (clé avec session_day) et exportés.
</success_criteria>

<output>
Créer `.planning/phases/04-moteur-ia-v-t-ran-scoring/04-01-SUMMARY.md` à la fin.
</output>
