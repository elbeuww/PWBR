---
phase: 03-moteur-d-analyse-d-terministe
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - apps/jobs/src/jobs/fundamental-engine.ts
  - apps/jobs/src/jobs/news-engine.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/__tests__/fundamental-engine.test.ts
  - apps/jobs/__tests__/news-engine.test.ts
autonomous: true
requirements: [FUND-01, FUND-02, FUND-03]
user_setup: []

must_haves:
  truths:
    - "Le fundamental-engine dérive macro_bias + rate_environment par règles déterministes sur les séries FRED (D-38)"
    - "Les drivers par actif sont lus depuis la table asset_drivers (data-not-code), pas codés en dur"
    - "Le news-engine calcule net_sentiment par moyenne pondérée avec décroissance temporelle, fenêtre alignée au style (D-39)"
    - "news_risk flag les events high-impact imminents <2h (day) / <24h (swing) par instrument/style (D-40)"
    - "Les snapshots fundamental_context et news_context respectent la forme §3 (Zod-validés) et sont persistés idempotents"
  artifacts:
    - path: "apps/jobs/src/jobs/fundamental-engine.ts"
      provides: "macro_series + asset_drivers → fundamental_context §3"
      min_lines: 50
    - path: "apps/jobs/src/jobs/news-engine.ts"
      provides: "news (window+decay) + economic_calendar → news_context §3 + news_risk"
      min_lines: 50
  key_links:
    - from: "apps/jobs/src/jobs/fundamental-engine.ts"
      to: "@app/supabase getAssetDrivers"
      via: "lecture drivers par actif (D-38)"
      pattern: "getAssetDrivers"
    - from: "apps/jobs/src/jobs/news-engine.ts"
      to: "economic_calendar"
      via: "fenêtre news_risk <2h/<24h (D-40)"
      pattern: "news_risk"
    - from: "apps/jobs/src/dispatch.ts"
      to: "fundamental-engine + news-engine"
      via: "JOB_REGISTRY"
      pattern: "'fundamental-engine'"
---

<objective>
Deux slices verticaux fondamental + news : le `fundamental-engine` dérive par règles déterministes (zéro IA) `fundamental_context` (macro_bias risk_on/off, rate_environment hawkish/dovish, drivers par actif via la table asset_drivers — D-38) à partir des séries FRED ; le `news-engine` dérive `news_context` (net_sentiment pondéré-décroissant aligné au style, recent_catalysts, upcoming_events + flag news_risk <2h/<24h — D-39/D-40) à partir des tables news + economic_calendar déjà remplies en Phase 2. Snapshots §3 Zod-validés, hashés, upsertés. Enregistrés dans le dispatcher.

Purpose: Complète les trois snapshots que le moteur vétéran (Phase 4) consommera. Le code calcule tout ; aucune dérivation laissée à l'IA. Couvre FUND-01/02/03.
Output: jobs fundamental-engine + news-engine, snapshots fondamental/news en base, golden tests verts.

Slice vertical : après ce plan, les trois contextes (technique du plan 03 + fondamental + news) existent en base par instrument — l'entrée complète de la Phase 4 est prête.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-RESEARCH.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md

<interfaces>
<!-- Engine job harness — copy macro-ingest.ts verbatim structure (dotenv, lazy client, stats, per-item try/catch, WR-04) -->

<!-- From @app/supabase (plan 01 + Phase 2 repos) -->
getAssetDrivers(client, instrumentId)   → [{driver_code,direction,weight}]   (D-38)
upsertSnapshot(client, row: SnapshotInsert)   onConflict instrument_id,style,kind,computed_for_ts
-- Phase 2 repos: macroSeries (FRED DFF/CPIAUCSL/DTWEXBGS≈DXY/DFII10≈real_yields), news, economicCalendar

<!-- From @app/indicators (plan 02) -->
FundamentalContextSchema, NewsContextSchema, snapshotContentHash   from '@app/indicators'

<!-- LOCKED §3 shapes (ARCHITECTURE §3 lines 91-100) -->
fundamental_context: macro_bias risk_on|risk_off|neutral; rate_environment hawkish|dovish|neutral; dxy_trend; real_yields; asset_specific_drivers[]
news_context: net_sentiment -1..+1; recent_catalysts[{headline,ts,impact,direction}]; upcoming_events[{event,ts,expected_impact}]

<!-- Style windows (D-39/D-40) -->
Day  : sentiment window ≈ 24h ; news_risk < 2h
Swing: sentiment window ≈ 7j  ; news_risk < 24h
décroissance temporelle (EWMA-like, half-life = constante nommée pinée par golden test)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fundamental-engine (règles FRED déterministes + drivers par actif)</name>
  <files>apps/jobs/src/jobs/fundamental-engine.ts, apps/jobs/__tests__/fundamental-engine.test.ts</files>
  <read_first>
    - apps/jobs/src/jobs/macro-ingest.ts (analog EXACT du harness engine)
    - packages/data-sources/src/fred/schema.test.ts (analog golden test pur)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 311-328 (corps fundamental-engine)
    - ARCHITECTURE.md §3 lignes 91-95 (forme fundamental_context LOCKED)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-CONTEXT.md D-38 (règles macro + table drivers)
  </read_first>
  <behavior>
    - Fonction pure `deriveFundamentalContext(macroSeries, assetDrivers): FundamentalContext` : règles déterministes nommées — ex. DXY (DTWEXBGS) ↗ + real_yields (DFII10) ↗ → macro_bias risk_off ; DFF en hausse récente → rate_environment hawkish. Seuils = constantes nommées pinées par golden test (D-38).
    - asset_specific_drivers construits depuis getAssetDrivers (lecture table, jamais en dur — D-38) : pour l'or, DXY(-1) + REAL_YIELDS(-1) ; pour crypto, RISK_SENTIMENT(+1) + DXY(-1).
    - Le contexte passe FundamentalContextSchema.parse ; hash déterministe sur deux appels.
    - Un instrument sans drivers en table → contexte macro global sans personnalisation (pas de crash).
  </behavior>
  <action>
    Écrire fundamental-engine.test.ts AVANT (RED) : fixture macro_series offline + drivers factices → deriveFundamentalContext produit un objet parse-OK §3, macro_bias/rate_environment attendus pour des entrées connues, hash stable. Puis créer apps/jobs/src/jobs/fundamental-engine.ts copiant le squelette macro-ingest.ts (dotenv, getServiceClient lazy, `export async function fundamentalEngine(): Promise<Json>`, stats, per-instrument try/catch, WR-04). Exporter la fonction pure `deriveFundamentalContext` (testabilité, D-23). Le job lit macro_series (repo Phase 2) + getAssetDrivers par instrument → dérive le contexte → Zod-valide → snapshotContentHash → upsertSnapshot avec kind='fundamental'. computed_for_ts = ts de la dernière observation macro pertinente. Importer depuis @app/supabase / @app/indicators ; jamais service-client du barrel (D-07).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/fundamental-engine.test.ts && pnpm tsc -p apps/jobs/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test RED écrit avant implémentation ; après : `pnpm vitest run apps/jobs/__tests__/fundamental-engine.test.ts` exit 0
    - deriveFundamentalContext exportée (pure) ; macro_bias/rate_environment dérivés par règles nommées, pas de magie
    - asset_specific_drivers lus via getAssetDrivers (table), aucun driver codé en dur dans le .ts
    - Snapshot parse §3 OK, upserté kind='fundamental', hash déterministe
    - typecheck apps/jobs clean
  </acceptance_criteria>
  <done>fundamental_context §3 dérivé déterministe (FUND-01), drivers data-not-code, persisté idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: news-engine (sentiment pondéré-décroissant + news_risk imminent)</name>
  <files>apps/jobs/src/jobs/news-engine.ts, apps/jobs/__tests__/news-engine.test.ts</files>
  <read_first>
    - apps/jobs/src/jobs/macro-ingest.ts (analog harness) + apps/jobs/src/jobs/news-ingest.ts (lecture table news Phase 2)
    - packages/data-sources/src/fred/schema.test.ts (analog golden)
    - ARCHITECTURE.md §3 lignes 96-100 (forme news_context LOCKED)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-CONTEXT.md D-39/D-40 (fenêtre+décroissance, seuils news_risk)
    - .planning/phases/02-ingestion-fiable-des-donn-es/02-CONTEXT.md D-31 (table economic_calendar remplie)
  </read_first>
  <behavior>
    - Fonction pure `deriveNewsContext(news, calendar, instrumentId, style, now): NewsContext` : net_sentiment = moyenne pondérée des sentiments news de l'instrument, fenêtre ≈24h (day) / ≈7j (swing), avec décroissance temporelle (news récente pèse plus ; half-life = constante nommée pinée). recent_catalysts = top items de la fenêtre. upcoming_events depuis economic_calendar.
    - news_risk = true si un event high-impact tombe < 2h (day) / < 24h (swing) pour l'instrument/style (D-40). Seuils LOCKED.
    - Le contexte passe NewsContextSchema.parse ; hash déterministe.
    - Instrument sans news dans la fenêtre → net_sentiment neutre (0), pas de crash ; sentiment provider null (free tier, D-24) traité comme absence, pas comme 0 pondéré faux.
  </behavior>
  <action>
    Écrire news-engine.test.ts AVANT (RED) : fixtures news + calendar offline avec timestamps relatifs à un `now` fixe injecté → vérifier que la news récente pèse plus (décroissance), que la fenêtre day≠swing, que net_sentiment ∈ [-1,1], que news_risk=true pour un event à <2h (day) et false à 10h, parse §3 OK, hash stable. Puis créer apps/jobs/src/jobs/news-engine.ts copiant le squelette macro-ingest.ts (dotenv, lazy client, `export async function newsEngine(): Promise<Json>`, stats, per-instrument try/catch, WR-04). Exporter `deriveNewsContext` (now injectable pour testabilité déterministe). Le job lit news + economic_calendar (repos Phase 2) par instrument × style → dérive → Zod-valide → hash → upsertSnapshot kind='news'. Utiliser luxon pour les fenêtres/TZ (jamais de Date arithmetic maison). Jamais service-client du barrel (D-07).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/news-engine.test.ts && pnpm tsc -p apps/jobs/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test RED écrit avant ; après : `pnpm vitest run apps/jobs/__tests__/news-engine.test.ts` exit 0
    - deriveNewsContext exportée pure avec `now` injectable ; décroissance temporelle prouvée (news récente > ancienne à sentiment égal)
    - Fenêtre day (≈24h) ≠ swing (≈7j) ; net_sentiment borné [-1,1]
    - news_risk=true pour event high-impact <2h (day) / <24h (swing), false au-delà (D-40)
    - Snapshot parse §3 OK, upserté kind='news', hash déterministe ; instrument sans news → neutre sans crash
    - typecheck apps/jobs clean
  </acceptance_criteria>
  <done>news_context §3 + news_risk dérivés déterministes (FUND-02/03), persisté idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Enregistrer fundamental-engine + news-engine dans le dispatcher</name>
  <files>apps/jobs/src/dispatch.ts</files>
  <read_first>
    - apps/jobs/src/dispatch.ts (JOB_REGISTRY après ajout technical-engine du plan 03)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 332-347
  </read_first>
  <action>
    Modifier apps/jobs/src/dispatch.ts : importer fundamentalEngine et newsEngine depuis leurs jobs, ajouter `'fundamental-engine': fundamentalEngine` et `'news-engine': newsEngine` au JOB_REGISTRY (à la suite de 'technical-engine' enregistré au plan 03). runJob enveloppe automatiquement.
  </action>
  <verify>
    <automated>grep -q "'fundamental-engine': fundamentalEngine" apps/jobs/src/dispatch.ts && grep -q "'news-engine': newsEngine" apps/jobs/src/dispatch.ts && pnpm tsc -p apps/jobs/tsconfig.json --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dispatch.ts enregistre 'fundamental-engine' et 'news-engine' (kebab-case)
    - Les trois moteurs (technical/fundamental/news) coexistent dans JOB_REGISTRY
    - `pnpm vitest run` (suite complète) vert ; typecheck apps/jobs clean
  </acceptance_criteria>
  <done>Les trois moteurs appelables par le scheduler ; chaque run écrit job_runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job (service_role) → snapshots | Écriture service_role bypass RLS ; clé en apps/jobs/.env gitignored |
| news/macro/calendar DB (trusted) → engines | Données Phase 2 ; revalidation §3 à la frontière (Zod avant persist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information disclosure | service_role exposé à apps/web | mitigate | service_role uniquement apps/jobs/.env ; jamais ré-exporté du barrel (D-07) — hérité |
| T-03-14 | Input validation | dérive de forme fundamental/news avant persist | mitigate | FundamentalContextSchema/NewsContextSchema.parse (§3) AVANT upsert ; fail fast |
| T-03-15 | Information disclosure | secret dans job_runs.stats | mitigate | stats.errors = message normalisé only (T-02-13) |
| T-03-16 | Tampering | drivers macro codés en dur (non auditables) | mitigate | drivers lus depuis table asset_drivers (D-38), extensibles par UPDATE SQL — auditables |
| T-03-17 | Integrity | news_risk faux (fenêtre TZ mal calculée) | mitigate | luxon pour fenêtres/TZ (jamais de Date maison) ; seuils <2h/<24h golden-testés |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/__tests__/fundamental-engine.test.ts` + `news-engine.test.ts` verts.
- `pnpm vitest run` (suite complète) vert.
- typecheck apps/jobs clean.
- Les trois moteurs présents dans JOB_REGISTRY.
</verification>

<success_criteria>
- fundamental_context §3 dérivé par règles FRED + drivers table (FUND-01), persisté idempotent.
- news_context §3 + news_risk <2h/<24h dérivés (FUND-02/03), décroissance temporelle prouvée.
- Les trois moteurs enregistrés dans le dispatcher, écrivent job_runs.
</success_criteria>

<output>
Create `.planning/phases/03-moteur-d-analyse-d-terministe/03-04-SUMMARY.md` when done
</output>
