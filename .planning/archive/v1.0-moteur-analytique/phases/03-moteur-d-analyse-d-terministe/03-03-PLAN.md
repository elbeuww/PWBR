---
phase: 03-moteur-d-analyse-d-terministe
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - apps/jobs/src/jobs/technical-engine.ts
  - apps/jobs/src/dispatch.ts
  - apps/jobs/__tests__/technical-engine.test.ts
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03, TECH-04]
user_setup: []

must_haves:
  truths:
    - "Le job technical-engine lit les bougies crypto réelles en base, calcule le technical_snapshot et le persiste"
    - "Le snapshot produit respecte la forme §3 LOCKED (Zod-validé avant insert)"
    - "Le timeframe_set par style suit D-36 : Day = HTF H4 / LTF H1, Swing = HTF Daily / LTF H4 (trend_htf/trend_ltf calculés sur ces paires)"
    - "Un instrument sans assez d'historique pour EMA200 produit un snapshot partial:true flaggé, pas un crash"
    - "Un instrument sans bougies (OANDA en attente) est isolé (stats.skipped), n'interrompt pas les autres"
    - "Re-run du job sur les mêmes bougies → même hash → aucun doublon (idempotent)"
  artifacts:
    - path: "apps/jobs/src/jobs/technical-engine.ts"
      provides: "Job technical-engine : candles → indicators+structure → §3 snapshot → upsert"
      min_lines: 50
    - path: "apps/jobs/__tests__/technical-engine.test.ts"
      provides: "Test forme §3 + hash déterministe + gap EMA200"
      min_lines: 20
  key_links:
    - from: "apps/jobs/src/jobs/technical-engine.ts"
      to: "@app/indicators"
      via: "wrappers + structure + snapshotContentHash + TechnicalSnapshotSchema"
      pattern: "@app/indicators"
    - from: "apps/jobs/src/jobs/technical-engine.ts"
      to: "@app/supabase upsertSnapshot"
      via: "persist avec Zod-validation avant insert"
      pattern: "upsertSnapshot"
    - from: "apps/jobs/src/dispatch.ts"
      to: "technical-engine"
      via: "JOB_REGISTRY"
      pattern: "'technical-engine'"
---

<objective>
Premier slice vertical complet de bout en bout : le job `technical-engine` lit les bougies crypto réelles déjà en base (~30 935 lignes BTC/ETH/SOL/BNB/XRP), calcule indicateurs + structure via @app/indicators, assemble un `technical_snapshot` conforme §3 par instrument × style (D-36 : Day=H4/H1, Swing=Daily/H4), le Zod-valide, le hashe (D-41) et l'upserte dans `snapshots`. Enregistré dans le dispatcher.

Purpose: Prouve la chaîne lecture→calcul→validation→hash→persistance lisible de bout en bout sur des données réelles — c'est le critère "même bougies → même snapshot". Couvre TECH-04 (assemblage) en consommant TECH-01/02/03 du plan 02.
Output: job technical-engine fonctionnel, snapshots technique en base, test §3+hash vert.

Slice vertical (MVP) : après ce plan, un snapshot technique réel existe en base et est consultable — capacité utilisateur nouvelle vs plans 01/02 (qui ne produisaient que des fonctions/tables vides).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-RESEARCH.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md

<interfaces>
<!-- Engine job harness — copy macro-ingest.ts verbatim structure -->
import 'dotenv/config'  (first)
getServiceClient() lazy : valide SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, createClient<Database>
export async function technicalEngine(): Promise<Json>
  stats = { inserted, skipped, errors: [{instrument, msg}] }   (msg normalisé only, T-02-13)
  per-instrument try/catch isolation (Pitfall 5 : OANDA vide ne crash pas)
  WR-04 : si 0 produit + erreurs → throw

<!-- From @app/indicators (plan 02) -->
rsi, macd, ema, atr, bollinger, detectSwings, detectBosChoch, clusterLevels, computePoc,
snapshotContentHash, TechnicalSnapshotSchema  from '@app/indicators'

<!-- From @app/supabase (plan 01) -->
upsertSnapshot(client, row: SnapshotInsert)  from '@app/supabase'   onConflict instrument_id,style,kind,computed_for_ts

<!-- From @app/core (consume, never redefine — D-10) -->
lastClosedCandleStart, TIMEFRAMES  from '@app/core'

<!-- TF→style mapping (D-36) -->
Day  : HTF=H4, LTF=H1   → timeframe_set 'H4/H1'
Swing: HTF=Daily, LTF=H4 → timeframe_set 'D/H4'
computed_for_ts = dernière bougie clôturée du LTF
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test technical-engine (forme §3 + hash déterministe + gap EMA200)</name>
  <files>apps/jobs/__tests__/technical-engine.test.ts</files>
  <read_first>
    - packages/data-sources/src/fred/schema.test.ts (analog golden : createRequire + fixture offline + pin exact)
    - packages/supabase/__tests__/idempotency.test.ts (analog DB : process.loadEnvFile apps/jobs/.env, upsert×2 count inchangé, cleanup WR-06)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 351-356 (pattern test engine)
    - ARCHITECTURE.md §3 lignes 82-90 (forme technical_snapshot LOCKED)
  </read_first>
  <behavior>
    - Test 1 (pur, offline) : une fonction pure d'assemblage `buildTechnicalSnapshot(candlesByTf, style, volumeSource)` (extraite et exportée pour testabilité, pattern D-23) produit un objet qui passe TechnicalSnapshotSchema.parse sans erreur, avec trend_htf/trend_ltf/momentum/volatility/key_levels/structure/volume_state remplis.
    - Test 2 (hash) : deux appels d'assemblage sur la MÊME fixture → snapshotContentHash identique.
    - Test 3 (gap EMA200) : avec une fixture de < 200 bougies, le snapshot est marqué partial:true et liste missing inclut 'ema200' (jamais zéro silencieux — RESEARCH Pitfall 2, décision planning : snapshot partiel flaggé).
    - Test 4 (POC source) : pour des candles crypto, key_levels poc porte volume_source 'real' ; pour candles à volume null (FX), 'proxy'.
  </behavior>
  <action>
    D'ABORD créer un stub minimal de apps/jobs/src/jobs/technical-engine.ts exportant la fonction pure `buildTechnicalSnapshot(candlesByTf, style, volumeSource)` dont le corps fait uniquement `throw new Error('NotImplemented')` (signature + export uniquement, aucune logique). But du stub : l'import du test se résout au chargement du module — le RED vient de l'échec d'assertion sur la forme du snapshot, PAS d'un crash du module-loader (vrai TDD RED). Le harness technicalEngine() complet et la logique réelle de buildTechnicalSnapshot arrivent en Task 2.
    ENSUITE écrire apps/jobs/__tests__/technical-engine.test.ts AVANT l'implémentation réelle (RED). Charger une fixture OHLCV offline (réutiliser ou dériver de packages/indicators/src/__fixtures__/btcusdt-h4.json). Importer la fonction pure d'assemblage depuis technical-engine.ts (le stub). Épingler : parse Zod OK, égalité de hash sur deux appels, partial:true sur fixture courte, volume_source attendu. Aucun réseau, aucune écriture DB dans ces 4 tests purs.
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/technical-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/jobs/src/jobs/technical-engine.ts existe en stub : buildTechnicalSnapshot exporté, corps `throw new Error('NotImplemented')`
    - Le fichier de test existe et, AVANT l'implémentation réelle, échoue (RED) sur l'assertion de forme du snapshot — l'import du module se résout proprement (pas d'erreur "module not found" ni de crash au chargement)
    - Après Task 2 : `pnpm vitest run apps/jobs/__tests__/technical-engine.test.ts` exit 0
    - Les 4 comportements sont assertés (parse §3, hash stable, partial EMA200, volume_source real|proxy)
  </acceptance_criteria>
  <done>Stub buildTechnicalSnapshot + golden tests RED écrits (échec sur assertion, import résolu) couvrant forme §3, déterminisme du hash, gap EMA200, flag volume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implémenter le job technical-engine (read → compute → §3 → hash → upsert)</name>
  <files>apps/jobs/src/jobs/technical-engine.ts</files>
  <read_first>
    - apps/jobs/src/jobs/macro-ingest.ts (analog EXACT du harness : dotenv, getServiceClient lazy, stats, per-item try/catch, WR-04 throw)
    - packages/supabase/src/repositories/candles.ts (getLastCandleTs / lecture par instrument+timeframe)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 311-328 (corps engine technique)
    - packages/core/src/index.ts (lastClosedCandleStart, TIMEFRAMES — consommer, jamais recalculer)
    - ARCHITECTURE.md §3 lignes 82-90 (forme §3)
  </read_first>
  <behavior>
    - Pour chaque instrument actif × style (day, swing), lire les bougies des deux TF du style (D-36), exclure la bougie en cours via lastClosedCandleStart (@app/core, D-10).
    - Assembler le technical_snapshot via la fonction pure `buildTechnicalSnapshot` (exportée), Zod-valider contre §3, calculer le hash, upsert avec kind='technical', computed_for_ts = dernière bougie LTF clôturée.
    - Instrument sans bougies → stats.skipped++, continue (isolation Pitfall 5). Instrument < 200 bougies → snapshot partial:true. Erreur → stats.errors push {instrument, msg normalisé} (T-02-13).
    - volume_source = 'real' si broker=binance (vrai volume), 'proxy' si oanda (tick) — déterminé par instrument metadata.
  </behavior>
  <action>
    Remplacer le stub de apps/jobs/src/jobs/technical-engine.ts par l'implémentation réelle, copiant verbatim le squelette macro-ingest.ts (import 'dotenv/config' en premier, getServiceClient lazy validant SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, `export async function technicalEngine(): Promise<Json>`, stats accumulator, boucle per-instrument try/catch, WR-04 throw si 0 produit + erreurs). Implémenter la fonction pure `buildTechnicalSnapshot(candlesByTf, style, volumeSource): TechnicalSnapshot` (déjà exportée en stub Task 1, séparation logique/IO pour testabilité, pattern D-23) qui : map candles→OHLCV (ohlcv.ts), calcule rsi/macd/ema20-50-200/atr/bollinger via @app/indicators, dérive trend_htf/trend_ltf (structure EMA200 + BOS/CHoCH), momentum, volatility (atr + percentile), key_levels (clusterLevels + computePoc avec volume_source), structure (last_swing_high/low + bos_choch), volume_state. Gérer le gap EMA200 → partial:true + missing:['ema200']. Importer upsertSnapshot/Json/Database depuis @app/supabase, les fns depuis @app/indicators, lastClosedCandleStart/TIMEFRAMES depuis @app/core. Ne JAMAIS importer service-client depuis le barrel (D-07).
  </action>
  <verify>
    <automated>pnpm vitest run apps/jobs/__tests__/technical-engine.test.ts && pnpm tsc -p apps/jobs/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/jobs/__tests__/technical-engine.test.ts` exit 0 (GREEN)
    - technicalEngine exporté + buildTechnicalSnapshot exporté (pure, testable, stub remplacé par la vraie logique)
    - Snapshot upserté avec kind='technical', onConflict via upsertSnapshot, computed_for_ts = dernière bougie LTF clôturée
    - Instrument vide → skipped ; < 200 bougies → partial:true + missing inclut ema200 ; jamais de crash global sur un instrument
    - Aucune logique temps recalculée (import @app/core), aucun import service-client
    - typecheck apps/jobs clean
  </acceptance_criteria>
  <done>Job technical-engine produit des technical_snapshots §3 déterministes sur données réelles, isolé par instrument.</done>
</task>

<task type="auto">
  <name>Task 3: Enregistrer technical-engine dans le dispatcher</name>
  <files>apps/jobs/src/dispatch.ts</files>
  <read_first>
    - apps/jobs/src/dispatch.ts (JOB_REGISTRY lignes 30-36, import lignes 19-23)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 332-347 (modif dispatch)
  </read_first>
  <action>
    Modifier apps/jobs/src/dispatch.ts : importer technicalEngine depuis './jobs/technical-engine', ajouter `'technical-engine': technicalEngine` au JOB_REGISTRY. Aucune autre modification (runJob enveloppe automatiquement → écrit job_runs).
  </action>
  <verify>
    <automated>grep -q "'technical-engine': technicalEngine" apps/jobs/src/dispatch.ts && pnpm tsc -p apps/jobs/tsconfig.json --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dispatch.ts importe technicalEngine et l'enregistre sous la clé kebab 'technical-engine'
    - `tsx src/dispatch.ts technical-engine` est invocable (clé présente dans JOB_REGISTRY)
    - typecheck apps/jobs clean
  </acceptance_criteria>
  <done>technical-engine appelable par le scheduler ; chaque run écrit une entrée job_runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job (service_role) → snapshots | Écriture via service_role bypass RLS ; clé en apps/jobs/.env gitignored |
| candles DB (trusted) → engine | Données Phase 2 ; revalidation de forme à la frontière (Zod §3 avant persist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information disclosure | service_role exposé via apps/web ou NEXT_PUBLIC_ | mitigate | service_role uniquement dans apps/jobs/.env (gitignored) ; jamais importé depuis le barrel (D-07) — hérité Phase 2 |
| T-03-10 | Input validation | dérive de forme du snapshot avant persist | mitigate | TechnicalSnapshotSchema.parse (§3) AVANT upsertSnapshot ; fail fast sur dérive |
| T-03-11 | Information disclosure | clé/secret dans job_runs.stats | mitigate | stats.errors = message normalisé only, jamais de valeur de clé (T-02-13, réutilisé) |
| T-03-12 | Availability | un instrument sans données crash le job entier | mitigate | per-instrument try/catch + stats.skipped (Pitfall 5) ; WR-04 throw seulement si 0 produit total |
</threat_model>

<verification>
- `pnpm vitest run apps/jobs/__tests__/technical-engine.test.ts` vert.
- `pnpm vitest run` (suite complète) vert au merge de wave.
- typecheck apps/jobs clean.
- technical-engine présent dans JOB_REGISTRY.
</verification>

<success_criteria>
- Snapshots technique §3 déterministes produits sur bougies crypto réelles et persistés.
- Gap EMA200 → partial flaggé ; instrument vide → skipped sans crash.
- Idempotence : re-run → même hash, aucun doublon.
- Job enregistré dans le dispatcher, écrit job_runs.
</success_criteria>

<output>
Create `.planning/phases/03-moteur-d-analyse-d-terministe/03-03-SUMMARY.md` when done
</output>
