---
phase: 03-moteur-d-analyse-d-terministe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_snapshots.sql
  - packages/supabase/src/database.types.ts
  - packages/supabase/src/repositories/snapshots.ts
  - packages/supabase/src/repositories/assetDrivers.ts
  - packages/supabase/src/index.ts
  - packages/supabase/__tests__/snapshots-rls.test.ts
autonomous: false
requirements: [TECH-04, FUND-01, FUND-02, FUND-03]
user_setup: []

must_haves:
  truths:
    - "La table snapshots existe en base avec RLS active (read=authenticated, no write policy)"
    - "La table asset_drivers existe, seedée, et est lisible par instrument"
    - "Un même (instrument_id, style, kind, computed_for_ts) ré-upserté ne crée pas de doublon"
    - "get_advisors retourne 0 erreur de sécurité après migration"
  artifacts:
    - path: "supabase/migrations/0005_snapshots.sql"
      provides: "Tables snapshots + asset_drivers + RLS + index uniques + seed drivers"
      contains: "create table public.snapshots"
    - path: "packages/supabase/src/repositories/snapshots.ts"
      provides: "upsertSnapshot idempotent + getSnapshotByHash"
      exports: ["upsertSnapshot", "getSnapshotByHash"]
    - path: "packages/supabase/src/repositories/assetDrivers.ts"
      provides: "getAssetDrivers(client, instrumentId)"
      exports: ["getAssetDrivers"]
  key_links:
    - from: "packages/supabase/src/repositories/snapshots.ts"
      to: "snapshots_uniq index"
      via: "onConflict 'instrument_id,style,kind,computed_for_ts'"
      pattern: "onConflict.*instrument_id,style,kind,computed_for_ts"
    - from: "packages/supabase/src/index.ts"
      to: "snapshots/assetDrivers repos"
      via: "barrel export"
      pattern: "export.*upsertSnapshot"
---

<objective>
Poser la frontière de persistance partagée par les trois moteurs de la Phase 3 : table `snapshots` (un snapshot daté par instrument × style × kind, hash de contenu = `raw_indicators_ref`, D-41) et table de données `asset_drivers` (drivers macro par actif, data-not-code extensible par UPDATE SQL, D-38). RLS dès la création, index uniques = clés d'upsert idempotent, types régénérés, repositories typés.

Purpose: Tous les moteurs (technical/fundamental/news des plans 03/04) écrivent leurs snapshots ici ; la Phase 4 les référence par hash. C'est la fondation DB partagée — légitimement horizontale car écrite par trois slices verticaux en aval.
Output: migration 0005 appliquée en base, types régénérés, repos `snapshots` + `assetDrivers`, test RLS/idempotence vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-CONTEXT.md
@.planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md

<interfaces>
<!-- Repository pattern (copy verbatim, change table + onConflict). From candles.ts -->
ServiceClient = SupabaseClient<Database>
upsertCandles(client, rows): onConflict 'instrument_id,timeframe,ts', ignoreDuplicates:false, throw on error
getLastCandleTs: .select().eq().eq().order().limit(1).maybeSingle(), return data?.x ?? null

<!-- Migration RLS conventions, from 0003_data_ingestion_tables.sql -->
alter table ... enable row level security;
create policy "<table>: lecture authentifiés" on public.<table> for select to authenticated using (true);
-- NO insert/update/delete policy → write = service_role bypass (D-05)
create unique index <table>_uniq on public.<table> (...);   -- = upsert key
create index <table>_read_idx on public.<table> (... desc);

<!-- snapshots + asset_drivers SQL — see 03-PATTERNS.md lines 218-256 for the exact DDL to copy -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration 0005_snapshots.sql (tables + RLS + index + seed drivers)</name>
  <read_first>
    - supabase/migrations/0003_data_ingestion_tables.sql (THE analog — table candles lignes 70-103, seed instruments lignes 38-67, policy style lignes 89-93)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 207-256 (DDL exact snapshots + asset_drivers à copier)
    - ARCHITECTURE.md §4 (lignes 168-181 — conventions snake_case / PK uuid / timestamptz UTC / RLS lecture authenticated écriture service_role)
  </read_first>
  <action>
    Créer supabase/migrations/0005_snapshots.sql. Table public.snapshots : id uuid PK default gen_random_uuid(), instrument_id uuid not null references public.instruments(id) on delete cascade, style text not null check (style in ('day','swing')), timeframe_set text not null (ex 'H4/H1' per D-36), kind text not null check (kind in ('technical','fundamental','news')), computed_for_ts timestamptz not null (dernière bougie LTF clôturée), content_hash text not null (= raw_indicators_ref, D-41), payload jsonb not null (forme §3 Zod-validée avant insert), partial boolean not null default false (gestion gap EMA200), created_at timestamptz not null default now(). Activer RLS, UNE policy select to authenticated using(true), AUCUNE policy write (service_role bypass, D-05). Index unique `snapshots_uniq` sur (instrument_id, style, kind, computed_for_ts) = clé d'upsert. Index lecture `snapshots_read_idx` sur (instrument_id, style, kind, computed_for_ts desc).
    Table public.asset_drivers (D-38, data-not-code) : id uuid PK, instrument_id uuid not null references instruments(id) on delete cascade, driver_code text not null (valeurs 'DXY','REAL_YIELDS','RATE_DIFF','RISK_SENTIMENT'), direction int not null (+1/-1), weight numeric not null default 1. RLS + select policy authenticated, AUCUNE write policy. Index unique `asset_drivers_uniq` sur (instrument_id, driver_code). Seeder les drivers par actif via `insert ... on conflict (instrument_id, driver_code) do update set ...` (style 0003 lignes 56-67), en joignant sur instruments.symbol : or (XAU) ↔ DXY(-1)/REAL_YIELDS(-1) ; JPY ↔ RATE_DIFF ; crypto (BTC/ETH/SOL/BNB/XRP) ↔ RISK_SENTIMENT(+1)/DXY(-1). Header de fichier commenté (style 0003 lignes 1-6) citant D-38/D-41.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0005_snapshots.sql | grep -c "enable row level security" | grep -qx 2 && grep -q "snapshots_uniq" supabase/migrations/0005_snapshots.sql && grep -q "asset_drivers_uniq" supabase/migrations/0005_snapshots.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fichier contient `create table public.snapshots` et `create table public.asset_drivers`
    - Deux `enable row level security` (snapshots + asset_drivers), chacune avec exactement UNE policy `for select to authenticated using (true)` et AUCUNE policy insert/update/delete
    - `create unique index snapshots_uniq on public.snapshots (instrument_id, style, kind, computed_for_ts)` présent
    - `create unique index asset_drivers_uniq on public.asset_drivers (instrument_id, driver_code)` présent
    - Bloc seed asset_drivers avec `on conflict (instrument_id, driver_code) do update set` mappant or/JPY/crypto à leurs drivers
    - check constraints `style in ('day','swing')` et `kind in ('technical','fundamental','news')` présents
  </acceptance_criteria>
  <done>Migration SQL écrite, conventions §4 respectées, seed drivers inclus.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Appliquer la migration en base cloud + régénérer types + gate sécurité</name>
  <read_first>
    - supabase/migrations/0005_snapshots.sql (la migration du Task 1)
    - .planning/STATE.md D-17 (convention projet : migrations via MCP apply_migration, project csotpitrjxryjkadyiml, types via generate_typescript_types, gate via get_advisors)
  </read_first>
  <what-built>
    Migration 0005 écrite. Elle doit être appliquée à la base cloud Supabase (project_ref csotpitrjxryjkadyiml) via le MCP Supabase `apply_migration`, PAS via `supabase db push` local (convention projet D-17, Phase 2). Sans ça, le build/tsc passe en faux-positif (les types viennent du fichier généré, pas de la base réelle) — la phase ne peut PAS passer la vérification sans migration appliquée.
  </what-built>
  <how-to-verify>
    1. Appliquer la migration : MCP Supabase `apply_migration` avec le contenu de supabase/migrations/0005_snapshots.sql sur project_ref csotpitrjxryjkadyiml.
    2. Régénérer les types : MCP `generate_typescript_types` → écraser packages/supabase/src/database.types.ts. Ajouter ensuite les exports de type `SnapshotRow/SnapshotInsert`, `AssetDriverRow/AssetDriverInsert` (pattern database.types.ts lignes 436-437, mêmes Row/Insert que CandleRow).
    3. Gate sécurité : MCP `get_advisors` (type security) → attendre 0 erreur de sécurité (miroir Phase 2 / migration 0003).
    4. Confirmer que les tables snapshots + asset_drivers apparaissent dans `list_tables` avec RLS enabled et le seed asset_drivers présent.
  </how-to-verify>
  <acceptance_criteria>
    - `list_tables` montre `snapshots` et `asset_drivers` avec rls_enabled=true
    - `get_advisors(security)` retourne 0 erreur
    - packages/supabase/src/database.types.ts contient les types des deux nouvelles tables + exports SnapshotRow/SnapshotInsert/AssetDriverRow/AssetDriverInsert
    - asset_drivers contient les lignes seedées (au moins or + crypto)
  </acceptance_criteria>
  <resume-signal>Tape "approved" une fois la migration appliquée, types régénérés et get_advisors à 0 erreur — ou décris l'erreur.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Repositories snapshots + assetDrivers + barrel + test RLS/idempotence</name>
  <read_first>
    - packages/supabase/src/repositories/candles.ts (analog exact upsert + lookup — lignes 11-55)
    - packages/supabase/src/repositories/macroSeries.ts (analog upsert simple + read accessor)
    - packages/supabase/src/index.ts (barrel à modifier — bloc export type lignes 18-52)
    - packages/supabase/__tests__/idempotency.test.ts (analog test : env-loading lignes 22-33, upsert×2 count inchangé + cleanup WR-06 lignes 106-166)
    - .planning/phases/03-moteur-d-analyse-d-terministe/03-PATTERNS.md lignes 260-307 (code repos à copier)
  </read_first>
  <action>
    Créer packages/supabase/src/repositories/snapshots.ts : `upsertSnapshot(client: ServiceClient, row: SnapshotInsert): Promise<void>` copiant le pattern candles.ts (upsert onConflict 'instrument_id,style,kind,computed_for_ts', ignoreDuplicates:false, throw on error) ; `getSnapshotByHash(client, hash: string)` via .select('*').eq('content_hash', hash).maybeSingle() retournant data ?? null (D-41, référencé par la Phase 4). Header documentant la clé d'idempotence + "JAMAIS importé depuis apps/web".
    Créer packages/supabase/src/repositories/assetDrivers.ts : `getAssetDrivers(client: ServiceClient, instrumentId: string)` via .select('*').eq('instrument_id', instrumentId) (consommé en lecture par le fundamental-engine). Même alias ServiceClient, même error-wrap.
    Modifier packages/supabase/src/index.ts : ajouter `export type { SnapshotRow, SnapshotInsert, AssetDriverRow, AssetDriverInsert }` au bloc de types, et `export { upsertSnapshot, getSnapshotByHash } from './repositories/snapshots'` + `export { getAssetDrivers } from './repositories/assetDrivers'`. Ne JAMAIS exporter service-client (D-07).
    Créer packages/supabase/__tests__/snapshots-rls.test.ts copiant le harness idempotency.test.ts : charger .env via process.loadEnvFile, upsert d'un snapshot factice deux fois → vérifier que le count reste identique (idempotence sur snapshots_uniq), cleanup en fin de test (WR-06).
  </action>
  <verify>
    <automated>pnpm vitest run packages/supabase</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/supabase` exit 0
    - snapshots.ts exporte `upsertSnapshot` et `getSnapshotByHash` ; onConflict = exactement 'instrument_id,style,kind,computed_for_ts'
    - assetDrivers.ts exporte `getAssetDrivers`
    - index.ts ré-exporte les 3 fonctions + les 4 types ; aucune ré-export de service-client
    - Le test snapshots-rls : deux upserts du même snapshot → count table inchangé (idempotence prouvée), nettoie ses lignes en fin
    - `pnpm tsc` (ou typecheck workspace supabase) clean
  </acceptance_criteria>
  <done>Repos typés + barrel à jour + idempotence snapshots prouvée par test vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs (service_role) → Postgres | Les moteurs écrivent snapshots via service_role (bypass RLS) |
| apps/web (anon/authenticated) → Postgres | Lecture seule des snapshots/asset_drivers, jamais d'écriture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | nouvelle table snapshots sans RLS | mitigate | `enable row level security` + UNE policy select-only dès 0005 ; AUCUNE write policy → écriture réservée service_role (D-05). Vérifié par get_advisors=0 (Task 2) |
| T-03-02 | Elevation of Privilege | nouvelle table asset_drivers sans RLS | mitigate | Idem : RLS + select-only policy dans 0005 |
| T-03-03 | Information disclosure | service_role exposé à apps/web via barrel | mitigate | index.ts n'exporte jamais service-client (D-07, garde ESLint existante) — hérité Phase 1/2 |
| T-03-04 | Tampering | content_hash forgeable / dérivé d'un secret | accept | Hash = sha256 de payload canonique (plan 02), pas dérivé d'un secret ; intégrité/idempotence seulement, pas un contrôle d'accès |
| T-03-SC | Tampering | npm installs | mitigate | Aucun nouveau package dans ce plan ; audit légitimité dans 03-RESEARCH.md (technicalindicators LOCKED, traité plan 02) |
</threat_model>

<verification>
- Migration 0005 appliquée en base, get_advisors(security)=0 (Task 2 checkpoint).
- `pnpm vitest run packages/supabase` vert (idempotence snapshots).
- Types régénérés contiennent snapshots + asset_drivers.
</verification>

<success_criteria>
- Tables snapshots + asset_drivers existent en base avec RLS active, seed drivers présent.
- Repos upsertSnapshot/getSnapshotByHash/getAssetDrivers typés et exportés du barrel.
- Idempotence snapshots prouvée par test golden DB.
</success_criteria>

<output>
Create `.planning/phases/03-moteur-d-analyse-d-terministe/03-01-SUMMARY.md` when done
</output>
