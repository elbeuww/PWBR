---
phase: quick-260617-sy5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/telegram/format.ts
  - packages/core/src/telegram/format.test.ts
autonomous: true
requirements: [LEGAL-01, D-06-REVISED, D-03, TG-02, D-11, T-06-INJ, T-06-BIDI]
must_haves:
  truths:
    - "formatMessage n'émet plus aucun disclaimer par message (ni FR ni AR)"
    - "Chaque message conserve titre + lignes de trades + ligne taux de réussite"
    - "La ligne taux de réussite reste présente pour N<30 et N>=30"
    - "LEGAL-01 reste satisfait via la DESCRIPTION du canal (hors-code)"
    - "Aucun affaiblissement des garde-fous bidi/seuil/escapeHtml/D-03/cap"
  artifacts:
    - path: "packages/core/src/telegram/format.ts"
      provides: "formatMessage pur sans disclaimer par message"
      contains: "winRateLineFr"
    - path: "packages/core/src/telegram/format.test.ts"
      provides: "golden tests vérifiant l'ABSENCE du disclaimer + présence du taux"
  key_links:
    - from: "format.ts"
      to: "applyThreshold"
      via: "winRateLineFr/winRateLineAr inchangés"
      pattern: "applyThreshold"
---

<objective>
Retirer le disclaimer LEGAL-01 (FR+AR) des messages Telegram produits par `formatMessage`.
Décision fondateur : le disclaimer vit désormais dans la DESCRIPTION du canal Telegram
(persistante, en tête de canal), plus répété par message. RÉVISE D-06 (Phase 6).

Purpose : alléger chaque post ; LEGAL-01 reste satisfait par la description du canal.
Output : `format.ts` sans constantes ni push de disclaimer ; `format.test.ts` inversé.
Périmètre STRICT : 2 fichiers, rien d'autre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contrats à PRÉSERVER intacts. Extraits de format.ts. -->

format.ts — à CONSERVER tels quels :
- `escapeHtml(s: string): string`  (T-06-INJ)
- `LRI` U+2066, `PDI` U+2069, `RLM` U+200F, `SEP`, `MAX_DETAILED`  (T-06-BIDI / Pitfall 3)
- `winRateLineFr(row)` / `winRateLineAr(row)` via `applyThreshold` (TG-02 / D-11) — LA LIGNE TAUX RESTE
- `interface FormatTrade { symbol; direction; outcome; realized_r }`  (D-03 : pas de entry/SL/TP)
- `pickTop`, `formatR`, `outcomeFr`, `outcomeAr`, titres FR/AR, jour vide D-10

format.ts — à SUPPRIMER :
- const `DISCLAIMER_FR` (~l.37-38) et `DISCLAIMER_AR` (~l.39-40)
- `frLines.push(DISCLAIMER_FR)` (~l.169)
- `arLines.push(`${RLM}${DISCLAIMER_AR}`)` (~l.197)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retirer le disclaimer de format.ts et inverser les golden tests</name>
  <files>packages/core/src/telegram/format.ts, packages/core/src/telegram/format.test.ts</files>
  <behavior>
    Tests (format.test.ts) qui doivent passer APRÈS modif :
    - Test disclaimer INVERSÉ : sortie `.not.toContain('Contenu éducatif')`,
      `.not.toContain('conseil en investissement')`, `.not.toContain('تعليمي')`,
      `.not.toContain('استثمارية')`.
    - Test taux PRÉSENT N>=30 : sortie `.toContain('58%')` ET `.toContain('N=142')`.
    - Test taux PRÉSENT N<30 : sortie `.toContain('échantillon insuffisant')` ET
      `.toContain('N=12')` ET `.not.toContain('%')`.
    - Tests existants INCHANGÉS et toujours verts : bidi (LRI/PDI/RLM présents),
      D-03/D-05 (EUR/USD, +2.3R, TP1 ; bannis entry/stop_loss/take_profit/SL =/TP =),
      jour vide D-10 (« Aucun trade » + « لا توجد » + 58%), escapeHtml/anti-injection,
      cap 4096 (50 trades < 4096 + « autres »).
  </behavior>
  <action>
    Dans `format.ts` : supprimer les constantes `DISCLAIMER_FR` et `DISCLAIMER_AR`.
    Supprimer la ligne `frLines.push(DISCLAIMER_FR)` (le dernier élément FR devient
    `winRateLineFr(winRate)`). Supprimer `arLines.push(`${RLM}${DISCLAIMER_AR}`)`
    (le dernier élément AR devient `winRateLineAr(winRate)`). NE PAS toucher au
    `return [...frLines, '', SEP, '', ...arLines].join('\n')`.
    Mettre à jour le commentaire d'en-tête : remplacer la puce
    « LEGAL-01 : chaque sortie contient le disclaimer FR ET AR (copy P2 identique). »
    par une note : LEGAL-01 est désormais porté par la DESCRIPTION du canal Telegram
    (décision révisée, ex-D-06) ; formatMessage n'émet plus de disclaimer par message.
    Conserver la fonction PURE (zéro I/O), TS strict, aucun console.log, immutabilité.

    Dans `format.test.ts` : dans le bloc `describe('formatMessage — disclaimer LEGAL-01')`,
    inverser les 4 assertions en `.not.toContain(...)` et renommer le `describe`/`it` pour
    refléter l'ABSENCE du disclaimer par message (ex-D-06 révisé). Mettre à jour le
    commentaire d'en-tête du fichier (LEGAL-01 = description canal, plus par message).
    Ajouter (ou confirmer) un test explicite « ligne taux de réussite TOUJOURS présente »
    couvrant N>=30 et N<30 si non déjà couvert par le bloc seuil existant.
    NE PAS affaiblir les autres assertions (bidi, seuil, D-03, escapeHtml, cap 4096).
  </action>
  <verify>
    <automated>cd "packages/core" && pnpm --filter @app/core test -- format && grep -v '^[[:space:]]*[/*]' src/telegram/format.ts | grep -c -e 'Contenu éducatif' -e 'محتوى تعليمي' -e 'DISCLAIMER_'</automated>
  </verify>
  <done>
    - `pnpm --filter @app/core test -- format` vert (suite format).
    - grep filtré (hors commentaires) retourne 0 occurrence de disclaimer/constante dans format.ts.
    - Ligne taux de réussite présente pour N<30 et N>=30 (tests verts).
    - Autres garde-fous (bidi/seuil/D-03/escapeHtml/cap) toujours verts.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @app/core test -- format` — suite format verte (disclaimer ABSENT, taux PRÉSENT).
2. `pnpm typecheck` — exit 0 (TS strict, jamais ignoreBuildErrors).
3. `grep -v '^[[:space:]]*[/*]' packages/core/src/telegram/format.ts | grep -c -e 'Contenu éducatif' -e 'محتوى تعليمي' -e 'DISCLAIMER_'` → `0`.
4. Aucun autre fichier modifié (PAS apps/jobs/src/jobs/telegram-publish.ts, PAS la description canal).
</verification>

<success_criteria>
- format.ts n'émet plus de disclaimer par message ; constantes DISCLAIMER_FR/AR supprimées.
- Ligne taux de réussite (winRateLineFr/winRateLineAr) conservée et testée (N<30 et N>=30).
- LEGAL-01 satisfait via description du canal (hors-code) ; D-06 noté comme révisé.
- `pnpm --filter @app/core test -- format` vert + `pnpm typecheck` exit 0 + grep = 0.
- SUMMARY note la révision de D-06 (disclaimer → description canal ; LEGAL-01 toujours satisfait).
</success_criteria>

<output>
Create `.planning/quick/260617-sy5-tg-no-disclaimer-msg/260617-sy5-SUMMARY.md` when done.
Le SUMMARY DOIT noter : D-06 révisée (disclaimer par message → description du canal Telegram),
LEGAL-01 toujours satisfait via la description du canal.
</output>
