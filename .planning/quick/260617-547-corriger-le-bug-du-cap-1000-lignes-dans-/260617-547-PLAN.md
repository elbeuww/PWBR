---
phase: quick-260617-547
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/jobs/src/jobs/technical-engine.ts
  - apps/jobs/src/jobs/__tests__/technical-engine.test.ts
autonomous: true
requirements: [BUGFIX-CAP1000]

must_haves:
  truths:
    - "Avec >1000 bougies en base, le snapshot a computed_for_ts = ts de la DERNIÈRE bougie clôturée (pas une vieille)"
    - "readClosedCandles récupère les N candles les plus récentes, ré-ordonnées en ascendant"
    - "Le test de régression échoue sur le code buggé (cap 1000 ascendant) et passe sur le fix"
    - "getCandlesForReplay et getLastCandleTs confirmés sûrs (bornés / desc limit 1)"
  artifacts:
    - path: "apps/jobs/src/jobs/technical-engine.ts"
      provides: "readClosedCandles bornée par CANDLE_WARMUP, ordre desc + reverse immutable"
      contains: "CANDLE_WARMUP"
    - path: "apps/jobs/src/jobs/__tests__/technical-engine.test.ts"
      provides: "Test régression cap 1000 — mock simule plafond PostgREST"
      contains: "computed_for_ts"
  key_links:
    - from: "apps/jobs/src/jobs/technical-engine.ts"
      to: "candles table"
      via: ".order('ts', { ascending: false }).limit(CANDLE_WARMUP)"
      pattern: "ascending: false.*limit\\(CANDLE_WARMUP\\)"
---

<objective>
Corriger le bug du cap 1000 lignes dans `readClosedCandles` (technical-engine).

Bug : la requête candles trie ascendant SANS `.limit()`. PostgREST plafonne à 1000 → renvoie les 1000 plus VIEILLES bougies → `ltf.at(-1)` (computed_for_ts) tombe sur une bougie périmée (ex. 23 janvier au lieu du 17 juin). Tout l'aval (analyses/setups/win rate) gelé sur données mortes.

Fix : récupérer les N plus RÉCENTES (`order desc` + `limit CANDLE_WARMUP`), puis ré-ordonner ascendant côté JS (immutable). WARMUP couvre EMA200 + ATR14 confortablement.

Purpose: Réactiver le pipeline d'analyse — computed_for_ts redevient la dernière bougie clôturée.
Output: readClosedCandles bornée + test de non-régression Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Contrats clés extraits du codebase. Pas d'exploration nécessaire. -->

readClosedCandles (technical-engine.ts:181-199) — code buggé actuel :
```
const { data, error } = await client
  .from('candles')
  .select('*')
  .eq('instrument_id', instrumentId)
  .eq('timeframe', timeframe)
  .lte('ts', cutoff.toISO())
  .order('ts', { ascending: true })   // ← cap 1000 PostgREST = 1000 plus VIEILLES
return data ?? []
```

Consommation (technical-engine.ts:247) : `const computedForTs = ltf.at(-1)!.ts`
buildTechnicalSnapshot attend candlesByTf en ordre ts ASCENDANT (doc ligne 96).

Constantes existantes : EMA_LONG = 200 (ligne 57). Le warmup doit dépasser 200 + marge ATR14.

Repository candles.ts — DÉJÀ SÛRS (ne pas toucher) :
- getLastCandleTs : `.order('ts', { ascending: false }).limit(1).maybeSingle()` → 1 ligne. SÛR.
- getCandlesForReplay : `.gte('ts', from).lte('ts', to)` borné fenêtre [created_at, valid_until] (< 1000 H1 sur durée setup). SÛR.

Pattern mock test (outcome-tracker.test.ts) : store en mémoire + builder chaînable thenable, `vi.mock('@supabase/supabase-js', () => ({ createClient: () => makeClient() }))`. order() termine la chaîne en Promise. Le mock applique tri dans resolveSelect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test de régression rouge — cap 1000 dans readClosedCandles</name>
  <files>apps/jobs/src/jobs/__tests__/technical-engine.test.ts</files>
  <behavior>
    - Store mémoire : instruments[] (1 instrument actif binance) + candles[].
    - Seed > 1000 bougies H1 ET > 1000 bougies H4 pour l'instrument, ts croissants espacés réguliers, la PLUS RÉCENTE <= cutoff (clôturée). Suffisamment pour EMA200.
    - Mock builder chaînable qui SIMULE LE CAP 1000 PostgREST : dans resolveSelect, appliquer .eq/.lte/.order ; si une limite explicite a été posée via .limit(n), tronquer à n APRÈS tri ; SINON tronquer à 1000 APRÈS tri (reproduit le plafond implicite). C'est ce qui capture le bug.
    - Mocker upsertSnapshot + listActiveInstruments (@app/supabase) pour capturer la row upsertée (computed_for_ts).
    - Test clé : après technicalEngine(), la row technical day/swing a computed_for_ts === ts de la DERNIÈRE bougie clôturée du LTF seedée (la plus récente <= cutoff), PAS la 1000ᵉ plus ancienne.
    - Sur le code buggé (order asc sans limit), le mock renvoie les 1000 plus vieilles → computed_for_ts = vieille → test ROUGE. C'est l'assertion de non-régression.
  </behavior>
  <action>Créer le test en suivant le pattern de outcome-tracker.test.ts (store mémoire, builder thenable, vi.mock '@supabase/supabase-js'). Le mock candles DOIT reproduire le cap 1000 : après application du tri demandé, si aucun .limit() n'a été appelé sur la chaîne, tronquer le résultat à 1000 lignes ; si .limit(n) appelé, tronquer à n. Stocker listActiveInstruments et upsertSnapshot via vi.mock('@app/supabase') — capturer la SnapshotInsert dans une variable de test. Seeder assez de bougies (>1000 par TF, EMA200 satisfait) avec un ts max clôturé connu. Assert : computed_for_ts capturé === ts max clôturé seedé. NE PAS encore corriger technical-engine — ce test doit échouer maintenant (RED).</action>
  <verify>
    <automated>cd apps/jobs && pnpm vitest run src/jobs/__tests__/technical-engine.test.ts 2>&1 | grep -v '^#' | grep -q -i 'fail\|✗\|×' && echo RED-OK</automated>
  </verify>
  <done>Test écrit, échoue sur le code actuel (computed_for_ts = vieille bougie à cause du cap 1000 simulé). RED confirmé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix readClosedCandles — borne CANDLE_WARMUP + reverse immutable (GREEN)</name>
  <files>apps/jobs/src/jobs/technical-engine.ts</files>
  <behavior>
    - readClosedCandles récupère les CANDLE_WARMUP bougies les plus récentes (order desc + limit), puis retourne un NOUVEAU tableau ascendant (pas de mutation in-place).
    - computed_for_ts = ltf.at(-1).ts redevient la dernière bougie clôturée.
    - Le test de Task 1 passe (GREEN).
  </behavior>
  <action>Ajouter une constante nommée commentée près de EMA_LONG : `const CANDLE_WARMUP = 500` (commentaire : couvre EMA200 + marge ATR14/percentile confortablement, sous le cap 1000 PostgREST). Dans readClosedCandles, remplacer `.order('ts', { ascending: true })` par `.order('ts', { ascending: false }).limit(CANDLE_WARMUP)`. Retourner l'ordre ascendant SANS muter : `return (data ?? []).slice().reverse()` (slice() crée la copie — immutabilité CLAUDE.md, pas de .reverse() sur data). Mettre à jour le JSDoc de readClosedCandles pour refléter "N plus récentes, ré-ordonnées ascendant". Ne toucher à rien d'autre (surgical).</action>
  <verify>
    <automated>cd apps/jobs && pnpm vitest run src/jobs/__tests__/technical-engine.test.ts && pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Test Task 1 passe (computed_for_ts = dernière clôturée). tsc strict clean. readClosedCandles borné par CANDLE_WARMUP, reverse sur copie (immutable).</done>
</task>

<task type="auto">
  <name>Task 3: Audit candles.ts — documenter sûreté (surgical, pas de code mort touché)</name>
  <files>packages/supabase/src/repositories/candles.ts</files>
  <behavior>
    - getLastCandleTs : desc + limit 1 → SÛR (1 ligne). getCandlesForReplay : borné fenêtre [from,to] → SÛR.
    - Aucun changement de logique — seulement un commentaire de sûreté si absent.
  </behavior>
  <action>Vérifier (déjà confirmé via lecture) que getLastCandleTs (order desc limit 1 maybeSingle) et getCandlesForReplay (gte/lte fenêtre bornée) ne dépassent jamais 1000 lignes. Ajouter une note brève dans le JSDoc de getCandlesForReplay : "fenêtre [from,to] bornée par valid_until → < cap 1000 (cf. bug fix readClosedCandles)". NE modifier AUCUNE logique de requête (code sain — surgical). Si après vérification l'un d'eux s'avérait non borné, le corriger sur le même modèle desc+limit ou window — sinon, documentation seule.</action>
  <verify>
    <automated>cd packages/supabase && pnpm tsc --noEmit 2>&1 | grep -v '^#' | grep -c -i error | grep -q '^0$' && echo NO-TS-ERRORS</automated>
  </verify>
  <done>Sûreté des deux fonctions confirmée et documentée. Aucune logique de requête modifiée. tsc clean.</done>
</task>

</tasks>

<verification>
- Suite jobs verte : `cd apps/jobs && pnpm vitest run`
- Le test technical-engine échoue sur HEAD~ (code buggé) et passe sur le fix — vraie non-régression (mock simule cap 1000).
- tsc strict clean sur apps/jobs et packages/supabase.
- readClosedCandles : grep confirme `ascending: false` + `limit(CANDLE_WARMUP)` + `.slice().reverse()`.
</verification>

<success_criteria>
- computed_for_ts = ts de la dernière bougie clôturée même avec >1000 bougies en base.
- CANDLE_WARMUP constante nommée et commentée, >= EMA200 + marge, < 1000.
- Aucune mutation in-place (reverse sur copie).
- candles.ts inchangé en logique, sûreté documentée.
- 1 test ciblé de régression vert.
</success_criteria>

<output>
Create `.planning/quick/260617-547-corriger-le-bug-du-cap-1000-lignes-dans-/260617-547-SUMMARY.md` when done
</output>
