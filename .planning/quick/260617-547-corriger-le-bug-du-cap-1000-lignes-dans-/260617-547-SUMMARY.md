---
phase: quick-260617-547
plan: 01
subsystem: technical-engine
tags: [bugfix, postgrest, candles, track-record]
requires: []
provides: [readClosedCandles bornée CANDLE_WARMUP, test régression cap 1000]
affects: [apps/jobs/src/jobs/technical-engine.ts, packages/supabase/src/repositories/candles.ts]
tech-stack:
  added: []
  patterns: [order desc + limit pour borner PostgREST, reverse immutable sur copie]
key-files:
  created:
    - apps/jobs/src/jobs/__tests__/technical-engine.test.ts
  modified:
    - apps/jobs/src/jobs/technical-engine.ts
    - packages/supabase/src/repositories/candles.ts
decisions:
  - "CANDLE_WARMUP=500 (>EMA200+marge ATR14, <1000 cap PostgREST)"
  - "Mock candles reproduit le cap 1000 implicite (troncature post-tri sans .limit)"
metrics:
  duration: ~12min
  completed: 2026-06-17
requirements: [BUGFIX-CAP1000]
---

# Quick 260617-547 : Corriger le bug du cap 1000 lignes dans readClosedCandles Summary

`readClosedCandles` charge désormais les N bougies les plus RÉCENTES (`order ts desc` + `.limit(CANDLE_WARMUP)` puis reverse immutable) au lieu de subir le plafond implicite 1000 lignes de PostgREST qui renvoyait les 1000 plus VIEILLES — réactivant le pipeline d'analyse (computed_for_ts redevient la dernière bougie clôturée).

## What Was Built

- **Test de régression** (`technical-engine.test.ts`) : store en mémoire, mock `@supabase/supabase-js` reproduisant le cap 1000 PostgREST (troncature à 1000 après tri quand aucun `.limit()` n'est posé, à n sinon), mock `@app/supabase` capturant les `SnapshotInsert`. Seed >1000 bougies H1/H4/D ; assert `computed_for_ts === ts max clôturé`.
- **Fix** (`technical-engine.ts`) : constante `CANDLE_WARMUP=500` près de `EMA_LONG` ; requête `order('ts', { ascending: false }).limit(CANDLE_WARMUP)` ; retour `(data ?? []).slice().reverse()` (copie immutable, CLAUDE.md). JSDoc mis à jour.
- **Audit** (`candles.ts`) : note de sûreté ajoutée au JSDoc de `getCandlesForReplay` (fenêtre [from,to] bornée par valid_until → <1000 H1). `getLastCandleTs` déjà sûr (desc + limit 1 maybeSingle). Aucune logique de requête modifiée.

## Tasks & Commits

| Task | Name | Commit |
| ---- | ---- | ------ |
| 1 | Test régression RED (cap 1000) | d7e596b |
| 2 | Fix readClosedCandles GREEN (CANDLE_WARMUP + reverse) | ced206c |
| 3 | Audit candles.ts (doc sûreté) | 821bdef |

## TDD Cycle

- **RED** (d7e596b) : sur le code buggé, `computed_for_ts = 2023-05-27T20:00:00Z` (1000ᵉ plus vieille H1) ≠ `2023-06-01T00:00:00Z` attendu → échec confirmé. Le mock capture bien le bug.
- **GREEN** (ced206c) : après fix, `computed_for_ts = 2023-06-01T00:00:00Z` (dernière clôturée) → passe.

## Verification

- `npx vitest run apps/jobs/src/jobs/__tests__/technical-engine.test.ts` : 1/1 vert (était rouge pré-fix).
- `npx vitest run apps/jobs` : **14 fichiers, 115 tests verts** (non-régression suite jobs).
- `tsc --noEmit` apps/jobs : **0 erreur dans technical-engine.ts et le test**.
- `tsc --noEmit` packages/supabase : **0 erreur dans candles.ts**.

## Deviations from Plan

None — plan exécuté tel qu'écrit (TDD RED → GREEN → audit).

## Deferred Issues (out of scope — pré-existants)

Erreurs tsc pré-existantes hors périmètre (Phase 04/06 WIP, non touchées par ce fix, tolérées au runtime par esbuild) :
- `apps/jobs/src/jobs/__tests__/telegram-publish.test.ts` (TS2352/TS2493, lignes 239/246)
- `apps/jobs/src/telegram/bot.ts:68` (TS2353 `disable_web_page_preview`)
- `packages/supabase/src/repositories/payments.ts` (TS2322, lignes 106/153/246)

## Self-Check: PASSED

- Fichiers : `apps/jobs/src/jobs/__tests__/technical-engine.test.ts` FOUND, `apps/jobs/src/jobs/technical-engine.ts` FOUND (CANDLE_WARMUP présent), `packages/supabase/src/repositories/candles.ts` FOUND.
- Commits : d7e596b FOUND, ced206c FOUND, 821bdef FOUND.
