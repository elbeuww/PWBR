# =============================================================================
# apps/jobs/.env — contrat de variables (COMMITÉ SANS VALEUR, D-12)
# Copier en apps/jobs/.env et renseigner les valeurs réelles.
# Ne JAMAIS commiter apps/jobs/.env avec des valeurs.
# =============================================================================

# --- Supabase (service_role — bypass RLS, JAMAIS exposer côté web) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Data Sources ---
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
BINANCE_API_KEY=
BINANCE_API_SECRET=
FINNHUB_API_KEY=
MARKETAUX_API_KEY=
FRED_API_KEY=

# --- Paiement USDT TRC-20 / TronGrid (lecture on-chain service_role) ---
TRONGRID_API_KEY=
USDT_RECEIVE_ADDRESS=
USDT_CONTRACT_ADDRESS=
TRON_NETWORK=

# --- Canal Telegram public (apps/jobs/.env UNIQUEMENT — jamais committé, jamais loggé) ---
# Token du bot Telegram (@BotFather → /newbot). SECRET — T-06-TOKEN : jamais en code/DB/logs.
TELEGRAM_BOT_TOKEN=
# ID numérique -100… du canal public (préféré au @username). Bot = ADMIN « Post Messages ».
TELEGRAM_CHANNEL_ID=
